# 单组件 Check List（Hyper-Structure 完整版）

> 用途：每次新增、重构或修改一个组件时，必须按本清单逐项检查并在 PR 中附带结果。  
> 执行顺序：先过第 1-2 节（架构与状态），再进入第 3-5 节（具体实现）。  
> 术语统一：`status-primitives` 指状态原语层（当前 crate 名为 `ui-state-primitives`）。

### 0. 适用范围与顺序纪律
本清单仅评估“一个组件”的改动结果，不替代仓库级治理。
先过第 1-2 节（架构与状态）再进入第 3-5 节（具体实现）。
不适用的条目必须明确标注 `N/A` 并说明理由，禁止机械打勾。
组件目标、非目标、风险边界已写清楚；发现跨组件/跨层系统性问题时升级为仓库级任务。

### 1. 架构边界与分层约束（Kernel/Shell 总线）
- [x] `status-primitives` 定义：纯状态原语层（受控/非受控、toggle、selection、list、overlay open state、expansion 等）。不依赖 Leptos/DOM/web-sys；只包含 Rust 数据结构和方法，不含视图与事件绑定。（`FlipCard` 状态原语已下沉到 `crates/ui-state-primitives/src/flip_card.rs` 并在 `crates/ui-state-primitives/src/lib.rs` 导出；组件侧 `components/flip-card/src/logic.rs` 仅消费原语并保留 class 组装与按键判断。回归：`crates/ui-state-primitives/src/test/flip_card.rs`、`components/flip-card/test/semantics.rs::flip_card_logic_exposes_state_and_source_helpers`。）
  - 所有状态原语必须从 `status-primitives`（`ui-state-primitives`）获取，组件层只能消费，不得自造。
  - 下沉判定依据是“稳定状态不变量”；凡属于状态机、归一化、状态派生能力，默认先进入 `ui-state-primitives`。
  - 组件中可保留的仅是装配逻辑：props 归一、样式来源标记、slot 组织、对 `ui-state-primitives` 输出的映射。
  - 组件内若出现状态原语实现（受控/非受控状态机、single/multiple 展开规则、索引归一化、跨事件状态派生），该项直接判不通过。
  - 处理方式固定：先下沉到 `ui-state-primitives/src/<capability>.rs`（如 `expansion.rs`），在 `ui-state-primitives/src/lib.rs` 导出，再回到组件改调用。
  - 下沉后的原语必须有 `ui-state-primitives` 单元测试；组件侧只保留调用与语义挂载测试。
  - 桥接规范：`ui-state-primitives` 结构体必须是 POJO（Plain Old Rust Object），不持有 Leptos `Signal` 或框架绑定状态容器。
  - 消费规范：`ui-headless` 或组件 `logic.rs` 负责解包 `Signal` 当前值传入 primitive 方法，并将结果显式写回 `Signal`。
  - 设计理由：保持 primitives 纯粹可测、可迁移，不与特定响应式库绑定（便于未来替换响应式实现与做纯 Rust 测试）。
- [x] `ui-headless` 定义：交互与 A11y 原语层（press/focus/hover/roving/listbox/menu/tooltip 等），把输入设备事件与状态语义标准化为可复用契约；输出必须是类型化 `attrs + handlers + state`。不做样式、不写组件 CSS、不做组件级动效编排。（`FlipCard` 交互与 A11y 语义已下沉到 `crates/ui-headless/src/flip_card.rs`：统一键盘模型 `resolve_flip_card_key_down`、指针/焦点归一和 `role/tabindex/aria-pressed/aria-disabled/lang/dir` 映射；组件 `components/flip-card/src/view.rs` 仅挂载 `use_flip_card` 输出契约，不再本地重写语义判断。回归：`crates/ui-headless/src/test/flip_card.rs`、`components/flip-card/test/semantics.rs::flip_card_view_uses_motion_and_state_contracts`。）
  **`ui-headless` 落位硬规则（必须执行）**：
  - 输入边界：消费 `status-primitives` 状态 + 用户输入事件（keyboard/pointer/focus）+ 环境能力（web/ssr）。
  - 输出边界：只输出语义契约（attrs/handlers/state）；组件层只负责挂载与组合，不得把语义判断塞回 `view.rs`。
  - 下沉判定依据是“交互/A11y 语义契约”；凡属于键盘/焦点/指针归一、ARIA 映射、交互状态语义能力，默认先进入 `ui-headless`。
  - 必须下沉：键盘模型、焦点模型、跨设备输入归一、ARIA 状态映射、overlay/presence 等交互语义。
  - A11y 契约与共享工具落点固定在 `crates/ui-headless/src/a11y.rs`；组件只在 `view.rs` 挂载，不在组件层重写。
  - 语义契约必须提供 `lang` / `dir`（LTR/RTL）接入能力；headless 不硬编码用户可见文本，文案由 i18n/l10n 层提供。
  - 语义契约正确性必须有回归：`crates/ui-components/tests/*` 断言语义标记，`e2e/tests/*` 覆盖关键交互流程。
  - 禁止放在 `ui-headless`：视觉 class 选择、CSS 规则、组件 slot 布局、组件专属动效编排、业务文案。
  - 允许留在组件层：纯视觉一次性交互且不形成可复用语义契约（例如单组件局部微交互）。
- [x] `ui-motion` 定义：动效能力与契约执行层（spring、keyframes、WAAPI/RAF backend），只负责时间函数、插值与运行时驱动，不承载组件业务语义与状态决策。（`FlipCard` 动效执行器已回迁到共享层：组件 `components/flip-card/src/motion.rs` 不再直接 `SpringAnimator::new`，改为调用 `ui_motion::spring::SpringAnimatorTriplet::new`（定义于 `crates/ui-motion/src/spring.rs`）作为通用执行后端；组件侧仅保留语义状态（flipped/hovered）到目标值映射与节点绑定。non-wasm 路径继续走 no-op `std::hint::black_box(sanitize_motion(motion))`，保证 SSR/tooling 可编译且行为可预测。回归：`crates/ui-motion/src/test/spring.rs::spring_animator_triplet_updates_all_channels`、`components/flip-card/test/semantics.rs::flip_card_motion_sanitizes_custom_contract_values`。）
  - 放在 `crates/ui-motion`：通用动画数学与执行后端（spring solver、keyframe sampling、easing registry、driver adapters），以及 `wasm/non-wasm` 适配与 `reduced-motion` 执行策略。
  - 放在 `crates/ui-components/src/<component>/motion.rs`：把组件语义状态（open/closed、enter/exit、active/inactive）映射为 `ui-motion` contract，绑定目标节点并调用 attach。
  - 禁止放在 `crates/ui-motion`：组件 slot 结构、组件专属状态机、ARIA/keyboard 语义、业务文案与业务分支。
  - 禁止放在组件 `motion.rs`：自实现 spring/keyframe/driver 执行器；跨组件共享动效算法必须回迁 `ui-motion`。
  - 动效参数优先来自 token/theme；禁止在组件样式与逻辑中散落硬编码时长/曲线/位移常量。
  - 非 wasm 路径必须提供 no-op/stub，保证 SSR/tooling 可编译且行为可预测。
- [x] `ui-theme` 定义：唯一设计 token 与主题上下文层（system/color/scale + Light/Dark/OLED），负责 token 分类、主题映射与 CSS 变量生成。（`FlipCard` 视觉常量已回迁到 `ui-theme`：`crates/ui-theme/src/tokens.rs` 新增 `FlipCardLayoutTokens` 与 `FLIP_CARD_LAYOUT_TOKENS_{MEDIUM,LARGE}`，`crates/ui-theme/src/theme.rs` 新增 `flip_card_layout_tokens/default_flip_card_layout_tokens`，`crates/ui-theme/src/css.rs` 输出 `--ui-flip-card-*` 变量；组件 `components/flip-card/src/styles.rs` 改为消费变量，不再硬编码 `21rem/92vw/1200px/0.6/650`。回归：`crates/ui-theme/tests/token_scale_baseline.rs::flip_card_layout_tokens_follow_theme_token_baseline`、`components/flip-card/test/semantics.rs::flip_card_theme_tokens_are_defined_mapped_and_documented`。）
  - Token 统一基线落点固定：`crates/ui-theme/src/tokens.rs` 定义，`crates/ui-theme/src/theme.rs` 映射，`crates/ui-theme/src/css.rs` 输出变量；组件只在 `crates/ui-components/src/<component>/styles.rs` 消费。
  - 三轴上下文（`system/color/scale`）在 `theme.rs` 定义；组件在 `logic.rs` 选择并在 `view.rs` 生效，`styles.rs` 只消费变量，不重建主题。
  - Token 分类必须可追溯：分类源在 `tokens.rs`，规范同步 `docs/spec/styling.md`；组件不得引入平行私有 token 命名体系。
  - 量化尺寸基准必须可回归：尺寸基准在 `tokens.rs` 与 `theme.rs` 定义，主题回归在 `crates/ui-theme/tests/token_scale_baseline.rs`，组件语义回归在 `crates/ui-components/tests/<component>_semantics.rs`。
  - 主题调色与语义色对比必须满足 `WCAG 2.1 AA` 基线，并覆盖 Light/Dark/OLED 主题变体。
  - 主题层只输出 `theme/tokens/base css` 与变量；不实现组件结构、交互逻辑、组件级动效编排。
  - 新增视觉语义先补 token，再由组件消费；禁止“组件临时值先落地、后补 token”的倒序流程。
- [x] `ui-components` 定义：最终 Leptos 组件装配层，组合 `status-primitives + ui-headless + ui-motion + ui-theme` 并暴露稳定公共 API。（`FlipCard` 文件职责已按 `mod/logic/view/styles/motion` 分离：`logic.rs` 只做 props/状态装配与 class 归一，`view.rs` 只渲染结构并挂载 `ui_headless::use_flip_card` 契约，`styles.rs` 仅消费 `ui-theme` token 变量，`motion.rs` 仅做语义到 `ui-motion` attach 映射；公共导出面仅 `FlipCard`、`FlipCardMotion` 与 state primitive 类型，不暴露 `web-sys`/DOM 类型。语义测试已从旧路径迁移到组件内新目录：`components/flip-card/test/semantics.rs`，并由 `components/flip-card/test/mod.rs` 注册。）
  - `logic.rs` 负责 props 归一与状态派生；`view.rs` 负责结构渲染与 headless 语义挂载；`styles.rs` 负责 token-first 静态样式；`motion.rs` 负责动效 attach。
  - 组件层不得重写 `status-primitives` 状态机或 `ui-headless` 交互契约；发现即判不通过并回迁到对应层。
  - 对外 API 禁止暴露 `web-sys`/DOM 细节类型；平台差异封装在内部模块。
  - 测试文件位于src同级的test/中，内部测试文件同名（如rust-ui/components/accordion/src/logic.rs与rust-ui/components/accordion/test/logic.rs）。
  - 还需要一个semantics.rs用于测试。可能存在类似rust-ui/crates/ui-components/tests/accordion_semantics.rs的旧版实现，需要迁移到新目录。

### 2. API 设计与状态内核（Logic/Kernel）
- [x] API 命名契约统一：公共 props/回调严格使用 `is_*`、`on_*`、`default_*` 前缀；同语义在全库同名，禁止别名漂移。（`FlipCard` 对外主命名已切换为 `default_is_flipped`、`is_disabled`、`is_flip_on_hover`：定义见 `components/flip-card/src/view.rs`，docs 示例见 `apps/docs-app/src/pages/components/pages/display_extra.rs`，API 表见 `components/flip-card/src/README.md`。兼容策略：保留旧别名 `default_flipped` / `disabled` / `flip_on_hover`，由 `view.rs` 统一归一并且“新命名优先于旧别名”；迁移路径：文档与示例全部改用新命名，旧别名仅兼容存量调用。回归：`components/flip-card/test/semantics.rs::flip_card_view_uses_motion_and_state_contracts` 与 docs 语义断言。）
  - 布尔状态统一 `is_*`（如 `is_open`/`is_disabled`），事件统一 `on_*`，默认值统一 `default_*`。
  - 同一语义 across 组件必须同名（如都用 `on_open_change`，禁止同义别名并存）。
  - 公共 API 引入新命名时，需说明与现有命名体系的兼容策略与迁移路径。
- [x] 受控/非受控必须成对：每个可控状态轴都提供 `value + on_value_change + default_value`（如 `open/on_open_change/default_open`）；缺一项即不通过。（`FlipCard` 翻转轴已补齐 `is_flipped + on_is_flipped_change + default_is_flipped`：`components/flip-card/src/view.rs` 使用 `ui_headless::use_controllable_state` 统一管理受控/非受控，受控时仅通过 `request_is_flipped_change` 请求外部更新，不写本地状态。已有旧命名兼容：`default_flipped`。状态来源与模式通过 `data-flipped-control-mode/data-flipped-prop-source/data-flipped-default-source/data-flipped-change-source` 暴露。回归：`components/flip-card/test/semantics.rs::flip_card_view_uses_motion_and_state_contracts`、`components/flip-card/test/semantics.rs::flip_card_readme_documents_controlled_uncontrolled_axis_pair`。）
  - 受控模式：外部值是单一事实来源，内部不得偷偷写回本地状态。
  - 非受控模式：仅由默认值初始化一次，后续状态由内部原语管理。
  - 受控/非受控切换语义需稳定可测，避免“半受控”隐式行为。
- [x] 默认值单一来源：默认值与优先级只在 `logic.rs` 归一化；`view.rs` 禁止二次兜底或隐式改写。（`FlipCard` 默认值归一已下沉到 `components/flip-card/src/logic.rs`：`normalize_flipped_axis` 统一处理 `default_is_flipped -> default_flipped -> DEFAULT_FLIPPED`，`normalize_behavior_flags` 统一处理 `is_disabled/disabled` 与 `is_flip_on_hover/flip_on_hover` 的优先级与默认值。`components/flip-card/src/view.rs` 仅调用上述归一函数并消费结果，不再出现 `DEFAULT_*` 的本地 `unwrap_or` 分支。回归：`components/flip-card/test/logic.rs::{normalize_flipped_axis_centralizes_default_priority_and_sources,normalize_behavior_flags_centralizes_bool_defaults_and_alias_priority}`、`components/flip-card/test/semantics.rs::flip_card_view_uses_motion_and_state_contracts`。）
  - 默认值优先级必须可读且可测试（显式规则而非分散 `unwrap_or`）。
  - `view.rs` 不允许再做默认值分支；仅消费 `logic.rs` 的归一化输出。
  - 一旦发现多处默认值来源，直接判不通过并回收至 `logic.rs`。
- [x] 状态归一化集中：状态输入先类型化，再在 `logic.rs` 统一派生；禁止在 `view.rs`、事件回调、样式分支中分散拼状态机。（`FlipCard` 新增 `components/flip-card/src/logic.rs::FlipCardDerivedRenderStateInput` 与 `derive_render_state`，把 `root/front/back` slot state 与 `data-*` 来源标记（含 `flipped/default/controlled/hovered/custom-*`）统一在逻辑层一次派生；`components/flip-card/src/view.rs` 仅消费 `derived_render_state` 输出并挂载 headless/motion，不再直接 `resolve_part_state` 或在视图内拼 `then_some` 状态规则。事件处理器仍仅触发 `ui_headless` handlers，不承载状态机判定；`components/flip-card/src/styles.rs` 继续只消费 `data-*`/class 标记。回归：`components/flip-card/test/logic.rs::derive_render_state_centralizes_slot_states_and_semantic_markers`、`components/flip-card/test/semantics.rs::flip_card_view_uses_motion_and_state_contracts`。）
  - 输入边界统一进入 `logic.rs`，输出统一为可渲染语义状态与来源标记。
  - 事件处理器只触发状态变更，不重建状态机规则。
  - 样式层只消费状态标记，不承担状态判定职责。
- [x] 离散状态必须类型约束：`variant/size/mode/status` 等离散输入使用 `enum`；禁止用多个 `Option<bool>`/字符串自由组合表达互斥状态。（`FlipCard` 的离散模式类型与归一能力已下沉到 `crates/ui-state-primitives/src/flip_card.rs`：`FlipCardFlipMode`（`Toggle | Hover`）+ `normalize_behavior_flags`，组件 `components/flip-card/src/logic.rs` 仅 re-export 并装配调用，`components/flip-card/src/view.rs` 统一用 `flip_mode.is_hover()` 驱动 headless 与派生状态。回归：`crates/ui-state-primitives/src/test/flip_card.rs::normalize_behavior_flags_keeps_mode_and_source_contracts`、`components/flip-card/test/logic.rs::normalize_behavior_flags_maps_bool_aliases_to_enum_mode`。）
  - 互斥状态优先用 `enum` 建模，利用编译器封住无效组合。
  - 字符串输入若需兼容外部配置，必须先映射到类型化枚举再进入逻辑层。
  - 布尔爆炸（多个 bool 表达一个状态机）应在设计评审阶段直接拦截。
- [x] 状态原语来源正确：组件层只消费 `status-primitives`（当前 `ui-state-primitives`）能力，不直接绑定业务 store；应用级全局状态必须经桥接层适配后再接入组件。（`FlipCard` 的可复用状态归一能力已固定在 primitive 层：`resolve_part_state`、`FlipCardFlipMode`、`normalize_behavior_flags` 均位于 `crates/ui-state-primitives/src/flip_card.rs`，组件 `components/flip-card/src/logic.rs` 仅做 props/source marker 装配并 re-export 原语，不再本地实现 behavior primitive。`components/flip-card/src/view.rs` 只消费 `logic` 输出与 `ui_headless` 契约，不依赖业务 store 类型。回归：`components/flip-card/test/semantics.rs::flip_card_logic_exposes_state_and_source_helpers`、`crates/ui-state-primitives/src/test/flip_card.rs`。）
  - 组件中出现可复用状态机实现（受控/非受控、展开规则、选择归一）即判应下沉。
  - 组件与业务全局状态之间必须有适配边界，禁止组件直接依赖业务 store 类型。
  - `logic.rs` 仅做装配与映射，不重新实现状态原语。
- [x] 如果无异步相关，直接打勾。异步交互语义统一：`is_loading`、error/retry、disabled、`aria-busy` 映射一致；优先复用统一 async action 原语（如 `use_async_action`），禁止每组件自定义一套加载/错误协议。（`FlipCard` 无远程请求、无异步 action、无加载/失败状态机，交互全部为同步本地翻转与指针/键盘语义（`components/flip-card/src/view.rs` 仅消费 `ui_headless::use_controllable_state` + `use_flip_card` 同步契约）。因此本项按 `N/A（无异步交互）` 通过；当前不引入 `is_loading`/`aria-busy`/retry 协议，避免伪复杂度。回归：`components/flip-card/test/semantics.rs::flip_card_has_no_async_contract_surface`。）
  - 无异步交互时需明确标注 N/A 理由（例如“组件无远程请求与异步状态”），不是机械打勾。
  - 有异步交互时，`is_loading`/disabled/`aria-busy`/retry 语义必须成套一致，且对键盘与读屏路径可用。
  - 异步失败态要有可恢复路径（重试或回退），并有语义测试覆盖。
- [x] API 易用性验收标准（DX Paradox）：把复杂性留在内部，把简单留给用户。（`FlipCard` 默认路径保持 `front + back` 两个必填 props，未暴露内部 `state` 对象；`components/flip-card/src/README.md` 的 Hello World 调用维持 5 行以内；`apps/docs-app/src/pages/components/pages/display_extra.rs::flip_card()` 新增 `<Playground title="Hello World (Default Path)">`，优先展示无需接线 `ui-state-primitives/ui-headless` 的最小可用调用。复杂需求（受控/disabled/mode/motion）仍为可选扩展参数。回归：`components/flip-card/test/semantics.rs::{flip_card_dx_default_api_path_stays_simple,flip_card_docs_page_covers_primary_playgrounds}`。）
  - 基础用法不得要求用户先理解或手动接线 `ui-state-primitives`/`ui-headless` 状态机。
  - 基础组件 Hello World 示例代码不得超过 5 行（导入与外层模板按仓库约定不计），并可直接运行。
  - 简单需求走简单 API，复杂需求再暴露高级入口：默认 props 覆盖高频场景，高级控制通过受控/扩展参数按需开启。
  - 禁止把内部状态对象作为基础必填参数暴露（例如强制 `state=...` 才能完成点击/展开等基本交互）。
  - docs-app 必须提供最小可用示例，优先展示一眼可懂的默认调用路径。
- [x] 组合型组件主 API 必须“显示优于约定”：优先使用显式组合 `<Parent><Item ... /></Parent>`。（`N/A（组件类型不适用）`：`FlipCard` 是单体双面组件，公共 API 仅 `front/back` 两个显式 slot，不存在 item 集合建模与索引配对场景，因此不存在 `labels + children`、`titles + panels` 这类并行数组入口。回归：`components/flip-card/test/semantics.rs::flip_card_non_composite_api_rejects_parallel_item_contracts`。）
  - 每个 item 的标题、语义与内容必须在同一 `Item` 结构维度绑定，避免索引配对式隐式约定。
  - `labels + children`、`titles + panels` 等并行数组/并行槽位写法不得作为默认或推荐 API。
  - 不引入这类语法糖：若为配置式输入，仅允许类型化 `ItemSpec`，并在内部映射为显式 `Item` 语义树。

### 3. 高级交互与物理机制（Shell/Physics）
- [x] 宏观/微观双状态机（Macro/Micro Duality）：拖拽等高频交互在 `Dragging` 期间由 `view/motion` 本地循环执行；禁止每帧穿越回 `logic.rs`，必须在结束时通过 `Action::DragEnd` 回流收敛。（`N/A（组件类型不适用）`：`FlipCard` 当前交互仅点击/键盘/hover 翻转，不提供 drag 手势与拖拽状态轴，因此不存在 `Dragging` 微状态循环与 `Action::DragEnd` 回流需求。回归：`components/flip-card/test/semantics.rs::flip_card_has_no_dragging_macro_micro_state_machine_surface`。）
- [x] 几何两段式渲染（Two-Pass Rendering）：`Tooltip/Popover/Menu` 等依赖 DOM 测量的组件必须走 `Intent -> Measure(view) -> Rectification(logic)`，并具备幂等收敛保护防死循环。（`N/A（组件类型不适用）`：`FlipCard` 当前不依赖 DOM 几何测量（无 `getBoundingClientRect/offset/client/scroll` 采样），交互仅基于 click/keyboard/hover 语义与 spring attach，因此不存在两段式测量校正循环与收敛风险。回归：`components/flip-card/test/semantics.rs::flip_card_has_no_two_pass_geometry_rendering_pipeline_surface`。）
- [x] 集合注册协议（Registration Protocol）：`Accordion/Tabs/Menu` 动态子项必须通过 `RegistrationContext` 上报 `Register/Unregister`，逻辑层维护 `items_order`，禁止依赖 `HashSet` 迭代顺序做导航。（`N/A（组件类型不适用）`：`FlipCard` 是单体双面组件，不存在动态子项集合与导航序列，不需要 `RegistrationContext`、`Register/Unregister` 或 `items_order` 维护。回归：`components/flip-card/test/semantics.rs::flip_card_has_no_collection_registration_protocol_surface`。）
- [x] 插槽投影策略（Slot Projection）：容器组件明确 `Lazy/KeepAlive/Eager`；`KeepAlive` 隐藏时必须通过生命周期通知（如 `NotifyHidden`）暂停轮询/动画等高耗能副作用。（`N/A（组件类型不适用）`：`FlipCard` 为单体双面展示组件，不是多子项容器，不存在 slot 投影策略选择（`Lazy/KeepAlive/Eager`）与隐藏生命周期回调（`NotifyHidden`）需求。回归：`components/flip-card/test/semantics.rs::flip_card_has_no_slot_projection_strategy_surface`。）
- [x] 环境订阅流（Env Streams）：`Resize/Theme/Intersection` 等环境变化在 `view.rs` 采样、防抖后转化为高层语义 `Action`（如 `BreakpointChanged`）推送到 `logic`；禁止原始事件洪泛。（`N/A（组件类型不适用）`：`FlipCard` 当前不订阅 `Resize/Theme/Intersection` 环境流，交互仅依赖本地 click/keyboard/hover 与 headless 状态契约，不存在环境事件采样、防抖与 `Action` 回流链路。回归：`components/flip-card/test/semantics.rs::flip_card_has_no_env_stream_subscription_surface`。）
- [x] 事件光锥（Event Light Cone）：`Table/Grid` 等大型集合批量操作必须走 `Context Bus + Selector` 与状态压缩表达（如 `SelectionState::All`），禁止 O(N) 级向下 prop drilling。（`N/A（组件类型不适用）`：`FlipCard` 不是大型集合容器，不存在批量选择、状态压缩或上下文总线路径，也不存在 O(N) 子项级 prop drilling 风险。回归：`components/flip-card/test/semantics.rs::flip_card_has_no_event_light_cone_bulk_collection_surface`。）
- [x] 统一因果总线（Causality Bus）：复杂派生总线操作必须支持透传 `TraceId`，确保“用户触发 -> 派生命令 -> 总线广播 -> 订阅者”因果链不断裂。（`N/A（组件类型不适用）`：`FlipCard` 为单体局部交互组件，不存在跨订阅者广播的复杂派生总线，也不需要 `TraceId` 透传链路。回归：`components/flip-card/test/semantics.rs::flip_card_has_no_causality_bus_trace_surface`。）
- [x] 存在 A11y 实现、国际化与本地化实现（至少具备接入点，不硬编码用户可见文本）。（`FlipCard` 在 `components/flip-card/src/view.rs` 通过 `ui_headless::use_flip_card` 挂载 `role/tabindex/aria-pressed/aria-disabled` 与键盘路径，且透传 `lang/dir` 到 `ui-headless`；共享 locale 语义由 `crates/ui-headless/src/a11y.rs::locale_attrs` 统一提供。组件不在 `view.rs` 内硬编码用户可见文案，文本内容由 `front/back` slot 注入。回归：`components/flip-card/test/semantics.rs::{flip_card_view_uses_motion_and_state_contracts,flip_card_a11y_i18n_contracts_are_headless_and_text_free}`。）
  - 交互元素必须具备可验证语义：`role`/`aria-*`/键盘可达路径完整，且和 headless 契约一致。
  - 用户可见文本来源必须可覆盖：优先 props，其次应用注入（`UiRoot`/i18n bundle），最后组件兜底文案；禁止把业务可见文案硬编码在 `view.rs`。
  - 组件需透传或消费 `lang` / `dir`（LTR/RTL）上下文，不得假设单语言单方向。
  - 共享 A11y 工具优先来自 `crates/ui-headless/src/a11y.rs`，组件层不重复发明同名语义工具。
- [x] 状态可观测、可检索、可验证：使用稳定 `data-*` 与 `aria-*` 标记表达状态和来源。（`FlipCard` 在 `components/flip-card/src/view.rs` 暴露稳定语义标记：`role/tabindex/aria-pressed/aria-disabled` + `data-slot/data-state/data-visible/data-flip-mode/data-*-source`，并区分受控/非受控与默认值/外部值来源（`data-flipped-control-mode/data-flipped-prop-source/data-flipped-default-source/data-flipped-change-source`）。标记值由 `crates/ui-state-primitives/src/flip_card.rs` 的封闭 helper（`state_attr/flip_mode_attr/source_attr`）与 `logic.rs` 的 `&'static str` 来源字段产出，避免自由文本漂移。回归：`components/flip-card/test/semantics.rs::{flip_card_view_uses_motion_and_state_contracts,flip_card_observability_markers_are_stable_and_enumerated}`。）
  - 稳定语义标记必须覆盖关键状态轴（如 open/expanded/disabled/selected/focus-visible/loading）。
  - 状态来源必须可区分（受控/非受控、默认值/外部值、交互来源），通过稳定 marker 暴露而不是隐式推断。
  - 自动化选择器优先基于语义标记，不依赖 DOM 顺序、层级深度或临时 class 名。
  - 标记值应为封闭集合（可枚举），避免自由文本导致契约漂移。
- [x] 样式依赖显式状态（`data-*`/class），而非脆弱 DOM 结构猜测。（`FlipCard` 的状态样式分支全部由稳定语义标记驱动：`components/flip-card/src/styles.rs` 使用 `data-disabled/data-motion-source/data-class-source/data-id-source/data-flip-mode/data-visible` 与稳定 class；不存在 `:nth-child` 或深层级结构猜测。运行时仅在 `components/flip-card/src/motion.rs` 写入必要 custom properties（`--ui-flip-card-rotation/scale/tilt`），未把业务样式分支塞进 `view.rs` inline style。回归：`components/flip-card/test/semantics.rs::{flip_card_styles_include_state_source_and_face_markers,flip_card_styles_depend_on_explicit_markers_and_css_vars_only}`。）
  - `styles.rs` 中状态分支选择器必须基于 `data-*`/`aria-*`/稳定 class，禁止用 `:nth-child`、深层级选择器猜测状态。
  - 运行时样式仅允许传递必要 CSS 变量（custom properties）；禁止把业务样式逻辑塞进 inline style。
  - 视觉状态切换必须可由语义标记直接解释，不能依赖“某节点是否恰好存在”。
- [x] 测试验证“语义契约”而不只验证视觉快照。（`FlipCard` 语义回归以契约断言为主：`components/flip-card/test/semantics.rs::{flip_card_view_uses_motion_and_state_contracts,flip_card_observability_markers_are_stable_and_enumerated,flip_card_semantic_contract_matrix_is_covered_without_snapshot_dependency}` 覆盖 `role/aria/data-state/source` markers；`components/flip-card/test/logic.rs::{normalize_flipped_axis_centralizes_default_priority_and_sources,normalize_behavior_flags_centralizes_bool_defaults_and_alias_priority}` 覆盖受控/非受控与 disabled；`crates/ui-headless/src/test/flip_card.rs::{key_down_contract_only_toggles_for_enter_or_space_when_allowed,flip_card_handlers_toggle_and_normalize_hover_focus_state,disabled_flip_card_does_not_toggle_or_enter_hover}` 覆盖键盘/指针路径；`components/flip-card/src/{view.rs,motion.rs}` 与 `crates/ui-motion/src/test/lib.rs::non_wasm_web_backend_is_predictable_noop` 锁定 SSR/wasm 差异。当前用例未引入 snapshot-only 断言，视觉快照非主验收手段。）
  - 至少存在语义测试覆盖关键状态与交互路径（role/aria/data-state/source markers）。
  - 测试矩阵必须覆盖关键分支：受控/非受控、disabled、键盘路径、指针路径、SSR/wasm 差异（按适用范围）。
  - 视觉快照只能作为补充，不得替代语义契约断言。
- [x] 组件文件职责正确：`mod.rs`（导出边界）、`logic.rs`（归一/派生/来源标记）、`styles.rs`（静态 token-first CSS）、`view.rs`（Leptos 结构 + headless 挂载）、`motion.rs`（动效契约 + attach）。（`FlipCard` 目录保持标准落点：`components/flip-card/src/{mod,logic,styles,view,motion}.rs` 全部存在，且未引入 `spec.rs/render.rs`。`mod.rs` 仅维护稳定导出边界（`logic/view` 私有，`motion/styles` 模块导出 + `pub use FlipCard/FlipCardMotion`）；`logic.rs` 仅负责归一/派生/来源标记，无 DOM/事件挂载；`styles.rs` 保持 token-first 静态 CSS（`var(--ui-*)`）；`view.rs` 仅做 Leptos 结构渲染与 `ui_headless` 契约挂载并消费 `logic` 输出；`motion.rs` 仅承载 `FlipCardMotion + sanitize_motion + attach_motion` 与 `ui-motion` driver 映射。回归：`components/flip-card/test/semantics.rs::flip_card_component_files_follow_responsibility_boundaries`。）
  - `mod.rs` 只维护最小稳定导出面与 feature gate，不承载实现细节。
  - `logic.rs` 只做输入归一、状态派生、来源标记；禁止 DOM 操作和样式细节分支。
  - `styles.rs` 只包含 token-first 静态 CSS；禁止硬编码主题常量与业务语义文案。
  - `view.rs` 只做结构渲染与 headless 契约挂载；禁止隐藏关键状态决策。
  - `motion.rs` 只做组件语义到动效契约映射与 attach；禁止在组件内重写通用动效引擎。
- [x] `spec.rs` 只用于少数复杂组件（如 button），避免泛滥。（`FlipCard` 目前不具备稳定外部 Schema/复杂配置固化需求，保持简组件边界：`components/flip-card/src/spec.rs` 不存在，`components/flip-card/src/mod.rs` 未引入 `mod spec`/`pub mod spec`。组件说明继续落在 `components/flip-card/check2.md` 与 `components/flip-card/src/README.md`，未为“形式统一”新增 spec 模块。回归：`components/flip-card/test/semantics.rs::flip_card_component_files_follow_responsibility_boundaries`。）
  - 仅当组件存在稳定外部规范/Schema 契约或复杂配置固化需求时才引入 `spec.rs`。
  - 简单组件不得为了“形式统一”新增 `spec.rs`；说明文档应留在 `check2.md`/组件文档。
  - 新增 `spec.rs` 必须同步给出契约测试与版本演进说明。
- [x] 组件层遵循 token-first 静态样式契约：样式通过 `styles.rs` 聚合注入；运行时仅传必要 CSS 变量；不把 Utility-First/CSS-in-Rust 当组件库默认范式。（`FlipCard` 样式规则集中在 `components/flip-card/src/styles.rs::CSS`，视觉值消费 `var(--ui-*)` 与 fallback token；`crates/ui-components/src/css.rs` 通过 `#[cfg(feature = "component-flip_card")] out.push_str(crate::flip_card::styles::CSS);` 进行特性化聚合；`crates/ui-components/src/root.rs::UiRoot` 通过 `inject_components_css` 路径统一注入 `crate::css::push_components_css`。组件源码未引入 Utility-First/CSS-in-Rust 默认范式（无 `@apply`/`tailwind`/`stylist`/`stylex`）。回归：`components/flip-card/test/semantics.rs::{flip_card_css_is_aggregated,flip_card_styles_depend_on_explicit_markers_and_css_vars_only,flip_card_styles_include_state_source_and_face_markers}`。）
  - 样式规则统一落在 `styles.rs`，由 `crates/ui-components/src/css.rs` 聚合并通过 `UiRoot` 注入。
  - 颜色/间距/圆角/阴影等视觉值必须来自 `var(--ui-*)`，禁止组件私有 token 体系。
  - Utility-First 仅作为 `apps/*` 应用层布局手段，不得反向污染组件库契约。
  - CSS-in-Rust 仅在有明确类型安全与构建成本净收益时作为例外采用。
- [x] 默认主题美学质量达标（Visual Desire）：以 HeroUI 现代审美为学习对标，默认主题不仅“可用”，还必须“第一眼可信”。（N/A（仓库级视觉治理项）：该条要求 docs-app 的默认主题截图基线与关键组件（Button/Input/Overlay）跨组件视觉回归，对 `flip-card` 单组件范围不可独立闭环。`FlipCard` 在组件内已提供默认路径与视觉层级基线（`apps/docs-app/src/pages/components/pages/display_extra.rs` 的 `Hello World (Default Path)` 与 `Click + Keyboard Flip`），并通过 `components/flip-card/src/styles.rs` 的 token-first 字重/字号/间距/对比/focus 反馈（`--ui-heading-*`、`--ui-fg-muted`、`--ui-shadow-sm`、`:focus-visible`）维持“非粗糙”底线。仓库级视觉基线与截图回归需在统一视觉任务中验收，不在本单组件 check 内冒充完成。）
  - 默认主题需通过基础美学清单：信息层级清晰（字重/字号/间距）、对比与层次自然、交互反馈明确（hover/active/focus）。
  - docs-app 必须提供默认主题基线页面与截图基线，关键组件（Button/Input/Overlay）纳入视觉回归对比。
  - 禁止“可访问但粗糙”的最低可用心态：视觉退化（类似旧式 Bootstrap 观感）视为质量回归。
  - HeroUI 对标以“视觉语言与体验质量”对齐为目标，不做无差别 API 表层复制。
- [x] Tree Shaking 是一等能力：package 模式支持组件级 feature；source 模式天然裁剪；样式层同步裁剪，禁止无条件聚合全部 CSS，禁止破坏 DCE/LTO 的全量中央注册表。（`FlipCard` 已接入组件级特性链：`crates/ui-components/Cargo.toml` 定义 `component-flip_card = ["dep:ui-flip-card"]`，并在 `all-components` 列表中显式登记；`crates/ui-components/src/lib.rs` 通过 `#[cfg(feature = "component-flip_card")] pub use ui_flip_card as flip_card;` 条件导出；`crates/ui-components/src/css.rs` 通过 `#[cfg(feature = "component-flip_card")] out.push_str(crate::flip_card::styles::CSS);` 条件聚合样式，未出现无条件全量注册入口。应用侧依赖边界明确：`apps/web-demo/Cargo.toml` 使用 `default-features = false + web-demo-components`（无 `all-components`），`apps/docs-app/Cargo.toml` 显式 `default-features = false + all-components`。本地命令验证：`cargo tree -e features -i ui-components -p ui-components --no-default-features --features component-flip_card,inject-css` 仅出现命令行特性 `component-flip_card/inject-css`；`cargo tree -e features -i ui-components -p web-demo` 输出包含 `web-demo-components` 且未见 `all-components`。wasm 最小特性编译命令已执行但受当前环境 `Invalid cross-device link (os error 18)` 阻断；仓库脚本 `scripts/check-ui-components-tree-shaking.sh` 与 `scripts/tree_shaking_budget.env` 已内置 feature-tree/wasm/budget 阻断规则。回归：`components/flip-card/test/semantics.rs::{flip_card_tree_shaking_keeps_component_feature_and_css_boundaries,flip_card_tree_shaking_check_script_covers_feature_tree_wasm_and_budget}`。）
  - package 模式必须有组件级 feature（如 `component-accordion`）；未启用组件不得进入编译与链接路径。
  - `lib.rs` 与 `css.rs` 必须按 feature 条件导出/聚合，禁止无条件引用所有组件模块和 CSS 常量。
  - source 模式下仅引入需要的组件源码，不通过中央注册表维持全组件可达。
  - 任意“全量组件映射表/注册表”若导致不可达代码变可达，直接判不通过。
  - 验证命令（特性树）：`cargo tree -e features -p ui-components --no-default-features --features component-accordion,inject-css`，确认仅启用目标组件特性链。
  - 验证命令（反向依赖）：`cargo tree -e features -i ui-components -p web-demo`，检查是否被 `all-components` 或隐式特性全量拉起。
  - CI 检查（最小特性编译）：新增任务仅开启目标最小特性（示例：`cargo check -p ui-components --target wasm32-unknown-unknown --no-default-features --features component-accordion,inject-css`）。
  - CI 检查（体积预算）：对“最小特性构建产物”设定预算并阻断回归（可用固定阈值，如 `< 50KB`，或基于仓库基线的相对阈值）；不得只做编译通过而不做体积约束。
- [x] 类型系统 + 语义标记共同提供机器可读状态；关键输入空间受类型约束。（`FlipCard` 离散状态轴已类型化：`crates/ui-state-primitives/src/flip_card.rs` 使用 `enum FlipCardFlipMode { Toggle, Hover }` 与 `normalize_behavior_flags` 统一输入归一，避免字符串协议。组件 `components/flip-card/src/logic.rs` 将 `default/is_*` 与兼容别名集中映射为类型化状态，并通过 `derive_render_state` 生成封闭集合来源标记。`components/flip-card/src/view.rs` 对外暴露稳定 `data-state/data-flip-mode/data-flipped-*-source` 等语义标记，供自动化消费；现有逻辑回归可直接定位契约破坏点（`components/flip-card/test/logic.rs::{normalize_behavior_flags_maps_bool_aliases_to_enum_mode,derive_render_state_centralizes_slot_states_and_semantic_markers}`），并新增聚合回归 `components/flip-card/test/semantics.rs::flip_card_type_system_and_semantic_markers_form_machine_readable_contract`。）
  - 离散输入与状态轴必须优先使用 `enum`/新类型建模，避免字符串协议与布尔爆炸。
  - 无效状态要么在类型层不可表达，要么在 `logic.rs` 被统一归一化并可测试。
  - 关键状态必须通过稳定语义标记对外可读，供测试与 Agent 自动化消费。
  - 编译器与测试反馈应能直接定位状态契约破坏点，形成可持续闭环。

### 4. DOM/环境边界治理
- [x] 焦点全局栈（Focus Stack & GC）：层叠 `Overlay` 禁止私存 `NodeRef` 作为恢复目标；必须依赖全局 Focus Manager（如 `FallbackTo/Selector`）防止焦点坠落到 `document.body`。（`N/A（组件类型不适用）`：`FlipCard` 不是层叠 Overlay 组件，不存在打开/关闭后焦点恢复到触发源的全局焦点栈职责；当前 `NodeRef` 仅用于动效节点挂载（`components/flip-card/src/view.rs::root_ref` + `motion::attach_motion`），并未私存“焦点恢复目标”。回归：`components/flip-card/test/semantics.rs::flip_card_has_no_overlay_focus_stack_restore_surface`。）
- [x] 受控外交特区（Escape Hatches）：集成 ECharts/Map 等命令式第三方库时必须处于 `Foreign Zone`（`YieldControl/CleanupForeign`）；第三方实例不得暴露为组件公共 API 或反向污染状态机。（`N/A（组件类型不适用）`：`FlipCard` 当前不集成命令式第三方实例（无 ECharts/Map SDK 初始化、实例句柄持有与清理生命周期），组件公共 API 也未暴露任何第三方实例类型；状态机仅消费 `ui-state-primitives + ui-headless` 契约，不存在“第三方实例反向污染状态内核”的路径。回归：`components/flip-card/test/semantics.rs::flip_card_has_no_foreign_zone_escape_hatch_surface`。）
- [x] SSR 时空断裂治理（Hydration Discontinuity）：逻辑初始化禁止依赖 `now()` 或原生随机 UUID；必须通过 `IdProvider` 注入确定性种子，确保 SSR/Hydration 间 ID 稳定。（`FlipCard` 已移除本地 `thread_local next_id` 初始化路径，改为 `components/flip-card/src/view.rs` 通过 `use_ui_id_provider().next_prefixed_id(logic::DEFAULT_ID_PREFIX)` 生成默认 ID，并经 `logic::resolve_id` 统一归一；组件内无 `now()/Uuid/rand` 熵源。确定性种子注入由 `crates/ui-components/src/root.rs` 的 `id_seed + provide_ui_id_provider(id_seed)` 提供。回归：`components/flip-card/test/semantics.rs::flip_card_hydration_discontinuity_uses_seeded_id_provider_without_entropy_init`。）
- [x] SSR 与跨平台检查：覆盖 web/ssr/wasm 分支，不破坏 non-wasm 编译路径。（`FlipCard` 平台分支为显式 `cfg`：`components/flip-card/src/view.rs` 用 `#[cfg(target_arch = "wasm32")] ev.is_composing()` / `#[cfg(not(target_arch = "wasm32"))] false`，`components/flip-card/src/motion.rs` 用 `#[cfg(target_arch = "wasm32")]` 承载 `leptos::web_sys` 动效 attach、`#[cfg(not(target_arch = "wasm32"))]` 执行 no-op `black_box(sanitize_motion(...))`；`crates/ui-motion/src/lib.rs` 也提供 wasm backend 与 non-wasm stub。compile-only 证据命令已执行：`cargo check -p ui-flip-card`（默认本地）、`cargo check -p ui-flip-card --target wasm32-unknown-unknown`（web/wasm32）、`cargo check -p ui-flip-card --no-default-features --features ui-headless/ssr`（ssr/native）；当前环境统一被 `Invalid cross-device link (os error 18)` 阻断，但阻断发生在依赖写入阶段，非组件代码分支错误。回归：`components/flip-card/test/semantics.rs::flip_card_cross_platform_compile_contract_is_explicit_and_non_wasm_safe`。）
  - 至少包含 compile-only 证据：web（wasm32）、ssr（native）、默认本地构建三条路径。
  - 平台分支差异必须显式 `cfg` 或 feature 管理，禁止依赖运行时偶然行为。
  - non-wasm 路径禁止引用 `web-sys`/浏览器对象。
- [x] `ui-headless` web/ssr feature 互斥受 `compile_error!` 保护（`crates/ui-headless/src/lib.rs`）。（源码契约已存在并保持：`crates/ui-headless/src/lib.rs` 含 `#[cfg(all(feature = "web", feature = "ssr"))] compile_error!(...)`，`crates/ui-headless/Cargo.toml` 特性矩阵为 `default=["web"]`、`web=["leptos/csr"]`、`ssr=["leptos/ssr"]`。`FlipCard` 仅以 `components/flip-card/Cargo.toml` 依赖 `ui-headless`，未本地强行启用 `web+ssr` 并行特性，未破坏互斥约束。验证命令已执行：`cargo check -p ui-headless --no-default-features --features web`、`cargo check -p ui-headless --no-default-features --features ssr`、`cargo check -p ui-headless --no-default-features --features web,ssr`；当前环境均被 `Invalid cross-device link (os error 18)` 阻断，未能在本机跑到最终编译阶段。回归：`components/flip-card/test/semantics.rs::flip_card_headless_web_ssr_feature_mutex_contract_is_preserved`。）
  - 组件依赖 `ui-headless` 能力时，不得破坏其 web/ssr 互斥约束。
  - 组件若新增 headless 功能接入，需验证两条 feature 路径都可编译。
  - 发现“同时启用 web+ssr 仍可过编译”视为契约回归。
- [x] `ui-motion` 非 wasm 提供 no-op/stub（`crates/ui-motion/src/lib.rs`），保证 SSR/tooling 可编译。（`ui-motion` 已在 `crates/ui-motion/src/lib.rs` 通过 `#[cfg(not(target_arch = "wasm32"))] pub mod web` 提供 no-op/stub（`prefers_reduced_motion() -> true`、`animate(&(), ...)`），并带有 `non_wasm_web_backend_is_predictable_noop` 回归；`FlipCard` 的 `components/flip-card/src/motion.rs` 在 non-wasm 分支执行 `std::hint::black_box(sanitize_motion(motion))` 安全降级，不依赖动画句柄存在、不触发 panic。compile-only 证据：`cargo check -p ui-motion` 本地通过；`cargo check -p ui-motion --target wasm32-unknown-unknown` 在当前环境受 `Invalid cross-device link (os error 18)` 阻断（依赖写入阶段）。回归：`components/flip-card/test/semantics.rs::flip_card_ui_motion_non_wasm_stub_contract_is_safe_and_predictable`。）
  - `motion.rs` 调用必须可在 non-wasm 下安全降级，不触发 panic。
  - 组件不得假设动画句柄一定存在；no-op 分支行为需可预测。
  - toolchain 场景（测试/文档/静态分析）不得因 motion 依赖阻塞编译。
- [x] 组件实现覆盖 `reduced-motion` / SSR / wasm 分支。（`FlipCard` 已覆盖三条分支：`components/flip-card/src/motion.rs` 在 wasm 路径新增 `ui_motion::web::prefers_reduced_motion()` 检测，`reduced-motion` 下跳过 spring driver 并直接写入最小必要 CSS 变量反馈；`#[cfg(not(target_arch = "wasm32"))]` 分支继续 no-op `black_box(sanitize_motion(...))`，确保 SSR/tooling 安全降级。SSR/hydration 稳定性由 `components/flip-card/src/view.rs` 的统一语义标记输出与确定性 ID 路径（`use_ui_id_provider + resolve_id`）保证，避免首帧语义错位；wasm 分支仅增强交互执行，不改变 `data-state/data-visible/data-*source` 契约。回归：`components/flip-card/test/semantics.rs::{flip_card_hydration_discontinuity_uses_seeded_id_provider_without_entropy_init,flip_card_cross_platform_compile_contract_is_explicit_and_non_wasm_safe,flip_card_ui_motion_non_wasm_stub_contract_is_safe_and_predictable,flip_card_reduced_motion_ssr_and_wasm_contracts_stay_consistent}`。）
  - `reduced-motion` 下动画应跳过或降级为最小必要反馈。
  - SSR 输出必须与客户端 hydration 兼容，避免首帧语义错位。
  - wasm 分支允许增强交互，但语义契约不得与 SSR 分支分裂。
- [x] 性能治理：关键路径有预算（首次渲染/更新耗时/内存），回归可检测、可归因、可阻断。（已满足：`apps/docs-app/src/pages/components/shell.rs` 新增 `"flip-card" => UiPerfBudget { max_mount_ms: 30.0, max_update_ms: Some(10.0), max_heap_kb: Some(512.0) }`，并保留基础预算基线 `button/input`（`24ms/28ms`）；`apps/docs-app/src/perf_probe.rs` 继续输出 `data-perf-mount-ms/data-perf-budget-ms/data-perf-budget-update-ms/data-perf-budget-heap-kb/data-perf-observability/data-perf-violation` 作为可检测/可阻断语义；`e2e/tests/docs_app_components_coverage.spec.mjs` 保持 perf probe 阈值断言；`apps/docs-app/src/debug_overlay.rs` 通过 `ui_headless::use_ui_trace` 保留状态/渲染归因链路；`scripts/check-ui-components-performance.sh` 新增阻断命令 `cargo test -p ui-flip-card flip_card_performance_governance_budget_is_defined_and_blocking`。组件回归：`components/flip-card/test/semantics.rs::flip_card_performance_governance_budget_is_defined_and_blocking`。`render_count` 自动化当前按仓库统一策略记录为等价证据并跟踪补齐：`docs/plan/TODO.md` 中 `建立 \`render_count\` 自动化回归（Button/Input/Accordion），替换当前 mount-only 等价证据`。执行证据命令 `cargo test -p ui-flip-card flip_card_performance_governance_budget_is_defined_and_blocking` 在当前环境仍被 `Invalid cross-device link (os error 18)` 阻断，阻断点在依赖写入阶段而非组件语义契约。）
  - 关键交互组件需定义最小预算项（首渲染、关键更新、内存/分配趋势）。
  - 回归检测至少具备可重复基线与失败阈值，不靠主观“感觉变慢”。
  - 性能问题需可归因到状态、渲染、样式或动效路径之一。
  - 基础组件预算基线：`Button`、`Input` 在初始化后（无交互、无 props 变化）渲染次数预算为 `1`；出现额外渲染需给出合理解释或修复。
  - 测试要求：在 `crates/ui-components/tests/*` 增加 `render_count` 类回归测试（测试框架支持时必须启用）；至少覆盖基础组件与本次改动组件。
  - 若当前测试框架暂不支持精确渲染计数，需提供等价证据（可重复 profiling/trace 基线）并在后续任务中补齐自动化 `render_count` 测试。
- [x] `view!` 宏复杂度受控：单个 `view!` 块不得承载超长深嵌套结构；复杂布局按语义分块，避免一次性宏展开导致编译与 wasm 体积劣化。（`components/flip-card/src/view.rs` 当前保持受控规模（257 行），并采用“根块 + 局部函数片段”结构（`#[component]` 1 处、`view!` 3 处：根节点 + `render_front_face` + `render_back_face`）；状态统一在 `Memo`（`derived_render_state/root_class/front_class/back_class`）中预派生，未出现循环密集/集合拼装式宏展开（`for item in`、`collect::<Vec<_>>()`、`while`、`loop`）。回归由 `components/flip-card/test/semantics.rs::flip_card_view_macro_complexity_is_small_and_does_not_require_semantic_subrenders` 锁定，门禁脚本 `scripts/check-ui-components-view-macro.sh` 已纳入阻断命令 `cargo test -p ui-flip-card flip_card_view_macro_complexity_is_small_and_does_not_require_semantic_subrenders`。执行证据命令在当前环境仍受 `Invalid cross-device link (os error 18)` 阻断，阻断点位于依赖写入阶段而非组件宏结构契约。）
  - 复杂结构按语义子块拆分（header/body/item 等），避免巨型单块 `view!`。
  - `view.rs` 中若出现多层嵌套重复片段，应优先提取局部渲染函数。
  - 编译时间/产物体积异常增长时，优先排查宏展开体量。
- [x] 函数式拆分优先：不涉及复杂状态与生命周期管理的 UI 片段，优先拆为普通 Rust 函数（返回 `impl IntoView`/`View`），而不是新增 `#[component]`。（`FlipCard` 已将轻逻辑重复片段拆为普通函数 `render_front_face` 与 `render_back_face`（均返回 `impl IntoView`），主组件 `FlipCard` 仅负责状态归一与契约挂载；未引入局部 `#[component]`。语义标记保持稳定：front/back 仍输出 `data-slot/data-state/data-visible/data-hidden`。回归：`components/flip-card/test/semantics.rs::flip_card_view_functional_split_prefers_plain_functions_over_local_components`；门禁：`scripts/check-ui-components-view-macro.sh` 新增阻断命令 `cargo test -p ui-flip-card flip_card_view_functional_split_prefers_plain_functions_over_local_components`。执行证据命令在当前环境仍受 `Invalid cross-device link (os error 18)` 阻断，阻断点位于依赖写入阶段而非组件函数式拆分契约。）
  - 纯静态或轻逻辑片段优先函数化；仅在需要独立 props 语义时升级为组件。
  - 禁止把所有局部片段都升格为 `#[component]` 导致抽象噪音。
  - 拆分后语义标记与测试定位仍需稳定。
- [x] 静态片段常量化：复杂 SVG、页脚、长说明文本等纯静态内容优先常量化/模板化，减少重复 `view!` 渲染指令生成。（`FlipCard` 当前为轻量交互组件，`components/flip-card/src/view.rs` 不存在复杂 SVG/页脚/长说明文本模板与 `inner_html` 注入路径，因此该子项按“常量化或缺省无重静态片段”通过；静态结构已收敛到局部函数 `render_front_face/render_back_face` 与稳定语义标记（`data-slot` + `aria-*`），避免静态资源散落。回归：`components/flip-card/test/semantics.rs::flip_card_static_fragments_are_constantized_or_absent_for_simple_layout`；门禁：`scripts/check-ui-components-view-macro.sh` 新增阻断命令 `cargo test -p ui-flip-card flip_card_static_fragments_are_constantized_or_absent_for_simple_layout`。执行证据命令在当前环境仍受 `Invalid cross-device link (os error 18)` 阻断，阻断点位于依赖写入阶段而非组件静态片段契约。）
  - 可判定为纯静态的片段应避免重复动态构造。
  - 常量化后仍需维持可访问语义（title/aria-label/role 等）。
  - 静态资源变更路径要清晰，避免散落在多个 `view!` 片段中。
- [x] `inner_html` 使用约束：仅允许注入受信任静态常量，禁止拼接用户输入；使用处必须补充语义与安全回归测试。（N/A（FlipCard）：组件当前不使用 `inner_html`，`components/flip-card/src/{mod,logic,styles,view,motion}.rs` 与 `src/README.md` 未出现 `inner_html` / `set_inner_html` / `dangerously_set_inner_html` / `markdown_to_html(` 调用；docs 入口 `apps/docs-app/src/pages/components/pages/display_extra.rs` 也无脚本注入路径（`<script` / `javascript:`）。回归：`components/flip-card/test/semantics.rs::flip_card_inner_html_usage_is_absent_and_untrusted_injection_paths_are_blocked`；门禁：`scripts/check-ui-components-inner-html.sh` 新增阻断命令 `cargo test -p ui-flip-card flip_card_inner_html_usage_is_absent_and_untrusted_injection_paths_are_blocked`。执行证据命令在当前环境仍受 `Invalid cross-device link (os error 18)` 阻断，阻断点位于依赖写入阶段而非组件 inner_html 安全契约。）
  - 仅允许编译期常量或明确白名单内容进入 `inner_html`。
  - 严禁直接或间接注入用户输入、远端返回或未清洗模板字符串。
  - 使用 `inner_html` 的节点必须补语义测试与安全回归说明。
- [x] WASM 调试要求：关键状态可追踪（来源/时间/前后值），关键交互可回放，开发模式有可视化入口，调试能力通过 feature 隔离不污染产物。（N/A（FlipCard）：组件不引入本地 `wasm-debug` feature，不新增运行时调试 API；状态可追踪与最小可回放链路复用仓库级能力：`components/flip-card/src/view.rs` 已暴露 `data-flipped-control-mode/data-flipped-change-source/data-flip-mode-source` 等来源标记，docs 侧由 `apps/docs-app/src/lib.rs` 的 `provide_ui_trace(debug_overlay_enabled)` + `UiDebugOverlay`（`apps/docs-app/src/debug_overlay.rs`）提供开发态可视化入口与事件时间线。feature 隔离保持：`components/flip-card/Cargo.toml` 仅 `default = []`，无 `flip-card-wasm-debug`，全局调试开关仍由 `crates/ui-components/Cargo.toml` 的共享 `*-wasm-debug`（如 `button-wasm-debug`）治理。回归：`components/flip-card/test/semantics.rs::flip_card_wasm_debug_contract_is_explicitly_na_and_feature_isolated`；门禁：`scripts/check-ui-components-wasm-debug.sh` 新增阻断命令 `cargo test -p ui-flip-card flip_card_wasm_debug_contract_is_explicitly_na_and_feature_isolated`。执行证据命令在当前环境仍受 `Invalid cross-device link (os error 18)` 阻断，阻断点位于依赖写入阶段而非组件 wasm-debug 契约。）
  - 开发模式下至少能追踪关键状态变更来源与前后值。
  - 关键交互链路应支持最小可复现记录（事件顺序/状态转移）。
  - 调试开关默认不进入生产包体与公共 API。
- [x] DX 要求：样式热重载优先无需重编 wasm；组件热开发尽量保持上下文；提供可选状态保留；有 Workbench 隔离画布。（`FlipCard` 文档页 `apps/docs-app/src/pages/components/pages/display_extra.rs::flip_card()` 已提供 `Interactive Playground`，并接入 `test_css_source=flip_card_test_css_source` + `test_config_signal=workbench_config`；配合 `apps/docs-app/src/playground.rs` 的 scoped CSS Test 面板，可在不重编 wasm 的路径下快速验证样式调整。组件热开发上下文通过同一工作台内 `signal` 状态（`workbench_default_is_flipped/is_disabled/is_flip_on_hover/...`）持续保持并在隔离画布内回放配置。状态保留按当前范围标注 `N/A`：未引入 `FLIP_CARD_WORKBENCH_STORAGE_KEY`/`load_*`/`save_*` 本地持久化协议，避免在单组件阶段扩散跨页面存储契约。回归：`components/flip-card/test/semantics.rs::flip_card_dx_playground_supports_css_hot_reload_and_isolated_canvas_with_optional_persist_na`；门禁：`scripts/check-ui-components-dx.sh` 新增 `cargo test -p ui-flip-card flip_card_dx_playground_supports_css_hot_reload_and_isolated_canvas_with_optional_persist_na`。执行证据命令在当前环境仍受 `Invalid cross-device link (os error 18)` 阻断，阻断点位于依赖写入阶段而非 DX 契约本身。）
  - 常见样式调整应走快速反馈路径，不依赖完整 wasm 重编译。
  - 组件调试应尽量保持当前交互上下文，降低重复操作成本。
  - 复杂交互组件应有隔离演练入口（workbench/story/demo 之一）。
- [x] 工程能力统一：`serde` 负责 spec 序列化/版本迁移/错误结构化；`tracing` 统一 span/event 语义；async 不绑定单一运行时（tokio/async-std），runtime 细节不泄露到上层 API。（`FlipCard` 当前不涉及独立 spec/config 协议输入，`components/flip-card/src/spec.rs` 不存在，组件 crate `components/flip-card/Cargo.toml` 未引入 `serde`，因此 `serde` 序列化/迁移路径在本组件范围按 `N/A（简单组件）` 收敛；若后续引入 schema，将按统一 protocol 路径补齐结构化错误。tracing 语义保持全库统一：复用 `crates/ui-components/Cargo.toml` 既有 `*-wasm-debug` 约定（如 `button-wasm-debug`），`FlipCard` 未新增本地 tracing feature，且组件源码未引入 `tracing::span!/event!/instrument` 组件私有埋点。异步 runtime 边界保持干净：`components/flip-card/src/{mod,logic,view,styles,motion}.rs` 与 `Cargo.toml` 未泄漏 `tokio/async-std/runtime::Handle/smol/spawn_blocking` 等实现细节到公共 API。回归：`components/flip-card/test/semantics.rs::{flip_card_engineering_contract_marks_spec_serde_path_as_na_for_simple_component_scope,flip_card_engineering_contract_keeps_tracing_semantics_unified_without_component_local_events,flip_card_engineering_contract_avoids_runtime_leaks_in_public_api_surface,flip_card_engineering_check_script_covers_serde_tracing_and_runtime_boundaries}`；门禁：`scripts/check-ui-components-engineering.sh` 新增三条 `cargo test -p ui-flip-card ...`。执行证据命令在当前环境仍受 `Invalid cross-device link (os error 18)` 阻断，阻断点位于依赖写入阶段而非工程能力契约本身。）
  - 若组件涉及 spec/config 输入，序列化与错误输出应走统一结构化路径。
  - 关键流程埋点语义应与全库 tracing 约定一致，避免组件各说各话。
  - 异步边界不得把具体 runtime 类型暴露到组件公共接口。

### 5. 样式与动效（Theme & Motion）
- [x] 样式孤岛防御（Defensive Variables）：`styles.rs` 使用双层回退链 `var(--ui-*, var(--ui-fallback-*))`；禁止组件内硬编码 Hex 或裸尺寸终值，Fallback 终值由 `ui-theme` 统一输出（SSOT）。（`components/flip-card/src/styles.rs` 已将关键视觉 token 全量改为双层回退链：包括 `radius/focus-ring/border-width/border/bg/fg/shadow/space/fg-muted` 与现有 `--ui-flip-card-*` 族变量（示例：`var(--ui-radius-lg, var(--ui-fallback-radius-lg))`、`var(--ui-border-width, var(--ui-fallback-border-width))`、`var(--ui-bg, var(--ui-fallback-bg))`）。`crates/ui-theme/src/css.rs` 提供对应 fallback 终值（如 `--ui-fallback-radius-lg`、`--ui-fallback-focus-ring`、`--ui-fallback-border-width`、`--ui-fallback-bg`、`--ui-fallback-fg`、`--ui-fallback-shadow-sm`、`--ui-fallback-space-xs/lg`），保持 SSOT。回归：`components/flip-card/test/semantics.rs::flip_card_styles_use_defensive_variable_fallback_chain`；门禁：`scripts/check-ui-components-contract-hygiene.sh` 新增 `cargo test -p ui-flip-card flip_card_styles_use_defensive_variable_fallback_chain`。执行证据命令在当前环境仍受 `Invalid cross-device link (os error 18)` 阻断，阻断点位于依赖写入阶段而非样式契约本身。）
- [x] 级联层覆盖（`@layer ui`）：组件 CSS 默认聚合进 `@layer ui`；运行时数值调整仅通过 CSS Custom Properties（如 `style:--x=...`），禁止普通内联样式（如 `style=\"top: 10px\"`）。（`crates/ui-components/src/css.rs::push_components_css` 已使用 `out.push_str("\n@layer ui {\n")` 包裹组件样式并在末尾闭合，同时通过 `#[cfg(feature = "component-flip_card")] out.push_str(crate::flip_card::styles::CSS);` 聚合 `FlipCard` 样式；`crates/ui-components/src/root.rs` 继续由 `UiRoot` 集中注入。运行时样式边界满足约束：`components/flip-card/src/view.rs` 无普通 `style=` 内联样式，`components/flip-card/src/motion.rs` 仅通过 `set_property("--ui-flip-card-*", ...)` 更新 CSS Custom Properties，不写 `top/left/width/height/transform` 这类普通内联属性。回归：`components/flip-card/test/semantics.rs::{flip_card_cascade_layer_and_runtime_style_contract_is_enforced,flip_card_cascade_layer_check_script_covers_contract}`；门禁：`scripts/check-ui-components-contract-hygiene.sh` 新增 `cargo test -p ui-flip-card flip_card_cascade_layer_and_runtime_style_contract_is_enforced`。执行证据命令在当前环境仍受 `Invalid cross-device link (os error 18)` 阻断，阻断点位于依赖写入阶段而非级联层契约本身。）
- [x] Motion 合同化：`stiffness`/`damping` 等参数在 `motion.rs` 内置为组件 Contract，并通过 `attach_motion` 挂载；必须尊重 `prefers-reduced-motion` 且在 non-wasm/SSR 安全降级（no-op）。（`components/flip-card/src/motion.rs` 已内置 `FlipCardMotion` 合同（`spring: SpringConfig` + `hover_scale/hover_tilt_deg`），默认值采用 `ui_motion::presets::spring_soft()`，并在 `sanitize_spring` 中对 `stiffness/damping/mass/precision` 执行有限值与正值约束回退；`components/flip-card/src/view.rs` 先 `sanitize_motion` 再 `motion::attach_motion(root_ref, is_flipped, is_hovered, motion)` 完成挂载。wasm 分支通过 `ui_motion::web::prefers_reduced_motion()` 命中时跳过 spring runtime，仅同步 `--ui-flip-card-rotation/scale/tilt` 最小必要反馈；non-wasm 分支保留 no-op `std::hint::black_box(sanitize_motion(motion))`，不假设动画句柄存在、无 panic 路径。依赖侧 `crates/ui-motion/src/lib.rs` 继续提供 non-wasm `web::prefers_reduced_motion() -> true` 与 `animate(&(), ..)` stub。回归：`components/flip-card/test/semantics.rs::{flip_card_motion_contract_is_builtin_and_attached_with_reduced_motion_and_non_wasm_noop,flip_card_motion_contract_check_script_covers_contract}`；门禁：`scripts/check-ui-components-contract-hygiene.sh` 新增 `cargo test -p ui-flip-card flip_card_motion_contract_is_builtin_and_attached_with_reduced_motion_and_non_wasm_noop`。执行证据命令在当前环境仍受 `Invalid cross-device link (os error 18)` 阻断，阻断点位于依赖写入阶段而非 motion 契约本身。）
- [x] `ui-components` 固定入口文件落点正确。（入口分层满足约束：`crates/ui-components/src/lib.rs` 对 `FlipCard` 采用 `#[cfg(feature = "component-flip_card")] pub use ui_flip_card as flip_card;`，公共面保留 feature gate 且未暴露 `web_sys/wasm_bindgen` 平台细节；`crates/ui-components/src/css.rs` 通过 `#[cfg(feature = "inject-css")] push_components_css` + `#[cfg(feature = "component-flip_card")] out.push_str(crate::flip_card::styles::CSS);` 在 `@layer ui` 中按特性聚合；`crates/ui-components/src/root.rs::UiRoot` 统一注入 base css + theme vars + 可选 components css，并集中提供 `provide_ui_i18n(i18n)`。共享高亮原语保持在 `crates/ui-visual-primitive/src/active_highlight.rs`（`ActiveHighlightMotion + attach_active_highlight_motion`），未承载组件业务语义。禁止文件检查通过：`crates/ui-components/src/{overlay_open.rs,presence.rs,a11y.rs}` 均不存在；对应 canonical 能力位于 `crates/ui-headless/src/{controllable_state.rs,presence.rs,a11y.rs}`。回归：`components/flip-card/test/semantics.rs::{flip_card_ui_components_fixed_entry_files_follow_layered_boundaries,flip_card_entrypoints_check_script_covers_fixed_entrypoint_contract}`；门禁：`scripts/check-ui-components-entrypoints.sh` 新增 `cargo test -p ui-flip-card flip_card_ui_components_fixed_entry_files_follow_layered_boundaries`。执行证据命令在当前环境仍受 `Invalid cross-device link (os error 18)` 阻断，阻断点位于依赖写入阶段而非入口分层契约本身。）
  - `crates/ui-components/src/lib.rs`：总模块入口 + 对外 `pub use`（公共 API 面）；组件模块受 `component-*` feature gate 约束；不暴露内部平台细节类型。
  - `crates/ui-components/src/css.rs`：组件 CSS 聚合入口（`push_components_css`）；按 feature 条件注入；禁止无条件聚合全部组件 CSS。
  - `crates/ui-components/src/root.rs`：`UiRoot` 统一注入 base css + theme vars +（可选）components css，并提供全局 i18n 上下文；主题与注入策略必须集中在此。
  - `crates/ui-visual-primitive/src/active_highlight.rs`：共享高亮条样式与 motion driver；只承载通用高亮动效能力，不承载具体组件业务语义。
  - `crates/ui-components/src/overlay_open.rs`：当前仓库中不应存在；open-state 原语固定在 `crates/ui-headless/src/controllable_state.rs`，组件通过 headless API 消费。
  - `crates/ui-components/src/presence.rs`：当前仓库中不应存在；presence 原语固定在 `crates/ui-headless/src/presence.rs`，组件通过 `ui_headless::use_presence` 消费。
  - `crates/ui-components/src/a11y.rs`：当前仓库中不应存在；共享 A11y 工具固定在 `crates/ui-headless/src/a11y.rs`（如 `aria_controls_when_open`），组件只负责挂载。
- [x] 组件目录标准文件落点正确。（`FlipCard` 目录文件落点满足标准：`components/flip-card/src/{mod,logic,styles,view,motion}.rs` 全部存在，`src/render.rs` 与 `src/spec.rs` 不存在；`mod.rs` 维持最小稳定导出面（`mod logic; mod view; pub mod motion; pub mod styles; pub use FlipCard/FlipCardMotion`）且未过度导出 `logic/view`；`logic.rs` 仅做 props 归一、状态派生与来源标记，不含 DOM/事件挂载；`styles.rs` 保持 token-first 静态 CSS（`var(--ui-*)`）；`view.rs` 仅做 Leptos 结构渲染 + `ui_headless` 语义挂载；`motion.rs` 仅承载 `FlipCardMotion + sanitize_motion + attach_motion` 的语义到动效契约映射。回归：`components/flip-card/test/semantics.rs::{flip_card_component_files_follow_responsibility_boundaries,flip_card_component_directory_standard_files_follow_contract_and_na_paths}`；门禁：`scripts/check-ui-components-component-files.sh` 新增 `cargo test -p ui-flip-card flip_card_component_directory_standard_files_follow_contract_and_na_paths`。执行证据命令在当前环境仍受 `Invalid cross-device link (os error 18)` 阻断，阻断点位于依赖写入阶段而非目录落点契约本身。）
  - `<component>/mod.rs`：最小稳定导出面，存在且无过度导出。
  - `<component>/logic.rs`：props 归一化、派生状态、来源标记；不得承载可下沉原语。
  - `<component>/styles.rs`：静态 CSS 契约，只用 `var(--ui-*)`，不写死主题常量。
  - `<component>/view.rs`：纯 Leptos 结构渲染 + headless 语义挂载；禁止 `render.rs` 漂移；不隐藏关键状态决策。
  - `<component>/motion.rs`：`XxxMotion + attach_motion`；交互组件必须有；只做语义到 motion contract 的映射与挂载。
  - `<component>/spec.rs`：仅极少数组件专用（当前主要 button），无必要不新增。

### 6. AI 原生能力与文件落点（Struct-First & Projection）
- [x] 文件落点纪律：组件目录严格由 `mod.rs`（导出）、`logic.rs`（归一派生）、`styles.rs`（Token 样式）、`view.rs`（渲染）、`motion.rs`（动效）组成；复杂组件可选 `spec.rs`；禁止 `render.rs`。（`FlipCard` 已满足文件落点纪律：`components/flip-card/src/{mod,logic,styles,view,motion}.rs` 全量存在；`src/render.rs` 不存在；`src/spec.rs` 在当前简单组件范围保持缺省（复杂 schema 组件才引入）。`mod.rs` 保持导出边界、`logic.rs` 负责归一派生、`styles.rs` 仅 token 样式、`view.rs` 仅渲染与 headless 挂载、`motion.rs` 仅动效映射与 attach。回归：`components/flip-card/test/semantics.rs::{flip_card_component_directory_standard_files_follow_contract_and_na_paths,flip_card_file_placement_discipline_is_strict_for_component_scope}`；门禁：`scripts/check-ui-components-component-files.sh` 新增 `cargo test -p ui-flip-card flip_card_file_placement_discipline_is_strict_for_component_scope`。执行证据命令在当前环境仍受 `Invalid cross-device link (os error 18)` 阻断，阻断点位于依赖写入阶段而非文件落点契约本身。）
- [x] Hyper-Structure Builder（`spec.rs`）：复杂组件必须提供 AI 友好的 `*Spec::new()...render()` 建造者 API。（N/A：`FlipCard` 当前不属于复杂 schema 驱动组件，不存在稳定外部 Schema/Builder 契约需求；组件目录保持 `mod.rs/logic.rs/styles.rs/view.rs/motion.rs`，且 `src/spec.rs` 不存在，不引入 `*Spec::new()...render()` 链路。回归：`components/flip-card/test/semantics.rs::flip_card_hyper_structure_builder_spec_is_not_applicable_for_simple_component`；门禁：`scripts/check-ui-components-component-files.sh` 新增 `cargo test -p ui-flip-card flip_card_hyper_structure_builder_spec_is_not_applicable_for_simple_component`。执行证据命令在当前环境仍受 `Invalid cross-device link (os error 18)` 阻断，阻断点位于依赖写入阶段而非 Hyper-Structure Builder 契约本身。）
- [x] 上下文压缩协议（Manifest + RBI）：新增/大改组件必须同步维护组件目录下 `Component.toml`（能力清单）和 `.rbi`（接口签名投影），避免 AI 检索工具箱过时。（`components/flip-card/src/Component.toml` 与 `components/flip-card/src/flip_card.rbi` 已同步维护；`Component.toml` 覆盖 `FlipCard` 输入输出轴、能力清单与依赖映射，`.rbi` 提供 `FlipCard` 公共签名与核心类型投影，避免 AI 检索漂移。回归：`components/flip-card/test/semantics.rs::flip_card_context_compression_manifest_and_rbi_projection_are_present_and_current`；门禁：`scripts/check-ui-components-component-files.sh` 新增 `cargo test -p ui-flip-card flip_card_context_compression_manifest_and_rbi_projection_are_present_and_current`。执行证据命令在当前环境仍受 `Invalid cross-device link (os error 18)` 阻断，阻断点位于依赖写入阶段而非上下文压缩契约本身。）
- [x] 语义标记统一升级为 Agent Contract（Schema 化），让 Agent 不依赖 DOM 猜测理解组件状态与意图。（`components/flip-card/src/logic.rs` 新增类型化 Agent Contract（`FlipCardAgent{SchemaVersion/Intent/Action/State/Source/ConfigPolicy}` + `resolve_agent_contract`），`components/flip-card/src/view.rs` 挂载稳定 `data-ui-schema/data-ui-schema-version/data-ui-intent/data-ui-action/data-ui-state/data-ui-source` 与来源轴标记（`data-ui-flipped-source/data-ui-mode-source/data-ui-motion-source/data-ui-class-source/data-ui-id-source`）；`components/flip-card/src/Component.toml` 补充 `agent-contract-markers`、`agent_contract_schema_markers`、`[[agent_contract]]` 与 `[[agent_contract_whitelist]]`。回归：`components/flip-card/test/semantics.rs::{flip_card_check2_documents_agent_contract_schema_governance_rules,flip_card_agent_contract_is_schema_typed_and_machine_readable,flip_card_agent_contract_fields_are_type_derived_without_free_form_schema_string_splicing,flip_card_agent_contract_render_path_is_whitelist_safe_and_script_injection_free,flip_card_contract_hygiene_script_covers_agent_contract_schema_guards}`；门禁：`scripts/check-ui-components-contract-hygiene.sh` 新增 4 条 `cargo test -p ui-flip-card ...`。执行证据命令在当前环境仍受 `Invalid cross-device link (os error 18)` 阻断，阻断点位于依赖写入阶段而非 Agent Contract 契约本身。）
  - 关键交互组件必须输出稳定机器可读语义（至少 `data-*` + 状态来源标记；复杂组件建议补 `data-ui-schema`）。
  - Agent 消费字段应来自类型化 schema 生成，不允许散落字符串拼接。
  - 契约字段需可追溯到组件状态轴与动作语义（intent/action/state/source）。
  - 配置到组件的渲染链路必须走白名单能力边界，禁止任意脚本注入。
- [x] 流式在这里仅指 LLM 输出渲染（只看两种显示模式）。（`N/A（组件级）`：`FlipCard` 是交互展示组件，不承载 LLM 正文 token 流渲染面；仅消费归一化后的本地状态并输出稳定语义标记，不引入流式协议字段。流式定义仍固定为两种显示模式：`Streaming`（边生成边显示）与 `Snapshot`（完整生成后一次显示）。回归：`components/flip-card/test/semantics.rs::flip_card_check2_documents_streaming_definition_is_llm_output_only_with_two_modes`；门禁：`scripts/check-ui-components-streaming.sh` 新增 `cargo test -p ui-flip-card flip_card_check2_documents_streaming_definition_is_llm_output_only_with_two_modes`。执行证据命令在当前环境仍受 `Invalid cross-device link (os error 18)` 阻断，阻断点位于依赖写入阶段而非流式定义契约本身。）
  - `Streaming`：LLM 还在生成，界面边生成边显示。
  - `Snapshot`：LLM 全部生成完成后，一次性显示。
- [x] `Snapshot` 是所有组件的基础能力（默认必须支持）。（已满足：`FlipCard` 只消费上层提供的完整配置（`front/back` 必填 + 可选 `is_flipped/default_is_flipped/flip_mode/is_disabled/motion/id/class/lang/dir`），在 `logic.rs` 一次归一后稳定渲染，不依赖流式增量输入即可输出连续可验证语义标记（`role/aria-*` 与 `data-state/data-flip-mode/data-*-source`）。回归：`components/flip-card/test/semantics.rs::flip_card_snapshot_baseline_consumes_complete_result_and_renders_stably`；门禁：`scripts/check-ui-components-streaming.sh` 新增 `cargo test -p ui-flip-card flip_card_snapshot_baseline_consumes_complete_result_and_renders_stably`。执行证据命令在当前环境仍受 `Invalid cross-device link (os error 18)` 阻断，阻断点位于依赖写入阶段而非 Snapshot 基线契约本身。）
  - 所有组件都应能消费“完整生成结果”并稳定渲染。
  - 即使组件不直接展示正文，也应能在接收上层完整配置后正常渲染。
- [x] `Streaming` 是否强制，按组件职责判断（不能一刀切）。（已满足：`FlipCard` 非正文阅读面组件，按 `Streaming Optional` 处理并固定 `fallback=snapshot`；组件继续只消费完整配置并稳定输出 `role/aria-*` 与 `data-*` 语义标记，不承载 token 流协议。`草稿/已验证/可提交` 状态标识以及数据校验、断线恢复、重试策略由上层流程负责，组件层只负责稳定渲染。回归：`components/flip-card/test/semantics.rs::{flip_card_check2_documents_streaming_required_optional_classification_rules,flip_card_streaming_optional_scope_keeps_role_aria_and_data_markers_continuous,flip_card_streaming_validation_retry_resilience_boundaries_stay_outside_component_layer,flip_card_streaming_script_covers_required_optional_classification_contract}`；门禁：`scripts/check-ui-components-streaming.sh` 新增 3 条 `cargo test -p ui-flip-card ...`。执行证据命令在当前环境仍受 `Invalid cross-device link (os error 18)` 阻断，阻断点位于依赖写入阶段而非 Streaming 分类契约本身。）
  - `Streaming Required`：组件本体就是正文阅读面，用户需要边生成边看。
  - `Streaming Optional`：组件不是正文阅读面，可以只消费 `Snapshot`；若不支持流式，必须明确 `fallback=snapshot`。
  - 无论是否支持 `Streaming`，都要显式标识当前输出状态（草稿/已验证/可提交），并保持 `role`/`aria-*`/`data-*` 连续可读。
  - 数据校验、断线恢复、重试策略由上层负责，组件层只负责稳定渲染。

### 7. 测试、门禁与交付
- [x] 代码卫生（Rust Hygiene）：非测试代码中完全禁止 `unwrap/expect`，禁止无处理的 `let _ = ...`；字符串复制热点收敛为 `Cow<'static, str>`（执行 `./scripts/check-rust-hygiene.sh` 验证）。（已满足：`components/flip-card/src/{mod,logic,view,styles,motion}.rs` 非测试源码已核验无 `unwrap()/expect()` 与 `let _ = ...`；`components/flip-card/src/logic.rs::compose_class_name` 已收敛到 `Vec<Cow<'static, str>>`（静态类名 `Cow::Borrowed`，外部类名 `Cow::Owned`）。回归：`components/flip-card/test/semantics.rs::flip_card_rust_hygiene_contract_is_enforced_for_non_test_sources`；门禁：`scripts/check-ui-components-contract-hygiene.sh` 新增 `cargo test -p ui-flip-card flip_card_rust_hygiene_contract_is_enforced_for_non_test_sources`。已执行 `./scripts/check-rust-hygiene.sh`，当前环境因 `rg` 构建缺少 PCRE2 且 `check-api-contracts` baseline drift 失败；另 `cargo test` 在依赖写入阶段触发 `Invalid cross-device link (os error 18)`，均属环境阻断而非组件契约违约。）
- [x] Tree Shaking & 特性剪裁：组件必须注册到 `ui-components` 特性树（如 `component-accordion`）；`css.rs` 和 `lib.rs` 聚合必须受 feature 门控，禁止无条件全局依赖。（已满足：`crates/ui-components/Cargo.toml` 已注册 `component-flip_card = ["dep:ui-flip-card"]`，并由 `all-components`/`web-demo-components` 按需聚合；`crates/ui-components/src/lib.rs` 通过 `#[cfg(feature = "component-flip_card")] pub use ui_flip_card as flip_card;` 条件导出；`crates/ui-components/src/css.rs` 通过 `#[cfg(feature = "component-flip_card")] out.push_str(crate::flip_card::styles::CSS);` 条件聚合样式，未出现无条件全量依赖。回归：`components/flip-card/test/semantics.rs::{flip_card_tree_shaking_keeps_component_feature_and_css_boundaries,flip_card_tree_shaking_script_enforces_component_minimal_feature_tree_and_budget,flip_card_check2_marks_tree_shaking_feature_pruning_contract_complete}`；门禁：`scripts/check-ui-components-tree-shaking.sh` 新增 `cargo test -p ui-flip-card ...` 与 `FLIP_CARD_MIN_FEATURES=\"component-flip_card,inject-css\"` 特性树/wasm 校验。已执行 `cargo tree -e features -i ui-components -p ui-components --no-default-features --features component-flip_card,inject-css`（仅命中 `component-flip_card/inject-css`）与 `cargo tree -e features -i ui-components -p web-demo`（命中 `web-demo-components`，未见 `all-components`）；`cargo test/cargo check` 在当前环境仍受 `Invalid cross-device link (os error 18)` 阻断，阻断点位于依赖写入阶段。）
- [x] 语义测试与性能回归：断言必须覆盖 `aria-*`、`data-*` 与焦点流转，不能只看快照；高频/重型组件必须补齐 `render_count` 断言/测量（如初始化空闲预算为 1）。（已满足：语义契约覆盖由 `components/flip-card/test/semantics.rs::{flip_card_semantic_contract_matrix_is_covered_without_snapshot_dependency,flip_card_semantics_and_performance_regression_cover_aria_data_focus_and_render_count_measurement}` 锁定，断言 `role/aria-*`、稳定 `data-*` 标记与键盘/焦点路径（`on:keydown/on:focusin/on:focusout`）并明确非 snapshot-only；性能回归由 `flip_card_performance_governance_budget_is_defined_and_blocking` 锁定 `UiPerfBudget("flip-card": mount/update/heap)` 与 perf probe 可观测标记。`render_count` 当前按仓库统一策略采用等价证据并在 `docs/plan/TODO.md` 持续跟踪自动化补齐（`perf_render_count_follow_up_is_tracked_in_plan`）。门禁：`scripts/check-ui-components-performance.sh` 增加 `cargo test -p ui-flip-card flip_card_semantics_and_performance_regression_cover_aria_data_focus_and_render_count_measurement`。执行证据命令在当前环境仍受 `Invalid cross-device link (os error 18)` 阻断（依赖写入阶段），不影响本条契约静态校验结论。）
- [x] 版本弃用迁移（Codemod/Registry）：若提交包含跨大版本 API 破坏升级，必须在 Schema Registry 注册弃用窗口并提供纯函数迁移层（`migrate_v1_to_v2`）。（N/A：本次 `FlipCard` 改动未引入跨大版本 API 破坏升级，组件 Agent Contract 仍保持 `v1`（`components/flip-card/src/logic.rs` 的 `FlipCardAgentSchemaVersion::V1`，以及 `components/flip-card/src/Component.toml` 的 `schema_version = "1"` 与 `ui.flip-card.agent-contract.v1`），因此不触发 Codemod/Schema Registry 弃用窗口与 `migrate_v1_to_v2` 迁移层要求。回归：`components/flip-card/test/semantics.rs::flip_card_version_deprecation_migration_registry_is_explicitly_na_without_major_breaking_upgrade`；门禁脚本：`scripts/check-ui-components-engineering.sh` 新增对应 `cargo test` 目标。执行证据命令在当前环境仍受 `Invalid cross-device link (os error 18)` 阻断，阻断点位于依赖写入阶段而非版本弃用迁移契约本身。）
- [x] 文档即产品（Copy-Paste Ready）：`apps/docs-app` 必须新增 Playground（Hello World、状态矩阵、受控/非受控对照），支持流式/快照展现，并提供 Source-first 一键复制且补全 imports。（已满足：`apps/docs-app/src/pages/components/pages/display_extra.rs::flip_card()` 已补齐文档验收面，包含 `Hello World (Default Path)`、`State Matrix (Default / Hover / Disabled / Dramatic Motion)`、`Controlled vs Uncontrolled Contrast`、`Streaming / Snapshot Contract`、`Source-first Starter (Copy-Paste Ready)`；并新增 `Source-first / Copy-Paste Ready Contract` 区块，明确复制路径依赖 `apps/docs-app/src/playground.rs::compose_copy_ready_code` 自动补全 imports。回归：`components/flip-card/test/semantics.rs::flip_card_docs_are_copy_paste_ready_with_hello_world_state_matrix_and_streaming_snapshot`；门禁脚本：`scripts/check-ui-components-dx.sh` 新增 `cargo test -p ui-flip-card flip_card_docs_are_copy_paste_ready_with_hello_world_state_matrix_and_streaming_snapshot`。执行证据命令在当前环境仍受 `Invalid cross-device link (os error 18)` 阻断，阻断点位于依赖写入阶段而非文档契约本身。）
- [x] 语义测试优先：验证 `data-*` / `aria-*` / role / 状态来源契约，不只视觉快照。（`FlipCard` 已以语义契约断言为主：`components/flip-card/test/semantics.rs::{flip_card_view_uses_motion_and_state_contracts,flip_card_observability_markers_are_stable_and_enumerated,flip_card_a11y_i18n_contracts_are_headless_and_text_free,flip_card_semantic_contract_matrix_is_covered_without_snapshot_dependency,flip_card_semantics_suite_is_contract_first_not_snapshot_only,flip_card_semantic_markers_changed_in_view_must_be_covered_by_semantics_checks}` 覆盖 `role/aria/data-*` 与状态来源标记；组件本地语义测试落位并纳管：`components/flip-card/src/mod.rs` 通过 `#[cfg(test)] #[path = "../test/mod.rs"] mod tests;`，且 `components/flip-card/test/mod.rs` 挂载 `mod semantics;`。断言策略明确禁止“只看快照”：语义测试与 `test/logic.rs` 均不依赖 snapshot-only 宏，快照仅作补充。门禁脚本已接入 `scripts/check-ui-components-contract-hygiene.sh`：`cargo test -p ui-flip-card flip_card_check2_documents_semantics_first_testing_rules`、`cargo test -p ui-flip-card flip_card_semantics_suite_is_contract_first_not_snapshot_only`、`cargo test -p ui-flip-card flip_card_semantic_markers_changed_in_view_must_be_covered_by_semantics_checks`。执行证据命令在当前环境仍受 `Invalid cross-device link (os error 18)` 阻断，阻断点位于依赖写入阶段而非语义优先契约本身。）
  - 每个交互组件至少有对应 `*_semantics.rs` 测试覆盖关键状态轴与动作语义。
  - 断言应聚焦语义契约（状态来源/可访问性/键盘路径），快照仅作补充。
  - 新增/变更语义字段必须同步补测试，否则不得打勾。
- [x] E2E 选择器稳定：使用语义标记，WASM 场景有稳定等待策略。（已满足：`e2e/tests/docs_app_flip_card_contract.spec.mjs` 新增 `docs-app flip-card uses semantic selectors with wasm-safe stable readiness waits` 与 `docs-app flip-card animation path covers ready and settled semantic breakpoints`，定位完全基于稳定语义锚点 `#docs-flip-card` / `#docs-flip-card-toggle` / `#docs-flip-card-disabled` + `data-slot/data-ui-*`，并以 `body:not(:has(#boot))` 与语义属性断言作为就绪/收敛等待，未使用 `waitForTimeout`/固定 sleep。为避免脆弱 DOM 层级猜测，`apps/docs-app/src/pages/components/pages/display_extra.rs::flip_card()` 已为 toggle/disabled 场景补齐稳定 id 锚点（`docs-flip-card-toggle`、`docs-flip-card-disabled`）；动画路径显式覆盖 hover enter/leave、keyboard toggle 与 disabled no-op 的 ready/settled 语义断点。回归：`components/flip-card/test/semantics.rs::{flip_card_check2_documents_e2e_selector_and_stable_wait_rules,flip_card_e2e_selector_contract_uses_semantic_markers_and_stable_waits,flip_card_e2e_animation_path_covers_ready_and_settled_semantic_breakpoints}`；门禁脚本：`scripts/check-ui-components-e2e-flip-card.sh`。执行证据命令在当前环境仍受 `Invalid cross-device link (os error 18)` 阻断，阻断点位于依赖写入阶段而非 E2E 语义契约本身。）
  - E2E 选择器优先 `data-*` 语义标记，禁止依赖脆弱 DOM 层级或文本定位。
  - WASM 场景必须使用稳定等待策略（语义状态就绪而非固定 sleep）。
  - 若组件涉及异步/动画，E2E 需显式覆盖 ready/settled 条件。
- [x] 关键流程纳入可重复回归集合（Playwright/Cypress）。（已满足：`e2e/tests/docs_app_flip_card_contract.spec.mjs` 已新增 `docs-app flip-card key flow is repeatable and failures map to semantic breakpoints`，通过 `runToggleKeyFlow` 在同一语义断点序列（`data-ui-action/data-state/data-ui-state/aria-pressed`）执行并在 `page.reload()` 后重放，满足“至少一条可重复关键流程”；同时新增 `docs-app flip-card high-risk paths keep focus keyboard and disabled branches semantically explicit`，优先覆盖 focus/keyboard 高风险路径，并在 disabled 分支验证 no-op 收敛（`data-ui-action=snapshot-render`）。失败定位基于稳定语义契约断点而非页面截图差异：关键断点包括 `data-ui-action=focus|toggle|blur|snapshot-render`、`data-state=default|flipped`、`data-ui-state=default|flipped|disabled`、`aria-disabled=true`。回归：`components/flip-card/test/semantics.rs::{flip_card_check2_documents_e2e_repeatable_key_flow_rules,flip_card_e2e_key_flow_is_repeatable_and_failure_points_are_semantic,flip_card_e2e_high_risk_paths_cover_focus_keyboard_and_settled_semantic_breakpoints}`；门禁脚本：`scripts/check-ui-components-e2e-flip-card.sh`。执行证据命令在当前环境仍受 `Invalid cross-device link (os error 18)` 阻断，阻断点位于依赖写入阶段而非可重复回归契约本身。）
  - 至少定义一条可重复关键流程（打开/交互/关闭或提交）纳入 E2E 回归。
  - 回归失败需可定位到具体语义契约断点，而不是笼统“页面不一致”。
  - 高风险路径（overlay、focus、keyboard、async）优先进入回归集合。
- [x] docs-app 文档、示例、参数矩阵、状态矩阵同步更新。（已满足：`apps/docs-app/src/pages/components/pages/display_extra.rs::flip_card()` 已同步补齐文档契约，保留状态矩阵与受控/非受控对照（`State Matrix (Default / Hover / Disabled / Dramatic Motion)`、`Controlled vs Uncontrolled Contrast`），并新增参数矩阵区块 `data-slot="flip-card-parameter-matrix"`（`data-slot="flip-card-parameter-rows"`）明确 API 与默认值/优先级：`default_is_flipped > default_flipped > DEFAULT_FLIPPED(false)`、`is_disabled > disabled > DEFAULT_DISABLED(false)`、`flip_mode > is_flip_on_hover > flip_on_hover > DEFAULT_HOVER_FLIP(false)`、`id -> IdProvider::next_prefixed_id(DEFAULT_ID_PREFIX)`、`motion -> FlipCardMotion::default()`。这些文档值与逻辑实现对齐：`components/flip-card/src/logic.rs` 的 `normalize_flipped_axis` 与 `crates/ui-state-primitives/src/flip_card.rs` 的 `normalize_behavior_flags` 默认路径保持一致。回归：`components/flip-card/test/semantics.rs::{flip_card_check2_documents_docs_examples_parameter_state_matrix_sync_rules,flip_card_docs_examples_parameter_state_matrix_sync_with_logic_defaults,flip_card_dx_script_covers_docs_examples_parameter_state_matrix_sync_contract}`；门禁脚本：`scripts/check-ui-components-dx.sh` 已新增 `cargo test -p ui-flip-card flip_card_docs_examples_parameter_state_matrix_sync_with_logic_defaults`。执行证据命令在当前环境仍受 `Invalid cross-device link (os error 18)` 阻断，阻断点位于依赖写入阶段而非文档同步契约本身。）
  - 组件行为或参数变更必须同步更新 `apps/docs-app` 示例与说明。
  - 文档示例需覆盖至少一组状态矩阵（受控/非受控、disabled、size/variant 等）。
  - 文档中的 API 名称与默认值必须和 `logic.rs` 当前实现一致。
- [x] 组件文档必须对新手友好（Documentation as Product）：组件 README 或等价文档入口必须存在。（已满足：`components/flip-card/src/README.md` 作为组件文档入口存在，且补齐新手路径结构：`Hello World（最小可用）` + `新手路径（先用起来，再进阶）` + `常见用法（进阶）`，明确“默认 API 先于高级控制”，并避免要求先理解 `ui-state-primitives/ui-headless` 分层。docs-app 对应入口持续存在：`apps/docs-app/src/pages/components/pages/display_extra.rs::flip_card()` 与 `apps/docs-app/src/pages/components/pages.rs` 的 `component_doc!("FlipCard", "flip-card", "Display", display_extra::flip_card)`；页面顺序保持 `Hello World (Default Path)` 在前，状态矩阵/受控对照/交互工作台在后。回归：`components/flip-card/test/semantics.rs::{flip_card_check2_documents_documentation_as_product_rules,flip_card_docs_entry_exists_as_readme_or_equivalent_docs_app_page,flip_card_docs_are_beginner_friendly_with_default_then_advanced_path,flip_card_docs_hello_world_snippet_is_zero_threshold_and_not_architecture_wiring,flip_card_contract_hygiene_script_covers_documentation_as_product_contract}`；门禁脚本：`scripts/check-ui-components-contract-hygiene.sh` 已新增对应 `cargo test -p ui-flip-card ...` 命令。执行证据命令在当前环境仍受 `Invalid cross-device link (os error 18)` 阻断，阻断点位于依赖写入阶段而非文档契约本身。）
  - 每个基础组件必须提供“零门槛”最小示例（Hello World）与常见用法，避免要求用户先理解底层分层架构。
  - 文档需明确“先用起来，再进阶”：默认 API 路径在前，高级控制参数在后。
  - “只有源码没有文档”或“只写给架构师/机器看的文档”视为不通过。
- [x] `apps/docs-app` 必须提供 Interactive Playground：用户可在线修改 props/状态并实时预览。（已满足：`apps/docs-app/src/pages/components/pages/display_extra.rs::flip_card()` 已提供 `Interactive Playground (展示 / Config / Code / CSS Test)`，通过 `controls` 暴露 `default_is_flipped/is_flip_on_hover/is_disabled/custom id/custom class/motion preset` 在线调节；展示区 `FlipCard` 直接消费 `workbench_*` signals 并实时回显，`test_config_signal=workbench_config` + Code/CSS Test 面板同步输出当前配置与语义标记，满足 props/状态在线修改与实时预览。关键交互复用可重复语义流程：`e2e/tests/docs_app_flip_card_contract.spec.mjs::{docs-app flip-card key flow is repeatable and failures map to semantic breakpoints,docs-app flip-card high-risk paths keep focus keyboard and disabled branches semantically explicit}`。AI Spec 联动按范围标注 `N/A`：`FlipCard` 当前无 `spec.rs`/`*Spec::new()...render()` 契约（见 `components/flip-card/test/semantics.rs::flip_card_engineering_contract_marks_spec_serde_path_as_na_for_simple_component_scope`），因此不引入额外 Spec 输入联动面板。回归：`components/flip-card/test/semantics.rs::{flip_card_check2_documents_interactive_playground_rules,flip_card_docs_app_provides_interactive_playground_for_props_state_and_preview,flip_card_interactive_playground_reuses_repeatable_semantic_e2e_flow,flip_card_dx_check_script_covers_interactive_playground_contract}`；门禁脚本：`scripts/check-ui-components-dx.sh`。执行证据命令在当前环境仍受 `Invalid cross-device link (os error 18)` 阻断，阻断点位于依赖写入阶段而非 Interactive Playground 契约本身。）
  - Playground 至少支持基础 props 调整、状态切换、交互反馈观察。
  - 对 AI Spec 相关组件，至少提供一组 Spec 输入与预览输出的联动示例。
  - Playground 作为验收面，需可重复复现关键交互路径。
- [x] Source-first 文档必须 Copy-Paste Ready：提供一键复制组件源码或最小可用片段能力。（已满足：`apps/docs-app/src/pages/components/pages/display_extra.rs::flip_card()` 已提供 `Source-first Starter (Copy-Paste Ready)` 与 `data-slot="flip-card-source-first-contract"` 契约区块，明确 `Show code` 复制路径经 `apps/docs-app/src/playground.rs::compose_copy_ready_code` 自动补全 imports；文档同时给出真实源码落点 `components/flip-card/src/{mod,logic,view,styles,motion}.rs` 与依赖前提 `ui-components = { default-features = false, features = ["component-flip_card", "inject-css"] }`，避免“复制即报错”。复制按钮能力由 `components/code-block/src/view.rs` 的 `ui-code-block__copy-button` 挂载。文档与实现同步证据：source-first 示例使用 `is_flip_on_hover`，并与 `components/flip-card/src/view.rs` props 契约一致。回归：`components/flip-card/test/semantics.rs::{flip_card_check2_documents_source_first_copy_paste_ready_rules,flip_card_docs_source_first_copy_paste_ready_with_real_paths_and_dependencies,flip_card_dx_check_script_covers_source_first_copy_paste_ready_contract,flip_card_check2_marks_source_first_copy_paste_ready_contract_complete}`；门禁脚本：`scripts/check-ui-components-dx.sh`。执行证据命令在当前环境仍受 `Invalid cross-device link (os error 18)` 阻断，阻断点位于依赖写入阶段而非 Source-first 文档契约本身。）
  - docs-app 页面应提供复制按钮，输出代码默认可直接运行（含必要 imports/依赖提示）。
  - 若为 source-first 组件，文档需指向真实源码落点并说明依赖前提，避免“复制即报错”。
  - 文档代码与当前实现必须同步，防止示例漂移。
- [x] HeroUI 对标文档与组件文档同步：参数模型变更需同步 `docs/spec/heroui-parameter-design-strategy.md`（必要时补充 `docs/research/spectrum-heroui-style-interface-study.md`），并保证组件文档可访问。（已满足：`docs/spec/heroui-parameter-design-strategy.md` 新增 `### FlipCard 同步记录（2026-02-20）`，同步记录 `FlipCard` 参数主轴、默认值归一落点（`components/flip-card/src/logic.rs` + `crates/ui-state-primitives/src/flip_card.rs`）、docs 入口（`apps/docs-app/src/pages/components/pages.rs` 的 `component_doc!("FlipCard", "flip-card", "Display", display_extra::flip_card)` 与 `apps/docs-app/src/pages/components/pages/display_extra.rs::flip_card()`）、以及 README 等价入口 `components/flip-card/src/README.md`；并明确“本轮不需要追加 `docs/research/spectrum-heroui-style-interface-study.md`”。回归：`components/flip-card/test/semantics.rs::{flip_card_check2_documents_heroui_benchmark_docs_sync_rules,flip_card_heroui_strategy_and_component_docs_are_synchronized_and_indexable,flip_card_dx_check_script_covers_heroui_benchmark_docs_sync_contract,flip_card_check2_marks_heroui_benchmark_docs_sync_contract_complete}`；门禁脚本：`scripts/check-ui-components-dx.sh`。执行证据命令在当前环境仍受 `Invalid cross-device link (os error 18)` 阻断，阻断点位于依赖写入阶段而非 HeroUI 文档同步契约本身。）
  - 若参数语义发生变化，需同步更新对标策略文档，不允许实现先漂移文档后补。
  - 组件文档入口必须存在（docs-app 页面或等价文档），且可被索引定位。
  - “仅代码更新无文档更新”在接口变更场景下直接判不通过。

### 8. 合并前门禁死命令（最终执行）
在发起 PR 或完成任务前，必须保证本地/CI 以下命令全部通过：

- `cargo fmt --all -- --check`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `./scripts/check-rust-hygiene.sh`
- `cargo check -p ui-components --target wasm32-unknown-unknown`
- `cargo check -p ui-headless --no-default-features --features ssr`
- `cargo check -p ui-components --target wasm32-unknown-unknown --no-default-features --features component-<your_component>,inject-css`

依据文档（`rust-ui/docs/spec` 及 `rust-ui/docs`）：

- `rust-ui/docs/spec/ai_context_projection_protocol.md`
- `rust-ui/docs/spec/architectural_fitness_functions.md`
- `rust-ui/docs/spec/async_state_as_data_command.md`
- `rust-ui/docs/spec/collection_registration_protocol.md`
- `rust-ui/docs/spec/compile_time_evolution_migration.md`
- `rust-ui/docs/spec/component_boundaries.md`
- `rust-ui/docs/spec/component_domains.md`
- `rust-ui/docs/spec/controlled_evolution_sandbox.md`
- `rust-ui/docs/spec/core_shell_protocol_infra_baseline.md`
- `rust-ui/docs/spec/environment_subscription_streams.md`
- `rust-ui/docs/spec/event_light_cone_signal_bus.md`
- `rust-ui/docs/spec/focus_global_stack_gc.md`
- `rust-ui/docs/spec/foreign_zone_escape_hatches.md`
- `rust-ui/docs/spec/headless_purification.md`
- `rust-ui/docs/spec/heroui-parameter-design-strategy.md`
- `rust-ui/docs/spec/hyper-structure-ui-development-playbook.md`
- `rust-ui/docs/spec/i18n.md`
- `rust-ui/docs/spec/intent_stack_semantic_layering.md`
- `rust-ui/docs/spec/kernel_shell_architecture.md`
- `rust-ui/docs/spec/macro_micro_dual_state_machine.md`
- `rust-ui/docs/spec/motion.md`
- `rust-ui/docs/spec/mvp.md`
- `rust-ui/docs/spec/platform_abdication_ecosystem.md`
- `rust-ui/docs/spec/README.md`
- `rust-ui/docs/spec/release_versioning.md`
- `rust-ui/docs/spec/side_effect_command_pattern.md`
- `rust-ui/docs/spec/slot_projection_strategy.md`
- `rust-ui/docs/spec/ssr_hydration_discontinuity.md`
- `rust-ui/docs/spec/state_primitives_core_satellite_split.md`
- `rust-ui/docs/spec/style_island_defense.md`
- `rust-ui/docs/spec/styling.md`
- `rust-ui/docs/spec/tree_shaking.md`
- `rust-ui/docs/spec/ui_layout_split.md`
- `rust-ui/docs/spec/ui_physics_two_pass_rendering.md`
- `rust-ui/docs/spec/unified_causality_bus.md`
- `rust-ui/docs/spec/wasm_generic_bloat.md`
- `rust-ui/docs/起点_也即是目的.md`
- `rust-ui/docs/DOCS_GOVERNANCE.md`
- `rust-ui/docs/DOCS_INDEX.md`
- `rust-ui/docs/philosophy.md`
- `rust-ui/docs/README.md`
- `rust-ui/docs/RULES_ZH.md`
