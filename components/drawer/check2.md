# 单组件 Check List（Hyper-Structure 完整版）

> 用途：每次新增、重构或修改一个组件时，必须按本清单逐项检查并在 PR 中附带结果。  
> 执行顺序：先过第 1-2 节（架构与状态），再进入第 3-5 节（具体实现）。  
> 术语统一：`status-primitives` 指状态原语层（当前 crate 名为 `ui-state-primitives`）。

### 0. 适用范围与顺序纪律
本清单仅评估“一个组件”的改动结果，不替代仓库级治理。
先过第 1-2 节（架构与状态）再进入第 3-5 节（具体实现）。
不适用的条目必须明确标注 `N/A` 并说明理由，禁止机械打勾。
组件目标、非目标、风险边界已写清楚；发现跨组件/跨层系统性问题时升级为仓库级任务。

### 1. 架构边界与分层约束（Kernel/Shell 总线）
- [x] `status-primitives` 定义：纯状态原语层（受控/非受控、toggle、selection、list、overlay open state、expansion 等）。不依赖 Leptos/DOM/web-sys；只包含 Rust 数据结构和方法，不含视图与事件绑定。（`DrawerSlot/DrawerPartStateInput/DrawerPartState + normalize/resolve` 已下沉至 `crates/ui-state-primitives/src/drawer.rs`，并通过 `crates/ui-state-primitives/src/test/drawer.rs` 回归；组件 `components/drawer/src/logic.rs` 仅 `pub use` primitive + `to_sheet_placement` 桥接与 class 组装。）
  - 所有状态原语必须从 `status-primitives`（`ui-state-primitives`）获取，组件层只能消费，不得自造。
  - 下沉判定依据是“稳定状态不变量”；凡属于状态机、归一化、状态派生能力，默认先进入 `ui-state-primitives`。
  - 组件中可保留的仅是装配逻辑：props 归一、样式来源标记、slot 组织、对 `ui-state-primitives` 输出的映射。
  - 组件内若出现状态原语实现（受控/非受控状态机、single/multiple 展开规则、索引归一化、跨事件状态派生），该项直接判不通过。
  - 处理方式固定：先下沉到 `ui-state-primitives/src/<capability>.rs`（如 `expansion.rs`），在 `ui-state-primitives/src/lib.rs` 导出，再回到组件改调用。
  - 下沉后的原语必须有 `ui-state-primitives` 单元测试；组件侧只保留调用与语义挂载测试。
  - 桥接规范：`ui-state-primitives` 结构体必须是 POJO（Plain Old Rust Object），不持有 Leptos `Signal` 或框架绑定状态容器。
  - 消费规范：`ui-headless` 或组件 `logic.rs` 负责解包 `Signal` 当前值传入 primitive 方法，并将结果显式写回 `Signal`。
  - 设计理由：保持 primitives 纯粹可测、可迁移，不与特定响应式库绑定（便于未来替换响应式实现与做纯 Rust 测试）。
- [x] `ui-headless` 定义：交互与 A11y 原语层（press/focus/hover/roving/listbox/menu/tooltip 等），把输入设备事件与状态语义标准化为可复用契约；输出必须是类型化 `attrs + handlers + state`。不做样式、不写组件 CSS、不做组件级动效编排。（`Drawer` 本体不重写键盘/焦点/ARIA 语义，`view.rs` 仅组合 `<Sheet ...>`；交互/A11y 契约由 `components/sheet/src/view.rs` 内部消费 `ui-headless::{use_focus_trap,use_modal,use_overlay_stack_registration,overlay_dialog_attrs}` 提供。回归：`components/drawer/test/drawer_semantics.rs::drawer_delegates_interaction_and_a11y_to_sheet_headless_contract`。）
  **`ui-headless` 落位硬规则（必须执行）**：
  - 输入边界：消费 `status-primitives` 状态 + 用户输入事件（keyboard/pointer/focus）+ 环境能力（web/ssr）。
  - 输出边界：只输出语义契约（attrs/handlers/state）；组件层只负责挂载与组合，不得把语义判断塞回 `view.rs`。
  - 下沉判定依据是“交互/A11y 语义契约”；凡属于键盘/焦点/指针归一、ARIA 映射、交互状态语义能力，默认先进入 `ui-headless`。
  - 必须下沉：键盘模型、焦点模型、跨设备输入归一、ARIA 状态映射、overlay/presence 等交互语义。
  - A11y 契约与共享工具落点固定在 `crates/ui-headless/src/a11y.rs`；组件只在 `view.rs` 挂载，不在组件层重写。
  - 语义契约必须提供 `lang` / `dir`（LTR/RTL）接入能力；headless 不硬编码用户可见文本，文案由 i18n/l10n 层提供。
  - 语义契约正确性必须有回归：`components/*/test/**` 断言语义标记，`e2e/tests/*` 覆盖关键交互流程。
  - 禁止放在 `ui-headless`：视觉 class 选择、CSS 规则、组件 slot 布局、组件专属动效编排、业务文案。
  - 允许留在组件层：纯视觉一次性交互且不形成可复用语义契约（例如单组件局部微交互）。
- [x] `ui-motion` 定义：动效能力与契约执行层（spring、keyframes、WAAPI/RAF backend），只负责时间函数、插值与运行时驱动，不承载组件业务语义与状态决策。（`Drawer` 仅保留 `DrawerMotion { sheet }` 与 `sanitize_motion -> ui_sheet::motion::sanitize_motion`，不内嵌 spring/driver；运行时 attach、`prefers-reduced-motion` 与 non-wasm no-op 固定在 `components/sheet/src/motion.rs` + `crates/ui-motion/src/lib.rs`。回归：`components/drawer/test/drawer_semantics.rs::drawer_motion_layer_boundaries_delegate_runtime_to_sheet_and_ui_motion`。）
  - 放在 `crates/ui-motion`：通用动画数学与执行后端（spring solver、keyframe sampling、easing registry、driver adapters），以及 `wasm/non-wasm` 适配与 `reduced-motion` 执行策略。
  - 放在 `crates/ui/src/<component>/motion.rs`：把组件语义状态（open/closed、enter/exit、active/inactive）映射为 `ui-motion` contract，绑定目标节点并调用 attach。
  - 禁止放在 `crates/ui-motion`：组件 slot 结构、组件专属状态机、ARIA/keyboard 语义、业务文案与业务分支。
  - 禁止放在组件 `motion.rs`：自实现 spring/keyframe/driver 执行器；跨组件共享动效算法必须回迁 `ui-motion`。
  - 动效参数优先来自 token/theme；禁止在组件样式与逻辑中散落硬编码时长/曲线/位移常量。
  - 非 wasm 路径必须提供 no-op/stub，保证 SSR/tooling 可编译且行为可预测。
- [x] `ui-theme` 定义：唯一设计 token 与主题上下文层（system/color/scale + Light/Dark/OLED），负责 token 分类、主题映射与 CSS 变量生成。（`crates/ui-theme/src/tokens.rs` 持有 token 分类基线（含 `TypographyTokens`/`OverlayLayoutTokens`），`crates/ui-theme/src/theme.rs` 定义 `ThemeSystem/ThemeColor/ThemeScale` 三轴与映射，`crates/ui-theme/src/css.rs` 输出 `--ui-*` 变量；`components/drawer/src/styles.rs` 与 `components/sheet/src/styles.rs` 仅消费 `var(--ui-*)`，不重建主题结构。规范链路由 `docs/spec/styling.md` 固定为 `tokens.rs -> theme.rs -> css.rs`。回归：`components/drawer/test/drawer_semantics.rs::drawer_theme_layer_uses_ui_theme_tokens_and_component_styles_only_consume_vars` + `crates/ui-theme/tests/token_scale_baseline.rs`。）
  - Token 统一基线落点固定：`crates/ui-theme/src/tokens.rs` 定义，`crates/ui-theme/src/theme.rs` 映射，`crates/ui-theme/src/css.rs` 输出变量；组件只在 `crates/ui/src/<component>/styles.rs` 消费。
  - 三轴上下文（`system/color/scale`）在 `theme.rs` 定义；组件在 `logic.rs` 选择并在 `view.rs` 生效，`styles.rs` 只消费变量，不重建主题。
  - Token 分类必须可追溯：分类源在 `tokens.rs`，规范同步 `docs/spec/styling.md`；组件不得引入平行私有 token 命名体系。
  - 量化尺寸基准必须可回归：尺寸基准在 `tokens.rs` 与 `theme.rs` 定义，主题回归在 `crates/ui-theme/tests/token_scale_baseline.rs`，组件语义回归在 `components/*/test/*<component>_semantics.rs`。
  - 主题调色与语义色对比必须满足 `WCAG 2.1 AA` 基线，并覆盖 Light/Dark/OLED 主题变体。
  - 主题层只输出 `theme/tokens/base css` 与变量；不实现组件结构、交互逻辑、组件级动效编排。
  - 新增视觉语义先补 token，再由组件消费；禁止“组件临时值先落地、后补 token”的倒序流程。
- [x] `ui` 定义：最终 Leptos 组件装配层，组合 `status-primitives + ui-headless + ui-motion + ui-theme` 并暴露稳定公共 API。（`components/drawer/src/{logic.rs,view.rs,styles.rs,motion.rs}` 各司其职：`logic` 仅消费 `ui-state-primitives` + 桥接、`view` 仅做结构与语义挂载、`styles` 仅 token-first CSS、`motion` 仅 contract 映射；测试已迁移到 `components/drawer/test/{logic.rs,motion.rs,protocol.rs,semantics.rs}` 同级目录，并由 `components/drawer/src/mod.rs` 通过 `#[path = "../test/semantics.rs"]` 挂载。回归：`components/drawer/test/semantics.rs::logic_view_styles_motion_keep_single_responsibility` + `components/drawer/test/drawer_semantics.rs`。）
  - `logic.rs` 负责 props 归一与状态派生；`view.rs` 负责结构渲染与 headless 语义挂载；`styles.rs` 负责 token-first 静态样式；`motion.rs` 负责动效 attach。
  - 组件层不得重写 `status-primitives` 状态机或 `ui-headless` 交互契约；发现即判不通过并回迁到对应层。
  - 对外 API 禁止暴露 `web-sys`/DOM 细节类型；平台差异封装在内部模块。
  - 测试文件位于src同级的test/中，内部测试文件同名（如rust-ui/components/accordion/src/logic.rs与rust-ui/components/accordion/test/logic.rs）。
  - 还需要一个semantics.rs用于测试。可能存在类似rust-ui/components/accordion/test/accordion_semantics.rs的旧版实现，需要迁移到新目录。

### 2. API 设计与状态内核（Logic/Kernel）
- [x] API 命名契约统一：公共 props/回调严格使用 `is_*`、`on_*`、`default_*` 前缀；同语义在全库同名，禁止别名漂移。（`Drawer` 公共布尔输入已统一为 `is_open` 与 `is_close_button_visible`，事件保持 `on_close`；`components/drawer/src/view.rs` 不再暴露 `open/show_close_button` 旧命名。回归：`components/drawer/test/semantics.rs::public_api_uses_is_on_naming_contract_for_boolean_and_callbacks`，并同步 `apps/docs-app/src/pages/components/pages/overlays.rs` 与 `components/drawer/src/README.md` 示例命名。）
  - 布尔状态统一 `is_*`（如 `is_open`/`is_disabled`），事件统一 `on_*`，默认值统一 `default_*`。
  - 同一语义 across 组件必须同名（如都用 `on_open_change`，禁止同义别名并存）。
  - 公共 API 引入新命名时，需说明与现有命名体系的兼容策略与迁移路径。
- [x] 受控/非受控必须成对：每个可控状态轴都提供 `value + on_value_change + default_value`（如 `open/on_open_change/default_open`）；缺一项即不通过。（`Drawer` open 轴已提供 `is_open + on_open_change + default_open` 成对输入：`components/drawer/src/view.rs` 通过 `logic::normalize_open_state(...) + ui_headless::use_controllable_open_state_traced(...)` 统一驱动，受控模式仅经外部信号与回调同步，非受控模式仅由 `default_open` 初始化并走内部 request 收敛，未出现“半受控”双写。回归：`components/drawer/test/logic.rs::{normalize_open_state_supports_controlled_mode,normalize_open_state_supports_uncontrolled_mode_with_default}` + `components/drawer/test/semantics.rs::open_axis_exposes_controlled_uncontrolled_triplet_contract`。）
  - 受控模式：外部值是单一事实来源，内部不得偷偷写回本地状态。
  - 非受控模式：仅由默认值初始化一次，后续状态由内部原语管理。
  - 受控/非受控切换语义需稳定可测，避免“半受控”隐式行为。
- [x] 默认值单一来源：默认值与优先级只在 `logic.rs` 归一化；`view.rs` 禁止二次兜底或隐式改写。（`components/drawer/src/logic.rs` 新增 `DrawerViewConfigInput/DrawerViewConfig + normalize_view_config`，集中归一 `placement/is_close_button_visible/close_label/on_exit_complete` 默认值；`components/drawer/src/view.rs` 改为仅消费 `view_config`，移除 `#[prop(optional, default = ...)]` 与 `unwrap_or_else` 兜底。回归：`components/drawer/test/logic.rs::{normalize_view_config_uses_defaults_for_missing_values,normalize_view_config_respects_custom_values_and_trims_blank_label}` + `components/drawer/test/semantics.rs::default_values_are_normalized_in_logic_only` + `components/drawer/test/drawer_semantics.rs::drawer_view_uses_logic_state_contracts`。）
  - 默认值优先级必须可读且可测试（显式规则而非分散 `unwrap_or`）。
  - `view.rs` 不允许再做默认值分支；仅消费 `logic.rs` 的归一化输出。
  - 一旦发现多处默认值来源，直接判不通过并回收至 `logic.rs`。
- [x] 状态归一化集中：状态输入先类型化，再在 `logic.rs` 统一派生；禁止在 `view.rs`、事件回调、样式分支中分散拼状态机。（`components/drawer/src/logic.rs` 新增 `DrawerPartStatesInput/DrawerPartStates/DrawerPartClasses` 与 `resolve_part_states/resolve_part_classes`，并补 `can_request_open_change`，将 slot 状态派生与可关闭判定集中；`components/drawer/src/view.rs` 移除 7 处 `resolve_state(...)` 分散拼装，改为一次 `part_states/part_classes` 消费。回归：`components/drawer/test/logic.rs::{can_request_open_change_follows_control_contract,resolve_part_states_centralizes_slot_state_derivation,resolve_part_classes_uses_root_base_class_only}` + `components/drawer/test/semantics.rs::state_derivation_is_centralized_in_logic` + `components/drawer/test/drawer_semantics.rs::drawer_view_uses_logic_state_contracts`。）
  - 输入边界统一进入 `logic.rs`，输出统一为可渲染语义状态与来源标记。
  - 事件处理器只触发状态变更，不重建状态机规则。
  - 样式层只消费状态标记，不承担状态判定职责。
- [x] 离散状态必须类型约束：`variant/size/mode/status` 等离散输入使用 `enum`；禁止用多个 `Option<bool>`/字符串自由组合表达互斥状态。（`Drawer` 离散轴已类型化：`DrawerPlacement`（`crates/ui-state-primitives/src/drawer.rs`）、`DrawerOpenMode` 与新增 `DrawerVisibility`（`components/drawer/src/logic.rs`）；`is_close_button_visible: Option<bool>` 在边界 `normalize_view_config` 映射为 `DrawerVisibility`，后续仅消费 enum，不再以 bool 组合驱动互斥状态。回归：`components/drawer/test/logic.rs::{drawer_visibility_models_discrete_close_button_states,normalize_view_config_uses_defaults_for_missing_values}` + `components/drawer/test/semantics.rs::discrete_states_use_typed_enums` + `components/drawer/test/drawer_semantics.rs::drawer_logic_exposes_state_helpers`。）
  - 互斥状态优先用 `enum` 建模，利用编译器封住无效组合。
  - 字符串输入若需兼容外部配置，必须先映射到类型化枚举再进入逻辑层。
  - 布尔爆炸（多个 bool 表达一个状态机）应在设计评审阶段直接拦截。
- [x] 状态原语来源正确：组件层只消费 `status-primitives`（当前 `ui-state-primitives`）能力，不直接绑定业务 store；应用级全局状态必须经桥接层适配后再接入组件。（将可复用状态原语下沉到 `crates/ui-state-primitives/src/drawer.rs`：`DrawerOpenMode/DrawerOpenConfig*/resolve_open_config/can_request_open_change` 与 `DrawerVisibility/resolve_close_button_visibility`；`components/drawer/src/logic.rs` 仅做 Leptos `Signal/Callback` 桥接与装配映射，不再内嵌受控/非受控与可见性状态机规则。回归：`crates/ui-state-primitives/src/test/drawer.rs::{resolve_open_config_supports_controlled_and_uncontrolled_modes,can_request_open_change_follows_mode_and_handler,resolve_close_button_visibility_uses_default_and_custom_values}` + `components/drawer/test/semantics.rs::open_axis_exposes_controlled_uncontrolled_triplet_contract` + `components/drawer/test/drawer_semantics.rs::drawer_logic_exposes_state_helpers`。）
  - 组件中出现可复用状态机实现（受控/非受控、展开规则、选择归一）即判应下沉。
  - 组件与业务全局状态之间必须有适配边界，禁止组件直接依赖业务 store 类型。
  - `logic.rs` 仅做装配与映射，不重新实现状态原语。
- [x] 如果无异步相关，直接打勾。异步交互语义统一：`is_loading`、error/retry、disabled、`aria-busy` 映射一致；优先复用统一 async action 原语（如 `use_async_action`），禁止每组件自定义一套加载/错误协议。（N/A：`Drawer` 仅承载 overlay 展示/关闭语义，不发起远程请求，也未暴露 `is_loading`/`retry`/`aria-busy`/异步 runtime 契约；回归：`components/drawer/test/semantics.rs::drawer_async_interaction_contract_is_not_applicable`。）
  - 无异步交互时需明确标注 N/A 理由（例如“组件无远程请求与异步状态”），不是机械打勾。
  - 有异步交互时，`is_loading`/disabled/`aria-busy`/retry 语义必须成套一致，且对键盘与读屏路径可用。
  - 异步失败态要有可恢复路径（重试或回退），并有语义测试覆盖。
- [x] API 易用性验收标准（DX Paradox）：把复杂性留在内部，把简单留给用户。（`Drawer` 公共 API 不要求 `state=...` 这类内部状态对象；`components/drawer/src/README.md` 的 Hello World 收敛为 4 行最小调用（`<Drawer default_open=true ...>`），不要求手动接线 `ui-state-primitives/ui-headless`；`apps/docs-app/src/pages/components/pages/overlays.rs` 新增 `Hello World (Minimal API)` playground，默认路径可直接运行。回归：`components/drawer/test/semantics.rs::drawer_api_keeps_hello_world_minimal_and_state_free` + `components/drawer/test/drawer_semantics.rs::drawer_docs_page_includes_minimal_hello_world_path`。）
  - 基础用法不得要求用户先理解或手动接线 `ui-state-primitives`/`ui-headless` 状态机。
  - 基础组件 Hello World 示例代码不得超过 5 行（导入与外层模板按仓库约定不计），并可直接运行。
  - 简单需求走简单 API，复杂需求再暴露高级入口：默认 props 覆盖高频场景，高级控制通过受控/扩展参数按需开启。
  - 禁止把内部状态对象作为基础必填参数暴露（例如强制 `state=...` 才能完成点击/展开等基本交互）。
  - docs-app 必须提供最小可用示例，优先展示一眼可懂的默认调用路径。
- [x] 组合型组件主 API 必须“显示优于约定”：优先使用显式组合 `<Parent><Item ... /></Parent>`。（N/A：`Drawer` 为单容器 overlay 组件，不提供多 `Item` 子项集合语义，因此不存在 `labels + children` / `titles + panels` 索引配对风险；回归：`components/drawer/test/semantics.rs::drawer_composite_item_api_contract_is_not_applicable`。）
  - 每个 item 的标题、语义与内容必须在同一 `Item` 结构维度绑定，避免索引配对式隐式约定。
  - `labels + children`、`titles + panels` 等并行数组/并行槽位写法不得作为默认或推荐 API。
  - 不引入这类语法糖：若为配置式输入，仅允许类型化 `ItemSpec`，并在内部映射为显式 `Item` 语义树。

### 3. 高级交互与物理机制（Shell/Physics）
- [x] 宏观/微观双状态机（Macro/Micro Duality）：拖拽等高频交互在 `Dragging` 期间由 `view/motion` 本地循环执行；禁止每帧穿越回 `logic.rs`，必须在结束时通过 `Action::DragEnd` 回流收敛。（N/A：`Drawer` 当前不提供拖拽交互轴（无 `Dragging` 生命周期、无 `Action::DragEnd` 协议），仅消费 `Sheet` 的开关与进出场语义；回归：`components/drawer/test/semantics.rs::drawer_macro_micro_dragging_contract_is_not_applicable`。）
- [x] 几何两段式渲染（Two-Pass Rendering）：`Tooltip/Popover/Menu` 等依赖 DOM 测量的组件必须走 `Intent -> Measure(view) -> Rectification(logic)`，并具备幂等收敛保护防死循环。（N/A：`Drawer` 当前仅组合 `Sheet` 的开关与位置语义，不做锚点几何测量与回流纠偏；不存在 `Intent -> Measure -> Rectification` 状态回路。回归：`components/drawer/test/semantics.rs::drawer_two_pass_geometry_rendering_contract_is_not_applicable`。）
- [x] 集合注册协议（Registration Protocol）：`Accordion/Tabs/Menu` 动态子项必须通过 `RegistrationContext` 上报 `Register/Unregister`，逻辑层维护 `items_order`，禁止依赖 `HashSet` 迭代顺序做导航。（N/A：`Drawer` 不承载动态子项集合语义，不存在 `RegistrationContext`/`Register`/`Unregister` 协议与 `items_order` 导航状态；回归：`components/drawer/test/semantics.rs::drawer_registration_protocol_contract_is_not_applicable`。）
- [x] 插槽投影策略（Slot Projection）：容器组件明确 `Lazy/KeepAlive/Eager`；`KeepAlive` 隐藏时必须通过生命周期通知（如 `NotifyHidden`）暂停轮询/动画等高耗能副作用。（N/A：`Drawer` 作为单层 overlay 容器不提供多投影策略切换轴，不承载 `Lazy/KeepAlive/Eager` 生命周期协议；回归：`components/drawer/test/semantics.rs::drawer_slot_projection_policy_contract_is_not_applicable`。）
- [x] 环境订阅流（Env Streams）：`Resize/Theme/Intersection` 等环境变化在 `view.rs` 采样、防抖后转化为高层语义 `Action`（如 `BreakpointChanged`）推送到 `logic`；禁止原始事件洪泛。（N/A：`Drawer` 不直接订阅 `Resize/Theme/Intersection` 环境事件，当前仅消费 `Sheet` 开关与语义属性，不存在环境事件采样与 `Action` 回流链路。回归：`components/drawer/test/semantics.rs::drawer_env_streams_contract_is_not_applicable`。）
- [x] 事件光锥（Event Light Cone）：`Table/Grid` 等大型集合批量操作必须走 `Context Bus + Selector` 与状态压缩表达（如 `SelectionState::All`），禁止 O(N) 级向下 prop drilling。（N/A：`Drawer` 不承载大规模集合批量选择/导航语义，不存在 `Context Bus + Selector` 或 `SelectionState::All` 状态压缩模型。回归：`components/drawer/test/semantics.rs::drawer_event_light_cone_contract_is_not_applicable`。）
- [x] 统一因果总线（Causality Bus）：复杂派生总线操作必须支持透传 `TraceId`，确保“用户触发 -> 派生命令 -> 总线广播 -> 订阅者”因果链不断裂。（N/A：`Drawer` 不承载复杂派生总线编排，也不存在跨订阅者广播链路，因此无 `TraceId` 透传契约面。回归：`components/drawer/test/semantics.rs::drawer_causality_bus_contract_is_not_applicable`。）
- [x] 存在 A11y 实现、国际化与本地化实现（至少具备接入点，不硬编码用户可见文本）。（`Drawer` 交互语义继续委托 `Sheet + ui-headless::overlay_dialog_attrs/use_focus_trap/use_modal`；`close_label` 支持 props 覆盖并在 `logic.rs` 归一 fallback，`view.rs` 不硬编码用户可见文案；新增 `lang/dir` 输入并透传到 `Sheet`，覆盖 LTR/RTL 与语言接入点。回归：`components/drawer/test/semantics.rs::drawer_a11y_i18n_contract_is_delegated_and_lang_dir_are_passthrough` + `components/drawer/test/drawer_semantics.rs::drawer_delegates_interaction_and_a11y_to_sheet_headless_contract`。）
  - 交互元素必须具备可验证语义：`role`/`aria-*`/键盘可达路径完整，且和 headless 契约一致。
  - 用户可见文本来源必须可覆盖：优先 props，其次应用注入（`UiRoot`/i18n bundle），最后组件兜底文案；禁止把业务可见文案硬编码在 `view.rs`。
  - 组件需透传或消费 `lang` / `dir`（LTR/RTL）上下文，不得假设单语言单方向。
  - 共享 A11y 工具优先来自 `crates/ui-headless/src/a11y.rs`，组件层不重复发明同名语义工具。
- [x] 状态可观测、可检索、可验证：使用稳定 `data-*` 与 `aria-*` 标记表达状态和来源。（`Drawer` 根节点已暴露封闭集合 marker：`data-open-state=open|closed`、`data-open-mode=controlled|uncontrolled`、`data-open-source=external|default|primitive-default`、`data-open-action-source=programmatic|interaction`，并保留 `data-placement/data-description/data-footer/data-close-button` 与 `data-*-source` 契约；A11y 语义继续由 `Sheet` 挂载 `role=\"dialog\" + aria-modal + aria-labelledby/aria-describedby`。回归：`components/drawer/test/logic.rs::open_state_source_markers_are_closed_sets` + `components/drawer/test/semantics.rs::drawer_state_markers_expose_observable_closed_source_sets` + `components/drawer/test/drawer_semantics.rs::drawer_view_uses_logic_state_contracts`。）
  - 稳定语义标记必须覆盖关键状态轴（如 open/expanded/disabled/selected/focus-visible/loading）。
  - 状态来源必须可区分（受控/非受控、默认值/外部值、交互来源），通过稳定 marker 暴露而不是隐式推断。
  - 自动化选择器优先基于语义标记，不依赖 DOM 顺序、层级深度或临时 class 名。
  - 标记值应为封闭集合（可枚举），避免自由文本导致契约漂移。
- [x] 样式依赖显式状态（`data-*`/class），而非脆弱 DOM 结构猜测。（`components/drawer/src/styles.rs` 状态分支均由稳定 marker 驱动（如 `data-state/data-placement/data-close-button/data-footer` 与稳定 class），未使用 `:nth-child/:nth-of-type` 等结构猜测选择器；`components/drawer/src/view.rs` 未注入 `style=`/`style:*` 业务样式，运行时仅挂载语义标记。回归：`components/drawer/test/semantics.rs::styles_depend_on_semantic_markers_not_structural_guesses` + `components/drawer/test/drawer_semantics.rs::drawer_styles_include_state_and_source_markers`。）
  - `styles.rs` 中状态分支选择器必须基于 `data-*`/`aria-*`/稳定 class，禁止用 `:nth-child`、深层级选择器猜测状态。
  - 运行时样式仅允许传递必要 CSS 变量（custom properties）；禁止把业务样式逻辑塞进 inline style。
  - 视觉状态切换必须可由语义标记直接解释，不能依赖“某节点是否恰好存在”。
- [x] 测试验证“语义契约”而不只验证视觉快照。（`Drawer` 语义测试覆盖 `role/aria/data-state/source` 标记与交互路径：受控/非受控由 `components/drawer/test/logic.rs::{normalize_open_state_supports_controlled_mode,normalize_open_state_supports_uncontrolled_mode_with_default}` + `components/drawer/test/semantics.rs::open_axis_exposes_controlled_uncontrolled_triplet_contract` 回归；键盘/指针路径由 `Sheet` headless 契约断言（`on:keydown`/`on:click`/`on:pointerdown`）；SSR/wasm 差异由 `components/sheet/src/motion.rs` 的 `#[cfg(target_arch = \"wasm32\")]` / `#[cfg(not(target_arch = \"wasm32\"))]` 覆盖并被语义测试锁定；`disabled` 轴对 `Drawer` 不适用（组件无 `is_disabled`/`aria-disabled` API），已显式 N/A 断言。视觉快照仅作补充，语义套件禁止 `assert_snapshot!/insta::assert_*`。回归：`components/drawer/test/semantics.rs::drawer_semantics_tests_cover_contract_matrix_without_snapshot_dependency` + `components/drawer/test/drawer_semantics.rs::drawer_semantics_matrix_covers_interactions_and_platform_paths`。）
  - 至少存在语义测试覆盖关键状态与交互路径（role/aria/data-state/source markers）。
  - 测试矩阵必须覆盖关键分支：受控/非受控、disabled、键盘路径、指针路径、SSR/wasm 差异（按适用范围）。
  - 视觉快照只能作为补充，不得替代语义契约断言。
- [x] 组件文件职责正确：`mod.rs`（导出边界）、`logic.rs`（归一/派生/来源标记）、`styles.rs`（静态 token-first CSS）、`view.rs`（Leptos 结构 + headless 挂载）、`motion.rs`（动效契约 + attach）。（`mod.rs` 仅最小导出 `Drawer/DrawerMotion/DrawerPlacement` 并挂测试入口；`logic.rs` 仅做归一/派生与 source marker，不含 `view!/DOM/web_sys`；`styles.rs` 仅静态 token-first CSS；`view.rs` 仅结构渲染与 `Sheet` 语义挂载，不重写 headless 契约；`motion.rs` 仅 `DrawerMotion` 映射与 `sanitize_motion`，运行时 attach 委托 `Sheet`。回归：`components/drawer/test/semantics.rs::{module_contract_keeps_component_assembly_boundaries,logic_view_styles_motion_keep_single_responsibility,state_derivation_is_centralized_in_logic}` + `components/drawer/test/drawer_semantics.rs::{drawer_view_uses_logic_state_contracts,drawer_delegates_interaction_and_a11y_to_sheet_headless_contract,drawer_motion_layer_boundaries_delegate_runtime_to_sheet_and_ui_motion}`。）
  - `mod.rs` 只维护最小稳定导出面与 feature gate，不承载实现细节。
  - `logic.rs` 只做输入归一、状态派生、来源标记；禁止 DOM 操作和样式细节分支。
  - `styles.rs` 只包含 token-first 静态 CSS；禁止硬编码主题常量与业务语义文案。
  - `view.rs` 只做结构渲染与 headless 契约挂载；禁止隐藏关键状态决策。
  - `motion.rs` 只做组件语义到动效契约映射与 attach；禁止在组件内重写通用动效引擎。
- [x] `spec.rs` 只用于少数复杂组件（如 button），避免泛滥。（N/A：`Drawer` 为中等装配型 overlay 组件，当前无稳定外部 Schema/Builder 固化需求，`components/drawer/src/` 未引入 `spec.rs`，说明文档保留在 `README.md + check2.md`；并通过语义回归防止“为形式统一而新增 spec”。回归：`components/drawer/test/semantics.rs::drawer_keeps_spec_rs_out_for_simple_component_scope` + `components/drawer/test/drawer_semantics.rs::drawer_does_not_introduce_spec_rs_builder_for_simple_scope`。）
  - 仅当组件存在稳定外部规范/Schema 契约或复杂配置固化需求时才引入 `spec.rs`。
  - 简单组件不得为了“形式统一”新增 `spec.rs`；说明文档应留在 `check2.md`/组件文档。
  - 新增 `spec.rs` 必须同步给出契约测试与版本演进说明。
- [x] 组件层遵循 token-first 静态样式契约：样式通过 `styles.rs` 聚合注入；运行时仅传必要 CSS 变量；不把 Utility-First/CSS-in-Rust 当组件库默认范式。（`Drawer` 样式规则集中在 `components/drawer/src/styles.rs`，并通过 `crates/ui/src/css.rs` 聚合 `out.push_str(crate::drawer::styles::CSS);` 后由 `UiRoot` 条件注入（`inject_components_css -> crate::css::push_components_css`）；视觉值统一消费 `var(--ui-*)`（含 `var(--ui-button-size-m-height, var(--ui-fallback-button-size-m-height))`、`var(--ui-space-2xs, var(--ui-fallback-space-2xs))`、`var(--ui-border-width, var(--ui-fallback-border-width)) solid var(--ui-border, var(--ui-fallback-border))`），已移除 raw spacing literal；组件源码未引入 Utility-First/CSS-in-Rust 依赖。回归：`components/drawer/test/semantics.rs::drawer_token_first_static_style_contract_is_enforced` + `components/drawer/test/drawer_semantics.rs::drawer_theme_layer_uses_ui_theme_tokens_and_component_styles_only_consume_vars` + `components/drawer/test/drawer_semantics.rs::drawer_css_is_aggregated`。）
  - 样式规则统一落在 `styles.rs`，由 `crates/ui/src/css.rs` 聚合并通过 `UiRoot` 注入。
  - 颜色/间距/圆角/阴影等视觉值必须来自 `var(--ui-*)`，禁止组件私有 token 体系。
  - Utility-First 仅作为 `apps/*` 应用层布局手段，不得反向污染组件库契约。
  - CSS-in-Rust 仅在有明确类型安全与构建成本净收益时作为例外采用。
- [x] 默认主题美学质量达标（Visual Desire）：以 HeroUI 现代审美为学习对标，默认主题不仅“可用”，还必须“第一眼可信”。（复用仓库级默认主题基线：`apps/docs-app/src/pages/components/pages/theme_visual_baseline.rs` 提供 Button/Input/Overlay 同屏视觉基线（层级/对比/交互反馈），`e2e/tests/docs_app_theme_visual_baseline.spec.mjs` 固定 page/button/input/overlay 四张截图（`docs-app-theme-visual-baseline-*.png`）作为视觉回归阈值；Drawer 侧新增回归锁定该契约：`components/drawer/test/semantics.rs::drawer_visual_desire_gate_reuses_theme_visual_baseline_and_screenshot_contracts` + `components/drawer/test/drawer_semantics.rs::drawer_visual_desire_reuses_theme_visual_baseline_and_screenshot_contracts`。）
  - 默认主题需通过基础美学清单：信息层级清晰（字重/字号/间距）、对比与层次自然、交互反馈明确（hover/active/focus）。
  - docs-app 必须提供默认主题基线页面与截图基线，关键组件（Button/Input/Overlay）纳入视觉回归对比。
  - 禁止“可访问但粗糙”的最低可用心态：视觉退化（类似旧式 Bootstrap 观感）视为质量回归。
  - HeroUI 对标以“视觉语言与体验质量”对齐为目标，不做无差别 API 表层复制。
- [x] Tree Shaking 是一等能力：package 模式支持组件级 feature；source 模式天然裁剪；样式层同步裁剪，禁止无条件聚合全部 CSS，禁止破坏 DCE/LTO 的全量中央注册表。（`ui` 已为 Drawer 提供 `component-drawer = ["dep:ui-drawer"]`，且 `ui-drawer` 依赖为 `optional`；`crates/ui/src/lib.rs` 与 `crates/ui/src/css.rs` 分别以 `#[cfg(feature = "component-drawer")]` 条件导出与样式聚合（`out.push_str(crate::drawer::styles::CSS);`），`components/drawer/Cargo.toml` 保持 `default = []` 且不依赖 `ui`。实测：`cargo tree -e features -p ui --no-default-features --features component-drawer,inject-css` 仅观察到命令行特性 `component-drawer/inject-css`，`cargo tree -e features -i ui -p web-demo` 仅观察到 `web-demo-components` 且未出现 `all-components`。CI 门禁由 `scripts/check-ui-tree-shaking.sh` + `scripts/tree_shaking_budget.env`（`TREE_SHAKING_BASELINE_RLIB_BYTES`/`TREE_SHAKING_MAX_RATIO_PERCENT`）与 `.github/workflows/ci.yml` 的 `Tree Shaking Budget` 步骤统一执行。回归：`components/drawer/test/semantics.rs::drawer_tree_shaking_contract_is_feature_gated_and_budget_guarded` + `components/drawer/test/drawer_semantics.rs::drawer_tree_shaking_contract_is_feature_gated_and_budget_guarded`。）
  - package 模式必须有组件级 feature（如 `component-accordion`）；未启用组件不得进入编译与链接路径。
  - `lib.rs` 与 `css.rs` 必须按 feature 条件导出/聚合，禁止无条件引用所有组件模块和 CSS 常量。
  - source 模式下仅引入需要的组件源码，不通过中央注册表维持全组件可达。
  - 任意“全量组件映射表/注册表”若导致不可达代码变可达，直接判不通过。
  - 验证命令（特性树）：`cargo tree -e features -p ui --no-default-features --features component-accordion,inject-css`，确认仅启用目标组件特性链。
  - 验证命令（反向依赖）：`cargo tree -e features -i ui -p web-demo`，检查是否被 `all-components` 或隐式特性全量拉起。
  - CI 检查（最小特性编译）：新增任务仅开启目标最小特性（示例：`cargo check -p ui --target wasm32-unknown-unknown --no-default-features --features component-accordion,inject-css`）。
  - CI 检查（体积预算）：对“最小特性构建产物”设定预算并阻断回归（可用固定阈值，如 `< 50KB`，或基于仓库基线的相对阈值）；不得只做编译通过而不做体积约束。
- [x] 类型系统 + 语义标记共同提供机器可读状态；关键输入空间受类型约束。（`Drawer` 的离散输入与状态轴已类型化为 `enum`：`DrawerPlacement`、`DrawerOpenMode`、`DrawerVisibility`、`DrawerOpenValueSource`、`DrawerOpenActionSource`（`components/drawer/src/logic.rs` + `crates/ui-state-primitives/src/drawer.rs`）；无效状态由 `logic.rs` 统一归一化（`normalize_open_state` / `normalize_view_config` / `resolve_part_states`）并有回归；关键状态通过稳定语义标记暴露（`data-state`、`data-open-state`、`data-open-mode`、`data-open-source`、`data-open-action-source` 及 `*-source` markers），供自动化与 Agent 读取。回归：`components/drawer/test/logic.rs::{normalize_open_state_supports_controlled_mode,normalize_open_state_supports_uncontrolled_mode_with_default,open_state_source_markers_are_closed_sets,resolve_part_states_centralizes_slot_state_derivation}` + `components/drawer/test/semantics.rs::drawer_type_system_and_semantic_markers_form_machine_readable_contract` + `components/drawer/test/drawer_semantics.rs::drawer_type_system_and_semantic_markers_form_machine_readable_contract`。）
  - 离散输入与状态轴必须优先使用 `enum`/新类型建模，避免字符串协议与布尔爆炸。
  - 无效状态要么在类型层不可表达，要么在 `logic.rs` 被统一归一化并可测试。
  - 关键状态必须通过稳定语义标记对外可读，供测试与 Agent 自动化消费。
  - 编译器与测试反馈应能直接定位状态契约破坏点，形成可持续闭环。

### 4. DOM/环境边界治理
- [x] 焦点全局栈（Focus Stack & GC）：层叠 `Overlay` 禁止私存 `NodeRef` 作为恢复目标；必须依赖全局 Focus Manager（如 `FallbackTo/Selector`）防止焦点坠落到 `document.body`。（`Drawer` 自身不持有本地恢复目标 `NodeRef`，焦点管理委托给 `Sheet` 与 `ui-headless`：`components/sheet/src/view.rs` 通过 `use_overlay_stack_registration()` + `use_focus_trap(FocusTrapOptions::enabled(panel_ref))` 接入全局焦点栈；`crates/ui-headless/src/focus_trap.rs` 提供 `FocusTrapFrame` 栈与 `RestorePolicy::{Selector,FallbackTo}`，并在恢复失败时兜底到 `document.body`。回归：`components/drawer/test/semantics.rs::drawer_focus_stack_gc_contract_uses_global_focus_manager_and_policy_restore` + `components/drawer/test/drawer_semantics.rs::drawer_focus_stack_gc_contract_uses_global_focus_manager_and_policy_restore`。）
- [x] 受控外交特区（Escape Hatches）：集成 ECharts/Map 等命令式第三方库时必须处于 `Foreign Zone`（`YieldControl/CleanupForeign`）；第三方实例不得暴露为组件公共 API 或反向污染状态机。（N/A：`Drawer` 当前未集成 ECharts/Map 等命令式第三方实例，也未在 `components/drawer/src/{mod.rs,logic.rs,view.rs,motion.rs}` 与 `components/sheet/src/view.rs` 暴露或持有第三方实例句柄；公共 API 仍是语义化 props，不存在第三方对象反向污染状态机路径。回归：`components/drawer/test/semantics.rs::drawer_escape_hatches_foreign_zone_contract_is_not_applicable` + `components/drawer/test/drawer_semantics.rs::drawer_escape_hatches_foreign_zone_contract_is_not_applicable`。）
- [x] SSR 时空断裂治理（Hydration Discontinuity）：逻辑初始化禁止依赖 `now()` 或原生随机 UUID；必须通过 `IdProvider` 注入确定性种子，确保 SSR/Hydration 间 ID 稳定。（N/A（组件内随机 ID 生成）：`Drawer` 不在组件内生成随机/时间型 ID，`title_id/description_id` 仅由 `id_base` 经 `normalize_id_base` 后做确定性字符串派生（`{id_base}-title` / `{id_base}-description`），`components/drawer/src/{logic.rs,view.rs,motion.rs}` 与 `components/sheet/src/{logic.rs,view.rs}` 不含 `now()/uuid/rand` 初始化路径；全局确定性种子注入由 `crates/ui/src/root.rs` 的 `id_seed + provide_ui_id_provider(id_seed)` 提供，底层能力在 `crates/ui-headless/src/id_provider.rs`。回归：`components/drawer/test/semantics.rs::drawer_hydration_discontinuity_contract_is_n_a_without_time_or_random_id_init` + `components/drawer/test/drawer_semantics.rs::drawer_hydration_discontinuity_contract_is_n_a_without_time_or_random_id_init`。）
- [x] SSR 与跨平台检查：覆盖 web/ssr/wasm 分支，不破坏 non-wasm 编译路径。（compile-only 证据链落在 `scripts/check-ui-platforms.sh`：`cargo check -p ui --target wasm32-unknown-unknown --no-default-features --features component-drawer,inject-css`（web wasm）、`cargo check -p ui-headless --no-default-features --features ssr`（ssr native）、`cargo check -p ui --no-default-features --features component-drawer,inject-css`（默认 native）。平台分支为显式 `cfg`：`components/sheet/src/motion.rs` 与 `components/sheet/src/view.rs` 均包含 `#[cfg(target_arch = "wasm32")] / #[cfg(not(target_arch = "wasm32"))]`。non-wasm 安全：`components/drawer/src/{mod.rs,logic.rs,styles.rs,view.rs,motion.rs}` 无 `web_sys/wasm_bindgen/js_sys/window/document` 引用；`sheet` 的 `web_sys` 仅存在于 `motion.rs` wasm 分支。回归：`components/drawer/test/semantics.rs::drawer_ssr_and_cross_platform_compile_contract_is_documented_and_non_wasm_safe` + `components/drawer/test/drawer_semantics.rs::drawer_ssr_and_cross_platform_compile_contract_is_documented_and_non_wasm_safe`。）
  - 至少包含 compile-only 证据：web（wasm32）、ssr（native）、默认本地构建三条路径。
  - 平台分支差异必须显式 `cfg` 或 feature 管理，禁止依赖运行时偶然行为。
  - non-wasm 路径禁止引用 `web-sys`/浏览器对象。
- [x] `ui-headless` web/ssr feature 互斥受 `compile_error!` 保护（`crates/ui-headless/src/lib.rs`）。（`crates/ui-headless/src/lib.rs` 保持 `#[cfg(all(feature = "web", feature = "ssr"))] compile_error!(...)` 互斥保护；`Drawer` 通过 `Sheet` 接入 `ui-headless`（`components/sheet/src/view.rs` 使用 `use_focus_trap/use_modal/use_overlay_stack_registration`），未绕开该约束。验证脚本 `scripts/check-ui-platforms.sh` 已覆盖：`cargo check -p ui-headless --no-default-features --features ssr`、`cargo check -p ui-headless --target wasm32-unknown-unknown --no-default-features --features web`，以及 `cargo check -p ui-headless --no-default-features --features web,ssr` 预期失败并匹配 `mutually exclusive`。回归：`components/drawer/test/semantics.rs::drawer_headless_web_ssr_mutex_guard_is_preserved` + `components/drawer/test/drawer_semantics.rs::drawer_headless_web_ssr_mutex_guard_is_preserved`。）
  - 组件依赖 `ui-headless` 能力时，不得破坏其 web/ssr 互斥约束。
  - 组件若新增 headless 功能接入，需验证两条 feature 路径都可编译。
  - 发现“同时启用 web+ssr 仍可过编译”视为契约回归。
- [x] `ui-motion` 非 wasm 提供 no-op/stub（`crates/ui-motion/src/lib.rs`），保证 SSR/tooling 可编译。（`crates/ui-motion/src/lib.rs` 在 `#[cfg(not(target_arch = "wasm32"))]` 下提供 `web::prefers_reduced_motion() -> true` 与 `web::animate(&(), ..)` no-op；`crates/ui-motion/tests/non_wasm_stub.rs` 覆盖非 wasm 行为。`Drawer` 动效通过 `Sheet` 执行，`components/sheet/src/motion.rs` 的 non-wasm `attach_motion` 分支仅做 `sanitize_motion` 与可预测 `finish_exit` 回调，不依赖动画句柄或浏览器对象。toolchain 证据：`scripts/check-ui-platforms.sh` 含 `cargo check -p ui-motion`、`cargo test -p ui-motion --test non_wasm_stub` 与 `cargo check -p ui-motion --target wasm32-unknown-unknown`。回归：`components/drawer/test/semantics.rs::drawer_ui_motion_non_wasm_stub_contract_is_predictable_and_tooling_safe` + `components/drawer/test/drawer_semantics.rs::drawer_ui_motion_non_wasm_stub_contract_is_predictable_and_tooling_safe`。）
  - `motion.rs` 调用必须可在 non-wasm 下安全降级，不触发 panic。
  - 组件不得假设动画句柄一定存在；no-op 分支行为需可预测。
  - toolchain 场景（测试/文档/静态分析）不得因 motion 依赖阻塞编译。
- [x] 组件实现覆盖 `reduced-motion` / SSR / wasm 分支。（`Drawer` 动效执行委托给 `Sheet`：`components/sheet/src/motion.rs` 在 wasm 分支显式读取 `ui_motion::web::prefers_reduced_motion()`，并在 `if prefers_reduced_motion` / `if reduced_motion` 路径降级为最小必要反馈（直接设置目标样式并在关闭时 `finish_exit.run(())`）；同时保留 `#[cfg(target_arch = "wasm32")] / #[cfg(not(target_arch = "wasm32"))]` 明确平台分支，non-wasm 走可预测 no-op + `finish_exit`。SSR/Hydration 兼容由确定性 ID 路径（`id_base -> title/description`，无 `now/uuid/rand`）与稳定语义标记（`data-state/role/aria-*`）共同保证，且 `Drawer` 视图未按平台分裂语义 attrs。回归：`components/drawer/test/semantics.rs::drawer_reduced_motion_ssr_wasm_branches_keep_semantics_consistent` + `components/drawer/test/drawer_semantics.rs::drawer_reduced_motion_ssr_wasm_branches_keep_semantics_consistent`。）
  - `reduced-motion` 下动画应跳过或降级为最小必要反馈。
  - SSR 输出必须与客户端 hydration 兼容，避免首帧语义错位。
  - wasm 分支允许增强交互，但语义契约不得与 SSR 分支分裂。
- [x] 性能治理：关键路径有预算（首次渲染/更新耗时/内存），回归可检测、可归因、可阻断。（已满足：`Drawer` 关键路径为 mount-only 语义装配（`components/drawer/src/view.rs` 仅 `on:click/on:keydown/on:pointerdown` 事件入口，无 `pointermove/ResizeObserver/requestAnimationFrame` 洪泛路径），预算与阻断复用仓库统一门禁：`apps/docs-app/src/pages/components/shell.rs` 通过 `component_page_perf_budget + UiPerfProbe` 提供可重复预算基线，`apps/docs-app/src/debug_overlay.rs` 的 `use_ui_trace()/trace.emit` 提供归因链路；`scripts/check-ui-performance.sh` 已纳入 `drawer_performance_governance_contract_is_budgeted_traceable_and_blocking`，并持续校验 `button_performance_governance_contract_is_budgeted_traceable_and_blocking`、`input_performance_governance_contract_is_budgeted_traceable_and_blocking`、`docs_perf_probe_budgets_are_wired_for_component_pages` 与 `perf_render_count_follow_up_is_tracked_in_plan`。基础预算来源：`components/button/check2.md` 与 `components/text-input/src/input/check2.md` 已锁定 `Button/Input` 初始化渲染次数预算为 `1`。当前精确 `render_count` 自动计数仍由 `docs/plan/TODO.md` 跟踪“建立 `render_count` 自动化回归（Button/Input/Accordion...）”，本组件按仓库约定使用 mount-only + trace 等价证据过渡。回归：`components/drawer/test/semantics.rs::drawer_performance_governance_contract_is_budgeted_traceable_and_blocking` + `components/drawer/test/drawer_semantics.rs::drawer_performance_governance_contract_is_budgeted_traceable_and_blocking`。）
  - 关键交互组件需定义最小预算项（首渲染、关键更新、内存/分配趋势）。
  - 回归检测至少具备可重复基线与失败阈值，不靠主观“感觉变慢”。
  - 性能问题需可归因到状态、渲染、样式或动效路径之一。
  - 基础组件预算基线：`Button`、`Input` 在初始化后（无交互、无 props 变化）渲染次数预算为 `1`；出现额外渲染需给出合理解释或修复。
  - 测试要求：在 `components/*/test/**` 增加 `render_count` 类回归测试（测试框架支持时必须启用）；至少覆盖基础组件与本次改动组件。
  - 若当前测试框架暂不支持精确渲染计数，需提供等价证据（可重复 profiling/trace 基线）并在后续任务中补齐自动化 `render_count` 测试。
- [x] `view!` 宏复杂度受控：单个 `view!` 块不得承载超长深嵌套结构；复杂布局按语义分块，避免一次性宏展开导致编译与 wasm 体积劣化。（已满足：`components/drawer/src/view.rs` 已拆分 `render_drawer_close`、`render_drawer_header`、`render_drawer_body`、`render_drawer_footer`、`render_drawer_root` 五个语义子块，`Drawer` 入口仅保留 `Sheet` 装配与分支选择；重复根容器宏块已收敛为单一 `render_drawer_root(...)`，避免描述/无描述两条路径复制整段嵌套结构。门禁：`scripts/check-ui-view-macro.sh` 新增 `drawer_view_macro_complexity_is_split_into_semantic_subrenders`；回归：`components/drawer/test/semantics.rs::drawer_view_macro_complexity_is_split_into_semantic_subrenders` + `components/drawer/test/drawer_semantics.rs::drawer_view_macro_complexity_is_split_into_semantic_subrenders`。）
  - 复杂结构按语义子块拆分（header/body/item 等），避免巨型单块 `view!`。
  - `view.rs` 中若出现多层嵌套重复片段，应优先提取局部渲染函数。
  - 编译时间/产物体积异常增长时，优先排查宏展开体量。
- [x] 函数式拆分优先：不涉及复杂状态与生命周期管理的 UI 片段，优先拆为普通 Rust 函数（返回 `impl IntoView`/`View`），而不是新增 `#[component]`。（已满足：`components/drawer/src/view.rs` 以普通 Rust 函数拆分局部片段（`render_drawer_close`、`render_drawer_header`、`render_drawer_body`、`render_drawer_footer`、`render_drawer_root`，均返回 `impl IntoView`），组件入口仅保留一个 `#[component] pub fn Drawer(...)`，未把局部片段升级为嵌套组件，避免抽象噪音；拆分后关键语义标记（`data-open-*`/`data-*-source`）仍由 `render_drawer_root` 统一输出，测试定位稳定。门禁：`scripts/check-ui-view-macro.sh` 新增 `drawer_view_functional_split_prefers_plain_functions_over_local_components`；回归：`components/drawer/test/semantics.rs::drawer_view_functional_split_prefers_plain_functions_over_local_components` + `components/drawer/test/drawer_semantics.rs::drawer_view_functional_split_prefers_plain_functions_over_local_components`。）
  - 纯静态或轻逻辑片段优先函数化；仅在需要独立 props 语义时升级为组件。
  - 禁止把所有局部片段都升格为 `#[component]` 导致抽象噪音。
  - 拆分后语义标记与测试定位仍需稳定。
- [x] 静态片段常量化：复杂 SVG、页脚、长说明文本等纯静态内容优先常量化/模板化，减少重复 `view!` 渲染指令生成。（已满足：`components/drawer/src/view.rs` 将关闭图标模板集中为 `DRAWER_CLOSE_ICON_VIEWBOX` / `DRAWER_CLOSE_ICON_PATH` / `DRAWER_CLOSE_ICON_STROKE_WIDTH` 常量，并通过单一 `render_drawer_close_icon` 渲染入口复用，`render_drawer_close` 仅负责语义挂载，不再散落内联 SVG 路径；A11y 标记 `aria-hidden="true"` 保持不变。门禁：`scripts/check-ui-view-macro.sh` 新增 `drawer_static_fragments_are_constantized_or_absent_for_simple_overlay_layout`。回归：`components/drawer/test/semantics.rs::drawer_static_fragments_are_constantized_or_absent_for_simple_overlay_layout` + `components/drawer/test/drawer_semantics.rs::drawer_static_fragments_are_constantized_or_absent_for_simple_overlay_layout`。）
  - 可判定为纯静态的片段应避免重复动态构造。
  - 常量化后仍需维持可访问语义（title/aria-label/role 等）。
  - 静态资源变更路径要清晰，避免散落在多个 `view!` 片段中。
- [x] `inner_html` 使用约束：仅允许注入受信任静态常量，禁止拼接用户输入；使用处必须补充语义与安全回归测试。（已满足：`Drawer` 组件与示例为零注入面，`components/drawer/src/{mod.rs,logic.rs,styles.rs,view.rs,motion.rs,README.md}` 与 `apps/docs-app/src/pages/components/pages/overlays.rs` 不包含 `inner_html/set_inner_html/dangerously_set_inner_html` 等原始 HTML 注入入口，也未出现 `markdown_to_html(<untrusted>)`、`<script`、`javascript:`、`onerror=`、`onload=` 等高风险 token。门禁：`scripts/check-ui-inner-html.sh` 新增 `drawer_inner_html_usage_is_forbidden_in_component_and_docs_examples`。回归：`components/drawer/test/semantics.rs::drawer_inner_html_usage_is_forbidden_in_component_and_docs_examples` + `components/drawer/test/semantics.rs::drawer_inner_html_check_script_covers_security_contract` + `components/drawer/test/drawer_semantics.rs::drawer_inner_html_usage_is_forbidden_in_component_and_docs_examples` + `components/drawer/test/drawer_semantics.rs::drawer_inner_html_check_script_covers_security_contract`。）
  - 仅允许编译期常量或明确白名单内容进入 `inner_html`。
  - 严禁直接或间接注入用户输入、远端返回或未清洗模板字符串。
  - 使用 `inner_html` 的节点必须补语义测试与安全回归说明。
- [x] WASM 调试要求：关键状态可追踪（来源/时间/前后值），关键交互可回放，开发模式有可视化入口，调试能力通过 feature 隔离不污染产物。（已满足：`Drawer` open 轴通过 `use_controllable_open_state_traced("drawer", ..)` 复用 `ui-headless` 统一追踪事件（`UiTraceEventKind::OpenChange`，含 `ts_ms/component/open`），并在视图暴露 `data-open-mode/data-open-source/data-open-action-source` 做前后值与来源定位；关键交互最小回放路径由 docs `overlays::drawer` 的 `State + Source Markers` 示例提供（`open_custom_raw` + open/close 按钮 + on_close/on_exit_complete），可复现事件顺序与状态迁移。开发可视化入口由 `apps/docs-app/src/lib.rs` 的 dev-only `UiDebugOverlay` 挂载（`cfg!(debug_assertions)` + `provide_ui_trace(debug_overlay_enabled)` + `<Show when=...>`）提供，面板显示事件时间戳与 inspect 快照。feature 隔离：`crates/ui/Cargo.toml` 不存在 `drawer-wasm-debug` 公共特性（仅保留共享 `accordion/button/sheet` wasm-debug 特性），Drawer 组件自身不暴露 `debug` props、不引入 `tracing::*` 宏。门禁：`scripts/check-ui-wasm-debug.sh` 新增 `drawer_wasm_debug_contract_reuses_global_trace_and_stays_feature_isolated`。回归：`components/drawer/test/semantics.rs::drawer_wasm_debug_contract_reuses_global_trace_and_stays_feature_isolated` + `components/drawer/test/semantics.rs::drawer_wasm_debug_check_script_covers_shared_contract` + `components/drawer/test/drawer_semantics.rs::drawer_wasm_debug_contract_reuses_global_trace_and_stays_feature_isolated` + `components/drawer/test/drawer_semantics.rs::drawer_wasm_debug_check_script_covers_shared_contract`。）
  - 开发模式下至少能追踪关键状态变更来源与前后值。
  - 关键交互链路应支持最小可复现记录（事件顺序/状态转移）。
  - 调试开关默认不进入生产包体与公共 API。
- [x] DX 要求：样式热重载优先无需重编 wasm；组件热开发尽量保持上下文；提供可选状态保留；有 Workbench 隔离画布。（已满足：`Drawer` 文档演练入口位于 `apps/docs-app/src/pages/components/pages/overlays.rs::drawer()`，包含 `Hello World (Minimal API)`、`Right Drawer + Slots`、`State + Source Markers` 三个 Playground；样式热调路径复用 `apps/docs-app/src/playground.rs` 的 scoped CSS 编辑器（`compose_scoped_css` + `data-slot="playground-test"` + `Restore original CSS`），无需重编 wasm。上下文保持：drawer 示例均基于可观测信号（`open_semantic_raw/open_custom_raw`）和 `on_close/on_exit_complete` 闭环，可在同一画布内连续调试状态迁移。可选状态保留：当前 Drawer docs 未引入本地持久化存储键（N/A），避免把 demo 状态持久化逻辑污染组件契约。隔离画布：`Playground` 统一提供 `data-playground-scope` + `playground__preview-stage` 隔离预览区域。门禁：`scripts/check-ui-dx.sh` 新增 `drawer_dx_playground_supports_css_hot_reload_without_wasm_rebuild` 与 `drawer_dx_interactive_scope_keeps_isolated_canvas_and_context_visible_with_optional_persist_na`。回归：`components/drawer/test/semantics.rs::drawer_dx_playground_supports_css_hot_reload_without_wasm_rebuild` + `components/drawer/test/semantics.rs::drawer_dx_interactive_scope_keeps_isolated_canvas_and_context_visible_with_optional_persist_na` + `components/drawer/test/semantics.rs::drawer_dx_check_script_covers_hot_reload_and_isolated_canvas_contract` + `components/drawer/test/drawer_semantics.rs::drawer_dx_playground_supports_css_hot_reload_without_wasm_rebuild` + `components/drawer/test/drawer_semantics.rs::drawer_dx_interactive_scope_keeps_isolated_canvas_and_context_visible_with_optional_persist_na` + `components/drawer/test/drawer_semantics.rs::drawer_dx_check_script_covers_hot_reload_and_isolated_canvas_contract`。）
  - 常见样式调整应走快速反馈路径，不依赖完整 wasm 重编译。
  - 组件调试应尽量保持当前交互上下文，降低重复操作成本。
  - 复杂交互组件应有隔离演练入口（workbench/story/demo 之一）。
- [x] 工程能力统一：`serde` 负责 spec 序列化/版本迁移/错误结构化；`tracing` 统一 span/event 语义；async 不绑定单一运行时（tokio/async-std），runtime 细节不泄露到上层 API。（已满足：`components/drawer/src/protocol.rs` 提供最小 `serde` 协议契约（`DrawerComponentSchemaVersion` + `DrawerComponentSpec`，含 `Serialize/Deserialize/Default` 与 `#[serde(default)]`），并通过 `components/drawer/src/mod.rs` 导出 `pub mod protocol;` 保持协议可发现性；交互 tracing 语义复用统一 headless 通道（`components/drawer/src/view.rs` 使用 `use_controllable_open_state_traced(\"drawer\", ..)`），组件侧未新增 `tracing::span!/event!/instrument` 或本地 trace target；`components/drawer/src/{mod,logic,view,styles,motion,protocol}.rs` 未引入 `tokio/async-std/runtime::Handle/smol/spawn_blocking` 等运行时泄漏 token。门禁：`scripts/check-ui-engineering.sh` 新增 drawer 三条定向命令。回归：`components/drawer/test/semantics.rs::drawer_engineering_contract_uses_serde_protocol_and_structured_schema_defaults`、`components/drawer/test/semantics.rs::drawer_engineering_contract_keeps_tracing_semantics_unified_without_component_local_events`、`components/drawer/test/semantics.rs::drawer_engineering_contract_avoids_runtime_leaks_in_public_api_surface`、`components/drawer/test/semantics.rs::drawer_engineering_check_script_covers_serde_tracing_and_runtime_boundaries`，以及 `components/drawer/test/drawer_semantics.rs::drawer_engineering_contract_uses_serde_protocol_and_structured_schema_defaults`、`components/drawer/test/drawer_semantics.rs::drawer_engineering_contract_keeps_tracing_semantics_unified_without_component_local_events`、`components/drawer/test/drawer_semantics.rs::drawer_engineering_contract_avoids_runtime_leaks_in_public_api_surface`、`components/drawer/test/drawer_semantics.rs::drawer_engineering_check_script_covers_serde_tracing_and_runtime_boundaries`。执行验证命令：`cargo test -p ui --test drawer_semantics --no-default-features --features component-drawer,inject-css drawer_engineering_contract_uses_serde_protocol_and_structured_schema_defaults`、`cargo test -p ui --test drawer_semantics --no-default-features --features component-drawer,inject-css drawer_engineering_contract_keeps_tracing_semantics_unified_without_component_local_events`、`cargo test -p ui --test drawer_semantics --no-default-features --features component-drawer,inject-css drawer_engineering_contract_avoids_runtime_leaks_in_public_api_surface`；当前环境已知限制仍为 `Invalid cross-device link (os error 18)`。）
  - 若组件涉及 spec/config 输入，序列化与错误输出应走统一结构化路径。
  - 关键流程埋点语义应与全库 tracing 约定一致，避免组件各说各话。
  - 异步边界不得把具体 runtime 类型暴露到组件公共接口。

### 5. 样式与动效（Theme & Motion）
- [x] 样式孤岛防御（Defensive Variables）：`styles.rs` 使用双层回退链 `var(--ui-*, var(--ui-fallback-*))`；禁止组件内硬编码 Hex 或裸尺寸终值，Fallback 终值由 `ui-theme` 统一输出（SSOT）。（已满足：`components/drawer/src/styles.rs` 已将间距/字号/行高/边框/关闭按钮偏移统一改为双层回退链（如 `var(--ui-space-md, var(--ui-fallback-space-md))`、`var(--ui-border-width, var(--ui-fallback-border-width))`、`var(--ui-border, var(--ui-fallback-border))`、`var(--ui-button-size-m-height, var(--ui-fallback-button-size-m-height))`）；`crates/ui-theme/src/css.rs` 新增并统一输出 `--ui-fallback-button-size-m-height`，其余 `space/typography/color/border` fallback 终值同样由 theme SSOT 提供。门禁：`scripts/check-ui-contract-hygiene.sh` 新增 `cargo test -p ui --test drawer_semantics --no-default-features --features component-drawer,inject-css drawer_styles_use_defensive_variable_fallback_chain_with_ui_theme_ssot_terminals`。回归：`components/drawer/test/semantics.rs::drawer_styles_use_defensive_variable_fallback_chain_with_ui_theme_ssot_terminals`、`components/drawer/test/semantics.rs::drawer_defensive_variables_check_script_covers_contract`、`components/drawer/test/drawer_semantics.rs::drawer_styles_use_defensive_variable_fallback_chain_with_ui_theme_ssot_terminals`、`components/drawer/test/drawer_semantics.rs::drawer_defensive_variables_check_script_covers_contract`。执行验证命令同上；当前环境已知限制仍为 `Invalid cross-device link (os error 18)`。）
- [x] 级联层覆盖（`@layer ui`）：组件 CSS 默认聚合进 `@layer ui`；运行时数值调整仅通过 CSS Custom Properties（如 `style:--x=...`），禁止普通内联样式（如 `style=\"top: 10px\"`）。（已满足：`crates/ui/src/css.rs` 通过 `push_components_css` 统一包裹 `@layer ui`（`out.push_str("\\n@layer ui {\\n") ... out.push_str("\\n}\\n")`），并在 `#[cfg(feature = "component-drawer")]` 下聚合 `out.push_str(crate::drawer::styles::CSS);`；`crates/ui/src/root.rs` 保持集中注入路径（`if inject_components_css.get_value() { crate::css::push_components_css(&mut out); }` + `<style>{move || css_text.get()}</style>`）；`components/drawer/src/view.rs` 无普通 inline `style=\"...\"`，运行时也未出现业务样式直写，仅结构与语义挂载。门禁：`scripts/check-ui-contract-hygiene.sh` 新增 `cargo test -p ui --test drawer_semantics --no-default-features --features component-drawer,inject-css drawer_cascade_layer_and_runtime_style_contract_is_enforced`。回归：`components/drawer/test/semantics.rs::drawer_cascade_layer_and_runtime_style_contract_is_enforced`、`components/drawer/test/semantics.rs::drawer_cascade_layer_check_script_covers_contract`、`components/drawer/test/drawer_semantics.rs::drawer_cascade_layer_and_runtime_style_contract_is_enforced`、`components/drawer/test/drawer_semantics.rs::drawer_cascade_layer_check_script_covers_contract`。执行验证命令同上；当前环境已知限制仍为 `Invalid cross-device link (os error 18)`。）
- [x] Motion 合同化：`stiffness`/`damping` 等参数在 `motion.rs` 内置为组件 Contract，并通过 `attach_motion` 挂载；必须尊重 `prefers-reduced-motion` 且在 non-wasm/SSR 安全降级（no-op）。（已满足：`components/drawer/src/motion.rs` 仅承载 `DrawerMotion` 合同与 `sanitize_motion` 映射，参数清洗委托到 `components/sheet/src/motion.rs`（默认 spring 合同来自 `ui_motion::presets::spring_slide()`）；运行时挂载仍在 `components/sheet/src/motion.rs::attach_motion`，其 wasm 分支显式使用 `ui_motion::web::prefers_reduced_motion()`，non-wasm 分支以 no-op/安全降级收敛并在关闭路径执行 `finish_exit.run(())`；共享 backend 与 no-op 表面由 `crates/ui-motion/src/lib.rs` 提供。门禁：`scripts/check-ui-contract-hygiene.sh` 新增 `cargo test -p ui --test drawer_semantics --no-default-features --features component-drawer,inject-css drawer_motion_contract_is_builtin_and_attached_with_reduced_motion_and_non_wasm_noop`。回归：`components/drawer/test/semantics.rs::drawer_motion_contract_is_builtin_and_attached_with_reduced_motion_and_non_wasm_noop`、`components/drawer/test/semantics.rs::drawer_motion_contract_check_script_covers_contract`、`components/drawer/test/semantics.rs::drawer_check2_marks_motion_contract_complete`、`components/drawer/test/drawer_semantics.rs::drawer_motion_contract_is_builtin_and_attached_with_reduced_motion_and_non_wasm_noop`、`components/drawer/test/drawer_semantics.rs::drawer_motion_contract_check_script_covers_contract`、`components/drawer/test/drawer_semantics.rs::drawer_check2_marks_motion_contract_complete`。执行验证命令同上；当前环境已知限制仍为 `Invalid cross-device link (os error 18)`。）
- [x] `ui` 固定入口文件落点正确。（已满足：`crates/ui/src/lib.rs` 保持入口边界与公共导出（`#[cfg(feature = "component-drawer")] pub use ui_drawer as drawer;`、`pub mod root;`、`pub use root::UiRoot;`，且 `pub use drawer::{Drawer, DrawerMotion, DrawerPlacement};` 受特性约束）；`crates/ui/src/css.rs` 以 `push_components_css` 作为唯一组件样式聚合入口，在 `inject-css + component-drawer` 门控下聚合 `out.push_str(crate::drawer::styles::CSS);`，并提供 `#[cfg(not(feature = "inject-css"))]` no-op；`crates/ui/src/root.rs` 集中注入 base css + theme vars + optional components css（`out.push_str(css::BASE_CSS);`、`out.push_str(&theme.get().to_css_variables());`、`if inject_components_css.get_value() { crate::css::push_components_css(&mut out); ... }`），并通过 `provide_ui_i18n(i18n)` 提供全局 i18n 上下文；共享高亮原语保持在 `crates/ui-visual-primitive/src/active_highlight.rs`，仅承载通用高亮样式与 motion driver；`crates/ui/src/overlay_open.rs`、`crates/ui/src/presence.rs`、`crates/ui/src/a11y.rs` 不存在，canonical 路径固定在 `crates/ui-headless/src/controllable_state.rs`、`crates/ui-headless/src/presence.rs`、`crates/ui-headless/src/a11y.rs`。门禁：`scripts/check-ui-entrypoints.sh` 新增 `cargo test -p ui --test drawer_semantics --no-default-features --features component-drawer,inject-css drawer_ui_components_fixed_entry_files_follow_layered_boundaries`。回归：`components/drawer/test/semantics.rs::drawer_ui_components_fixed_entry_files_follow_layered_boundaries`、`components/drawer/test/semantics.rs::drawer_entrypoints_check_script_covers_fixed_entry_files_gate`、`components/drawer/test/semantics.rs::drawer_check2_marks_ui_components_fixed_entry_files_contract_complete`、`components/drawer/test/drawer_semantics.rs::drawer_ui_components_fixed_entry_files_follow_layered_boundaries`、`components/drawer/test/drawer_semantics.rs::drawer_entrypoints_check_script_covers_fixed_entry_files_gate`、`components/drawer/test/drawer_semantics.rs::drawer_check2_marks_ui_components_fixed_entry_files_contract_complete`。执行验证命令同上；当前环境已知限制仍为 `Invalid cross-device link (os error 18)`。）
  - `crates/ui/src/lib.rs`：总模块入口 + 对外 `pub use`（公共 API 面）；组件模块受 `component-*` feature gate 约束；不暴露内部平台细节类型。
  - `crates/ui/src/css.rs`：组件 CSS 聚合入口（`push_components_css`）；按 feature 条件注入；禁止无条件聚合全部组件 CSS。
  - `crates/ui/src/root.rs`：`UiRoot` 统一注入 base css + theme vars +（可选）components css，并提供全局 i18n 上下文；主题与注入策略必须集中在此。
  - `crates/ui-visual-primitive/src/active_highlight.rs`：共享高亮条样式与 motion driver；只承载通用高亮动效能力，不承载具体组件业务语义。
  - `crates/ui/src/overlay_open.rs`：当前仓库中不应存在；open-state 原语固定在 `crates/ui-headless/src/controllable_state.rs`，组件通过 headless API 消费。
  - `crates/ui/src/presence.rs`：当前仓库中不应存在；presence 原语固定在 `crates/ui-headless/src/presence.rs`，组件通过 `ui_headless::use_presence` 消费。
  - `crates/ui/src/a11y.rs`：当前仓库中不应存在；共享 A11y 工具固定在 `crates/ui-headless/src/a11y.rs`（如 `aria_controls_when_open`），组件只负责挂载。
- [x] 组件目录标准文件落点正确。（已满足：`components/drawer/src/` 保持标准文件集合 `mod.rs + logic.rs + styles.rs + view.rs + motion.rs`；`mod.rs` 维持最小稳定导出面（`pub use view::Drawer;`、`pub use motion::DrawerMotion;`，不对外暴露 `logic/view` 实现模块）；`logic.rs` 仅做 props 归一/状态派生/来源标记与映射（`normalize_open_state`、`normalize_view_config`、`resolve_part_states`、`resolve_part_classes`），不承载 DOM/视图绑定；`styles.rs` 维持静态 token-first CSS（`pub const CSS` + `var(--ui-*)`/fallback 链），无平台运行时细节；`view.rs` 仅做 Leptos 结构渲染与 headless 契约挂载（通过 `use_controllable_open_state_traced` 与稳定 `data-*` 语义标记输出），未引入 `render.rs` 漂移；`motion.rs` 仅保留 `DrawerMotion + sanitize_motion` 合同映射并委托 `ui_sheet` 运行时 attach；`components/drawer/src/render.rs` 与 `components/drawer/src/spec.rs` 均不存在（`spec.rs` 对 Drawer 为 N/A）。门禁：`scripts/check-ui-component-files.sh` 新增 `cargo test -p ui --test drawer_semantics --no-default-features --features component-drawer,inject-css drawer_component_directory_standard_files_follow_contract_and_na_paths`。回归：`components/drawer/test/semantics.rs::drawer_component_directory_standard_files_follow_contract_and_na_paths`、`components/drawer/test/semantics.rs::drawer_component_files_check_script_covers_standard_layout_gate`、`components/drawer/test/semantics.rs::drawer_check2_marks_component_directory_standard_files_contract_complete`、`components/drawer/test/drawer_semantics.rs::drawer_component_directory_standard_files_follow_contract_and_na_paths`、`components/drawer/test/drawer_semantics.rs::drawer_component_files_check_script_covers_standard_layout_gate`、`components/drawer/test/drawer_semantics.rs::drawer_check2_marks_component_directory_standard_files_contract_complete`。执行验证命令同上；当前环境已知限制仍为 `Invalid cross-device link (os error 18)`。）
  - `<component>/mod.rs`：最小稳定导出面，存在且无过度导出。
  - `<component>/logic.rs`：props 归一化、派生状态、来源标记；不得承载可下沉原语。
  - `<component>/styles.rs`：静态 CSS 契约，只用 `var(--ui-*)`，不写死主题常量。
  - `<component>/view.rs`：纯 Leptos 结构渲染 + headless 语义挂载；禁止 `render.rs` 漂移；不隐藏关键状态决策。
  - `<component>/motion.rs`：`XxxMotion + attach_motion`；交互组件必须有；只做语义到 motion contract 的映射与挂载。
  - `<component>/spec.rs`：仅极少数组件专用（当前主要 button），无必要不新增。

### 6. AI 原生能力与文件落点（Struct-First & Projection）
- [x] 文件落点纪律：组件目录严格由 `mod.rs`（导出）、`logic.rs`（归一派生）、`styles.rs`（Token 样式）、`view.rs`（渲染）、`motion.rs`（动效）组成；复杂组件可选 `spec.rs`；禁止 `render.rs`。（已满足：`components/drawer/src/` 维持核心文件 `mod.rs + logic.rs + styles.rs + view.rs + motion.rs`；`mod.rs` 仅维护最小稳定导出（`pub use view::Drawer;`、`pub use motion::DrawerMotion;`），未过度导出内部实现；`logic.rs` 保持输入归一与状态派生（`normalize_open_state` / `normalize_view_config` / `resolve_part_states`）；`styles.rs` 保持 token-first 静态 CSS；`view.rs` 保持 Leptos 渲染与语义挂载；`motion.rs` 保持语义到 motion contract 的映射；`components/drawer/src/render.rs` 与 `components/drawer/src/spec.rs` 均不存在。仓库例外 `components/drawer/src/protocol.rs` 仅承载版本化 schema（`DrawerComponentSchemaVersion` / `DrawerComponentSpec`）。门禁：`scripts/check-ui-component-files.sh` 新增 `cargo test -p ui --test drawer_semantics --no-default-features --features component-drawer,inject-css drawer_file_placement_discipline_is_strict_for_component_scope`。回归：`components/drawer/test/semantics.rs::drawer_file_placement_discipline_is_strict_for_component_scope`、`components/drawer/test/semantics.rs::drawer_file_placement_check_script_covers_contract`、`components/drawer/test/semantics.rs::drawer_check2_marks_file_placement_discipline_contract_complete`、`components/drawer/test/drawer_semantics.rs::drawer_file_placement_discipline_is_strict_for_component_scope`、`components/drawer/test/drawer_semantics.rs::drawer_file_placement_check_script_covers_contract`、`components/drawer/test/drawer_semantics.rs::drawer_check2_marks_file_placement_discipline_contract_complete`。执行验证命令同上；当前环境已知限制仍为 `Invalid cross-device link (os error 18)`。）
- [x] Hyper-Structure Builder（`spec.rs`）：复杂组件必须提供 AI 友好的 `*Spec::new()...render()` 建造者 API。（N/A（`drawer` 非复杂 builder/spec 组件）：`components/drawer/src/spec.rs` 保持不存在，版本化协议固定在 `components/drawer/src/protocol.rs`（`DrawerComponentSchemaVersion` + `DrawerComponentSpec`），且 `components/drawer/src/{mod.rs,view.rs,logic.rs}` 不暴露 `Spec::new()`/`.render()`/`DrawerSpec`。门禁：`scripts/check-ui-component-files.sh` 新增 `cargo test -p ui --test drawer_semantics --no-default-features --features component-drawer,inject-css drawer_hyper_structure_builder_spec_is_not_applicable_for_simple_component`。回归：`components/drawer/test/semantics.rs::drawer_hyper_structure_builder_spec_is_not_applicable_for_simple_component`、`components/drawer/test/semantics.rs::drawer_hyper_structure_builder_check_script_covers_na_contract`、`components/drawer/test/semantics.rs::drawer_check2_marks_hyper_structure_builder_contract_complete`、`components/drawer/test/drawer_semantics.rs::drawer_hyper_structure_builder_spec_is_not_applicable_for_simple_component`、`components/drawer/test/drawer_semantics.rs::drawer_hyper_structure_builder_check_script_covers_na_contract`、`components/drawer/test/drawer_semantics.rs::drawer_check2_marks_hyper_structure_builder_contract_complete`。执行验证命令同上；当前环境已知限制仍为 `Invalid cross-device link (os error 18)`。）
- [x] 上下文压缩协议（Manifest + RBI）：新增/大改组件必须同步维护组件目录下 `Component.toml`（能力清单）和 `.rbi`（接口签名投影），避免 AI 检索工具箱过时。（已满足：新增 `components/drawer/src/Component.toml`（能力清单）与 `components/drawer/src/drawer.rbi`（接口签名投影），并与 `components/drawer/src/view.rs::Drawer` 参数面保持对齐（`is_open/default_open/on_open_change/on_close/id_base/title/children/description/footer/placement/motion/is_close_button_visible/close_label/lang/dir/on_exit_complete/class_name`）；协议层仍由 `components/drawer/src/protocol.rs` 提供版本化 `DrawerComponentSchemaVersion + DrawerComponentSpec`。门禁：`scripts/check-ui-component-files.sh` 新增 `cargo test -p ui --test drawer_semantics --no-default-features --features component-drawer,inject-css drawer_context_compression_manifest_and_rbi_projection_are_present_and_current`。回归：`components/drawer/test/semantics.rs::drawer_context_compression_manifest_and_rbi_projection_are_present_and_current`、`components/drawer/test/semantics.rs::drawer_component_files_check_script_covers_context_compression_manifest_contract`、`components/drawer/test/semantics.rs::drawer_check2_marks_context_compression_manifest_and_rbi_contract_complete`、`components/drawer/test/drawer_semantics.rs::drawer_context_compression_manifest_and_rbi_projection_are_present_and_current`、`components/drawer/test/drawer_semantics.rs::drawer_component_files_check_script_covers_context_compression_manifest_contract`、`components/drawer/test/drawer_semantics.rs::drawer_check2_marks_context_compression_manifest_and_rbi_contract_complete`。执行验证命令同上；当前环境已知限制仍为 `Invalid cross-device link (os error 18)`。）
- [x] 语义标记统一升级为 Agent Contract（Schema 化），让 Agent 不依赖 DOM 猜测理解组件状态与意图。（已满足：`components/drawer/src/logic.rs` 新增类型化 Agent Contract（`DRAWER_AGENT_SCHEMA`、`DrawerAgentSchemaVersion/Intent/Action/State/Source/ConfigPolicy/OutputStatus`、`DrawerAgentCapabilities`、`DrawerAgentContractInput`、`DrawerAgentContract`、`resolve_agent_contract`）；`components/drawer/src/view.rs` 通过 `Signal::derive` 调用 resolver 并挂载 `data-ui-schema/data-ui-schema-version/data-ui-intent/data-ui-action/data-ui-state/data-ui-source/data-ui-config-policy/data-ui-output-status` 与 `data-ui-capability-*`。`components/drawer/src/Component.toml` 同步补齐 `agent-contract-markers`、`agent_contract_schema_markers`、`agent_contract_whitelist_render_policy`，并新增 `[agent_contract]` + `[[agent_contract_markers]]` + `[[agent_contract_whitelist]]` 约束渲染白名单边界；`components/drawer/src/drawer.rbi` 同步投影 Agent Contract 类型与 `resolve_agent_contract` 签名，避免 Manifest/RBI/代码漂移。回归：`components/drawer/test/semantics.rs::drawer_check2_documents_agent_contract_schema_governance_rules`、`components/drawer/test/semantics.rs::drawer_agent_contract_is_schema_typed_and_machine_readable`、`components/drawer/test/semantics.rs::drawer_agent_contract_fields_are_type_derived_without_free_form_schema_string_splicing`、`components/drawer/test/semantics.rs::drawer_agent_contract_render_path_is_whitelist_safe_and_script_injection_free`、`components/drawer/test/semantics.rs::drawer_contract_hygiene_script_covers_agent_contract_schema_guards`、`components/drawer/test/semantics.rs::drawer_check2_marks_agent_contract_schema_governance_complete`、`components/drawer/test/drawer_semantics.rs::drawer_check2_documents_agent_contract_schema_governance_rules`、`components/drawer/test/drawer_semantics.rs::drawer_agent_contract_is_schema_typed_and_machine_readable`、`components/drawer/test/drawer_semantics.rs::drawer_agent_contract_fields_are_type_derived_without_free_form_schema_string_splicing`、`components/drawer/test/drawer_semantics.rs::drawer_agent_contract_render_path_is_whitelist_safe_and_script_injection_free`、`components/drawer/test/drawer_semantics.rs::drawer_contract_hygiene_script_covers_agent_contract_schema_guards`、`components/drawer/test/drawer_semantics.rs::drawer_check2_marks_agent_contract_schema_governance_complete`。门禁：`scripts/check-ui-contract-hygiene.sh` 新增 4 条 drawer agent-contract 命令。执行验证命令同上；当前环境已知限制仍为 `Invalid cross-device link (os error 18)`。）
  - 关键交互组件必须输出稳定机器可读语义（至少 `data-*` + 状态来源标记；复杂组件建议补 `data-ui-schema`）。
  - Agent 消费字段应来自类型化 schema 生成，不允许散落字符串拼接。
  - 契约字段需可追溯到组件状态轴与动作语义（intent/action/state/source）。
  - 配置到组件的渲染链路必须走白名单能力边界，禁止任意脚本注入。
- [x] 流式在这里仅指 LLM 输出渲染（只看两种显示模式）。（已满足：`Drawer` 当前不承担 LLM 正文渲染职责，N/A：`Drawer` 不是 LLM 正文渲染组件；组件运行时路径 `components/drawer/src/{view.rs,logic.rs,mod.rs,motion.rs}` 不引入 `AiRenderMode/AiOutputStatus/data-stream-*` 等流式协议字段，避免把流式语义扩散到非正文组件。门禁：`scripts/check-ui-streaming.sh` 新增 `cargo test -p ui --test drawer_semantics --no-default-features --features component-drawer,inject-css drawer_check2_documents_streaming_definition_is_llm_output_only_with_two_modes`。回归：`components/drawer/test/semantics.rs::drawer_check2_documents_streaming_definition_is_llm_output_only_with_two_modes`、`components/drawer/test/semantics.rs::drawer_streaming_script_covers_two_mode_definition_contract`、`components/drawer/test/semantics.rs::drawer_check2_marks_streaming_two_mode_definition_complete`、`components/drawer/test/drawer_semantics.rs::drawer_check2_documents_streaming_definition_is_llm_output_only_with_two_modes`、`components/drawer/test/drawer_semantics.rs::drawer_streaming_script_covers_two_mode_definition_contract`、`components/drawer/test/drawer_semantics.rs::drawer_check2_marks_streaming_two_mode_definition_complete`。执行验证命令同上；当前环境已知限制仍为 `Invalid cross-device link (os error 18)`。）
  - `Streaming`：LLM 还在生成，界面边生成边显示。
  - `Snapshot`：LLM 全部生成完成后，一次性显示。
- [x] `Snapshot` 是所有组件的基础能力（默认必须支持）。（已满足：`Drawer` 默认消费完整配置后一次性渲染并输出稳定语义，不依赖增量 token 流；`components/drawer/src/view.rs` 通过 `normalize_open_state/normalize_view_config/resolve_part_states/resolve_part_classes` 完成完整输入归一后渲染 `data-open-state/data-open-mode/data-open-source/data-open-action-source`，满足 snapshot 基线可预测性。N/A：`Drawer` 不直接渲染 LLM 正文，但必须在接收上层完整结果后稳定渲染结构与语义标记。门禁：`scripts/check-ui-streaming.sh` 新增 `cargo test -p ui --test drawer_semantics --no-default-features --features component-drawer,inject-css drawer_check2_documents_snapshot_as_default_baseline_capability` 与 `cargo test -p ui --test drawer_semantics --no-default-features --features component-drawer,inject-css drawer_snapshot_baseline_consumes_complete_result_and_renders_stably`。回归：`components/drawer/test/semantics.rs::drawer_check2_documents_snapshot_as_default_baseline_capability`、`components/drawer/test/semantics.rs::drawer_snapshot_baseline_consumes_complete_result_and_renders_stably`、`components/drawer/test/semantics.rs::drawer_streaming_script_covers_snapshot_baseline_contract`、`components/drawer/test/semantics.rs::drawer_check2_marks_snapshot_baseline_capability_complete`、`components/drawer/test/drawer_semantics.rs::drawer_check2_documents_snapshot_as_default_baseline_capability`、`components/drawer/test/drawer_semantics.rs::drawer_snapshot_baseline_consumes_complete_result_and_renders_stably`、`components/drawer/test/drawer_semantics.rs::drawer_streaming_script_covers_snapshot_baseline_contract`、`components/drawer/test/drawer_semantics.rs::drawer_check2_marks_snapshot_baseline_capability_complete`。执行验证命令同上；当前环境已知限制仍为 `Invalid cross-device link (os error 18)`。）
  - 所有组件都应能消费“完整生成结果”并稳定渲染。
  - 即使组件不直接展示正文，也应能在接收上层完整配置后正常渲染。
- [x] `Streaming` 是否强制，按组件职责判断（不能一刀切）。（已满足：`Drawer` 归类为 `Streaming Optional`，组件职责是 Overlay 容器装配而非 LLM 正文阅读面，默认消费 `Snapshot`；流式兜底通过 `components/sheet/src/logic.rs::agent_contract()` 显式固定 `streaming_attr: "optional"` 与 `fallback_attr: "snapshot"`。语义连续性由 `components/drawer/src/view.rs` 挂载的 `data-open-* + data-ui-state/source/output-status`，以及 `components/sheet/src/view.rs` 的 `role="dialog" + aria-modal + aria-labelledby + aria-describedby + data-ui-fallback/data-ui-output-status` 共同保证；当前输出状态显式为 `verified`。数据校验、断线恢复、重试策略保持在上层编排，不下沉到 Drawer 组件层。回归：`components/drawer/test/semantics.rs::drawer_check2_documents_streaming_required_optional_classification_rules`、`components/drawer/test/semantics.rs::drawer_streaming_optional_scope_keeps_role_aria_and_data_markers_continuous`、`components/drawer/test/semantics.rs::drawer_streaming_validation_retry_resilience_boundaries_stay_outside_component_layer`、`components/drawer/test/semantics.rs::drawer_streaming_script_covers_required_optional_classification_contract`、`components/drawer/test/semantics.rs::drawer_check2_marks_streaming_required_optional_classification_complete`、`components/drawer/test/drawer_semantics.rs::drawer_check2_documents_streaming_required_optional_classification_rules`、`components/drawer/test/drawer_semantics.rs::drawer_streaming_optional_scope_keeps_role_aria_and_data_markers_continuous`、`components/drawer/test/drawer_semantics.rs::drawer_streaming_validation_retry_resilience_boundaries_stay_outside_component_layer`、`components/drawer/test/drawer_semantics.rs::drawer_streaming_script_covers_required_optional_classification_contract`、`components/drawer/test/drawer_semantics.rs::drawer_check2_marks_streaming_required_optional_classification_complete`；门禁脚本：`scripts/check-ui-streaming.sh` 新增对应 `cargo test` 目标。执行验证命令同上；当前环境已知限制仍为 `Invalid cross-device link (os error 18)`。）
  - `Streaming Required`：组件本体就是正文阅读面，用户需要边生成边看。
  - `Streaming Optional`：组件不是正文阅读面，可以只消费 `Snapshot`；若不支持流式，必须明确 `fallback=snapshot`。
  - 无论是否支持 `Streaming`，都要显式标识当前输出状态（草稿/已验证/可提交），并保持 `role`/`aria-*`/`data-*` 连续可读。
  - 数据校验、断线恢复、重试策略由上层负责，组件层只负责稳定渲染。

### 7. 测试、门禁与交付
- [x] 代码卫生（Rust Hygiene）：非测试代码中完全禁止 `unwrap/expect`，禁止无处理的 `let _ = ...`；字符串复制热点收敛为 `Cow<'static, str>`（执行 `./scripts/check-rust-hygiene.sh` 验证）。（已满足：`components/drawer/src/{mod,logic,styles,view,motion}.rs` 非测试源码核验无 `.unwrap(` / `.expect(` / `let _ =`；`components/drawer/src/logic.rs::compose_class_name` 已改为 `Cow<'static, str>` + `Vec<Cow<'static, str>>`，静态类名使用 `Cow::Borrowed`，仅外部 `class_name` 走 `Cow::Owned`。回归：`components/drawer/test/semantics.rs::drawer_rust_hygiene_contract_forbids_unwrap_expect_and_let_underscore_in_non_test_sources`、`components/drawer/test/semantics.rs::drawer_rust_hygiene_string_clone_hotspots_converge_to_cow_or_are_absent`、`components/drawer/test/semantics.rs::drawer_rust_hygiene_script_enforces_repo_level_hygiene_guards`、`components/drawer/test/semantics.rs::drawer_check2_marks_rust_hygiene_contract_complete`、`components/drawer/test/drawer_semantics.rs::drawer_rust_hygiene_contract_forbids_unwrap_expect_and_let_underscore_in_non_test_sources`、`components/drawer/test/drawer_semantics.rs::drawer_rust_hygiene_string_clone_hotspots_converge_to_cow_or_are_absent`、`components/drawer/test/drawer_semantics.rs::drawer_rust_hygiene_script_enforces_repo_level_hygiene_guards`、`components/drawer/test/drawer_semantics.rs::drawer_check2_marks_rust_hygiene_contract_complete`；门禁脚本：`scripts/check-ui-engineering.sh` 新增对应 `cargo test` 目标。另执行：`./scripts/check-rust-hygiene.sh`（当前环境失败：`rg` 缺少 PCRE2 支持，且 `scripts/baseline/api_contract_violations.txt` 存在 baseline drift）。）
- [x] Tree Shaking & 特性剪裁：组件必须注册到 `ui` 特性树（如 `component-accordion`）；`css.rs` 和 `lib.rs` 聚合必须受 feature 门控，禁止无条件全局依赖。（已满足：`crates/ui/Cargo.toml` 维持 `component-drawer = ["dep:ui-drawer"]` 与可选依赖 `ui-drawer = { path = "../../components/drawer", optional = true }`；`crates/ui/src/lib.rs` 以 `#[cfg(feature = "component-drawer")] pub use ui_drawer as drawer;` 做导出门控；`crates/ui/src/css.rs` 以 `#[cfg(feature = "component-drawer")] out.push_str(crate::drawer::styles::CSS);` 聚合并保留 `#[cfg(not(feature = "inject-css"))] pub fn push_components_css(_out: &mut String) {}` no-op 分支。门禁脚本：`scripts/check-ui-tree-shaking.sh` 新增 drawer 专项最小特性树链路（`DRAWER_MIN_FEATURES="component-drawer,inject-css"`、`cargo tree -e features -i ui -p ui --no-default-features --features "$DRAWER_MIN_FEATURES"`、`if grep -q 'all-components' <<<"$DRAWER_TREE_OUTPUT"` 阻断）与最小 wasm 编译（`cargo check -p ui --target wasm32-unknown-unknown --no-default-features --features "$DRAWER_MIN_FEATURES"`），并执行 `drawer_tree_shaking_contract_is_feature_gated_and_budget_guarded` / `drawer_tree_shaking_script_enforces_component_minimal_feature_tree_and_budget` / `drawer_check2_marks_tree_shaking_feature_pruning_contract_complete`。回归：`components/drawer/test/semantics.rs::drawer_tree_shaking_contract_is_feature_gated_and_budget_guarded`、`components/drawer/test/semantics.rs::drawer_tree_shaking_script_enforces_component_minimal_feature_tree_and_budget`、`components/drawer/test/semantics.rs::drawer_check2_marks_tree_shaking_feature_pruning_contract_complete`、`components/drawer/test/drawer_semantics.rs::drawer_tree_shaking_contract_is_feature_gated_and_budget_guarded`、`components/drawer/test/drawer_semantics.rs::drawer_tree_shaking_script_enforces_component_minimal_feature_tree_and_budget`、`components/drawer/test/drawer_semantics.rs::drawer_check2_marks_tree_shaking_feature_pruning_contract_complete`。执行验证命令同上；当前环境已知限制仍为 `Invalid cross-device link (os error 18)`。）
- [x] 语义测试与性能回归：断言必须覆盖 `aria-*`、`data-*` 与焦点流转，不能只看快照；高频/重型组件必须补齐 `render_count` 断言/测量（如初始化空闲预算为 1）。（已满足：`drawer_semantics_tests_cover_contract_matrix_without_snapshot_dependency` 覆盖 `aria-*`/`data-*` 语义矩阵且不依赖快照；`drawer_focus_stack_gc_contract_uses_global_focus_manager_and_policy_restore` 联合 `crates/ui-headless/src/focus_trap.rs` 锁定焦点栈恢复链路；`drawer_performance_governance_contract_is_budgeted_traceable_and_blocking` 持续阻断预算/归因回归；新增 `drawer_semantics_and_performance_regression_cover_aria_data_focus_and_render_count_measurement` 合并语义+性能门禁；脚本门禁：`scripts/check-ui-performance.sh` 新增 `cargo test -p ui --test drawer_semantics --no-default-features --features component-drawer,inject-css drawer_semantics_and_performance_regression_cover_aria_data_focus_and_render_count_measurement`。`render_count` 自动化回归仍在仓库统一 follow-up（`docs/plan/TODO.md`），当前按约定以 mount-only + trace 等价证据过渡；本地验证受环境限制：`Invalid cross-device link (os error 18)`。）
- [x] 版本弃用迁移（Codemod/Registry）：若提交包含跨大版本 API 破坏升级，必须在 Schema Registry 注册弃用窗口并提供纯函数迁移层（`migrate_v1_to_v2`）。（N/A：本次 `Drawer` 未发生跨大版本 API 破坏升级）
  - N/A 判定依据：`components/drawer/src/Component.toml` 保持 `schema_version = "1"`；`components/drawer/src/drawer.rbi` 的 `Drawer(...)` 公共签名未发生破坏性移除/重命名；`components/drawer/src/{mod.rs,logic.rs,view.rs,styles.rs,motion.rs,protocol.rs}` 未引入 `migrate_v1_to_v2`/`deprecation_window`/`SchemaRegistry`/`contract.v2`。
  - 回归锁定：`components/drawer/test/semantics.rs::drawer_version_deprecation_migration_is_na_without_major_breaking_upgrade`、`components/drawer/test/semantics.rs::drawer_version_deprecation_migration_script_covers_engineering_gate`、`components/drawer/test/drawer_semantics.rs::drawer_version_deprecation_migration_is_na_without_major_breaking_upgrade`、`components/drawer/test/drawer_semantics.rs::drawer_version_deprecation_migration_script_covers_engineering_gate`。
  - 脚本门禁：`scripts/check-ui-engineering.sh` 新增 `cargo test -p ui --test drawer_semantics --no-default-features --features component-drawer,inject-css drawer_version_deprecation_migration_is_na_without_major_breaking_upgrade`。
  - 验证记录：执行上述 `cargo test` 命令，当前环境返回 `Invalid cross-device link (os error 18)`（编译产物写入阶段失败）。
- [x] 文档即产品（Copy-Paste Ready）：`apps/docs-app` 必须新增 Playground（Hello World、状态矩阵、受控/非受控对照），支持流式/快照展现，并提供 Source-first 一键复制且补全 imports。（已落地于 `apps/docs-app/src/pages/components/pages/overlays.rs::drawer`：新增并锁定 `Hello World (Minimal API)`、`State Matrix`、`Controlled vs Uncontrolled`、`Streaming / Snapshot Contract` 四类 Playground，且统一通过 `DRAWER_DOC_IMPORTS` 补全导入；Source-first 区块新增 `data-slot="drawer-source-first"` / `data-slot="drawer-source-paths"` 与依赖提示（`component-drawer` + `inject-css`），复制链路复用 `apps/docs-app/src/playground.rs::compose_copy_ready_code`。回归：`components/drawer/test/semantics.rs::drawer_docs_are_copy_paste_ready_with_imports_and_streaming_snapshot_contract`、`components/drawer/test/semantics.rs::drawer_dx_check_script_covers_docs_product_copy_paste_ready_contract`、`components/drawer/test/semantics.rs::drawer_check2_marks_docs_product_copy_paste_ready_contract_complete`、`components/drawer/test/drawer_semantics.rs::drawer_docs_are_copy_paste_ready_with_imports_and_streaming_snapshot_contract`、`components/drawer/test/drawer_semantics.rs::drawer_dx_check_script_covers_docs_product_copy_paste_ready_contract`、`components/drawer/test/drawer_semantics.rs::drawer_check2_marks_docs_product_copy_paste_ready_contract_complete`；脚本门禁：`scripts/check-ui-dx.sh` 新增 `cargo test -p ui --test drawer_semantics --no-default-features --features component-drawer,inject-css drawer_docs_are_copy_paste_ready_with_imports_and_streaming_snapshot_contract`。本地验证命令受环境限制：`Invalid cross-device link (os error 18)`（编译产物写入阶段失败）。）
- [x] 语义测试优先：验证 `data-*` / `aria-*` / role / 状态来源契约，不只视觉快照。
  - 每个交互组件至少有对应 `*_semantics.rs` 测试覆盖关键状态轴与动作语义。
  - 断言应聚焦语义契约（状态来源/可访问性/键盘路径），快照仅作补充。
  - 新增/变更语义字段必须同步补测试，否则不得打勾。
  - 已满足（组件级语义测试）：`components/drawer/test/semantics.rs` 已覆盖关键状态轴与动作语义（`drawer_semantics_tests_cover_contract_matrix_without_snapshot_dependency`），断言聚焦 `data-*`、`aria-*`、`role` 与状态来源标记。
  - 已满足（快照仅补充）：本地语义套件显式禁止将视觉快照当作主判据（`assert_snapshot!`/`to_match_snapshot`/`insta::assert_` 禁用约束），并以键盘/指针语义路径断言为主。
  - 已满足（变更同步补测）：新增 `components/drawer/test/semantics.rs::drawer_semantic_test_priority_prefers_data_aria_role_and_source_contracts_over_snapshot_only_checks` 与 `components/drawer/test/drawer_semantics.rs::drawer_semantic_test_priority_prefers_data_aria_role_and_source_contracts_over_snapshot_only_checks`，并新增脚本回归 `drawer_performance_script_covers_semantic_test_priority_contract`。
  - 语义优先门禁命令：
    - `cargo test -p ui --test drawer_semantics --no-default-features --features component-drawer,inject-css drawer_semantic_test_priority_prefers_data_aria_role_and_source_contracts_over_snapshot_only_checks`
  - 当前环境说明：上述 `cargo test` 命令在本执行环境于依赖编译阶段触发 `Invalid cross-device link (os error 18)`；该阻断为环境问题，非 drawer 语义测试优先契约回归。
  - 回归：`components/drawer/test/semantics.rs::drawer_semantics_tests_cover_contract_matrix_without_snapshot_dependency`、`components/drawer/test/semantics.rs::drawer_semantic_test_priority_prefers_data_aria_role_and_source_contracts_over_snapshot_only_checks`、`components/drawer/test/drawer_semantics.rs::drawer_semantic_test_priority_prefers_data_aria_role_and_source_contracts_over_snapshot_only_checks`、`scripts/check-ui-performance.sh`。
- [x] E2E 选择器稳定：使用语义标记，WASM 场景有稳定等待策略。
  - E2E 选择器优先 `data-*` 语义标记，禁止依赖脆弱 DOM 层级或文本定位。
  - WASM 场景必须使用稳定等待策略（语义状态就绪而非固定 sleep）。
  - 若组件涉及异步/动画，E2E 需显式覆盖 ready/settled 条件。
  - 已满足（语义选择器）：新增 `e2e/tests/docs_app_drawer_contract.spec.mjs`，定位仅使用稳定语义锚点（`data-component="drawer"`、`data-slot="drawer-e2e-right-controls"`、`data-slot="drawer-e2e-open-right"`、`data-slot="drawer-e2e-custom-controls"`、`data-slot="drawer-e2e-open-custom"`、`data-slot="overlay-panel"`、`data-slot="drawer"`、`data-slot="overlay-backdrop"`）；`apps/docs-app/src/pages/components/pages/overlays.rs::drawer` 已同步补齐上述 `data-slot` 挂载点。
  - 已满足（WASM 稳定等待）：E2E 路径统一以 `body:not(:has(#boot))` 作为 wasm 就绪断点，未使用 `waitForTimeout`/固定 sleep。
  - 已满足（ready/settled 覆盖）：覆盖 `Escape` 与 backdrop 点击两条关闭路径，并显式断言 settled 条件（`toHaveCount(0)`）保证动画/退出阶段收敛可观测。
  - 脚本门禁：新增 `components/drawer/scripts/check-ui-e2e-drawer.sh`，接入  
    `cargo test -p ui --test drawer_semantics --no-default-features --features component-drawer,inject-css drawer_check2_documents_e2e_selector_and_stable_wait_rules`  
    `cargo test -p ui --test drawer_semantics --no-default-features --features component-drawer,inject-css drawer_e2e_selector_contract_uses_semantic_markers_and_settled_waits`  
    `cargo test -p ui --test drawer_semantics --no-default-features --features component-drawer,inject-css drawer_e2e_contract_covers_ready_and_settled_conditions_for_overlay_paths`
  - 回归：`components/drawer/test/semantics.rs::drawer_check2_documents_e2e_selector_and_stable_wait_rules`、`components/drawer/test/semantics.rs::drawer_e2e_selector_contract_uses_semantic_markers_and_settled_waits`、`components/drawer/test/semantics.rs::drawer_e2e_contract_covers_ready_and_settled_conditions_for_overlay_paths`、`components/drawer/test/semantics.rs::drawer_e2e_check_script_covers_selector_and_settled_wait_contract`、`components/drawer/test/semantics.rs::drawer_check2_marks_e2e_selector_stability_item_complete`、`components/drawer/test/drawer_semantics.rs::drawer_e2e_selector_contract_uses_semantic_markers_and_settled_waits`、`components/drawer/test/drawer_semantics.rs::drawer_e2e_contract_covers_ready_and_settled_conditions_for_overlay_paths`、`components/drawer/test/drawer_semantics.rs::drawer_e2e_check_script_covers_selector_and_settled_wait_contract`、`components/drawer/test/drawer_semantics.rs::drawer_check2_marks_e2e_selector_stability_item_complete`。
  - 本地验证命令受环境限制：`Invalid cross-device link (os error 18)`（编译产物写入阶段失败）。
- [x] 关键流程纳入可重复回归集合（Playwright/Cypress）。
  - 至少定义一条可重复关键流程（打开/交互/关闭或提交）纳入 E2E 回归。
  - 回归失败需可定位到具体语义契约断点，而不是笼统“页面不一致”。
  - 高风险路径（overlay、focus、keyboard、async）优先进入回归集合。
  - 已满足（可重复关键流程）：`e2e/tests/docs_app_drawer_contract.spec.mjs` 新增 `docs-app drawer key flow is repeatable with semantic breakpoints`，并通过 `for (const cycle of [1, 2])` 固定回放两轮 `open -> interact(Tab) -> close(Escape)`。
  - 已满足（语义断点可定位）：关键断点断言包含 `await expect(rightDrawer).toHaveAttribute("data-open-mode", "controlled")`、`await expect(rightDrawer).toHaveAttribute("data-open-source", "external")`、`await expectFocusInsidePanel(rightPanel)`、`await expect(openRight).toBeFocused()`，失败可直接定位到语义契约断点而非视觉差异。
  - 已满足（高风险路径优先）：新增 `docs-app drawer high-risk paths keep overlay focus keyboard and settled semantic breakpoints`，覆盖 overlay/focus/keyboard（`await page.keyboard.press("Shift+Tab")`）与 settled 收敛（`await expectDrawerSettledClosed(customPanel, customDrawer, customOverlay)`）。
  - 脚本门禁：`components/drawer/scripts/check-ui-e2e-drawer.sh` 新增  
    `cargo test -p ui --test drawer_semantics --no-default-features --features component-drawer,inject-css drawer_check2_documents_e2e_repeatable_key_flow_rules`  
    `cargo test -p ui --test drawer_semantics --no-default-features --features component-drawer,inject-css drawer_e2e_key_flow_is_repeatable_and_failure_points_are_semantic`  
    `cargo test -p ui --test drawer_semantics --no-default-features --features component-drawer,inject-css drawer_e2e_high_risk_paths_cover_focus_keyboard_and_settled_semantic_breakpoints`
  - 回归：`components/drawer/test/semantics.rs::drawer_check2_documents_e2e_repeatable_key_flow_rules`、`components/drawer/test/semantics.rs::drawer_e2e_key_flow_is_repeatable_and_failure_points_are_semantic`、`components/drawer/test/semantics.rs::drawer_e2e_high_risk_paths_cover_focus_keyboard_and_settled_semantic_breakpoints`、`components/drawer/test/semantics.rs::drawer_e2e_check_script_covers_repeatable_key_flow_contracts`、`components/drawer/test/semantics.rs::drawer_check2_marks_replayable_e2e_critical_flow_item_complete`、`components/drawer/test/drawer_semantics.rs::drawer_check2_documents_e2e_repeatable_key_flow_rules`、`components/drawer/test/drawer_semantics.rs::drawer_e2e_key_flow_is_repeatable_and_failure_points_are_semantic`、`components/drawer/test/drawer_semantics.rs::drawer_e2e_high_risk_paths_cover_focus_keyboard_and_settled_semantic_breakpoints`、`components/drawer/test/drawer_semantics.rs::drawer_e2e_check_script_covers_repeatable_key_flow_contracts`、`components/drawer/test/drawer_semantics.rs::drawer_check2_marks_replayable_e2e_critical_flow_item_complete`。
  - 本地验证命令受环境限制：`Invalid cross-device link (os error 18)`（编译产物写入阶段失败）。
- [x] docs-app 文档、示例、参数矩阵、状态矩阵同步更新。
  - 组件行为或参数变更必须同步更新 `apps/docs-app` 示例与说明。
  - 文档示例需覆盖至少一组状态矩阵（受控/非受控、disabled、size/variant 等）。
  - 文档中的 API 名称与默认值必须和 `logic.rs` 当前实现一致。
  - 已满足（docs 示例与说明同步）：`apps/docs-app/src/pages/components/pages/overlays.rs::drawer` 保持 `Hello World (Minimal API)`、`State Matrix`、`Controlled vs Uncontrolled` 三组核心示例，覆盖基础路径与受控/非受控对照。
  - 已满足（状态矩阵覆盖）：`State Matrix` 明确覆盖 `is_open`（受控）、`default_open`（非受控初始化）与 description 分支；`Controlled vs Uncontrolled` 提供并列对照与 `on_open_change` 观测输出。
  - 已满足（API 名称与默认值对齐 logic）：文档示例统一使用 `is_open/default_open/on_open_change` 命名；新增 `data-slot="drawer-defaults-contract"` 固定默认契约说明：`components/drawer/src/logic.rs`（re-export）与 `crates/ui-state-primitives/src/drawer.rs` 的 `id_base="ui-drawer"`、`title="Drawer"`、`default_open=false`。
  - 脚本门禁：`scripts/check-ui-dx.sh` 新增  
    `cargo test -p ui --test drawer_semantics --no-default-features --features component-drawer,inject-css drawer_check2_documents_docs_sync_and_state_matrix_rules`  
    `cargo test -p ui --test drawer_semantics --no-default-features --features component-drawer,inject-css drawer_docs_examples_and_state_matrix_sync_with_logic_api_names_and_defaults`
  - 回归：`components/drawer/test/semantics.rs::drawer_check2_documents_docs_sync_and_state_matrix_rules`、`components/drawer/test/semantics.rs::drawer_docs_examples_and_state_matrix_sync_with_logic_api_names_and_defaults`、`components/drawer/test/semantics.rs::drawer_dx_check_script_covers_docs_sync_and_state_matrix_contract`、`components/drawer/test/semantics.rs::drawer_check2_marks_docs_sync_and_state_matrix_item_complete`、`components/drawer/test/drawer_semantics.rs::drawer_check2_documents_docs_sync_and_state_matrix_rules`、`components/drawer/test/drawer_semantics.rs::drawer_docs_examples_and_state_matrix_sync_with_logic_api_names_and_defaults`、`components/drawer/test/drawer_semantics.rs::drawer_dx_check_script_covers_docs_sync_and_state_matrix_contract`、`components/drawer/test/drawer_semantics.rs::drawer_check2_marks_docs_sync_and_state_matrix_item_complete`。
  - 本地验证命令受环境限制：`Invalid cross-device link (os error 18)`（编译产物写入阶段失败）。
- [x] 组件文档必须对新手友好（Documentation as Product）：组件 README 或等价文档入口必须存在。
  - 每个基础组件必须提供“零门槛”最小示例（Hello World）与常见用法，避免要求用户先理解底层分层架构。
  - 文档需明确“先用起来，再进阶”：默认 API 路径在前，高级控制参数在后。
  - “只有源码没有文档”或“只写给架构师/机器看的文档”视为不通过。
  - 已满足（README 入口存在且新手优先）：`components/drawer/src/README.md` 已提供 `## Hello World（最小可用）`、`## 先用起来，再进阶`、`## 常见用法` 与 `### Controlled Example（高级入口）`，并明确“默认路径在前、进阶控制在后”。
  - 已满足（零门槛最小示例 + 常见用法）：README 保留最小可运行示例（`default_open + id_base + title`）与常见受控/非受控使用方式，不要求先理解底层分层架构。
  - 已满足（等价文档入口可索引）：`apps/docs-app/src/pages/components/pages.rs` 保持 `component_doc!("Drawer", "drawer", "Overlays", overlays::drawer)`，并由 `apps/docs-app/src/pages/components/pages/overlays.rs::drawer` 提供 `Hello World (Minimal API)`、`State Matrix`、`Controlled vs Uncontrolled` 页面入口。
  - 脚本门禁：`scripts/check-ui-dx.sh` 新增  
    `cargo test -p ui --test drawer_semantics --no-default-features --features component-drawer,inject-css drawer_check2_documents_documentation_as_product_rules`  
    `cargo test -p ui --test drawer_semantics --no-default-features --features component-drawer,inject-css drawer_documentation_entry_exists_with_beginner_first_progression`
  - 回归：`components/drawer/test/semantics.rs::drawer_check2_documents_documentation_as_product_rules`、`components/drawer/test/semantics.rs::drawer_documentation_entry_exists_with_beginner_first_progression`、`components/drawer/test/semantics.rs::drawer_dx_check_script_covers_documentation_as_product_contract`、`components/drawer/test/semantics.rs::drawer_check2_marks_documentation_as_product_item_complete`、`components/drawer/test/drawer_semantics.rs::drawer_check2_documents_documentation_as_product_rules`、`components/drawer/test/drawer_semantics.rs::drawer_documentation_entry_exists_with_beginner_first_progression`、`components/drawer/test/drawer_semantics.rs::drawer_dx_check_script_covers_documentation_as_product_contract`、`components/drawer/test/drawer_semantics.rs::drawer_check2_marks_documentation_as_product_item_complete`。
  - 本地验证命令受环境限制：`Invalid cross-device link (os error 18)`（编译产物写入阶段失败）。
- [x] `apps/docs-app` 必须提供 Interactive Playground：用户可在线修改 props/状态并实时预览。
  - Playground 至少支持基础 props 调整、状态切换、交互反馈观察。
  - 对 AI Spec 相关组件，至少提供一组 Spec 输入与预览输出的联动示例。
  - Playground 作为验收面，需可重复复现关键交互路径。
  - 已满足（可在线修改 props/状态并实时预览）：`apps/docs-app/src/pages/components/pages/overlays.rs::drawer` 提供交互式验收面：`State Matrix`（`SegmentedControl` + `is_open/default_open/on_open_change`）与 `Controlled vs Uncontrolled`（并列受控/非受控状态切换），支持基础 props 调整、状态切换与交互反馈观察。
  - 已满足（配置输入与预览输出联动）：`State Matrix` 通过场景切换联动 `description/default_open/is_open`，并在页面实时输出 `"mode/default_open/with_description"` 反馈；`Controlled vs Uncontrolled` 实时输出 `"open"` 与 `"open (reported by on_open_change)"`。
  - N/A：`Drawer` 非 AI Spec 组件（无独立 spec 输入协议）；本条按“交互配置 -> 语义状态反馈”的通用 Playground 验收，不引入伪 spec 层。
  - 已满足（验收面可重复关键流程）：复用 `e2e/tests/docs_app_drawer_contract.spec.mjs` 的两条回放流程（`docs-app drawer key flow is repeatable with semantic breakpoints` 与 `docs-app drawer high-risk paths keep overlay focus keyboard and settled semantic breakpoints`），覆盖 `open -> interact -> close` 双轮回放与语义断点。
  - 脚本门禁：`scripts/check-ui-dx.sh` 新增  
    `cargo test -p ui --test drawer_semantics --no-default-features --features component-drawer,inject-css drawer_check2_documents_interactive_playground_rules`  
    `cargo test -p ui --test drawer_semantics --no-default-features --features component-drawer,inject-css drawer_docs_app_provides_interactive_playground_for_props_state_and_preview`  
    `cargo test -p ui --test drawer_semantics --no-default-features --features component-drawer,inject-css drawer_interactive_playground_reuses_repeatable_semantic_e2e_flow`
  - 回归：`components/drawer/test/semantics.rs::drawer_check2_documents_interactive_playground_rules`、`components/drawer/test/semantics.rs::drawer_docs_app_provides_interactive_playground_for_props_state_and_preview`、`components/drawer/test/semantics.rs::drawer_interactive_playground_reuses_repeatable_semantic_e2e_flow`、`components/drawer/test/semantics.rs::drawer_dx_check_script_covers_interactive_playground_contract`、`components/drawer/test/semantics.rs::drawer_check2_marks_interactive_playground_item_complete`、`components/drawer/test/drawer_semantics.rs::drawer_check2_documents_interactive_playground_rules`、`components/drawer/test/drawer_semantics.rs::drawer_docs_app_provides_interactive_playground_for_props_state_and_preview`、`components/drawer/test/drawer_semantics.rs::drawer_interactive_playground_reuses_repeatable_semantic_e2e_flow`、`components/drawer/test/drawer_semantics.rs::drawer_dx_check_script_covers_interactive_playground_contract`、`components/drawer/test/drawer_semantics.rs::drawer_check2_marks_interactive_playground_item_complete`。
  - 本地验证命令受环境限制：`Invalid cross-device link (os error 18)`（编译产物写入阶段失败）。
- [x] Source-first 文档必须 Copy-Paste Ready：提供一键复制组件源码或最小可用片段能力。
  - docs-app 页面应提供复制按钮，输出代码默认可直接运行（含必要 imports/依赖提示）。
  - 若为 source-first 组件，文档需指向真实源码落点并说明依赖前提，避免“复制即报错”。
  - 文档代码与当前实现必须同步，防止示例漂移。
  - 已满足（copy-ready 一键复制链路）：`apps/docs-app/src/pages/components/pages/overlays.rs::drawer` 的 `data-slot="drawer-source-first"` 明确以 `Show code` 为复制入口，`apps/docs-app/src/playground.rs::compose_copy_ready_code` 负责补全 `DRAWER_DOC_IMPORTS`，保证代码可直接运行。
  - 已满足（真实源码落点 + 依赖前提）：`data-slot="drawer-source-paths"` 列出 `components/drawer/src/{mod,logic,view,styles,motion}.rs`；依赖前提明确 `ui` 特性 `component-drawer` + `inject-css`，避免“复制即报错”。
  - 已满足（文档与实现同步）：source-first 文案与 `DRAWER_DOC_IMPORTS`、`compose_copy_ready_code`、真实源码路径由语义测试锁定，防止示例漂移。
  - 脚本门禁：`scripts/check-ui-dx.sh` 新增  
    `cargo test -p ui --test drawer_semantics --no-default-features --features component-drawer,inject-css drawer_check2_documents_source_first_copy_paste_ready_rules`  
    `cargo test -p ui --test drawer_semantics --no-default-features --features component-drawer,inject-css drawer_docs_source_first_copy_paste_ready_with_real_paths_and_dependencies`
  - 回归：`components/drawer/test/semantics.rs::drawer_check2_documents_source_first_copy_paste_ready_rules`、`components/drawer/test/semantics.rs::drawer_docs_source_first_copy_paste_ready_with_real_paths_and_dependencies`、`components/drawer/test/semantics.rs::drawer_dx_check_script_covers_source_first_copy_paste_ready_contract`、`components/drawer/test/semantics.rs::drawer_check2_marks_source_first_copy_paste_ready_contract_complete`、`components/drawer/test/drawer_semantics.rs::drawer_check2_documents_source_first_copy_paste_ready_rules`、`components/drawer/test/drawer_semantics.rs::drawer_docs_source_first_copy_paste_ready_with_real_paths_and_dependencies`、`components/drawer/test/drawer_semantics.rs::drawer_dx_check_script_covers_source_first_copy_paste_ready_contract`、`components/drawer/test/drawer_semantics.rs::drawer_check2_marks_source_first_copy_paste_ready_contract_complete`。
  - 本地验证命令受环境限制：`Invalid cross-device link (os error 18)`（编译产物写入阶段失败）。
- [x] HeroUI 对标文档与组件文档同步：参数模型变更需同步 `docs/spec/heroui-parameter-design-strategy.md`（必要时补充 `docs/research/spectrum-heroui-style-interface-study.md`），并保证组件文档可访问。
  - 若参数语义发生变化，需同步更新对标策略文档，不允许实现先漂移文档后补。
  - 组件文档入口必须存在（docs-app 页面或等价文档），且可被索引定位。
  - “仅代码更新无文档更新”在接口变更场景下直接判不通过。
  - 已满足（策略文档同步）：`docs/spec/heroui-parameter-design-strategy.md` 新增 `### Drawer 同步记录（2026-02-20）`，明确 `Drawer` 参数主轴与 docs/README 同步约束，并标注“本轮不需要追加 `docs/research/spectrum-heroui-style-interface-study.md`”。
  - 已满足（docs 入口可索引）：`apps/docs-app/src/pages/components/pages.rs` 保持 `component_doc!("Drawer", "drawer", "Overlays", overlays::drawer)`；`apps/docs-app/src/pages/components/pages/overlays.rs::drawer()` 保持 `title="Drawer"` + `slug="drawer"`。
  - 已满足（等价组件文档入口）：`components/drawer/src/README.md` 持续提供 `# Drawer` 与 `## Hello World（最小可用）`。
  - 脚本门禁：`scripts/check-ui-dx.sh` 新增  
    `cargo test -p ui --test drawer_semantics --no-default-features --features component-drawer,inject-css drawer_check2_documents_heroui_benchmark_docs_sync_rules`  
    `cargo test -p ui --test drawer_semantics --no-default-features --features component-drawer,inject-css drawer_heroui_strategy_and_component_docs_are_synchronized_and_indexable`
  - 回归：`components/drawer/test/semantics.rs::drawer_check2_documents_heroui_benchmark_docs_sync_rules`、`components/drawer/test/semantics.rs::drawer_heroui_strategy_and_component_docs_are_synchronized_and_indexable`、`components/drawer/test/semantics.rs::drawer_dx_check_script_covers_heroui_benchmark_docs_sync_contract`、`components/drawer/test/semantics.rs::drawer_check2_marks_heroui_benchmark_docs_sync_contract_complete`、`components/drawer/test/drawer_semantics.rs::drawer_check2_documents_heroui_benchmark_docs_sync_rules`、`components/drawer/test/drawer_semantics.rs::drawer_heroui_strategy_and_component_docs_are_synchronized_and_indexable`、`components/drawer/test/drawer_semantics.rs::drawer_dx_check_script_covers_heroui_benchmark_docs_sync_contract`、`components/drawer/test/drawer_semantics.rs::drawer_check2_marks_heroui_benchmark_docs_sync_contract_complete`。
  - 本地验证命令受环境限制：`Invalid cross-device link (os error 18)`（编译产物写入阶段失败）。

### 8. 合并前门禁死命令（最终执行）
在发起 PR 或完成任务前，必须保证本地/CI 以下命令全部通过：

- `cargo fmt --all -- --check`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `./scripts/check-rust-hygiene.sh`
- `cargo check -p ui --target wasm32-unknown-unknown`
- `cargo check -p ui-headless --no-default-features --features ssr`
- `cargo check -p ui --target wasm32-unknown-unknown --no-default-features --features component-<your_component>,inject-css`

依据文档（`rust-ui/docs/spec` 及 `rust-ui/docs`）：

- `rust-ui/docs/spec/ai_context_projection_protocol.md`
- `rust-ui/docs/spec/architectural_fitness_functions.md`
- `rust-ui/docs/spec/async_state_as_data_command.md`
- `rust-ui/docs/spec/collection_registration_protocol.md`
- `rust-ui/docs/spec/compile_time_evolution_migration.md`
- `rust-ui/docs/spec/component_boundaries.md`
- `rust-ui/docs/spec/component_domains.md`
- `rust-ui/docs/spec/controlled_evolution_sandbox.md`
- `rust-ui/docs/spec/core_shell_protocol_infra_baseline.md`
- `rust-ui/docs/spec/environment_subscription_streams.md`
- `rust-ui/docs/spec/event_light_cone_signal_bus.md`
- `rust-ui/docs/spec/focus_global_stack_gc.md`
- `rust-ui/docs/spec/foreign_zone_escape_hatches.md`
- `rust-ui/docs/spec/headless_purification.md`
- `rust-ui/docs/spec/heroui-parameter-design-strategy.md`
- `rust-ui/docs/spec/hyper-structure-ui-development-playbook.md`
- `rust-ui/docs/spec/i18n.md`
- `rust-ui/docs/spec/intent_stack_semantic_layering.md`
- `rust-ui/docs/spec/kernel_shell_architecture.md`
- `rust-ui/docs/spec/macro_micro_dual_state_machine.md`
- `rust-ui/docs/spec/motion.md`
- `rust-ui/docs/spec/mvp.md`
- `rust-ui/docs/spec/platform_abdication_ecosystem.md`
- `rust-ui/docs/spec/README.md`
- `rust-ui/docs/spec/release_versioning.md`
- `rust-ui/docs/spec/side_effect_command_pattern.md`
- `rust-ui/docs/spec/slot_projection_strategy.md`
- `rust-ui/docs/spec/ssr_hydration_discontinuity.md`
- `rust-ui/docs/spec/state_primitives_core_satellite_split.md`
- `rust-ui/docs/spec/style_island_defense.md`
- `rust-ui/docs/spec/styling.md`
- `rust-ui/docs/spec/tree_shaking.md`
- `rust-ui/docs/spec/ui_layout_split.md`
- `rust-ui/docs/spec/ui_physics_two_pass_rendering.md`
- `rust-ui/docs/spec/unified_causality_bus.md`
- `rust-ui/docs/spec/wasm_generic_bloat.md`
- `rust-ui/docs/起点_也即是目的.md`
- `rust-ui/docs/DOCS_GOVERNANCE.md`
- `rust-ui/docs/DOCS_INDEX.md`
- `rust-ui/docs/philosophy.md`
- `rust-ui/docs/README.md`
- `rust-ui/docs/RULES_ZH.md`
