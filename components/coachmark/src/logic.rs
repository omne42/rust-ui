pub use ui_state_primitives::coachmark::{
    CoachmarkState, CoachmarkStateInput, DEFAULT_ASSET_LABEL, DEFAULT_TITLE, compose_class_name,
    compose_heading, compose_step_label, normalize_modifier_keys, normalize_optional_text,
    resolve_state,
};

const _: Option<CoachmarkState> = None;
const _: &str = DEFAULT_TITLE;
