# 单组件 Check List（Hyper-Structure 完整版）

> 用途：每次新增、重构或修改一个组件时，必须按本清单逐项检查并在 PR 中附带结果。  
> 执行顺序：先过第 1-2 节（架构与状态），再进入第 3-5 节（具体实现）。  
> 术语统一：`status-primitives` 指状态原语层（当前 crate 名为 `ui-state-primitives`）。

### 0. 适用范围与顺序纪律
本清单仅评估“一个组件”的改动结果，不替代仓库级治理。
先过第 1-2 节（架构与状态）再进入第 3-5 节（具体实现）。
不适用的条目必须明确标注 `N/A` 并说明理由，禁止机械打勾。
组件目标、非目标、风险边界已写清楚；发现跨组件/跨层系统性问题时升级为仓库级任务。

### 1. 架构边界与分层约束（Kernel/Shell 总线）
- [x] `status-primitives` 定义：纯状态原语层（受控/非受控、toggle、selection、list、overlay open state、expansion 等）。不依赖 Leptos/DOM/web-sys；只包含 Rust 数据结构和方法，不含视图与事件绑定。（`CircularProgressState* / resolve_state / normalize_optional_text / resolve_aria_label / sanitize_dimension` 固定在 `crates/ui-state-primitives/src/circular_progress.rs`；组件 `components/circular-progress/src/logic.rs` 仅消费 primitive，并在组件层完成 `compose_style_vars/compose_class_name` 样式装配映射。）
  - 所有状态原语必须从 `status-primitives`（`ui-state-primitives`）获取，组件层只能消费，不得自造。
  - 下沉判定依据是“稳定状态不变量”；凡属于状态机、归一化、状态派生能力，默认先进入 `ui-state-primitives`。
  - 组件中可保留的仅是装配逻辑：props 归一、样式来源标记、slot 组织、对 `ui-state-primitives` 输出的映射。
  - 组件内若出现状态原语实现（受控/非受控状态机、single/multiple 展开规则、索引归一化、跨事件状态派生），该项直接判不通过。
  - 处理方式固定：先下沉到 `ui-state-primitives/src/<capability>.rs`（如 `expansion.rs`），在 `ui-state-primitives/src/lib.rs` 导出，再回到组件改调用。
  - 下沉后的原语必须有 `ui-state-primitives` 单元测试；组件侧只保留调用与语义挂载测试。
  - 桥接规范：`ui-state-primitives` 结构体必须是 POJO（Plain Old Rust Object），不持有 Leptos `Signal` 或框架绑定状态容器。
  - 消费规范：`ui-headless` 或组件 `logic.rs` 负责解包 `Signal` 当前值传入 primitive 方法，并将结果显式写回 `Signal`。
  - 设计理由：保持 primitives 纯粹可测、可迁移，不与特定响应式库绑定（便于未来替换响应式实现与做纯 Rust 测试）。
- [x] `ui-headless` 定义：交互与 A11y 原语层（press/focus/hover/roving/listbox/menu/tooltip 等），把输入设备事件与状态语义标准化为可复用契约；输出必须是类型化 `attrs + handlers + state`。不做样式、不写组件 CSS、不做组件级动效编排。（`CircularProgress` 已将 A11y 语义映射下沉至 `crates/ui-headless/src/circular_progress.rs` 的 `CircularProgressContract { attrs, handlers, state }`，组件 `components/circular-progress/src/view.rs` 仅挂载 `use_circular_progress` 输出；该组件无键盘/指针交互，`handlers` 为 typed no-op，属非交互场景的最小合同实现。回归：`crates/ui-headless/src/test/circular_progress.rs` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_consumes_ui_headless_a11y_contract`。）
  **`ui-headless` 落位硬规则（必须执行）**：
  - 输入边界：消费 `status-primitives` 状态 + 用户输入事件（keyboard/pointer/focus）+ 环境能力（web/ssr）。
  - 输出边界：只输出语义契约（attrs/handlers/state）；组件层只负责挂载与组合，不得把语义判断塞回 `view.rs`。
  - 下沉判定依据是“交互/A11y 语义契约”；凡属于键盘/焦点/指针归一、ARIA 映射、交互状态语义能力，默认先进入 `ui-headless`。
  - 必须下沉：键盘模型、焦点模型、跨设备输入归一、ARIA 状态映射、overlay/presence 等交互语义。
  - A11y 契约与共享工具落点固定在 `crates/ui-headless/src/a11y.rs`；组件只在 `view.rs` 挂载，不在组件层重写。
  - 语义契约必须提供 `lang` / `dir`（LTR/RTL）接入能力；headless 不硬编码用户可见文本，文案由 i18n/l10n 层提供。
  - 语义契约正确性必须有回归：`components/*/test/**` 断言语义标记，`e2e/tests/*` 覆盖关键交互流程。
  - 禁止放在 `ui-headless`：视觉 class 选择、CSS 规则、组件 slot 布局、组件专属动效编排、业务文案。
  - 允许留在组件层：纯视觉一次性交互且不形成可复用语义契约（例如单组件局部微交互）。
- [x] `ui-motion` 定义：动效能力与契约执行层（spring、keyframes、WAAPI/RAF backend），只负责时间函数、插值与运行时驱动，不承载组件业务语义与状态决策。（`CircularProgress` 采用 `styles.rs` 的 CSS keyframe 微动效合同，旋转时长已改为 token 优先 `var(--ui-cp-rotation-duration, var(--ui-button-spinner-duration, var(--ui-fallback-button-spinner-duration)))`，组件侧未引入 `ui-motion` 运行时执行器（本组件无 enter/exit/open/close 动画 attach 需求，属 runtime attach N/A）；`crates/ui-motion/src/lib.rs` 保持 non-wasm `web::animate` no-op 与 `prefers_reduced_motion` 预测性降级。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_motion_contract_stays_token_driven_and_runtime_free`。）
  - 放在 `crates/ui-motion`：通用动画数学与执行后端（spring solver、keyframe sampling、easing registry、driver adapters），以及 `wasm/non-wasm` 适配与 `reduced-motion` 执行策略。
  - 放在 `crates/ui-components/src/<component>/motion.rs`：把组件语义状态（open/closed、enter/exit、active/inactive）映射为 `ui-motion` contract，绑定目标节点并调用 attach。
  - 禁止放在 `crates/ui-motion`：组件 slot 结构、组件专属状态机、ARIA/keyboard 语义、业务文案与业务分支。
  - 禁止放在组件 `motion.rs`：自实现 spring/keyframe/driver 执行器；跨组件共享动效算法必须回迁 `ui-motion`。
  - 动效参数优先来自 token/theme；禁止在组件样式与逻辑中散落硬编码时长/曲线/位移常量。
  - 非 wasm 路径必须提供 no-op/stub，保证 SSR/tooling 可编译且行为可预测。
- [x] `ui-theme` 定义：唯一设计 token 与主题上下文层（system/color/scale + Light/Dark/OLED），负责 token 分类、主题映射与 CSS 变量生成。（`CircularProgress` 样式仅消费 `var(--ui-*)` 变量（见 `components/circular-progress/src/styles.rs`），组件 `logic.rs/view.rs` 不重建主题三轴映射；token 基线与三轴来源固定在 `crates/ui-theme/src/tokens.rs` / `crates/ui-theme/src/theme.rs` / `crates/ui-theme/src/css.rs`，并由 `crates/ui-theme/tests/token_scale_baseline.rs` 回归覆盖。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_ui_theme_layer_uses_shared_token_pipeline_without_rebuilding_theme`。）
  - Token 统一基线落点固定：`crates/ui-theme/src/tokens.rs` 定义，`crates/ui-theme/src/theme.rs` 映射，`crates/ui-theme/src/css.rs` 输出变量；组件只在 `crates/ui-components/src/<component>/styles.rs` 消费。
  - 三轴上下文（`system/color/scale`）在 `theme.rs` 定义；组件在 `logic.rs` 选择并在 `view.rs` 生效，`styles.rs` 只消费变量，不重建主题。
  - Token 分类必须可追溯：分类源在 `tokens.rs`，规范同步 `docs/spec/styling.md`；组件不得引入平行私有 token 命名体系。
  - 量化尺寸基准必须可回归：尺寸基准在 `tokens.rs` 与 `theme.rs` 定义，主题回归在 `crates/ui-theme/tests/token_scale_baseline.rs`，组件语义回归在 `components/*/test/*<component>_semantics.rs`。
  - 主题调色与语义色对比必须满足 `WCAG 2.1 AA` 基线，并覆盖 Light/Dark/OLED 主题变体。
  - 主题层只输出 `theme/tokens/base css` 与变量；不实现组件结构、交互逻辑、组件级动效编排。
  - 新增视觉语义先补 token，再由组件消费；禁止“组件临时值先落地、后补 token”的倒序流程。
- [x] `ui-components` 定义：最终 Leptos 组件装配层，组合 `status-primitives + ui-headless + ui-motion + ui-theme` 并暴露稳定公共 API。（`CircularProgress` 目录保持装配边界：`logic.rs` 仅归一/状态派生并消费 `ui-state-primitives`，`view.rs` 仅结构渲染并挂载 `ui-headless` 语义合同，`styles.rs` 仅 token-first 静态样式；公共导出面在 `src/mod.rs` 保持最小且不泄露 `web-sys`/DOM 类型。按“语义测试迁移”要求补齐了本地 `components/circular-progress/test/semantics.rs`，并通过 `src/mod.rs` 的 `#[cfg(test)]` 接入。回归：`components/circular-progress/test/semantics.rs` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_ui_components_layer_keeps_assembly_boundaries`。）
  - `logic.rs` 负责 props 归一与状态派生；`view.rs` 负责结构渲染与 headless 语义挂载；`styles.rs` 负责 token-first 静态样式；`motion.rs` 负责动效 attach。
  - 组件层不得重写 `status-primitives` 状态机或 `ui-headless` 交互契约；发现即判不通过并回迁到对应层。
  - 对外 API 禁止暴露 `web-sys`/DOM 细节类型；平台差异封装在内部模块。
  - 测试文件位于src同级的test/中，内部测试文件同名（如rust-ui/components/accordion/src/logic.rs与rust-ui/components/accordion/test/logic.rs）。
  - 还需要一个semantics.rs用于测试。可能存在类似rust-ui/components/accordion/test/accordion_semantics.rs的旧版实现，需要迁移到新目录。

### 2. API 设计与状态内核（Logic/Kernel）
- [x] API 命名契约统一：公共 props/回调严格使用 `is_*`、`on_*`、`default_*` 前缀；同语义在全库同名，禁止别名漂移。（`CircularProgress` 当前公共 props 为 `aria_label/size_px/thickness_px/class_name/lang/dir`，无布尔状态轴与受控回调轴，因此 `is_*` / `on_*` / `default_*` 在该组件 public props 层面为 N/A；同时未引入 `ariaLabel/className/open/on_open_change/default_open` 等别名漂移。docs 示例保持同名调用。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_api_naming_contract_is_stable_and_n_a_for_state_axis_prefixes`。）
  - 布尔状态统一 `is_*`（如 `is_open`/`is_disabled`），事件统一 `on_*`，默认值统一 `default_*`。
  - 同一语义 across 组件必须同名（如都用 `on_open_change`，禁止同义别名并存）。
  - 公共 API 引入新命名时，需说明与现有命名体系的兼容策略与迁移路径。
- [x] 受控/非受控必须成对：每个可控状态轴都提供 `value + on_value_change + default_value`（如 `open/on_open_change/default_open`）；缺一项即不通过。（`CircularProgress` 是只读 indeterminate 展示组件，不存在可控状态轴，因此该条在本组件为 N/A-by-design：`src/view.rs` / `src/logic.rs` 不暴露 `value/default_value/on_value_change` 或 `open/default_open/on_open_change`，且不存在内部写回“半受控”路径。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_controlled_uncontrolled_pair_contract_is_explicitly_not_applicable` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_controlled_uncontrolled_pair_contract_complete`。）
  - 受控模式：外部值是单一事实来源，内部不得偷偷写回本地状态。
  - 非受控模式：仅由默认值初始化一次，后续状态由内部原语管理。
  - 受控/非受控切换语义需稳定可测，避免“半受控”隐式行为。
- [x] 默认值单一来源：默认值与优先级只在 `logic.rs` 归一化；`view.rs` 禁止二次兜底或隐式改写。（`CircularProgress` 默认值优先级已集中到 `components/circular-progress/src/logic.rs::resolve_component_contract`：`aria_label`（用户显式）> `loading_aria_label`（i18n 注入并 trim 后非空）> `DEFAULT_ARIA_LABEL`。`view.rs` 仅调用 `resolve_component_contract` 并消费 `CircularProgressLogicOutput`，不再调用 `normalize_optional_text/resolve_aria_label/resolve_state` 做二次兜底。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_default_values_are_normalized_once_in_logic` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_single_default_source_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_defaults_are_resolved_in_logic_only`。）
  - 默认值优先级必须可读且可测试（显式规则而非分散 `unwrap_or`）。
  - `view.rs` 不允许再做默认值分支；仅消费 `logic.rs` 的归一化输出。
  - 一旦发现多处默认值来源，直接判不通过并回收至 `logic.rs`。
- [x] 状态归一化集中：状态输入先类型化，再在 `logic.rs` 统一派生；禁止在 `view.rs`、事件回调、样式分支中分散拼状态机。（`CircularProgress` 的输入统一进入 `components/circular-progress/src/logic.rs::resolve_component_contract(CircularProgressLogicInput)` 并派生 `CircularProgressLogicOutput { state, aria_label, lang, class, style_vars }`；`view.rs` 仅消费该输出并挂载 headless 语义，不再做 `resolve_state/normalize_optional_text/resolve_aria_label`。`styles.rs` 仅消费稳定状态标记（`data-state/data-*-source` 与稳定 class），不承载状态判定逻辑。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_state_normalization_is_centralized_in_logic` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_state_normalization_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_state_normalization_flows_from_logic_to_view_and_styles`。）
  - 输入边界统一进入 `logic.rs`，输出统一为可渲染语义状态与来源标记。
  - 事件处理器只触发状态变更，不重建状态机规则。
  - 样式层只消费状态标记，不承担状态判定职责。
- [x] 离散状态必须类型约束：`variant/size/mode/status` 等离散输入使用 `enum`；禁止用多个 `Option<bool>`/字符串自由组合表达互斥状态。（`CircularProgress` 当前无对外“离散互斥状态轴”（无 `variant/mode/status` 输入），该条在本组件为 N/A-by-design：public props 仅 `aria_label/size_px/thickness_px/class_name/lang/dir`；运行态仅固定 indeterminate 语义并由 `logic.rs` + headless 合同派生稳定标记，不存在字符串自由组合状态协议，也不存在多布尔拼一个互斥状态机。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_discrete_state_contract_is_n_a_without_free_string_or_boolean_explosion` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_discrete_state_type_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_has_no_discrete_mutually_exclusive_state_axis`。）
  - 互斥状态优先用 `enum` 建模，利用编译器封住无效组合。
  - 字符串输入若需兼容外部配置，必须先映射到类型化枚举再进入逻辑层。
  - 布尔爆炸（多个 bool 表达一个状态机）应在设计评审阶段直接拦截。
- [x] 状态原语来源正确：组件层只消费 `status-primitives`（当前 `ui-state-primitives`）能力，不直接绑定业务 store；应用级全局状态必须经桥接层适配后再接入组件。（`CircularProgress` 的状态派生入口固定为 `components/circular-progress/src/logic.rs` 对 `ui_state_primitives::circular_progress` 的消费（`CircularProgressState* / resolve_state / resolve_aria_label / normalize_optional_text`），组件层未引入业务 store 类型或全局状态容器；`view.rs` 仅消费 `CircularProgressLogicOutput` 与 headless 语义合同。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_state_primitives_source_stays_in_ui_state_primitives_without_business_store_binding` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_state_primitive_source_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_consumes_state_primitives_without_business_store_coupling`。）
  - 组件中出现可复用状态机实现（受控/非受控、展开规则、选择归一）即判应下沉。
  - 组件与业务全局状态之间必须有适配边界，禁止组件直接依赖业务 store 类型。
  - `logic.rs` 仅做装配与映射，不重新实现状态原语。
- [x] 如果无异步相关，直接打勾。异步交互语义统一：`is_loading`、error/retry、disabled、`aria-busy` 映射一致；优先复用统一 async action 原语（如 `use_async_action`），禁止每组件自定义一套加载/错误协议。（`CircularProgress` 为只读展示组件，无远程请求、无异步状态轴、无失败恢复交互；因此该条在本组件为 N/A-by-design。组件 public props 与语义输出未引入 `is_loading/error/retry/disabled/aria-busy` 协议，也未接入 `use_async_action` 或自定义 async 协议。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_async_interaction_contract_is_not_applicable_for_display_only_component` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_async_contract_complete_for_non_async_component` + `components/circular-progress/test/semantics.rs::circular_progress_has_no_async_loading_error_retry_contract`。）
  - 无异步交互时需明确标注 N/A 理由（例如“组件无远程请求与异步状态”），不是机械打勾。
  - 有异步交互时，`is_loading`/disabled/`aria-busy`/retry 语义必须成套一致，且对键盘与读屏路径可用。
  - 异步失败态要有可恢复路径（重试或回退），并有语义测试覆盖。
- [x] API 易用性验收标准（DX Paradox）：把复杂性留在内部，把简单留给用户。（`CircularProgress` 基础使用无需手动接线 `ui-state-primitives`/`ui-headless` 状态机，调用 `<CircularProgress />` 即可渲染；docs-app 已新增最小可用 `Hello World` 示例（单行源码），组件 public API 未暴露内部 `state` 对象为必填。高级能力通过可选 props（如 `aria_label/size_px/thickness_px/class_name/lang/dir`）按需开启，默认调用路径保持最短。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_dx_paradox_keeps_hello_world_simple_and_hides_internal_state_wiring` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_dx_paradox_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_dx_hello_world_is_minimal_and_does_not_require_internal_state_plumbing`。）
  - 基础用法不得要求用户先理解或手动接线 `ui-state-primitives`/`ui-headless` 状态机。
  - 基础组件 Hello World 示例代码不得超过 5 行（导入与外层模板按仓库约定不计），并可直接运行。
  - 简单需求走简单 API，复杂需求再暴露高级入口：默认 props 覆盖高频场景，高级控制通过受控/扩展参数按需开启。
  - 禁止把内部状态对象作为基础必填参数暴露（例如强制 `state=...` 才能完成点击/展开等基本交互）。
  - docs-app 必须提供最小可用示例，优先展示一眼可懂的默认调用路径。
- [x] 组合型组件主 API 必须“显示优于约定”：优先使用显式组合 `<Parent><Item ... /></Parent>`。（`CircularProgress` 不是组合型容器组件，该条在本组件为 N/A-by-design：public API 无 `children/items/labels/titles/panels/ItemSpec` 等集合装配输入；docs 默认路径为单体 `<CircularProgress />`，不存在并行数组或并行槽位约定。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_composite_parent_item_api_contract_is_not_applicable_for_single_node_component` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_composite_parent_item_api_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_is_not_composite_parent_item_api_surface`。）
  - 每个 item 的标题、语义与内容必须在同一 `Item` 结构维度绑定，避免索引配对式隐式约定。
  - `labels + children`、`titles + panels` 等并行数组/并行槽位写法不得作为默认或推荐 API。
  - 不引入这类语法糖：若为配置式输入，仅允许类型化 `ItemSpec`，并在内部映射为显式 `Item` 语义树。

### 3. 高级交互与物理机制（Shell/Physics）
- [x] 宏观/微观双状态机（Macro/Micro Duality）：拖拽等高频交互在 `Dragging` 期间由 `view/motion` 本地循环执行；禁止每帧穿越回 `logic.rs`，必须在结束时通过 `Action::DragEnd` 回流收敛。（`CircularProgress` 不包含拖拽或高频逐帧交互，该条在本组件为 N/A-by-design：`view.rs/logic.rs/styles.rs` 均无 `Dragging/DragEnd/requestAnimationFrame/pointermove` 路径，组件也无 drag action 回流协议。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_macro_micro_duality_contract_is_not_applicable_without_drag_interaction` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_macro_micro_duality_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_has_no_dragging_macro_micro_duality_loop`。）
- [x] 几何两段式渲染（Two-Pass Rendering）：`Tooltip/Popover/Menu` 等依赖 DOM 测量的组件必须走 `Intent -> Measure(view) -> Rectification(logic)`，并具备幂等收敛保护防死循环。（`CircularProgress` 不依赖 DOM 几何测量（无 overlay 定位、无测量-纠偏回路），该条在本组件为 N/A-by-design：源码中无 `getBoundingClientRect/NodeRef/Measure/Rectification` 路径，也不存在基于测量结果回流 `logic.rs` 的循环。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_two_pass_rendering_contract_is_not_applicable_without_dom_measurement` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_two_pass_rendering_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_has_no_two_pass_measure_rectification_flow`。）
- [x] 集合注册协议（Registration Protocol）：`Accordion/Tabs/Menu` 动态子项必须通过 `RegistrationContext` 上报 `Register/Unregister`，逻辑层维护 `items_order`，禁止依赖 `HashSet` 迭代顺序做导航。（`CircularProgress` 为单节点展示组件，不存在动态子项集合与项级导航，因此该条在本组件为 N/A-by-design。源码与文档路径均无 `RegistrationContext/Register/Unregister/items_order/HashSet` 相关集合编排协议。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_registration_protocol_contract_is_not_applicable_for_single_node_component` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_registration_protocol_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_has_no_registration_context_items_order_protocol`。）
- [x] 插槽投影策略（Slot Projection）：容器组件明确 `Lazy/KeepAlive/Eager`；`KeepAlive` 隐藏时必须通过生命周期通知（如 `NotifyHidden`）暂停轮询/动画等高耗能副作用。（`CircularProgress` 为单节点展示组件，不承载容器型 slot 投影语义，因此该条在本组件为 N/A-by-design：源码与文档路径均无 `Lazy/KeepAlive/Eager/NotifyHidden` 或隐藏态副作用暂停协议。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_slot_projection_contract_is_not_applicable_for_single_node_component` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_slot_projection_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_has_no_slot_projection_keepalive_notifyhidden_protocol`。）
- [x] 环境订阅流（Env Streams）：`Resize/Theme/Intersection` 等环境变化在 `view.rs` 采样、防抖后转化为高层语义 `Action`（如 `BreakpointChanged`）推送到 `logic`；禁止原始事件洪泛。（`CircularProgress` 为单节点展示组件，无布局断点驱动、无环境订阅总线、无交互派生命令回流，因此该条在本组件为 N/A-by-design：源码与文档路径均无 `Resize/Theme/Intersection` 订阅、防抖采样或 `BreakpointChanged` 类高层 action 汇流。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_env_streams_contract_is_not_applicable_for_display_only_component` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_env_streams_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_has_no_env_streams_sampling_debounce_action_flow`。）
- [x] 事件光锥（Event Light Cone）：`Table/Grid` 等大型集合批量操作必须走 `Context Bus + Selector` 与状态压缩表达（如 `SelectionState::All`），禁止 O(N) 级向下 prop drilling。（`CircularProgress` 为单节点展示组件，不存在大型集合批量操作与逐项分发链路，因此该条在本组件为 N/A-by-design：源码与文档路径均无 `Context Bus + Selector`、`SelectionState::All` 或批量选择状态压缩协议。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_event_light_cone_contract_is_not_applicable_for_single_node_component` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_event_light_cone_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_has_no_event_light_cone_context_bus_selector_flow`。）
- [x] 统一因果总线（Causality Bus）：复杂派生总线操作必须支持透传 `TraceId`，确保“用户触发 -> 派生命令 -> 总线广播 -> 订阅者”因果链不断裂。（`CircularProgress` 为单节点展示组件，不存在复杂派生总线与多订阅者广播链路，因此该条在本组件为 N/A-by-design：源码与文档路径均无 `TraceId` 透传、`Causality Bus` 或“触发-派生-广播-订阅”事件总线协议。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_causality_bus_contract_is_not_applicable_for_single_node_component` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_causality_bus_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_has_no_causality_bus_trace_id_propagation_flow`。）
- [x] 存在 A11y 实现、国际化与本地化实现（至少具备接入点，不硬编码用户可见文本）。（`CircularProgress` 已接入完整语义与本地化链路：`view.rs` 通过 `use_circular_progress` 挂载 `role/aria-*` 与 `lang/dir`，并从 `ui_headless::i18n::CommonStrings` 注入 `loading_aria_label`；`logic.rs` 执行 `props aria_label > i18n 注入 > DEFAULT_ARIA_LABEL` 的可覆盖兜底，不在 `view.rs` 硬编码业务文案。共享 A11y 工具来自 `crates/ui-headless/src/a11y.rs::locale_attrs`，组件层未重复发明。该组件为非交互展示型 `progressbar`，无键盘操作目标，键盘路径在本项按 N/A 处理。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_a11y_i18n_l10n_contract_is_mounted_via_headless_and_i18n_chain` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_a11y_i18n_l10n_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_has_a11y_i18n_l10n_contract_without_view_level_hardcoded_copy` + `crates/ui-headless/src/test/circular_progress.rs`。）
  - 交互元素必须具备可验证语义：`role`/`aria-*`/键盘可达路径完整，且和 headless 契约一致。
  - 用户可见文本来源必须可覆盖：优先 props，其次应用注入（`UiRoot`/i18n bundle），最后组件兜底文案；禁止把业务可见文案硬编码在 `view.rs`。
  - 组件需透传或消费 `lang` / `dir`（LTR/RTL）上下文，不得假设单语言单方向。
  - 共享 A11y 工具优先来自 `crates/ui-headless/src/a11y.rs`，组件层不重复发明同名语义工具。
- [x] 状态可观测、可检索、可验证：使用稳定 `data-*` 与 `aria-*` 标记表达状态和来源。（`CircularProgress` 已通过稳定语义标记暴露状态与来源：`view.rs` 挂载 `data-state/data-motion/data-*-source/data-custom-*` 与 `role/aria-label/aria-valuemin/aria-valuemax`；来源轴由 `ui-state-primitives::resolve_state` 归一为封闭集合 `custom/default`，避免自由文本漂移。自动化断言基于语义标记与来源标记，不依赖 DOM 顺序或临时 class。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_state_observability_contract_uses_stable_data_and_aria_markers` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_state_observability_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_exposes_stable_observable_state_markers_for_selectors`。）
  - 稳定语义标记必须覆盖关键状态轴（如 open/expanded/disabled/selected/focus-visible/loading）。
  - 状态来源必须可区分（受控/非受控、默认值/外部值、交互来源），通过稳定 marker 暴露而不是隐式推断。
  - 自动化选择器优先基于语义标记，不依赖 DOM 顺序、层级深度或临时 class 名。
  - 标记值应为封闭集合（可枚举），避免自由文本导致契约漂移。
- [x] 样式依赖显式状态（`data-*`/class），而非脆弱 DOM 结构猜测。（`CircularProgress` 的状态样式完全基于显式语义标记：`styles.rs` 使用稳定 class 与 `data-state/data-motion/data-*-source/data-custom-*` 选择器，不使用 `:nth-child`/深层级结构猜测；`view.rs` 仅透传 `style=style_vars`，运行时样式内容由 `logic.rs::compose_style_vars` 限定为必要 CSS 自定义变量（`--ui-cp-size`/`--ui-cp-thickness`），未内联业务样式逻辑。视觉状态切换由语义标记直接解释，不依赖节点存在性偶然条件。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_style_contract_depends_on_explicit_state_markers_not_dom_structure_guessing` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_style_explicit_state_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_styles_rely_on_explicit_state_markers_not_dom_shape_guessing`。）
  - `styles.rs` 中状态分支选择器必须基于 `data-*`/`aria-*`/稳定 class，禁止用 `:nth-child`、深层级选择器猜测状态。
  - 运行时样式仅允许传递必要 CSS 变量（custom properties）；禁止把业务样式逻辑塞进 inline style。
  - 视觉状态切换必须可由语义标记直接解释，不能依赖“某节点是否恰好存在”。
- [x] 测试验证“语义契约”而不只验证视觉快照。（`CircularProgress` 的测试主路径已是语义契约断言：覆盖 `role/aria-*`、`data-state/data-*-source` 与来源标记，不依赖视觉快照框架。关键分支按适用范围覆盖：受控/非受控、disabled、键盘/指针路径在该非交互展示组件为 N/A-by-design，并已在对应条目有回归；SSR/wasm 差异通过现有 headless/motion 合同与 no-op 路径测试约束。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_semantic_contract_test_suite_prioritizes_contract_markers_over_visual_snapshots` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_semantic_contract_testing_complete` + `components/circular-progress/test/semantics.rs::circular_progress_semantic_tests_prioritize_contract_markers_over_visual_snapshots`。）
  - 至少存在语义测试覆盖关键状态与交互路径（role/aria/data-state/source markers）。
  - 测试矩阵必须覆盖关键分支：受控/非受控、disabled、键盘路径、指针路径、SSR/wasm 差异（按适用范围）。
  - 视觉快照只能作为补充，不得替代语义契约断言。
- [x] 组件文件职责正确：`mod.rs`（导出边界）、`logic.rs`（归一/派生/来源标记）、`styles.rs`（静态 token-first CSS）、`view.rs`（Leptos 结构 + headless 挂载）、`motion.rs`（动效契约 + attach）。（`CircularProgress` 当前文件职责边界清晰：`mod.rs` 仅最小导出；`logic.rs` 负责输入归一、状态派生与来源标记（无 DOM API）；`styles.rs` 仅 token-first 静态 CSS；`view.rs` 仅结构渲染与 headless 语义挂载。`motion.rs` 在本组件为 N/A-by-design：动效通过 token+CSS keyframes 直接表达，无组件运行时 attach 合同，也无自研动效引擎。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_component_file_responsibility_contract_is_correct_with_motion_runtime_not_applicable` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_component_file_responsibility_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_component_files_keep_clear_responsibility_boundaries`。）
  - `mod.rs` 只维护最小稳定导出面与 feature gate，不承载实现细节。
  - `logic.rs` 只做输入归一、状态派生、来源标记；禁止 DOM 操作和样式细节分支。
  - `styles.rs` 只包含 token-first 静态 CSS；禁止硬编码主题常量与业务语义文案。
  - `view.rs` 只做结构渲染与 headless 契约挂载；禁止隐藏关键状态决策。
  - `motion.rs` 只做组件语义到动效契约映射与 attach；禁止在组件内重写通用动效引擎。
- [x] `spec.rs` 只用于少数复杂组件（如 button），避免泛滥。（`CircularProgress` 为简单展示组件，不存在稳定外部 Schema 契约或复杂配置固化需求，因此该条在本组件为 N/A-by-design：组件目录未引入 `src/spec.rs`，`mod.rs` 也未暴露 `spec` 边界；说明与约束集中保留在 `check2.md` 与 docs 示例路径。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_spec_rs_contract_is_not_applicable_for_simple_component` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_spec_rs_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_has_no_spec_rs_builder_schema_contract_surface`。）
  - 仅当组件存在稳定外部规范/Schema 契约或复杂配置固化需求时才引入 `spec.rs`。
  - 简单组件不得为了“形式统一”新增 `spec.rs`；说明文档应留在 `check2.md`/组件文档。
  - 新增 `spec.rs` 必须同步给出契约测试与版本演进说明。
- [x] 组件层遵循 token-first 静态样式契约：样式通过 `styles.rs` 聚合注入；运行时仅传必要 CSS 变量；不把 Utility-First/CSS-in-Rust 当组件库默认范式。（`CircularProgress` 满足 token-first 样式链路：组件样式定义集中在 `components/circular-progress/src/styles.rs`，`crates/ui-components/src/css.rs` 以 feature-gated 方式聚合 `crate::circular_progress::styles::CSS`，并由 `UiRoot` 在 `inject_components_css` 路径下注入。视觉值使用 `var(--ui-*)`/`var(--ui-cp-*)` 变量体系；运行时仅透传 `logic.rs::compose_style_vars` 生成的必要 CSS 自定义变量（`--ui-cp-size`/`--ui-cp-thickness`）。组件未把 Utility-First 或 CSS-in-Rust 作为默认样式契约。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_token_first_static_style_contract_is_aggregated_and_injected_via_uiroot` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_token_first_static_style_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_token_first_style_contract_flows_from_styles_to_css_aggregation_and_uiroot`。）
  - 样式规则统一落在 `styles.rs`，由 `crates/ui-components/src/css.rs` 聚合并通过 `UiRoot` 注入。
  - 颜色/间距/圆角/阴影等视觉值必须来自 `var(--ui-*)`，禁止组件私有 token 体系。
  - Utility-First 仅作为 `apps/*` 应用层布局手段，不得反向污染组件库契约。
  - CSS-in-Rust 仅在有明确类型安全与构建成本净收益时作为例外采用。
- [x] 默认主题美学质量达标（Visual Desire）：以 HeroUI 现代审美为学习对标，默认主题不仅“可用”，还必须“第一眼可信”。（该项在 `CircularProgress` 维度按“跨组件基线”判通过：`apps/docs-app/src/pages/components/pages/theme_visual_baseline.rs` 提供默认主题基线页（覆盖 Button/Input/Overlay 的层级、对比与交互反馈演示），并在 `apps/docs-app/src/pages/components/pages.rs` 注册为 `theme-visual-baseline` 入口；`e2e/tests/docs_app_theme_visual_baseline.spec.mjs` 提供页面与分组件截图基线（page/button/input/overlay）视觉回归；HeroUI 对标遵循“视觉语言与体验质量”而非 API 同构，约束落在 `docs/spec/heroui-parameter-design-strategy.md` 的 Non-Goals。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_visual_desire_contract_is_guarded_by_theme_baseline_page_and_visual_regression` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_visual_desire_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_visual_desire_baseline_is_backed_by_docs_and_playwright_screenshots`。）
  - 默认主题需通过基础美学清单：信息层级清晰（字重/字号/间距）、对比与层次自然、交互反馈明确（hover/active/focus）。
  - docs-app 必须提供默认主题基线页面与截图基线，关键组件（Button/Input/Overlay）纳入视觉回归对比。
  - 禁止“可访问但粗糙”的最低可用心态：视觉退化（类似旧式 Bootstrap 观感）视为质量回归。
  - HeroUI 对标以“视觉语言与体验质量”对齐为目标，不做无差别 API 表层复制。
- [x] Tree Shaking 是一等能力：package 模式支持组件级 feature；source 模式天然裁剪；样式层同步裁剪，禁止无条件聚合全部 CSS，禁止破坏 DCE/LTO 的全量中央注册表。（`CircularProgress` 已接入并满足该契约：`crates/ui-components/Cargo.toml` 定义 `component-circular_progress = ["dep:ui-circular-progress"]`，`lib.rs` 与 `css.rs` 对组件导出/样式聚合均采用 `#[cfg(feature = "component-circular_progress")]` 门控；`cargo tree -e features -p ui-components --no-default-features --features component-accordion,inject-css` 实测仅拉起最小特性链且未出现 `all-components`；`cargo tree -e features -i ui-components -p web-demo` 实测走 `web-demo-components` 而非 `all-components`；`scripts/check-ui-components-tree-shaking.sh` 固化了最小特性 wasm 编译与 release 体积预算校验（`scripts/tree_shaking_budget.env`），并已接入 `.github/workflows/ci.yml`。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_tree_shaking_contract_is_feature_gated_and_prunable_for_package_and_source_modes` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_tree_shaking_contract_has_ci_budget_and_feature_tree_guards` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_tree_shaking_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_tree_shaking_contract_is_backed_by_feature_gates_and_budget_script`。）
  - package 模式必须有组件级 feature（如 `component-accordion`）；未启用组件不得进入编译与链接路径。
  - `lib.rs` 与 `css.rs` 必须按 feature 条件导出/聚合，禁止无条件引用所有组件模块和 CSS 常量。
  - source 模式下仅引入需要的组件源码，不通过中央注册表维持全组件可达。
  - 任意“全量组件映射表/注册表”若导致不可达代码变可达，直接判不通过。
  - 验证命令（特性树）：`cargo tree -e features -p ui-components --no-default-features --features component-accordion,inject-css`，确认仅启用目标组件特性链。
  - 验证命令（反向依赖）：`cargo tree -e features -i ui-components -p web-demo`，检查是否被 `all-components` 或隐式特性全量拉起。
  - CI 检查（最小特性编译）：新增任务仅开启目标最小特性（示例：`cargo check -p ui-components --target wasm32-unknown-unknown --no-default-features --features component-accordion,inject-css`）。
  - CI 检查（体积预算）：对“最小特性构建产物”设定预算并阻断回归（可用固定阈值，如 `< 50KB`，或基于仓库基线的相对阈值）；不得只做编译通过而不做体积约束。
- [x] 类型系统 + 语义标记共同提供机器可读状态；关键输入空间受类型约束。（`CircularProgress` 已满足该契约：关键输入空间由类型约束（`size_px/thickness_px: Option<f64>`、`dir: Option<A11yDirection>`）和状态原语结构（`CircularProgressStateInput`/`CircularProgressState`）承载；无效离散组合通过“无自由字符串协议/无布尔爆炸”路径被设计层拦截并在 `logic.rs` 统一归一化；关键状态通过稳定 `data-*`/`aria-*` 标记暴露（含来源标记 `custom/default`），可供测试与 Agent 自动化消费；契约破坏可由编译器与语义测试直接定位。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_type_system_and_semantic_markers_form_machine_readable_state_contract` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_type_system_semantic_marker_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_type_system_and_semantic_markers_keep_machine_readable_contract` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_discrete_state_contract_is_n_a_without_free_string_or_boolean_explosion` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_state_observability_contract_uses_stable_data_and_aria_markers`。）
  - 离散输入与状态轴必须优先使用 `enum`/新类型建模，避免字符串协议与布尔爆炸。
  - 无效状态要么在类型层不可表达，要么在 `logic.rs` 被统一归一化并可测试。
  - 关键状态必须通过稳定语义标记对外可读，供测试与 Agent 自动化消费。
  - 编译器与测试反馈应能直接定位状态契约破坏点，形成可持续闭环。

### 4. DOM/环境边界治理
- [x] 焦点全局栈（Focus Stack & GC）：层叠 `Overlay` 禁止私存 `NodeRef` 作为恢复目标；必须依赖全局 Focus Manager（如 `FallbackTo/Selector`）防止焦点坠落到 `document.body`。（`CircularProgress` 非 overlay/层叠焦点管理组件，该条在本组件为 N/A-by-design：组件侧 `logic.rs/view.rs` 不持有 `NodeRef`、不做恢复目标私存，也不实现焦点栈逻辑；焦点恢复与防坠落能力统一位于 `crates/ui-headless/src/focus_trap.rs` 的全局 Focus Manager（`FOCUS_MANAGER_STACK` + `RestorePolicy::FallbackTo/Selector`）中。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_focus_stack_contract_is_not_applicable_while_global_focus_manager_stays_in_headless` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_focus_stack_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_focus_stack_contract_is_n_a_and_global_focus_manager_remains_in_headless`。）
- [x] 受控外交特区（Escape Hatches）：集成 ECharts/Map 等命令式第三方库时必须处于 `Foreign Zone`（`YieldControl/CleanupForeign`）；第三方实例不得暴露为组件公共 API 或反向污染状态机。（`CircularProgress` 不涉及 ECharts/Map 等命令式第三方实例集成，该条在本组件为 N/A-by-design：`logic.rs/view.rs` 无第三方实例持有或清理路径（无 `YieldControl/CleanupForeign`、无第三方实例类型暴露），组件公共 API 仍保持纯语义输入，不反向污染状态原语。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_escape_hatch_contract_is_not_applicable_without_imperative_third_party_integration` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_escape_hatch_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_escape_hatch_contract_is_n_a_without_foreign_zone_integration`。）
- [x] SSR 时空断裂治理（Hydration Discontinuity）：逻辑初始化禁止依赖 `now()` 或原生随机 UUID；必须通过 `IdProvider` 注入确定性种子，确保 SSR/Hydration 间 ID 稳定。（`CircularProgress` 本身不生成稳定业务 ID，也不包含 hydration 前后需对齐的 ID 初始化路径，因此该条在本组件为 N/A-by-design：`logic.rs/view.rs/mod.rs` 与 `ui-state-primitives::circular_progress` 不依赖 `now()`、随机 UUID 或随机种子；ID 种子能力统一由 `crates/ui-headless/src/id_provider.rs` 提供（`UiIdProvider::new(seed)` / `provide_ui_id_provider(seed)`），供真正需要稳定 ID 的组件注入。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_hydration_discontinuity_contract_is_not_applicable_without_time_or_random_id_init` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_hydration_discontinuity_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_hydration_discontinuity_contract_is_n_a_without_time_random_or_uuid_init`。）
- [x] SSR 与跨平台检查：覆盖 web/ssr/wasm 分支，不破坏 non-wasm 编译路径。（`CircularProgress` 已补齐 compile-only 与平台守卫证据：`scripts/check-ui-components-platforms.sh` 新增 `component-circular_progress` 的 native/wasm `cargo check`，并保留默认 native + `ui-headless` ssr（native）+ `ui-headless` web（wasm32）三路径；平台分支差异通过 `crates/ui-headless/src/lib.rs` 的 `#[cfg(all(feature = "web", feature = "ssr"))] compile_error!` 与 `crates/ui-motion/src/lib.rs` 的 wasm/non-wasm `cfg` no-op stub 显式管理；`components/circular-progress/src/{mod,logic,styles,view}.rs` non-wasm 路径不引用 `web-sys`/浏览器对象。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_cross_platform_compile_contract_covers_default_native_ssr_and_wasm_paths` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_cross_platform_ssr_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_cross_platform_compile_contract_uses_explicit_cfg_and_feature_guards`。）
  - 至少包含 compile-only 证据：web（wasm32）、ssr（native）、默认本地构建三条路径。
  - 平台分支差异必须显式 `cfg` 或 feature 管理，禁止依赖运行时偶然行为。
  - non-wasm 路径禁止引用 `web-sys`/浏览器对象。
- [x] `ui-headless` web/ssr feature 互斥受 `compile_error!` 保护（`crates/ui-headless/src/lib.rs`）。（`CircularProgress` 依赖 `ui-headless` 语义合同（`view.rs` 挂载 `use_circular_progress`）且未破坏互斥约束：`crates/ui-headless/src/lib.rs` 保持 `#[cfg(all(feature = "web", feature = "ssr"))] compile_error!(...)`；`scripts/check-ui-components-platforms.sh` 同时覆盖了 `ui-headless` 的 ssr native 编译、web wasm 编译，并显式校验 `--features web,ssr` 必须失败且错误信息含 `mutually exclusive`。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_ui_headless_web_ssr_mutex_contract_is_guarded_by_compile_error_and_platform_gate` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_ui_headless_web_ssr_mutex_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_ui_headless_web_ssr_mutex_contract_is_compile_error_guarded_and_platform_checked`。）
  - 组件依赖 `ui-headless` 能力时，不得破坏其 web/ssr 互斥约束。
  - 组件若新增 headless 功能接入，需验证两条 feature 路径都可编译。
  - 发现“同时启用 web+ssr 仍可过编译”视为契约回归。
- [x] `ui-motion` 非 wasm 提供 no-op/stub（`crates/ui-motion/src/lib.rs`），保证 SSR/tooling 可编译。（`CircularProgress` 在组件层不依赖 runtime motion handle（无 `motion.rs` attach 调用，`view.rs/logic.rs` 不假设动画句柄存在），可在 non-wasm 路径安全降级；`crates/ui-motion/src/lib.rs` 保持 `#[cfg(not(target_arch = "wasm32"))]` 的 `prefers_reduced_motion() -> true` 与 `animate` no-op stub，并带有 non-wasm 预测性单测；`scripts/check-ui-components-platforms.sh` 同时覆盖 `ui-motion` native/wasm compile-only 与 `non_wasm_stub`/`spring` tooling 测试，确保测试/文档/静态分析链路不被 motion 依赖阻塞。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_ui_motion_non_wasm_stub_contract_is_predictable_and_tooling_safe` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_ui_motion_non_wasm_stub_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_ui_motion_non_wasm_stub_contract_is_predictable_and_tooling_safe_locally`。）
  - `motion.rs` 调用必须可在 non-wasm 下安全降级，不触发 panic。
  - 组件不得假设动画句柄一定存在；no-op 分支行为需可预测。
  - toolchain 场景（测试/文档/静态分析）不得因 motion 依赖阻塞编译。
- [x] 组件实现覆盖 `reduced-motion` / SSR / wasm 分支。（`CircularProgress` 已覆盖三条分支语义：`styles.rs` 通过 `@media (prefers-reduced-motion: reduce)` 将旋转降级为最小必要反馈（`animation-duration: 1ms; animation-iteration-count: 1;`）；SSR/hydration 路径维持稳定语义输出（`view.rs` 与 `ui-headless/src/circular_progress.rs` 不做 wasm/ssr 条件分裂，`role/aria/data-*` 合同恒定）；wasm 路径允许平台增强但不改变语义契约（`scripts/check-ui-components-platforms.sh` 覆盖 `ui-headless` ssr native 编译、`ui-components` circular-progress wasm 编译、`ui-motion` wasm 编译，并新增 `circular_progress_reduced_motion_ssr_wasm_branches_keep_semantics_consistent` 契约测试入口）。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_reduced_motion_ssr_wasm_branches_keep_semantics_consistent` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_reduced_motion_ssr_wasm_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_reduced_motion_ssr_wasm_branches_keep_semantics_consistent_locally`。）
  - `reduced-motion` 下动画应跳过或降级为最小必要反馈。
  - SSR 输出必须与客户端 hydration 兼容，避免首帧语义错位。
  - wasm 分支允许增强交互，但语义契约不得与 SSR 分支分裂。
- [x] 性能治理：关键路径有预算（首次渲染/更新耗时/内存），回归可检测、可归因、可阻断。（`CircularProgress` 已补齐性能治理证据链：`apps/docs-app/src/pages/components/shell.rs` 为 `circular-progress` 增加显式 `UiPerfBudget { max_mount_ms: 20.0, max_update_ms: Some(6.0), max_heap_kb: Some(320.0) }`，并保留 `button/input` 初始化预算基线；`apps/docs-app/src/perf_probe.rs` 与 `e2e/tests/docs_app_components_coverage.spec.mjs` 提供可重复阈值检测与失败标记（`data-perf-*`/`data-perf-violation`）；状态/渲染/样式/动效归因路径在 `view.rs`/`logic.rs`/`styles.rs` 可检索（`resolve_component_contract`、`data-state/data-motion`、`@media (prefers-reduced-motion: reduce)`）；`scripts/check-ui-components-performance.sh` 新增本组件阻断测试入口。`render_count` 方面当前测试栈未提供稳定自动计数能力，按清单要求采用 mount+budget 等价证据并通过 `docs/plan/TODO.md` 持续跟踪自动化补齐。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_performance_governance_budget_is_defined_and_blocking` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_performance_check_script_covers_budget_and_follow_up_gates` + `components/circular-progress/test/semantics.rs::circular_progress_performance_governance_budget_is_defined_and_blocking_locally`。）
  - 关键交互组件需定义最小预算项（首渲染、关键更新、内存/分配趋势）。
  - 回归检测至少具备可重复基线与失败阈值，不靠主观“感觉变慢”。
  - 性能问题需可归因到状态、渲染、样式或动效路径之一。
  - 基础组件预算基线：`Button`、`Input` 在初始化后（无交互、无 props 变化）渲染次数预算为 `1`；出现额外渲染需给出合理解释或修复。
  - 测试要求：在 `components/*/test/**` 增加 `render_count` 类回归测试（测试框架支持时必须启用）；至少覆盖基础组件与本次改动组件。
  - 若当前测试框架暂不支持精确渲染计数，需提供等价证据（可重复 profiling/trace 基线）并在后续任务中补齐自动化 `render_count` 测试。
- [x] `view!` 宏复杂度受控：单个 `view!` 块不得承载超长深嵌套结构；复杂布局按语义分块，避免一次性宏展开导致编译与 wasm 体积劣化。（`CircularProgress` 当前 `components/circular-progress/src/view.rs` 保持单一、扁平 `view!` 渲染块（仅一个 `<span>` + 语义属性挂载），无深层嵌套、无列表展开、无循环拼装；`view.rs` 规模受控（单文件小体量），不需要为“形式统一”引入额外子渲染函数。`scripts/check-ui-components-view-macro.sh` 已新增本组件宏复杂度 gate（最小 feature 构建路径）。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_view_macro_complexity_is_small_and_does_not_require_semantic_subrenders` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_view_macro_check_script_covers_complexity_gate` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_view_macro_complexity_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_view_macro_complexity_is_small_and_does_not_require_semantic_subrenders_locally`。）
  - 复杂结构按语义子块拆分（header/body/item 等），避免巨型单块 `view!`。
  - `view.rs` 中若出现多层嵌套重复片段，应优先提取局部渲染函数。
  - 编译时间/产物体积异常增长时，优先排查宏展开体量。
- [x] 函数式拆分优先：不涉及复杂状态与生命周期管理的 UI 片段，优先拆为普通 Rust 函数（返回 `impl IntoView`/`View`），而不是新增 `#[component]`。（`CircularProgress` 目前为单节点简单结构，`components/circular-progress/src/view.rs` 仅保留一个公共组件边界 `#[component] pub fn CircularProgress`，未引入任何局部 `#[component]` 子组件或 `render_*` 抽象噪音；在“无复杂子片段”前提下，保持最小边界即为函数式拆分优先的等价实现（不为形式而拆）。同时稳定语义标记（如 `data-state`/`data-motion`/`role`/`aria-*`）未因拆分策略变化而漂移。`scripts/check-ui-components-view-macro.sh` 已新增本组件 function-first gate。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_view_functional_split_prefers_no_extra_local_components_for_simple_layout` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_view_macro_check_script_covers_functional_split_gate` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_view_functional_split_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_view_functional_split_prefers_no_extra_local_components_for_simple_layout_locally`。）
  - 纯静态或轻逻辑片段优先函数化；仅在需要独立 props 语义时升级为组件。
  - 禁止把所有局部片段都升格为 `#[component]` 导致抽象噪音。
  - 拆分后语义标记与测试定位仍需稳定。
- [x] 静态片段常量化：复杂 SVG、页脚、长说明文本等纯静态内容优先常量化/模板化，减少重复 `view!` 渲染指令生成。（`CircularProgress` 为单节点指示器组件，`components/circular-progress/src/view.rs` 不包含复杂 SVG/path、长说明文本、页脚/分区静态模板等重静态片段；在该前提下该条以 “constantized-or-absent” 路径通过：避免引入重复静态构造而保持最小渲染面。组件语义可访问标记（`role`、`aria-label`、`aria-valuemin`、`aria-valuemax`、`data-state`、`data-motion`）保持稳定，未因静态片段策略发生漂移。`scripts/check-ui-components-view-macro.sh` 已新增本组件 static-fragment gate。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_static_fragments_are_constantized_or_absent_for_simple_indicator_layout` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_view_macro_check_script_covers_static_fragment_gate` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_static_fragment_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_static_fragments_are_constantized_or_absent_for_simple_indicator_layout_locally`。）
  - 可判定为纯静态的片段应避免重复动态构造。
  - 常量化后仍需维持可访问语义（title/aria-label/role 等）。
  - 静态资源变更路径要清晰，避免散落在多个 `view!` 片段中。
- [x] `inner_html` 使用约束：仅允许注入受信任静态常量，禁止拼接用户输入；使用处必须补充语义与安全回归测试。（`CircularProgress` 组件与 docs 示例均未使用 `inner_html/set_inner_html/dangerously_set_inner_html`，也未引入 `<script>`、`javascript:` 或模板拼接注入路径；该组件语义输出统一走类型化 attrs 挂载（`role/aria/data-*`），不存在原始 HTML 注入面。`scripts/check-ui-components-inner-html.sh` 已新增本组件 security gate。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_inner_html_usage_is_forbidden_in_component_and_docs_examples` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_inner_html_check_script_covers_security_contract` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_inner_html_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_inner_html_usage_is_forbidden_in_component_and_docs_examples_locally`。）
  - 仅允许编译期常量或明确白名单内容进入 `inner_html`。
  - 严禁直接或间接注入用户输入、远端返回或未清洗模板字符串。
  - 使用 `inner_html` 的节点必须补语义测试与安全回归说明。
- [x] WASM 调试要求：关键状态可追踪（来源/时间/前后值），关键交互可回放，开发模式有可视化入口，调试能力通过 feature 隔离不污染产物。（`CircularProgress` 属非交互展示组件，交互回放在本组件维度为 N/A-by-design（`view.rs` 无 pointer/keyboard/input handlers）；但关键状态可追踪能力已通过稳定语义与来源标记暴露（`data-state/data-motion/data-size-source/data-thickness-source/data-label-source/data-class-source` + `role/aria-*`）。开发模式可视化入口与时间线由全局 `ui_headless::provide_ui_trace` + `apps/docs-app/src/debug_overlay.rs::UiDebugOverlay` 统一提供；组件未引入私有 wasm-debug feature（仅复用共享 `button-wasm-debug` 基线），也未在公共 API 泄露调试开关。`scripts/check-ui-components-wasm-debug.sh` 已新增本组件 gate。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_wasm_debug_contract_is_explicitly_na_and_feature_isolated` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_wasm_debug_check_script_covers_shared_contract` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_wasm_debug_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_wasm_debug_contract_is_explicitly_na_and_feature_isolated_locally`。）
  - 开发模式下至少能追踪关键状态变更来源与前后值。
  - 关键交互链路应支持最小可复现记录（事件顺序/状态转移）。
  - 调试开关默认不进入生产包体与公共 API。
- [x] DX 要求：样式热重载优先无需重编 wasm；组件热开发尽量保持上下文；提供可选状态保留；有 Workbench 隔离画布。（`CircularProgress` 作为非交互展示组件，在 docs 端通过 `Playground` 提供隔离演练入口（Hello World / Matrix / Custom 三组 demo），并复用 `apps/docs-app/src/playground.rs` 的 scoped CSS test 面板实现样式热重载（无需重编 wasm）。组件本身无可持久化交互状态轴，因此“可选状态保留”在本组件维度为 N/A-by-design（无 `load/save/clear_workbench_*` 路径），同时保持公共 API 不被调试状态污染。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_dx_playground_supports_css_hot_reload_without_wasm_rebuild` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_dx_non_interactive_scope_keeps_isolated_canvas_and_marks_persist_state_na` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_dx_check_script_covers_hot_reload_and_isolated_canvas_contract` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_dx_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_dx_non_interactive_scope_keeps_isolated_canvas_and_marks_persist_state_na_locally`。）
  - 常见样式调整应走快速反馈路径，不依赖完整 wasm 重编译。
  - 组件调试应尽量保持当前交互上下文，降低重复操作成本。
  - 复杂交互组件应有隔离演练入口（workbench/story/demo 之一）。
- [x] 工程能力统一：`serde` 负责 spec 序列化/版本迁移/错误结构化；`tracing` 统一 span/event 语义；async 不绑定单一运行时（tokio/async-std），runtime 细节不泄露到上层 API。（`CircularProgress` 为简单展示组件，不引入 `spec.rs` 与配置序列化入口，因此 `serde/spec` 迁移路径在本组件为 N/A-by-design；`tracing` 语义复用全库共享基线（`button-wasm-debug` / `accordion-wasm-debug`）且组件本身不新增局部 `span/event` target，避免语义漂移；组件公开边界与实现文件（`mod.rs/logic.rs/view.rs/styles.rs`）未泄露 `tokio`/`async-std`/`runtime::Handle` 等运行时细节。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_engineering_contract_marks_spec_serde_path_as_na_for_simple_component_scope` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_engineering_contract_keeps_tracing_semantics_unified_without_component_local_events` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_engineering_contract_avoids_runtime_leaks_in_public_api_surface` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_engineering_check_script_covers_serde_tracing_and_runtime_boundaries` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_engineering_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_engineering_contract_is_spec_free_tracing_aligned_and_runtime_agnostic_locally`。）
  - 若组件涉及 spec/config 输入，序列化与错误输出应走统一结构化路径。
  - 关键流程埋点语义应与全库 tracing 约定一致，避免组件各说各话。
  - 异步边界不得把具体 runtime 类型暴露到组件公共接口。

### 5. 样式与动效（Theme & Motion）
- [x] 样式孤岛防御（Defensive Variables）：`styles.rs` 使用双层回退链 `var(--ui-*, var(--ui-fallback-*))`；禁止组件内硬编码 Hex 或裸尺寸终值，Fallback 终值由 `ui-theme` 统一输出（SSOT）。（`components/circular-progress/src/styles.rs` 已将尺寸/线宽/边框/主题色/动效时长收敛为双层回退链（如 `var(--ui-cp-size, var(--ui-button-spinner-size, var(--ui-fallback-button-spinner-size)))`、`var(--ui-cp-thickness, var(--ui-button-spinner-border, var(--ui-fallback-button-spinner-border)))`、`var(--ui-cp-rotation-duration, var(--ui-button-spinner-duration, var(--ui-fallback-button-spinner-duration)))`），并移除裸终值 `16px/2px/800ms` 与 `border-radius: 9999px`。fallback 终值由 `crates/ui-theme/src/css.rs` 统一输出（新增 `--ui-fallback-button-spinner-size` / `--ui-fallback-button-spinner-border` / `--ui-fallback-button-spinner-duration`）。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_styles_use_defensive_variable_fallback_chain` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_defensive_variables_check_script_covers_style_fallback_contract` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_defensive_variables_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_styles_use_defensive_variable_fallback_chain_locally`。）
- [x] 级联层覆盖（`@layer ui`）：组件 CSS 默认聚合进 `@layer ui`；运行时数值调整仅通过 CSS Custom Properties（如 `style:--x=...`），禁止普通内联样式（如 `style=\"top: 10px\"`）。（`CircularProgress` 的样式聚合路径已固定在 `crates/ui-components/src/css.rs` 的 `@layer ui` 包裹区间，并通过 feature gate 注入 `crate::circular_progress::styles::CSS`；组件运行时样式仅由 `components/circular-progress/src/logic.rs::compose_style_vars` 生成 `--ui-cp-size/--ui-cp-thickness` 两个 CSS 变量，并在 `view.rs` 以 `style=style_vars` 挂载。组件源码不存在 `style=\"top:*\"`、`style=\"left:*\"` 等普通内联布局样式写法。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_cascade_layer_and_runtime_style_contract_is_enforced` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_cascade_layer_check_script_covers_layer_and_inline_style_guard` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_cascade_layer_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_cascade_layer_and_runtime_style_contract_is_enforced_locally`。）
- [x] Motion 合同化：`stiffness`/`damping` 等参数在 `motion.rs` 内置为组件 Contract，并通过 `attach_motion` 挂载；必须尊重 `prefers-reduced-motion` 且在 non-wasm/SSR 安全降级（no-op）。（`CircularProgress` 作为单节点展示组件，动效合同采用 N/A-by-design 的 runtime attach 路径：不新增 `motion.rs` 与 `attach_motion`，避免为静态旋转指示器引入多余运行时状态。组件动效由 `styles.rs` 的 token-first keyframe 表达，并通过 `@media (prefers-reduced-motion: reduce)` 做最小反馈降级；`crates/ui-motion/src/lib.rs` 在 non-wasm 路径提供 `prefers_reduced_motion() -> true` 与 `animate` no-op stub，SSR/tooling 可预测编译。平台门禁继续覆盖 `component-circular_progress` native/wasm compile-only 与 reduced-motion 分支。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_motion_contract_is_explicitly_na_for_runtime_attach_and_keeps_reduced_motion_noop_guards` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_motion_contract_check_script_covers_reduced_motion_and_noop_guards` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_motion_contractualization_complete` + `components/circular-progress/test/semantics.rs::circular_progress_motion_contract_is_explicitly_na_for_runtime_attach_and_keeps_reduced_motion_noop_guards_locally`。）
- [x] `ui-components` 固定入口文件落点正确。（`crates/ui-components/src/lib.rs` 保持 `component-circular_progress` feature gate + `pub use ui_circular_progress as circular_progress`，且未泄露 `web-sys/NodeRef/HtmlElement/JsValue` 等平台细节；`crates/ui-components/src/css.rs` 保持 `push_components_css` + `@layer ui` 聚合并以 `#[cfg(feature = "component-circular_progress")] out.push_str(crate::circular_progress::styles::CSS);` 按特性注入；`crates/ui-components/src/root.rs` 集中注入 `BASE_CSS + theme vars + (optional) components css` 并提供 `provide_ui_i18n/provide_ui_id_provider`；`crates/ui-visual-primitive/src/active_highlight.rs` 仅提供共享高亮样式与 motion driver，不含组件业务语义；`crates/ui-components/src/overlay_open.rs`/`presence.rs`/`a11y.rs` 不存在，且 canonical 能力文件固定在 `crates/ui-headless/src/{controllable_state.rs,presence.rs,a11y.rs}`。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_ui_components_fixed_entry_files_follow_layered_boundaries` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_entrypoints_check_script_covers_fixed_entry_files_contract` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_ui_components_fixed_entry_files_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_ui_components_fixed_entry_files_follow_layered_boundaries_locally`。）
  - `crates/ui-components/src/lib.rs`：总模块入口 + 对外 `pub use`（公共 API 面）；组件模块受 `component-*` feature gate 约束；不暴露内部平台细节类型。
  - `crates/ui-components/src/css.rs`：组件 CSS 聚合入口（`push_components_css`）；按 feature 条件注入；禁止无条件聚合全部组件 CSS。
  - `crates/ui-components/src/root.rs`：`UiRoot` 统一注入 base css + theme vars +（可选）components css，并提供全局 i18n 上下文；主题与注入策略必须集中在此。
  - `crates/ui-visual-primitive/src/active_highlight.rs`：共享高亮条样式与 motion driver；只承载通用高亮动效能力，不承载具体组件业务语义。
  - `crates/ui-components/src/overlay_open.rs`：当前仓库中不应存在；open-state 原语固定在 `crates/ui-headless/src/controllable_state.rs`，组件通过 headless API 消费。
  - `crates/ui-components/src/presence.rs`：当前仓库中不应存在；presence 原语固定在 `crates/ui-headless/src/presence.rs`，组件通过 `ui_headless::use_presence` 消费。
  - `crates/ui-components/src/a11y.rs`：当前仓库中不应存在；共享 A11y 工具固定在 `crates/ui-headless/src/a11y.rs`（如 `aria_controls_when_open`），组件只负责挂载。
- [x] 组件目录标准文件落点正确。（`components/circular-progress/src` 仅保留 `mod.rs/logic.rs/styles.rs/view.rs` 四个标准文件；`mod.rs` 维持最小稳定导出面（仅暴露 `CircularProgress` 与 `DEFAULT_ARIA_LABEL`，不公开 `logic/view` 内部实现）；`logic.rs` 负责 props 归一化、状态派生与来源标记，且不承载 DOM/web-sys 操作；`styles.rs` 保持静态 token-first CSS（`var(--ui-*)`），不混入渲染/语义挂载逻辑；`view.rs` 仅做 Leptos 结构渲染与 headless 契约挂载，未引入 `render.rs` 漂移。`motion.rs` 与 `spec.rs` 在本组件维度为 N/A-by-design：`CircularProgress` 为非交互展示组件，不需要 `attach_motion` 运行时挂载，也不存在复杂 schema/spec 构建契约。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_component_directory_standard_files_follow_contract_and_na_paths` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_component_files_check_script_covers_standard_directory_contract` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_component_directory_standard_files_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_component_directory_standard_files_follow_contract_and_na_paths_locally`。）
  - `<component>/mod.rs`：最小稳定导出面，存在且无过度导出。
  - `<component>/logic.rs`：props 归一化、派生状态、来源标记；不得承载可下沉原语。
  - `<component>/styles.rs`：静态 CSS 契约，只用 `var(--ui-*)`，不写死主题常量。
  - `<component>/view.rs`：纯 Leptos 结构渲染 + headless 语义挂载；禁止 `render.rs` 漂移；不隐藏关键状态决策。
  - `<component>/motion.rs`：`XxxMotion + attach_motion`；交互组件必须有；只做语义到 motion contract 的映射与挂载。
  - `<component>/spec.rs`：仅极少数组件专用（当前主要 button），无必要不新增。

### 6. AI 原生能力与文件落点（Struct-First & Projection）
- [x] 文件落点纪律：组件目录严格由 `mod.rs`（导出）、`logic.rs`（归一派生）、`styles.rs`（Token 样式）、`view.rs`（渲染）、`motion.rs`（动效）组成；复杂组件可选 `spec.rs`；禁止 `render.rs`。（`CircularProgress` 在本组件维度按“核心文件 + N/A 例外”满足该契约：`components/circular-progress/src` 保留 `mod.rs/logic.rs/styles.rs/view.rs`；`render.rs` 不存在；`motion.rs` 与 `spec.rs` 在当前非交互展示组件范围内为 N/A-by-design（无 runtime attach motion、无复杂 spec/schema 固化需求）。该约束由组件目录回归与脚本门禁共同覆盖。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_component_directory_standard_files_follow_contract_and_na_paths` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_component_files_check_script_covers_standard_directory_contract` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_file_placement_discipline_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_component_directory_standard_files_follow_contract_and_na_paths_locally`。）
- [x] Hyper-Structure Builder（`spec.rs`）：复杂组件必须提供 AI 友好的 `*Spec::new()...render()` 建造者 API。（`CircularProgress` 为简单展示组件，不属于需引入 `spec.rs` 的复杂配置域，因此该条在本组件维度为 N/A-by-design：组件目录不存在 `src/spec.rs`，`mod.rs/view.rs/logic.rs` 与 docs 入口均不暴露 `Spec::new()...render()` 或 `CircularProgressSpec` 构建路径。若未来演进为复杂配置组件，再按本条补齐 `spec.rs` 建造者 API。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_hyper_structure_builder_spec_contract_is_not_applicable_for_simple_component` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_component_files_check_script_covers_hyper_structure_builder_contract` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_hyper_structure_builder_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_hyper_structure_builder_spec_contract_is_not_applicable_for_simple_component_locally`。）
- [x] 上下文压缩协议（Manifest + RBI）：新增/大改组件必须同步维护组件目录下 `Component.toml`（能力清单）和 `.rbi`（接口签名投影），避免 AI 检索工具箱过时。（`CircularProgress` 已补齐 `components/circular-progress/src/Component.toml` 与 `components/circular-progress/src/circular_progress.rbi`：Manifest 声明组件输入/输出、能力位与依赖边界；RBI 提供稳定签名投影（`DEFAULT_ARIA_LABEL` 与 `CircularProgress(...)`）。该约束已接入组件文件门禁，避免后续改动遗漏上下文压缩资产。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_context_compression_manifest_and_rbi_are_present_and_consistent` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_component_files_check_script_covers_context_compression_contract` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_context_compression_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_context_compression_manifest_and_rbi_are_present_and_consistent_locally`。）
- [x] 语义标记统一升级为 Agent Contract（Schema 化），让 Agent 不依赖 DOM 猜测理解组件状态与意图。（`CircularProgress` 已升级为类型化 Agent Contract 输出：`logic.rs` 提供 `CircularProgressAgentContract`（`schema/intent/action/state/source` + 各状态来源轴）与 `resolve_agent_contract`，`view.rs` 仅做挂载（`data-ui-schema/data-ui-schema-version/data-ui-intent/data-ui-action/data-ui-state/data-ui-source/data-ui-*-source`），不进行自由字符串拼接。契约字段可追溯到状态原语来源（`size/thickness/label/class` source attrs），并且渲染链路只使用白名单静态 token（无 `inner_html`/`javascript:`/脚本注入路径）。同时同步维护 `Component.toml + circular_progress.rbi`，保证 Agent 检索与签名投影一致。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_documents_agent_contract_schema_governance_rules` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_agent_contract_is_schema_typed_and_machine_readable` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_agent_contract_fields_are_type_derived_without_free_form_schema_string_splicing` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_agent_contract_render_path_is_whitelist_safe_and_script_injection_free` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_contract_hygiene_script_covers_agent_contract_schema_guards` + `components/circular-progress/test/semantics.rs::circular_progress_agent_contract_is_schema_typed_and_machine_readable_locally`。）
  - 关键交互组件必须输出稳定机器可读语义（至少 `data-*` + 状态来源标记；复杂组件建议补 `data-ui-schema`）。
  - Agent 消费字段应来自类型化 schema 生成，不允许散落字符串拼接。
  - 契约字段需可追溯到组件状态轴与动作语义（intent/action/state/source）。
  - 配置到组件的渲染链路必须走白名单能力边界，禁止任意脚本注入。
- [x] 流式在这里仅指 LLM 输出渲染（只看两种显示模式）。（该条在 `CircularProgress` 维度作为“定义治理项”通过：组件本体不是 LLM 正文渲染容器，保持非流式展示语义，不引入 `data-ui-stream-*` 协议字段；同时 checklist 明确 Streaming/Snapshot 两种显示模式定义，避免把非 LLM UI 误判为“流式渲染组件”。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_documents_streaming_definition_is_llm_output_only_with_two_modes` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_streaming_definition_contract_is_snapshot_only_and_protocol_free` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_streaming_check_script_covers_two_mode_definition_contract` + `components/circular-progress/test/semantics.rs::circular_progress_streaming_definition_contract_is_snapshot_only_and_protocol_free_locally`。）
  - `Streaming`：LLM 还在生成，界面边生成边显示。
  - `Snapshot`：LLM 全部生成完成后，一次性显示。
- [x] `Snapshot` 是所有组件的基础能力（默认必须支持）。（`CircularProgress` 作为 snapshot-only 展示组件，`view.rs` 可消费完整配置输入（`aria_label/size_px/thickness_px/class_name/lang/dir`）并通过 `logic::resolve_component_contract` 一次归一后稳定挂载 `role/aria-*` 与 `data-state/data-*-source/data-ui-*` 语义标记；`apps/docs-app/src/pages/components/pages/display.rs::circular_progress` 提供 `Hello World / Size + Thickness Matrix / Custom Label + Class` 完整配置示例，确保即使不渲染正文也能接收上层完整配置稳定渲染。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_documents_snapshot_as_default_baseline_capability` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_snapshot_baseline_consumes_complete_result_and_renders_stably` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_streaming_check_script_covers_snapshot_baseline_contract` + `components/circular-progress/test/semantics.rs::circular_progress_snapshot_baseline_consumes_complete_result_and_renders_stably_locally`。）
  - 所有组件都应能消费“完整生成结果”并稳定渲染。
  - 即使组件不直接展示正文，也应能在接收上层完整配置后正常渲染。
- [x] `Streaming` 是否强制，按组件职责判断（不能一刀切）。（`CircularProgress` 不是 LLM 正文阅读面，归类为 `Streaming Optional`，当前实现为 snapshot-only 并显式采用 `fallback=snapshot`（不引入 `data-ui-stream-*` 增量协议字段）；输出状态（草稿/已验证/可提交）由上层 LLM 容器决策并透传，组件层仅保证 `role`/`aria-*`/`data-*` 语义连续可读（`progressbar` + `data-state/data-*-source/data-ui-*`）；数据校验、断线恢复、重试策略保持在上层，不下沉到组件。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_documents_streaming_required_optional_classification_rules` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_streaming_optional_scope_keeps_role_aria_and_data_markers_continuous` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_streaming_validation_retry_resilience_boundaries_stay_outside_component_layer` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_streaming_check_script_covers_required_optional_classification_contract` + `components/circular-progress/test/semantics.rs::circular_progress_streaming_required_optional_contract_is_snapshot_fallback_with_semantic_continuity_locally`。）
  - `Streaming Required`：组件本体就是正文阅读面，用户需要边生成边看。
  - `Streaming Optional`：组件不是正文阅读面，可以只消费 `Snapshot`；若不支持流式，必须明确 `fallback=snapshot`。
  - 无论是否支持 `Streaming`，都要显式标识当前输出状态（草稿/已验证/可提交），并保持 `role`/`aria-*`/`data-*` 连续可读。
  - 数据校验、断线恢复、重试策略由上层负责，组件层只负责稳定渲染。

### 7. 测试、门禁与交付
- [x] 代码卫生（Rust Hygiene）：非测试代码中完全禁止 `unwrap/expect`，禁止无处理的 `let _ = ...`；字符串复制热点收敛为 `Cow<'static, str>`（执行 `./scripts/check-rust-hygiene.sh` 验证）。（`components/circular-progress/src` 非测试源码已校验：`mod.rs/logic.rs/view.rs/styles.rs` 不含 `unwrap()/expect()/unwrap_err()`，不含 `let _ = ...`；字符串热点已在 `logic.rs::compose_class_name` 收敛为 `Vec<Cow<'static, str>>`（静态类名 `Cow::Borrowed`，外部类名 `Cow::Owned`）。仓库级 hygiene 门禁脚本 `scripts/check-rust-hygiene.sh` 持续提供 `unwrap/expect`、`let _ =`、字符串 clone churn 检测；组件级门禁通过 `scripts/check-ui-components-engineering.sh` 新增 `circular_progress_rust_hygiene_*` 三条测试命令锁定。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_rust_hygiene_contract_forbids_unwrap_expect_and_let_underscore_in_non_test_sources` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_rust_hygiene_string_clone_hotspots_converge_to_cow_or_are_absent` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_rust_hygiene_script_enforces_repo_level_hygiene_guards` + `components/circular-progress/test/semantics.rs::circular_progress_rust_hygiene_contract_forbids_unwrap_expect_let_underscore_and_converges_hotspots_to_cow_locally`。）
- [x] Tree Shaking & 特性剪裁：组件必须注册到 `ui-components` 特性树（如 `component-accordion`）；`css.rs` 和 `lib.rs` 聚合必须受 feature 门控，禁止无条件全局依赖。（`component-circular_progress` 已注册到 `crates/ui-components/Cargo.toml`，并在 `crates/ui-components/src/lib.rs` 与 `crates/ui-components/src/css.rs` 通过 `#[cfg(feature = "component-circular_progress")]` 做条件聚合；门禁脚本 `scripts/check-ui-components-tree-shaking.sh` 新增 `CIRCULAR_PROGRESS_MIN_FEATURES="component-circular_progress,inject-css"`、最小特性树 `cargo tree -e features -i ui-components -p ui-components --no-default-features --features component-circular_progress,inject-css`、`all-components` 污染拦截与 wasm compile-only 校验。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_tree_shaking_contract_is_feature_gated_and_prunable_for_package_and_source_modes` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_tree_shaking_script_enforces_component_minimal_feature_tree_and_budget` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_tree_shaking_feature_pruning_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_tree_shaking_feature_pruning_contract_is_gated_in_lib_css_and_script_locally`。）
- [x] 语义测试与性能回归：断言必须覆盖 `aria-*`、`data-*` 与焦点流转，不能只看快照；高频/重型组件必须补齐 `render_count` 断言/测量（如初始化空闲预算为 1）。（语义断言由 `circular_progress_semantic_contract_test_suite_prioritizes_contract_markers_over_visual_snapshots` 与 `circular_progress_exposes_stable_observable_state_markers_for_selectors` 覆盖 `role/aria-* + data-*`；焦点流转在 `CircularProgress` 上为 N/A-by-design（非交互 `progressbar`），通过 `circular_progress_focus_stack_contract_is_not_applicable_while_global_focus_manager_stays_in_headless` 与 `crates/ui-headless/src/focus_trap.rs` 回归全局焦点栈职责不回流组件层；性能回归由 `circular_progress_performance_governance_budget_is_defined_and_blocking` + `circular_progress_semantics_and_performance_regression_cover_aria_data_focus_and_render_count_measurement` 联合门禁，`scripts/check-ui-components-performance.sh` 已新增对应命令。`render_count` 自动化回归仍在仓库统一 follow-up（`docs/plan/TODO.md::perf_render_count_follow_up_is_tracked_in_plan`），当前按清单允许路径采用 mount/update/heap 预算与 `data-perf-*` 等价证据阻断。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_semantics_and_performance_regression_cover_aria_data_focus_and_render_count_measurement` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_semantics_and_performance_script_covers_contract` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_semantics_and_performance_regression_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_semantics_and_performance_regression_cover_aria_data_focus_and_render_count_measurement_locally`。）
- [x] 版本弃用迁移（Codemod/Registry）：若提交包含跨大版本 API 破坏升级，必须在 Schema Registry 注册弃用窗口并提供纯函数迁移层（`migrate_v1_to_v2`）。（N/A：本次 `CircularProgress` 未发生跨大版本 API 破坏升级；组件契约保持 `schema_version = "1"`（`components/circular-progress/src/Component.toml`）、`CircularProgressAgentSchemaVersion::V1`（`components/circular-progress/src/circular_progress.rbi`/`components/circular-progress/src/logic.rs`），且未引入 `migrate_v1_to_v2`/`deprecation_window`/`SchemaRegistry`/`contract.v2`。门禁：`scripts/check-ui-components-engineering.sh` 已新增 `cargo test -p ui-components --test circular_progress_semantics --no-default-features --features component-circular_progress,inject-css circular_progress_version_deprecation_migration_is_na_without_major_breaking_upgrade`。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_version_deprecation_migration_is_na_without_major_breaking_upgrade` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_version_deprecation_migration_script_covers_engineering_gate` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_version_deprecation_migration_item_complete` + `components/circular-progress/test/semantics.rs::circular_progress_version_deprecation_migration_is_na_without_major_breaking_upgrade_locally`。当前环境定向执行仍受 `Invalid cross-device link (os error 18)` 阻塞。）
- [x] 文档即产品（Copy-Paste Ready）：`apps/docs-app` 必须新增 Playground（Hello World、状态矩阵、受控/非受控对照），支持流式/快照展现，并提供 Source-first 一键复制且补全 imports。（`apps/docs-app/src/pages/components/pages/display.rs::circular_progress` 已补齐 `Hello World / Size + Thickness Matrix / Custom Label + Class / Controlled vs Uncontrolled (N/A) / Streaming Optional / Snapshot / Source-first Starter (Copy-Paste Ready)`；所有示例通过 `code_imports="use leptos::prelude::*;\nuse ui_components::CircularProgress;"` 接入一键复制补全 imports，复制链路由 `apps/docs-app/src/playground.rs::compose_copy_ready_code` 与 `components/code-block/src/view.rs` 复制按钮共同保证；`data-slot="circular-progress-source-first"` 区块已声明 `component-circular_progress + inject-css` 依赖与真实源码路径（`components/circular-progress/src/{mod,logic,view,styles}.rs`）。回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_docs_are_copy_paste_ready_with_hello_world_state_matrix_and_streaming_snapshot` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_docs_are_source_first_copy_paste_ready_with_imports_copy_button_and_sync` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_documents_docs_product_copy_paste_ready_rules` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_dx_check_script_covers_docs_product_copy_paste_ready_contract` + `components/circular-progress/test/semantics.rs::circular_progress_docs_product_copy_paste_ready_contract_is_documented_and_scripted_locally`；门禁脚本：`scripts/check-ui-components-dx.sh`。当前环境定向执行仍受 `Invalid cross-device link (os error 18)` 阻塞。）
- [x] 语义测试优先：验证 `data-*` / `aria-*` / role / 状态来源契约，不只视觉快照。（`CircularProgress` 为非交互 `progressbar`，键盘动作路径 N/A-by-design，但语义契约测试优先策略已落地：`role/aria-* + data-* + source` 标记通过 `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_semantic_contract_test_suite_prioritizes_contract_markers_over_visual_snapshots` 与 `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_semantic_test_priority_prefers_data_aria_role_and_source_contracts_over_snapshot_only_checks` 锁定；组件本地镜像为 `components/circular-progress/test/semantics.rs::circular_progress_semantic_test_priority_prefers_data_aria_role_and_source_contracts_over_snapshot_only_checks_locally`。性能门禁脚本 `scripts/check-ui-components-performance.sh` 已新增同名命令并由 `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_performance_script_covers_semantic_test_priority_contract` 断言覆盖。当前环境定向执行仍受 `Invalid cross-device link (os error 18)` 阻塞。）
  - 每个交互组件至少有对应 `*_semantics.rs` 测试覆盖关键状态轴与动作语义。
  - 断言应聚焦语义契约（状态来源/可访问性/键盘路径），快照仅作补充。
  - 新增/变更语义字段必须同步补测试，否则不得打勾。
- [x] E2E 选择器稳定：使用语义标记，WASM 场景有稳定等待策略。（新增 `e2e/tests/docs_app_circular_progress_contract.spec.mjs`，仅使用语义选择器（`[data-component=\"circular-progress\"]` + `[data-slot=\"circular-progress\"]` + `data-ui-* / data-state / data-motion / role`）并以 `body:not(:has(#boot))` + 语义属性断言作为 wasm 稳定等待，不使用固定 sleep。动画路径通过 `data-motion=\"spin\"` 与 reload 后 `data-state=\"indeterminate\"` 断言覆盖 ready/settled 语义断点。门禁脚本新增 `scripts/check-ui-components-e2e-circular-progress.sh`，并由回归 `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_documents_e2e_selector_and_stable_wait_rules` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_e2e_selector_contract_uses_semantic_markers_and_settled_waits` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_e2e_contract_covers_ready_and_settled_semantic_breakpoints` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_e2e_check_script_covers_selector_and_settled_wait_contract` + `components/circular-progress/test/semantics.rs::circular_progress_e2e_selector_stability_prefers_semantic_markers_and_settled_waits_locally` 锁定。当前环境定向执行仍受 `Invalid cross-device link (os error 18)` 阻塞。）
  - E2E 选择器优先 `data-*` 语义标记，禁止依赖脆弱 DOM 层级或文本定位。
  - WASM 场景必须使用稳定等待策略（语义状态就绪而非固定 sleep）。
  - 若组件涉及异步/动画，E2E 需显式覆盖 ready/settled 条件。
- [x] 关键流程纳入可重复回归集合（Playwright/Cypress）。（`CircularProgress` 已定义并纳入可重复关键流程回归：`e2e/tests/docs_app_circular_progress_contract.spec.mjs::docs-app circular-progress key flow regression uses semantic breakpoints for diagnosis` 固定执行 `open route -> semantic ready -> source markers -> reload/remount settled` 链路，断点全部使用 `data-ui-* / data-state / role` 语义契约定位，失败可直接落到具体语义断点而非笼统“页面不一致”。高风险路径优先覆盖 wasm 就绪与动画稳定性（`body:not(:has(#boot))` + `data-motion=\"spin\"`）。门禁脚本 `scripts/check-ui-components-e2e-circular-progress.sh` 已新增关键流程契约命令，并由回归 `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_documents_key_flow_regression_collection_rules` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_e2e_key_flow_regression_is_repeatable_and_breakpoint_diagnosable` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_e2e_check_script_covers_key_flow_regression_contract` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_key_flow_regression_collection_complete` + `components/circular-progress/test/semantics.rs::circular_progress_e2e_key_flow_regression_collection_is_repeatable_and_breakpoint_diagnosable_locally` 锁定。当前环境定向执行仍受 `Invalid cross-device link (os error 18)` 阻塞。）
  - 至少定义一条可重复关键流程（打开/交互/关闭或提交）纳入 E2E 回归。
  - 回归失败需可定位到具体语义契约断点，而不是笼统“页面不一致”。
  - 高风险路径（overlay、focus、keyboard、async）优先进入回归集合。
- [x] docs-app 文档、示例、参数矩阵、状态矩阵同步更新。（`apps/docs-app/src/pages/components/pages/display.rs::circular_progress` 已同步补齐文档矩阵：示例面覆盖 `Hello World / Size + Thickness Matrix / Custom Label + Class / Controlled vs Uncontrolled (N/A)`，并新增 `data-slot="circular-progress-docs-sync-matrix"` 的 `State Matrix + Parameter Matrix` 说明，显式对齐 `components/circular-progress/src/view.rs` API（`aria_label/size_px/thickness_px/class_name/lang/dir`）与 `components/circular-progress/src/logic.rs`/`crates/ui-state-primitives/src/circular_progress.rs` 默认归一规则（`resolve_component_contract`、`DEFAULT_ARIA_LABEL`、`sanitize_dimension`、source=`default|custom`）。门禁由 `scripts/check-ui-components-dx.sh` 新增 docs-sync 契约命令，并由回归 `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_documents_docs_sync_and_state_matrix_rules` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_docs_examples_and_state_matrix_sync_with_logic_api_names_and_defaults` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_dx_check_script_covers_docs_sync_and_state_matrix_contract` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_docs_sync_and_state_matrix_item_complete` + `components/circular-progress/test/semantics.rs::circular_progress_docs_sync_and_state_matrix_contract_is_documented_and_scripted_locally` 锁定。当前环境定向执行仍受 `Invalid cross-device link (os error 18)` 阻塞。）
  - 组件行为或参数变更必须同步更新 `apps/docs-app` 示例与说明。
  - 文档示例需覆盖至少一组状态矩阵（受控/非受控、disabled、size/variant 等）。
  - 文档中的 API 名称与默认值必须和 `logic.rs` 当前实现一致。
- [x] 组件文档必须对新手友好（Documentation as Product）：组件 README 或等价文档入口必须存在。（`CircularProgress` 已补齐新手友好文档入口：新增 `components/circular-progress/src/README.md`，结构按“先用起来再进阶”组织（`Hello World -> 常见用法 -> 进阶用法 -> docs-app 入口`），并提供最小可运行示例 `<CircularProgress />` 与常见参数示例（`size_px/thickness_px/aria_label/class_name/lang/dir`）；等价入口 `apps/docs-app/src/pages/components/pages/display.rs::circular_progress` 继续作为可索引验收面（默认调用路径 `title="Hello World"` 在前，进阶块 `Controlled vs Uncontrolled (N/A)` 在后）。门禁由 `scripts/check-ui-components-dx.sh` 新增 documentation-as-product 契约命令，并由回归 `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_documents_documentation_as_product_rules` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_documentation_entry_exists_with_beginner_first_progression` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_dx_check_script_covers_documentation_as_product_contract` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_documentation_as_product_item_complete` + `components/circular-progress/test/semantics.rs::circular_progress_documentation_as_product_contract_is_documented_and_scripted_locally` 锁定。当前环境定向执行仍受 `Invalid cross-device link (os error 18)` 阻塞。）
  - 每个基础组件必须提供“零门槛”最小示例（Hello World）与常见用法，避免要求用户先理解底层分层架构。
  - 文档需明确“先用起来，再进阶”：默认 API 路径在前，高级控制参数在后。
  - “只有源码没有文档”或“只写给架构师/机器看的文档”视为不通过。
- [x] `apps/docs-app` 必须提供 Interactive Playground：用户可在线修改 props/状态并实时预览。（`apps/docs-app/src/pages/components/pages/display.rs::circular_progress` 已新增 `Interactive Playground (Props / State / Preview)`：通过 `data-slot="circular-progress-workbench-controls"` 在线切换 `size/thickness/label/class/lang/dir`，并在 `data-slot="circular-progress-workbench-preview"` 实时观察 `data-size-source/data-thickness-source/data-label-source/data-class-source/dir` 语义反馈；该组件无内部受控状态轴，状态切换按 props 装配路径验收。AI Spec 相关联动在本组件为 N/A（`CircularProgress` 无 `spec.rs`/Spec 输入协议）。可重复关键路径由 `e2e/tests/docs_app_circular_progress_contract.spec.mjs::docs-app circular-progress interactive playground updates props and semantic markers` 覆盖（调整 props -> 观察语义 -> remount 重放）。门禁由 `scripts/check-ui-components-dx.sh` 新增 interactive-playground 契约命令，并由回归 `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_documents_interactive_playground_rules` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_docs_app_provides_interactive_playground_for_props_state_and_preview` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_interactive_playground_reuses_repeatable_semantic_e2e_flow` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_dx_check_script_covers_interactive_playground_contract` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_interactive_playground_item_complete` + `components/circular-progress/test/semantics.rs::circular_progress_interactive_playground_contract_is_documented_and_scripted_locally` 锁定。当前环境定向执行仍受 `Invalid cross-device link (os error 18)` 阻塞。）
  - Playground 至少支持基础 props 调整、状态切换、交互反馈观察。
  - 对 AI Spec 相关组件，至少提供一组 Spec 输入与预览输出的联动示例。
  - Playground 作为验收面，需可重复复现关键交互路径。
- [x] Source-first 文档必须 Copy-Paste Ready：提供一键复制组件源码或最小可用片段能力。（`apps/docs-app/src/pages/components/pages/display.rs::circular_progress` 已提供双路径一键复制：`Playground title="Source-first Starter (Copy-Paste Ready)"` 通过 `code_imports="use leptos::prelude::*;\nuse ui_components::CircularProgress;"` 走 `apps/docs-app/src/playground.rs::compose_copy_ready_code` 输出可直接运行片段；`data-slot="circular-progress-source-first"` 额外提供 `Snippet(copyable=true)`（`label="Copy circular-progress starter"`，class=`docs-circular-progress-source-copy`）作为显式 copy 按钮入口。Source-first 区块已声明依赖前提（`component-circular_progress + inject-css`，以及 `UiRoot`/CSS 注入前提）并列出真实源码落点（`components/circular-progress/src/{mod,logic,view,styles}.rs`）。同步防漂移通过 `data-slot="circular-progress-source-sync-note"` 约束：示例文本来自 `source_first_code` 并与 `view.rs` API 同步更新。门禁与回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_documents_source_first_copy_paste_ready_rules` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_docs_source_first_copy_paste_ready_with_real_paths_and_dependencies` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_dx_check_script_covers_source_first_copy_paste_ready_contract` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_source_first_copy_paste_ready_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_source_first_copy_paste_ready_contract_is_documented_and_scripted_locally`；脚本：`scripts/check-ui-components-dx.sh`。当前环境定向执行仍受 `Invalid cross-device link (os error 18)` 阻塞。）
  - docs-app 页面应提供复制按钮，输出代码默认可直接运行（含必要 imports/依赖提示）。
  - 若为 source-first 组件，文档需指向真实源码落点并说明依赖前提，避免“复制即报错”。
  - 文档代码与当前实现必须同步，防止示例漂移。
- [x] HeroUI 对标文档与组件文档同步：参数模型变更需同步 `docs/spec/heroui-parameter-design-strategy.md`（必要时补充 `docs/research/spectrum-heroui-style-interface-study.md`），并保证组件文档可访问。（`docs/spec/heroui-parameter-design-strategy.md` 已新增 `### CircularProgress 同步记录（2026-02-20）`，明确参数主轴 `aria_label/size_px/thickness_px/class_name/lang/dir`、docs/README 同步边界与“先文档后实现”的约束；本轮为参数语义与文档入口对齐，不引入新的 Spectrum/HeroUI 风格结论，`docs/research/spectrum-heroui-style-interface-study.md` 无需追加。组件文档入口与索引可达性已固定：`apps/docs-app/src/pages/components/pages.rs` 保持 `component_doc!("CircularProgress", "circular-progress", "Display", display::circular_progress)`，`apps/docs-app/src/pages/components/pages/display.rs::circular_progress` 与 `components/circular-progress/src/README.md` 持续可访问。门禁与回归：`components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_documents_heroui_benchmark_docs_sync_rules` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_heroui_strategy_and_component_docs_are_synchronized_and_indexable` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_dx_check_script_covers_heroui_benchmark_docs_sync_contract` + `components/circular-progress/test/circular_progress_semantics.rs::circular_progress_check2_marks_heroui_benchmark_docs_sync_contract_complete` + `components/circular-progress/test/semantics.rs::circular_progress_heroui_benchmark_docs_sync_contract_is_documented_and_scripted_locally`；脚本：`scripts/check-ui-components-dx.sh`。当前环境定向执行仍受 `Invalid cross-device link (os error 18)` 阻塞。）
  - 若参数语义发生变化，需同步更新对标策略文档，不允许实现先漂移文档后补。
  - 组件文档入口必须存在（docs-app 页面或等价文档），且可被索引定位。
  - “仅代码更新无文档更新”在接口变更场景下直接判不通过。

### 8. 合并前门禁死命令（最终执行）
在发起 PR 或完成任务前，必须保证本地/CI 以下命令全部通过：

- `cargo fmt --all -- --check`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `./scripts/check-rust-hygiene.sh`
- `cargo check -p ui-components --target wasm32-unknown-unknown`
- `cargo check -p ui-headless --no-default-features --features ssr`
- `cargo check -p ui-components --target wasm32-unknown-unknown --no-default-features --features component-<your_component>,inject-css`

依据文档（`rust-ui/docs/spec` 及 `rust-ui/docs`）：

- `rust-ui/docs/spec/ai_context_projection_protocol.md`
- `rust-ui/docs/spec/architectural_fitness_functions.md`
- `rust-ui/docs/spec/async_state_as_data_command.md`
- `rust-ui/docs/spec/collection_registration_protocol.md`
- `rust-ui/docs/spec/compile_time_evolution_migration.md`
- `rust-ui/docs/spec/component_boundaries.md`
- `rust-ui/docs/spec/component_domains.md`
- `rust-ui/docs/spec/controlled_evolution_sandbox.md`
- `rust-ui/docs/spec/core_shell_protocol_infra_baseline.md`
- `rust-ui/docs/spec/environment_subscription_streams.md`
- `rust-ui/docs/spec/event_light_cone_signal_bus.md`
- `rust-ui/docs/spec/focus_global_stack_gc.md`
- `rust-ui/docs/spec/foreign_zone_escape_hatches.md`
- `rust-ui/docs/spec/headless_purification.md`
- `rust-ui/docs/spec/heroui-parameter-design-strategy.md`
- `rust-ui/docs/spec/hyper-structure-ui-development-playbook.md`
- `rust-ui/docs/spec/i18n.md`
- `rust-ui/docs/spec/intent_stack_semantic_layering.md`
- `rust-ui/docs/spec/kernel_shell_architecture.md`
- `rust-ui/docs/spec/macro_micro_dual_state_machine.md`
- `rust-ui/docs/spec/motion.md`
- `rust-ui/docs/spec/mvp.md`
- `rust-ui/docs/spec/platform_abdication_ecosystem.md`
- `rust-ui/docs/spec/README.md`
- `rust-ui/docs/spec/release_versioning.md`
- `rust-ui/docs/spec/side_effect_command_pattern.md`
- `rust-ui/docs/spec/slot_projection_strategy.md`
- `rust-ui/docs/spec/ssr_hydration_discontinuity.md`
- `rust-ui/docs/spec/state_primitives_core_satellite_split.md`
- `rust-ui/docs/spec/style_island_defense.md`
- `rust-ui/docs/spec/styling.md`
- `rust-ui/docs/spec/tree_shaking.md`
- `rust-ui/docs/spec/ui_layout_split.md`
- `rust-ui/docs/spec/ui_physics_two_pass_rendering.md`
- `rust-ui/docs/spec/unified_causality_bus.md`
- `rust-ui/docs/spec/wasm_generic_bloat.md`
- `rust-ui/docs/起点_也即是目的.md`
- `rust-ui/docs/DOCS_GOVERNANCE.md`
- `rust-ui/docs/DOCS_INDEX.md`
- `rust-ui/docs/philosophy.md`
- `rust-ui/docs/README.md`
- `rust-ui/docs/RULES_ZH.md`
