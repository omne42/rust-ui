# 单组件 Check List（Hyper-Structure 完整版）

> 用途：每次新增、重构或修改一个组件时，必须按本清单逐项检查并在 PR 中附带结果。  
> 执行顺序：先过第 1-2 节（架构与状态），再进入第 3-5 节（具体实现）。  
> 术语统一：`status-primitives` 指状态原语层（当前 crate 名为 `ui-state-primitives`）。

### 0. 适用范围与顺序纪律
本清单仅评估“一个组件”的改动结果，不替代仓库级治理。
先过第 1-2 节（架构与状态）再进入第 3-5 节（具体实现）。
不适用的条目必须明确标注 `N/A` 并说明理由，禁止机械打勾。
组件目标、非目标、风险边界已写清楚；发现跨组件/跨层系统性问题时升级为仓库级任务。

### 1. 架构边界与分层约束（Kernel/Shell 总线）
- [x] `status-primitives` 定义：纯状态原语层（受控/非受控、toggle、selection、list、overlay open state、expansion 等）。不依赖 Leptos/DOM/web-sys；只包含 Rust 数据结构和方法，不含视图与事件绑定。（`ToggleGroup` 的 single/multiple 选中规则、选中集归一化、item 归一化、group state 派生，以及 `ToggleButton`/`ToggleButtonGroup` 的交互状态派生，已下沉到 `crates/ui-state-primitives/src/toggle_button.rs`；`ActionGroup` 的 item 索引归一化已下沉到 `crates/ui-state-primitives/src/action_group.rs`；`components/button/src/toggle/logic.rs`、`components/button/src/toggle_button/logic.rs` 与 `components/button/src/action/logic.rs` 仅保留装配与调用；下沉单测位于 `crates/ui-state-primitives/src/test/toggle_button.rs` 与 `crates/ui-state-primitives/src/test/action_group.rs`，并通过 `cargo test -p ui-state-primitives toggle_button && cargo test -p ui-state-primitives action_group`）
  - 所有状态原语必须从 `status-primitives`（`ui-state-primitives`）获取，组件层只能消费，不得自造。
  - 下沉判定依据是“稳定状态不变量”；凡属于状态机、归一化、状态派生能力，默认先进入 `ui-state-primitives`。
  - 组件中可保留的仅是装配逻辑：props 归一、样式来源标记、slot 组织、对 `ui-state-primitives` 输出的映射。
  - 组件内若出现状态原语实现（受控/非受控状态机、single/multiple 展开规则、索引归一化、跨事件状态派生），该项直接判不通过。
  - 处理方式固定：先下沉到 `ui-state-primitives/src/<capability>.rs`（如 `expansion.rs`），在 `ui-state-primitives/src/lib.rs` 导出，再回到组件改调用。
  - 下沉后的原语必须有 `ui-state-primitives` 单元测试；组件侧只保留调用与语义挂载测试。
  - 桥接规范：`ui-state-primitives` 结构体必须是 POJO（Plain Old Rust Object），不持有 Leptos `Signal` 或框架绑定状态容器。
  - 消费规范：`ui-headless` 或组件 `logic.rs` 负责解包 `Signal` 当前值传入 primitive 方法，并将结果显式写回 `Signal`。
  - 设计理由：保持 primitives 纯粹可测、可迁移，不与特定响应式库绑定（便于未来替换响应式实现与做纯 Rust 测试）。
- [x] `ui-headless` 定义：交互与 A11y 原语层（press/focus/hover/roving/listbox/menu/tooltip 等），把输入设备事件与状态语义标准化为可复用契约；输出必须是类型化 `attrs + handlers + state`。不做样式、不写组件 CSS、不做组件级动效编排。（`Button`/`Toggle`/`ToggleButton`/`ActionButton` 持续通过 `use_button + use_focus_ring + use_hover` 消费交互语义；`ButtonGroup`/`ToggleGroup`/`ToggleButtonGroup` 改为挂载 `ui_headless::labeled_group_attrs`；`ActionButtonGroup`/`ActionGroup` 改为挂载 `ui_headless::labeled_toolbar_attrs`，并统一接入 `lang/dir`；契约回归覆盖位于 `crates/ui-headless/src/test/a11y.rs`、`components/button/test/button_group_semantics.rs`、`components/button/test/toggle_group_semantics.rs`、`components/button/test/toggle_button_group_semantics.rs`、`components/button/test/action_button_group_semantics.rs`、`components/button/test/action_group_semantics.rs`）
  **`ui-headless` 落位硬规则（必须执行）**：
  - 输入边界：消费 `status-primitives` 状态 + 用户输入事件（keyboard/pointer/focus）+ 环境能力（web/ssr）。
  - 输出边界：只输出语义契约（attrs/handlers/state）；组件层只负责挂载与组合，不得把语义判断塞回 `view.rs`。
  - 下沉判定依据是“交互/A11y 语义契约”；凡属于键盘/焦点/指针归一、ARIA 映射、交互状态语义能力，默认先进入 `ui-headless`。
  - 必须下沉：键盘模型、焦点模型、跨设备输入归一、ARIA 状态映射、overlay/presence 等交互语义。
  - A11y 契约与共享工具落点固定在 `crates/ui-headless/src/a11y.rs`；组件只在 `view.rs` 挂载，不在组件层重写。
  - 语义契约必须提供 `lang` / `dir`（LTR/RTL）接入能力；headless 不硬编码用户可见文本，文案由 i18n/l10n 层提供。
  - 语义契约正确性必须有回归：`components/*/test/**` 断言语义标记，`e2e/tests/*` 覆盖关键交互流程。
  - 禁止放在 `ui-headless`：视觉 class 选择、CSS 规则、组件 slot 布局、组件专属动效编排、业务文案。
  - 允许留在组件层：纯视觉一次性交互且不形成可复用语义契约（例如单组件局部微交互）。
- [x] `ui-motion` 定义：动效能力与契约执行层（spring、keyframes、WAAPI/RAF backend），只负责时间函数、插值与运行时驱动，不承载组件业务语义与状态决策。（`crates/ui-motion/src/spring.rs`/`keyframes.rs`/`options.rs`/`web.rs` 承载通用动画数学与后端，且 `crates/ui-motion/src/lib.rs` 提供 non-wasm no-op/stub；`components/button/src/motion.rs`、`components/button/src/toggle_button/motion.rs`、`components/button/src/action/motion.rs` 等仅做语义态到动效 contract 的映射与 attach；`ButtonMotion::default()` 对齐 `ui_theme::default_button_motion_tokens()`，避免组件层散落基础 spring 常量；回归覆盖见 `components/button/test/motion.rs`、`components/button/test/toggle_button/motion.rs`、`components/button/test/action/motion.rs` 与 `crates/ui-motion/src/test/spring.rs`）
  - 放在 `crates/ui-motion`：通用动画数学与执行后端（spring solver、keyframe sampling、easing registry、driver adapters），以及 `wasm/non-wasm` 适配与 `reduced-motion` 执行策略。
  - 放在 `crates/ui/src/<component>/motion.rs`：把组件语义状态（open/closed、enter/exit、active/inactive）映射为 `ui-motion` contract，绑定目标节点并调用 attach。
  - 禁止放在 `crates/ui-motion`：组件 slot 结构、组件专属状态机、ARIA/keyboard 语义、业务文案与业务分支。
  - 禁止放在组件 `motion.rs`：自实现 spring/keyframe/driver 执行器；跨组件共享动效算法必须回迁 `ui-motion`。
  - 动效参数优先来自 token/theme；禁止在组件样式与逻辑中散落硬编码时长/曲线/位移常量。
  - 非 wasm 路径必须提供 no-op/stub，保证 SSR/tooling 可编译且行为可预测。
- [x] `ui-theme` 定义：唯一设计 token 与主题上下文层（system/color/scale + Light/Dark/OLED），负责 token 分类、主题映射与 CSS 变量生成。（Token/主题映射/变量输出链路落在 `crates/ui-theme/src/tokens.rs`、`crates/ui-theme/src/theme.rs`、`crates/ui-theme/src/css.rs`；三轴上下文由 `ThemeContext { system,color,scale }` 定义并由 `UiRoot` 注入；`button` 样式消费 `--ui-*` 变量（如 `components/button/src/styles.rs`、`components/button/src/toggle_button/styles.rs`），不在组件侧重建主题；主题尺寸与尺度回归见 `crates/ui-theme/tests/token_scale_baseline.rs`，WCAG AA 回归见 `crates/ui-theme/tests/wcag_contrast.rs`，规范同步见 `docs/spec/styling.md`，组件语义回归见 `components/button/test/button_semantics.rs`）
  - Token 统一基线落点固定：`crates/ui-theme/src/tokens.rs` 定义，`crates/ui-theme/src/theme.rs` 映射，`crates/ui-theme/src/css.rs` 输出变量；组件只在 `crates/ui/src/<component>/styles.rs` 消费。
  - 三轴上下文（`system/color/scale`）在 `theme.rs` 定义；组件在 `logic.rs` 选择并在 `view.rs` 生效，`styles.rs` 只消费变量，不重建主题。
  - Token 分类必须可追溯：分类源在 `tokens.rs`，规范同步 `docs/spec/styling.md`；组件不得引入平行私有 token 命名体系。
  - 量化尺寸基准必须可回归：尺寸基准在 `tokens.rs` 与 `theme.rs` 定义，主题回归在 `crates/ui-theme/tests/token_scale_baseline.rs`，组件语义回归在 `components/*/test/*<component>_semantics.rs`。
  - 主题调色与语义色对比必须满足 `WCAG 2.1 AA` 基线，并覆盖 Light/Dark/OLED 主题变体。
  - 主题层只输出 `theme/tokens/base css` 与变量；不实现组件结构、交互逻辑、组件级动效编排。
  - 新增视觉语义先补 token，再由组件消费；禁止“组件临时值先落地、后补 token”的倒序流程。
- [x] `ui` 定义：最终 Leptos 组件装配层，组合 `status-primitives + ui-headless + ui-motion + ui-theme` 并暴露稳定公共 API。（`components/button/src/logic.rs` 持续承载 props 归一/状态派生并复用 `ui-state-primitives`；`components/button/src/view.rs`/`components/button/src/toggle/view.rs`/`components/button/src/action/view.rs` 仅做结构渲染与 headless 语义挂载；`components/button/src/styles.rs` 与子组件 `styles.rs` 保持 token-first 静态样式；`components/button/src/motion.rs` 与子组件 `motion.rs` 仅做 motion attach；旧版 `components/button/test/button_semantics.rs` 已迁移为 `components/button/test/semantics.rs`，并由 `components/button/test/button_semantics.rs` 路径转发，满足 src 同级 `test/semantics.rs` 约束）
  - `logic.rs` 负责 props 归一与状态派生；`view.rs` 负责结构渲染与 headless 语义挂载；`styles.rs` 负责 token-first 静态样式；`motion.rs` 负责动效 attach。
  - 组件层不得重写 `status-primitives` 状态机或 `ui-headless` 交互契约；发现即判不通过并回迁到对应层。
  - 对外 API 禁止暴露 `web-sys`/DOM 细节类型；平台差异封装在内部模块。
  - 测试文件位于src同级的test/中，内部测试文件同名（如rust-ui/components/accordion/src/logic.rs与rust-ui/components/accordion/test/logic.rs）。
  - 还需要一个semantics.rs用于测试。可能存在类似rust-ui/components/accordion/test/semantics.rs的旧版实现，需要迁移到新目录。

### 2. API 设计与状态内核（Logic/Kernel）
- [x] API 命名契约统一：公共 props/回调严格使用 `is_*`、`on_*`、`default_*` 前缀；同语义在全库同名，禁止别名漂移。（导出 API 已统一为 `is_*`/`on_*`：`ButtonGroup(is_attached)`、`Toggle(is_disabled)`、`ToggleGroup(is_disabled/is_attached)`、`ToggleButton(is_disabled/on_pressed_change)`、`ToggleButtonGroup(is_attached)`、`ThemeToggleButton(is_disabled)`、`ActionGroup(on_selected_ids_change)`；迁移映射固定为 `attached -> is_attached`、`disabled -> is_disabled`、`on_change -> on_pressed_change`、`on_selected_change -> on_selected_ids_change`；实现位于 `components/button/src/view.rs`、`components/button/src/toggle/view.rs`、`components/button/src/toggle_button/view.rs`、`components/button/src/theme_toggle/view.rs`、`components/button/src/action/view.rs`；示例与语义断言已同步更新 `apps/docs-app/src/pages/components/pages/actions.rs`、`apps/docs-app/src/pages/components/pages/actions_extra.rs`、`components/button/test/button_group_semantics.rs`、`components/button/test/toggle_semantics.rs`、`components/button/test/toggle_group_semantics.rs`、`components/button/test/toggle_button_semantics.rs`、`components/button/test/toggle_button_group_semantics.rs`、`components/button/test/button_theme_toggle_semantics.rs`、`components/button/test/theme_toggle_button_semantics.rs`、`components/button/test/action_group_semantics.rs`）
  - 布尔状态统一 `is_*`（如 `is_open`/`is_disabled`），事件统一 `on_*`，默认值统一 `default_*`。
  - 同一语义 across 组件必须同名（如都用 `on_open_change`，禁止同义别名并存）。
  - 公共 API 引入新命名时，需说明与现有命名体系的兼容策略与迁移路径。
- [x] 受控/非受控必须成对：每个可控状态轴都提供 `value + on_value_change + default_value`（如 `open/on_open_change/default_open`）；缺一项即不通过。（`Toggle` 与 `ToggleButton` 已统一为 `is_pressed + on_pressed_change + default_pressed`，并通过 `overlay_open::use_controllable_state(...)` 落实受控/非受控语义；`ToggleGroup` 与 `ActionGroup` 保持 `selected_ids + on_selected_ids_change + default_selected_ids` 三元契约；实现见 `components/button/src/toggle/view.rs`、`components/button/src/toggle_button/view.rs`、`components/button/src/action/view.rs`，语义回归覆盖见 `components/button/test/semantics.rs`、`components/button/test/toggle_semantics.rs`、`components/button/test/toggle_button_semantics.rs`、`components/button/test/toggle_group_semantics.rs`、`components/button/test/action_group_semantics.rs`，文档示例同步见 `apps/docs-app/src/pages/components/pages/actions.rs`、`apps/docs-app/src/pages/components/pages/actions_extra.rs`）
  - 受控模式：外部值是单一事实来源，内部不得偷偷写回本地状态。
  - 非受控模式：仅由默认值初始化一次，后续状态由内部原语管理。
  - 受控/非受控切换语义需稳定可测，避免“半受控”隐式行为。
- [x] 默认值单一来源：默认值与优先级只在 `logic.rs` 归一化；`view.rs` 禁止二次兜底或隐式改写。（`button` 主路径与子模块默认值兜底已回收至逻辑层：`components/button/src/logic.rs`（`compose_button_group_class_name`）、`components/button/src/toggle/logic.rs`（`normalize_toggle_group_default_selected_ids`）、`components/button/src/toggle_button/logic.rs`（`compose_class_name`/`compose_toggle_button_group_class_name`）、`components/button/src/theme_toggle/logic.rs`（`normalize_aria_label`）、`components/button/src/action/logic.rs`（`resolve_button_type`/`normalize_default_selected_ids`）、`components/button/src/clear_button/logic.rs`（`resolve_visibility_signals`）、`components/button/src/search_input/logic.rs`（`resolve_shortcut_labels`）、`components/button/src/picker_button/logic.rs`（`resolve_inner_class_name`）、`components/button/src/copy/logic.rs`（`resolve_text_contract_with_defaults`/`resolve_copy_failed_status_text`）；对应 `view.rs` 已仅消费逻辑输出并移除 `unwrap_or*`/`.or(...)` 默认分支，语义断言同步更新 `components/button/test/button_group_semantics.rs`、`components/button/test/toggle_group_semantics.rs`、`components/button/test/toggle_button_semantics.rs`、`components/button/test/toggle_button_group_semantics.rs`、`components/button/test/theme_toggle_button_semantics.rs`、`components/button/test/action_button/semantics.rs`、`components/button/test/action_group_semantics.rs`、`components/button/test/button_copy_semantics.rs`）
  - 默认值优先级必须可读且可测试（显式规则而非分散 `unwrap_or`）。
  - `view.rs` 不允许再做默认值分支；仅消费 `logic.rs` 的归一化输出。
  - 一旦发现多处默认值来源，直接判不通过并回收至 `logic.rs`。
- [x] 状态归一化集中：状态输入先类型化，再在 `logic.rs` 统一派生；禁止在 `view.rs`、事件回调、样式分支中分散拼状态机。（`ActionGroup` 的 item 渲染状态与点击后选择迁移已下沉到 `components/button/src/action/logic.rs`：新增 `resolve_item_render_state` 与 `resolve_next_selected_ids`，`components/button/src/action/view.rs` 仅消费逻辑输出并触发回调，不再在事件处理器中拼接 `sanitize + toggle` 规则，也不在 view 侧拼接 selected/disabled item class；语义与逻辑回归已补 `components/button/src/action/logic/action_group_logic/tests.rs`、`components/button/test/semantics.rs`（`button_state_normalization_is_centralized_in_logic_only`）、`components/button/test/action_group_semantics.rs`）
  - 输入边界统一进入 `logic.rs`，输出统一为可渲染语义状态与来源标记。
  - 事件处理器只触发状态变更，不重建状态机规则。
  - 样式层只消费状态标记，不承担状态判定职责。
- [x] 离散状态必须类型约束：`variant/size/mode/status` 等离散输入使用 `enum`；禁止用多个 `Option<bool>`/字符串自由组合表达互斥状态。（`ClearButton` 已将互斥焦点策略从 `prevent_focus + exclude_from_tab_order` 双布尔收敛为 `ClearButtonFocusMode` 枚举：定义与映射在 `components/button/src/clear_button/logic.rs`，输入边界改为 `components/button/src/clear_button/view.rs` 的 `focus_mode: ClearButtonFocusMode`，状态输入改为 `components/button/src/clear_button/mod.rs` 的 `ClearButtonStateInput { focus_mode }`；调用侧同步改为枚举值 `components/text-input/src/input/view.rs`、`components/text-input/src/search_field/view.rs`、`apps/docs-app/src/pages/components/pages/actions_extra.rs`；回归断言已更新 `components/button/test/clear_button/logic.rs`、`components/button/test/clear_button_semantics.rs`、`components/text-input/test/input_semantics.rs`、`components/text-input/test/search_field_semantics.rs`）
  - 互斥状态优先用 `enum` 建模，利用编译器封住无效组合。
  - 字符串输入若需兼容外部配置，必须先映射到类型化枚举再进入逻辑层。
  - 布尔爆炸（多个 bool 表达一个状态机）应在设计评审阶段直接拦截。
- [x] 状态原语来源正确：组件层只消费 `status-primitives`（当前 `ui-state-primitives`）能力，不直接绑定业务 store；应用级全局状态必须经桥接层适配后再接入组件。（`button` 家族可复用状态机与归一化已统一委托 `ui-state-primitives`：`components/button/src/logic.rs`（`button` core）、`components/button/src/toggle/logic.rs`（toggle-group selection）、`components/button/src/action/logic.rs`（action-group selection）、`components/button/src/copy/logic.rs`（copy state）、`components/button/src/flip/logic.rs`（flip core）、`components/button/src/share/logic.rs`（share items）；组件层仅保留信号桥接与挂载。并将多处组件内文本归一统一改为 `ui_state_primitives::button::normalize_optional_text`（`components/button/src/clear_button/logic.rs`、`components/button/src/infield_button/logic.rs`、`components/button/src/close_button/logic.rs`、`components/button/src/link_button/logic.rs`、`components/button/src/picker_button/logic.rs`、`components/button/src/theme_toggle/logic.rs`、`components/button/src/toggle_button/logic.rs`、`components/button/src/logic_button/logic.rs`、`components/button/src/action/logic.rs`）；语义守卫新增 `components/button/test/semantics.rs`（`button_state_primitives_are_sourced_from_ui_state_primitives`），并断言未直接绑定业务 store）
  - 组件中出现可复用状态机实现（受控/非受控、展开规则、选择归一）即判应下沉。
  - 组件与业务全局状态之间必须有适配边界，禁止组件直接依赖业务 store 类型。
  - `logic.rs` 仅做装配与映射，不重新实现状态原语。
- [x] 如果无异步相关，直接打勾。异步交互语义统一：`is_loading`、error/retry、disabled、`aria-busy` 映射一致；优先复用统一 async action 原语（如 `use_async_action`），禁止每组件自定义一套加载/错误协议。（`button` 非 N/A：存在异步语义。`Button`/`ActionButton` 统一将 `is_loading` 映射为 `disabled + aria-busy`（`components/button/src/logic.rs`、`components/button/src/view.rs`、`components/button/src/action/view.rs`）；`ButtonCopy` 复用统一 headless async 原语链路 `crate::snippet::use_snippet_logic -> ui_headless::use_snippet_copy`，失败态通过 `data-copy-error` + `aria-live` 暴露，重试路径由同一 copy 动作恢复（`on_retry = on_copy`），未在组件层自定义私有 retry 协议；守卫回归见 `components/button/test/semantics.rs`（`button_loading_forces_disabled_and_sets_aria_busy`、`button_async_contract_is_externalized_and_has_no_internal_retry_protocol`、`button_copy_async_failure_state_has_recoverable_path_and_semantic_markers`）与 `components/button/test/button_copy_semantics.rs`）
  - 无异步交互时需明确标注 N/A 理由（例如“组件无远程请求与异步状态”），不是机械打勾。
  - 有异步交互时，`is_loading`/disabled/`aria-busy`/retry 语义必须成套一致，且对键盘与读屏路径可用。
  - 异步失败态要有可恢复路径（重试或回退），并有语义测试覆盖。
- [x] API 易用性验收标准（DX Paradox）：把复杂性留在内部，把简单留给用户。（已满足：基础调用路径为 docs `Hello world` 一行示例 `<Button>"Button"</Button>`（`apps/docs-app/src/pages/components/pages/actions.rs`）；组件公开 API 不要求 `state=...` 等内部状态对象，状态机接线封装在组件内部（守卫见 `components/button/test/semantics.rs`：`button_dx_minimal_hello_world_path_is_documented_and_state_free`）；简单需求走默认 props，复杂需求通过 `Variants & sizes`/Workbench 扩展 props 按需开启（`apps/docs-app/src/pages/components/pages/actions.rs`，守卫见 `button_docs_page_covers_button_playground_contracts`、`button_dx_workbench_supports_optional_state_persistence_and_isolated_canvas`））
  - 基础用法不得要求用户先理解或手动接线 `ui-state-primitives`/`ui-headless` 状态机。
  - 基础组件 Hello World 示例代码不得超过 5 行（导入与外层模板按仓库约定不计），并可直接运行。
  - 简单需求走简单 API，复杂需求再暴露高级入口：默认 props 覆盖高频场景，高级控制通过受控/扩展参数按需开启。
  - 禁止把内部状态对象作为基础必填参数暴露（例如强制 `state=...` 才能完成点击/展开等基本交互）。
  - docs-app 必须提供最小可用示例，优先展示一眼可懂的默认调用路径。
- [x] 组合型组件主 API 必须“显示优于约定”：优先使用显式组合 `<Parent><Item ... /></Parent>`。（已满足：`ActionButtonGroup`/`ToggleButtonGroup` 采用显式父子组合 `children: Children`（`components/button/src/action/view.rs`、`components/button/src/toggle_button/view.rs`）；`ActionGroup`/`ToggleGroup` 的配置式入口仅接受类型化 `ActionGroupItem`/`ToggleGroupItem`（`components/button/src/action/view.rs`、`components/button/src/toggle/view.rs`、`components/button/src/action/mod.rs`、`components/button/src/toggle/mod.rs`），并在内部映射为显式 item 语义树渲染；docs 仅展示类型化 item 用法（`apps/docs-app/src/pages/components/pages/actions_extra.rs`）；语义守卫已覆盖并行数组禁用与组合契约（`components/button/test/action_group_semantics.rs`：`action_composite_apis_use_explicit_children_or_typed_item_specs`，`components/button/test/toggle_group_semantics.rs`））
  - 每个 item 的标题、语义与内容必须在同一 `Item` 结构维度绑定，避免索引配对式隐式约定。
  - `labels + children`、`titles + panels` 等并行数组/并行槽位写法不得作为默认或推荐 API。
  - 不引入这类语法糖：若为配置式输入，仅允许类型化 `ItemSpec`，并在内部映射为显式 `Item` 语义树。

### 3. 高级交互与物理机制（Shell/Physics）
- [x] 宏观/微观双状态机（Macro/Micro Duality）：拖拽等高频交互在 `Dragging` 期间由 `view/motion` 本地循环执行；禁止每帧穿越回 `logic.rs`，必须在结束时通过 `Action::DragEnd` 回流收敛。（N/A：`button` 当前仅有 press/hover/focus 与加载态交互，不包含拖拽手势会话；`components/button/src` 与 `components/button/test` 中无 `pointermove`/`Dragging`/`DragEnd` 路径，`motion.rs` 仅做 hover/press spring attach，不存在逐帧回流 `logic.rs` 的拖拽状态机。）
- [x] 几何两段式渲染（Two-Pass Rendering）：`Tooltip/Popover/Menu` 等依赖 DOM 测量的组件必须走 `Intent -> Measure(view) -> Rectification(logic)`，并具备幂等收敛保护防死循环。（N/A：`button` 组件族当前不承载依赖 DOM 几何测量的 overlay 定位流程；`components/button/src` 中无 `getBoundingClientRect`/`offset*`/`ResizeObserver` 等测量路径，也无测量后回写 `logic.rs` 的 rectification 收敛回路，因此不存在该条要求的两段式渲染风险面。）
- [x] 集合注册协议（Registration Protocol）：`Accordion/Tabs/Menu` 动态子项必须通过 `RegistrationContext` 上报 `Register/Unregister`，逻辑层维护 `items_order`，禁止依赖 `HashSet` 迭代顺序做导航。（N/A：`button` 组件族当前不提供 `Accordion/Tabs/Menu` 这类动态注册式子项容器；`ActionGroup`/`ToggleGroup` 均以显式 `Vec<Item>` 输入并按声明顺序渲染（`components/button/src/action/view.rs`、`components/button/src/toggle/view.rs`），逻辑层仅用 `BTreeSet` 做成员归一化，不依赖 `HashSet` 迭代顺序；并新增守卫 `components/button/test/semantics.rs`：`button_collection_registration_protocol_is_not_applicable_without_dynamic_child_registry`。）
- [x] 插槽投影策略（Slot Projection）：容器组件明确 `Lazy/KeepAlive/Eager`；`KeepAlive` 隐藏时必须通过生命周期通知（如 `NotifyHidden`）暂停轮询/动画等高耗能副作用。（N/A：`button` 组件族非内容容器，不承载 `Lazy/KeepAlive/Eager` 投影策略；当前仅做直接插槽渲染与交互态映射，不存在隐藏态生命周期通知（`NotifyHidden`）与高耗能副作用暂停链路。守卫见 `components/button/test/semantics.rs`：`button_slot_projection_strategy_is_not_applicable_without_keep_alive_container`。）
- [x] 环境订阅流（Env Streams）：`Resize/Theme/Intersection` 等环境变化在 `view.rs` 采样、防抖后转化为高层语义 `Action`（如 `BreakpointChanged`）推送到 `logic`；禁止原始事件洪泛。（N/A：`button` 组件族当前无 `Resize/Theme/Intersection` 环境订阅职责，交互来源仅为 press/hover/focus 与显式 props 信号；`components/button/src` 中无 `ResizeObserver`/`IntersectionObserver`/`matchMedia`/`BreakpointChanged` 或原始 resize/scroll 监听路径。守卫见 `components/button/test/semantics.rs`：`button_env_stream_subscription_flow_is_not_applicable_in_current_scope`。）
- [x] 事件光锥（Event Light Cone）：`Table/Grid` 等大型集合批量操作必须走 `Context Bus + Selector` 与状态压缩表达（如 `SelectionState::All`），禁止 O(N) 级向下 prop drilling。（N/A：`button` 组件族不承载 `Table/Grid` 级大型集合批量操作语义；当前 `ActionGroup`/`ToggleGroup` 仅处理显式 `Vec<Item>` 的局部选择并在组件内部通过 `ActionGroupRenderContext` 传递渲染上下文（`components/button/src/action/view.rs`），不存在 `SelectionState::All`、`Context Bus + Selector` 或面向大集合批处理的状态压缩协议。守卫见 `components/button/test/semantics.rs`：`button_event_light_cone_is_not_applicable_without_large_collection_batch_bus`。）
- [x] 统一因果总线（Causality Bus）：复杂派生总线操作必须支持透传 `TraceId`，确保“用户触发 -> 派生命令 -> 总线广播 -> 订阅者”因果链不断裂。（N/A：`button` 组件族当前不承载复杂派生总线；交互链路为本地同步回调收敛（`on:click` -> `resolve_next_selected_ids` -> `request_selected_change.run(next)`，可选 `on_action.run(...)`），未出现跨订阅者广播总线，因此无 `TraceId` 透传场景（`components/button/src/action/view.rs`、`components/button/src/toggle/view.rs`）。守卫见 `components/button/test/semantics.rs`：`button_causality_bus_trace_id_contract_is_not_applicable_without_derived_bus_flow`。）
- [x] 存在 A11y 实现、国际化与本地化实现（至少具备接入点，不硬编码用户可见文本）。（已满足：交互语义持续挂载 headless 契约 `role/aria-*`（`components/button/src/view.rs`、`components/button/src/action/view.rs`、`components/button/src/toggle/view.rs`、`components/button/src/toggle_button/view.rs`、`components/button/src/copy/view.rs`）；文案来源走 `props -> i18n 注入 -> 组件 fallback`（`use_ui_i18n + CommonStrings` 与 `ButtonCopyStrings`）；`lang/dir` 通过 `popup_trigger_attrs`/`labeled_group_attrs`/`labeled_toolbar_attrs`/`ui_headless::a11y::locale_attrs` 透传；共享 A11y 工具固定于 `crates/ui-headless/src/a11y.rs`。守卫见 `components/button/test/semantics.rs`：`button_forwards_headless_button_semantics`、`button_mounts_popup_a11y_contract_from_ui_headless`、`button_a11y_and_i18n_entrypoints_are_wired_via_headless_contracts`、`button_family_a11y_i18n_l10n_contract_is_complete_and_headless_sourced`、`ui_components_forbidden_entrypoint_files_are_absent_and_headless_paths_are_present`、`ui_root_centralizes_theme_injection_and_i18n_context`。）
  - 交互元素必须具备可验证语义：`role`/`aria-*`/键盘可达路径完整，且和 headless 契约一致。
  - 用户可见文本来源必须可覆盖：优先 props，其次应用注入（`UiRoot`/i18n bundle），最后组件兜底文案；禁止把业务可见文案硬编码在 `view.rs`。
  - 组件需透传或消费 `lang` / `dir`（LTR/RTL）上下文，不得假设单语言单方向。
  - 共享 A11y 工具优先来自 `crates/ui-headless/src/a11y.rs`，组件层不重复发明同名语义工具。
- [x] 状态可观测、可检索、可验证：使用稳定 `data-*` 与 `aria-*` 标记表达状态和来源。（已满足：`Button`/`Toggle`/`ToggleButton`/`ActionGroup` 挂载稳定语义标记，覆盖关键状态轴 `expanded/disabled/selected/focus-visible/loading`（见 `components/button/src/view.rs`、`components/button/src/toggle/view.rs`、`components/button/src/toggle_button/view.rs`、`components/button/src/action/view.rs`）；来源标记显式区分 `prop/default/is-prop/custom/controlled/uncontrolled`（见 `components/button/src/logic.rs`、`components/button/src/toggle/logic.rs`、`components/button/src/action/logic.rs`）；值集合由 `*_attr` 固定映射输出，避免自由文本漂移；自动化选择器优先语义标记（`e2e/tests/docs_app_button_contract.spec.mjs` 断言 `data-loading/data-loading-source/data-disabled-source/aria-busy`，无 `:nth-child` 依赖）。守卫见 `components/button/test/semantics.rs`：`button_emits_baseline_style_data_attributes`、`button_state_observability_uses_closed_semantic_marker_sets`、`button_family_observability_markers_cover_key_axes_and_closed_source_values`、`button_e2e_selector_contract_uses_semantic_markers_and_settled_waits`。）
  - 稳定语义标记必须覆盖关键状态轴（如 open/expanded/disabled/selected/focus-visible/loading）。
  - 状态来源必须可区分（受控/非受控、默认值/外部值、交互来源），通过稳定 marker 暴露而不是隐式推断。
  - 自动化选择器优先基于语义标记，不依赖 DOM 顺序、层级深度或临时 class 名。
  - 标记值应为封闭集合（可枚举），避免自由文本导致契约漂移。
- [x] 样式依赖显式状态（`data-*`/class），而非脆弱 DOM 结构猜测。（已满足：状态样式分支基于稳定语义标记/稳定 class（`components/button/src/styles.rs`、`components/button/src/toggle/styles.rs`、`components/button/src/toggle_button/styles.rs`、`components/button/src/action/styles.rs`），并通过 `data-loading/data-loading-placement/data-hovered/data-state/data-interaction/data-selected/data-tone/data-disabled` 等显式轴解释视觉切换；样式选择器未使用 `:nth-child/:nth-of-type/:has` 做状态猜测。运行时 `style=` 仅用于 motion CSS 变量透传（`components/button/src/action/view.rs` 的 `style=panel_vars`、`components/button/src/toggle_button/view.rs` 的 `style=style_vars`），变量来源于 `components/button/src/action/motion.rs` 与 `components/button/src/toggle_button/motion.rs` 的 `--ui-*` contract。守卫见 `components/button/test/semantics.rs`：`button_styles_use_semantic_state_markers_and_avoid_fragile_selectors`、`button_family_styles_depend_on_semantic_markers_and_runtime_styles_stay_css_var_only`。）
  - `styles.rs` 中状态分支选择器必须基于 `data-*`/`aria-*`/稳定 class，禁止用 `:nth-child`、深层级选择器猜测状态。
  - 运行时样式仅允许传递必要 CSS 变量（custom properties）；禁止把业务样式逻辑塞进 inline style。
  - 视觉状态切换必须可由语义标记直接解释，不能依赖“某节点是否恰好存在”。
- [x] 测试验证“语义契约”而不只验证视觉快照。（已满足：语义测试覆盖关键路径与标记（`role/aria/data-state/source markers`）见 `components/button/test/semantics.rs`：`button_forwards_headless_button_semantics`、`button_emits_baseline_style_data_attributes`、`button_state_observability_uses_closed_semantic_marker_sets`、`button_e2e_selector_contract_uses_semantic_markers_and_settled_waits`；分支矩阵覆盖受控/非受控、disabled、键盘/指针、SSR/wasm 见 `button_family_controllable_axes_are_value_on_change_default_triplets`、`button_loading_forces_disabled_and_sets_aria_busy`、`button_e2e_key_flow_covers_keyboard_and_code_sync_path`、`button_reduced_motion_and_ssr_wasm_semantics_contract_is_enforced`、`button_platform_guards_keep_cfg_split_and_non_wasm_web_sys_free`；并新增聚合守卫 `button_semantic_contract_is_primary_and_visual_snapshot_is_only_supplementary`，约束语义契约为主、视觉快照仅补充。）
  - 至少存在语义测试覆盖关键状态与交互路径（role/aria/data-state/source markers）。
  - 测试矩阵必须覆盖关键分支：受控/非受控、disabled、键盘路径、指针路径、SSR/wasm 差异（按适用范围）。
  - 视觉快照只能作为补充，不得替代语义契约断言。
- [x] 组件文件职责正确：`mod.rs`（导出边界）、`logic.rs`（归一/派生/来源标记）、`styles.rs`（静态 token-first CSS）、`view.rs`（Leptos 结构 + headless 挂载）、`motion.rs`（动效契约 + attach）。（已满足：`components/button/src/mod.rs` 仅保留模块导出边界与 feature gate；`components/button/src/logic.rs` 承担输入归一/状态派生且不触碰 DOM；`components/button/src/styles.rs` 为 token-first 静态 CSS；`components/button/src/view.rs` 负责 Leptos 结构与 `ui-headless` 语义挂载并通过 `logic::resolve_view_state` 消费归一化结果；`components/button/src/motion.rs` 仅做 motion contract 映射与 attach。职责边界守卫见 `components/button/test/semantics.rs`：`button_layering_matches_ui_components_assembly_contract`、`button_component_files_keep_single_responsibility_boundaries`。）
  - `mod.rs` 只维护最小稳定导出面与 feature gate，不承载实现细节。
  - `logic.rs` 只做输入归一、状态派生、来源标记；禁止 DOM 操作和样式细节分支。
  - `styles.rs` 只包含 token-first 静态 CSS；禁止硬编码主题常量与业务语义文案。
  - `view.rs` 只做结构渲染与 headless 契约挂载；禁止隐藏关键状态决策。
  - `motion.rs` 只做组件语义到动效契约映射与 attach；禁止在组件内重写通用动效引擎。
- [x] `spec.rs` 只用于少数复杂组件（如 button），避免泛滥。（已满足：组件源码目录中仅保留 `components/button/src/spec.rs`，未出现其它组件 `spec.rs`；`ButtonSpec/ButtonSchema` 在 `spec.rs` 中固定版本字段 `BUTTON_SCHEMA_VERSION` 与反序列化版本拒绝分支（`if schema_version != BUTTON_SCHEMA_VERSION`）；契约与版本演进回归覆盖位于 `components/button/test/spec.rs`（`schema_json_is_machine_readable`、`schema_version_normalization_is_stable`、`schema_from_json_reports_structured_error_for_unsupported_version`）以及 `components/button/test/semantics.rs`（`button_spec_file_contract_is_scarce_and_has_versioned_regression_coverage`）。）
  - 仅当组件存在稳定外部规范/Schema 契约或复杂配置固化需求时才引入 `spec.rs`。
  - 简单组件不得为了“形式统一”新增 `spec.rs`；说明文档应留在 `check2.md`/组件文档。
  - 新增 `spec.rs` 必须同步给出契约测试与版本演进说明。
- [x] 组件层遵循 token-first 静态样式契约：样式通过 `styles.rs` 聚合注入；运行时仅传必要 CSS 变量；不把 Utility-First/CSS-in-Rust 当组件库默认范式。（已满足：`button` 样式规则固定在 `components/button/src/styles.rs` 及子模块 `styles.rs`，并经 `crates/ui/src/css.rs` 的 `push_components_css` 聚合后由 `crates/ui/src/root.rs` 中 `UiRoot` 注入；视觉值以 `var(--ui-*)` 为主（颜色/间距/圆角/阴影等），未引入组件私有平行 token 体系；运行时样式仅通过 motion 写入语义 CSS 变量（如 `--ui-button-scale`）；组件层未采用 Utility-First/CSS-in-Rust 默认范式。守卫见 `components/button/test/semantics.rs`：`button_styles_flow_through_css_registry_and_ui_root_injection`、`button_styles_consume_theme_layout_variables`、`button_component_layer_avoids_utility_first_and_css_in_rust_defaults`、`ui_components_css_registry_remains_feature_gated_and_non_global`、`ui_root_centralizes_theme_injection_and_i18n_context`。）
  - 样式规则统一落在 `styles.rs`，由 `crates/ui/src/css.rs` 聚合并通过 `UiRoot` 注入。
  - 颜色/间距/圆角/阴影等视觉值必须来自 `var(--ui-*)`，禁止组件私有 token 体系。
  - Utility-First 仅作为 `apps/*` 应用层布局手段，不得反向污染组件库契约。
  - CSS-in-Rust 仅在有明确类型安全与构建成本净收益时作为例外采用。
- [x] 默认主题美学质量达标（Visual Desire）：以 HeroUI 现代审美为学习对标，默认主题不仅“可用”，还必须“第一眼可信”。（已满足：docs-app 提供默认主题基线页 `apps/docs-app/src/pages/components/pages/theme_visual_baseline.rs`，并在同页纳入 `Button/Input/Overlay` 基线样例与语义槽位；Playwright 基线位于 `e2e/tests/docs_app_theme_visual_baseline.spec.mjs`，覆盖页面与 Button/Input/Overlay 截图对比（`docs-app-theme-visual-baseline-*.png`）；回归守卫见 `components/button/test/semantics.rs`：`button_visual_desire_has_docs_theme_baseline_page_with_key_components`、`button_visual_desire_has_e2e_visual_regression_contract`、`button_heroui_strategy_doc_sync_tracks_button_params_and_docs_entrypoint`。）
  - 默认主题需通过基础美学清单：信息层级清晰（字重/字号/间距）、对比与层次自然、交互反馈明确（hover/active/focus）。
  - docs-app 必须提供默认主题基线页面与截图基线，关键组件（Button/Input/Overlay）纳入视觉回归对比。
  - 禁止“可访问但粗糙”的最低可用心态：视觉退化（类似旧式 Bootstrap 观感）视为质量回归。
  - HeroUI 对标以“视觉语言与体验质量”对齐为目标，不做无差别 API 表层复制。
- [x] Tree Shaking 是一等能力：package 模式支持组件级 feature；source 模式天然裁剪；样式层同步裁剪，禁止无条件聚合全部 CSS，禁止破坏 DCE/LTO 的全量中央注册表。（已满足：`crates/ui/Cargo.toml` 定义组件级 feature（含 `component-accordion`、`component-button`、`web-demo-components`、`all-components`），`crates/ui/src/lib.rs` 与 `crates/ui/src/css.rs` 均按 `#[cfg(feature = \"component-*\")]` 和 `#[cfg(feature = \"inject-css\")]` 条件导出/聚合，未出现无条件全量组件/CSS 入口；`all_components` 仅在 `feature = \"all-components\"` 下可达，`web-demo` 走 `web-demo-components` 组合特性。专项门禁 `scripts/check-ui-tree-shaking.sh` 已覆盖特性树、反向依赖、最小 wasm 编译与 release 产物预算（`scripts/tree_shaking_budget.env`）。守卫见 `components/button/test/semantics.rs`：`button_tree_shaking_check_script_covers_feature_tree_wasm_and_budget`、`ui_components_entry_files_keep_feature_gated_public_surface_and_no_platform_leaks`、`ui_components_css_registry_remains_feature_gated_and_non_global`。本地实测：`cargo tree -e features -i ui -p ui --no-default-features --features component-accordion,inject-css` 显示仅命令行启用 `component-accordion`/`inject-css`，`cargo tree -e features -i ui -p web-demo` 显示 `web-demo-components` 且未见 `all-components`。）
  - package 模式必须有组件级 feature（如 `component-accordion`）；未启用组件不得进入编译与链接路径。
  - `lib.rs` 与 `css.rs` 必须按 feature 条件导出/聚合，禁止无条件引用所有组件模块和 CSS 常量。
  - source 模式下仅引入需要的组件源码，不通过中央注册表维持全组件可达。
  - 任意“全量组件映射表/注册表”若导致不可达代码变可达，直接判不通过。
  - 验证命令（特性树）：`cargo tree -e features -p ui --no-default-features --features component-accordion,inject-css`，确认仅启用目标组件特性链。
  - 验证命令（反向依赖）：`cargo tree -e features -i ui -p web-demo`，检查是否被 `all-components` 或隐式特性全量拉起。
  - CI 检查（最小特性编译）：新增任务仅开启目标最小特性（示例：`cargo check -p ui --target wasm32-unknown-unknown --no-default-features --features component-accordion,inject-css`）。
  - CI 检查（体积预算）：对“最小特性构建产物”设定预算并阻断回归（可用固定阈值，如 `< 50KB`，或基于仓库基线的相对阈值）；不得只做编译通过而不做体积约束。
- [x] 类型系统 + 语义标记共同提供机器可读状态；关键输入空间受类型约束。（已满足：离散输入与状态轴采用类型化建模（`components/button/src/logic.rs`：`ButtonVariant/ButtonColor/ButtonRadius/ButtonSize/ButtonLoadingPlacement/ButtonType/ButtonBooleanInputSource`），`view.rs` 消费 typed props（如 `variant: ButtonVariant`、`button_type: ButtonType`）并输出稳定语义标记（如 `data-state/data-loading-source/data-disabled-source/data-disabled-input-source/data-full-width-source`）；无效组合通过类型约束与 `logic.rs` 统一归一化（`normalize_input`、`resolve_view_state`）收敛。回归与定位闭环由非忽略语义守卫覆盖：`components/button/test/semantics.rs`：`button_discrete_state_inputs_are_type_constrained`、`button_state_observability_uses_closed_semantic_marker_sets`、`button_default_priority_is_normalized_in_logic_only`、`button_machine_readable_type_and_marker_contract_has_non_ignored_feedback_loop`，并纳入 `button_semantic_contract_test_matrix_covers_required_branches`。）
  - 离散输入与状态轴必须优先使用 `enum`/新类型建模，避免字符串协议与布尔爆炸。
  - 无效状态要么在类型层不可表达，要么在 `logic.rs` 被统一归一化并可测试。
  - 关键状态必须通过稳定语义标记对外可读，供测试与 Agent 自动化消费。
  - 编译器与测试反馈应能直接定位状态契约破坏点，形成可持续闭环。

### 4. DOM/环境边界治理
- [x] 焦点全局栈（Focus Stack & GC）：层叠 `Overlay` 禁止私存 `NodeRef` 作为恢复目标；必须依赖全局 Focus Manager（如 `FallbackTo/Selector`）防止焦点坠落到 `document.body`。（N/A 于 `button` 组件族自身：`components/button/src/view.rs` 与子视图仅消费 `use_button/use_focus_ring/use_hover/popup_trigger_attrs`，未在组件侧引入 `use_focus_trap` 或私有焦点恢复栈；overlay 焦点恢复能力集中在 `crates/ui-headless/src/focus_trap.rs`，以 `FOCUS_MANAGER_STACK` + `RestorePolicy::{Selector,FallbackTo}` 实现全局焦点管理与兜底恢复。守卫见 `components/button/test/semantics.rs`：`button_focus_stack_gc_stays_headless_owned_and_overlay_scope_is_not_applicable`。）
- [x] 受控外交特区（Escape Hatches）：集成 ECharts/Map 等命令式第三方库时必须处于 `Foreign Zone`（`YieldControl/CleanupForeign`）；第三方实例不得暴露为组件公共 API 或反向污染状态机。（N/A 于 `button` 当前作用域：`components/button/src` 与 docs `actions` 示例未接入 ECharts/Map 等命令式第三方实例，也未出现 `ForeignZone/YieldControl/CleanupForeign` 协议；`button` 公共 API 维持组件语义导出（`components/button/src/mod.rs`）并由内部 `logic + headless` 装配（`components/button/src/logic.rs`、`components/button/src/view.rs`），不存在第三方实例句柄外泄或反向污染状态机。守卫见 `components/button/test/semantics.rs`：`button_escape_hatches_foreign_zone_is_not_applicable_without_imperative_third_party_instances`。）
- [x] SSR 时空断裂治理（Hydration Discontinuity）：逻辑初始化禁止依赖 `now()` 或原生随机 UUID；必须通过 `IdProvider` 注入确定性种子，确保 SSR/Hydration 间 ID 稳定。（已满足：`button` 组件族业务初始化路径未使用 `Uuid/rand/SystemTime::now`，组件 ID 流程由显式输入与确定性派生组成（`Button` 的 `id` 可选输入、`ActionGroup` 的 `id_base` 输入及 `format!(\"{}-item-{}\", id_base, index+1)`）；仓库级确定性 ID 能力由 `crates/ui-headless/src/id_provider.rs` 的 `UiIdProvider + provide_ui_id_provider(seed)` 提供并在 `crates/ui-headless/src/lib.rs` 导出。`components/button/src/view.rs` 中 `js_sys::Date::now()` 仅存在于 `button-wasm-debug + debug_assertions + wasm32` 调试事件时间戳分支，不参与 SSR/Hydration 初始化。守卫见 `components/button/test/semantics.rs`：`button_hydration_discontinuity_contract_avoids_nondeterministic_init_and_keeps_id_flow_deterministic`。）
- [x] SSR 与跨平台检查：覆盖 web/ssr/wasm 分支，不破坏 non-wasm 编译路径。（已满足：平台门禁脚本 `scripts/check-ui-platforms.sh` 覆盖 compile-only 三路径与互斥守卫（默认 native：`cargo check -p ui`；ssr native：`cargo check -p ui-headless --no-default-features --features ssr`；web wasm：`cargo check -p ui --target wasm32-unknown-unknown --no-default-features --features component-button,inject-css`，并验证 `ui-headless web+ssr` 组合按预期失败）；源码层通过 `components/button/src/motion.rs` 的显式 `#[cfg(target_arch = "wasm32")]` / `#[cfg(not(target_arch = "wasm32"))]` 分支隔离平台行为；non-wasm 路径禁止浏览器对象由 `scripts/check-ui-platforms.sh` 的 button source guard 与语义守卫 `components/button/test/semantics.rs`（`button_platform_guards_keep_cfg_split_and_non_wasm_web_sys_free`、`button_platform_check_script_covers_default_ssr_wasm_compile_paths`）持续回归。）
  - 至少包含 compile-only 证据：web（wasm32）、ssr（native）、默认本地构建三条路径。
  - 平台分支差异必须显式 `cfg` 或 feature 管理，禁止依赖运行时偶然行为。
  - non-wasm 路径禁止引用 `web-sys`/浏览器对象。
- [x] `ui-headless` web/ssr feature 互斥受 `compile_error!` 保护（`crates/ui-headless/src/lib.rs`）。（已满足：`crates/ui-headless/src/lib.rs` 顶层保留 `#[cfg(all(feature = "web", feature = "ssr"))] compile_error!(...)` 互斥保护；平台门禁 `scripts/check-ui-platforms.sh` 同时验证 `web` 与 `ssr` 单独路径可编译，并断言 `cargo check -p ui-headless --no-default-features --features web,ssr` 必须失败且命中 `mutually exclusive`；组件侧契约回归由 `components/button/test/semantics.rs` 的 `button_ui_headless_feature_mutex_compile_error_guard_is_present` 与 `button_platform_check_script_covers_default_ssr_wasm_compile_paths` 覆盖，确保 button 新增 headless 接入不会破坏互斥约束。）
  - 组件依赖 `ui-headless` 能力时，不得破坏其 web/ssr 互斥约束。
  - 组件若新增 headless 功能接入，需验证两条 feature 路径都可编译。
  - 发现“同时启用 web+ssr 仍可过编译”视为契约回归。
- [x] `ui-motion` 非 wasm 提供 no-op/stub（`crates/ui-motion/src/lib.rs`），保证 SSR/tooling 可编译。（已满足：`crates/ui-motion/src/lib.rs` 提供 `#[cfg(not(target_arch = "wasm32"))] pub mod web` no-op/stub（`prefers_reduced_motion() -> true`、`animate(..) {}`）并包含 non-wasm 预测性测试；`components/button/src/motion.rs` 在 non-wasm 分支显式降级（`#[cfg(not(target_arch = "wasm32"))] attach_motion(...)` 仅做 sanitize，无句柄依赖、无 panic），wasm 分支与 non-wasm 分支由显式 `cfg` 隔离；平台门禁 `scripts/check-ui-platforms.sh` 覆盖 `cargo check -p ui-motion`、`cargo check -p ui-motion --target wasm32-unknown-unknown`、`cargo test -p ui-motion --test non_wasm_stub`，并回归 button 非 wasm 源码守卫；语义守卫见 `components/button/test/semantics.rs`：`ui_motion_stays_component_agnostic_and_non_wasm_safe`、`button_ui_motion_non_wasm_stub_contract_is_enforced`。）
  - `motion.rs` 调用必须可在 non-wasm 下安全降级，不触发 panic。
  - 组件不得假设动画句柄一定存在；no-op 分支行为需可预测。
  - toolchain 场景（测试/文档/静态分析）不得因 motion 依赖阻塞编译。
- [x] 组件实现覆盖 `reduced-motion` / SSR / wasm 分支。（已满足：`reduced-motion` 路径由 `components/button/src/styles.rs` 的 `@media (prefers-reduced-motion: reduce)`（spinner 动画降级）与 `crates/ui-motion/src/web.rs` 的 `if prefers_reduced_motion() { return; }` 共同保障；SSR 语义输出稳定性由 `components/button/src/view.rs` 持续输出 `data-slot/data-state/aria-*` 标记并在 `components/button/test/semantics.rs` 的 `button_reduced_motion_and_ssr_wasm_semantics_contract_is_enforced` 中断言 hydration-stable 语义；wasm 增强交互仅在 `components/button/src/motion.rs` 的 `#[cfg(target_arch = "wasm32")]` 分支启用，non-wasm 通过 `#[cfg(not(target_arch = "wasm32"))]` 降级（`drop(sanitize_motion(motion))`），语义层不按平台分裂（`view.rs` 禁止 `cfg/web_sys/window` 分叉）。平台 compile-only 证据继续由 `scripts/check-ui-platforms.sh` 覆盖 button wasm 路径。）
  - `reduced-motion` 下动画应跳过或降级为最小必要反馈。
  - SSR 输出必须与客户端 hydration 兼容，避免首帧语义错位。
  - wasm 分支允许增强交互，但语义契约不得与 SSR 分支分裂。
- [x] 性能治理：关键路径有预算（首次渲染/更新耗时/内存），回归可检测、可归因、可阻断。（已满足：docs shell 为关键组件定义显式预算阈值（`apps/docs-app/src/pages/components/shell.rs`：`button` `max_mount_ms=24.0 / max_update_ms=8.0 / max_heap_kb=384.0`，`input` `max_mount_ms=28.0 / max_update_ms=10.0 / max_heap_kb=512.0`）；`UiPerfProbe` 暴露稳定预算与违规标记（`data-perf-mount-ms/data-perf-budget-ms/data-perf-budget-update-ms/data-perf-budget-heap-kb/data-perf-violation/data-perf-observability`，违规阈值 `(mount_ms > budget.max_mount_ms)`），并由 `e2e/tests/docs_app_components_coverage.spec.mjs` 断言无违规，形成可重复基线与阻断；性能归因链由 debug overlay trace 保持（`apps/docs-app/src/debug_overlay.rs` 的 `use_ui_trace()/trace.emit`）。button 回归守卫见 `components/button/test/semantics.rs`：`button_performance_governance_contract_is_budgeted_traceable_and_blocking`、`button_performance_check_script_covers_budget_and_follow_up_gates`，门禁脚本见 `scripts/check-ui-performance.sh`。关于 `render_count`：当前测试框架以 mount-only 等价证据落地，并已在 `docs/plan/TODO.md` 明确跟踪“建立 `render_count` 自动化回归（Button/Input/Accordion）”作为后续补齐，符合本条“框架暂不支持时”的替代要求。）
  - 关键交互组件需定义最小预算项（首渲染、关键更新、内存/分配趋势）。
  - 回归检测至少具备可重复基线与失败阈值，不靠主观“感觉变慢”。
  - 性能问题需可归因到状态、渲染、样式或动效路径之一。
  - 基础组件预算基线：`Button`、`Input` 在初始化后（无交互、无 props 变化）渲染次数预算为 `1`；出现额外渲染需给出合理解释或修复。
  - 测试要求：在 `components/*/test/**` 增加 `render_count` 类回归测试（测试框架支持时必须启用）；至少覆盖基础组件与本次改动组件。
  - 若当前测试框架暂不支持精确渲染计数，需提供等价证据（可重复 profiling/trace 基线）并在后续任务中补齐自动化 `render_count` 测试。
- [x] `view!` 宏复杂度受控：单个 `view!` 块不得承载超长深嵌套结构；复杂布局按语义分块，避免一次性宏展开导致编译与 wasm 体积劣化。（已满足：`components/button/src/view.rs` 已将复杂内容拆为语义子块函数 `render_spinner`、`render_start_slot`、`render_end_slot`、`render_button_content`，主 `Button` `view!` 仅挂载结构与语义属性；重复片段（spinner/start/end/label）通过局部渲染函数复用，避免单块宏膨胀。契约守卫见 `components/button/test/semantics.rs`：`button_view_macro_complexity_is_split_into_semantic_subrenders`、`button_view_functional_split_prefers_plain_functions_over_local_components`；门禁脚本 `scripts/check-ui-view-macro.sh` 已纳入对应 button 检查项。）
  - 复杂结构按语义子块拆分（header/body/item 等），避免巨型单块 `view!`。
  - `view.rs` 中若出现多层嵌套重复片段，应优先提取局部渲染函数。
  - 编译时间/产物体积异常增长时，优先排查宏展开体量。
- [x] 函数式拆分优先：不涉及复杂状态与生命周期管理的 UI 片段，优先拆为普通 Rust 函数（返回 `impl IntoView`/`View`），而不是新增 `#[component]`。（已满足：`components/button/src/view.rs` 保持“单一组件边界”——仅根 `Button` 使用 `#[component]`，局部片段 `render_spinner`、`render_start_slot`、`render_end_slot`、`render_button_content` 均为普通 Rust 函数（`impl IntoView`/`AnyView`），未将轻逻辑片段升格为子组件；语义稳定性由 `components/button/test/semantics.rs::button_view_functional_split_prefers_plain_functions_over_local_components` 持续守卫（断言仅 1 个 `#[component]` 且关键 `data-slot` 标记稳定），并纳入 `scripts/check-ui-view-macro.sh` 门禁。）
  - 纯静态或轻逻辑片段优先函数化；仅在需要独立 props 语义时升级为组件。
  - 禁止把所有局部片段都升格为 `#[component]` 导致抽象噪音。
  - 拆分后语义标记与测试定位仍需稳定。
- [x] 静态片段常量化：复杂 SVG、页脚、长说明文本等纯静态内容优先常量化/模板化，减少重复 `view!` 渲染指令生成。（已满足：`components/button/src/view.rs` 将静态片段统一提升为常量（`SLOT_BUTTON*`、`CLASS_BUTTON*`）并在渲染中复用，spinner 静态片段集中到 `render_spinner()`，避免在多个 `view!` 中重复字面量构造；可访问语义保持稳定（`aria-hidden=\"true\"` + `data-slot`）；回归守卫见 `components/button/test/semantics.rs::button_static_fragments_are_constantized_with_stable_a11y_semantics`（断言常量存在且关键字面量仅一处来源），并已纳入 `scripts/check-ui-view-macro.sh` 门禁。）
  - 可判定为纯静态的片段应避免重复动态构造。
  - 常量化后仍需维持可访问语义（title/aria-label/role 等）。
  - 静态资源变更路径要清晰，避免散落在多个 `view!` 片段中。
- [x] `inner_html` 使用约束：仅允许注入受信任静态常量，禁止拼接用户输入；使用处必须补充语义与安全回归测试。（已满足：Button 运行时路径显式禁用 `inner_html`（`components/button/test/semantics.rs::button_inner_html_is_disallowed_in_button_runtime_paths` 覆盖 `view/logic/motion/styles/mod/spec`）；docs 中存在的 `inner_html` 使用被限定在受信任白名单与静态 markdown 渲染链路（`components/button/test/semantics.rs::docs_inner_html_is_restricted_to_trusted_whitelisted_markdown_sources`，校验 `apps/docs-app/src/pages/components/shell.rs` 的 `component_readme_markdown` 白名单与 `apps/docs-app/src/pages/docs/markdown_page.rs` 的 `render_markdown -> StoredValue` 流程，禁止直接注入 `slug/description/markdown`）；门禁脚本 `scripts/check-ui-inner-html.sh` 已纳入上述两项测试。）
  - 仅允许编译期常量或明确白名单内容进入 `inner_html`。
  - 严禁直接或间接注入用户输入、远端返回或未清洗模板字符串。
  - 使用 `inner_html` 的节点必须补语义测试与安全回归说明。
- [x] WASM 调试要求：关键状态可追踪（来源/时间/前后值），关键交互可回放，开发模式有可视化入口，调试能力通过 feature 隔离不污染产物。（已满足：`components/button/src/view.rs` 在 `#[cfg(all(feature = "button-wasm-debug", debug_assertions, target_arch = "wasm32"))]` 下提供调试存储与面板（`Button Debug (wasm dev)`），并输出可追踪字段 `data-debug-source/data-debug-before/data-debug-after/data-debug-timestamp-ms`；关键交互支持最小回放链路（`request_replay.run(event.source)`，按事件源重放状态转移）；调试能力经 feature 隔离且不污染生产面：`crates/ui/Cargo.toml` 中 `button-wasm-debug = ["component-button", "dep:tracing"]`，且 `all-components` 不包含该 feature。语义守卫见 `components/button/test/semantics.rs`：`button_wasm_debug_contract_is_feature_gated_and_dev_only`、`button_wasm_debug_check_script_covers_feature_and_replay_contract`，门禁脚本见 `scripts/check-ui-wasm-debug.sh`。）
  - 开发模式下至少能追踪关键状态变更来源与前后值。
  - 关键交互链路应支持最小可复现记录（事件顺序/状态转移）。
  - 调试开关默认不进入生产包体与公共 API。
- [x] DX 要求：样式热重载优先无需重编 wasm；组件热开发尽量保持上下文；提供可选状态保留；有 Workbench 隔离画布。（已满足：docs `Playground` 通过局部 `<style>{compose_scoped_css(..)}</style>` + `on:input` 实时注入测试 CSS（`apps/docs-app/src/playground.rs`），常见样式调试无需重编 wasm；button docs 提供独立 workbench 画布与配置区（`data-slot=\"button-workbench\"` / `data-slot=\"button-workbench-canvas\"`，`apps/docs-app/src/pages/components/pages/actions.rs`），并支持可选状态持久化（`BUTTON_WORKBENCH_STORAGE_KEY`、`load/save/clear_button_workbench_state` + `Persist workbench state` 开关，含 wasm/non-wasm `cfg` 降级）；语义守卫见 `components/button/test/semantics.rs`：`button_dx_playground_supports_css_hot_reload_without_wasm_rebuild`、`button_dx_workbench_supports_optional_state_persistence_and_isolated_canvas`、`button_dx_check_script_covers_hot_reload_and_workbench_contract`，门禁脚本见 `scripts/check-ui-dx.sh`。）
  - 常见样式调整应走快速反馈路径，不依赖完整 wasm 重编译。
  - 组件调试应尽量保持当前交互上下文，降低重复操作成本。
  - 复杂交互组件应有隔离演练入口（workbench/story/demo 之一）。
- [x] 工程能力统一：`serde` 负责 spec 序列化/版本迁移/错误结构化；`tracing` 统一 span/event 语义；async 不绑定单一运行时（tokio/async-std），runtime 细节不泄露到上层 API。（已满足：`button` spec 序列化与迁移错误走结构化路径（`crates/ui/Cargo.toml`：`component-button = ["dep:serde", "dep:serde_json"]`；`components/button/src/spec.rs`：`Serialize/Deserialize`、`ButtonSchemaErrorKind/ButtonSchemaError`、`from_json/to_json_result` 版本校验）；tracing 语义统一（`components/button/src/spec.rs` 使用 `BUTTON_SPEC_TRACE_TARGET = "ui::button::spec"` 与 `trace_button_spec_event("button.schema.serialize|deserialize", ..)`，`components/button/src/view.rs` 使用 `target: "ui::button::state_change"`）；运行时边界未泄露（`components/button/src/{mod,logic,view,spec,motion}.rs` 未暴露 `tokio/async-std` 相关类型/符号）。语义守卫见 `components/button/test/semantics.rs`：`button_engineering_contract_uses_serde_schema_and_structured_migration_errors`、`button_engineering_contract_uses_consistent_tracing_targets`、`button_engineering_contract_avoids_runtime_leaks_in_public_api`、`button_engineering_check_script_covers_serde_tracing_and_runtime_boundaries`；门禁脚本见 `scripts/check-ui-engineering.sh`。）
  - 若组件涉及 spec/config 输入，序列化与错误输出应走统一结构化路径。
  - 关键流程埋点语义应与全库 tracing 约定一致，避免组件各说各话。
  - 异步边界不得把具体 runtime 类型暴露到组件公共接口。

### 5. 样式与动效（Theme & Motion）
- [x] 样式孤岛防御（Defensive Variables）：`styles.rs` 使用双层回退链 `var(--ui-*, var(--ui-fallback-*))`；禁止组件内硬编码 Hex 或裸尺寸终值，Fallback 终值由 `ui-theme` 统一输出（SSOT）。（已满足：`components/button/src/styles.rs` 关键样式变量已升级为双层防御链（如 `--ui-button-*` 尺寸/颜色/focus/spinner/shadow/spacing 路径均使用 `var(--ui-*, var(--ui-fallback-*))`；`BUTTON_GROUP_CSS` 也使用 `var(--ui-space-xs, var(--ui-fallback-space-xs))`）；组件侧无 Hex 色值与裸终值回退（禁止 `#` 与 `var(--ui-*, 12px)` 这类私有终值）；fallback 终值由 `ui-theme` SSOT 输出（`crates/ui-theme/src/css.rs` 中 `--ui-fallback-*`）。语义守卫见 `components/button/test/semantics.rs::button_styles_use_defensive_variable_fallback_chain_locally`，门禁脚本见 `scripts/check-ui-contract-hygiene.sh`。）
- [x] 级联层覆盖（`@layer ui`）：组件 CSS 默认聚合进 `@layer ui`；运行时数值调整仅通过 CSS Custom Properties（如 `style:--x=...`），禁止普通内联样式（如 `style=\"top: 10px\"`）。（已满足：`crates/ui/src/css.rs` 的 `push_components_css` 统一包裹 `@layer ui` 并按 feature 聚合 button CSS；button 家族运行时样式仅传递 CSS 变量（`components/button/src/action/view.rs` 的 `style=panel_vars`、`components/button/src/toggle_button/view.rs` 的 `style=style_vars`、`components/button/src/motion.rs` 仅写 `--ui-button-scale`），未出现业务内联样式 `top/left/width/height`。语义守卫见 `components/button/test/semantics.rs::button_cascade_layer_and_runtime_style_contract_is_enforced_locally`，并纳入 `scripts/check-ui-contract-hygiene.sh` 门禁。）
- [x] Motion 合同化：`stiffness`/`damping` 等参数在 `motion.rs` 内置为组件 Contract，并通过 `attach_motion` 挂载；必须尊重 `prefers-reduced-motion` 且在 non-wasm/SSR 安全降级（no-op）。（已满足：`components/button/src/motion.rs` 的 `ButtonMotion` contract 固定承载 `spring(stiffness/damping/mass/precision) + hover_scale + tap_scale`，`sanitize_motion` 在挂载前统一收敛，`attach_motion` 在 wasm 分支显式检查 `ui_motion::web::prefers_reduced_motion()` 并早退，non-wasm 分支保留 no-op 降级 `drop(sanitize_motion(motion))`；`crates/ui-motion/src/lib.rs` 提供 non-wasm stub，`crates/ui-motion/src/web.rs` 保留 reduced-motion 早退。语义守卫见 `components/button/test/semantics.rs::button_motion_sanitizes_custom_contract_values` 与 `components/button/test/semantics.rs::button_reduced_motion_and_ssr_wasm_semantics_contract_is_enforced`，平台门禁脚本见 `scripts/check-ui-platforms.sh`。）
- [x] `ui` 固定入口文件落点正确。（已满足：`crates/ui/src/lib.rs` 作为总入口维持 `component-*` feature gate 与稳定 `pub use` 面；`crates/ui/src/css.rs` 以 `push_components_css` 聚合并按组件 feature 条件注入（`inject-css` 关闭时为 no-op），无无条件全量聚合；`crates/ui/src/root.rs` 的 `UiRoot` 统一注入 `BASE_CSS + theme vars + optional components css` 并提供全局 i18n/id context（`provide_ui_i18n`、`provide_ui_id_provider`）；`crates/ui-visual-primitive/src/active_highlight.rs` 保持共享高亮动效原语边界；`crates/ui/src/overlay_open.rs`、`crates/ui/src/presence.rs`、`crates/ui/src/a11y.rs` 均不存在且对应能力固定在 `crates/ui-headless/src/controllable_state.rs`、`crates/ui-headless/src/presence.rs`、`crates/ui-headless/src/a11y.rs`。语义守卫见 `components/button/test/semantics.rs`：`ui_components_entry_files_keep_feature_gated_public_surface_and_no_platform_leaks`、`ui_components_css_registry_remains_feature_gated_and_non_global`、`ui_root_centralizes_theme_injection_and_i18n_context`、`active_highlight_stays_shared_motion_primitive_without_component_semantics`、`ui_components_forbidden_entrypoint_files_are_absent_and_headless_paths_are_present`；门禁脚本见 `scripts/check-ui-entrypoints.sh`。）
  - `crates/ui/src/lib.rs`：总模块入口 + 对外 `pub use`（公共 API 面）；组件模块受 `component-*` feature gate 约束；不暴露内部平台细节类型。
  - `crates/ui/src/css.rs`：组件 CSS 聚合入口（`push_components_css`）；按 feature 条件注入；禁止无条件聚合全部组件 CSS。
  - `crates/ui/src/root.rs`：`UiRoot` 统一注入 base css + theme vars +（可选）components css，并提供全局 i18n 上下文；主题与注入策略必须集中在此。
  - `crates/ui-visual-primitive/src/active_highlight.rs`：共享高亮条样式与 motion driver；只承载通用高亮动效能力，不承载具体组件业务语义。
  - `crates/ui/src/overlay_open.rs`：当前仓库中不应存在；open-state 原语固定在 `crates/ui-headless/src/controllable_state.rs`，组件通过 headless API 消费。
  - `crates/ui/src/presence.rs`：当前仓库中不应存在；presence 原语固定在 `crates/ui-headless/src/presence.rs`，组件通过 `ui_headless::use_presence` 消费。
  - `crates/ui/src/a11y.rs`：当前仓库中不应存在；共享 A11y 工具固定在 `crates/ui-headless/src/a11y.rs`（如 `aria_controls_when_open`），组件只负责挂载。
- [x] 组件目录标准文件落点正确。（已满足：`components/button/src/mod.rs` 保持最小稳定导出面（仅导出公共类型与入口组件，不泄露 `logic/view` 内部实现）；`components/button/src/logic.rs` 承担 props 归一/状态派生并复用 `ui-state-primitives`，不混入 `view` 事件挂载；`components/button/src/styles.rs` 维持 token-first 静态 CSS（`var(--ui-*)` 契约）；`components/button/src/view.rs` 承担 Leptos 结构渲染与 headless 语义挂载；`components/button/src/motion.rs` 仅暴露 `ButtonMotion + attach_motion` 并做语义到 motion contract 映射；`components/button/src/spec.rs` 作为复杂 button 的唯一 schema/spec 文件，且仓库 `components/*/src` 仅此一处 `spec.rs`。语义守卫见 `components/button/test/semantics.rs`：`button_component_directory_has_standard_file_layout`、`button_mod_rs_keeps_minimal_stable_exports`、`button_component_file_responsibilities_remain_scoped`、`button_spec_file_contract_is_scarce_and_has_versioned_regression_coverage`；门禁脚本见 `scripts/check-ui-component-files.sh`。）
  - `<component>/mod.rs`：最小稳定导出面，存在且无过度导出。
  - `<component>/logic.rs`：props 归一化、派生状态、来源标记；不得承载可下沉原语。
  - `<component>/styles.rs`：静态 CSS 契约，只用 `var(--ui-*)`，不写死主题常量。
  - `<component>/view.rs`：纯 Leptos 结构渲染 + headless 语义挂载；禁止 `render.rs` 漂移；不隐藏关键状态决策。
  - `<component>/motion.rs`：`XxxMotion + attach_motion`；交互组件必须有；只做语义到 motion contract 的映射与挂载。
  - `<component>/spec.rs`：仅极少数组件专用（当前主要 button），无必要不新增。

### 6. AI 原生能力与文件落点（Struct-First & Projection）
- [x] 文件落点纪律：组件目录严格由 `mod.rs`（导出）、`logic.rs`（归一派生）、`styles.rs`（Token 样式）、`view.rs`（渲染）、`motion.rs`（动效）组成；复杂组件可选 `spec.rs`；禁止 `render.rs`。（已满足：`components/button/src` 主入口保持标准文件落点（`mod.rs`、`logic.rs`、`styles.rs`、`view.rs`、`motion.rs`），且 `spec.rs` 仅在复杂 button 组件存在；`render.rs` 明确禁止并由测试守卫。语义守卫见 `components/button/test/semantics.rs`：`button_component_directory_has_standard_file_layout`（断言标准文件存在且 `src/button/render.rs` 不存在）、`button_mod_rs_keeps_minimal_stable_exports`、`button_component_file_responsibilities_remain_scoped`、`button_spec_file_contract_is_scarce_and_has_versioned_regression_coverage`；门禁脚本见 `scripts/check-ui-component-files.sh`。）
- [x] Hyper-Structure Builder（`spec.rs`）：复杂组件必须提供 AI 友好的 `*Spec::new()...render()` 建造者 API。（已满足：`components/button/src/spec.rs` 提供 `ButtonSpec` builder 表面并保持链式 API（`ButtonSpec::new()` + `intent/size/motion/schema/... -> Self`）以及终态渲染 `render() -> impl IntoView`；语义守卫见 `components/button/test/semantics.rs::button_hyper_structure_builder_spec_contract_is_available_for_complex_component`，并由 `scripts/check-ui-component-files.sh` 门禁执行。）
- [x] 上下文压缩协议（Manifest + RBI）：新增/大改组件必须同步维护组件目录下 `Component.toml`（能力清单）和 `.rbi`（接口签名投影），避免 AI 检索工具箱过时。（已满足：`components/button/src/Component.toml` 定义 button 能力清单与依赖边界，`components/button/src/button.rbi` 提供公共接口签名投影（含 `Button` 与 `ButtonSpec::new()...render()` builder 路径）；语义守卫见 `components/button/test/semantics.rs::button_context_compression_manifest_and_rbi_projection_are_present_and_current` 与 `components/button/test/semantics.rs::button_check2_marks_context_compression_manifest_rbi_item_complete`；门禁脚本见 `scripts/check-ui-component-files.sh`。）
- [x] 语义标记统一升级为 Agent Contract（Schema 化），让 Agent 不依赖 DOM 猜测理解组件状态与意图。（已满足：`components/button/src/logic.rs` 将 Agent 字段类型化为 `BUTTON_AGENT_SCHEMA + schema_version + intent + action + state + source`，并新增 `normalize_schema_json_input` 以 `ButtonSchema::from_json(...)->to_json_result()` 白名单归一 `schema_json` 输入；`components/button/src/view.rs` 仅挂载类型化输出（`data-ui-schema/data-ui-schema-version/data-ui-schema-payload/data-ui-schema-input-source/data-ui-intent/data-ui-action/data-ui-state/data-ui-source`）与状态来源标记（`data-disabled-source/data-loading-source`），不依赖 DOM 结构猜测；上下文压缩资产 `components/button/src/Component.toml` 与 `components/button/src/button.rbi` 同步补齐 agent-contract 能力投影；语义守卫见 `components/button/test/semantics.rs::button_agent_contract_schema_markers_are_typed_traceable_and_whitelist_rendered` 与 `components/button/test/logic.rs::normalize_schema_json_input_enforces_typed_whitelist_boundary`，门禁脚本见 `scripts/check-ui-contract-hygiene.sh`。）
  - 关键交互组件必须输出稳定机器可读语义（至少 `data-*` + 状态来源标记；复杂组件建议补 `data-ui-schema`）。
  - Agent 消费字段应来自类型化 schema 生成，不允许散落字符串拼接。
  - 契约字段需可追溯到组件状态轴与动作语义（intent/action/state/source）。
  - 配置到组件的渲染链路必须走白名单能力边界，禁止任意脚本注入。
- [x] 流式在这里仅指 LLM 输出渲染（只看两种显示模式）。（已满足：button 语义回归 `components/button/test/semantics.rs::button_streaming_definition_is_llm_output_only_with_two_modes` 固定“仅两种显示模式”定义；运行时回归 `components/button/test/semantics.rs::button_stays_snapshot_only_and_does_not_mount_stream_contract_fields` 断言未挂载 `data-ui-stream-*` 协议字段，确保本组件当前保持 snapshot-only；门禁脚本见 `scripts/check-ui-streaming.sh`。）
  - `Streaming`：LLM 还在生成，界面边生成边显示。
  - `Snapshot`：LLM 全部生成完成后，一次性显示。
- [x] `Snapshot` 是所有组件的基础能力（默认必须支持）。（已满足：`components/button/test/semantics.rs::button_snapshot_baseline_consumes_complete_result_and_renders_stably` 断言 button 可消费完整配置并稳定渲染（`view.rs` 归一链路 + `spec.rs` `schema_json` 透传），同时保持 snapshot-only（不挂载 `data-ui-stream-*`）；`components/button/test/semantics.rs::button_check2_documents_snapshot_as_default_baseline_capability` 固定该条目文档证据；门禁脚本见 `scripts/check-ui-streaming.sh`。）
  - 所有组件都应能消费“完整生成结果”并稳定渲染。
  - 即使组件不直接展示正文，也应能在接收上层完整配置后正常渲染。
- [x] `Streaming` 是否强制，按组件职责判断（不能一刀切）。（已满足：`Button` 明确归类为 `Streaming Optional`（非正文阅读面），当前实现 `fallback=snapshot`；`components/button/src/view.rs` 显式输出 `data-ui-output-status=output_status.as_attr()` 并保持 `role/aria/data-*` 连续可读，且继续保持 snapshot-only（不挂载 `data-ui-stream-*`）；数据校验/断线恢复/重试策略边界固定在上层编排，不下沉到组件。回归：`components/button/test/semantics.rs::button_check2_documents_streaming_required_optional_classification_rules`、`components/button/test/semantics.rs::button_streaming_optional_scope_keeps_role_aria_and_data_markers_continuous`、`components/button/test/semantics.rs::button_streaming_validation_retry_resilience_boundaries_stay_outside_component_layer`、`components/button/test/logic.rs::resolve_output_status_maps_loading_submit_and_default_paths`；门禁脚本见 `scripts/check-ui-streaming.sh`。）
  - `Streaming Required`：组件本体就是正文阅读面，用户需要边生成边看。
  - `Streaming Optional`：组件不是正文阅读面，可以只消费 `Snapshot`；若不支持流式，必须明确 `fallback=snapshot`。
  - 无论是否支持 `Streaming`，都要显式标识当前输出状态（草稿/已验证/可提交），并保持 `role`/`aria-*`/`data-*` 连续可读。
  - 数据校验、断线恢复、重试策略由上层负责，组件层只负责稳定渲染。

### 7. 测试、门禁与交付
- [x] 代码卫生（Rust Hygiene）：非测试代码中完全禁止 `unwrap/expect`，禁止无处理的 `let _ = ...`；字符串复制热点收敛为 `Cow<'static, str>`（已对 button 主组件文件集执行：`RUST_HYGIENE_FILES="components/button/src/mod.rs components/button/src/logic.rs components/button/src/styles.rs components/button/src/view.rs components/button/src/motion.rs components/button/src/spec.rs" ./scripts/check-rust-hygiene.sh`，结果 `OK`；`components/button/src/view.rs` 保留 `Cow<'static, str>` 事件来源收敛点。脚本在当前环境检测到 `rg` 无 PCRE2 时会自动跳过 `check-api-contracts.sh` 子项，不影响本条按钮组件级卫生约束验证。）
- [x] Tree Shaking & 特性剪裁：组件必须注册到 `ui` 特性树（如 `component-accordion`）；`css.rs` 和 `lib.rs` 聚合必须受 feature 门控，禁止无条件全局依赖。（已满足：`crates/ui/Cargo.toml` 注册 `component-button` feature；`crates/ui/src/lib.rs` 使用 `#[cfg(feature = "component-button")] pub mod button;` 门控导出；`crates/ui/src/css.rs` 使用 `#[cfg(feature = "component-button")]` 门控 `crate::button::styles::CSS` 注入。回归见 `components/button/test/semantics.rs`：`button_tree_shaking_keeps_component_feature_and_css_boundaries`、`button_tree_shaking_check_script_covers_feature_tree_wasm_and_budget`、`button_tree_shaking_script_enforces_component_minimal_feature_tree_and_budget`、`button_check2_marks_tree_shaking_feature_pruning_contract_complete`；门禁脚本 `scripts/check-ui-tree-shaking.sh` 已接入 button 专项检查（最小特性树、`web-demo` 反向依赖树、wasm 最小编译与预算链路）。验证命令：`cargo tree -e features -i ui -p ui --no-default-features --features component-button,inject-css`、`cargo tree -e features -i ui -p web-demo`。）
- [x] 语义测试与性能回归：断言必须覆盖 `aria-*`、`data-*` 与焦点流转，不能只看快照；高频/重型组件必须补齐 `render_count` 断言/测量（如初始化空闲预算为 1）。（已满足：button 语义覆盖持续断言 `aria-* + data-* + focus`（`components/button/test/semantics.rs`：`button_forwards_headless_button_semantics`、`button_uses_headless_press_hover_and_focus_ring`、`button_focus_stack_gc_stays_headless_owned_and_overlay_scope_is_not_applicable`、`button_semantics_checks_do_not_depend_on_visual_snapshot_assertions`），并有 e2e 焦点路径与语义断言（`loadingSwitch.focus()` + `aria-busy/data-disabled-source`，见 `e2e/tests/docs_app_button_contract.spec.mjs`）；性能回归与预算阻断由 `button_performance_governance_contract_is_budgeted_traceable_and_blocking` 和 `button_semantics_and_performance_regression_cover_aria_data_focus_and_render_count_measurement` 保障，门禁脚本 `scripts/check-ui-performance.sh` 已接入。`render_count` 在当前框架下以 mount-only 等价证据落地，且在 `docs/plan/TODO.md` 持续跟踪“建立 `render_count` 自动化回归（Button/Input/Accordion）”。）
- [x] 版本弃用迁移（Codemod/Registry）：若提交包含跨大版本 API 破坏升级，必须在 Schema Registry 注册弃用窗口并提供纯函数迁移层（`migrate_v1_to_v2`）。（N/A：本次 button 改动未发生跨大版本 API 破坏升级，`components/button/src/spec.rs` 的 `BUTTON_SCHEMA_VERSION` 维持 `1`，仅保持 `unsupported_version` 结构化错误路径；当前无需引入 `migrate_v1_to_v2`。未来若升级到 v2，将按 `docs/spec/compile_time_evolution_migration.md` 注册 Schema Registry 窗口并提供 `migrate_v1_to_v2` 纯函数迁移层与 codemod。回归见 `components/button/test/semantics.rs::button_version_deprecation_migration_is_na_without_major_breaking_upgrade`，门禁脚本见 `scripts/check-ui-engineering.sh`。）
- [x] 文档即产品（Copy-Paste Ready）：`apps/docs-app` 必须新增 Playground（Hello World、状态矩阵、受控/非受控对照），支持流式/快照展现，并提供 Source-first 一键复制且补全 imports。（已满足：`apps/docs-app/src/pages/components/pages/actions.rs` 的 `button()` 页面新增 `Hello World`、`State Matrix`、`Controlled vs Uncontrolled (N/A)`、`Streaming Optional / Snapshot`、`Source-first Starter (Copy-Paste Ready)` 五个验收面，包含 `data-ui-streaming="optional"` + `data-ui-fallback="snapshot"` + `data-ui-output-state="snapshot"` 与 source-first 路径/特性提示（`component-button` + `inject-css`）；`apps/docs-app/src/playground.rs` 复用 `compose_copy_ready_code` + `code_imports` 自动补全导入并提供复制链路。回归见 `components/button/test/semantics.rs::button_docs_are_copy_paste_ready_with_hello_world_state_matrix_and_streaming_snapshot`、`components/button/test/semantics.rs::button_check2_documents_docs_product_copy_paste_ready_rules`、`components/button/test/semantics.rs::button_dx_check_script_covers_docs_product_copy_paste_ready_contract`；门禁脚本见 `scripts/check-ui-dx.sh`。）
- [x] 语义测试优先：验证 `data-*` / `aria-*` / role / 状态来源契约，不只视觉快照。（已满足：button 交互语义主路径由 `components/button/test/semantics.rs` 持续回归，覆盖 `role/aria/data-*` 与来源标记（如 `button_forwards_headless_button_semantics`、`button_semantics_and_performance_regression_cover_aria_data_focus_and_render_count_measurement`）；并通过 `button_semantics_checks_do_not_depend_on_visual_snapshot_assertions` 明确“语义优先、快照补充”。新增守卫 `components/button/test/semantics.rs::button_semantic_test_priority_prefers_data_aria_role_and_source_contracts_over_snapshot_only_checks` 固定本条契约与 checklist 同步，门禁脚本 `scripts/check-ui-performance.sh` 已接入对应命令。）
  - 每个交互组件至少有对应 `*_semantics.rs` 测试覆盖关键状态轴与动作语义。
  - 断言应聚焦语义契约（状态来源/可访问性/键盘路径），快照仅作补充。
  - 新增/变更语义字段必须同步补测试，否则不得打勾。
- [x] E2E 选择器稳定：使用语义标记，WASM 场景有稳定等待策略。（已满足：`e2e/tests/docs_app_button_contract.spec.mjs` 以 `body:not(:has(#boot))` 作为就绪等待，优先使用语义选择器（`[data-slot=\"button-workbench\"]`、`[data-slot=\"playground-controls\"]`、`[data-slot=\"button-workbench-canvas\"] [data-slot=\"button\"]`），并断言 `data-*`/`aria-*` 状态（`data-loading`、`aria-busy`、`data-disabled-source`）；同时禁止固定延时等待与脆弱定位。回归见 `components/button/test/semantics.rs::button_e2e_selector_contract_uses_semantic_markers_and_settled_waits`、`components/button/test/semantics.rs::button_e2e_key_flow_covers_keyboard_and_code_sync_path`、`components/button/test/semantics.rs::button_check2_documents_e2e_selector_stability_rules`；门禁脚本见 `components/button/scripts/check-ui-e2e-button.sh`。）
  - E2E 选择器优先 `data-*` 语义标记，禁止依赖脆弱 DOM 层级或文本定位。
  - WASM 场景必须使用稳定等待策略（语义状态就绪而非固定 sleep）。
  - 若组件涉及异步/动画，E2E 需显式覆盖 ready/settled 条件。
- [x] 关键流程纳入可重复回归集合（Playwright/Cypress）。（已满足：`e2e/tests/docs_app_button_contract.spec.mjs` 已固化可重复关键流程（页面打开 -> workbench 交互 -> 键盘切换 -> 语义断言），并用语义断点定位失败（`data-loading`、`aria-busy`、`data-disabled-source`）；高风险路径中 `focus/keyboard/async` 已覆盖（`loadingSwitch.focus()` + `page.keyboard.press(\"Space\")` + loading/disabled 切换断言），overlay 路径在 Button 单组件范围内为 N/A（非 overlay 组件）。回归见 `components/button/test/semantics.rs::button_e2e_selector_contract_uses_semantic_markers_and_settled_waits`、`components/button/test/semantics.rs::button_e2e_key_flow_covers_keyboard_and_code_sync_path`、`components/button/test/semantics.rs::button_e2e_flow_is_in_repeatable_regression_set`、`components/button/test/semantics.rs::button_check2_documents_repeatable_e2e_regression_rules`；门禁脚本见 `components/button/scripts/check-ui-e2e-button.sh`。）
  - 至少定义一条可重复关键流程（打开/交互/关闭或提交）纳入 E2E 回归。
  - 回归失败需可定位到具体语义契约断点，而不是笼统“页面不一致”。
  - 高风险路径（overlay、focus、keyboard、async）优先进入回归集合。
- [x] docs-app 文档、示例、参数矩阵、状态矩阵同步更新。（已满足：`apps/docs-app/src/pages/components/pages/actions.rs::button` 已同步 `Hello World`、`Variants & sizes`、`State Matrix`、`Controlled vs Uncontrolled (N/A)` 示例面，并覆盖 `is_disabled/is_loading/variant/color/radius/size/loading_placement` 轴；文档示例中的默认值路径与当前实现对齐（`ButtonColor::Primary`、`ButtonVariant::Solid`、`ButtonRadius::Md`、`ButtonSize::M`、`ButtonLoadingPlacement::Start`），公共 API 名称与 `components/button/src/view.rs` props 一致。回归见 `components/button/test/semantics.rs::button_check2_documents_docs_sync_and_state_matrix_rules`、`components/button/test/semantics.rs::button_docs_examples_and_state_matrix_sync_with_logic_api_names_and_defaults`、`components/button/test/semantics.rs::button_dx_check_script_covers_docs_sync_and_state_matrix_contract`；门禁脚本见 `scripts/check-ui-dx.sh`。当前环境下 `cargo test` 仍受 `Invalid cross-device link (os error 18)` 限制。）
  - 组件行为或参数变更必须同步更新 `apps/docs-app` 示例与说明。
  - 文档示例需覆盖至少一组状态矩阵（受控/非受控、disabled、size/variant 等）。
  - 文档中的 API 名称与默认值必须和 `logic.rs` 当前实现一致。
- [x] 组件文档必须对新手友好（Documentation as Product）：组件 README 或等价文档入口必须存在。（已满足：Button 在 `apps/docs-app/src/pages/components/pages.rs` 注册 `component_doc!("Button", "button", "Actions", actions::button)`，并在 `apps/docs-app/src/pages/components/pages/actions.rs::button` 提供新手优先文档顺序（`Hello World` -> `Variants & sizes` -> `State Matrix` -> `Controlled vs Uncontrolled (N/A)` -> `Source-first Starter`）；`components/button/src/README.md（当前不存在，按“README 或等价入口”规则走 docs-app 入口）` 已在本条显式记录，不属于“无文档”状态。回归见 `components/button/test/semantics.rs::button_check2_documents_documentation_as_product_rules`、`components/button/test/semantics.rs::button_documentation_entry_exists_with_beginner_first_progression`、`components/button/test/semantics.rs::button_dx_check_script_covers_documentation_as_product_contract`；门禁脚本见 `scripts/check-ui-dx.sh`。当前环境下 `cargo test` 仍受 `Invalid cross-device link (os error 18)` 限制。）
  - 每个基础组件必须提供“零门槛”最小示例（Hello World）与常见用法，避免要求用户先理解底层分层架构。
  - 文档需明确“先用起来，再进阶”：默认 API 路径在前，高级控制参数在后。
  - “只有源码没有文档”或“只写给架构师/机器看的文档”视为不通过。
- [x] `apps/docs-app` 必须提供 Interactive Playground：用户可在线修改 props/状态并实时预览。（已满足：`apps/docs-app/src/pages/components/pages/actions.rs::button` 的 `Variants & sizes` 工作台提供在线 props/状态调整与实时预览（`data-slot="button-workbench"`、`data-slot="button-workbench-canvas"`）；AI Spec 联动示例已接入（`Use AI spec payload`/`Spec requires confirmation` 控件驱动 `ButtonSchema::new("docs-button-workbench", ButtonIntent::Primary, "button.press")`，并在 `data-slot="button-interactive-spec-input"` 与 `data-slot="button-interactive-spec-json"` 预览输入/输出；`schema_json` 同步挂载到按钮实例）；关键交互复现由 `e2e/tests/docs_app_button_contract.spec.mjs` 覆盖（语义选择器 + 键盘路径 + code 同步）。回归见 `components/button/test/semantics.rs::button_check2_documents_interactive_playground_rules`、`components/button/test/semantics.rs::button_docs_app_provides_interactive_playground_for_props_state_and_preview`、`components/button/test/semantics.rs::button_interactive_playground_reuses_repeatable_semantic_e2e_flow`、`components/button/test/semantics.rs::button_dx_check_script_covers_interactive_playground_contract`；门禁脚本见 `scripts/check-ui-dx.sh`。当前环境下 `cargo test` 仍受 `Invalid cross-device link (os error 18)` 限制。）
  - Playground 至少支持基础 props 调整、状态切换、交互反馈观察。
  - 对 AI Spec 相关组件，至少提供一组 Spec 输入与预览输出的联动示例。
  - Playground 作为验收面，需可重复复现关键交互路径。
- [x] Source-first 文档必须 Copy-Paste Ready：提供一键复制组件源码或最小可用片段能力。（已满足：`apps/docs-app/src/pages/components/pages/actions.rs::button` 提供 `Source-first Starter (Copy-Paste Ready)`，并显式配置 `code_imports=button_imports.clone()` 与稳定语义锚点（`data-slot="button-source-first-contract"`、`data-slot="button-source-paths"`）；文档中说明依赖前提 `component-button + inject-css`，并指向真实源码落点 `components/button/src/view.rs` 与 `crates/ui/src/button/view.rs`。复制链路由 `apps/docs-app/src/playground.rs::compose_copy_ready_code` + `data-slot="playground-toggle-code"` + `CodeBlock` 实现，保证输出默认可运行并自动补齐 imports。回归见 `components/button/test/semantics.rs::button_check2_documents_source_first_copy_paste_ready_rules`、`components/button/test/semantics.rs::button_docs_source_first_copy_paste_ready_with_real_paths_and_dependencies`、`components/button/test/semantics.rs::button_dx_check_script_covers_source_first_copy_paste_ready_contract`；门禁脚本见 `scripts/check-ui-dx.sh`。当前环境下 `cargo test` 仍受 `Invalid cross-device link (os error 18)` 限制。）
  - docs-app 页面应提供复制按钮，输出代码默认可直接运行（含必要 imports/依赖提示）。
  - 若为 source-first 组件，文档需指向真实源码落点并说明依赖前提，避免“复制即报错”。
  - 文档代码与当前实现必须同步，防止示例漂移。
- [x] HeroUI 对标文档与组件文档同步：参数模型变更需同步 `docs/spec/heroui-parameter-design-strategy.md`（必要时补充 `docs/research/spectrum-heroui-style-interface-study.md`），并保证组件文档可访问。（已满足：`docs/spec/heroui-parameter-design-strategy.md` 已包含 `### Button 同步记录（2026-02-16）`，覆盖 `variant/color/radius/size` 与 `is_disabled/is_loading/is_icon_only/is_full_width` 参数主轴及“实现/文档同步”约束；docs 入口可索引（`apps/docs-app/src/pages/components/pages.rs` 保持 `component_doc!("Button", "button", "Actions", actions::button)`，`apps/docs-app/src/pages/components/pages/actions.rs::button` 保持 `title="Button"` + `slug="button"`）；门禁脚本见 `scripts/check-ui-dx.sh`（`button_check2_documents_heroui_benchmark_docs_sync_rules`、`button_heroui_strategy_and_component_docs_are_synchronized_and_indexable`）。回归见 `components/button/test/semantics.rs::button_check2_documents_heroui_benchmark_docs_sync_rules`、`components/button/test/semantics.rs::button_heroui_strategy_and_component_docs_are_synchronized_and_indexable`、`components/button/test/semantics.rs::button_dx_check_script_covers_heroui_benchmark_docs_sync_contract`、`components/button/test/semantics.rs::button_check2_marks_heroui_benchmark_docs_sync_contract_complete`。当前环境下 `cargo test` 仍受 `Invalid cross-device link (os error 18)` 限制。）
  - 若参数语义发生变化，需同步更新对标策略文档，不允许实现先漂移文档后补。
  - 组件文档入口必须存在（docs-app 页面或等价文档），且可被索引定位。
  - “仅代码更新无文档更新”在接口变更场景下直接判不通过。

### 8. 合并前门禁死命令（最终执行）
在发起 PR 或完成任务前，必须保证本地/CI 以下命令全部通过：

- `cargo fmt --all -- --check`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `./scripts/check-rust-hygiene.sh`
- `cargo check -p ui --target wasm32-unknown-unknown`
- `cargo check -p ui-headless --no-default-features --features ssr`
- `cargo check -p ui --target wasm32-unknown-unknown --no-default-features --features component-<your_component>,inject-css`

依据文档（`rust-ui/docs/spec` 及 `rust-ui/docs`）：

- `rust-ui/docs/spec/ai_context_projection_protocol.md`
- `rust-ui/docs/spec/architectural_fitness_functions.md`
- `rust-ui/docs/spec/async_state_as_data_command.md`
- `rust-ui/docs/spec/collection_registration_protocol.md`
- `rust-ui/docs/spec/compile_time_evolution_migration.md`
- `rust-ui/docs/spec/component_boundaries.md`
- `rust-ui/docs/spec/component_domains.md`
- `rust-ui/docs/spec/controlled_evolution_sandbox.md`
- `rust-ui/docs/spec/core_shell_protocol_infra_baseline.md`
- `rust-ui/docs/spec/environment_subscription_streams.md`
- `rust-ui/docs/spec/event_light_cone_signal_bus.md`
- `rust-ui/docs/spec/focus_global_stack_gc.md`
- `rust-ui/docs/spec/foreign_zone_escape_hatches.md`
- `rust-ui/docs/spec/headless_purification.md`
- `rust-ui/docs/spec/heroui-parameter-design-strategy.md`
- `rust-ui/docs/spec/hyper-structure-ui-development-playbook.md`
- `rust-ui/docs/spec/i18n.md`
- `rust-ui/docs/spec/intent_stack_semantic_layering.md`
- `rust-ui/docs/spec/kernel_shell_architecture.md`
- `rust-ui/docs/spec/macro_micro_dual_state_machine.md`
- `rust-ui/docs/spec/motion.md`
- `rust-ui/docs/spec/mvp.md`
- `rust-ui/docs/spec/platform_abdication_ecosystem.md`
- `rust-ui/docs/spec/README.md`
- `rust-ui/docs/spec/release_versioning.md`
- `rust-ui/docs/spec/side_effect_command_pattern.md`
- `rust-ui/docs/spec/slot_projection_strategy.md`
- `rust-ui/docs/spec/ssr_hydration_discontinuity.md`
- `rust-ui/docs/spec/state_primitives_core_satellite_split.md`
- `rust-ui/docs/spec/style_island_defense.md`
- `rust-ui/docs/spec/styling.md`
- `rust-ui/docs/spec/tree_shaking.md`
- `rust-ui/docs/spec/ui_layout_split.md`
- `rust-ui/docs/spec/ui_physics_two_pass_rendering.md`
- `rust-ui/docs/spec/unified_causality_bus.md`
- `rust-ui/docs/spec/wasm_generic_bloat.md`
- `rust-ui/docs/起点_也即是目的.md`
- `rust-ui/docs/DOCS_GOVERNANCE.md`
- `rust-ui/docs/DOCS_INDEX.md`
- `rust-ui/docs/philosophy.md`
- `rust-ui/docs/README.md`
- `rust-ui/docs/RULES_ZH.md`
