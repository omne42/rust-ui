# 单组件 Check List（Hyper-Structure 完整版）

> 用途：每次新增、重构或修改一个组件时，必须按本清单逐项检查并在 PR 中附带结果。  
> 执行顺序：先过第 1-2 节（架构与状态），再进入第 3-5 节（具体实现）。  
> 术语统一：`status-primitives` 指状态原语层（当前 crate 名为 `ui-state-primitives`）。

### 0. 适用范围与顺序纪律
本清单仅评估“一个组件”的改动结果，不替代仓库级治理。
先过第 1-2 节（架构与状态）再进入第 3-5 节（具体实现）。
不适用的条目必须明确标注 `N/A` 并说明理由，禁止机械打勾。
组件目标、非目标、风险边界已写清楚；发现跨组件/跨层系统性问题时升级为仓库级任务。

### 1. 架构边界与分层约束（Kernel/Shell 总线）
- [x] `status-primitives` 定义：纯状态原语层（受控/非受控、toggle、selection、list、overlay open state、expansion 等）。不依赖 Leptos/DOM/web-sys；只包含 Rust 数据结构和方法，不含视图与事件绑定。（证据：`crates/ui-state-primitives/src/avatar_group.rs` 仅保留语义状态与归一化；样式 class 映射在 `components/avatar-group/src/logic.rs`；原语单测见 `crates/ui-state-primitives/src/test/avatar_group.rs`。）
  - 所有状态原语必须从 `status-primitives`（`ui-state-primitives`）获取，组件层只能消费，不得自造。
  - 下沉判定依据是“稳定状态不变量”；凡属于状态机、归一化、状态派生能力，默认先进入 `ui-state-primitives`。
  - 组件中可保留的仅是装配逻辑：props 归一、样式来源标记、slot 组织、对 `ui-state-primitives` 输出的映射。
  - 组件内若出现状态原语实现（受控/非受控状态机、single/multiple 展开规则、索引归一化、跨事件状态派生），该项直接判不通过。
  - 处理方式固定：先下沉到 `ui-state-primitives/src/<capability>.rs`（如 `expansion.rs`），在 `ui-state-primitives/src/lib.rs` 导出，再回到组件改调用。
  - 下沉后的原语必须有 `ui-state-primitives` 单元测试；组件侧只保留调用与语义挂载测试。
  - 桥接规范：`ui-state-primitives` 结构体必须是 POJO（Plain Old Rust Object），不持有 Leptos `Signal` 或框架绑定状态容器。
  - 消费规范：`ui-headless` 或组件 `logic.rs` 负责解包 `Signal` 当前值传入 primitive 方法，并将结果显式写回 `Signal`。
  - 设计理由：保持 primitives 纯粹可测、可迁移，不与特定响应式库绑定（便于未来替换响应式实现与做纯 Rust 测试）。
- [x] `ui-headless` 定义：交互与 A11y 原语层（press/focus/hover/roving/listbox/menu/tooltip 等），把输入设备事件与状态语义标准化为可复用契约；输出必须是类型化 `attrs + handlers + state`。不做样式、不写组件 CSS、不做组件级动效编排。（证据：`components/avatar-group/src/view.rs` 使用 `ui_headless::labeled_group_attrs` 挂载 `role/aria-label/lang/dir`，未在组件层重写共享 A11y 工具；`crates/ui-headless/src/a11y.rs` 提供 `LabeledGroupA11yAttrs` 与 `labeled_group_attrs`；语义回归见 `components/avatar-group/test/avatar_group/semantics.rs` 的 `avatar_group_a11y_i18n_l10n_contract_is_headless_driven_and_no_view_hardcoded_copy`。该组件为静态展示，无键盘/指针交互 handler，`handlers` 与交互 e2e 流程判定为 N/A。）
  **`ui-headless` 落位硬规则（必须执行）**：
  - 输入边界：消费 `status-primitives` 状态 + 用户输入事件（keyboard/pointer/focus）+ 环境能力（web/ssr）。
  - 输出边界：只输出语义契约（attrs/handlers/state）；组件层只负责挂载与组合，不得把语义判断塞回 `view.rs`。
  - 下沉判定依据是“交互/A11y 语义契约”；凡属于键盘/焦点/指针归一、ARIA 映射、交互状态语义能力，默认先进入 `ui-headless`。
  - 必须下沉：键盘模型、焦点模型、跨设备输入归一、ARIA 状态映射、overlay/presence 等交互语义。
  - A11y 契约与共享工具落点固定在 `crates/ui-headless/src/a11y.rs`；组件只在 `view.rs` 挂载，不在组件层重写。
  - 语义契约必须提供 `lang` / `dir`（LTR/RTL）接入能力；headless 不硬编码用户可见文本，文案由 i18n/l10n 层提供。
  - 语义契约正确性必须有回归：`components/*/test/**` 断言语义标记，`e2e/tests/*` 覆盖关键交互流程。
  - 禁止放在 `ui-headless`：视觉 class 选择、CSS 规则、组件 slot 布局、组件专属动效编排、业务文案。
  - 允许留在组件层：纯视觉一次性交互且不形成可复用语义契约（例如单组件局部微交互）。
- [x] `ui-motion` 定义：动效能力与契约执行层（spring、keyframes、WAAPI/RAF backend），只负责时间函数、插值与运行时驱动，不承载组件业务语义与状态决策。（证据：`components/avatar-group/src` 无 `motion.rs`，`view.rs`/`logic.rs` 无 `ui_motion`、`attach_motion`、`SpringAnimator`、`request_animation_frame`；`styles.rs` 无 `transition:`/`animation:`；回归见 `components/avatar-group/test/avatar_group/semantics.rs` 的 `avatar_group_does_not_define_component_motion_runtime`。该组件当前无 enter/exit/open/close 动效语义，组件级 motion contract 判定为 N/A；全局 non-wasm no-op/stub 由 `crates/ui-motion/src/lib.rs` 提供并含 `non_wasm_web_backend_is_predictable_noop` 测试。）
  - 放在 `crates/ui-motion`：通用动画数学与执行后端（spring solver、keyframe sampling、easing registry、driver adapters），以及 `wasm/non-wasm` 适配与 `reduced-motion` 执行策略。
  - 放在 `crates/ui/src/<component>/motion.rs`：把组件语义状态（open/closed、enter/exit、active/inactive）映射为 `ui-motion` contract，绑定目标节点并调用 attach。
  - 禁止放在 `crates/ui-motion`：组件 slot 结构、组件专属状态机、ARIA/keyboard 语义、业务文案与业务分支。
  - 禁止放在组件 `motion.rs`：自实现 spring/keyframe/driver 执行器；跨组件共享动效算法必须回迁 `ui-motion`。
  - 动效参数优先来自 token/theme；禁止在组件样式与逻辑中散落硬编码时长/曲线/位移常量。
  - 非 wasm 路径必须提供 no-op/stub，保证 SSR/tooling 可编译且行为可预测。
- [x] `ui-theme` 定义：唯一设计 token 与主题上下文层（system/color/scale + Light/Dark/OLED），负责 token 分类、主题映射与 CSS 变量生成。（证据：主题三轴与映射在 `crates/ui-theme/src/theme.rs`（`ThemeSystem/ThemeColor/ThemeScale/ThemeContext`），token 分类在 `crates/ui-theme/src/tokens.rs`，CSS 变量输出在 `crates/ui-theme/src/css.rs`；规范落点在 `docs/spec/styling.md`。`avatar-group` 组件仅在 `components/avatar-group/src/styles.rs` 消费 `var(--ui-*)`，`components/avatar-group/src/view.rs`/`components/avatar-group/src/logic.rs` 未引入 `ThemeContext`/`ui_theme::`/`theme_to_css_variables`；回归见 `components/avatar-group/test/avatar_group/semantics.rs` 的 `avatar_group_theme_contract_consumes_ui_variables_only`。主题尺寸基线回归在 `crates/ui-theme/tests/token_scale_baseline.rs`，WCAG 对比回归在 `crates/ui-theme/tests/wcag_contrast.rs`（覆盖 Light/Dark/Oled）。）
  - Token 统一基线落点固定：`crates/ui-theme/src/tokens.rs` 定义，`crates/ui-theme/src/theme.rs` 映射，`crates/ui-theme/src/css.rs` 输出变量；组件只在 `crates/ui/src/<component>/styles.rs` 消费。
  - 三轴上下文（`system/color/scale`）在 `theme.rs` 定义；组件在 `logic.rs` 选择并在 `view.rs` 生效，`styles.rs` 只消费变量，不重建主题。
  - Token 分类必须可追溯：分类源在 `tokens.rs`，规范同步 `docs/spec/styling.md`；组件不得引入平行私有 token 命名体系。
  - 量化尺寸基准必须可回归：尺寸基准在 `tokens.rs` 与 `theme.rs` 定义，主题回归在 `crates/ui-theme/tests/token_scale_baseline.rs`，组件语义回归在 `components/*/test/*<component>_semantics.rs`。
  - 主题调色与语义色对比必须满足 `WCAG 2.1 AA` 基线，并覆盖 Light/Dark/OLED 主题变体。
  - 主题层只输出 `theme/tokens/base css` 与变量；不实现组件结构、交互逻辑、组件级动效编排。
  - 新增视觉语义先补 token，再由组件消费；禁止“组件临时值先落地、后补 token”的倒序流程。
- [x] `ui` 定义：最终 Leptos 组件装配层，组合 `status-primitives + ui-headless + ui-motion + ui-theme` 并暴露稳定公共 API。（证据：文件职责拆分为 `components/avatar-group/src/logic.rs`（归一/派生）、`components/avatar-group/src/view.rs`（结构 + headless 语义挂载）、`components/avatar-group/src/styles.rs`（token-first 静态样式）、`components/avatar-group/src/mod.rs`（最小导出边界）；组件层消费 `ui-state-primitives` 与 `ui-headless`，未重写状态机与交互契约；公共导出未暴露 `web-sys`/DOM 细节。语义测试已迁移到组件目录：`components/avatar-group/test/semantics.rs`（并由 `components/avatar-group/src/mod.rs` 在 `#[cfg(test)]` 下接入）。）
  - `logic.rs` 负责 props 归一与状态派生；`view.rs` 负责结构渲染与 headless 语义挂载；`styles.rs` 负责 token-first 静态样式；`motion.rs` 负责动效 attach。
  - 组件层不得重写 `status-primitives` 状态机或 `ui-headless` 交互契约；发现即判不通过并回迁到对应层。
  - 对外 API 禁止暴露 `web-sys`/DOM 细节类型；平台差异封装在内部模块。
  - 测试文件位于src同级的test/中，内部测试文件同名（如rust-ui/components/accordion/src/logic.rs与rust-ui/components/accordion/test/logic.rs）。
  - 还需要一个semantics.rs用于测试。可能存在类似rust-ui/components/accordion/test/semantics.rs的旧版实现，需要迁移到新目录。

### 2. API 设计与状态内核（Logic/Kernel）
- [x] API 命名契约统一：公共 props/回调严格使用 `is_*`、`on_*`、`default_*` 前缀；同语义在全库同名，禁止别名漂移。（证据：`components/avatar-group/src/view.rs` 的公开签名为 `items/max/size/aria_label/class_name/lang/dir`，当前无布尔状态 props、无回调、无默认值 props；命名前缀规则在本组件属于“未来扩展约束”，非现状缺口。回归覆盖：`components/avatar-group/test/semantics.rs` 的 `avatar_group_api_naming_contract_is_prefix_ready_without_alias_drift` 与 `components/avatar-group/test/avatar_group/semantics.rs` 的 `avatar_group_public_api_naming_contract_is_stable_and_prefix_ready`。）
  - 布尔状态统一 `is_*`（如 `is_open`/`is_disabled`），事件统一 `on_*`，默认值统一 `default_*`。
  - 同一语义 across 组件必须同名（如都用 `on_open_change`，禁止同义别名并存）。
  - 公共 API 引入新命名时，需说明与现有命名体系的兼容策略与迁移路径。
- [x] 受控/非受控必须成对：每个可控状态轴都提供 `value + on_value_change + default_value`（如 `open/on_open_change/default_open`）；缺一项即不通过。（证据：`components/avatar-group/src/view.rs` 的 `AvatarGroup` 公开 API 当前无可控状态轴（无 `value`/`on_*_change`/`default_*` 参数），因此本项按 N/A 语义通过；组件未引入 `use_controllable_state` 或等价半受控逻辑。回归覆盖：`components/avatar-group/test/semantics.rs` 的 `avatar_group_has_no_controllable_state_axis_and_avoids_half_controlled_api` 与 `components/avatar-group/test/avatar_group/semantics.rs` 的 `avatar_group_has_no_controllable_state_axis_and_no_half_controlled_api`。）
  - 受控模式：外部值是单一事实来源，内部不得偷偷写回本地状态。
  - 非受控模式：仅由默认值初始化一次，后续状态由内部原语管理。
  - 受控/非受控切换语义需稳定可测，避免“半受控”隐式行为。
- [x] 默认值单一来源：默认值与优先级只在 `logic.rs` 归一化；`view.rs` 禁止二次兜底或隐式改写。（证据：`components/avatar-group/src/logic.rs` 的 `normalize_avatar_group_input` 与 `normalize_avatar_group_item_fields` 统一处理默认值与兜底（`resolve_avatar_group_aria_label_with_fallback`、`unwrap_or_default`）；`components/avatar-group/src/view.rs` 仅消费 `logic::normalize_avatar_group_input` 输出，不含 `unwrap_or*` 兜底分支。回归覆盖：`components/avatar-group/test/semantics.rs` 的 `avatar_group_defaults_are_normalized_in_logic_only`，以及 `components/avatar-group/test/avatar_group/semantics.rs` 的 `avatar_group_defaults_are_centralized_in_logic`。）
  - 默认值优先级必须可读且可测试（显式规则而非分散 `unwrap_or`）。
  - `view.rs` 不允许再做默认值分支；仅消费 `logic.rs` 的归一化输出。
  - 一旦发现多处默认值来源，直接判不通过并回收至 `logic.rs`。
- [x] 状态归一化集中：状态输入先类型化，再在 `logic.rs` 统一派生；禁止在 `view.rs`、事件回调、样式分支中分散拼状态机。（证据：`components/avatar-group/src/logic.rs` 以 `AvatarGroupStateInput` 类型化输入，并由 `normalize_avatar_group_input`/`normalize_avatar_group_item_fields`/`resolve_avatar_group_render_state` 集中归一与派生；`components/avatar-group/src/view.rs` 仅构造 `logic::AvatarGroupStateInput` 并消费 `logic::*` 输出，未直接调用 `ui_state_primitives` 派生函数；`components/avatar-group/src/styles.rs` 仅消费 `data-state` 等语义标记。回归覆盖：`components/avatar-group/test/semantics.rs` 的 `avatar_group_state_normalization_is_centralized_in_logic`，以及 `components/avatar-group/test/avatar_group/semantics.rs` 的 `avatar_group_state_normalization_is_centralized_in_logic`。该组件无事件处理器，事件子条目按 N/A 语义成立。）
  - 输入边界统一进入 `logic.rs`，输出统一为可渲染语义状态与来源标记。
  - 事件处理器只触发状态变更，不重建状态机规则。
  - 样式层只消费状态标记，不承担状态判定职责。
- [x] 离散状态必须类型约束：`variant/size/mode/status` 等离散输入使用 `enum`；禁止用多个 `Option<bool>`/字符串自由组合表达互斥状态。（证据：`components/avatar-group/src/view.rs` 公开签名使用 `size: AvatarSize`，未暴露 `variant/mode/status` 字符串协议；`crates/ui-state-primitives/src/avatar_group.rs` 以 `AvatarGroupVisualState`、`AvatarGroupAriaLabelSource`、`AvatarGroupClassSource` 三个枚举封装互斥状态并通过 `as_str()` 输出闭合集。回归覆盖：`components/avatar-group/test/semantics.rs` 的 `avatar_group_discrete_state_axes_are_type_safe_enums`，`crates/ui-state-primitives/src/test/avatar_group.rs` 的 `resolve_render_state_maps_discrete_status_and_sources_to_enums`，以及 `components/avatar-group/test/avatar_group/semantics.rs` 的 `avatar_group_state_markers_are_observable_and_closed_set_contracts`。）
  - 互斥状态优先用 `enum` 建模，利用编译器封住无效组合。
  - 字符串输入若需兼容外部配置，必须先映射到类型化枚举再进入逻辑层。
  - 布尔爆炸（多个 bool 表达一个状态机）应在设计评审阶段直接拦截。
- [x] 状态原语来源正确：组件层只消费 `status-primitives`（当前 `ui-state-primitives`）能力，不直接绑定业务 store；应用级全局状态必须经桥接层适配后再接入组件。（证据：`components/avatar-group/src/logic.rs` 仅通过 `pub use ui_state_primitives::avatar_group::{AvatarGroupRenderState, AvatarGroupStateInput}` 与 `ui_state_primitives::avatar_group::resolve_render_state(input)` 消费原语；`components/avatar-group/src/view.rs` 仅构造 `logic::AvatarGroupStateInput` 并消费 `logic::*` 结果，未直接绑定业务 store；`crates/ui-state-primitives/src/avatar_group.rs` 持有 `AvatarGroupStateInput/AvatarGroupRenderState` 与 `resolve_render_state` 原语定义。回归覆盖：`components/avatar-group/test/semantics.rs` 的 `avatar_group_state_primitive_source_boundary_is_enforced`，以及 `components/avatar-group/test/avatar_group/semantics.rs` 的 `avatar_group_state_primitive_source_boundary_is_enforced`。）
  - 组件中出现可复用状态机实现（受控/非受控、展开规则、选择归一）即判应下沉。
  - 组件与业务全局状态之间必须有适配边界，禁止组件直接依赖业务 store 类型。
  - `logic.rs` 仅做装配与映射，不重新实现状态原语。
- [x] 如果无异步相关，直接打勾。异步交互语义统一：`is_loading`、error/retry、disabled、`aria-busy` 映射一致；优先复用统一 async action 原语（如 `use_async_action`），禁止每组件自定义一套加载/错误协议。（证据：`components/avatar-group/src/view.rs`/`components/avatar-group/src/logic.rs`/`components/avatar-group/src/styles.rs` 均无 `use_async_action`、`is_loading`、`aria-busy`、`retry`、`async fn` 等异步交互协议；该组件为同步静态渲染路径，无远程请求与异步状态，因此本项按 N/A 语义通过。回归覆盖：`components/avatar-group/test/semantics.rs` 的 `avatar_group_has_no_async_loading_protocol_and_keeps_sync_render_contract`，以及 `components/avatar-group/test/avatar_group/semantics.rs` 的同名测试。）
  - 无异步交互时需明确标注 N/A 理由（例如“组件无远程请求与异步状态”），不是机械打勾。
  - 有异步交互时，`is_loading`/disabled/`aria-busy`/retry 语义必须成套一致，且对键盘与读屏路径可用。
  - 异步失败态要有可恢复路径（重试或回退），并有语义测试覆盖。
- [x] API 易用性验收标准（DX Paradox）：把复杂性留在内部，把简单留给用户。（证据：`components/avatar-group/src/view.rs` 公开 API 仅需 `items`，其余为可选扩展（`max/size/aria_label/class_name/lang/dir`），不暴露 `state` 或 `Signal` 等内部状态容器；`apps/docs-app/src/pages/components/pages/display.rs` 提供最小可用示例 `r#"<AvatarGroup items=Vec::<AvatarGroupItem>::new() />"#`（1 行 Hello World，不要求接线 `ui-state-primitives/ui-headless`）并同时提供 Overflow/Size/Custom Aria+Class 高阶示例。回归覆盖：`components/avatar-group/test/semantics.rs` 的 `avatar_group_dx_api_is_simple_and_docs_offer_minimal_hello_world`，以及 `components/avatar-group/test/avatar_group/semantics.rs` 的 `avatar_group_docs_expose_hello_world_path_without_state_machine_wiring` 与 `avatar_group_docs_playgrounds_lock_state_matrix_contract_values`。）
  - 基础用法不得要求用户先理解或手动接线 `ui-state-primitives`/`ui-headless` 状态机。
  - 基础组件 Hello World 示例代码不得超过 5 行（导入与外层模板按仓库约定不计），并可直接运行。
  - 简单需求走简单 API，复杂需求再暴露高级入口：默认 props 覆盖高频场景，高级控制通过受控/扩展参数按需开启。
  - 禁止把内部状态对象作为基础必填参数暴露（例如强制 `state=...` 才能完成点击/展开等基本交互）。
  - docs-app 必须提供最小可用示例，优先展示一眼可懂的默认调用路径。
- [x] 组合型组件主 API 必须“显示优于约定”：优先使用显式组合 `<Parent><Item ... /></Parent>`。（证据：`components/avatar-group/src/view.rs` 采用类型化配置输入 `items: Vec<AvatarGroupItem>`，每个 item 的名称/资源/语义在同一 `AvatarGroupItem` 结构维度绑定，并在渲染时通过 `normalize_avatar_group_item_fields(item.name, item.src, item.alt)` 显式映射；未暴露 `labels + children`、`titles + panels` 等并行数组/并行槽位 API。`apps/docs-app/src/pages/components/pages/display.rs` 仅展示 `AvatarGroupItem` 类型化示例。回归覆盖：`components/avatar-group/test/semantics.rs` 的 `avatar_group_composition_api_uses_typed_item_specs_and_rejects_parallel_arrays`，以及 `components/avatar-group/test/avatar_group/semantics.rs` 的同名测试。）
  - 每个 item 的标题、语义与内容必须在同一 `Item` 结构维度绑定，避免索引配对式隐式约定。
  - `labels + children`、`titles + panels` 等并行数组/并行槽位写法不得作为默认或推荐 API。
  - 不引入这类语法糖：若为配置式输入，仅允许类型化 `ItemSpec`，并在内部映射为显式 `Item` 语义树。

### 3. 高级交互与物理机制（Shell/Physics）
- [x] 宏观/微观双状态机（Macro/Micro Duality）：拖拽等高频交互在 `Dragging` 期间由 `view/motion` 本地循环执行；禁止每帧穿越回 `logic.rs`，必须在结束时通过 `Action::DragEnd` 回流收敛。（证据：`avatar-group` 当前无拖拽语义与高频交互路径，`components/avatar-group/src` 不存在 `motion.rs`，`logic.rs`/`view.rs`/`styles.rs` 无 `Dragging`/`DragEnd`/`request_animation_frame`/`pointermove` 等逐帧拖拽循环标记；因此本条按 N/A 语义通过。回归覆盖：`components/avatar-group/test/semantics.rs` 的 `avatar_group_has_no_macro_micro_drag_state_machine_in_current_scope`，以及 `components/avatar-group/test/avatar_group/semantics.rs` 的 `avatar_group_does_not_define_component_motion_runtime`。）
- [x] 几何两段式渲染（Two-Pass Rendering）：`Tooltip/Popover/Menu` 等依赖 DOM 测量的组件必须走 `Intent -> Measure(view) -> Rectification(logic)`，并具备幂等收敛保护防死循环。（证据：`avatar-group` 当前不承担 `Tooltip/Popover/Menu` 这类依赖 DOM 测量与几何纠偏的交互职责，`components/avatar-group/src` 不存在 `motion.rs`，`logic.rs`/`view.rs`/`styles.rs` 无 `getBoundingClientRect`/`ResizeObserver`/`DOMRect`/`Rectification` 等几何测量与回流标记；因此本条按 N/A 语义通过。回归覆盖：`components/avatar-group/test/semantics.rs` 的 `avatar_group_has_no_two_pass_geometry_rectification_pipeline_in_current_scope`，以及 `components/avatar-group/test/avatar_group/semantics.rs` 的同名测试。）
- [x] 集合注册协议（Registration Protocol）：`Accordion/Tabs/Menu` 动态子项必须通过 `RegistrationContext` 上报 `Register/Unregister`，逻辑层维护 `items_order`，禁止依赖 `HashSet` 迭代顺序做导航。（证据：`avatar-group` 当前为静态集合渲染组件，输入为 `items: Vec<AvatarGroupItem>`，渲染顺序由 `Vec` 与 `.enumerate()` 显式决定，不承担动态子项注册/注销与 roving 导航职责；`components/avatar-group/src/logic.rs`/`view.rs`/`styles.rs` 无 `RegistrationContext`、`Register/Unregister`、`items_order`、`HashSet` 等注册协议标记，因此本条按 N/A 语义通过。回归覆盖：`components/avatar-group/test/semantics.rs` 的 `avatar_group_has_no_dynamic_registration_protocol_in_current_scope`，以及 `components/avatar-group/test/avatar_group/semantics.rs` 的同名测试。）
- [x] 插槽投影策略（Slot Projection）：容器组件明确 `Lazy/KeepAlive/Eager`；`KeepAlive` 隐藏时必须通过生命周期通知（如 `NotifyHidden`）暂停轮询/动画等高耗能副作用。（证据：`avatar-group` 当前不是容器投影组件，采用直接渲染路径（`items` + `.take(state.visible_count)` + `Show` 溢出标记），不提供 `Lazy/KeepAlive/Eager` 投影模式，也无 `NotifyHidden` 生命周期通知与隐藏态副作用治理需求；`components/avatar-group/src/logic.rs`/`view.rs`/`styles.rs` 无 `KeepAlive`、`NotifyHidden`、`set_interval`、`request_animation_frame` 等高耗能循环控制标记，因此本条按 N/A 语义通过。回归覆盖：`components/avatar-group/test/semantics.rs` 的 `avatar_group_has_no_slot_projection_lifecycle_protocol_in_current_scope`，以及 `components/avatar-group/test/avatar_group/semantics.rs` 的同名测试。）
- [x] 环境订阅流（Env Streams）：`Resize/Theme/Intersection` 等环境变化在 `view.rs` 采样、防抖后转化为高层语义 `Action`（如 `BreakpointChanged`）推送到 `logic`；禁止原始事件洪泛。（证据：`avatar-group` 当前为静态 props 驱动渲染组件，`view.rs` 仅通过 `logic::normalize_avatar_group_input` 与 `logic::resolve_avatar_group_render_state` 派生输出，不订阅 `Resize/Theme/Intersection` 环境流；`components/avatar-group/src/logic.rs`/`view.rs`/`styles.rs` 无 `ResizeObserver`、`IntersectionObserver`、`BreakpointChanged`、`debounce`/`throttle` 等环境采样与回流标记，因此本条按 N/A 语义通过。回归覆盖：`components/avatar-group/test/semantics.rs` 的 `avatar_group_has_no_env_stream_subscription_pipeline_in_current_scope`，以及 `components/avatar-group/test/avatar_group/semantics.rs` 的同名测试。）
- [x] 事件光锥（Event Light Cone）：`Table/Grid` 等大型集合批量操作必须走 `Context Bus + Selector` 与状态压缩表达（如 `SelectionState::All`），禁止 O(N) 级向下 prop drilling。（证据：`avatar-group` 当前不是 `Table/Grid` 这类大型集合批量交互组件，渲染路径为 `items: Vec<AvatarGroupItem>` + `.take(state.visible_count)` + `.enumerate()` 的直接列表输出，不承担批量选择压缩状态与总线分发职责；`components/avatar-group/src/logic.rs`/`view.rs`/`styles.rs` 无 `ContextBus`、`Selector`、`SelectionState::All`、`select_all` 等事件光锥协议标记，因此本条按 N/A 语义通过。回归覆盖：`components/avatar-group/test/semantics.rs` 的 `avatar_group_has_no_event_light_cone_batch_protocol_in_current_scope`，以及 `components/avatar-group/test/avatar_group/semantics.rs` 的同名测试。）
- [x] 统一因果总线（Causality Bus）：复杂派生总线操作必须支持透传 `TraceId`，确保“用户触发 -> 派生命令 -> 总线广播 -> 订阅者”因果链不断裂。（证据：`avatar-group` 当前不含复杂派生总线流程，渲染由本地 props 归一与状态派生直接完成（`logic::normalize_avatar_group_input` + `logic::resolve_avatar_group_render_state`），不存在跨订阅者广播链路；`components/avatar-group/src/logic.rs`/`view.rs`/`styles.rs` 无 `TraceId`、`CausalityBus`、`publish/broadcast/subscribe` 等因果总线标记，因此本条按 N/A 语义通过。回归覆盖：`components/avatar-group/test/semantics.rs` 的 `avatar_group_has_no_causality_bus_trace_pipeline_in_current_scope`，以及 `components/avatar-group/test/avatar_group/semantics.rs` 的同名测试。）
- [x] 存在 A11y 实现、国际化与本地化实现（至少具备接入点，不硬编码用户可见文本）。（证据：`components/avatar-group/src/view.rs` 通过 `ui_headless::labeled_group_attrs` 挂载 `role/aria-label/lang/dir` 语义，并消费 `ui_headless::i18n::CommonStrings` 的 `avatar_group_aria_label` 与 `avatar_group_overflow_aria_label_suffix`，未硬编码用户可见文案；公开 API 透传 `lang/dir`（`lang: Option<String>`, `dir: Option<A11yDirection>`）。共享 A11y 工具来源为 `crates/ui-headless/src/a11y.rs` 的 `labeled_group_attrs/locale_attrs`。该组件为非交互容器，键盘路径子项按 N/A 语义成立（无 `on:keydown`/`tabindex` 等交互焦点模型）。回归覆盖：`components/avatar-group/test/semantics.rs` 的 `avatar_group_a11y_i18n_l10n_contract_is_headless_driven_and_no_view_hardcoded_copy_local`，以及 `components/avatar-group/test/avatar_group/semantics.rs` 的 `avatar_group_a11y_i18n_l10n_contract_is_headless_driven_and_no_view_hardcoded_copy`。）
  - 交互元素必须具备可验证语义：`role`/`aria-*`/键盘可达路径完整，且和 headless 契约一致。
  - 用户可见文本来源必须可覆盖：优先 props，其次应用注入（`UiRoot`/i18n bundle），最后组件兜底文案；禁止把业务可见文案硬编码在 `view.rs`。
  - 组件需透传或消费 `lang` / `dir`（LTR/RTL）上下文，不得假设单语言单方向。
  - 共享 A11y 工具优先来自 `crates/ui-headless/src/a11y.rs`，组件层不重复发明同名语义工具。
- [x] 状态可观测、可检索、可验证：使用稳定 `data-*` 与 `aria-*` 标记表达状态和来源。（证据：`components/avatar-group/src/view.rs` 通过稳定语义标记暴露关键状态与来源（`data-state`、`data-empty`、`data-has-overflow`、`data-aria-label-source`、`data-class-source`、`role`、`aria-label`），`crates/ui-state-primitives/src/avatar_group.rs` 以枚举 `as_str()` 输出封闭值集合（`stable/overflow/empty/default/custom`），`components/avatar-group/src/styles.rs` 仅基于 `data-*` 状态源选择器分支（无 `:nth-child`/深层级猜测）。回归覆盖：`components/avatar-group/test/semantics.rs` 的 `avatar_group_state_markers_are_observable_and_closed_set_contracts_local`，以及 `components/avatar-group/test/avatar_group/semantics.rs` 的 `avatar_group_state_markers_are_observable_and_closed_set_contracts` 与 `avatar_group_styles_depend_on_explicit_state_markers_not_dom_guessing`。）
  - 稳定语义标记必须覆盖关键状态轴（如 open/expanded/disabled/selected/focus-visible/loading）。
  - 状态来源必须可区分（受控/非受控、默认值/外部值、交互来源），通过稳定 marker 暴露而不是隐式推断。
  - 自动化选择器优先基于语义标记，不依赖 DOM 顺序、层级深度或临时 class 名。
  - 标记值应为封闭集合（可枚举），避免自由文本导致契约漂移。
- [x] 样式依赖显式状态（`data-*`/class），而非脆弱 DOM 结构猜测。（证据：`components/avatar-group/src/styles.rs` 的状态分支基于稳定语义标记（如 `.ui-avatar-group[data-state=\"stable|overflow|empty\"]`、`.ui-avatar-group[data-aria-label-source=\"default|custom\"]`、`.ui-avatar-group[data-class-source=\"custom\"]`），未使用 `:nth-child` 或深层级结构猜测；`components/avatar-group/src/view.rs` 运行时通过 `data-state/data-aria-label-source/data-class-source` 传递状态且无 `style=` 业务内联样式；视觉状态切换由上述语义标记直接驱动，不依赖“节点是否恰好存在”。回归覆盖：`components/avatar-group/test/semantics.rs` 的 `avatar_group_state_markers_are_observable_and_closed_set_contracts_local`，以及 `components/avatar-group/test/avatar_group/semantics.rs` 的 `avatar_group_styles_depend_on_explicit_state_markers_not_dom_guessing` 与 `avatar_group_state_markers_are_observable_and_closed_set_contracts`。）
  - `styles.rs` 中状态分支选择器必须基于 `data-*`/`aria-*`/稳定 class，禁止用 `:nth-child`、深层级选择器猜测状态。
  - 运行时样式仅允许传递必要 CSS 变量（custom properties）；禁止把业务样式逻辑塞进 inline style。
  - 视觉状态切换必须可由语义标记直接解释，不能依赖“某节点是否恰好存在”。
- [x] 测试验证“语义契约”而不只验证视觉快照。（证据：语义契约回归已覆盖关键状态与来源标记（`components/avatar-group/test/semantics.rs` 的 `avatar_group_state_markers_are_observable_and_closed_set_contracts_local`；`components/avatar-group/test/avatar_group/semantics.rs` 的 `avatar_group_state_markers_are_observable_and_closed_set_contracts`、`avatar_group_a11y_i18n_l10n_contract_is_headless_driven_and_no_view_hardcoded_copy`）；测试矩阵按适用范围覆盖为“有则验证、无则显式 N/A”（受控/非受控由 `avatar_group_has_no_controllable_state_axis_and_no_half_controlled_api` 证明当前无该状态轴，异步/loading 由 `avatar_group_has_no_async_loading_protocol_and_keeps_sync_render_contract` 证明 N/A，动态交互分支由 `avatar_group_has_no_dynamic_registration_protocol_in_current_scope` 等证明 N/A）；视觉快照未作为主契约，`avatar_group_semantics_suite_prioritizes_contract_assertions_over_snapshots` 显式禁止 snapshot 断言令牌，仅保留语义断言。）
  - 至少存在语义测试覆盖关键状态与交互路径（role/aria/data-state/source markers）。
  - 测试矩阵必须覆盖关键分支：受控/非受控、disabled、键盘路径、指针路径、SSR/wasm 差异（按适用范围）。
  - 视觉快照只能作为补充，不得替代语义契约断言。
- [x] 组件文件职责正确：`mod.rs`（导出边界）、`logic.rs`（归一/派生/来源标记）、`styles.rs`（静态 token-first CSS）、`view.rs`（Leptos 结构 + headless 挂载）、`motion.rs`（动效契约 + attach）。（证据：`components/avatar-group/src/mod.rs` 仅保留最小导出边界（`mod logic; pub mod styles; mod view; pub use view::{AvatarGroup, AvatarGroupItem};`）且未外泄实现细节；`components/avatar-group/src/logic.rs` 仅做输入归一/状态派生/来源标记，无 DOM 与视图细节；`components/avatar-group/src/styles.rs` 为 token-first 静态 CSS（`var(--ui-*)`）且不含业务文案；`components/avatar-group/src/view.rs` 负责结构渲染与 `ui_headless::labeled_group_attrs` 语义挂载，并消费 `logic` 输出而不隐藏关键状态决策。`motion.rs` 在该组件当前为 N/A（无动效契约需求，`components/avatar-group/src/motion.rs` 不存在），并由语义回归守护。回归覆盖：`components/avatar-group/test/semantics.rs` 的 `avatar_group_module_boundary_is_minimal_and_wires_component_semantics_tests`、`avatar_group_logic_view_styles_follow_ui_components_layered_responsibilities`，以及 `components/avatar-group/test/avatar_group/semantics.rs` 的 `avatar_group_component_files_follow_layered_responsibilities`、`avatar_group_does_not_define_component_motion_runtime`。）
  - `mod.rs` 只维护最小稳定导出面与 feature gate，不承载实现细节。
  - `logic.rs` 只做输入归一、状态派生、来源标记；禁止 DOM 操作和样式细节分支。
  - `styles.rs` 只包含 token-first 静态 CSS；禁止硬编码主题常量与业务语义文案。
  - `view.rs` 只做结构渲染与 headless 契约挂载；禁止隐藏关键状态决策。
  - `motion.rs` 只做组件语义到动效契约映射与 attach；禁止在组件内重写通用动效引擎。
- [x] `spec.rs` 只用于少数复杂组件（如 button），避免泛滥。（证据：`components/avatar-group/src` 当前无 `spec.rs`，且 `components/avatar-group/src/mod.rs` 仅保留 `logic/styles/view` 最小导出边界，不存在 `mod spec`/`pub mod spec`/`pub use spec::*`；回归覆盖：`components/avatar-group/test/avatar_group/semantics.rs` 的 `avatar_group_does_not_introduce_spec_rs_and_keeps_lightweight_exports`。该组件当前无稳定外部 Schema 固化需求，因此“新增 `spec.rs` 时需契约测试与版本演进说明”子条目按 N/A 语义成立。）
  - 仅当组件存在稳定外部规范/Schema 契约或复杂配置固化需求时才引入 `spec.rs`。
  - 简单组件不得为了“形式统一”新增 `spec.rs`；说明文档应留在 `check2.md`/组件文档。
  - 新增 `spec.rs` 必须同步给出契约测试与版本演进说明。
- [x] 组件层遵循 token-first 静态样式契约：样式通过 `styles.rs` 聚合注入；运行时仅传必要 CSS 变量；不把 Utility-First/CSS-in-Rust 当组件库默认范式。（证据：`components/avatar-group/src/styles.rs` 样式规则集中管理并以 `var(--ui-*)` 消费主题变量；`crates/ui/src/css.rs` 聚合组件 CSS，`crates/ui/src/root.rs` 通过 `UiRoot` 的 `crate::css::push_components_css(&mut out)` 注入；`components/avatar-group/src/view.rs`/`logic.rs` 无 `style=` 业务内联样式。契约回归由 `components/avatar-group/test/avatar_group/semantics.rs` 的 `avatar_group_token_first_static_styles_contract_is_enforced_via_ui_root_css_pipeline` 覆盖，并显式禁止 `@apply`/`tailwind`/`styled(` /`emotion`/`stylex` 等 Utility-First/CSS-in-Rust 反向污染标记。）
  - 样式规则统一落在 `styles.rs`，由 `crates/ui/src/css.rs` 聚合并通过 `UiRoot` 注入。
  - 颜色/间距/圆角/阴影等视觉值必须来自 `var(--ui-*)`，禁止组件私有 token 体系。
  - Utility-First 仅作为 `apps/*` 应用层布局手段，不得反向污染组件库契约。
  - CSS-in-Rust 仅在有明确类型安全与构建成本净收益时作为例外采用。
- [x] 默认主题美学质量达标（Visual Desire）：以 HeroUI 现代审美为学习对标，默认主题不仅“可用”，还必须“第一眼可信”。（证据：默认主题视觉基线已落地到 docs-app 的 `apps/docs-app/src/pages/components/pages/theme_visual_baseline.rs`，并由 `e2e/tests/docs_app_theme_visual_baseline.spec.mjs` 做页面/关键组件（Button/Input/Overlay）截图回归（`docs-app-theme-visual-baseline-page/button/input/overlay.png`）；`components/avatar-group/test/avatar_group/semantics.rs` 的 `avatar_group_visual_desire_gate_reuses_theme_visual_baseline_and_heroui_contracts` 同步约束基线页面、e2e 视觉回归与 `docs/spec/heroui-parameter-design-strategy.md` 的 HeroUI 对齐文档，确保目标是视觉语言与体验质量对齐，而非 API 表层复制。）
  - 默认主题需通过基础美学清单：信息层级清晰（字重/字号/间距）、对比与层次自然、交互反馈明确（hover/active/focus）。
  - docs-app 必须提供默认主题基线页面与截图基线，关键组件（Button/Input/Overlay）纳入视觉回归对比。
  - 禁止“可访问但粗糙”的最低可用心态：视觉退化（类似旧式 Bootstrap 观感）视为质量回归。
  - HeroUI 对标以“视觉语言与体验质量”对齐为目标，不做无差别 API 表层复制。
- [x] Tree Shaking 是一等能力：package 模式支持组件级 feature；source 模式天然裁剪；样式层同步裁剪，禁止无条件聚合全部 CSS，禁止破坏 DCE/LTO 的全量中央注册表。（证据：`crates/ui/Cargo.toml` 显式声明 `component-avatar = ["dep:ui-avatar"]` 与 `component-avatar_group = ["component-avatar", "dep:ui-avatar-group"]`；`crates/ui/src/lib.rs` 与 `crates/ui/src/css.rs` 对 avatar/avatar-group 的导出与 CSS 聚合均受 `#[cfg(feature = "...")]` 条件门控；`components/avatar-group/test/avatar_group/semantics.rs` 的 `avatar_group_tree_shaking_contract_enforces_source_mode_reachability_boundaries` 持续断言 source-mode 可达性边界与“无全量注册表”约束；本地复验 `cargo tree -e features -p ui --no-default-features --features component-accordion,inject-css` 与 `cargo tree -e features -i ui -p web-demo` 均通过，后者仅见 `web-demo-components` 路径且未出现 `all-components`；CI 侧最小特性 wasm 编译与体积预算阻断由 `scripts/check-ui-tree-shaking.sh` + `scripts/tree_shaking_budget.env` 落地。）
  - package 模式必须有组件级 feature（如 `component-accordion`）；未启用组件不得进入编译与链接路径。
  - `lib.rs` 与 `css.rs` 必须按 feature 条件导出/聚合，禁止无条件引用所有组件模块和 CSS 常量。
  - source 模式下仅引入需要的组件源码，不通过中央注册表维持全组件可达。
  - 任意“全量组件映射表/注册表”若导致不可达代码变可达，直接判不通过。
  - 验证命令（特性树）：`cargo tree -e features -p ui --no-default-features --features component-accordion,inject-css`，确认仅启用目标组件特性链。
  - 验证命令（反向依赖）：`cargo tree -e features -i ui -p web-demo`，检查是否被 `all-components` 或隐式特性全量拉起。
  - CI 检查（最小特性编译）：新增任务仅开启目标最小特性（示例：`cargo check -p ui --target wasm32-unknown-unknown --no-default-features --features component-accordion,inject-css`）。
  - CI 检查（体积预算）：对“最小特性构建产物”设定预算并阻断回归（可用固定阈值，如 `< 50KB`，或基于仓库基线的相对阈值）；不得只做编译通过而不做体积约束。
- [x] 类型系统 + 语义标记共同提供机器可读状态；关键输入空间受类型约束。（证据：`crates/ui-state-primitives/src/avatar_group.rs` 以 `AvatarGroupStateInput`/`AvatarGroupRenderState` + `AvatarGroupVisualState`/`AvatarGroupAriaLabelSource`/`AvatarGroupClassSource` 建模关键状态轴，并通过 `as_str()` 输出闭合集；`components/avatar-group/src/logic.rs` 仅消费 `ui_state_primitives::avatar_group::resolve_render_state` 做统一归一化，不在组件层重建状态机；`components/avatar-group/src/view.rs` 通过 `data-state`/`data-empty`/`data-has-overflow`/`data-aria-label-source`/`data-class-source` 暴露稳定机器可读语义标记。回归闭环：`components/avatar-group/test/semantics.rs` 的 `avatar_group_discrete_state_axes_are_type_safe_enums`、`avatar_group_state_markers_are_observable_and_closed_set_contracts_local`，`crates/ui-state-primitives/src/test/avatar_group.rs` 的 `resolve_render_state_maps_discrete_status_and_sources_to_enums`，以及 `components/avatar-group/test/avatar_group/semantics.rs` 的 `avatar_group_machine_readable_contract_uses_typed_inputs_and_semantic_markers`，可直接定位契约破坏点。）
  - 离散输入与状态轴必须优先使用 `enum`/新类型建模，避免字符串协议与布尔爆炸。
  - 无效状态要么在类型层不可表达，要么在 `logic.rs` 被统一归一化并可测试。
  - 关键状态必须通过稳定语义标记对外可读，供测试与 Agent 自动化消费。
  - 编译器与测试反馈应能直接定位状态契约破坏点，形成可持续闭环。

### 4. DOM/环境边界治理
- [x] 焦点全局栈（Focus Stack & GC）：层叠 `Overlay` 禁止私存 `NodeRef` 作为恢复目标；必须依赖全局 Focus Manager（如 `FallbackTo/Selector`）防止焦点坠落到 `document.body`。（证据：`avatar-group` 当前是静态头像集合渲染组件，不承载 overlay/focus-trap 恢复职责（`components/avatar-group/src` 无 `motion.rs`，`logic.rs`/`view.rs`/`styles.rs` 无 `NodeRef`/`document.body`/`use_focus_trap`/`overlay_stack` 令牌），因此本条按 N/A 语义通过；回归覆盖：`components/avatar-group/test/semantics.rs` 的 `avatar_group_has_no_overlay_focus_stack_gc_contract_in_current_scope` 与 `components/avatar-group/test/avatar_group/semantics.rs` 的同名测试。焦点栈与恢复原语固定落在 `ui-headless`（`crates/ui-headless/src/overlay_stack.rs` 的 `provide_overlay_stack`/`use_overlay_stack_registration`，`crates/ui-headless/src/focus_trap.rs` 的 `FocusTrapOptions`/`use_focus_trap`）。）
- [x] 受控外交特区（Escape Hatches）：集成 ECharts/Map 等命令式第三方库时必须处于 `Foreign Zone`（`YieldControl/CleanupForeign`）；第三方实例不得暴露为组件公共 API 或反向污染状态机。（证据：`avatar-group` 当前不集成 ECharts/Map 等命令式第三方实例，`components/avatar-group/src/logic.rs`/`view.rs`/`styles.rs` 无 `ECharts/Mapbox/Leaflet/ForeignZone/YieldControl/CleanupForeign` 等令牌，且 `AvatarGroup` 公共签名未暴露第三方实例句柄类型（无 `chart_instance/map_instance/web_sys::/wasm_bindgen::JsValue`）；因此本条按 N/A 语义通过。回归覆盖：`components/avatar-group/test/semantics.rs` 与 `components/avatar-group/test/avatar_group/semantics.rs` 的 `avatar_group_has_no_foreign_zone_escape_hatch_integration_in_current_scope`。治理基线文档存在：`docs/spec/foreign_zone_escape_hatches.md`。）
- [x] SSR 时空断裂治理（Hydration Discontinuity）：逻辑初始化禁止依赖 `now()` 或原生随机 UUID；必须通过 `IdProvider` 注入确定性种子，确保 SSR/Hydration 间 ID 稳定。（证据：`avatar-group` 当前不分配运行时 ID，`components/avatar-group/src/mod.rs`/`logic.rs`/`view.rs`/`styles.rs` 无 `now()/Date::now/SystemTime::now/Uuid/rand` 等熵源，因此该轴按 N/A 语义通过；确定性 ID 注入入口保持在 `UiRoot`（`crates/ui/src/root.rs` 的 `#[prop(optional, default = 1)] id_seed: u64` + `provide_ui_id_provider(id_seed);`），底层提供者在 `crates/ui-headless/src/id_provider.rs`。回归覆盖：`components/avatar-group/test/semantics.rs` 与 `components/avatar-group/test/avatar_group/semantics.rs` 的 `avatar_group_hydration_discontinuity_contract_is_explicitly_na_without_time_or_random_id_init`。）
- [x] SSR 与跨平台检查：覆盖 web/ssr/wasm 分支，不破坏 non-wasm 编译路径。（证据：`scripts/check-ui-platforms.sh` 已纳入 `avatar-group` 的 compile-only 路径（native：`cargo check -p ui --no-default-features --features component-avatar_group,inject-css`；wasm：`cargo check -p ui --target wasm32-unknown-unknown --no-default-features --features component-avatar_group,inject-css`），并同时覆盖 `ui-headless` 的 `ssr` 与 `web` 检查；`components/avatar-group/src/mod.rs`/`logic.rs`/`view.rs`/`styles.rs` 无 `web_sys/window./document.` 浏览器对象引用，non-wasm 路径保持洁净；平台分支治理通过显式 cfg/feature（`crates/ui-headless/Cargo.toml` 的 `web/ssr` 切分 + `crates/ui-headless/src/lib.rs` 互斥 `compile_error!`，`crates/ui/Cargo.toml` 的 wasm target 依赖门控）达成。回归覆盖：`components/avatar-group/test/semantics.rs` 与 `components/avatar-group/test/avatar_group/semantics.rs` 的 `avatar_group_ssr_cross_platform_contract_uses_explicit_cfg_and_keeps_non_wasm_clean`。）
  - 至少包含 compile-only 证据：web（wasm32）、ssr（native）、默认本地构建三条路径。
  - 平台分支差异必须显式 `cfg` 或 feature 管理，禁止依赖运行时偶然行为。
  - non-wasm 路径禁止引用 `web-sys`/浏览器对象。
- [x] `ui-headless` web/ssr feature 互斥受 `compile_error!` 保护（`crates/ui-headless/src/lib.rs`）。（证据：`crates/ui-headless/src/lib.rs` 含 `#[cfg(all(feature = "web", feature = "ssr"))] compile_error!(...)` 互斥守卫；`scripts/check-ui-platforms.sh` 同时覆盖 `cargo check -p ui-headless --no-default-features --features ssr` 与 `cargo check -p ui-headless --target wasm32-unknown-unknown --no-default-features --features web` 两条可编译路径，并以 `cargo check -p ui-headless --no-default-features --features web,ssr` 做负向断言（命令成功即失败，且日志必须含 `mutually exclusive`）；回归覆盖：`components/avatar-group/test/semantics.rs` 与 `components/avatar-group/test/avatar_group/semantics.rs` 的 `avatar_group_ui_headless_web_ssr_feature_mutex_contract_is_enforced`。）
  - 组件依赖 `ui-headless` 能力时，不得破坏其 web/ssr 互斥约束。
  - 组件若新增 headless 功能接入，需验证两条 feature 路径都可编译。
  - 发现“同时启用 web+ssr 仍可过编译”视为契约回归。
- [x] `ui-motion` 非 wasm 提供 no-op/stub（`crates/ui-motion/src/lib.rs`），保证 SSR/tooling 可编译。（证据：`crates/ui-motion/src/lib.rs` 在 `#[cfg(not(target_arch = "wasm32"))]` 下提供 `web::prefers_reduced_motion() -> true` 与 `web::animate(_element: &(), ..)` no-op stub；`crates/ui-motion/tests/non_wasm_stub.rs` 以 `#![cfg(not(target_arch = "wasm32"))]` 覆盖 `non_wasm_web_backend_prefers_reduced_motion` 与 `non_wasm_web_backend_animate_is_safe_noop`，验证 non-wasm 路径可预测且不 panic；`scripts/check-ui-platforms.sh` 同时执行 `cargo check -p ui-motion`、`cargo check -p ui-motion --target wasm32-unknown-unknown` 与 `cargo test -p ui-motion --test non_wasm_stub`，确保 SSR/tooling 与 wasm 双路径可编译。`avatar-group` 当前无 `motion.rs`（`components/avatar-group/src` 不存在 `motion.rs`，`view.rs`/`logic.rs` 无 `ui_motion::`/`attach_motion`），因此组件层不会假设动画句柄必然存在。回归覆盖：`components/avatar-group/test/semantics.rs` 与 `components/avatar-group/test/avatar_group/semantics.rs` 的 `avatar_group_ui_motion_non_wasm_noop_stub_contract_is_enforced`。）
  - `motion.rs` 调用必须可在 non-wasm 下安全降级，不触发 panic。
  - 组件不得假设动画句柄一定存在；no-op 分支行为需可预测。
  - toolchain 场景（测试/文档/静态分析）不得因 motion 依赖阻塞编译。
- [x] 组件实现覆盖 `reduced-motion` / SSR / wasm 分支。（证据：`avatar-group` 当前无组件级 motion contract（`components/avatar-group/src` 不存在 `motion.rs`，`view.rs`/`logic.rs`/`styles.rs` 无 `ui_motion::`/`attach_motion`），因此 `reduced-motion` 轴按 N/A 语义通过且不会产生“动画未降级”风险；通用降级能力由 `crates/ui-motion/src/lib.rs` 的 non-wasm `web::prefers_reduced_motion() -> true` 与 `web::animate(..)` no-op stub 提供，并由 `crates/ui-motion/tests/non_wasm_stub.rs` 与 `crates/ui-motion/tests/spring.rs` 的 reduced-motion 用例回归。SSR/wasm 一致性由 `components/avatar-group/test/semantics.rs` 与 `components/avatar-group/test/avatar_group/semantics.rs` 的 `avatar_group_ssr_cross_platform_contract_uses_explicit_cfg_and_keeps_non_wasm_clean`（跨平台编译与 non-wasm 洁净）+ `avatar_group_reduced_motion_ssr_wasm_branch_contract_is_explicitly_na_and_consistent`（语义契约不分裂）共同锁定；`scripts/check-ui-platforms.sh` 也持续执行 reduced-motion/ssr/wasm 合同回归与 compile-only 路径。）
  - `reduced-motion` 下动画应跳过或降级为最小必要反馈。
  - SSR 输出必须与客户端 hydration 兼容，避免首帧语义错位。
  - wasm 分支允许增强交互，但语义契约不得与 SSR 分支分裂。
- [x] 性能治理：关键路径有预算（首次渲染/更新耗时/内存），回归可检测、可归因、可阻断。（证据：`apps/docs-app/src/pages/components/shell.rs` 已为 `avatar-group` 声明预算（`"avatar-group" => UiPerfBudget { max_mount_ms: 24.0, max_update_ms: Some(8.0), max_heap_kb: Some(384.0) }`），并通过 `apps/docs-app/src/perf_probe.rs` 稳定输出 `data-perf-mount-ms/data-perf-budget-ms/data-perf-budget-update-ms/data-perf-budget-heap-kb/data-perf-violation/data-perf-observability`，`e2e/tests/docs_app_components_coverage.spec.mjs` 对预算标记做回归断言；`scripts/check-ui-performance.sh` 已纳入 `avatar_group_performance_governance_budget_is_defined_and_blocking` 作为阻断门禁，并同时绑定 Button/Input 预算与 `perf_render_count_follow_up_is_tracked_in_plan`。关键路径归因保持可读：`components/avatar-group/src/view.rs` 以 `logic::resolve_avatar_group_render_state(...)` 派生状态并通过 `data-state/data-visible-count/data-overflow-count` 暴露渲染语义，未引入高频循环。基础组件渲染次数预算为 `1`（Button/Input）继续受统一门禁约束；`render_count` 自动化暂未具备通用基建，当前采用可重复等价证据并在 `docs/plan/TODO.md` 持续跟踪（“建立 `render_count` 自动化回归（Button/Input/Accordion/DropZone），替换当前 mount-only 等价证据”）。回归覆盖：`components/avatar-group/test/semantics.rs` 与 `components/avatar-group/test/avatar_group/semantics.rs` 的 `avatar_group_performance_governance_budget_is_defined_and_blocking`。）
  - 关键交互组件需定义最小预算项（首渲染、关键更新、内存/分配趋势）。
  - 回归检测至少具备可重复基线与失败阈值，不靠主观“感觉变慢”。
  - 性能问题需可归因到状态、渲染、样式或动效路径之一。
  - 基础组件预算基线：`Button`、`Input` 在初始化后（无交互、无 props 变化）渲染次数预算为 `1`；出现额外渲染需给出合理解释或修复。
  - 测试要求：在 `components/*/test/**` 增加 `render_count` 类回归测试（测试框架支持时必须启用）；至少覆盖基础组件与本次改动组件。
  - 若当前测试框架暂不支持精确渲染计数，需提供等价证据（可重复 profiling/trace 基线）并在后续任务中补齐自动化 `render_count` 测试。
- [x] `view!` 宏复杂度受控：单个 `view!` 块不得承载超长深嵌套结构；复杂布局按语义分块，避免一次性宏展开导致编译与 wasm 体积劣化。（证据：`components/avatar-group/src/view.rs` 当前为小布局：根渲染 + item 子片段共 2 个 `view!` 块（外层容器与 item 映射片段），`view.rs` 文件行数受控；结构分块沿语义维度拆为 `group root / item / overflow`（`data-slot=\"avatar-group\"`、`data-slot=\"avatar-group-item\"`、`data-slot=\"avatar-group-overflow\"`），未出现巨型单块宏或深层重复片段堆叠。回归覆盖：`components/avatar-group/test/semantics.rs` 与 `components/avatar-group/test/avatar_group/semantics.rs` 的 `avatar_group_view_macro_complexity_is_bounded_and_semantically_split_for_items`。）
  - 复杂结构按语义子块拆分（header/body/item 等），避免巨型单块 `view!`。
  - `view.rs` 中若出现多层嵌套重复片段，应优先提取局部渲染函数。
  - 编译时间/产物体积异常增长时，优先排查宏展开体量。
- [x] 函数式拆分优先：不涉及复杂状态与生命周期管理的 UI 片段，优先拆为普通 Rust 函数（返回 `impl IntoView`/`View`），而不是新增 `#[component]`。（证据：`components/avatar-group/src/view.rs` 已将 item 子片段下沉为普通函数 `fn render_avatar_group_item(index: usize, item: AvatarGroupItem, size: AvatarSize) -> impl IntoView`，主组件 `AvatarGroup` 仅保留 1 个 `#[component]` 入口并通过 `.map(|(index, item)| render_avatar_group_item(index, item, state.size))` 复用渲染，未新增局部 `#[component]` 抽象噪音；语义标记 `data-slot=\"avatar-group-item\"` 与测试定位保持稳定。回归覆盖：`components/avatar-group/test/semantics.rs` 的 `avatar_group_prefers_functional_fragment_split_over_extra_component_defs_local` 与 `components/avatar-group/test/avatar_group/semantics.rs` 的 `avatar_group_prefers_functional_fragment_split_over_extra_component_defs`。）
  - 纯静态或轻逻辑片段优先函数化；仅在需要独立 props 语义时升级为组件。
  - 禁止把所有局部片段都升格为 `#[component]` 导致抽象噪音。
  - 拆分后语义标记与测试定位仍需稳定。
- [x] 静态片段常量化：复杂 SVG、页脚、长说明文本等纯静态内容优先常量化/模板化，减少重复 `view!` 渲染指令生成。（证据：`components/avatar-group/src/view.rs` 已将溢出文案静态前缀提取为常量 `OVERFLOW_VISIBLE_LABEL_PREFIX`，并通过单一路径函数 `render_avatar_group_overflow_label` 生成显示文案，替代内联 `format!(\"+{}\", ...)`；静态模板变更路径集中在同一文件顶部，不再散落。当前组件无复杂 SVG/页脚/长说明文本静态块，按适用范围为 N/A；同时保持可访问语义挂载不变（`aria-label=overflow_aria_label.clone()`）。回归覆盖：`components/avatar-group/test/semantics.rs` 的 `avatar_group_static_fragment_constantization_is_centralized_and_scope_bounded_local` 与 `components/avatar-group/test/avatar_group/semantics.rs` 的 `avatar_group_static_fragment_constantization_is_centralized_and_scope_bounded`。）
  - 可判定为纯静态的片段应避免重复动态构造。
  - 常量化后仍需维持可访问语义（title/aria-label/role 等）。
  - 静态资源变更路径要清晰，避免散落在多个 `view!` 片段中。
- [x] `inner_html` 使用约束：仅允许注入受信任静态常量，禁止拼接用户输入；使用处必须补充语义与安全回归测试。（证据：`avatar-group` 当前无 `inner_html` 使用场景，按适用范围为 N/A；`components/avatar-group/src/logic.rs`、`components/avatar-group/src/view.rs`、`components/avatar-group/src/styles.rs` 与 `AvatarGroup` 签名均无 `inner_html=`/`set_inner_html`/`dangerouslySetInnerHTML`/`<script`/`javascript:`/`onerror=` 等注入入口。语义与安全回归已补：`components/avatar-group/test/semantics.rs` 的 `avatar_group_inner_html_contract_is_explicitly_na_and_user_input_injection_free_local`，以及 `components/avatar-group/test/avatar_group/semantics.rs` 的 `avatar_group_inner_html_contract_is_explicitly_na_and_user_input_injection_free`。）
  - 仅允许编译期常量或明确白名单内容进入 `inner_html`。
  - 严禁直接或间接注入用户输入、远端返回或未清洗模板字符串。
  - 使用 `inner_html` 的节点必须补语义测试与安全回归说明。
- [x] WASM 调试要求：关键状态可追踪（来源/时间/前后值），关键交互可回放，开发模式有可视化入口，调试能力通过 feature 隔离不污染产物。（证据：`avatar-group` 当前无交互事件状态机与回放链路（静态渲染路径：`items: Vec<AvatarGroupItem>` + `.enumerate()`），该项按适用范围为 N/A；`components/avatar-group/src/logic.rs`/`view.rs`/`styles.rs` 与 `AvatarGroup` 公共签名无 `tracing::`/`trace!`/`debug!`/`record_event`/`replay`/`timeline`/`cfg(debug_assertions)` 等调试注入令牌。特性隔离与产物洁净：`components/avatar-group/Cargo.toml` 为 `default = []`，且无 `wasm-debug`/`avatar-group-wasm-debug` 特性暴露；`crates/ui/Cargo.toml` 存在其它组件调试特性（如 `accordion-wasm-debug`）但不包含 `avatar-group` 调试特性，避免污染该组件公共 API 与生产包体。回归覆盖：`components/avatar-group/test/semantics.rs` 的 `avatar_group_wasm_debug_contract_is_explicitly_na_and_feature_isolation_clean_local`，以及 `components/avatar-group/test/avatar_group/semantics.rs` 的 `avatar_group_wasm_debug_contract_is_explicitly_na_and_feature_isolation_clean`。）
  - 开发模式下至少能追踪关键状态变更来源与前后值。
  - 关键交互链路应支持最小可复现记录（事件顺序/状态转移）。
  - 调试开关默认不进入生产包体与公共 API。
- [x] DX 要求：样式热重载优先无需重编 wasm；组件热开发尽量保持上下文；提供可选状态保留；有 Workbench 隔离画布。（证据：开发脚本 `scripts/dev-docs-app.sh` 与 `scripts/dev-web-demo.sh` 均以 `trunk serve` 启动（`exec trunk serve --open true \"$@\"`），提供前端热开发反馈路径；`apps/docs-app/src/pages/components/pages/display.rs` 为 `avatar-group` 提供 `Playground` 隔离演练入口（`Hello World`、`Overflow Stack`、`Custom Aria + Class`），满足 demo/workbench 要求；`apps/docs-app/src/playground.rs` 提供 scoped CSS 实时编辑与恢复（`compose_scoped_css`、`data-playground-scope`、`test_css` + `"Restore original CSS"`），常见样式调整不需完整 wasm 重编。`avatar-group` 本体是无交互状态机的展示组件（`components/avatar-group/src/view.rs` 无 `signal`/事件处理器），其“可选状态保留”轴按 N/A 语义成立，状态上下文由 docs Playground 承担。回归覆盖：`components/avatar-group/test/semantics.rs` 的 `avatar_group_dx_contract_prefers_playground_isolation_and_fast_style_feedback_local`，以及 `components/avatar-group/test/avatar_group/semantics.rs` 的 `avatar_group_dx_contract_prefers_playground_isolation_and_fast_style_feedback`。）
  - 常见样式调整应走快速反馈路径，不依赖完整 wasm 重编译。
  - 组件调试应尽量保持当前交互上下文，降低重复操作成本。
  - 复杂交互组件应有隔离演练入口（workbench/story/demo 之一）。
- [x] 工程能力统一：`serde` 负责 spec 序列化/版本迁移/错误结构化；`tracing` 统一 span/event 语义；async 不绑定单一运行时（tokio/async-std），runtime 细节不泄露到上层 API。（证据：`avatar-group` 当前不涉及 spec/schema 输入与序列化迁移，`components/avatar-group/src/logic.rs`/`view.rs`/`styles.rs`/`Cargo.toml` 与 `AvatarGroup` 公共签名均无 `serde`/`serde_json`/`tracing`/`tokio`/`async-std`/`Future`/`.await` 等耦合令牌，按适用范围为 N/A；统一能力仍集中在 `crates/ui/Cargo.toml`（例如 `component-button = [\"dep:serde\", \"dep:serde_json\"]`、`accordion-wasm-debug = [\"component-accordion\", \"dep:tracing\"]`），且 `component-avatar_group = [\"component-avatar\", \"dep:ui-avatar-group\"]` 未引入 serde/tracing/runtime 泄漏。回归覆盖：`components/avatar-group/test/semantics.rs` 的 `avatar_group_engineering_unification_contract_is_explicitly_na_and_runtime_agnostic_local`，以及 `components/avatar-group/test/avatar_group/semantics.rs` 的 `avatar_group_engineering_unification_contract_is_explicitly_na_and_runtime_agnostic`。）
  - 若组件涉及 spec/config 输入，序列化与错误输出应走统一结构化路径。
  - 关键流程埋点语义应与全库 tracing 约定一致，避免组件各说各话。
  - 异步边界不得把具体 runtime 类型暴露到组件公共接口。

### 5. 样式与动效（Theme & Motion）
- [x] 样式孤岛防御（Defensive Variables）：`styles.rs` 使用双层回退链 `var(--ui-*, var(--ui-fallback-*))`；禁止组件内硬编码 Hex 或裸尺寸终值，Fallback 终值由 `ui-theme` 统一输出（SSOT）。（证据：`components/avatar-group/src/styles.rs` 已将尺寸/间距/字体/行高/颜色/阴影/圆角/边框统一改为 defensive chain（如 `var(--ui-avatar-size-md, var(--ui-fallback-avatar-size-md))`、`var(--ui-bg, var(--ui-fallback-bg))`、`var(--ui-line-height-100, var(--ui-fallback-line-height-100))`），并移除 `2rem/10px/9999px/16px` 等组件级裸终值；本地回归：`components/avatar-group/test/semantics.rs::avatar_group_styles_use_defensive_variable_fallback_chains_local`；全局回归：`components/avatar-group/test/avatar_group/semantics.rs::avatar_group_styles_use_defensive_variable_fallback_chains`，且已纳入 `avatar_group_semantics_suite_prioritizes_contract_assertions_over_snapshots` 必测清单。）
- [x] 级联层覆盖（`@layer ui`）：组件 CSS 默认聚合进 `@layer ui`；运行时数值调整仅通过 CSS Custom Properties（如 `style:--x=...`），禁止普通内联样式（如 `style=\"top: 10px\"`）。（证据：`crates/ui/src/css.rs` 的 `push_components_css` 使用 `out.push_str(\"\\n@layer ui {\\n\");` + `#[cfg(feature = \"component-avatar_group\")] out.push_str(crate::avatar_group::styles::CSS);` + `out.push_str(\"\\n}\\n\");`，确保 avatar-group CSS 进入统一 `@layer ui`；`components/avatar-group/src/view.rs`/`components/avatar-group/src/logic.rs` 无普通内联样式（无 `style=\"top:...\"`、`style:top` 等），运行时样式变更仅允许 `style:--*` 形式。回归覆盖：`components/avatar-group/test/semantics.rs::avatar_group_cascade_layer_and_runtime_style_contract_is_enforced_local` 与 `components/avatar-group/test/avatar_group/semantics.rs::avatar_group_cascade_layer_and_runtime_style_contract_is_enforced`，并已纳入 `avatar_group_semantics_suite_prioritizes_contract_assertions_over_snapshots`。）
- [x] Motion 合同化：`stiffness`/`damping` 等参数在 `motion.rs` 内置为组件 Contract，并通过 `attach_motion` 挂载；必须尊重 `prefers-reduced-motion` 且在 non-wasm/SSR 安全降级（no-op）。（证据：`avatar-group` 当前为静态展示组件，`components/avatar-group/src` 不存在 `motion.rs`，`view.rs`/`logic.rs`/`styles.rs` 无 `attach_motion`、`SpringAnimator`、`ui_motion::` 等组件级动效引擎调用，因此组件级 motion contract 在本范围按 N/A 语义成立；同时 non-wasm/SSR 安全降级由 `crates/ui-motion/src/lib.rs` 的 no-op stub（`#[cfg(not(target_arch = \"wasm32\"))]` 下 `prefers_reduced_motion() -> true` 与 `animate(..)` no-op）提供，并由 `crates/ui-motion/tests/non_wasm_stub.rs` 回归。语义回归覆盖：`components/avatar-group/test/semantics.rs` 的 `avatar_group_does_not_define_component_motion_runtime`、`avatar_group_ui_motion_non_wasm_noop_stub_contract_is_enforced`、`avatar_group_reduced_motion_ssr_wasm_branch_contract_is_explicitly_na_and_consistent`，以及 `components/avatar-group/test/avatar_group/semantics.rs` 的同名测试。）
- [x] `ui` 固定入口文件落点正确。（证据：`crates/ui/src/lib.rs` 通过 `#[cfg(feature = "component-avatar_group")] pub use ui_avatar_group as avatar_group;` 暴露稳定公共入口并保留 `pub use root::UiRoot;`；`crates/ui/src/css.rs` 在 `push_components_css` 中以 `@layer ui` + `#[cfg(feature = "component-avatar_group")] out.push_str(crate::avatar_group::styles::CSS);` 做 feature-gated 聚合；`crates/ui/src/root.rs` 统一注入 `BASE_CSS + theme vars + optional components css` 并集中提供 `provide_ui_i18n(i18n)` 与 `provide_ui_id_provider(id_seed)`；共享高亮能力固定在 `crates/ui-visual-primitive/src/active_highlight.rs`（`ActiveHighlightMotion` + `attach_active_highlight_motion`），不承载 avatar-group 业务语义；`crates/ui/src/overlay_open.rs`、`crates/ui/src/presence.rs`、`crates/ui/src/a11y.rs` 均不存在，原语边界固定在 `crates/ui-headless/src/controllable_state.rs`、`crates/ui-headless/src/presence.rs`、`crates/ui-headless/src/a11y.rs`。回归覆盖：`components/avatar-group/test/semantics.rs::avatar_group_ui_components_entrypoints_and_headless_boundaries_are_correct_local` 与 `components/avatar-group/test/avatar_group/semantics.rs::avatar_group_ui_components_entrypoints_and_headless_boundaries_are_correct`，并已纳入 `avatar_group_semantics_suite_prioritizes_contract_assertions_over_snapshots`。）
  - `crates/ui/src/lib.rs`：总模块入口 + 对外 `pub use`（公共 API 面）；组件模块受 `component-*` feature gate 约束；不暴露内部平台细节类型。
  - `crates/ui/src/css.rs`：组件 CSS 聚合入口（`push_components_css`）；按 feature 条件注入；禁止无条件聚合全部组件 CSS。
  - `crates/ui/src/root.rs`：`UiRoot` 统一注入 base css + theme vars +（可选）components css，并提供全局 i18n 上下文；主题与注入策略必须集中在此。
  - `crates/ui-visual-primitive/src/active_highlight.rs`：共享高亮条样式与 motion driver；只承载通用高亮动效能力，不承载具体组件业务语义。
  - `crates/ui/src/overlay_open.rs`：当前仓库中不应存在；open-state 原语固定在 `crates/ui-headless/src/controllable_state.rs`，组件通过 headless API 消费。
  - `crates/ui/src/presence.rs`：当前仓库中不应存在；presence 原语固定在 `crates/ui-headless/src/presence.rs`，组件通过 `ui_headless::use_presence` 消费。
  - `crates/ui/src/a11y.rs`：当前仓库中不应存在；共享 A11y 工具固定在 `crates/ui-headless/src/a11y.rs`（如 `aria_controls_when_open`），组件只负责挂载。
- [x] 组件目录标准文件落点正确。（证据：目录存在 `components/avatar-group/src/mod.rs`、`components/avatar-group/src/logic.rs`、`components/avatar-group/src/styles.rs`、`components/avatar-group/src/view.rs`，且不存在 `components/avatar-group/src/render.rs`、`components/avatar-group/src/spec.rs`；`motion.rs` 在当前静态非交互范围按 N/A 保持缺席；本地回归 `components/avatar-group/test/semantics.rs::avatar_group_component_directory_standard_file_layout_is_correct_local`，全局回归 `components/avatar-group/test/avatar_group/semantics.rs::avatar_group_component_directory_standard_file_layout_is_correct`，并已纳入 `components/avatar-group/test/avatar_group/semantics.rs::avatar_group_semantics_suite_prioritizes_contract_assertions_over_snapshots` 必测清单。）
  - `<component>/mod.rs`：最小稳定导出面，存在且无过度导出。
  - `<component>/logic.rs`：props 归一化、派生状态、来源标记；不得承载可下沉原语。
  - `<component>/styles.rs`：静态 CSS 契约，只用 `var(--ui-*)`，不写死主题常量。
  - `<component>/view.rs`：纯 Leptos 结构渲染 + headless 语义挂载；禁止 `render.rs` 漂移；不隐藏关键状态决策。
  - `<component>/motion.rs`：`XxxMotion + attach_motion`；交互组件必须有；只做语义到 motion contract 的映射与挂载。
  - `<component>/spec.rs`：仅极少数组件专用（当前主要 button），无必要不新增。

### 6. AI 原生能力与文件落点（Struct-First & Projection）
- [x] 文件落点纪律：组件目录严格由 `mod.rs`（导出）、`logic.rs`（归一派生）、`styles.rs`（Token 样式）、`view.rs`（渲染）、`motion.rs`（动效）组成；复杂组件可选 `spec.rs`；禁止 `render.rs`。（证据：`components/avatar-group/src/` 当前落点为 `mod.rs`/`logic.rs`/`styles.rs`/`view.rs`，且不存在 `render.rs`、`spec.rs`；`avatar-group` 为当前范围内静态非交互组件，`motion.rs` 按 N/A 保持缺席并由测试锁定。回归覆盖：本地 `components/avatar-group/test/semantics.rs::avatar_group_component_directory_standard_file_layout_is_correct_local`，全局 `components/avatar-group/test/avatar_group/semantics.rs::avatar_group_component_directory_standard_file_layout_is_correct`，并已纳入 `components/avatar-group/test/avatar_group/semantics.rs::avatar_group_semantics_suite_prioritizes_contract_assertions_over_snapshots` 必测清单。）
- [x] Hyper-Structure Builder（`spec.rs`）：复杂组件必须提供 AI 友好的 `*Spec::new()...render()` 建造者 API。（证据：`avatar-group` 当前为轻量展示组件，不存在稳定外部 Schema/复杂配置固化需求，因此本条按 N/A 语义通过；`components/avatar-group/src/spec.rs` 不存在且 `components/avatar-group/src/mod.rs` 无 `mod spec`/`pub mod spec` 导出，避免无效抽象。回归覆盖：`components/avatar-group/test/avatar_group/semantics.rs::avatar_group_does_not_introduce_spec_rs_and_keeps_lightweight_exports` 与 `components/avatar-group/test/semantics.rs::avatar_group_component_directory_standard_file_layout_is_correct_local`。若后续演进为复杂 schema 组件，再补 `AvatarGroupSpec::new()...render()` 与对应契约测试。）
- [x] 上下文压缩协议（Manifest + RBI）：新增/大改组件必须同步维护组件目录下 `Component.toml`（能力清单）和 `.rbi`（接口签名投影），避免 AI 检索工具箱过时。（证据：`components/avatar-group/src/Component.toml` 与 `components/avatar-group/src/avatar_group.rbi` 已存在并与当前 API 同步；`Component.toml` 已声明 `context_compression_manifest`、`rbi_signature_projection` 能力，`.rbi` 覆盖 `AvatarGroupItem` 与 `AvatarGroup(...) -> impl IntoView` 投影签名。回归覆盖：本地 `components/avatar-group/test/semantics.rs::avatar_group_context_compression_manifest_and_rbi_are_present_and_consistent_locally`，全局 `components/avatar-group/test/avatar_group/semantics.rs::avatar_group_context_compression_manifest_and_rbi_are_present_and_consistent`，并已纳入 `components/avatar-group/test/avatar_group/semantics.rs::avatar_group_semantics_suite_prioritizes_contract_assertions_over_snapshots` 必测清单。）
- [x] 语义标记统一升级为 Agent Contract（Schema 化），让 Agent 不依赖 DOM 猜测理解组件状态与意图。（证据：`components/avatar-group/src/logic.rs` 新增类型化 `AvatarGroupAgentContract`（`schema + intent + action + state + source`）及 `resolve_avatar_group_agent_contract`；`components/avatar-group/src/view.rs` 根节点统一挂载 `data-ui-schema/data-ui-intent/data-ui-action/data-ui-state/data-ui-source`，并继续保留 `data-state/data-aria-label-source/data-class-source` 等稳定来源标记；`components/avatar-group/src/Component.toml` 已声明 `agent_contract_schema_markers` 与 `whitelist_render_policy_no_script_injection` 能力，渲染链路无 `inner_html`/`<script` 注入入口。回归覆盖：本地 `components/avatar-group/test/semantics.rs::avatar_group_agent_contract_schema_is_typed_traceable_and_whitelisted_locally`，全局 `components/avatar-group/test/avatar_group/semantics.rs::avatar_group_agent_contract_schema_is_typed_traceable_and_whitelisted`，并已纳入 `components/avatar-group/test/avatar_group/semantics.rs::avatar_group_semantics_suite_prioritizes_contract_assertions_over_snapshots` 必测清单。）
  - 关键交互组件必须输出稳定机器可读语义（至少 `data-*` + 状态来源标记；复杂组件建议补 `data-ui-schema`）。
  - Agent 消费字段应来自类型化 schema 生成，不允许散落字符串拼接。
  - 契约字段需可追溯到组件状态轴与动作语义（intent/action/state/source）。
  - 配置到组件的渲染链路必须走白名单能力边界，禁止任意脚本注入。
- [x] 流式在这里仅指 LLM 输出渲染（只看两种显示模式）。（证据：`avatar-group` 仍非 LLM 正文阅读组件，未引入 token 增量流协议字段（`is_streaming/on_stream/token_delta/data-ui-stream-mode`），并继续以 Streaming/Snapshot 两种定义约束语义边界；同时仅输出职责声明型标记 `data-ui-stream-support`/`data-ui-stream-fallback`/`data-ui-output-status`，不承担上层流式传输与恢复策略。回归覆盖：本地 `components/avatar-group/test/semantics.rs::avatar_group_streaming_definition_is_llm_output_only_with_two_modes_locally` 与 `components/avatar-group/test/semantics.rs::avatar_group_streaming_policy_is_optional_with_snapshot_fallback_and_explicit_output_status_locally`，全局 `components/avatar-group/test/avatar_group/semantics.rs::avatar_group_streaming_definition_is_llm_output_only_with_two_modes` 与 `components/avatar-group/test/avatar_group/semantics.rs::avatar_group_streaming_policy_is_optional_with_snapshot_fallback_and_explicit_output_status`，并已纳入 `components/avatar-group/test/avatar_group/semantics.rs::avatar_group_semantics_suite_prioritizes_contract_assertions_over_snapshots` 必测清单。）
  - `Streaming`：LLM 还在生成，界面边生成边显示。
  - `Snapshot`：LLM 全部生成完成后，一次性显示。
- [x] `Snapshot` 是所有组件的基础能力（默认必须支持）。（证据：`avatar-group` 公开 API 可直接消费完整配置并稳定渲染（`items + max + size + aria_label + class_name + lang + dir`），对应渲染路径由 `components/avatar-group/src/view.rs` 的 `logic::normalize_avatar_group_input(...)` + `logic::resolve_avatar_group_render_state(...)` 收敛；契约投影在 `components/avatar-group/src/Component.toml` 与 `components/avatar-group/src/avatar_group.rbi` 同步声明完整输入，不依赖流式协议字段。回归覆盖：本地 `components/avatar-group/test/semantics.rs::avatar_group_snapshot_base_capability_accepts_complete_configuration_locally`，全局 `components/avatar-group/test/avatar_group/semantics.rs::avatar_group_snapshot_base_capability_accepts_complete_configuration`，并已纳入 `components/avatar-group/test/avatar_group/semantics.rs::avatar_group_semantics_suite_prioritizes_contract_assertions_over_snapshots` 必测清单。）
  - 所有组件都应能消费“完整生成结果”并稳定渲染。
  - 即使组件不直接展示正文，也应能在接收上层完整配置后正常渲染。
- [x] `Streaming` 是否强制，按组件职责判断（不能一刀切）。（证据：`avatar-group` 在 `components/avatar-group/src/logic.rs` 的 `AvatarGroupAgentContract` 明确提供 `stream_support + stream_fallback + output_status` 三轴，当前实现固定 `stream_support=optional`、`stream_fallback=snapshot`，并由类型化 `AvatarGroupAgentOutputStatus` 输出 `draft/verified/submittable` 封闭集合；`components/avatar-group/src/view.rs` 根节点挂载 `data-ui-stream-support/data-ui-stream-fallback/data-ui-output-status`，同时持续输出 `role/aria-label/lang/dir` 与既有 `data-*` 状态标记，保证语义连续可读；`components/avatar-group/src/Component.toml` 已登记 `streaming_optional_with_snapshot_fallback_and_output_status_markers` 能力。回归覆盖：本地 `components/avatar-group/test/semantics.rs::avatar_group_streaming_policy_is_optional_with_snapshot_fallback_and_explicit_output_status_locally`，全局 `components/avatar-group/test/avatar_group/semantics.rs::avatar_group_streaming_policy_is_optional_with_snapshot_fallback_and_explicit_output_status`，并已纳入 `components/avatar-group/test/avatar_group/semantics.rs::avatar_group_semantics_suite_prioritizes_contract_assertions_over_snapshots` 必测清单。）
  - `Streaming Required`：组件本体就是正文阅读面，用户需要边生成边看。
  - `Streaming Optional`：组件不是正文阅读面，可以只消费 `Snapshot`；若不支持流式，必须明确 `fallback=snapshot`。
  - 无论是否支持 `Streaming`，都要显式标识当前输出状态（草稿/已验证/可提交），并保持 `role`/`aria-*`/`data-*` 连续可读。
  - 数据校验、断线恢复、重试策略由上层负责，组件层只负责稳定渲染。

### 7. 测试、门禁与交付
- [x] 代码卫生（Rust Hygiene）：非测试代码中完全禁止 `unwrap/expect`，禁止无处理的 `let _ = ...`；字符串复制热点收敛为 `Cow<'static, str>`（执行 `./scripts/check-rust-hygiene.sh` 验证）。（证据：`components/avatar-group/src/logic.rs`/`view.rs`/`styles.rs` 非测试实现已无 `.unwrap(`/`.expect(` 与 `let _ =`；类名拼装热点在 `components/avatar-group/src/logic.rs::compose_avatar_group_class_name` 收敛为 `Vec<Cow<'static, str>>`（静态片段 `Cow::Borrowed`，动态片段 `Cow::Owned`）。回归覆盖：本地 `components/avatar-group/test/semantics.rs::avatar_group_rust_hygiene_disallows_unwrap_expect_let_underscore_and_uses_cow_locally`，全局 `components/avatar-group/test/avatar_group/semantics.rs::avatar_group_rust_hygiene_disallows_unwrap_expect_let_underscore_and_uses_cow`，并已纳入 `components/avatar-group/test/avatar_group/semantics.rs::avatar_group_semantics_suite_prioritizes_contract_assertions_over_snapshots` 必测清单。门禁执行记录：`./scripts/check-rust-hygiene.sh` 在当前环境因 `rg` 缺 PCRE2 且仓库级 `check-api-contracts` 基线漂移失败（非 avatar-group 变更导致）；组件级等价扫描命令 `rg -n '\\.(unwrap|unwrap_err|expect)\\s*\\(' components/avatar-group/src/*.rs` 与 `rg -n '^[[:space:]]*let[[:space:]]+_[[:space:]]*=' components/avatar-group/src/*.rs` 返回空集。）
- [x] Tree Shaking & 特性剪裁：组件必须注册到 `ui` 特性树（如 `component-accordion`）；`css.rs` 和 `lib.rs` 聚合必须受 feature 门控，禁止无条件全局依赖。（证据：`crates/ui/Cargo.toml` 已声明 `component-avatar_group = ["component-avatar", "dep:ui-avatar-group"]`，并纳入 `web-demo-components`/`all-components` 可选特性树；`crates/ui/src/lib.rs` 中 `avatar_group` 导出受 `#[cfg(feature = "component-avatar_group")]` 门控；`crates/ui/src/css.rs` 的 `push_components_css` 在 `inject-css` 下聚合，且 `avatar_group` 样式注入受 `#[cfg(feature = "component-avatar_group")]` 门控，不存在无条件全量组件注册表。回归覆盖：本地 `components/avatar-group/test/semantics.rs::avatar_group_ui_components_entrypoints_and_headless_boundaries_are_correct_local` 与 `components/avatar-group/test/semantics.rs::avatar_group_engineering_unification_contract_is_explicitly_na_and_runtime_agnostic_local`，全局 `components/avatar-group/test/avatar_group/semantics.rs::avatar_group_tree_shaking_contract_enforces_source_mode_reachability_boundaries`，并已纳入 `components/avatar-group/test/avatar_group/semantics.rs::avatar_group_semantics_suite_prioritizes_contract_assertions_over_snapshots` 必测清单。命令验证：`cargo tree -e features -p ui --no-default-features --features component-avatar_group,inject-css` 与 `cargo tree -e features -i ui -p web-demo` 已执行，确认组件特性链与反向依赖关系符合门控预期。）
- [x] 语义测试与性能回归：断言必须覆盖 `aria-*`、`data-*` 与焦点流转，不能只看快照；高频/重型组件必须补齐 `render_count` 断言/测量（如初始化空闲预算为 1）。（证据：语义断言已覆盖 `aria-* + data-* + role` 与状态来源契约，核心回归为本地 `components/avatar-group/test/semantics.rs::avatar_group_state_markers_are_observable_and_closed_set_contracts_local`、`components/avatar-group/test/semantics.rs::avatar_group_a11y_i18n_l10n_contract_is_headless_driven_and_no_view_hardcoded_copy_local`，以及全局 `components/avatar-group/test/avatar_group/semantics.rs::avatar_group_state_markers_are_observable_and_closed_set_contracts`、`components/avatar-group/test/avatar_group/semantics.rs::avatar_group_semantics_suite_prioritizes_contract_assertions_over_snapshots`；焦点流转在 `avatar-group` 当前职责中为 N/A（非可聚焦交互组件），并由本地/全局 `avatar_group_has_no_overlay_focus_stack_gc_contract_in_current_scope` 固化边界；性能与回归阻断由本地/全局 `avatar_group_performance_governance_budget_is_defined_and_blocking` 覆盖，`render_count` 自动化尚在仓库级基建推进中，当前以可重复 mount-budget 等价证据阻断并在 `docs/plan/TODO.md` 跟踪补齐。）
- [x] 版本弃用迁移（Codemod/Registry）：若提交包含跨大版本 API 破坏升级，必须在 Schema Registry 注册弃用窗口并提供纯函数迁移层（`migrate_v1_to_v2`）。（N/A：本次 `avatar-group` 变更未引入跨大版本破坏性 API 升级，不存在 v1/v2 双轨兼容窗口，也无 `migrate_v1_to_v2` 迁移需求；公共面仍维持既有组件入口与命名约束，参见 `components/avatar-group/test/avatar_group/semantics.rs::avatar_group_public_api_naming_contract_is_stable_and_prefix_ready` 与 `components/avatar-group/test/avatar_group/semantics.rs::avatar_group_does_not_expose_logic_or_view_modules`。）
- [x] 文档即产品（Copy-Paste Ready）：`apps/docs-app` 必须新增 Playground（Hello World、状态矩阵、受控/非受控对照），支持流式/快照展现，并提供 Source-first 一键复制且补全 imports。（证据：`apps/docs-app/src/pages/components/pages/display.rs::avatar_group()` 已补齐 `Hello World`、`State Matrix`、`Controlled vs Uncontrolled (N/A)`、`Streaming Optional (fallback=snapshot)`、`Source-first Starter (Copy-Paste Ready)` 五类演示，分别覆盖最小路径、状态矩阵、受控/非受控对照（N/A 轴说明）、流式/快照策略展示与 Source-first 复制路径；每个 Playground 通过 `code_imports` 注入 `AvatarGroup/AvatarGroupItem/AvatarSize` 导入，复用 `apps/docs-app/src/playground.rs::compose_copy_ready_code` 自动补齐 imports，页面含稳定提示标记 `data-slot=\"avatar-group-streaming-policy\"` 与 `data-slot=\"avatar-group-copy-ready-hint\"`。回归覆盖：本地 `components/avatar-group/test/semantics.rs::avatar_group_dx_api_is_simple_and_docs_offer_minimal_hello_world` 与 `components/avatar-group/test/semantics.rs::avatar_group_dx_contract_prefers_playground_isolation_and_fast_style_feedback_local`；全局 `components/avatar-group/test/avatar_group/semantics.rs::avatar_group_docs_page_covers_primary_playgrounds`、`components/avatar-group/test/avatar_group/semantics.rs::avatar_group_docs_playgrounds_lock_state_matrix_contract_values`、`components/avatar-group/test/avatar_group/semantics.rs::avatar_group_dx_contract_prefers_playground_isolation_and_fast_style_feedback`。）
- [x] 语义测试优先：验证 `data-*` / `aria-*` / role / 状态来源契约，不只视觉快照。（证据：`avatar-group` 已具备本地与全局 `*_semantics.rs` 回归入口（`components/avatar-group/test/semantics.rs`、`components/avatar-group/test/avatar_group/semantics.rs`）；语义契约断言覆盖 `role + aria-* + data-* + 状态来源`，核心用例为本地 `avatar_group_state_markers_are_observable_and_closed_set_contracts_local`、`avatar_group_a11y_i18n_l10n_contract_is_headless_driven_and_no_view_hardcoded_copy_local` 与全局 `avatar_group_state_markers_are_observable_and_closed_set_contracts`、`avatar_group_a11y_i18n_l10n_contract_is_headless_driven_and_no_view_hardcoded_copy`；快照仅作补充且语义套件显式禁止快照依赖（`components/avatar-group/test/avatar_group/semantics.rs::avatar_group_semantics_suite_prioritizes_contract_assertions_over_snapshots`）；语义字段变更守卫已就位（同文件对 `data-state/data-aria-label-source/data-class-source` 必需字段与 `format!` 自由拼接禁用断言同步存在），避免新增/变更字段未补测。）
  - 每个交互组件至少有对应 `*_semantics.rs` 测试覆盖关键状态轴与动作语义。
  - 断言应聚焦语义契约（状态来源/可访问性/键盘路径），快照仅作补充。
  - 新增/变更语义字段必须同步补测试，否则不得打勾。
- [x] E2E 选择器稳定：使用语义标记，WASM 场景有稳定等待策略。（证据：新增 `e2e/tests/docs_app_avatar_group_contract.spec.mjs`，选择器仅使用稳定语义标记（`data-component/data-slot/data-state/data-ui-*`），无 `nth-child`/文本定位与固定 sleep；WASM 就绪统一使用 `body:not(:has(#boot))` + 语义状态断言；`avatar-group` 当前无 async/motion 运行时路径，E2E 通过 `toHaveCount(0)` 对 `aria-busy/data-loading/data-motion-state` 做显式 N/A 守卫，并保留可重复回放断点（reload 后语义状态一致）。回归覆盖：本地 `components/avatar-group/test/semantics.rs::avatar_group_e2e_selectors_are_semantic_and_wasm_waits_are_stable_local`，全局 `components/avatar-group/test/avatar_group/semantics.rs::avatar_group_e2e_selectors_are_semantic_and_wasm_waits_are_stable`。）
  - E2E 选择器优先 `data-*` 语义标记，禁止依赖脆弱 DOM 层级或文本定位。
  - WASM 场景必须使用稳定等待策略（语义状态就绪而非固定 sleep）。
  - 若组件涉及异步/动画，E2E 需显式覆盖 ready/settled 条件。
- [x] 关键流程纳入可重复回归集合（Playwright/Cypress）。（证据：`e2e/tests/docs_app_avatar_group_contract.spec.mjs` 已纳入可重复关键流程 `docs-app avatar-group key flow is repeatable with semantic checkpoints`，流程为“进入组件页 -> 语义就绪 -> overflow 状态断点校验 -> reload -> 同断点复验”；断点均为稳定语义契约（`data-slot=\"avatar-group\"`、`data-state=\"overflow\"`、`data-has-overflow=\"true\"`、`data-ui-state=\"overflow\"`），失败可直接定位到契约字段而非笼统页面差异；高风险路径方面，`avatar-group` 当前无 overlay/focus/keyboard/async 运行时职责，E2E 已显式以 `toHaveCount(0)` 守卫 `aria-busy/data-loading/data-motion-state`，并由本地 `components/avatar-group/test/semantics.rs::avatar_group_e2e_selectors_are_semantic_and_wasm_waits_are_stable_local` 与全局 `components/avatar-group/test/avatar_group/semantics.rs::avatar_group_e2e_selectors_are_semantic_and_wasm_waits_are_stable` 回归锁定。）
  - 至少定义一条可重复关键流程（打开/交互/关闭或提交）纳入 E2E 回归。
  - 回归失败需可定位到具体语义契约断点，而不是笼统“页面不一致”。
  - 高风险路径（overlay、focus、keyboard、async）优先进入回归集合。
- [x] docs-app 文档、示例、参数矩阵、状态矩阵同步更新。（证据：`apps/docs-app/src/pages/components/pages/display.rs::avatar_group()` 已同步覆盖示例与矩阵：示例 Playground 含 `Hello World`/`Overflow Stack`/`Sizes Without Overflow`/`Custom Aria + Class`，状态矩阵含 `Playground title="State Matrix"`，并新增显式文档矩阵段 `data-slot="avatar-group-state-matrix"` 与 `data-slot="avatar-group-parameter-matrix"`；参数与默认值说明已与实现对齐（`max: Option<usize> -> default None -> normalize to 4`、`size: AvatarSize -> default Md`、`aria_label/class_name/lang/dir` 默认与归一化语义）。一致性回归：本地 `components/avatar-group/test/semantics.rs::avatar_group_docs_parameter_and_state_matrix_match_logic_defaults`，全局 `components/avatar-group/test/avatar_group/semantics.rs::avatar_group_docs_parameter_and_state_matrix_match_logic_defaults`，并纳入 `components/avatar-group/test/avatar_group/semantics.rs::avatar_group_semantics_suite_prioritizes_contract_assertions_over_snapshots`。）
  - 组件行为或参数变更必须同步更新 `apps/docs-app` 示例与说明。
  - 文档示例需覆盖至少一组状态矩阵（受控/非受控、disabled、size/variant 等）。
  - 文档中的 API 名称与默认值必须和 `logic.rs` 当前实现一致。
- [x] 组件文档必须对新手友好（Documentation as Product）：组件 README 或等价文档入口必须存在。（证据：`components/avatar-group/src/README.md` 已补齐新手路径，结构明确为 `先用起来（Quick Start / Hello World） -> 常见用法 -> 默认参数 -> 进阶`，并给出零门槛最小示例 `<AvatarGroup items=Vec::<AvatarGroupItem>::new() />` 与常见 overflow/custom 用法；`apps/docs-app/src/pages/components/pages/display.rs` 保持可发现入口 `avatar_group()` + `slug="avatar-group"` + `Playground title="Hello World"`。回归覆盖：本地 `components/avatar-group/test/semantics.rs::avatar_group_readme_is_beginner_friendly_with_default_path_before_advanced`，全局 `components/avatar-group/test/avatar_group/semantics.rs::avatar_group_readme_is_beginner_friendly_with_default_path_before_advanced`，并纳入 `components/avatar-group/test/avatar_group/semantics.rs::avatar_group_semantics_suite_prioritizes_contract_assertions_over_snapshots` 必测清单。）
  - 每个基础组件必须提供“零门槛”最小示例（Hello World）与常见用法，避免要求用户先理解底层分层架构。
  - 文档需明确“先用起来，再进阶”：默认 API 路径在前，高级控制参数在后。
  - “只有源码没有文档”或“只写给架构师/机器看的文档”视为不通过。
- [x] `apps/docs-app` 必须提供 Interactive Playground：用户可在线修改 props/状态并实时预览。（证据：`apps/docs-app/src/pages/components/pages/display.rs::avatar_group()` 新增 `Playground title="Interactive Playground (Props + State + Preview)"`，提供 `roster/size/max` 分段控件与 `custom aria/custom class/rtl` 开关，实时驱动 `data-slot="avatar-group-workbench-configured"` 预览并输出 `data-slot="avatar-group-workbench-state"` 反馈；关键交互回归由 `e2e/tests/docs_app_avatar_group_contract.spec.mjs::docs-app avatar-group interactive playground updates semantic state markers with live controls` 覆盖（控件切换 -> 语义状态变化 -> reload 后可重复基线）；AI Spec 联动条目对本组件为 N/A（非 spec 组件），已在工作台显式标记 `data-slot="avatar-group-spec-preview-na"`。语义守卫：本地 `components/avatar-group/test/semantics.rs::avatar_group_docs_interactive_playground_supports_live_prop_controls_and_preview_feedback`，全局 `components/avatar-group/test/avatar_group/semantics.rs::avatar_group_docs_interactive_playground_supports_live_prop_controls_and_preview_feedback`，并纳入 `avatar_group_semantics_suite_prioritizes_contract_assertions_over_snapshots`。）
  - Playground 至少支持基础 props 调整、状态切换、交互反馈观察。
  - 对 AI Spec 相关组件，至少提供一组 Spec 输入与预览输出的联动示例。
  - Playground 作为验收面，需可重复复现关键交互路径。
- [x] Source-first 文档必须 Copy-Paste Ready：提供一键复制组件源码或最小可用片段能力。（证据：`apps/docs-app/src/pages/components/pages/display.rs::avatar_group()` 已补齐独立 source-first 文档卡片 `data-slot="avatar-group-source-first"`：包含可见复制按钮 `Snippet(copyable=true, label="Copy avatar-group starter")`，复制内容直接来自 `source_first_code`（通过 `apps/docs-app/src/playground.rs::compose_copy_ready_code` 自动补 imports）；同时提供依赖前提 `component-avatar-group`/`inject-css`/`UiRoot`（`data-slot="avatar-group-source-prerequisites"`）与真实源码落点 `components/avatar-group/src/{mod,logic,view,styles}.rs`（`data-slot="avatar-group-source-paths"`），并新增同步说明 `data-slot="avatar-group-source-sync-note"` 防示例漂移。回归覆盖：`e2e/tests/docs_app_avatar_group_contract.spec.mjs::docs-app avatar-group source-first section exposes copy-ready starter and source anchors`，本地语义守卫 `components/avatar-group/test/semantics.rs::avatar_group_source_first_docs_are_copy_paste_ready_and_traceable_local`，全局语义守卫 `components/avatar-group/test/avatar_group/semantics.rs::avatar_group_source_first_docs_are_copy_paste_ready_and_traceable`，并纳入 `avatar_group_semantics_suite_prioritizes_contract_assertions_over_snapshots`。）
  - docs-app 页面应提供复制按钮，输出代码默认可直接运行（含必要 imports/依赖提示）。
  - 若为 source-first 组件，文档需指向真实源码落点并说明依赖前提，避免“复制即报错”。
  - 文档代码与当前实现必须同步，防止示例漂移。
- [x] HeroUI 对标文档与组件文档同步：参数模型变更需同步 `docs/spec/heroui-parameter-design-strategy.md`（必要时补充 `docs/research/spectrum-heroui-style-interface-study.md`），并保证组件文档可访问。（证据：`docs/spec/heroui-parameter-design-strategy.md` 已新增 `### AvatarGroup 同步记录（2026-02-20）`，明确参数模型 `items/max/size/aria_label/class_name/lang/dir`、docs 入口 `apps/docs-app/src/pages/components/pages.rs::component_doc!("AvatarGroup", "avatar-group", "Display", display::avatar_group)` 与索引路由 `#/components/avatar-group`，并记录 Source-first 依赖前提与源码落点；研究文档补充判定已声明“本轮不需要追加 `docs/research/spectrum-heroui-style-interface-study.md`”。组件文档入口可访问性：`apps/docs-app/src/pages/components/pages/display.rs::avatar_group()` + `slug="avatar-group"`，`components/avatar-group/src/README.md` 含 `#/components/avatar-group` 入口。回归覆盖：本地 `components/avatar-group/test/semantics.rs::avatar_group_heroui_alignment_docs_and_component_entry_are_synced_local`，全局 `components/avatar-group/test/avatar_group/semantics.rs::avatar_group_heroui_alignment_docs_and_component_entry_are_synced`，并纳入 `avatar_group_semantics_suite_prioritizes_contract_assertions_over_snapshots`；约束“仅代码更新无文档更新”已在策略文档与本条证据链中固定。）
  - 若参数语义发生变化，需同步更新对标策略文档，不允许实现先漂移文档后补。
  - 组件文档入口必须存在（docs-app 页面或等价文档），且可被索引定位。
  - “仅代码更新无文档更新”在接口变更场景下直接判不通过。

### 8. 合并前门禁死命令（最终执行）
在发起 PR 或完成任务前，必须保证本地/CI 以下命令全部通过：

- `cargo fmt --all -- --check`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `./scripts/check-rust-hygiene.sh`
- `cargo check -p ui --target wasm32-unknown-unknown`
- `cargo check -p ui-headless --no-default-features --features ssr`
- `cargo check -p ui --target wasm32-unknown-unknown --no-default-features --features component-<your_component>,inject-css`

依据文档（`rust-ui/docs/spec` 及 `rust-ui/docs`）：

- `rust-ui/docs/spec/ai_context_projection_protocol.md`
- `rust-ui/docs/spec/architectural_fitness_functions.md`
- `rust-ui/docs/spec/async_state_as_data_command.md`
- `rust-ui/docs/spec/collection_registration_protocol.md`
- `rust-ui/docs/spec/compile_time_evolution_migration.md`
- `rust-ui/docs/spec/component_boundaries.md`
- `rust-ui/docs/spec/component_domains.md`
- `rust-ui/docs/spec/controlled_evolution_sandbox.md`
- `rust-ui/docs/spec/core_shell_protocol_infra_baseline.md`
- `rust-ui/docs/spec/environment_subscription_streams.md`
- `rust-ui/docs/spec/event_light_cone_signal_bus.md`
- `rust-ui/docs/spec/focus_global_stack_gc.md`
- `rust-ui/docs/spec/foreign_zone_escape_hatches.md`
- `rust-ui/docs/spec/headless_purification.md`
- `rust-ui/docs/spec/heroui-parameter-design-strategy.md`
- `rust-ui/docs/spec/hyper-structure-ui-development-playbook.md`
- `rust-ui/docs/spec/i18n.md`
- `rust-ui/docs/spec/intent_stack_semantic_layering.md`
- `rust-ui/docs/spec/kernel_shell_architecture.md`
- `rust-ui/docs/spec/macro_micro_dual_state_machine.md`
- `rust-ui/docs/spec/motion.md`
- `rust-ui/docs/spec/mvp.md`
- `rust-ui/docs/spec/platform_abdication_ecosystem.md`
- `rust-ui/docs/spec/README.md`
- `rust-ui/docs/spec/release_versioning.md`
- `rust-ui/docs/spec/side_effect_command_pattern.md`
- `rust-ui/docs/spec/slot_projection_strategy.md`
- `rust-ui/docs/spec/ssr_hydration_discontinuity.md`
- `rust-ui/docs/spec/state_primitives_core_satellite_split.md`
- `rust-ui/docs/spec/style_island_defense.md`
- `rust-ui/docs/spec/styling.md`
- `rust-ui/docs/spec/tree_shaking.md`
- `rust-ui/docs/spec/ui_layout_split.md`
- `rust-ui/docs/spec/ui_physics_two_pass_rendering.md`
- `rust-ui/docs/spec/unified_causality_bus.md`
- `rust-ui/docs/spec/wasm_generic_bloat.md`
- `rust-ui/docs/起点_也即是目的.md`
- `rust-ui/docs/DOCS_GOVERNANCE.md`
- `rust-ui/docs/DOCS_INDEX.md`
- `rust-ui/docs/philosophy.md`
- `rust-ui/docs/README.md`
- `rust-ui/docs/RULES_ZH.md`
