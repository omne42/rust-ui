# 单组件 Check List（Hyper-Structure 完整版）

> 用途：每次新增、重构或修改一个组件时，必须按本清单逐项检查并在 PR 中附带结果。  
> 执行顺序：先过第 1-2 节（架构与状态），再进入第 3-5 节（具体实现）。  
> 术语统一：`status-primitives` 指状态原语层（当前 crate 名为 `ui-state-primitives`）。

### 0. 适用范围与顺序纪律
本清单仅评估“一个组件”的改动结果，不替代仓库级治理。
先过第 1-2 节（架构与状态）再进入第 3-5 节（具体实现）。
不适用的条目必须明确标注 `N/A` 并说明理由，禁止机械打勾。
组件目标、非目标、风险边界已写清楚；发现跨组件/跨层系统性问题时升级为仓库级任务。

### 1. 架构边界与分层约束（Kernel/Shell 总线）
- [x] `status-primitives` 定义：纯状态原语层（受控/非受控、toggle、selection、list、overlay open state、expansion 等）。不依赖 Leptos/DOM/web-sys；只包含 Rust 数据结构和方法，不含视图与事件绑定。（`List` 的可访问名/列表状态/item-state/section-state 已下沉到 `crates/ui-state-primitives/src/list.rs`；组件 `components/list/src/logic.rs` 仅做装配与样式 class 组合；下沉原语回归见 `crates/ui-state-primitives/src/test/list.rs`。）
  - 所有状态原语必须从 `status-primitives`（`ui-state-primitives`）获取，组件层只能消费，不得自造。
  - 下沉判定依据是“稳定状态不变量”；凡属于状态机、归一化、状态派生能力，默认先进入 `ui-state-primitives`。
  - 组件中可保留的仅是装配逻辑：props 归一、样式来源标记、slot 组织、对 `ui-state-primitives` 输出的映射。
  - 组件内若出现状态原语实现（受控/非受控状态机、single/multiple 展开规则、索引归一化、跨事件状态派生），该项直接判不通过。
  - 处理方式固定：先下沉到 `ui-state-primitives/src/<capability>.rs`（如 `expansion.rs`），在 `ui-state-primitives/src/lib.rs` 导出，再回到组件改调用。
  - 下沉后的原语必须有 `ui-state-primitives` 单元测试；组件侧只保留调用与语义挂载测试。
  - 桥接规范：`ui-state-primitives` 结构体必须是 POJO（Plain Old Rust Object），不持有 Leptos `Signal` 或框架绑定状态容器。
  - 消费规范：`ui-headless` 或组件 `logic.rs` 负责解包 `Signal` 当前值传入 primitive 方法，并将结果显式写回 `Signal`。
  - 设计理由：保持 primitives 纯粹可测、可迁移，不与特定响应式库绑定（便于未来替换响应式实现与做纯 Rust 测试）。
- [x] `ui-headless` 定义：交互与 A11y 原语层（press/focus/hover/roving/listbox/menu/tooltip 等），把输入设备事件与状态语义标准化为可复用契约；输出必须是类型化 `attrs + handlers + state`。不做样式、不写组件 CSS、不做组件级动效编排。（`List` 交互契约由 `ui_headless::use_listbox/use_focus_ring` 提供，option 语义映射下沉为 `ui_headless::listbox_option_a11y_attrs`，组件仅挂载；`lang/dir` 通过 `ui_headless::locale_attrs` 接入。回归约束见 `crates/ui-headless/src/test/listbox.rs` 与 `crates/ui-components/tests/list_module_semantics.rs`。）
  **`ui-headless` 落位硬规则（必须执行）**：
  - 输入边界：消费 `status-primitives` 状态 + 用户输入事件（keyboard/pointer/focus）+ 环境能力（web/ssr）。
  - 输出边界：只输出语义契约（attrs/handlers/state）；组件层只负责挂载与组合，不得把语义判断塞回 `view.rs`。
  - 下沉判定依据是“交互/A11y 语义契约”；凡属于键盘/焦点/指针归一、ARIA 映射、交互状态语义能力，默认先进入 `ui-headless`。
  - 必须下沉：键盘模型、焦点模型、跨设备输入归一、ARIA 状态映射、overlay/presence 等交互语义。
  - A11y 契约与共享工具落点固定在 `crates/ui-headless/src/a11y.rs`；组件只在 `view.rs` 挂载，不在组件层重写。
  - 语义契约必须提供 `lang` / `dir`（LTR/RTL）接入能力；headless 不硬编码用户可见文本，文案由 i18n/l10n 层提供。
  - 语义契约正确性必须有回归：`crates/ui-components/tests/*` 断言语义标记，`e2e/tests/*` 覆盖关键交互流程。
  - 禁止放在 `ui-headless`：视觉 class 选择、CSS 规则、组件 slot 布局、组件专属动效编排、业务文案。
  - 允许留在组件层：纯视觉一次性交互且不形成可复用语义契约（例如单组件局部微交互）。
- [x] `ui-motion` 定义：动效能力与契约执行层（spring、keyframes、WAAPI/RAF backend），只负责时间函数、插值与运行时驱动，不承载组件业务语义与状态决策。（`List` 动效映射已收敛到 `components/list/src/motion.rs`：`resolve_motion + attach_motion + attach_section_motion`，`view.rs` 仅调用 `crate::motion::attach_motion` 挂载；样式过渡参数改为 `--ui-motion-*` token 变量回退链；`ui-motion` 非 wasm no-op/stub 由 `crates/ui-motion/src/lib.rs` 保证。回归见 `components/list/test/motion.rs` 与 `crates/ui-components/tests/list_module_semantics.rs`。）
  - 放在 `crates/ui-motion`：通用动画数学与执行后端（spring solver、keyframe sampling、easing registry、driver adapters），以及 `wasm/non-wasm` 适配与 `reduced-motion` 执行策略。
  - 放在 `crates/ui-components/src/<component>/motion.rs`：把组件语义状态（open/closed、enter/exit、active/inactive）映射为 `ui-motion` contract，绑定目标节点并调用 attach。
  - 禁止放在 `crates/ui-motion`：组件 slot 结构、组件专属状态机、ARIA/keyboard 语义、业务文案与业务分支。
  - 禁止放在组件 `motion.rs`：自实现 spring/keyframe/driver 执行器；跨组件共享动效算法必须回迁 `ui-motion`。
  - 动效参数优先来自 token/theme；禁止在组件样式与逻辑中散落硬编码时长/曲线/位移常量。
  - 非 wasm 路径必须提供 no-op/stub，保证 SSR/tooling 可编译且行为可预测。
- [x] `ui-theme` 定义：唯一设计 token 与主题上下文层（system/color/scale + Light/Dark/OLED），负责 token 分类、主题映射与 CSS 变量生成。（`List` 样式继续仅消费 `var(--ui-*)`，并去除组件私有 `--ui-listbox-*` 主题变量；主题三轴与变量注入仍集中在 `crates/ui-theme/src/tokens.rs` → `crates/ui-theme/src/theme.rs` → `crates/ui-theme/src/css.rs` 与 `crates/ui-components/src/root.rs`（`data-theme-system/color/scale`）。量化基线与对比回归分别由 `crates/ui-theme/tests/token_scale_baseline.rs`、`crates/ui-theme/tests/wcag_contrast.rs` 保证；组件侧回归新增 `crates/ui-components/tests/list_module_semantics.rs::list_theme_contract_is_token_first_and_ui_theme_backed`。）
  - Token 统一基线落点固定：`crates/ui-theme/src/tokens.rs` 定义，`crates/ui-theme/src/theme.rs` 映射，`crates/ui-theme/src/css.rs` 输出变量；组件只在 `crates/ui-components/src/<component>/styles.rs` 消费。
  - 三轴上下文（`system/color/scale`）在 `theme.rs` 定义；组件在 `logic.rs` 选择并在 `view.rs` 生效，`styles.rs` 只消费变量，不重建主题。
  - Token 分类必须可追溯：分类源在 `tokens.rs`，规范同步 `docs/spec/styling.md`；组件不得引入平行私有 token 命名体系。
  - 量化尺寸基准必须可回归：尺寸基准在 `tokens.rs` 与 `theme.rs` 定义，主题回归在 `crates/ui-theme/tests/token_scale_baseline.rs`，组件语义回归在 `crates/ui-components/tests/<component>_semantics.rs`。
  - 主题调色与语义色对比必须满足 `WCAG 2.1 AA` 基线，并覆盖 Light/Dark/OLED 主题变体。
  - 主题层只输出 `theme/tokens/base css` 与变量；不实现组件结构、交互逻辑、组件级动效编排。
  - 新增视觉语义先补 token，再由组件消费；禁止“组件临时值先落地、后补 token”的倒序流程。
- [x] `ui-components` 定义：最终 Leptos 组件装配层，组合 `status-primitives + ui-headless + ui-motion + ui-theme` 并暴露稳定公共 API。（`List` 目录职责分层已固定：`logic.rs` 仅做 props/状态装配并消费 `ui-state-primitives`，`view.rs` 仅渲染结构并挂载 `ui-headless`，`styles.rs` 为 token-first 静态 CSS，`motion.rs` 仅做动效映射与 attach；`components/list/src/mod.rs` 保持最小导出面。新增组件本地语义回归 `components/list/test/semantics.rs`，并在 `components/list/src/mod.rs` 通过 `#[path = "../test/semantics.rs"] mod semantics_tests;` 挂载，替代仅依赖旧的 `crates/ui-components/tests/list_module_semantics.rs` 远端语义锁定。）
  - `logic.rs` 负责 props 归一与状态派生；`view.rs` 负责结构渲染与 headless 语义挂载；`styles.rs` 负责 token-first 静态样式；`motion.rs` 负责动效 attach。
  - 组件层不得重写 `status-primitives` 状态机或 `ui-headless` 交互契约；发现即判不通过并回迁到对应层。
  - 对外 API 禁止暴露 `web-sys`/DOM 细节类型；平台差异封装在内部模块。
  - 测试文件位于src同级的test/中，内部测试文件同名（如rust-ui/components/accordion/src/logic.rs与rust-ui/components/accordion/test/logic.rs）。
  - 还需要一个semantics.rs用于测试。可能存在类似rust-ui/crates/ui-components/tests/accordion_semantics.rs的旧版实现，需要迁移到新目录。

### 2. API 设计与状态内核（Logic/Kernel）
- [x] API 命名契约统一：公共 props/回调严格使用 `is_*`、`on_*`、`default_*` 前缀；同语义在全库同名，禁止别名漂移。（`components/list/src/view.rs` 已统一公开布尔与默认值命名：`List` 使用 `is_disabled`、`default_active_index`、`is_active_index_synced_to_selected`；`ListItem` 使用 `is_selected/is_focused/is_disabled/is_selection_indicator_visible/is_divider_visible`；`ListSection` 使用 `is_disabled/is_sticky_heading/is_divider_visible`。旧别名已从公开签名移除；迁移路径为将调用点统一替换到新命名（`disabled -> is_disabled`、`sync_active_index_to_selected -> is_active_index_synced_to_selected`、`show_selection_indicator -> is_selection_indicator_visible` 等），调用端同步到 `apps/docs-app/src/pages/components/pages/collections.rs`、`apps/docs-app/src/pages/components/pages/collections_extra.rs` 与 `components/list/src/README.md`。回归锁定：`components/list/test/semantics.rs::list_public_props_follow_is_on_default_naming_contract`。）
  - 布尔状态统一 `is_*`（如 `is_open`/`is_disabled`），事件统一 `on_*`，默认值统一 `default_*`。
  - 同一语义 across 组件必须同名（如都用 `on_open_change`，禁止同义别名并存）。
  - 公共 API 引入新命名时，需说明与现有命名体系的兼容策略与迁移路径。
- [x] 受控/非受控必须成对：每个可控状态轴都提供 `value + on_value_change + default_value`（如 `open/on_open_change/default_open`）；缺一项即不通过。（`List` 选择轴已统一为 `selected_index + on_selected_index_change + default_selected_index`：`components/list/src/view.rs` 通过 `logic::normalize_selection_axis` + `ui_headless::use_controllable_state` 实现受控/非受控双模式；受控模式仅透传变更请求并保持外部值为事实来源，非受控模式由默认值初始化一次后内部管理。调用面已同步到 `components/select/src/view.rs`、`apps/docs-app/src/pages/components/pages/collections.rs`、`apps/web-demo/src/demos/listbox_demo.rs`、`components/list/src/README.md`；回归锁定见 `components/list/test/semantics.rs` 与 `crates/ui-components/tests/list_module_semantics.rs`。）
  - 受控模式：外部值是单一事实来源，内部不得偷偷写回本地状态。
  - 非受控模式：仅由默认值初始化一次，后续状态由内部原语管理。
  - 受控/非受控切换语义需稳定可测，避免“半受控”隐式行为。
- [x] 默认值单一来源：默认值与优先级只在 `logic.rs` 归一化；`view.rs` 禁止二次兜底或隐式改写。（默认归一已收敛到 `components/list/src/logic.rs`：`normalize_list_class_name`（根 class 默认）、`item::normalize_callbacks`（item 回调默认）、`section::normalize_item_count`（section `item_count` 默认）、`section::resolve_title_text`（section 标题文本默认）。`components/list/src/view.rs` 仅消费这些归一化输出，不再保留 `item_count.unwrap_or(1)`、`on_press.unwrap_or_else(...)`、`on_pointer_move.unwrap_or_else(...)`、`title.get_value().unwrap_or_default()` 等本地兜底分支。回归锁定：`components/list/test/semantics.rs::list_defaults_are_normalized_in_logic_layer` 与 `crates/ui-components/tests/list_module_semantics.rs::list_defaults_are_single_sourced_in_logic_layer`。）
  - 默认值优先级必须可读且可测试（显式规则而非分散 `unwrap_or`）。
  - `view.rs` 不允许再做默认值分支；仅消费 `logic.rs` 的归一化输出。
  - 一旦发现多处默认值来源，直接判不通过并回收至 `logic.rs`。
- [x] 状态归一化集中：状态输入先类型化，再在 `logic.rs` 统一派生；禁止在 `view.rs`、事件回调、样式分支中分散拼状态机。（`components/list/src/logic.rs` 新增类型化输入/派生结构：`ListOptionsAxisInput/ListOptionsAxis` 与 `ListOptionStateInput/ListOptionState`，并集中提供 `normalize_options_axis`、`resolve_option_state`、`is_disabled_index`、`item::is_interaction_blocked`。`components/list/src/view.rs` 改为仅消费这些逻辑输出：通过 `logic::normalize_options_axis(...)` 构建 disabled 轴，通过 `logic::resolve_option_state(...)` 组装 option 语义输入，item 事件回调仅调用 `is_interaction_blocked` 结果，不再在回调中重建 `is_disabled` 判定规则。回归锁定：`components/list/test/semantics.rs::list_state_normalization_is_concentrated_in_logic_layer` 与 `crates/ui-components/tests/list_module_semantics.rs::list_state_normalization_is_concentrated_in_logic_layer`。）
  - 输入边界统一进入 `logic.rs`，输出统一为可渲染语义状态与来源标记。
  - 事件处理器只触发状态变更，不重建状态机规则。
  - 样式层只消费状态标记，不承担状态判定职责。
- [x] 离散状态必须类型约束：`variant/size/mode/status` 等离散输入使用 `enum`；禁止用多个 `Option<bool>`/字符串自由组合表达互斥状态。（`components/list/src/logic.rs` 通过 `pub use primitives::{ListItemSelectionIndicator, ListSectionHeadingTone};` 对离散轴暴露类型化枚举；`components/list/src/view.rs` 的离散 props 使用 `Option<A11yDirection>`、`ListMotion`、`ListSectionMotion`、`ListSectionHeadingTone`，未引入 `variant/size/mode/status` 的字符串输入或 `Option<bool>` 互斥轴。回归锁定：`components/list/test/semantics.rs::list_discrete_state_axes_are_type_constrained` 与 `crates/ui-components/tests/list_module_semantics.rs::list_discrete_state_axes_are_type_constrained`。）
  - 互斥状态优先用 `enum` 建模，利用编译器封住无效组合。
  - 字符串输入若需兼容外部配置，必须先映射到类型化枚举再进入逻辑层。
  - 布尔爆炸（多个 bool 表达一个状态机）应在设计评审阶段直接拦截。
- [x] 状态原语来源正确：组件层只消费 `status-primitives`（当前 `ui-state-primitives`）能力，不直接绑定业务 store；应用级全局状态必须经桥接层适配后再接入组件。（`components/list/src/logic.rs` 通过 `use ui_state_primitives::list as primitives;` 统一消费 `ListViewState/ListItemState/ListSectionState` 与相关 resolver，`components/list/src/view.rs` 仅通过 `logic::normalize_selection_axis + use_controllable_state` 桥接并消费 `logic::resolve_state/item::resolve_state/section::resolve_state` 输出；未发现业务 store 绑定。回归锁定：`components/list/test/semantics.rs::list_state_primitives_are_consumed_via_logic_without_business_store_binding` 与 `crates/ui-components/tests/list_module_semantics.rs::list_state_primitives_are_consumed_via_logic_without_business_store_binding`。）
  - 组件中出现可复用状态机实现（受控/非受控、展开规则、选择归一）即判应下沉。
  - 组件与业务全局状态之间必须有适配边界，禁止组件直接依赖业务 store 类型。
  - `logic.rs` 仅做装配与映射，不重新实现状态原语。
- [x] 如果无异步相关，直接打勾。异步交互语义统一：`is_loading`、error/retry、disabled、`aria-busy` 映射一致；优先复用统一 async action 原语（如 `use_async_action`），禁止每组件自定义一套加载/错误协议。（N/A：`List` 当前仅本地集合导航与选择，不包含远程请求/异步加载状态。）
  - 无异步交互时需明确标注 N/A 理由（例如“组件无远程请求与异步状态”），不是机械打勾。
  - 有异步交互时，`is_loading`/disabled/`aria-busy`/retry 语义必须成套一致，且对键盘与读屏路径可用。
  - 异步失败态要有可恢复路径（重试或回退），并有语义测试覆盖。
- [x] API 易用性验收标准（DX Paradox）：把复杂性留在内部，把简单留给用户。（`List` 基础 API 仅要求 `id_base + items`，受控轴 `selected_index/on_selected_index_change/default_selected_index` 保持可选；docs 已提供最小默认路径 `apps/docs-app/src/pages/components/pages/collections.rs` 的 `Hello World (Uncontrolled)`，`README` 的 Hello World 同步为无状态接线示例。回归锁定：`components/list/test/semantics.rs::list_dx_hello_world_is_minimal_and_does_not_require_state_wiring` 与 `crates/ui-components/tests/list_module_semantics.rs::list_dx_hello_world_is_minimal_and_state_machine_free`。）
  - 基础用法不得要求用户先理解或手动接线 `ui-state-primitives`/`ui-headless` 状态机。
  - 基础组件 Hello World 示例代码不得超过 5 行（导入与外层模板按仓库约定不计），并可直接运行。
  - 简单需求走简单 API，复杂需求再暴露高级入口：默认 props 覆盖高频场景，高级控制通过受控/扩展参数按需开启。
  - 禁止把内部状态对象作为基础必填参数暴露（例如强制 `state=...` 才能完成点击/展开等基本交互）。
  - docs-app 必须提供最小可用示例，优先展示一眼可懂的默认调用路径。
- [x] 组合型组件主 API 必须“显示优于约定”：优先使用显式组合 `<Parent><Item ... /></Parent>`。（`List` 根 API 维持单一 `items: Arc<[String]>` 数据轴，不引入 `labels/titles/panels` 并行数组语法；组合面通过 `ListSection(children)` 与 `ListItem(children)` 保持显式结构（`components/list/src/view.rs`），docs 在 `apps/docs-app/src/pages/components/pages/collections_extra.rs` 提供 `<ListSection><ListItem .../></ListSection>` 示例。回归锁定：`components/list/test/semantics.rs::list_composite_api_avoids_parallel_array_conventions` 与 `crates/ui-components/tests/list_module_semantics.rs::list_composite_api_prefers_explicit_parent_item_structure`。）
  - 每个 item 的标题、语义与内容必须在同一 `Item` 结构维度绑定，避免索引配对式隐式约定。
  - `labels + children`、`titles + panels` 等并行数组/并行槽位写法不得作为默认或推荐 API。
  - 不引入这类语法糖：若为配置式输入，仅允许类型化 `ItemSpec`，并在内部映射为显式 `Item` 语义树。

### 3. 高级交互与物理机制（Shell/Physics）
- [x] 宏观/微观双状态机（Macro/Micro Duality）：拖拽等高频交互在 `Dragging` 期间由 `view/motion` 本地循环执行；禁止每帧穿越回 `logic.rs`，必须在结束时通过 `Action::DragEnd` 回流收敛。（N/A：`List` 当前交互仅 listbox 导航与 `pointermove/click`，动效仅 `active_highlight` attach，不存在拖拽语义轴与 `Dragging -> Action::DragEnd` 协议。回归锁定：`components/list/test/semantics.rs::list_macro_micro_dragging_contract_is_not_applicable_in_current_scope` 与 `crates/ui-components/tests/list_module_semantics.rs::list_macro_micro_dragging_contract_is_not_applicable_in_current_scope`。）
- [x] 几何两段式渲染（Two-Pass Rendering）：`Tooltip/Popover/Menu` 等依赖 DOM 测量的组件必须走 `Intent -> Measure(view) -> Rectification(logic)`，并具备幂等收敛保护防死循环。（N/A：`List` 当前仅 listbox 导航与 `active_highlight` 动效挂载，不涉及依赖 DOM 几何测量的 overlay/定位修正流程，因此不存在 `Intent -> Measure -> Rectification` 两段式收敛链路。回归锁定：`components/list/test/semantics.rs::list_two_pass_geometry_contract_is_not_applicable_in_current_scope` 与 `crates/ui-components/tests/list_module_semantics.rs::list_two_pass_geometry_contract_is_not_applicable_in_current_scope`。）
- [x] 集合注册协议（Registration Protocol）：`Accordion/Tabs/Menu` 动态子项必须通过 `RegistrationContext` 上报 `Register/Unregister`，逻辑层维护 `items_order`，禁止依赖 `HashSet` 迭代顺序做导航。（N/A：`List` 当前主 API 以 `items: Arc<[String]>` 快照输入并按索引枚举渲染，未提供动态子项挂载/卸载生命周期，因此不存在 `RegistrationContext -> Register/Unregister -> items_order` 协议面；`HashSet` 仅用于 `disabled_indices.contains(index)` 成员查询，不参与导航顺序。回归锁定：`components/list/test/semantics.rs::list_registration_protocol_is_not_applicable_in_current_scope` 与 `crates/ui-components/tests/list_module_semantics.rs::list_registration_protocol_is_not_applicable_in_current_scope`。）
- [x] 插槽投影策略（Slot Projection）：容器组件明确 `Lazy/KeepAlive/Eager`；`KeepAlive` 隐藏时必须通过生命周期通知（如 `NotifyHidden`）暂停轮询/动画等高耗能副作用。（N/A：`List` 当前为单层 listbox 渲染，`items` 直接枚举输出 option，不存在容器级 slot 投影策略切换（`Lazy/KeepAlive/Eager`）与隐藏生命周期通知链路；动效仅 `attach_motion` 高亮绑定，不承载后台轮询/动画驻留语义。回归锁定：`components/list/test/semantics.rs::list_slot_projection_contract_is_not_applicable_in_current_scope` 与 `crates/ui-components/tests/list_module_semantics.rs::list_slot_projection_contract_is_not_applicable_in_current_scope`。）
- [x] 环境订阅流（Env Streams）：`Resize/Theme/Intersection` 等环境变化在 `view.rs` 采样、防抖后转化为高层语义 `Action`（如 `BreakpointChanged`）推送到 `logic`；禁止原始事件洪泛。（N/A：`List` 当前无响应式布局断点、主题侦听或可见性观测语义轴；交互仅键盘/指针 listbox 导航与高亮动效挂载，不存在 `Resize/Theme/Intersection` 采样与防抖后回流 `logic Action` 的链路。回归锁定：`components/list/test/semantics.rs::list_env_streams_contract_is_not_applicable_in_current_scope` 与 `crates/ui-components/tests/list_module_semantics.rs::list_env_streams_contract_is_not_applicable_in_current_scope`。）
- [x] 事件光锥（Event Light Cone）：`Table/Grid` 等大型集合批量操作必须走 `Context Bus + Selector` 与状态压缩表达（如 `SelectionState::All`），禁止 O(N) 级向下 prop drilling。（N/A：`List` 当前是单 listbox 选择模型，状态轴为 `selected_index/active_index/disabled_indices`，不存在表格级批量选择、跨分区总线广播或 `SelectionState::All` 压缩态，因此不需要 `Context Bus + Selector` 光锥协议。回归锁定：`components/list/test/semantics.rs::list_event_light_cone_contract_is_not_applicable_in_current_scope` 与 `crates/ui-components/tests/list_module_semantics.rs::list_event_light_cone_contract_is_not_applicable_in_current_scope`。）
- [x] 统一因果总线（Causality Bus）：复杂派生总线操作必须支持透传 `TraceId`，确保“用户触发 -> 派生命令 -> 总线广播 -> 订阅者”因果链不断裂。（N/A：`List` 当前事件路径为 listbox 交互直接驱动 `on_action` 回调，不存在跨模块派生广播、订阅扇出或总线封包层，因此没有 `TraceId` 透传需求。回归锁定：`components/list/test/semantics.rs::list_causality_bus_contract_is_not_applicable_in_current_scope` 与 `crates/ui-components/tests/list_module_semantics.rs::list_causality_bus_contract_is_not_applicable_in_current_scope`。）
- [x] 存在 A11y 实现、国际化与本地化实现（至少具备接入点，不硬编码用户可见文本）。（`List` 主视图通过 `ui_headless::use_listbox/use_focus_ring + listbox_option_a11y_attrs` 挂载 `role/aria-*` 键盘语义；`lang/dir` 由 `ui_headless::a11y::locale_attrs` 透传；`ListItem` 读屏文案从硬编码迁移为 `selected_text/unselected_text` 可覆盖输入，并在 `logic::item::normalize_selection_status_text` 统一兜底。回归锁定：`components/list/test/semantics.rs::list_a11y_i18n_l10n_contract_is_mounted_and_text_is_overridable` 与 `crates/ui-components/tests/list_module_semantics.rs::list_a11y_i18n_l10n_contract_is_mounted_and_text_is_overridable`。）
  - 交互元素必须具备可验证语义：`role`/`aria-*`/键盘可达路径完整，且和 headless 契约一致。
  - 用户可见文本来源必须可覆盖：优先 props，其次应用注入（`UiRoot`/i18n bundle），最后组件兜底文案；禁止把业务可见文案硬编码在 `view.rs`。
  - 组件需透传或消费 `lang` / `dir`（LTR/RTL）上下文，不得假设单语言单方向。
  - 共享 A11y 工具优先来自 `crates/ui-headless/src/a11y.rs`，组件层不重复发明同名语义工具。
- [x] 状态可观测、可检索、可验证：使用稳定 `data-*` 与 `aria-*` 标记表达状态和来源。（`List` 根节点与 option/item/section 已统一输出稳定语义标记：状态轴 `data-state/data-selected/data-focused/data-disabled`，来源轴 `data-selection-mode/data-selection-value-source/data-default-selection-source/data-selection-change-source/data-interaction-source` 与 `data-aria-source/data-class-source/data-title-source`；交互焦点补充 `data-focus-visible`。来源值由 `logic::resolve_selection_source_state` 与 `ListInteractionSource::as_attr` 提供封闭集合（`controlled/uncontrolled`、`external/internal`、`provided/none`、`none/keyboard/pointer`），避免自由文本漂移。回归锁定：`components/list/test/semantics.rs::list_state_observability_contract_uses_stable_data_and_aria_markers` 与 `crates/ui-components/tests/list_module_semantics.rs::list_state_observability_contract_uses_stable_data_and_aria_markers`。）
  - 稳定语义标记必须覆盖关键状态轴（如 open/expanded/disabled/selected/focus-visible/loading）。
  - 状态来源必须可区分（受控/非受控、默认值/外部值、交互来源），通过稳定 marker 暴露而不是隐式推断。
  - 自动化选择器优先基于语义标记，不依赖 DOM 顺序、层级深度或临时 class 名。
  - 标记值应为封闭集合（可枚举），避免自由文本导致契约漂移。
- [x] 样式依赖显式状态（`data-*`/class），而非脆弱 DOM 结构猜测。（`List` 样式状态分支统一由 `data-*`/稳定 class 驱动：`styles.rs` 使用 `.ui-listbox__option[data-selected|data-disabled]`、`.ui-listbox-item[data-selected|data-focused|data-disabled|data-show-selection-indicator|data-has-divider]`、`.ui-listbox-section[data-tone|data-empty|data-disabled|data-sticky-heading|data-divided]`；未使用 `:nth-child/:nth-of-type` 等结构猜测。`view.rs` 无业务内联样式注入（无 `style=`/`style:` 路径），视觉状态切换由语义标记解释而非 DOM 巧合。回归锁定：`components/list/test/semantics.rs::list_styles_depend_on_explicit_state_markers_not_fragile_dom_guessing` 与 `crates/ui-components/tests/list_module_semantics.rs::list_styles_depend_on_explicit_state_markers_not_fragile_dom_guessing`。）
  - `styles.rs` 中状态分支选择器必须基于 `data-*`/`aria-*`/稳定 class，禁止用 `:nth-child`、深层级选择器猜测状态。
  - 运行时样式仅允许传递必要 CSS 变量（custom properties）；禁止把业务样式逻辑塞进 inline style。
  - 视觉状态切换必须可由语义标记直接解释，不能依赖“某节点是否恰好存在”。
- [x] 测试验证“语义契约”而不只验证视觉快照。（`List` 已以语义断言为主：`components/list/test/semantics.rs` 覆盖 `role/aria/data-*` 与来源 marker，`components/list/test/logic.rs` 覆盖受控/非受控与 disabled 分支，`view.rs` 键盘/指针路径由 `on:keydown` + `on:pointerdown/on:pointermove` 回归锁定；组件层无 `#[cfg(target_arch = "wasm32")]` 分叉，SSR/wasm 语义契约同源（按适用范围）。快照断言未作为主验收手段。回归锁定：`components/list/test/semantics.rs::list_semantic_contract_tests_cover_matrix_without_snapshot_only_assertions` 与 `crates/ui-components/tests/list_module_semantics.rs::list_semantic_contract_tests_cover_matrix_without_snapshot_only_assertions`。）
  - 至少存在语义测试覆盖关键状态与交互路径（role/aria/data-state/source markers）。
  - 测试矩阵必须覆盖关键分支：受控/非受控、disabled、键盘路径、指针路径、SSR/wasm 差异（按适用范围）。
  - 视觉快照只能作为补充，不得替代语义契约断言。
- [x] 组件文件职责正确：`mod.rs`（导出边界）、`logic.rs`（归一/派生/来源标记）、`styles.rs`（静态 token-first CSS）、`view.rs`（Leptos 结构 + headless 挂载）、`motion.rs`（动效契约 + attach）。（`mod.rs` 仅导出稳定 API 与本地语义测试挂载；`logic.rs` 负责归一/派生/来源标记且不含 `view!/headless` 挂载；`styles.rs` 保持 token-first 静态 CSS；`view.rs` 负责 Leptos 结构与 `ui-headless` 挂载，不直连 `ui-state-primitives`；`motion.rs` 仅做语义到动效 contract 映射并调用共享 driver。回归锁定：`components/list/test/semantics.rs::list_module_boundary_is_minimal_and_wires_local_semantics_tests`、`components/list/test/semantics.rs::list_layered_files_keep_ui_components_assembly_split`、`crates/ui-components/tests/list_module_semantics.rs::list_module_reexports_canonical_list_contracts`、`crates/ui-components/tests/list_module_semantics.rs::list_motion_contract_is_mapped_in_component_motion_layer`。）
  - `mod.rs` 只维护最小稳定导出面与 feature gate，不承载实现细节。
  - `logic.rs` 只做输入归一、状态派生、来源标记；禁止 DOM 操作和样式细节分支。
  - `styles.rs` 只包含 token-first 静态 CSS；禁止硬编码主题常量与业务语义文案。
  - `view.rs` 只做结构渲染与 headless 契约挂载；禁止隐藏关键状态决策。
  - `motion.rs` 只做组件语义到动效契约映射与 attach；禁止在组件内重写通用动效引擎。
- [x] `spec.rs` 只用于少数复杂组件（如 button），避免泛滥。（`List` 当前为简单装配组件，无稳定外部 Schema 契约与复杂配置固化需求；组件目录保持 `mod/logic/styles/view/motion` 主体，不引入 `src/spec.rs`。组件说明继续落在 `components/list/check2.md` 与 `components/list/src/README.md`。回归锁定：`components/list/test/semantics.rs::list_component_does_not_introduce_spec_rs_for_simple_scope` 与 `crates/ui-components/tests/list_module_semantics.rs::list_component_does_not_introduce_spec_rs_for_simple_scope`。）
  - 仅当组件存在稳定外部规范/Schema 契约或复杂配置固化需求时才引入 `spec.rs`。
  - 简单组件不得为了“形式统一”新增 `spec.rs`；说明文档应留在 `check2.md`/组件文档。
  - 新增 `spec.rs` 必须同步给出契约测试与版本演进说明。
- [x] 组件层遵循 token-first 静态样式契约：样式通过 `styles.rs` 聚合注入；运行时仅传必要 CSS 变量；不把 Utility-First/CSS-in-Rust 当组件库默认范式。（`List` 样式统一落在 `components/list/src/styles.rs` 并由 `crates/ui-components/src/css.rs` 在 `component-list` feature 下聚合注入（`CSS/ITEM_CSS/SECTION_CSS`），`UiRoot` 通过 `crate::css::push_components_css` 注入；视觉值使用 `var(--ui-*)`，未引入 `--ui-listbox-*` 私有 token 命名；组件源码不使用 Utility-First 类和 CSS-in-Rust 默认范式。回归锁定：`components/list/test/semantics.rs::list_styles_remain_token_first_and_avoid_utility_or_css_in_rust_contracts`、`crates/ui-components/tests/list_module_semantics.rs::list_component_styles_are_aggregated_by_css_rs_and_injected_via_ui_root`，并由 `crates/ui-components/tests/list_module_semantics.rs::list_theme_contract_is_token_first_and_ui_theme_backed` 维持 ui-theme SSOT 约束。）
  - 样式规则统一落在 `styles.rs`，由 `crates/ui-components/src/css.rs` 聚合并通过 `UiRoot` 注入。
  - 颜色/间距/圆角/阴影等视觉值必须来自 `var(--ui-*)`，禁止组件私有 token 体系。
  - Utility-First 仅作为 `apps/*` 应用层布局手段，不得反向污染组件库契约。
  - CSS-in-Rust 仅在有明确类型安全与构建成本净收益时作为例外采用。
- [x] 默认主题美学质量达标（Visual Desire）：以 HeroUI 现代审美为学习对标，默认主题不仅“可用”，还必须“第一眼可信”。（`List` 在 docs-app 提供默认主题展示基线：`展示：多场景对比` + `Workbench（展示 + Config + Code + CSS Test）`，并通过 `test_css_source=workbench_test_css` 绑定样式回放；组件样式层保留层次与反馈标记（`focus-visible/selected/disabled`），避免“仅可访问但粗糙”的退化。`Button/Input/Overlay` 的跨组件截图基线属于仓库级视觉治理，本单组件条目在 `List` 范围以内 docs baseline + CSS baseline 证据验收。回归锁定：`components/list/test/semantics.rs::list_visual_desire_baseline_is_documented_for_component_scope` 与 `crates/ui-components/tests/list_module_semantics.rs::list_visual_desire_baseline_is_documented_for_component_scope`。）
  - 默认主题需通过基础美学清单：信息层级清晰（字重/字号/间距）、对比与层次自然、交互反馈明确（hover/active/focus）。
  - docs-app 必须提供默认主题基线页面与截图基线，关键组件（Button/Input/Overlay）纳入视觉回归对比。
  - 禁止“可访问但粗糙”的最低可用心态：视觉退化（类似旧式 Bootstrap 观感）视为质量回归。
  - HeroUI 对标以“视觉语言与体验质量”对齐为目标，不做无差别 API 表层复制。
- [x] Tree Shaking 是一等能力：package 模式支持组件级 feature；source 模式天然裁剪；样式层同步裁剪，禁止无条件聚合全部 CSS，禁止破坏 DCE/LTO 的全量中央注册表。（`component-list` 在 `crates/ui-components/Cargo.toml` 已独立声明并仅依赖 `component-active_highlight + component-illustrated_message + dep:ui-list`；`crates/ui-components/src/lib.rs` 与 `crates/ui-components/src/css.rs` 均通过 `#[cfg(feature = "component-list")]` 条件导出/聚合 `List` 与其样式，避免未启用特性进入可达路径；`all-components` 汇总导出保持 feature-gated，不影响最小特性构建。回归锁定：`components/list/test/semantics.rs::list_tree_shaking_contract_uses_feature_gates_and_no_unconditional_registry_path` 与 `crates/ui-components/tests/list_module_semantics.rs::list_tree_shaking_contract_uses_component_feature_gates`。）
  - package 模式必须有组件级 feature（如 `component-accordion`）；未启用组件不得进入编译与链接路径。
  - `lib.rs` 与 `css.rs` 必须按 feature 条件导出/聚合，禁止无条件引用所有组件模块和 CSS 常量。
  - source 模式下仅引入需要的组件源码，不通过中央注册表维持全组件可达。
  - 任意“全量组件映射表/注册表”若导致不可达代码变可达，直接判不通过。
  - 验证命令（特性树）：`cargo tree -e features -p ui-components --no-default-features --features component-accordion,inject-css`，确认仅启用目标组件特性链。
  - 验证命令（反向依赖）：`cargo tree -e features -i ui-components -p web-demo`，检查是否被 `all-components` 或隐式特性全量拉起。
  - CI 检查（最小特性编译）：新增任务仅开启目标最小特性（示例：`cargo check -p ui-components --target wasm32-unknown-unknown --no-default-features --features component-accordion,inject-css`）。
  - CI 检查（体积预算）：对“最小特性构建产物”设定预算并阻断回归（可用固定阈值，如 `< 50KB`，或基于仓库基线的相对阈值）；不得只做编译通过而不做体积约束。
- [x] 类型系统 + 语义标记共同提供机器可读状态；关键输入空间受类型约束。（`List` 离散状态轴以类型建模：`ListOptionStateInput`、`ListSelectionSourceStateInput`、`ListInteractionSource` 等在 `logic.rs` 统一归一/派生并可测试；`view.rs` 通过稳定语义标记输出机器可读状态（如 `data-state`、`data-selection-mode/value-source/change-source`、`data-interaction-source`），避免字符串协议漂移与隐式推断。回归锁定：`components/list/test/semantics.rs::list_type_system_and_semantic_markers_form_machine_readable_contract` 与 `crates/ui-components/tests/list_module_semantics.rs::list_type_system_and_semantic_markers_form_machine_readable_contract`；基础分支覆盖由 `list_discrete_state_axes_are_type_constrained`、`list_state_normalization_is_concentrated_in_logic_layer`、`list_state_observability_contract_uses_stable_data_and_aria_markers` 保持。）
  - 离散输入与状态轴必须优先使用 `enum`/新类型建模，避免字符串协议与布尔爆炸。
  - 无效状态要么在类型层不可表达，要么在 `logic.rs` 被统一归一化并可测试。
  - 关键状态必须通过稳定语义标记对外可读，供测试与 Agent 自动化消费。
  - 编译器与测试反馈应能直接定位状态契约破坏点，形成可持续闭环。

### 4. DOM/环境边界治理
- [x] 焦点全局栈（Focus Stack & GC）：层叠 `Overlay` 禁止私存 `NodeRef` 作为恢复目标；必须依赖全局 Focus Manager（如 `FallbackTo/Selector`）防止焦点坠落到 `document.body`。（N/A：`List` 当前为 listbox 组件，不承载 overlay 层叠打开/关闭与焦点回收职责；组件内焦点语义仅由 `use_focus_ring` 本地挂载，不实现 `document.body` 回退或私有 `NodeRef` 恢复目标协议。若后续扩展到 overlay 场景，必须回迁到全局 Focus Manager 契约。回归锁定：`components/list/test/semantics.rs::list_focus_stack_and_gc_contract_is_not_applicable_in_current_scope` 与 `crates/ui-components/tests/list_module_semantics.rs::list_focus_stack_and_gc_contract_is_not_applicable_in_current_scope`。）
- [x] 受控外交特区（Escape Hatches）：集成 ECharts/Map 等命令式第三方库时必须处于 `Foreign Zone`（`YieldControl/CleanupForeign`）；第三方实例不得暴露为组件公共 API 或反向污染状态机。（N/A：`List` 当前只组合 `ui-state-primitives + ui-headless + ui-motion`，不存在 ECharts/Map 等命令式第三方实例接入，也没有对外暴露第三方实例句柄；状态机保持在组件内逻辑归一链路，不受外部 imperative instance 反向污染。回归锁定：`components/list/test/semantics.rs::list_escape_hatches_foreign_zone_contract_is_not_applicable_in_current_scope` 与 `crates/ui-components/tests/list_module_semantics.rs::list_escape_hatches_foreign_zone_contract_is_not_applicable_in_current_scope`。）
- [x] SSR 时空断裂治理（Hydration Discontinuity）：逻辑初始化禁止依赖 `now()` 或原生随机 UUID；必须通过 `IdProvider` 注入确定性种子，确保 SSR/Hydration 间 ID 稳定。（`List` 现改为 `id_base: Option<String>`，未显式传入时走 `use_ui_id_provider().next_prefixed_id(logic::DEFAULT_ID_BASE)`，再由 `logic::normalize_id_base` 归一为空白安全的稳定前缀；`UiRoot` 继续通过 `provide_ui_id_provider(id_seed)` 注入确定性种子链路，避免 SSR/Hydration 间 ID 漂移。组件源码未引入 `now()/UUID/rand` 等非确定性 ID 初始化路径。回归锁定：`components/list/test/semantics.rs::list_hydration_discontinuity_contract_uses_deterministic_id_provider_path` 与 `crates/ui-components/tests/list_module_semantics.rs::list_hydration_discontinuity_contract_uses_deterministic_id_provider_path`。）
- [x] SSR 与跨平台检查：覆盖 web/ssr/wasm 分支，不破坏 non-wasm 编译路径。（`List` 组件层源码（`mod/logic/view/styles/motion`）未直接引用 `web_sys/js_sys/window/document/wasm_bindgen` 等浏览器对象；平台差异由动效原语层显式 `cfg` 管理：`crates/ui-visual-primitive/src/active_highlight.rs` 与 `components/illustrated-message/src/motion.rs` 均提供 `#[cfg(target_arch = "wasm32")]` 与 `#[cfg(not(target_arch = "wasm32"))]` 双分支，non-wasm 路径为可预测 no-op/safe stub。compile-only 证据（当前环境受文件系统限制阻塞）：`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-platform cargo check -p ui-components --no-default-features --features component-list,inject-css`、`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-platform cargo check -p ui-components --target wasm32-unknown-unknown --no-default-features --features component-list,inject-css`、`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-platform cargo check -p ui-headless --no-default-features --features ssr` 均在依赖编译阶段报 `Invalid cross-device link (os error 18)`，属于环境问题非组件代码分支错误。回归锁定：`components/list/test/semantics.rs::list_ssr_cross_platform_contract_keeps_non_wasm_safe_and_cfg_explicit` 与 `crates/ui-components/tests/list_module_semantics.rs::list_ssr_cross_platform_contract_keeps_non_wasm_safe_and_cfg_explicit`。）
  - 至少包含 compile-only 证据：web（wasm32）、ssr（native）、默认本地构建三条路径。
  - 平台分支差异必须显式 `cfg` 或 feature 管理，禁止依赖运行时偶然行为。
  - non-wasm 路径禁止引用 `web-sys`/浏览器对象。
- [x] `ui-headless` web/ssr feature 互斥受 `compile_error!` 保护（`crates/ui-headless/src/lib.rs`）。（上游互斥保护仍存在：`crates/ui-headless/src/lib.rs` 保留 `#[cfg(all(feature = "web", feature = "ssr"))] compile_error!(...)`，可阻断同时启用 `web+ssr`；`List` 组件继续通过 `use_listbox/use_focus_ring/use_ui_id_provider` 消费 headless 能力，组件层未引入 `#[cfg(feature = "web/ssr")]` 分歧或重写互斥逻辑，不会破坏该约束。特性路径验证命令（当前环境受文件系统限制阻塞）：`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-headless-mut cargo check -p ui-headless --no-default-features --features web`、`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-headless-mut cargo check -p ui-headless --no-default-features --features ssr`、`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-headless-mut cargo check -p ui-headless --no-default-features --features web,ssr` 均在依赖编译阶段报 `Invalid cross-device link (os error 18)`，属于环境问题。互斥语义由源码级 `compile_error!` 与回归锁共同保障。回归锁定：`components/list/test/semantics.rs::list_ui_headless_web_ssr_mutual_exclusion_contract_is_preserved` 与 `crates/ui-components/tests/list_module_semantics.rs::list_ui_headless_web_ssr_mutual_exclusion_contract_is_preserved`。）
  - 组件依赖 `ui-headless` 能力时，不得破坏其 web/ssr 互斥约束。
  - 组件若新增 headless 功能接入，需验证两条 feature 路径都可编译。
  - 发现“同时启用 web+ssr 仍可过编译”视为契约回归。
- [x] `ui-motion` 非 wasm 提供 no-op/stub（`crates/ui-motion/src/lib.rs`），保证 SSR/tooling 可编译。（`crates/ui-motion/src/lib.rs` 已提供显式 non-wasm stub：`#[cfg(not(target_arch = "wasm32"))] pub mod web` 内 `prefers_reduced_motion() -> true` 与 `animate(..) {}` no-op，并有 `#[cfg(all(test, not(target_arch = "wasm32")))] non_wasm_web_backend_is_predictable_noop` 回归；`List` 组件侧 `components/list/src/motion.rs` 仅做 contract 映射并调用 `attach_motion/attach_section_motion`，不持有动画句柄、不依赖浏览器对象，在 non-wasm 路径安全降级且行为可预测。compile-only 证据（当前环境受文件系统限制阻塞）：`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-motion-noop cargo check -p ui-motion`、`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-motion-noop cargo check -p ui-components --no-default-features --features component-list,inject-css`、`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-motion-noop cargo check -p ui-components --target wasm32-unknown-unknown --no-default-features --features component-list,inject-css` 均在依赖编译阶段报 `Invalid cross-device link (os error 18)`，属于环境问题。回归锁定：`components/list/test/semantics.rs::list_ui_motion_non_wasm_noop_contract_is_preserved` 与 `crates/ui-components/tests/list_module_semantics.rs::list_ui_motion_non_wasm_noop_contract_is_preserved`。）
  - `motion.rs` 调用必须可在 non-wasm 下安全降级，不触发 panic。
  - 组件不得假设动画句柄一定存在；no-op 分支行为需可预测。
  - toolchain 场景（测试/文档/静态分析）不得因 motion 依赖阻塞编译。
- [x] 组件实现覆盖 `reduced-motion` / SSR / wasm 分支。（已落实：`crates/ui-motion/src/spring.rs` 在 `SpringAnimator::set_target` 中通过 `crate::web::prefers_reduced_motion()` 短路到目标值并同步触发 `apply/on_rest`，`reduced-motion` 下跳过连续帧动画；`components/list/src/motion.rs` 仅做 contract 映射，wasm 增强由 `crates/ui-visual-primitive/src/active_highlight.rs` 与 `components/illustrated-message/src/motion.rs` 的 `#[cfg(target_arch = "wasm32")]` 分支承载，non-wasm 路径显式降级；`components/list/src/view.rs` 未做平台条件分叉，`role/aria/data-*` 语义标记在 SSR/wasm 保持一致，且 `crates/ui-visual-primitive/src/active_highlight.rs` 的 `var(--ui-active-highlight-*, fallback)` 预置保证 SSR 首帧与 hydration 对齐。compile-only 证据（当前环境受文件系统限制阻塞）：`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-reduced cargo check -p ui-motion`、`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-reduced cargo check -p ui-components --no-default-features --features component-list,inject-css`、`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-reduced cargo check -p ui-components --target wasm32-unknown-unknown --no-default-features --features component-list,inject-css`、`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-reduced cargo test -p ui-list list_reduced_motion_ssr_wasm_branches_keep_semantic_contract_stable -- --nocapture` 均在依赖编译阶段报 `Invalid cross-device link (os error 18)`。回归锁定：`components/list/test/semantics.rs::list_reduced_motion_ssr_wasm_branches_keep_semantic_contract_stable` 与 `crates/ui-components/tests/list_module_semantics.rs::list_reduced_motion_ssr_wasm_branches_keep_semantic_contract_stable`。）
  - `reduced-motion` 下动画应跳过或降级为最小必要反馈。
  - SSR 输出必须与客户端 hydration 兼容，避免首帧语义错位。
  - wasm 分支允许增强交互，但语义契约不得与 SSR 分支分裂。
- [x] 性能治理：关键路径有预算（首次渲染/更新耗时/内存），回归可检测、可归因、可阻断。（已落实：`scripts/check-ui-components-performance.sh` 已纳入 `list_performance_governance_contract_is_budgeted_traceable_and_blocking_via_global_gates` 阻断命令，并继续覆盖 `button/input` 基线与 `docs_perf_probe_budgets_are_wired_for_component_pages`、`perf_render_count_follow_up_is_tracked_in_plan`；`apps/docs-app/src/pages/components/shell.rs` 在 `component_page_perf_budget` 为 `"list"` 显式定义 `UiPerfBudget { max_mount_ms: 34.0, max_update_ms: Some(11.0), max_heap_kb: Some(576.0) }`，并通过 `UiPerfProbe` 暴露 `data-perf-*` 可重复观测；`List` 交互/更新/动效路径在源码可归因（`on:keydown`、`on:pointerdown/on:pointermove`、`Effect::new` 同步、`attach_motion`）。当前框架对 `List` 尚无稳定精确 `render_count` 自动计数，按仓库策略使用 perf-probe 等价基线并由 `docs/plan/TODO.md` 持续跟踪 `render_count` 自动化补齐项。验证命令（当前环境受文件系统限制阻塞）：`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-perf cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_performance_governance_contract_is_budgeted_traceable_and_blocking_via_global_gates`、`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-perf cargo test -p ui-components --test accordion_semantics docs_perf_probe_budgets_are_wired_for_component_pages`、`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-perf cargo test -p ui-components --test accordion_semantics perf_render_count_follow_up_is_tracked_in_plan` 均在依赖编译阶段报 `Invalid cross-device link (os error 18)`。回归锁定：`components/list/test/semantics.rs::list_performance_governance_contract_is_budgeted_traceable_and_blocking_via_global_gates` 与 `crates/ui-components/tests/list_module_semantics.rs::list_performance_governance_contract_is_budgeted_traceable_and_blocking_via_global_gates`。）
  - 关键交互组件需定义最小预算项（首渲染、关键更新、内存/分配趋势）。
  - 回归检测至少具备可重复基线与失败阈值，不靠主观“感觉变慢”。
  - 性能问题需可归因到状态、渲染、样式或动效路径之一。
  - 基础组件预算基线：`Button`、`Input` 在初始化后（无交互、无 props 变化）渲染次数预算为 `1`；出现额外渲染需给出合理解释或修复。
  - 测试要求：在 `crates/ui-components/tests/*` 增加 `render_count` 类回归测试（测试框架支持时必须启用）；至少覆盖基础组件与本次改动组件。
  - 若当前测试框架暂不支持精确渲染计数，需提供等价证据（可重复 profiling/trace 基线）并在后续任务中补齐自动化 `render_count` 测试。
- [x] `view!` 宏复杂度受控：单个 `view!` 块不得承载超长深嵌套结构；复杂布局按语义分块，避免一次性宏展开导致编译与 wasm 体积劣化。（已落实：`view.rs` 已将 option 行渲染从主 `List` `view!` 中下沉到局部函数 `render_list_option(...)`，主块只保留根容器/语义标记/选项列表装配，避免 `map` 内联巨型 `view!`；当前 `components/list/src/view.rs` 的 `view!` 块数量受控（`List + render_list_option + ListItem + ListSection`）。宏展开门禁接入 `scripts/check-ui-components-view-macro.sh`，新增 `cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_view_macro_complexity_is_split_into_semantic_subrenders`。验证命令（当前环境受文件系统限制阻塞）：`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-view-macro cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_view_macro_complexity_is_split_into_semantic_subrenders`、`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-view-macro bash ./scripts/check-ui-components-view-macro.sh` 均在依赖编译阶段报 `Invalid cross-device link (os error 18)`。回归锁定：`components/list/test/semantics.rs::list_view_macro_complexity_is_split_into_semantic_subrenders` 与 `crates/ui-components/tests/list_module_semantics.rs::list_view_macro_complexity_is_split_into_semantic_subrenders`。）
  - 复杂结构按语义子块拆分（header/body/item 等），避免巨型单块 `view!`。
  - `view.rs` 中若出现多层嵌套重复片段，应优先提取局部渲染函数。
  - 编译时间/产物体积异常增长时，优先排查宏展开体量。
- [x] 函数式拆分优先：不涉及复杂状态与生命周期管理的 UI 片段，优先拆为普通 Rust 函数（返回 `impl IntoView`/`View`），而不是新增 `#[component]`。（已落实：`components/list/src/view.rs` 将轻逻辑 option 子片段下沉为普通函数 `render_list_option(...) -> impl IntoView`，由 `List` 主渲染在 `.map(|(index, label)| ...)` 中调用，避免把局部片段升格为新组件；同时仅保留 3 个公共 `#[component]` 边界（`List`/`ListItem`/`ListSection`），未引入额外局部 `#[component]` 噪音。语义标记稳定性由 `render_list_option` 内 `role/aria-*` 与 `data-state/data-selected/data-focused/data-disabled` 挂载保持，并受语义回归锁约束。门禁接入：`scripts/check-ui-components-view-macro.sh` 新增 `cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_view_functional_split_prefers_plain_functions_over_local_components`。验证命令（当前环境受文件系统限制阻塞）：`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-function-split cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_view_functional_split_prefers_plain_functions_over_local_components`、`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-function-split bash ./scripts/check-ui-components-view-macro.sh` 均在依赖编译阶段报 `Invalid cross-device link (os error 18)`。回归锁定：`components/list/test/semantics.rs::list_view_functional_split_prefers_plain_functions_over_local_components` 与 `crates/ui-components/tests/list_module_semantics.rs::list_view_functional_split_prefers_plain_functions_over_local_components`。）
  - 纯静态或轻逻辑片段优先函数化；仅在需要独立 props 语义时升级为组件。
  - 禁止把所有局部片段都升格为 `#[component]` 导致抽象噪音。
  - 拆分后语义标记与测试定位仍需稳定。
- [x] 静态片段常量化：复杂 SVG、页脚、长说明文本等纯静态内容优先常量化/模板化，减少重复 `view!` 渲染指令生成。（已落实：`components/list/src/view.rs` 将纯静态片段 class/slot 路径集中到常量（`LISTBOX_HIGHLIGHT_CLASS/SLOT`、`LIST_ITEM_DIVIDER_CLASS/SLOT`、`LIST_SECTION_DIVIDER_CLASS/SLOT`），并在 `List`/`ListItem`/`ListSection` 渲染中统一通过常量挂载，避免静态标记散落在多个 `view!` 片段中；常量化后语义保持不变（`data-slot` 与结构角色未改写），满足“可访问语义不回退”要求。门禁接入：`scripts/check-ui-components-view-macro.sh` 新增 `cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_static_fragments_are_constantized_with_stable_a11y_markers`。验证命令（当前环境受文件系统限制阻塞）：`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-static-fragments cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_static_fragments_are_constantized_with_stable_a11y_markers`、`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-static-fragments bash ./scripts/check-ui-components-view-macro.sh` 均在依赖编译阶段报 `Invalid cross-device link (os error 18)`。回归锁定：`components/list/test/semantics.rs::list_static_fragments_are_constantized_with_stable_a11y_markers` 与 `crates/ui-components/tests/list_module_semantics.rs::list_static_fragments_are_constantized_with_stable_a11y_markers`。）
  - 可判定为纯静态的片段应避免重复动态构造。
  - 常量化后仍需维持可访问语义（title/aria-label/role 等）。
  - 静态资源变更路径要清晰，避免散落在多个 `view!` 片段中。
- [x] `inner_html` 使用约束：仅允许注入受信任静态常量，禁止拼接用户输入；使用处必须补充语义与安全回归测试。（N/A：`List` 当前为零注入面，`components/list/src/{mod,logic,styles,view,motion}.rs` 与 docs `collections.rs` 的 `list()` 区段均无 `inner_html/set_inner_html/dangerously_set_inner_html`，不存在用户输入拼接 HTML 或远端模板注入路径。门禁接入：`scripts/check-ui-components-inner-html.sh` 新增 `cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_inner_html_usage_is_explicitly_na_and_guarded`。验证命令（当前环境受文件系统限制阻塞）：`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-inner-html cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_inner_html_usage_is_explicitly_na_and_guarded`、`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-inner-html bash ./scripts/check-ui-components-inner-html.sh` 均在依赖编译阶段报 `Invalid cross-device link (os error 18)`。回归锁定：`components/list/test/semantics.rs::list_inner_html_usage_is_explicitly_na_and_guarded_locally` 与 `crates/ui-components/tests/list_module_semantics.rs::list_inner_html_usage_is_explicitly_na_and_guarded`。）
  - 仅允许编译期常量或明确白名单内容进入 `inner_html`。
  - 严禁直接或间接注入用户输入、远端返回或未清洗模板字符串。
  - 使用 `inner_html` 的节点必须补语义测试与安全回归说明。
- [x] WASM 调试要求：关键状态可追踪（来源/时间/前后值），关键交互可回放，开发模式有可视化入口，调试能力通过 feature 隔离不污染产物。（本组件判定：N/A（组件级不自建 wasm 调试/回放管线）。`ui-list` crate 仅保留 `default = []`，不引入 `wasm-debug` 私有 feature；共享调试能力继续由 `crates/ui-components/src/lib.rs` 的 `wasm_debug_proxy`、`apps/docs-app/src/lib.rs` 的 `debug_overlay_enabled` 入口与 `crates/ui-headless/src/trace.rs` 的时间戳事件结构统一提供。`List` 组件侧保持可追踪/可回放最小语义链：`data-selection-* + data-interaction-source` 与键盘/指针事件路径（`on:keydown/on:pointerdown/on:pointermove`），docs `collections.rs` 的 `list()` 保留 Workbench 可视化入口用于复现状态转移；同时组件源码不泄露 `trace.emit/debug_overlay/request_replay` 等调试内部接口到公共 API。门禁接入：`scripts/check-ui-components-wasm-debug.sh` 新增 `cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_wasm_debug_contract_is_explicitly_na_and_feature_isolated`。验证命令（当前环境受文件系统限制阻塞）：`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-wasm-debug cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_wasm_debug_contract_is_explicitly_na_and_feature_isolated`、`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-wasm-debug bash ./scripts/check-ui-components-wasm-debug.sh` 均在依赖编译阶段报 `Invalid cross-device link (os error 18)`。回归锁定：`components/list/test/semantics.rs::list_wasm_debug_contract_is_explicitly_na_and_feature_isolated` 与 `crates/ui-components/tests/list_module_semantics.rs::list_wasm_debug_contract_is_explicitly_na_and_feature_isolated`。）
  - 开发模式下至少能追踪关键状态变更来源与前后值。
  - 关键交互链路应支持最小可复现记录（事件顺序/状态转移）。
  - 调试开关默认不进入生产包体与公共 API。
- [x] DX 要求：样式热重载优先无需重编 wasm；组件热开发尽量保持上下文；提供可选状态保留；有 Workbench 隔离画布。（本组件已提供 Workbench 隔离画布（含 settings/code/css-test）：`apps/docs-app/src/pages/components/pages/collections.rs` 的 `list()` 已接入 `Playground + test_css_source=workbench_test_css + test_config_signal=workbench_actual_config`，并通过 `data-slot=\"list-workbench-controls\"/\"list-workbench-canvas\"` 固化隔离演练入口；交互上下文在同画布连续保持（`selected` 文本、`set_workbench_selected`、`set_workbench_sync_active/disable_last/root_disabled/custom_class` 切换链路）。常见样式调整走 CSS Test 面板即可回放，不依赖完整 wasm 重编译。可选状态保留在 List scope 判定为 N/A（不引入本地持久化存储），避免把 `localStorage/sessionStorage` 注入组件 DX 基线。门禁接入：`scripts/check-ui-components-dx.sh` 新增 `cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_dx_playground_supports_css_hot_reload_and_isolated_canvas_with_optional_persist_na`。验证命令（当前环境受文件系统限制阻塞）：`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-dx cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_dx_playground_supports_css_hot_reload_and_isolated_canvas_with_optional_persist_na`、`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-dx bash ./scripts/check-ui-components-dx.sh` 均在依赖编译阶段报 `Invalid cross-device link (os error 18)`。回归锁定：`components/list/test/semantics.rs::list_dx_playground_supports_css_hot_reload_and_isolated_canvas_with_optional_persist_na` 与 `crates/ui-components/tests/list_module_semantics.rs::list_dx_playground_supports_css_hot_reload_and_isolated_canvas_with_optional_persist_na`。）
  - 常见样式调整应走快速反馈路径，不依赖完整 wasm 重编译。
  - 组件调试应尽量保持当前交互上下文，降低重复操作成本。
  - 复杂交互组件应有隔离演练入口（workbench/story/demo 之一）。
- [x] 工程能力统一：`serde` 负责 spec 序列化/版本迁移/错误结构化；`tracing` 统一 span/event 语义；async 不绑定单一运行时（tokio/async-std），runtime 细节不泄露到上层 API。（`List` 在 `components/list/src/protocol.rs` 保持结构化 serde 协议（`ListComponentSchemaVersion/ListComponentSpec` + `#[serde(default)]`），用于 spec/schema 版本字段的稳定反序列化；组件实现层（`mod/logic/view/styles/motion`）未引入组件私有 tracing 事件或 `target: \"ui_components::list::*\"` 语义漂移，继续复用仓库统一 tracing 基线（`button-wasm-debug`/`accordion-wasm-debug`）并避免每组件各自定义；异步边界保持 runtime-agnostic，公共组件面无 `tokio/async-std/smol/runtime::Handle` 暴露。门禁接入：`scripts/check-ui-components-engineering.sh` 新增 `cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_engineering_contract_uses_serde_protocol_and_keeps_tracing_runtime_boundaries`。验证命令（当前环境受文件系统限制阻塞）：`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-engineering cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_engineering_contract_uses_serde_protocol_and_keeps_tracing_runtime_boundaries`、`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-engineering bash ./scripts/check-ui-components-engineering.sh` 均在依赖编译阶段报 `Invalid cross-device link (os error 18)`。回归锁定：`components/list/test/semantics.rs::list_engineering_contract_uses_serde_protocol_and_keeps_tracing_runtime_boundaries` 与 `crates/ui-components/tests/list_module_semantics.rs::list_engineering_contract_uses_serde_protocol_and_keeps_tracing_runtime_boundaries`。）
  - 若组件涉及 spec/config 输入，序列化与错误输出应走统一结构化路径。
  - 关键流程埋点语义应与全库 tracing 约定一致，避免组件各说各话。
  - 异步边界不得把具体 runtime 类型暴露到组件公共接口。

### 5. 样式与动效（Theme & Motion）
- [x] 样式孤岛防御（Defensive Variables）：`styles.rs` 使用双层回退链 `var(--ui-*, var(--ui-fallback-*))`；禁止组件内硬编码 Hex 或裸尺寸终值，Fallback 终值由 `ui-theme` 统一输出（SSOT）。（`components/list/src/styles.rs` 已将样式终值收敛到双层回退链（如 `var(--ui-space-sm, var(--ui-fallback-space-sm))`、`var(--ui-border-width, var(--ui-fallback-border-width))`、`var(--ui-bg, var(--ui-fallback-bg))`、`var(--ui-motion-duration-medium, var(--ui-fallback-text-field-motion-duration))`），并移除组件内 Hex 与裸 `px` 终值；Fallback 终值统一来自 `crates/ui-theme/src/css.rs` 输出的 `--ui-fallback-*` 变量（SSOT）。门禁接入：`scripts/check-ui-components-contract-hygiene.sh` 新增 `cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_styles_use_defensive_variable_fallback_chain_with_ui_theme_ssot_terminals`。验证命令（当前环境受文件系统限制阻塞）：`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-defensive-vars cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_styles_use_defensive_variable_fallback_chain_with_ui_theme_ssot_terminals`、`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-defensive-vars bash ./scripts/check-ui-components-contract-hygiene.sh` 均在依赖编译阶段报 `Invalid cross-device link (os error 18)`。回归锁定：`components/list/test/semantics.rs::list_styles_use_defensive_variable_fallback_chain_with_ui_theme_ssot_terminals` 与 `crates/ui-components/tests/list_module_semantics.rs::list_styles_use_defensive_variable_fallback_chain_with_ui_theme_ssot_terminals`。）
- [x] 级联层覆盖（`@layer ui`）：组件 CSS 默认聚合进 `@layer ui`；运行时数值调整仅通过 CSS Custom Properties（如 `style:--x=...`），禁止普通内联样式（如 `style=\"top: 10px\"`）。（已核验（聚合层）：`crates/ui-components/src/css.rs::push_components_css` 使用 `out.push_str("\n@layer ui {\n")` 包裹组件样式并在末尾闭合，且 `component-list` 仅注入 `crate::list::styles::{CSS, ITEM_CSS, SECTION_CSS}`。已核验（运行时样式边界）：`components/list/src/view.rs` 不含 `style=`/`style:\"top`/`style:\"left` 等普通内联样式写法。N/A（list，运行时数值注入）：当前 `List/ListItem/ListSection` 无运行时动态样式写入路径，后续若引入仅允许 CSS 自定义变量注入（`style:--ui-*`）。门禁接入：`scripts/check-ui-components-contract-hygiene.sh` 新增 `cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_cascade_layer_contract_is_aggregated_in_ui_layer_and_rejects_plain_inline_style_rules`。验证命令（当前环境受文件系统限制阻塞）：`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-cascade-layer cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_cascade_layer_contract_is_aggregated_in_ui_layer_and_rejects_plain_inline_style_rules`、`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-cascade-layer bash ./scripts/check-ui-components-contract-hygiene.sh` 均在依赖编译阶段报 `Invalid cross-device link (os error 18)`。回归锁定：`components/list/test/semantics.rs::list_cascade_layer_contract_is_aggregated_in_ui_layer_and_rejects_plain_inline_style_rules` 与 `crates/ui-components/tests/list_module_semantics.rs::list_cascade_layer_contract_is_aggregated_in_ui_layer_and_rejects_plain_inline_style_rules`。）
- [x] Motion 合同化：`stiffness`/`damping` 等参数在 `motion.rs` 内置为组件 Contract，并通过 `attach_motion` 挂载；必须尊重 `prefers-reduced-motion` 且在 non-wasm/SSR 安全降级（no-op）。（`components/list/src/motion.rs` 已以组件合同固化 `SpringConfig` 清洗：`sanitize_spring` 对 `stiffness/damping/mass/precision` 非法值回退默认，并通过 `sanitize_motion + resolve_motion` 输出稳定合同；`view.rs` 仅通过 `crate::motion::attach_motion(...)` 与 `attach_section_motion(...)` 挂载，不在组件层重写执行器。`reduced-motion` 与 non-wasm/SSR 降级依赖统一底座：`crates/ui-motion/src/spring.rs` 在 `prefers_reduced_motion()` 下短路到目标值，`crates/ui-motion/src/lib.rs` 的 non-wasm `web::animate` 为 no-op stub。门禁接入：`scripts/check-ui-components-platforms.sh` 新增 `cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_motion_contract_is_component_scoped_reduced_motion_aware_and_non_wasm_safe`。验证命令（当前环境受文件系统限制阻塞）：`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-motion-contract cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_motion_contract_is_component_scoped_reduced_motion_aware_and_non_wasm_safe`、`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-motion-contract bash ./scripts/check-ui-components-platforms.sh` 均在依赖编译阶段报 `Invalid cross-device link (os error 18)`。回归锁定：`components/list/test/semantics.rs::list_motion_contract_is_component_scoped_reduced_motion_aware_and_non_wasm_safe` 与 `crates/ui-components/tests/list_module_semantics.rs::list_motion_contract_is_component_scoped_reduced_motion_aware_and_non_wasm_safe`。）
- [x] `ui-components` 固定入口文件落点正确。（已核验（入口文件）：`crates/ui-components/src/lib.rs` 以 `#[cfg(feature = "component-list")] pub use ui_list as list;` 暴露公共 API，`crates/ui-components/src/css.rs` 通过 `push_components_css` 在 `inject-css + component-list` 条件下聚合 `crate::list::styles::{CSS, ITEM_CSS, SECTION_CSS}`，`crates/ui-components/src/root.rs` 的 `UiRoot` 统一注入 base css/theme vars/components css 并提供 i18n + id provider。已核验（共享原语边界）：`crates/ui-visual-primitive/src/active_highlight.rs` 保持通用高亮样式与 motion driver，不承载 `List` 业务语义。已核验（禁止入口文件）：`crates/ui-components/src/overlay_open.rs`、`crates/ui-components/src/presence.rs`、`crates/ui-components/src/a11y.rs` 不存在，open/presence/a11y 原语固定由 `crates/ui-headless/src/{controllable_state,presence,a11y}.rs` 提供。门禁接入：`scripts/check-ui-components-entrypoints.sh` 新增 `cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_ui_components_fixed_entry_files_follow_layered_boundaries`。验证命令（当前环境受文件系统限制阻塞）：`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-entrypoints cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_ui_components_fixed_entry_files_follow_layered_boundaries`、`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-entrypoints bash ./scripts/check-ui-components-entrypoints.sh` 均在依赖编译阶段报 `Invalid cross-device link (os error 18)`。回归锁定：`components/list/test/semantics.rs::list_ui_components_fixed_entry_files_follow_layered_boundaries` 与 `crates/ui-components/tests/list_module_semantics.rs::list_ui_components_fixed_entry_files_follow_layered_boundaries`。）
  - `crates/ui-components/src/lib.rs`：总模块入口 + 对外 `pub use`（公共 API 面）；组件模块受 `component-*` feature gate 约束；不暴露内部平台细节类型。
  - `crates/ui-components/src/css.rs`：组件 CSS 聚合入口（`push_components_css`）；按 feature 条件注入；禁止无条件聚合全部组件 CSS。
  - `crates/ui-components/src/root.rs`：`UiRoot` 统一注入 base css + theme vars +（可选）components css，并提供全局 i18n 上下文；主题与注入策略必须集中在此。
  - `crates/ui-visual-primitive/src/active_highlight.rs`：共享高亮条样式与 motion driver；只承载通用高亮动效能力，不承载具体组件业务语义。
  - `crates/ui-components/src/overlay_open.rs`：当前仓库中不应存在；open-state 原语固定在 `crates/ui-headless/src/controllable_state.rs`，组件通过 headless API 消费。
  - `crates/ui-components/src/presence.rs`：当前仓库中不应存在；presence 原语固定在 `crates/ui-headless/src/presence.rs`，组件通过 `ui_headless::use_presence` 消费。
  - `crates/ui-components/src/a11y.rs`：当前仓库中不应存在；共享 A11y 工具固定在 `crates/ui-headless/src/a11y.rs`（如 `aria_controls_when_open`），组件只负责挂载。
- [x] 组件目录标准文件落点正确。（`components/list/src` 已保持标准文件集：`mod.rs`、`logic.rs`、`styles.rs`、`view.rs`、`motion.rs` 存在；`render.rs` 与 `spec.rs` 在当前简单组件范围内均不存在。`mod.rs` 仅保留最小稳定导出（`List/ListItem/ListSection` 与必要类型重导出），未过度暴露内部模块；`logic.rs` 维持 props 归一与状态派生，不承载 `view!/headless` 挂载；`styles.rs` 保持 token-first 静态 CSS；`view.rs` 仅做 Leptos 结构渲染与 headless 语义挂载并显式输出关键状态标记；`motion.rs` 仅做 `XxxMotion + attach_motion` 合同映射与挂载。门禁接入：`scripts/check-ui-components-component-files.sh` 新增 `cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_component_directory_has_standard_file_layout`。验证命令（当前环境受文件系统限制阻塞）：`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-component-files cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_component_directory_has_standard_file_layout`、`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-component-files bash ./scripts/check-ui-components-component-files.sh` 均在依赖编译阶段报 `Invalid cross-device link (os error 18)`。回归锁定：`components/list/test/semantics.rs::list_component_directory_has_standard_file_layout` 与 `crates/ui-components/tests/list_module_semantics.rs::list_component_directory_has_standard_file_layout`。）
  - `<component>/mod.rs`：最小稳定导出面，存在且无过度导出。
  - `<component>/logic.rs`：props 归一化、派生状态、来源标记；不得承载可下沉原语。
  - `<component>/styles.rs`：静态 CSS 契约，只用 `var(--ui-*)`，不写死主题常量。
  - `<component>/view.rs`：纯 Leptos 结构渲染 + headless 语义挂载；禁止 `render.rs` 漂移；不隐藏关键状态决策。
  - `<component>/motion.rs`：`XxxMotion + attach_motion`；交互组件必须有；只做语义到 motion contract 的映射与挂载。
  - `<component>/spec.rs`：仅极少数组件专用（当前主要 button），无必要不新增。

### 6. AI 原生能力与文件落点（Struct-First & Projection）
- [x] 文件落点纪律：组件目录严格由 `mod.rs`（导出）、`logic.rs`（归一派生）、`styles.rs`（Token 样式）、`view.rs`（渲染）、`motion.rs`（动效）组成；复杂组件可选 `spec.rs`；禁止 `render.rs`。（已核验 `components/list/src` 仅包含 `mod.rs`、`logic.rs`、`styles.rs`、`view.rs`、`motion.rs` 五件套；`render.rs` 不存在，且简单组件范围不引入 `spec.rs`。职责边界与回归锁定复用既有门禁：`components/list/test/semantics.rs::list_component_directory_has_standard_file_layout`、`crates/ui-components/tests/list_module_semantics.rs::list_component_directory_has_standard_file_layout`，以及 `scripts/check-ui-components-component-files.sh` 中对应 `cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_component_directory_has_standard_file_layout`。）
- [x] Hyper-Structure Builder（`spec.rs`）：复杂组件必须提供 AI 友好的 `*Spec::new()...render()` 建造者 API。（N/A：`List` 当前为简单装配组件，不引入 `spec.rs`，因此不存在 `*Spec::new()...render()` 建造者入口需求。复杂组件场景约束已由仓库规则固定在 `button` 等少数组件。回归证据复用：`components/list/test/semantics.rs::list_component_does_not_introduce_spec_rs_for_simple_scope`、`crates/ui-components/tests/list_module_semantics.rs::list_component_does_not_introduce_spec_rs_for_simple_scope`。）
- [x] 上下文压缩协议（Manifest + RBI）：新增/大改组件必须同步维护组件目录下 `Component.toml`（能力清单）和 `.rbi`（接口签名投影），避免 AI 检索工具箱过时。（已新增并落点固定：`components/list/src/Component.toml`（能力清单）与 `components/list/src/list.rbi`（接口签名投影）。`Component.toml` 记录 `List` 的输入轴、输出语义标记、`context_compression_manifest/rbi_signature_projection` 能力与分层依赖；`list.rbi` 投影 `List/ListItem/ListSection` 公共签名与关键类型别名，避免 AI 检索漂移。门禁接入：`scripts/check-ui-components-component-files.sh` 新增 `cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_context_compression_manifest_and_rbi_projection_are_present_and_current`。验证命令（当前环境受文件系统限制阻塞）：`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-manifest-rbi cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_context_compression_manifest_and_rbi_projection_are_present_and_current`、`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-manifest-rbi bash ./scripts/check-ui-components-component-files.sh` 均在依赖编译阶段报 `Invalid cross-device link (os error 18)`。回归锁定：`components/list/test/semantics.rs::list_context_compression_manifest_and_rbi_projection_are_present_and_current` 与 `crates/ui-components/tests/list_module_semantics.rs::list_context_compression_manifest_and_rbi_projection_are_present_and_current`。）
- [x] 语义标记统一升级为 Agent Contract（Schema 化），让 Agent 不依赖 DOM 猜测理解组件状态与意图。（已落实（类型化契约）：`components/list/src/logic.rs` 新增 `ListAgent*` 枚举与 `resolve_agent_contract(ListAgentContractInput)`，以类型约束生成 `schema/intent/action/state/source`；`components/list/src/view.rs` 统一挂载 `data-ui-schema/data-ui-schema-version/data-ui-intent/data-ui-action/data-ui-state/data-ui-source/data-ui-stream-support/data-ui-stream-fallback/data-ui-output-status/data-ui-config-policy`，不通过自由字符串拼接构造。已落实（Schema 与白名单）：`components/list/src/Component.toml` 新增 `[agent_contract]`、`[[agent_contract_markers]]` 与 `[[agent_contract_whitelist]]`（`allowed` 渲染路径 + `blocked` 注入令牌），并在 `components/list/src/list.rbi` 投影 `ListAgent*` 类型与 `resolve_agent_contract` 签名。门禁接入：`scripts/check-ui-components-contract-hygiene.sh` 新增 `list_check2_documents_agent_contract_schema_governance_rules`、`list_agent_contract_is_schema_typed_and_machine_readable`、`list_agent_contract_render_path_is_whitelist_safe_and_script_injection_free` 三条命令。验证命令（当前环境受文件系统限制阻塞）：`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-agent-contract cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_check2_documents_agent_contract_schema_governance_rules`、`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-agent-contract cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_agent_contract_is_schema_typed_and_machine_readable`、`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-agent-contract cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_agent_contract_render_path_is_whitelist_safe_and_script_injection_free`、`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-agent-contract bash ./scripts/check-ui-components-contract-hygiene.sh` 均在依赖编译阶段报 `Invalid cross-device link (os error 18)`。回归锁定：`components/list/test/semantics.rs::list_check2_documents_agent_contract_schema_governance_rules`、`components/list/test/semantics.rs::list_agent_contract_is_schema_typed_and_machine_readable`、`components/list/test/semantics.rs::list_agent_contract_render_path_is_whitelist_safe_and_script_injection_free`、`crates/ui-components/tests/list_module_semantics.rs::list_check2_documents_agent_contract_schema_governance_rules`、`crates/ui-components/tests/list_module_semantics.rs::list_agent_contract_is_schema_typed_and_machine_readable`、`crates/ui-components/tests/list_module_semantics.rs::list_agent_contract_render_path_is_whitelist_safe_and_script_injection_free`。）
  - 关键交互组件必须输出稳定机器可读语义（至少 `data-*` + 状态来源标记；复杂组件建议补 `data-ui-schema`）。
  - Agent 消费字段应来自类型化 schema 生成，不允许散落字符串拼接。
  - 契约字段需可追溯到组件状态轴与动作语义（intent/action/state/source）。
  - 配置到组件的渲染链路必须走白名单能力边界，禁止任意脚本注入。
- [x] 流式在这里仅指 LLM 输出渲染（只看两种显示模式）。（已明确限定术语边界：`Streaming` 仅指 LLM 正在生成时边产出边渲染；`Snapshot` 仅指 LLM 完整生成后一次性渲染，避免把网络请求进度/普通异步加载误记为 streaming。门禁接入：`scripts/check-ui-components-streaming.sh` 新增 `cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_check2_documents_streaming_definition_is_llm_output_only_with_two_modes`。验证命令（当前环境受文件系统限制阻塞）：`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-streaming-definition cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_check2_documents_streaming_definition_is_llm_output_only_with_two_modes`、`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-streaming-definition bash ./scripts/check-ui-components-streaming.sh` 均在依赖编译阶段报 `Invalid cross-device link (os error 18)`。回归锁定：`components/list/test/semantics.rs::list_check2_documents_streaming_definition_is_llm_output_only_with_two_modes` 与 `crates/ui-components/tests/list_module_semantics.rs::list_check2_documents_streaming_definition_is_llm_output_only_with_two_modes`。）
  - `Streaming`：LLM 还在生成，界面边生成边显示。
  - `Snapshot`：LLM 全部生成完成后，一次性显示。
- [x] `Snapshot` 是所有组件的基础能力（默认必须支持）。（`List` 继续以完整快照输入 `items: Arc<[String]>` 作为基线路径并稳定渲染（`components/list/src/view.rs`），`Component.toml` 明确 `snapshot_rendering` 能力开启且 `items` 为 required，`list.rbi` 同步投影该输入契约。门禁接入：`scripts/check-ui-components-streaming.sh` 新增 `cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_check2_documents_snapshot_as_default_baseline_capability` 与 `cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_snapshot_baseline_consumes_complete_result_and_renders_stably`。验证命令（当前环境受文件系统限制阻塞）：`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-snapshot-baseline cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_check2_documents_snapshot_as_default_baseline_capability`、`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-snapshot-baseline cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_snapshot_baseline_consumes_complete_result_and_renders_stably`、`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-snapshot-baseline bash ./scripts/check-ui-components-streaming.sh` 均在依赖编译阶段报 `Invalid cross-device link (os error 18)`。回归锁定：`components/list/test/semantics.rs::list_check2_documents_snapshot_as_default_baseline_capability`、`components/list/test/semantics.rs::list_snapshot_baseline_consumes_complete_result_and_renders_stably`、`crates/ui-components/tests/list_module_semantics.rs::list_check2_documents_snapshot_as_default_baseline_capability`、`crates/ui-components/tests/list_module_semantics.rs::list_snapshot_baseline_consumes_complete_result_and_renders_stably`。）
  - 所有组件都应能消费“完整生成结果”并稳定渲染。
  - 即使组件不直接展示正文，也应能在接收上层完整配置后正常渲染。
- [x] `Streaming` 是否强制，按组件职责判断（不能一刀切）。（`List` 在当前职责下归类为 `Streaming Optional`，以 `Snapshot` 渲染为基线并固定 `fallback=snapshot`：`components/list/src/logic.rs` 通过 `ListAgentStreamSupport::Optional + ListAgentStreamFallback::Snapshot + ListAgentOutputStatus::Verified` 输出类型化契约，`components/list/src/view.rs` 挂载 `data-ui-stream-support/data-ui-stream-fallback/data-ui-output-status`，`components/list/src/Component.toml` 同步约束 `optional/snapshot/verified` 封闭值集合。无论是否启用 streaming，`List` 均通过上述 `role/aria-* / data-*` 连续标记维持可读状态；数据校验、断线恢复、重试策略由上层负责，组件层只负责稳定渲染。门禁接入：`scripts/check-ui-components-streaming.sh` 新增 `cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_check2_marks_streaming_scope_as_optional_with_snapshot_fallback`。验证命令（当前环境受文件系统限制阻塞）：`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-streaming-governance cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_check2_marks_streaming_scope_as_optional_with_snapshot_fallback`、`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-streaming-governance bash ./scripts/check-ui-components-streaming.sh` 均在依赖编译阶段报 `Invalid cross-device link (os error 18)`。回归锁定：`components/list/test/semantics.rs::list_check2_marks_streaming_scope_as_optional_with_snapshot_fallback` 与 `crates/ui-components/tests/list_module_semantics.rs::list_check2_marks_streaming_scope_as_optional_with_snapshot_fallback`。）
  - `Streaming Required`：组件本体就是正文阅读面，用户需要边生成边看。
  - `Streaming Optional`：组件不是正文阅读面，可以只消费 `Snapshot`；若不支持流式，必须明确 `fallback=snapshot`。
  - 无论是否支持 `Streaming`，都要显式标识当前输出状态（草稿/已验证/可提交），并保持 `role`/`aria-*`/`data-*` 连续可读。
  - 数据校验、断线恢复、重试策略由上层负责，组件层只负责稳定渲染。

### 7. 测试、门禁与交付
- [x] 代码卫生（Rust Hygiene）：非测试代码中完全禁止 `unwrap/expect`，禁止无处理的 `let _ = ...`；字符串复制热点收敛为 `Cow<'static, str>`（执行 `./scripts/check-rust-hygiene.sh` 验证）。（`List` 非测试源码（`mod.rs/logic.rs/styles.rs/view.rs/motion.rs/protocol.rs`）未引入 `unwrap/expect/let _ =`；字符串热点已在 `components/list/src/logic.rs` 收敛为 `Vec<Cow<'static, str>>`（`item::compose_class_name`、`section::compose_class_name`）并移除常量 `.to_string()` 拼接。门禁接入：`scripts/check-ui-components-engineering.sh` 新增 `cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_rust_hygiene_contract_forbids_unwrap_expect_and_let_underscore_in_non_test_sources`、`cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_rust_hygiene_string_clone_hotspots_converge_to_cow_or_are_absent`、`cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_rust_hygiene_script_enforces_repo_level_hygiene_guards`。验证命令结果：`./scripts/check-rust-hygiene.sh` 在当前环境先于规则阶段报 `PCRE2 is not available in this build of ripgrep`（由 `check-api-contracts` 前置调用触发）；`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-rust-hygiene cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_rust_hygiene_contract_forbids_unwrap_expect_and_let_underscore_in_non_test_sources`、`... list_rust_hygiene_string_clone_hotspots_converge_to_cow_or_are_absent`、`... list_rust_hygiene_script_enforces_repo_level_hygiene_guards` 及 `TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-rust-hygiene bash ./scripts/check-ui-components-engineering.sh` 均在依赖编译阶段报 `Invalid cross-device link (os error 18)`。回归锁定：`components/list/test/semantics.rs::list_rust_hygiene_contract_forbids_unwrap_expect_and_let_underscore_in_non_test_sources`、`components/list/test/semantics.rs::list_rust_hygiene_string_clone_hotspots_converge_to_cow_or_are_absent`、`components/list/test/semantics.rs::list_rust_hygiene_script_enforces_repo_level_hygiene_guards`、`crates/ui-components/tests/list_module_semantics.rs::list_rust_hygiene_contract_forbids_unwrap_expect_and_let_underscore_in_non_test_sources`、`crates/ui-components/tests/list_module_semantics.rs::list_rust_hygiene_string_clone_hotspots_converge_to_cow_or_are_absent`、`crates/ui-components/tests/list_module_semantics.rs::list_rust_hygiene_script_enforces_repo_level_hygiene_guards`。）
- [x] Tree Shaking & 特性剪裁：组件必须注册到 `ui-components` 特性树（如 `component-accordion`）；`css.rs` 和 `lib.rs` 聚合必须受 feature 门控，禁止无条件全局依赖。（已核验 list 注册与聚合门控链路：`crates/ui-components/Cargo.toml` 声明 `component-list = ["component-active_highlight", "component-illustrated_message", "dep:ui-list"]`，`crates/ui-components/src/lib.rs` 通过 `#[cfg(feature = "component-list")] pub use ui_list as list;` 条件导出，`crates/ui-components/src/css.rs` 通过 `#[cfg(feature = "component-list")]` 条件注入 `crate::list::styles::{CSS, ITEM_CSS, SECTION_CSS}`。树摇门禁脚本已接入 list 路径：`scripts/check-ui-components-tree-shaking.sh` 新增 `LIST_MIN_FEATURES="component-list,inject-css"`、`LIST_TREE_OUTPUT` 检查、`all-components` 反向阻断、wasm 最小特性编译，以及 `cargo test -p ui-list ...` / `cargo test -p ui-components --test list_module_semantics ...` 双侧语义契约命令。验证命令（当前环境受文件系统限制阻塞）：`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-tree-shaking cargo test -p ui-list list_tree_shaking_contract_uses_feature_gates_and_no_unconditional_registry_path`、`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-tree-shaking cargo test -p ui-list list_tree_shaking_script_enforces_component_minimal_feature_tree_and_budget`、`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-tree-shaking cargo test -p ui-list list_check2_marks_tree_shaking_feature_pruning_contract_complete`、`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-tree-shaking cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_tree_shaking_contract_uses_component_feature_gates`、`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-tree-shaking cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_tree_shaking_script_enforces_component_minimal_feature_tree_and_budget`、`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-tree-shaking cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_check2_marks_tree_shaking_feature_pruning_contract_complete`、`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-tree-shaking bash ./scripts/check-ui-components-tree-shaking.sh` 在依赖编译阶段报 `Invalid cross-device link (os error 18)`。回归锁定：`components/list/test/semantics.rs::list_tree_shaking_contract_uses_feature_gates_and_no_unconditional_registry_path`、`components/list/test/semantics.rs::list_tree_shaking_script_enforces_component_minimal_feature_tree_and_budget`、`components/list/test/semantics.rs::list_check2_marks_tree_shaking_feature_pruning_contract_complete`、`crates/ui-components/tests/list_module_semantics.rs::list_tree_shaking_contract_uses_component_feature_gates`、`crates/ui-components/tests/list_module_semantics.rs::list_tree_shaking_script_enforces_component_minimal_feature_tree_and_budget`、`crates/ui-components/tests/list_module_semantics.rs::list_check2_marks_tree_shaking_feature_pruning_contract_complete`。）
- [x] 语义测试与性能回归：断言必须覆盖 `aria-*`、`data-*` 与焦点流转，不能只看快照；高频/重型组件必须补齐 `render_count` 断言/测量（如初始化空闲预算为 1）。（`List` 语义矩阵回归继续以 `list_semantic_contract_tests_cover_matrix_without_snapshot_only_assertions` 锁定 `aria-* + data-* + 键盘/指针` 路径；本次新增 `list_semantics_and_performance_regression_cover_aria_data_focus_and_render_count_measurement`（组件本地 + 聚合）补齐焦点流转断言（`on:focus/on:blur + data-focus-visible`）并绑定 `render_count` 治理跟踪。性能侧复用 `list_performance_governance_contract_is_budgeted_traceable_and_blocking_via_global_gates` 的预算/可观测/阻断链路，并在 `scripts/check-ui-components-performance.sh` 新增 `cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_semantics_and_performance_regression_cover_aria_data_focus_and_render_count_measurement`。`render_count` 自动化回归仍在仓库统一 follow-up（`docs/plan/TODO.md`，当前以可重复 perf-probe 等价证据过渡）。验证命令（当前环境受文件系统限制阻塞）：`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-semantic-perf cargo test -p ui-list list_semantics_and_performance_regression_cover_aria_data_focus_and_render_count_measurement`、`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-semantic-perf cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_semantics_and_performance_regression_cover_aria_data_focus_and_render_count_measurement`、`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-semantic-perf bash ./scripts/check-ui-components-performance.sh` 在依赖编译阶段报 `Invalid cross-device link (os error 18)`。回归锁定：`components/list/test/semantics.rs::list_semantics_and_performance_regression_cover_aria_data_focus_and_render_count_measurement`、`components/list/test/semantics.rs::list_semantics_and_performance_script_covers_contract`、`components/list/test/semantics.rs::list_check2_marks_semantics_and_performance_regression_contract_complete`、`crates/ui-components/tests/list_module_semantics.rs::list_semantics_and_performance_regression_cover_aria_data_focus_and_render_count_measurement`、`crates/ui-components/tests/list_module_semantics.rs::list_semantics_and_performance_script_covers_contract`、`crates/ui-components/tests/list_module_semantics.rs::list_check2_marks_semantics_and_performance_regression_contract_complete`。）
- [x] 版本弃用迁移（Codemod/Registry）：若提交包含跨大版本 API 破坏升级，必须在 Schema Registry 注册弃用窗口并提供纯函数迁移层（`migrate_v1_to_v2`）。（N/A：本次 `List` 变更未引入跨大版本 API 破坏升级，组件协议与 Agent Contract 仍保持 `v1`（`components/list/src/protocol.rs` 仅声明 `ListComponentSchemaVersion::V1`，`components/list/src/Component.toml` 维持 `schema_version = "1"` 且 `data-ui-schema-version` 封闭值为 `v1`，`components/list/src/list.rbi` 仍为 `ListAgentSchemaVersion::V1`），因此不触发 Codemod/Schema Registry 弃用窗口与 `migrate_v1_to_v2` 迁移层要求。回归新增：`components/list/test/semantics.rs::list_version_deprecation_migration_registry_is_explicitly_na_without_major_breaking_upgrade`、`components/list/test/semantics.rs::list_version_deprecation_migration_script_covers_engineering_gate`、`crates/ui-components/tests/list_module_semantics.rs::list_version_deprecation_migration_registry_is_explicitly_na_without_major_breaking_upgrade`、`crates/ui-components/tests/list_module_semantics.rs::list_version_deprecation_migration_script_covers_engineering_gate`。门禁接入：`scripts/check-ui-components-engineering.sh` 新增 `cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_version_deprecation_migration_registry_is_explicitly_na_without_major_breaking_upgrade`。验证命令（当前环境受文件系统限制阻塞）：`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-version-migration cargo test -p ui-list list_version_deprecation_migration_registry_is_explicitly_na_without_major_breaking_upgrade`、`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-version-migration cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_version_deprecation_migration_registry_is_explicitly_na_without_major_breaking_upgrade`、`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-version-migration bash ./scripts/check-ui-components-engineering.sh` 在依赖编译阶段报 `Invalid cross-device link (os error 18)`。）
- [x] 文档即产品（Copy-Paste Ready）：`apps/docs-app` 必须新增 Playground（Hello World、状态矩阵、受控/非受控对照），支持流式/快照展现，并提供 Source-first 一键复制且补全 imports。（`apps/docs-app/src/pages/components/pages/collections.rs` 的 `list()` 已补齐文档即产品验收面：保留 `Hello World (Uncontrolled)`，新增 `状态矩阵 State Matrix（受控 / 非受控）`（`data-slot="list-state-matrix"`，显式对照 uncontrolled/controlled/disabled）与 `Streaming/Snapshot Display`（`data-slot="list-streaming-snapshot"`，双列 `data-ui-streaming="optional" + data-ui-fallback="snapshot" + data-ui-output-state=snapshot|streaming`），并新增 `data-slot="list-source-first"` 区块提供 `Copy starter`、真实源码落点与 feature 依赖说明。复制链路统一走 `Playground` 的 `Show code`，`code_imports=list_code_imports` 明确补齐 imports，且文档中显式锚定 `apps/docs-app/src/playground.rs::compose_copy_ready_code`。README 同步新增 `## 状态矩阵（受控 / 非受控）`、`## Streaming/Snapshot Display`、`## Source-first / Copy-Paste Ready`，避免 docs 与实现漂移。门禁接入：`scripts/check-ui-components-dx.sh` 新增 `cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_docs_are_copy_paste_ready_with_hello_world_state_matrix_and_streaming_snapshot`。验证命令（当前环境受文件系统限制阻塞）：`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-docs-product cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_docs_are_copy_paste_ready_with_hello_world_state_matrix_and_streaming_snapshot`、`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-docs-product bash ./scripts/check-ui-components-dx.sh` 在依赖编译阶段报 `Invalid cross-device link (os error 18)`。回归锁定：`components/list/test/semantics.rs::list_docs_are_copy_paste_ready_with_hello_world_state_matrix_and_streaming_snapshot`、`crates/ui-components/tests/list_module_semantics.rs::list_docs_are_copy_paste_ready_with_hello_world_state_matrix_and_streaming_snapshot`。）
- [x] 语义测试优先：验证 `data-*` / `aria-*` / role / 状态来源契约，不只视觉快照。（`List` 语义测试继续坚持 contract-first：`components/list/test/semantics.rs` 与 `crates/ui-components/tests/list_module_semantics.rs` 新增 `list_check2_documents_semantics_first_testing_rules`、`list_semantics_suite_is_contract_first_not_snapshot_only`、`list_semantic_markers_changed_in_view_must_be_covered_by_semantics_checks`、`list_semantics_first_testing_script_covers_contract`，分别锁定 check2 规则文本、`*_semantics.rs` 语义矩阵覆盖、快照断言非主路径、以及 `view.rs` 关键语义字段（`role/aria-* / data-* / source marker`）变更必须同步被语义测试消费。门禁接入：`scripts/check-ui-components-contract-hygiene.sh` 新增三条 list 命令（`... list_check2_documents_semantics_first_testing_rules`、`... list_semantics_suite_is_contract_first_not_snapshot_only`、`... list_semantic_markers_changed_in_view_must_be_covered_by_semantics_checks`）。验证命令（当前环境受文件系统限制阻塞）：`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-semantics-first cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_check2_documents_semantics_first_testing_rules`、`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-semantics-first cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_semantics_suite_is_contract_first_not_snapshot_only`、`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-semantics-first cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_semantic_markers_changed_in_view_must_be_covered_by_semantics_checks`、`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-semantics-first bash ./scripts/check-ui-components-contract-hygiene.sh` 在依赖编译阶段报 `Invalid cross-device link (os error 18)`。回归锁定：`components/list/test/semantics.rs::list_check2_documents_semantics_first_testing_rules`、`components/list/test/semantics.rs::list_semantics_suite_is_contract_first_not_snapshot_only`、`components/list/test/semantics.rs::list_semantic_markers_changed_in_view_must_be_covered_by_semantics_checks`、`components/list/test/semantics.rs::list_semantics_first_testing_script_covers_contract`、`crates/ui-components/tests/list_module_semantics.rs::list_check2_documents_semantics_first_testing_rules`、`crates/ui-components/tests/list_module_semantics.rs::list_semantics_suite_is_contract_first_not_snapshot_only`、`crates/ui-components/tests/list_module_semantics.rs::list_semantic_markers_changed_in_view_must_be_covered_by_semantics_checks`、`crates/ui-components/tests/list_module_semantics.rs::list_semantics_first_testing_script_covers_contract`。）
  - 每个交互组件至少有对应 `*_semantics.rs` 测试覆盖关键状态轴与动作语义。
  - 断言应聚焦语义契约（状态来源/可访问性/键盘路径），快照仅作补充。
  - 新增/变更语义字段必须同步补测试，否则不得打勾。
- [x] E2E 选择器稳定：使用语义标记，WASM 场景有稳定等待策略。（已新增 `e2e/tests/docs_app_list_contract.spec.mjs`：只用语义选择器（`[data-component="list"]`、`[data-slot="list-showcase"]`、`[data-slot="listbox"]`、`[data-slot="listbox-option"]`），并以 `body:not(:has(#boot))` 作为 wasm ready 等待；交互路径覆盖 pointer/keyboard 的 ready/settled 断点（`data-interaction-source`、`aria-activedescendant`、`data-selected`）。语义锚点来源：`apps/docs-app/src/pages/components/pages/collections.rs`（`data-slot="list-showcase|list-state-matrix|list-workbench|list-workbench-controls|list-workbench-canvas|list-streaming-snapshot"`）。门禁脚本：`scripts/check-ui-components-e2e-list.sh`，包含 `cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_check2_documents_e2e_selector_and_stable_wait_rules`、`cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_e2e_selector_contract_uses_semantic_markers_and_settled_waits`、`cargo test -p ui-components --test list_module_semantics --no-default-features --features component-list,inject-css list_e2e_contract_covers_ready_and_settled_conditions_for_list_paths`。回归锁定：`components/list/test/semantics.rs::list_check2_documents_e2e_selector_and_stable_wait_rules`、`components/list/test/semantics.rs::list_e2e_selector_contract_uses_semantic_markers_and_settled_waits`、`components/list/test/semantics.rs::list_e2e_contract_covers_ready_and_settled_conditions_for_list_paths`、`components/list/test/semantics.rs::list_e2e_check_script_covers_selector_and_settled_wait_contract`、`crates/ui-components/tests/list_module_semantics.rs::list_check2_documents_e2e_selector_and_stable_wait_rules`、`crates/ui-components/tests/list_module_semantics.rs::list_e2e_selector_contract_uses_semantic_markers_and_settled_waits`、`crates/ui-components/tests/list_module_semantics.rs::list_e2e_contract_covers_ready_and_settled_conditions_for_list_paths`、`crates/ui-components/tests/list_module_semantics.rs::list_e2e_check_script_covers_selector_and_settled_wait_contract`。验证命令（当前环境）：`TMPDIR=/tmp CARGO_TARGET_DIR=/tmp/codex-list-e2e-selector bash ./scripts/check-ui-components-e2e-list.sh` 在依赖编译阶段报 `Invalid cross-device link (os error 18)`。）
  - E2E 选择器优先 `data-*` 语义标记，禁止依赖脆弱 DOM 层级或文本定位。
  - WASM 场景必须使用稳定等待策略（语义状态就绪而非固定 sleep）。
  - 若组件涉及异步/动画，E2E 需显式覆盖 ready/settled 条件。
- [x] 关键流程纳入可重复回归集合（Playwright/Cypress）。（已落地可重复关键流程：`e2e/tests/docs_app_list_contract.spec.mjs` 的 `docs-app list flow covers ready and settled semantic breakpoints` 在同一用例中覆盖 `ready -> pointer 交互 -> keyboard 焦点流转 -> Enter settle -> reload 后再次复现`，并以语义断点定位失败（`data-interaction-source`、`aria-activedescendant`、`data-selected`），避免笼统“页面不一致”。高风险路径覆盖：`focus + keyboard + wasm-ready/settled`；`overlay` 关闭链路对 `List` 组件范围为 N/A。门禁脚本：`scripts/check-ui-components-e2e-list.sh`，回归锁定：`components/list/test/semantics.rs::list_e2e_contract_covers_ready_and_settled_conditions_for_list_paths`、`components/list/test/semantics.rs::list_e2e_check_script_covers_selector_and_settled_wait_contract`、`crates/ui-components/tests/list_module_semantics.rs::list_e2e_contract_covers_ready_and_settled_conditions_for_list_paths`、`crates/ui-components/tests/list_module_semantics.rs::list_e2e_check_script_covers_selector_and_settled_wait_contract`。）
  - 至少定义一条可重复关键流程（打开/交互/关闭或提交）纳入 E2E 回归。
  - 回归失败需可定位到具体语义契约断点，而不是笼统“页面不一致”。
  - 高风险路径（overlay、focus、keyboard、async）优先进入回归集合。
- [x] docs-app 文档、示例、参数矩阵、状态矩阵同步更新。（`apps/docs-app/src/pages/components/pages/collections.rs` 的 `list()` 已同步 `Hello World (Uncontrolled)` 与 `状态矩阵 State Matrix（受控 / 非受控）`，并通过 `data-slot="list-state-matrix"` 显式覆盖 uncontrolled/controlled/disabled 三组状态。文档示例 props 使用 `id_base/items/selected_index/on_selected_index_change/is_disabled/disabled_indices/aria_label`，与 `components/list/src/view.rs::List` 当前 API 命名一致；默认路径明确“仅传 `id_base + items + aria_label`”，与 `components/list/src/logic.rs` 的归一化流程（`normalize_selection_axis`/`normalize_options_axis`）保持一致。回归锁定：`crates/ui-components/tests/list_module_semantics.rs::list_docs_page_exposes_showcase_and_workbench_contracts`、`crates/ui-components/tests/list_module_semantics.rs::list_dx_hello_world_is_minimal_and_state_machine_free`、`crates/ui-components/tests/list_module_semantics.rs::list_view_public_props_follow_is_on_default_naming_contract`、`crates/ui-components/tests/list_module_semantics.rs::list_defaults_are_single_sourced_in_logic_layer`、`components/list/test/semantics.rs::list_docs_are_copy_paste_ready_with_hello_world_state_matrix_and_streaming_snapshot`。）
  - 组件行为或参数变更必须同步更新 `apps/docs-app` 示例与说明。
  - 文档示例需覆盖至少一组状态矩阵（受控/非受控、disabled、size/variant 等）。
  - 文档中的 API 名称与默认值必须和 `logic.rs` 当前实现一致。
- [x] 组件文档必须对新手友好（Documentation as Product）：组件 README 或等价文档入口必须存在。（`components/list/src/README.md` 已提供稳定文档入口，并按新手路径组织：`## Hello World`（最小可运行示例）在前，随后是 `## 展示 (Display)`、`## 状态矩阵（受控 / 非受控）`、`## Config (Workbench Settings)` 等进阶内容，满足“先用起来，再进阶”。文档不要求先理解底层分层架构即可使用（最小示例仅 `List + id_base + items + aria_label`），且 docs-app 对应入口 `apps/docs-app/src/pages/components/pages/collections.rs::list()` 提供同构示例。回归锁定：`crates/ui-components/tests/list_module_semantics.rs::list_readme_documents_display_config_code_css_test_sections`、`crates/ui-components/tests/list_module_semantics.rs::list_dx_hello_world_is_minimal_and_state_machine_free`、`crates/ui-components/tests/list_module_semantics.rs::list_docs_page_exposes_showcase_and_workbench_contracts`、`components/list/test/semantics.rs::list_docs_are_copy_paste_ready_with_hello_world_state_matrix_and_streaming_snapshot`。）
  - 每个基础组件必须提供“零门槛”最小示例（Hello World）与常见用法，避免要求用户先理解底层分层架构。
  - 文档需明确“先用起来，再进阶”：默认 API 路径在前，高级控制参数在后。
  - “只有源码没有文档”或“只写给架构师/机器看的文档”视为不通过。
- [x] `apps/docs-app` 必须提供 Interactive Playground：用户可在线修改 props/状态并实时预览。（`apps/docs-app/src/pages/components/pages/collections.rs::list()` 已提供 `Playground title="Workbench（展示 + Config + Code + CSS Test）"`，并通过 `data-slot="list-workbench-controls"` 的四个交互开关（`Sync active index to selected`、`Disable last option`、`Disable root`、`Custom class marker`）实现基础 props 调整、状态切换与实时预览（`data-slot="list-workbench-canvas"`）。AI Spec 子项在 `List` 组件范围为 N/A（非 Spec 驱动组件）。可重复关键交互路径已由 `e2e/tests/docs_app_list_contract.spec.mjs` 与回归测试 `components/list/test/semantics.rs::list_e2e_contract_covers_ready_and_settled_conditions_for_list_paths`、`crates/ui-components/tests/list_module_semantics.rs::list_e2e_contract_covers_ready_and_settled_conditions_for_list_paths` 锁定，门禁脚本为 `scripts/check-ui-components-e2e-list.sh`。）
  - Playground 至少支持基础 props 调整、状态切换、交互反馈观察。
  - 对 AI Spec 相关组件，至少提供一组 Spec 输入与预览输出的联动示例。
  - Playground 作为验收面，需可重复复现关键交互路径。
- [x] Source-first 文档必须 Copy-Paste Ready：提供一键复制组件源码或最小可用片段能力。（`apps/docs-app/src/pages/components/pages/collections.rs::list()` 已提供 `data-slot="list-source-first"` 区块与 `Copy starter` 复制按钮；所有 Playground 代码复制链路统一走 `apps/docs-app/src/playground.rs::compose_copy_ready_code`，并通过 `code_imports=list_code_imports` 补齐 imports。文档同时给出真实源码落点 `data-slot="list-source-paths"`（`components/list/src/{mod,logic,view,styles,motion}.rs`）与依赖前提 `data-slot="list-source-prerequisites"`（`component-list` + `inject-css`），避免复制即报错。同步回归锁定：`components/list/test/semantics.rs::list_docs_are_copy_paste_ready_with_hello_world_state_matrix_and_streaming_snapshot`、`crates/ui-components/tests/list_module_semantics.rs::list_docs_are_copy_paste_ready_with_hello_world_state_matrix_and_streaming_snapshot`。）
  - docs-app 页面应提供复制按钮，输出代码默认可直接运行（含必要 imports/依赖提示）。
  - 若为 source-first 组件，文档需指向真实源码落点并说明依赖前提，避免“复制即报错”。
  - 文档代码与当前实现必须同步，防止示例漂移。
- [x] HeroUI 对标文档与组件文档同步：参数模型变更需同步 `docs/spec/heroui-parameter-design-strategy.md`（必要时补充 `docs/research/spectrum-heroui-style-interface-study.md`），并保证组件文档可访问。（本轮 `List` 未引入新的参数语义变更，按规则不触发新增 HeroUI 对标结论；沿用 `docs/spec/heroui-parameter-design-strategy.md` 的统一约束即可，`docs/research/spectrum-heroui-style-interface-study.md` 在本轮判定为无需追加。组件文档入口已可索引：`apps/docs-app/src/pages/components/pages/collections.rs::list()` 与 `components/list/src/README.md`。接口与文档同步回归证据：`crates/ui-components/tests/list_module_semantics.rs::list_view_public_props_follow_is_on_default_naming_contract`、`crates/ui-components/tests/list_module_semantics.rs::list_docs_page_exposes_showcase_and_workbench_contracts`、`crates/ui-components/tests/list_module_semantics.rs::list_readme_documents_display_config_code_css_test_sections`。）
  - 若参数语义发生变化，需同步更新对标策略文档，不允许实现先漂移文档后补。
  - 组件文档入口必须存在（docs-app 页面或等价文档），且可被索引定位。
  - “仅代码更新无文档更新”在接口变更场景下直接判不通过。

### 8. 合并前门禁死命令（最终执行）
在发起 PR 或完成任务前，必须保证本地/CI 以下命令全部通过：

- `cargo fmt --all -- --check`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `./scripts/check-rust-hygiene.sh`
- `cargo check -p ui-components --target wasm32-unknown-unknown`
- `cargo check -p ui-headless --no-default-features --features ssr`
- `cargo check -p ui-components --target wasm32-unknown-unknown --no-default-features --features component-<your_component>,inject-css`

依据文档（`rust-ui/docs/spec` 及 `rust-ui/docs`）：

- `rust-ui/docs/spec/ai_context_projection_protocol.md`
- `rust-ui/docs/spec/architectural_fitness_functions.md`
- `rust-ui/docs/spec/async_state_as_data_command.md`
- `rust-ui/docs/spec/collection_registration_protocol.md`
- `rust-ui/docs/spec/compile_time_evolution_migration.md`
- `rust-ui/docs/spec/component_boundaries.md`
- `rust-ui/docs/spec/component_domains.md`
- `rust-ui/docs/spec/controlled_evolution_sandbox.md`
- `rust-ui/docs/spec/core_shell_protocol_infra_baseline.md`
- `rust-ui/docs/spec/environment_subscription_streams.md`
- `rust-ui/docs/spec/event_light_cone_signal_bus.md`
- `rust-ui/docs/spec/focus_global_stack_gc.md`
- `rust-ui/docs/spec/foreign_zone_escape_hatches.md`
- `rust-ui/docs/spec/headless_purification.md`
- `rust-ui/docs/spec/heroui-parameter-design-strategy.md`
- `rust-ui/docs/spec/hyper-structure-ui-development-playbook.md`
- `rust-ui/docs/spec/i18n.md`
- `rust-ui/docs/spec/intent_stack_semantic_layering.md`
- `rust-ui/docs/spec/kernel_shell_architecture.md`
- `rust-ui/docs/spec/macro_micro_dual_state_machine.md`
- `rust-ui/docs/spec/motion.md`
- `rust-ui/docs/spec/mvp.md`
- `rust-ui/docs/spec/platform_abdication_ecosystem.md`
- `rust-ui/docs/spec/README.md`
- `rust-ui/docs/spec/release_versioning.md`
- `rust-ui/docs/spec/side_effect_command_pattern.md`
- `rust-ui/docs/spec/slot_projection_strategy.md`
- `rust-ui/docs/spec/ssr_hydration_discontinuity.md`
- `rust-ui/docs/spec/state_primitives_core_satellite_split.md`
- `rust-ui/docs/spec/style_island_defense.md`
- `rust-ui/docs/spec/styling.md`
- `rust-ui/docs/spec/tree_shaking.md`
- `rust-ui/docs/spec/ui_layout_split.md`
- `rust-ui/docs/spec/ui_physics_two_pass_rendering.md`
- `rust-ui/docs/spec/unified_causality_bus.md`
- `rust-ui/docs/spec/wasm_generic_bloat.md`
- `rust-ui/docs/起点_也即是目的.md`
- `rust-ui/docs/DOCS_GOVERNANCE.md`
- `rust-ui/docs/DOCS_INDEX.md`
- `rust-ui/docs/philosophy.md`
- `rust-ui/docs/README.md`
- `rust-ui/docs/RULES_ZH.md`
