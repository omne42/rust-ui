# 单组件 Check List（Hyper-Structure 完整版）

> 用途：每次新增、重构或修改一个组件时，必须按本清单逐项检查并在 PR 中附带结果。  
> 执行顺序：先过第 1-2 节（架构与状态），再进入第 3-5 节（具体实现）。  
> 术语统一：`status-primitives` 指状态原语层（当前 crate 名为 `ui-state-primitives`）。

### 0. 适用范围与顺序纪律
本清单仅评估“一个组件”的改动结果，不替代仓库级治理。
先过第 1-2 节（架构与状态）再进入第 3-5 节（具体实现）。
不适用的条目必须明确标注 `N/A` 并说明理由，禁止机械打勾。
组件目标、非目标、风险边界已写清楚；发现跨组件/跨层系统性问题时升级为仓库级任务。

### 1. 架构边界与分层约束（Kernel/Shell 总线）
- [x] `status-primitives` 定义：纯状态原语层（受控/非受控、toggle、selection、list、overlay open state、expansion 等）。不依赖 Leptos/DOM/web-sys；只包含 Rust 数据结构和方法，不含视图与事件绑定。（`ActionBar` 状态轴与归一化来自 `crates/ui-state-primitives/src/action_bar.rs`，并在 `crates/ui-state-primitives/src/lib.rs` 导出；对应单测在 `crates/ui-state-primitives/src/test/action_bar.rs`。）
  - 所有状态原语必须从 `status-primitives`（`ui-state-primitives`）获取，组件层只能消费，不得自造。
  - 下沉判定依据是“稳定状态不变量”；凡属于状态机、归一化、状态派生能力，默认先进入 `ui-state-primitives`。
  - 组件中可保留的仅是装配逻辑：props 归一、样式来源标记、slot 组织、对 `ui-state-primitives` 输出的映射。
  - 组件内若出现状态原语实现（受控/非受控状态机、single/multiple 展开规则、索引归一化、跨事件状态派生），该项直接判不通过。
  - 处理方式固定：先下沉到 `ui-state-primitives/src/<capability>.rs`（如 `expansion.rs`），在 `ui-state-primitives/src/lib.rs` 导出，再回到组件改调用。
  - 下沉后的原语必须有 `ui-state-primitives` 单元测试；组件侧只保留调用与语义挂载测试。
  - 桥接规范：`ui-state-primitives` 结构体必须是 POJO（Plain Old Rust Object），不持有 Leptos `Signal` 或框架绑定状态容器。
  - 消费规范：`ui-headless` 或组件 `logic.rs` 负责解包 `Signal` 当前值传入 primitive 方法，并将结果显式写回 `Signal`。
  - 设计理由：保持 primitives 纯粹可测、可迁移，不与特定响应式库绑定（便于未来替换响应式实现与做纯 Rust 测试）。
- [x] `ui-headless` 定义：交互与 A11y 原语层（press/focus/hover/roving/listbox/menu/tooltip 等），把输入设备事件与状态语义标准化为可复用契约；输出必须是类型化 `attrs + handlers + state`。不做样式、不写组件 CSS、不做组件级动效编排。（组件在 `components/action-bar/src/view.rs` 通过 `ui_headless::use_controllable_state`、`ui_headless::i18n` 与 `locale_attrs` 消费语义能力；清除动作复用 `ui_button::Button` 的 headless 交互契约。）
  **`ui-headless` 落位硬规则（必须执行）**：
  - 输入边界：消费 `status-primitives` 状态 + 用户输入事件（keyboard/pointer/focus）+ 环境能力（web/ssr）。
  - 输出边界：只输出语义契约（attrs/handlers/state）；组件层只负责挂载与组合，不得把语义判断塞回 `view.rs`。
  - 下沉判定依据是“交互/A11y 语义契约”；凡属于键盘/焦点/指针归一、ARIA 映射、交互状态语义能力，默认先进入 `ui-headless`。
  - 必须下沉：键盘模型、焦点模型、跨设备输入归一、ARIA 状态映射、overlay/presence 等交互语义。
  - A11y 契约与共享工具落点固定在 `crates/ui-headless/src/a11y.rs`；组件只在 `view.rs` 挂载，不在组件层重写。
  - 语义契约必须提供 `lang` / `dir`（LTR/RTL）接入能力；headless 不硬编码用户可见文本，文案由 i18n/l10n 层提供。
  - 语义契约正确性必须有回归：`components/*/test/**` 断言语义标记，`e2e/tests/*` 覆盖关键交互流程。
  - 禁止放在 `ui-headless`：视觉 class 选择、CSS 规则、组件 slot 布局、组件专属动效编排、业务文案。
  - 允许留在组件层：纯视觉一次性交互且不形成可复用语义契约（例如单组件局部微交互）。
- [x] `ui-motion` 定义：动效能力与契约执行层（spring、keyframes、WAAPI/RAF backend），只负责时间函数、插值与运行时驱动，不承载组件业务语义与状态决策。（`components/action-bar/src/motion.rs` 仅做 `ActionBarMotion` 合同清洗与 attach，执行器复用 `ui_motion::spring::SpringAnimator`，并保留 `#[cfg(not(target_arch = "wasm32"))]` no-op 分支。）
  - 放在 `crates/ui-motion`：通用动画数学与执行后端（spring solver、keyframe sampling、easing registry、driver adapters），以及 `wasm/non-wasm` 适配与 `reduced-motion` 执行策略。
  - 放在 `crates/ui/src/<component>/motion.rs`：把组件语义状态（open/closed、enter/exit、active/inactive）映射为 `ui-motion` contract，绑定目标节点并调用 attach。
  - 禁止放在 `crates/ui-motion`：组件 slot 结构、组件专属状态机、ARIA/keyboard 语义、业务文案与业务分支。
  - 禁止放在组件 `motion.rs`：自实现 spring/keyframe/driver 执行器；跨组件共享动效算法必须回迁 `ui-motion`。
  - 动效参数优先来自 token/theme；禁止在组件样式与逻辑中散落硬编码时长/曲线/位移常量。
  - 非 wasm 路径必须提供 no-op/stub，保证 SSR/tooling 可编译且行为可预测。
- [x] `ui-theme` 定义：唯一设计 token 与主题上下文层（system/color/scale + Light/Dark/OLED），负责 token 分类、主题映射与 CSS 变量生成。（`components/action-bar/src/styles.rs` 仅消费 `var(--ui-*)` 及 fallback 变量，无组件私有 token 体系与主题重建逻辑。）
  - Token 统一基线落点固定：`crates/ui-theme/src/tokens.rs` 定义，`crates/ui-theme/src/theme.rs` 映射，`crates/ui-theme/src/css.rs` 输出变量；组件只在 `crates/ui/src/<component>/styles.rs` 消费。
  - 三轴上下文（`system/color/scale`）在 `theme.rs` 定义；组件在 `logic.rs` 选择并在 `view.rs` 生效，`styles.rs` 只消费变量，不重建主题。
  - Token 分类必须可追溯：分类源在 `tokens.rs`，规范同步 `docs/spec/styling.md`；组件不得引入平行私有 token 命名体系。
  - 量化尺寸基准必须可回归：尺寸基准在 `tokens.rs` 与 `theme.rs` 定义，主题回归在 `crates/ui-theme/tests/token_scale_baseline.rs`，组件语义回归在 `components/*/test/*<component>_semantics.rs`。
  - 主题调色与语义色对比必须满足 `WCAG 2.1 AA` 基线，并覆盖 Light/Dark/OLED 主题变体。
  - 主题层只输出 `theme/tokens/base css` 与变量；不实现组件结构、交互逻辑、组件级动效编排。
  - 新增视觉语义先补 token，再由组件消费；禁止“组件临时值先落地、后补 token”的倒序流程。
- [x] `ui` 定义：最终 Leptos 组件装配层，组合 `status-primitives + ui-headless + ui-motion + ui-theme` 并暴露稳定公共 API。（`components/action-bar/src/{mod,logic,view,styles,motion}.rs` 职责分离完整；公共导出为 `ActionBar/ActionBarMotion`，并由 `crates/ui/src/lib.rs` 在 `component-action_bar` feature 下稳定 re-export。）
  - `logic.rs` 负责 props 归一与状态派生；`view.rs` 负责结构渲染与 headless 语义挂载；`styles.rs` 负责 token-first 静态样式；`motion.rs` 负责动效 attach。
  - 组件层不得重写 `status-primitives` 状态机或 `ui-headless` 交互契约；发现即判不通过并回迁到对应层。
  - 对外 API 禁止暴露 `web-sys`/DOM 细节类型；平台差异封装在内部模块。
  - 测试文件位于src同级的test/中，内部测试文件同名（如rust-ui/components/accordion/src/logic.rs与rust-ui/components/accordion/test/logic.rs）。
  - 还需要一个semantics.rs用于测试。可能存在类似rust-ui/components/accordion/test/semantics.rs的旧版实现，需要迁移到新目录。

### 2. API 设计与状态内核（Logic/Kernel）
- [x] API 命名契约统一：公共 props/回调严格使用 `is_*`、`on_*`、`default_*` 前缀；同语义在全库同名，禁止别名漂移。（`ActionBar` 公共轴命名已对齐：布尔轴 `is_force_visible`、事件轴 `on_selected_count_change/on_clear_selection`、默认值轴 `default_selected_count`；并由 `components/action-bar/test/semantics.rs::action_bar_api_naming_uses_is_on_default_prefix_contract` 锁定，且显式阻止旧名 `force_visible` 回归。）
  - 布尔状态统一 `is_*`（如 `is_open`/`is_disabled`），事件统一 `on_*`，默认值统一 `default_*`。
  - 同一语义 across 组件必须同名（如都用 `on_open_change`，禁止同义别名并存）。
  - 公共 API 引入新命名时，需说明与现有命名体系的兼容策略与迁移路径。
- [x] 受控/非受控必须成对：每个可控状态轴都提供 `value + on_value_change + default_value`（如 `open/on_open_change/default_open`）；缺一项即不通过。（`ActionBar` 的选择数量轴已成对提供 `selected_count + on_selected_count_change + default_selected_count`，并通过 `ui_headless::use_controllable_state(selected_count, Some(default_selected_count), on_selected_count_change)` 维持受控/非受控语义；回归由 `components/action-bar/test/semantics.rs::action_bar_selected_count_supports_controlled_and_uncontrolled_contract` 锁定。）
  - 受控模式：外部值是单一事实来源，内部不得偷偷写回本地状态。
  - 非受控模式：仅由默认值初始化一次，后续状态由内部原语管理。
  - 受控/非受控切换语义需稳定可测，避免“半受控”隐式行为。
- [x] 默认值单一来源：默认值与优先级只在 `logic.rs` 归一化；`view.rs` 禁止二次兜底或隐式改写。（`ActionBar` 默认值统一经 `logic::normalize_default_selected_count` 归一；`view.rs` 仅消费 `let default_selected_count = logic::normalize_default_selected_count(default_selected_count);` 并传给 `use_controllable_state`，未出现二次 `default_selected_count.unwrap_or*`；回归由 `components/action-bar/test/semantics.rs::action_bar_default_values_have_single_logic_source` 锁定。）
  - 默认值优先级必须可读且可测试（显式规则而非分散 `unwrap_or`）。
  - `view.rs` 不允许再做默认值分支；仅消费 `logic.rs` 的归一化输出。
  - 一旦发现多处默认值来源，直接判不通过并回收至 `logic.rs`。
- [x] 状态归一化集中：状态输入先类型化，再在 `logic.rs` 统一派生；禁止在 `view.rs`、事件回调、样式分支中分散拼状态机。（`ActionBar` 通过 `logic::ActionBarViewStateInput` 作为类型化输入边界，并在 `logic::resolve_view_state` 内统一派生 `ActionBarState`；`view.rs` 仅调用 `logic::resolve_view_state(...)` 与触发 `request_selected_count_change.run(0)`，未重建状态机；`styles.rs` 未包含 `resolve_state/resolve_view_state`。回归由 `components/action-bar/test/semantics.rs::action_bar_state_normalization_is_centralized_in_logic` 锁定。）
  - 输入边界统一进入 `logic.rs`，输出统一为可渲染语义状态与来源标记。
  - 事件处理器只触发状态变更，不重建状态机规则。
  - 样式层只消费状态标记，不承担状态判定职责。
- [x] 离散状态必须类型约束：`variant/size/mode/status` 等离散输入使用 `enum`；禁止用多个 `Option<bool>`/字符串自由组合表达互斥状态。（`ActionBar` 离散轴由 `ui-state-primitives` 的 `ActionBarPosition/ActionBarPhase/ActionBarSelectionKind` 三个 `enum` 建模，`view.rs` 输入为 `position: ActionBarPosition`，并显式阻断 `position: String` 与 `is_top/is_bottom` 这类布尔爆炸路径；回归由 `components/action-bar/test/semantics.rs::action_bar_discrete_state_axes_are_enum_typed` 锁定。）
  - 互斥状态优先用 `enum` 建模，利用编译器封住无效组合。
  - 字符串输入若需兼容外部配置，必须先映射到类型化枚举再进入逻辑层。
  - 布尔爆炸（多个 bool 表达一个状态机）应在设计评审阶段直接拦截。
- [x] 状态原语来源正确：组件层只消费 `status-primitives`（当前 `ui-state-primitives`）能力，不直接绑定业务 store；应用级全局状态必须经桥接层适配后再接入组件。（`ActionBar` 在 `logic.rs` 通过 `pub use ui_state_primitives::action_bar::{...}` 仅消费状态原语并委托 `resolve_state(ActionBarStateInput { ... })`，组件层未重写原语；`logic.rs/view.rs` 未出现 `GlobalStore/AppStore/BusinessStore/use crate::store/use app::store` 直连业务 store。回归由 `components/action-bar/test/semantics.rs::action_bar_status_primitives_are_sourced_from_ui_state_primitives` 与 `action_bar_status_primitive_boundary_avoids_local_state_machine_and_business_store_coupling` 锁定。）
  - 组件中出现可复用状态机实现（受控/非受控、展开规则、选择归一）即判应下沉。
  - 组件与业务全局状态之间必须有适配边界，禁止组件直接依赖业务 store 类型。
  - `logic.rs` 仅做装配与映射，不重新实现状态原语。
- [x] 如果无异步相关，直接打勾。异步交互语义统一：`is_loading`、error/retry、disabled、`aria-busy` 映射一致；优先复用统一 async action 原语（如 `use_async_action`），禁止每组件自定义一套加载/错误协议。（N/A：`ActionBar` 无远程请求与异步状态，当前仅存在同步 clear 交互 `request_selected_count_change.run(0)` + `on_clear_selection.run(())`；`view.rs/logic.rs` 未引入 `use_async_action`、`is_loading`、`aria-busy`、`retry`、`async fn/.await`。回归由 `components/action-bar/test/semantics.rs::action_bar_has_no_async_loading_protocol_and_keeps_sync_clear_contract` 锁定。）
  - 无异步交互时需明确标注 N/A 理由（例如“组件无远程请求与异步状态”），不是机械打勾。
  - 有异步交互时，`is_loading`/disabled/`aria-busy`/retry 语义必须成套一致，且对键盘与读屏路径可用。
  - 异步失败态要有可恢复路径（重试或回退），并有语义测试覆盖。
- [x] API 易用性验收标准（DX Paradox）：把复杂性留在内部，把简单留给用户。（`docs-app` 提供零门槛 `<ActionBar default_selected_count=1>` Hello World（`apps/docs-app/src/pages/components/pages/actions_extra.rs` 的 `hello_code` 为 3 行可复制示例），基础路径不要求手动接线 `ui-state-primitives/ui-headless`，并由 `components/action-bar/test/semantics.rs::action_bar_docs_expose_hello_world_path_without_state_machine_wiring` 显式禁止 `<ActionBar state=...>` 与内部层暴露；同页同时提供受控/自定义文本/自定义动效 Playground，保持 simple-first + advanced-on-demand。）
  - 基础用法不得要求用户先理解或手动接线 `ui-state-primitives`/`ui-headless` 状态机。
  - 基础组件 Hello World 示例代码不得超过 5 行（导入与外层模板按仓库约定不计），并可直接运行。
  - 简单需求走简单 API，复杂需求再暴露高级入口：默认 props 覆盖高频场景，高级控制通过受控/扩展参数按需开启。
  - 禁止把内部状态对象作为基础必填参数暴露（例如强制 `state=...` 才能完成点击/展开等基本交互）。
  - docs-app 必须提供最小可用示例，优先展示一眼可懂的默认调用路径。
- [x] 组合型组件主 API 必须“显示优于约定”：优先使用显式组合 `<Parent><Item ... /></Parent>`。（`ActionBar` 主 API 通过 `#[prop(optional)] children: Option<Children>` + `{children.map(|children| children())}` 暴露显式组合，docs 默认示例使用 `<ActionBar><ActionButton ... /></ActionBar>`；未引入 `labels/titles/panels` 并行数组或 `ItemSpec` 配置语法。回归由 `components/action-bar/test/semantics.rs::action_bar_composition_api_prefers_explicit_parent_item_structure` 锁定。）
  - 每个 item 的标题、语义与内容必须在同一 `Item` 结构维度绑定，避免索引配对式隐式约定。
  - `labels + children`、`titles + panels` 等并行数组/并行槽位写法不得作为默认或推荐 API。
  - 不引入这类语法糖：若为配置式输入，仅允许类型化 `ItemSpec`，并在内部映射为显式 `Item` 语义树。

### 3. 高级交互与物理机制（Shell/Physics）
- [x] 宏观/微观双状态机（Macro/Micro Duality）：拖拽等高频交互在 `Dragging` 期间由 `view/motion` 本地循环执行；禁止每帧穿越回 `logic.rs`，必须在结束时通过 `Action::DragEnd` 回流收敛。（N/A：`ActionBar` 当前无拖拽/手势轨迹交互与 `Dragging` 生命周期，不存在逐帧回流 `logic.rs` 的路径；组件交互仅为同步选择计数与 clear 动作，`motion.rs` 仅处理显隐动画 attach。）
- [x] 几何两段式渲染（Two-Pass Rendering）：`Tooltip/Popover/Menu` 等依赖 DOM 测量的组件必须走 `Intent -> Measure(view) -> Rectification(logic)`，并具备幂等收敛保护防死循环。（N/A：`ActionBar` 不依赖浮层定位或 DOM 几何测量回流，当前无 `Measure -> Rectification` 闭环与测量副作用（如 `getBoundingClientRect/offset*`）；组件仅消费已归一化状态并渲染固定结构。）
- [x] 集合注册协议（Registration Protocol）：`Accordion/Tabs/Menu` 动态子项必须通过 `RegistrationContext` 上报 `Register/Unregister`，逻辑层维护 `items_order`，禁止依赖 `HashSet` 迭代顺序做导航。（N/A：`ActionBar` 非集合导航组件，不承担动态子项注册/顺序导航职责；当前仅以显式 `children` 渲染动作项，不存在 `RegistrationContext`、`Register/Unregister`、`items_order` 或基于 `HashSet` 迭代的导航路径。）
- [x] 插槽投影策略（Slot Projection）：容器组件明确 `Lazy/KeepAlive/Eager`；`KeepAlive` 隐藏时必须通过生命周期通知（如 `NotifyHidden`）暂停轮询/动画等高耗能副作用。（N/A：`ActionBar` 非内容投影容器，不提供 `Lazy/KeepAlive/Eager` 模式切换；组件仅渲染显式 `children` 与 clear 操作，不存在隐藏子树生命周期管理或 `NotifyHidden` 协议。）
- [x] 环境订阅流（Env Streams）：`Resize/Theme/Intersection` 等环境变化在 `view.rs` 采样、防抖后转化为高层语义 `Action`（如 `BreakpointChanged`）推送到 `logic`；禁止原始事件洪泛。（N/A：`ActionBar` 当前无 `Resize/Theme/Intersection` 驱动的环境订阅语义；`components/action-bar/src/view.rs` 仅处理选择计数与 clear 点击，不存在 `ResizeObserver`/`matchMedia`/`IntersectionObserver` 或 window 原始事件订阅，因此不存在事件洪泛路径。）
- [x] 事件光锥（Event Light Cone）：`Table/Grid` 等大型集合批量操作必须走 `Context Bus + Selector` 与状态压缩表达（如 `SelectionState::All`），禁止 O(N) 级向下 prop drilling。（N/A：`ActionBar` 非大型集合承载组件，不承担 `Table/Grid` 级批量选择传播；当前仅接收聚合后的 `selected_count` 与显式 `children`，不存在 `Context Bus + Selector` 总线或 O(N) 向下分发批量选择状态的路径。）
- [x] 统一因果总线（Causality Bus）：复杂派生总线操作必须支持透传 `TraceId`，确保“用户触发 -> 派生命令 -> 总线广播 -> 订阅者”因果链不断裂。（N/A：`ActionBar` 当前无复杂派生命令总线与多订阅者广播模型；交互路径仅为按钮触发 `selected_count` 变更与 `on_clear_selection` 回调，不存在 `TraceId` 透传链路可断裂的问题。）
- [x] 存在 A11y 实现、国际化与本地化实现（至少具备接入点，不硬编码用户可见文本）。（`components/action-bar/src/view.rs` 已挂载 `role="toolbar"`、`aria-label`、`aria-hidden`，并透传 `lang/dir`；文案来源走 `ui_headless::i18n::use_ui_i18n` 的 `ActionBarStrings`，同时允许 `aria_label/clear_label/selection_text` props 覆盖；locale 由共享工具 `ui_headless::locale_attrs` 提供（定义见 `crates/ui-headless/src/a11y.rs`），组件层未重造同名工具。语义回归由 `components/action-bar/test/semantics.rs::action_bar_a11y_i18n_and_locale_contract_is_wired` 与 `action_bar_emits_toolbar_semantics_and_state_attributes` 覆盖，并显式禁止在 `view.rs` 硬编码用户可见文案。）
  - 交互元素必须具备可验证语义：`role`/`aria-*`/键盘可达路径完整，且和 headless 契约一致。
  - 用户可见文本来源必须可覆盖：优先 props，其次应用注入（`UiRoot`/i18n bundle），最后组件兜底文案；禁止把业务可见文案硬编码在 `view.rs`。
  - 组件需透传或消费 `lang` / `dir`（LTR/RTL）上下文，不得假设单语言单方向。
  - 共享 A11y 工具优先来自 `crates/ui-headless/src/a11y.rs`，组件层不重复发明同名语义工具。
- [x] 状态可观测、可检索、可验证：使用稳定 `data-*` 与 `aria-*` 标记表达状态和来源。（`components/action-bar/src/view.rs` 已稳定输出 `data-state/data-position/data-selection/data-control-mode` 与 `data-*-source`（如 `data-selected-count-source`、`data-default-selected-count-source`、`data-clear-label-source`）以及 `role="toolbar"`、`aria-label`、`aria-hidden`；状态值来自 `crates/ui-state-primitives/src/action_bar.rs` 的封闭枚举与固定属性映射（如 `visible|hidden`、`top|bottom`、`empty|single|multiple`、`controlled|uncontrolled`、`custom|default|provided|none|implicit|external`），避免自由文本漂移。自动化语义选择器与回归由 `components/action-bar/test/semantics.rs::action_bar_machine_readable_contract_uses_typed_inputs_and_semantic_markers`、`action_bar_emits_toolbar_semantics_and_state_attributes`、`action_bar_semantics_cover_data_aria_and_interaction_matrix` 锁定。）
  - 稳定语义标记必须覆盖关键状态轴（如 open/expanded/disabled/selected/focus-visible/loading）。
  - 状态来源必须可区分（受控/非受控、默认值/外部值、交互来源），通过稳定 marker 暴露而不是隐式推断。
  - 自动化选择器优先基于语义标记，不依赖 DOM 顺序、层级深度或临时 class 名。
  - 标记值应为封闭集合（可枚举），避免自由文本导致契约漂移。
- [x] 样式依赖显式状态（`data-*`/class），而非脆弱 DOM 结构猜测。（`components/action-bar/src/styles.rs` 的状态分支选择器均基于稳定 `data-*`/class（如 `data-state/data-selection/data-has-clear/data-*-source`），未使用 `:nth-child`/`:nth-of-type` 等结构猜测；`components/action-bar/src/view.rs` 未注入 `style=` 业务内联样式，运行时样式更新仅在 `components/action-bar/src/motion.rs` 通过 CSS 变量 `--ui-action-bar-translate-y/--ui-action-bar-opacity` 进行。回归由 `components/action-bar/test/semantics.rs::action_bar_styles_depend_on_explicit_state_markers_not_dom_guessing`、`action_bar_styles_include_position_state_and_source_contracts`、`action_bar_token_first_styles_are_static_and_aggregated_via_ui_root` 锁定。）
  - `styles.rs` 中状态分支选择器必须基于 `data-*`/`aria-*`/稳定 class，禁止用 `:nth-child`、深层级选择器猜测状态。
  - 运行时样式仅允许传递必要 CSS 变量（custom properties）；禁止把业务样式逻辑塞进 inline style。
  - 视觉状态切换必须可由语义标记直接解释，不能依赖“某节点是否恰好存在”。
- [x] 测试验证“语义契约”而不只验证视觉快照。（`components/action-bar/test/semantics.rs` 已以语义断言为主覆盖 `role/aria/data-state/source markers`：如 `action_bar_emits_toolbar_semantics_and_state_attributes`、`action_bar_state_markers_are_observable_and_closed_set_contracts`、`action_bar_machine_readable_contract_uses_typed_inputs_and_semantic_markers`；关键分支包含受控/非受控（`action_bar_selected_count_supports_controlled_and_uncontrolled_contract`）、交互路径（通过 `on_press` 契约挂载并禁止组件层 `on:click/on:keydown` 的 `action_bar_mounts_headless_contract_in_view_not_logic_layer`）、SSR/wasm 差异（`action_bar_reduced_motion_ssr_wasm_branches_keep_semantics_consistent`）。`ActionBar` 无 `is_disabled` 状态轴，因此 disabled 分支为 N/A（按适用范围）。视觉快照替代被 `action_bar_semantics_suite_prioritizes_contract_assertions_over_snapshots` 显式禁止。）
  - 至少存在语义测试覆盖关键状态与交互路径（role/aria/data-state/source markers）。
  - 测试矩阵必须覆盖关键分支：受控/非受控、disabled、键盘路径、指针路径、SSR/wasm 差异（按适用范围）。
  - 视觉快照只能作为补充，不得替代语义契约断言。
- [x] 组件文件职责正确：`mod.rs`（导出边界）、`logic.rs`（归一/派生/来源标记）、`styles.rs`（静态 token-first CSS）、`view.rs`（Leptos 结构 + headless 挂载）、`motion.rs`（动效契约 + attach）。（`components/action-bar/src/mod.rs` 仅做模块组织与稳定导出；`components/action-bar/src/logic.rs` 只做归一/派生/来源标记并复用 `ui-state-primitives`，无 DOM 与事件绑定；`components/action-bar/src/styles.rs` 仅 token-first 静态 CSS；`components/action-bar/src/view.rs` 负责结构渲染与 headless/A11y 挂载并消费 `logic` 输出；`components/action-bar/src/motion.rs` 仅做语义到 `ui-motion` 合同映射与 attach，未在组件侧重写动效引擎。回归由 `components/action-bar/test/semantics.rs::action_bar_component_files_follow_layered_responsibilities`、`action_bar_stays_in_ui_components_assembly_layer_and_public_api_boundary_is_stable`、`action_bar_platform_guards_keep_cfg_split_and_non_wasm_web_sys_free` 约束。）
  - `mod.rs` 只维护最小稳定导出面与 feature gate，不承载实现细节。
  - `logic.rs` 只做输入归一、状态派生、来源标记；禁止 DOM 操作和样式细节分支。
  - `styles.rs` 只包含 token-first 静态 CSS；禁止硬编码主题常量与业务语义文案。
  - `view.rs` 只做结构渲染与 headless 契约挂载；禁止隐藏关键状态决策。
  - `motion.rs` 只做组件语义到动效契约映射与 attach；禁止在组件内重写通用动效引擎。
- [x] `spec.rs` 只用于少数复杂组件（如 button），避免泛滥。（`ActionBar` 组件目录不存在本地 `spec.rs`（`components/action-bar/src/`），保持轻量实现；复杂组件 `button` 保留规范化 `spec.rs`（`components/button/src/spec.rs` 或 `crates/ui/src/button/spec.rs`）作为对照。该边界由 `components/action-bar/test/semantics.rs::action_bar_spec_boundary_reuses_button_spec_without_local_spec_file` 锁定，并显式禁止 `ActionBarSpec/ActionBarSchema` 暴露。由于本次未新增 `spec.rs`，新增 spec 的契约测试与版本演进说明要求为 N/A。）
  - 仅当组件存在稳定外部规范/Schema 契约或复杂配置固化需求时才引入 `spec.rs`。
  - 简单组件不得为了“形式统一”新增 `spec.rs`；说明文档应留在 `check2.md`/组件文档。
  - 新增 `spec.rs` 必须同步给出契约测试与版本演进说明。
- [x] 组件层遵循 token-first 静态样式契约：样式通过 `styles.rs` 聚合注入；运行时仅传必要 CSS 变量；不把 Utility-First/CSS-in-Rust 当组件库默认范式。（`components/action-bar/src/styles.rs` 为唯一样式规则落点，视觉值统一来自 `var(--ui-*)` 与 fallback 变量；样式由 `crates/ui/src/css.rs` 在 `component-action_bar` feature 下聚合 `out.push_str(crate::action_bar::styles::CSS);`，并由 `crates/ui/src/root.rs` 的 `UiRoot` 统一注入。运行时仅通过 `components/action-bar/src/motion.rs` 写入 `--ui-action-bar-translate-y/--ui-action-bar-opacity` 两个 CSS 变量，不注入业务样式属性。组件层未采用 Tailwind/Utility-First/CSS-in-Rust 默认范式（无 `@apply`/`tw-`/`styled(`/`css!(`），对应回归由 `components/action-bar/test/semantics.rs::action_bar_token_first_styles_are_static_and_aggregated_via_ui_root` 锁定。）
  - 样式规则统一落在 `styles.rs`，由 `crates/ui/src/css.rs` 聚合并通过 `UiRoot` 注入。
  - 颜色/间距/圆角/阴影等视觉值必须来自 `var(--ui-*)`，禁止组件私有 token 体系。
  - Utility-First 仅作为 `apps/*` 应用层布局手段，不得反向污染组件库契约。
  - CSS-in-Rust 仅在有明确类型安全与构建成本净收益时作为例外采用。
- [x] 默认主题美学质量达标（Visual Desire）：以 HeroUI 现代审美为学习对标，默认主题不仅“可用”，还必须“第一眼可信”。（`docs-app` 已提供默认主题基线页 `theme-visual-baseline`（`apps/docs-app/src/pages/components/pages/theme_visual_baseline.rs`，含 Button/Input/Overlay 与层级/间距/对比/交互反馈说明），并有 E2E 视觉回归基线 `e2e/tests/docs_app_theme_visual_baseline.spec.mjs`（页面及 button/input/overlay 截图快照）。`ActionBar` 文档入口挂在同一基线门禁下（`apps/docs-app/src/pages/components/pages/actions_extra.rs`），语义回归由 `components/action-bar/test/semantics.rs::action_bar_visual_desire_reuses_theme_visual_baseline_gate` 与 `action_bar_visual_desire_heroui_alignment_targets_experience_not_api_copy` 锁定，且明确“对齐视觉语言与体验，不做 HeroUI API 表层复制”。）
  - 默认主题需通过基础美学清单：信息层级清晰（字重/字号/间距）、对比与层次自然、交互反馈明确（hover/active/focus）。
  - docs-app 必须提供默认主题基线页面与截图基线，关键组件（Button/Input/Overlay）纳入视觉回归对比。
  - 禁止“可访问但粗糙”的最低可用心态：视觉退化（类似旧式 Bootstrap 观感）视为质量回归。
  - HeroUI 对标以“视觉语言与体验质量”对齐为目标，不做无差别 API 表层复制。
- [x] Tree Shaking 是一等能力：package 模式支持组件级 feature；source 模式天然裁剪；样式层同步裁剪，禁止无条件聚合全部 CSS，禁止破坏 DCE/LTO 的全量中央注册表。（`crates/ui/Cargo.toml` 已提供组件级 feature（`component-action_bar = ["component-button", "dep:ui-action-bar"]`）与 `web-demo-components/all-components` 分层；`crates/ui/src/lib.rs` 对 `action_bar` 导出做 `#[cfg(feature = "component-action_bar")]` 门控，`crates/ui/src/css.rs` 对 `action_bar::styles::CSS` 与 `push_components_css` 做 `component-action_bar + inject-css` 双门控，不存在无条件全量 CSS 聚合。`apps/web-demo/Cargo.toml` 使用 `default-features = false` + `web-demo-components`，未拉起 `all-components`；`apps/docs-app/Cargo.toml` 显式选择 `all-components`（非隐式默认）。验证脚本 `scripts/check-ui-tree-shaking.sh` 覆盖特性树检查、反向依赖检查、最小 wasm 编译与体积预算（`scripts/tree_shaking_budget.env`），并阻断 `all-components` 泄漏。回归由 `components/action-bar/test/semantics.rs::action_bar_tree_shaking_keeps_component_feature_and_css_boundaries` 与 `action_bar_tree_shaking_check_script_covers_feature_tree_wasm_and_budget` 锁定。）
  - package 模式必须有组件级 feature（如 `component-accordion`）；未启用组件不得进入编译与链接路径。
  - `lib.rs` 与 `css.rs` 必须按 feature 条件导出/聚合，禁止无条件引用所有组件模块和 CSS 常量。
  - source 模式下仅引入需要的组件源码，不通过中央注册表维持全组件可达。
  - 任意“全量组件映射表/注册表”若导致不可达代码变可达，直接判不通过。
  - 验证命令（特性树）：`cargo tree -e features -p ui --no-default-features --features component-accordion,inject-css`，确认仅启用目标组件特性链。
  - 验证命令（反向依赖）：`cargo tree -e features -i ui -p web-demo`，检查是否被 `all-components` 或隐式特性全量拉起。
  - CI 检查（最小特性编译）：新增任务仅开启目标最小特性（示例：`cargo check -p ui --target wasm32-unknown-unknown --no-default-features --features component-accordion,inject-css`）。
  - CI 检查（体积预算）：对“最小特性构建产物”设定预算并阻断回归（可用固定阈值，如 `< 50KB`，或基于仓库基线的相对阈值）；不得只做编译通过而不做体积约束。
- [x] 类型系统 + 语义标记共同提供机器可读状态；关键输入空间受类型约束。（`ActionBar` 的离散输入/状态轴由 `crates/ui-state-primitives/src/action_bar.rs` 的 `ActionBarPosition/ActionBarPhase/ActionBarSelectionKind` 等 `enum` 建模，并通过 `resolve_selection_kind` 与 `resolve_state` 统一归一化，避免字符串协议与布尔爆炸；`components/action-bar/src/view.rs` 对外稳定暴露 `data-state/data-selection/data-control-mode` 及 `data-*-source` 机器可读标记供测试与 Agent 消费。契约破坏点可由编译器（类型不匹配）与语义测试直接定位，回归由 `components/action-bar/test/semantics.rs::action_bar_discrete_state_axes_are_enum_typed`、`action_bar_machine_readable_contract_uses_typed_inputs_and_semantic_markers`、`action_bar_state_markers_are_observable_and_closed_set_contracts`、`action_bar_state_normalization_is_centralized_in_logic` 锁定。）
  - 离散输入与状态轴必须优先使用 `enum`/新类型建模，避免字符串协议与布尔爆炸。
  - 无效状态要么在类型层不可表达，要么在 `logic.rs` 被统一归一化并可测试。
  - 关键状态必须通过稳定语义标记对外可读，供测试与 Agent 自动化消费。
  - 编译器与测试反馈应能直接定位状态契约破坏点，形成可持续闭环。

### 4. DOM/环境边界治理
- [x] 焦点全局栈（Focus Stack & GC）：层叠 `Overlay` 禁止私存 `NodeRef` 作为恢复目标；必须依赖全局 Focus Manager（如 `FallbackTo/Selector`）防止焦点坠落到 `document.body`。（N/A：`ActionBar` 不是层叠 `Overlay` 组件，不承担焦点捕获/焦点恢复职责；`components/action-bar/src/view.rs` 中 `NodeRef` 仅用于 `motion::attach_motion` 动画挂载，未保存“恢复焦点目标”也未实现 overlay 焦点栈逻辑，因此不存在“焦点坠落到 document.body”的该类风险面。）
- [x] 受控外交特区（Escape Hatches）：集成 ECharts/Map 等命令式第三方库时必须处于 `Foreign Zone`（`YieldControl/CleanupForeign`）；第三方实例不得暴露为组件公共 API 或反向污染状态机。（N/A：`ActionBar` 未集成 ECharts/Map 等命令式第三方实例，组件依赖仅包含 `leptos/ui-state-primitives/ui-headless/ui-motion/ui-button`（见 `components/action-bar/src/Component.toml`），且公共导出面仅暴露 `ActionBar`/`ActionBarMotion`/状态类型，不存在第三方实例透传或反向污染状态机（见 `components/action-bar/src/mod.rs`、`components/action-bar/src/view.rs`）。）
- [x] SSR 时空断裂治理（Hydration Discontinuity）：逻辑初始化禁止依赖 `now()` 或原生随机 UUID；必须通过 `IdProvider` 注入确定性种子，确保 SSR/Hydration 间 ID 稳定。（N/A：`ActionBar` 当前不生成运行时 ID，也未使用 `now()/rand/uuid` 做初始化；`components/action-bar/src/view.rs`、`components/action-bar/src/logic.rs`、`crates/ui-state-primitives/src/action_bar.rs` 均无相关调用，且 `view.rs` 未出现需配对动态 ID 的 `id`/`aria-controls`/`aria-labelledby` 属性。SSR/Hydration 契约以稳定语义标记输出（`data-state`、`data-position`、`data-selection`），回归由 `components/action-bar/test/semantics.rs::action_bar_reduced_motion_ssr_wasm_branches_keep_semantics_consistent` 覆盖。）
- [x] SSR 与跨平台检查：覆盖 web/ssr/wasm 分支，不破坏 non-wasm 编译路径。（平台分支在 `components/action-bar/src/motion.rs` 显式采用 `#[cfg(target_arch = "wasm32")]` / `#[cfg(not(target_arch = "wasm32"))]`，避免运行时偶然分支；compile-only 证据由 `scripts/check-ui-platforms.sh` 覆盖 `cargo check -p ui`（默认 native）、`cargo check -p ui-headless --no-default-features --features ssr`（ssr native）、`cargo check -p ui --target wasm32-unknown-unknown --no-default-features --features component-action_bar,inject-css`（web wasm）。non-wasm 的 `web_sys` 禁止由脚本中 `components/action-bar/src/{mod,i18n,logic,styles,view}.rs` 源码守卫与语义回归 `components/action-bar/test/semantics.rs::action_bar_platform_guards_keep_cfg_split_and_non_wasm_web_sys_free` 双重锁定。）
  - 至少包含 compile-only 证据：web（wasm32）、ssr（native）、默认本地构建三条路径。
  - 平台分支差异必须显式 `cfg` 或 feature 管理，禁止依赖运行时偶然行为。
  - non-wasm 路径禁止引用 `web-sys`/浏览器对象。
- [x] `ui-headless` web/ssr feature 互斥受 `compile_error!` 保护（`crates/ui-headless/src/lib.rs`）。（`crates/ui-headless/src/lib.rs` 已声明 `#[cfg(all(feature = "web", feature = "ssr"))] compile_error!(...)` 互斥守卫；`ActionBar` 在 `components/action-bar/src/view.rs` 明确消费 `ui_headless::{use_controllable_state, locale_attrs, A11yDirection}`，未绕开该约束。验证路径由 `scripts/check-ui-platforms.sh` 覆盖：`cargo check -p ui-headless --no-default-features --features ssr`、`cargo check -p ui-headless --target wasm32-unknown-unknown --no-default-features --features web`，并且 `cargo check -p ui-headless --no-default-features --features web,ssr` 必须失败且日志命中 `mutually exclusive`。语义回归由 `components/action-bar/test/semantics.rs::action_bar_ui_headless_feature_mutex_contract_is_guarded` 锁定。）
  - 组件依赖 `ui-headless` 能力时，不得破坏其 web/ssr 互斥约束。
  - 组件若新增 headless 功能接入，需验证两条 feature 路径都可编译。
  - 发现“同时启用 web+ssr 仍可过编译”视为契约回归。
- [x] `ui-motion` 非 wasm 提供 no-op/stub（`crates/ui-motion/src/lib.rs`），保证 SSR/tooling 可编译。（`crates/ui-motion/src/lib.rs` 在 `#[cfg(not(target_arch = "wasm32"))]` 下提供 `web::prefers_reduced_motion() -> true` 与 `web::animate(...) {}` no-op/stub，并带 `non_wasm_web_backend_is_predictable_noop` 测试；`crates/ui-motion/tests/non_wasm_stub.rs` 进一步验证 non-wasm no-op 安全调用。`ActionBar` 侧 `components/action-bar/src/motion.rs` 提供 non-wasm 空实现 `attach_motion(...) {}`，且 wasm 分支仅在 `springs.get_value()` 为 `Some` 时才驱动动画，不假设动画句柄必然存在。tooling/编译守卫由 `scripts/check-ui-platforms.sh` 覆盖 `cargo check -p ui-motion` 与 `cargo test -p ui-motion --test non_wasm_stub`，并由 `components/action-bar/test/semantics.rs::action_bar_ui_motion_non_wasm_noop_stub_contract_is_guarded` 锁定。）
  - `motion.rs` 调用必须可在 non-wasm 下安全降级，不触发 panic。
  - 组件不得假设动画句柄一定存在；no-op 分支行为需可预测。
  - toolchain 场景（测试/文档/静态分析）不得因 motion 依赖阻塞编译。
- [x] 组件实现覆盖 `reduced-motion` / SSR / wasm 分支。（`components/action-bar/src/motion.rs` 在 `!motion.enabled || ui_motion::web::prefers_reduced_motion()` 路径下走最小必要反馈（清理 on_rest 并直接收敛到目标态），满足 reduced-motion 降级；同文件以 `#[cfg(target_arch = "wasm32")]` / `#[cfg(not(target_arch = "wasm32"))]` 显式分支，non-wasm 下 `attach_motion` 安全 no-op。`components/action-bar/src/view.rs` 保持统一语义输出（`role=\"toolbar\"`、`data-state/data-position/data-selection`、`aria-hidden`）且无平台 `cfg` 分裂，保证 SSR 输出与 hydration 语义一致。回归由 `components/action-bar/test/semantics.rs::action_bar_reduced_motion_ssr_wasm_branches_keep_semantics_consistent` 锁定；compile-only 路径由 `scripts/check-ui-platforms.sh` 覆盖 `cargo check -p ui-headless --no-default-features --features ssr` 与 `cargo check -p ui --target wasm32-unknown-unknown --no-default-features --features component-action_bar,inject-css`。）
  - `reduced-motion` 下动画应跳过或降级为最小必要反馈。
  - SSR 输出必须与客户端 hydration 兼容，避免首帧语义错位。
  - wasm 分支允许增强交互，但语义契约不得与 SSR 分支分裂。
- [x] 性能治理：关键路径有预算（首次渲染/更新耗时/内存），回归可检测、可归因、可阻断。（预算与阈值由 `apps/docs-app/src/pages/components/shell.rs` 的 `UiPerfBudget` 固化，覆盖基础组件 `button/input` 与本次组件 `action-bar`（`max_mount_ms/max_update_ms/max_heap_kb`）；`apps/docs-app/src/perf_probe.rs` 输出 `data-perf-*` 与 `data-perf-violation`，并以阈值判定回归。可重复基线与阻断由 `scripts/check-ui-performance.sh` 执行组件级语义性能门禁（含 `button/input/action-bar`），`e2e/tests/docs_app_components_coverage.spec.mjs` 断言预算标记存在且无 violation。可归因通过 `apps/docs-app/src/debug_overlay.rs` 的 trace 事件链与 `components/action-bar/src/view.rs` 的 `data-state/data-control-mode/data-selection-source/data-motion-source` 暴露。`render_count` 方面：`components/action-bar/test/semantics.rs::action_bar_performance_governance_budget_is_defined_and_blocking` 已要求清单保留“渲染次数预算=1 + 等价证据”条目，并由 `scripts/check-ui-performance.sh` 的 `perf_render_count_follow_up_is_tracked_in_plan` 与 `docs/plan/TODO.md` 跟踪自动化补齐。）
  - 关键交互组件需定义最小预算项（首渲染、关键更新、内存/分配趋势）。
  - 回归检测至少具备可重复基线与失败阈值，不靠主观“感觉变慢”。
  - 性能问题需可归因到状态、渲染、样式或动效路径之一。
  - 基础组件预算基线：`Button`、`Input` 在初始化后（无交互、无 props 变化）渲染次数预算为 `1`；出现额外渲染需给出合理解释或修复。
  - 测试要求：在 `components/*/test/**` 增加 `render_count` 类回归测试（测试框架支持时必须启用）；至少覆盖基础组件与本次改动组件。
  - 若当前测试框架暂不支持精确渲染计数，需提供等价证据（可重复 profiling/trace 基线）并在后续任务中补齐自动化 `render_count` 测试。
- [x] `view!` 宏复杂度受控：单个 `view!` 块不得承载超长深嵌套结构；复杂布局按语义分块，避免一次性宏展开导致编译与 wasm 体积劣化。（`components/action-bar/src/view.rs` 当前仅 2 处 `view!`（主结构 + clear-action 子块），主块按语义分为 `selection` 与 `actions` 两个区块，未出现深层重复嵌套；可复用片段（清除按钮）已局部抽出到 `on_clear_selection.map(...)` 子 `view!`，避免巨型单块展开。结构职责与可维护性由 `components/action-bar/test/semantics.rs::action_bar_component_files_follow_layered_responsibilities` 约束；编译/产物回归由 `scripts/check-ui-platforms.sh`（wasm compile-only）与 `scripts/check-ui-tree-shaking.sh`（wasm release 体积预算阻断）形成门禁。）
  - 复杂结构按语义子块拆分（header/body/item 等），避免巨型单块 `view!`。
  - `view.rs` 中若出现多层嵌套重复片段，应优先提取局部渲染函数。
  - 编译时间/产物体积异常增长时，优先排查宏展开体量。
- [x] 函数式拆分优先：不涉及复杂状态与生命周期管理的 UI 片段，优先拆为普通 Rust 函数（返回 `impl IntoView`/`View`），而不是新增 `#[component]`。（`components/action-bar/src/view.rs` 仅保留根组件 `#[component] fn ActionBar(...)`；轻逻辑 clear-action 片段已下沉为普通函数 `fn render_clear_action(...) -> impl IntoView`，避免为局部片段新增 `#[component]` 抽象噪音。主 `view!` 只做语义装配并调用该函数，语义标记与行为契约保持稳定（如 `data-state`/`role=\"toolbar\"`/clear action `on_press` 路径），对应回归由 `components/action-bar/test/semantics.rs::action_bar_component_files_follow_layered_responsibilities`、`action_bar_has_no_async_loading_protocol_and_keeps_sync_clear_contract`、`action_bar_emits_toolbar_semantics_and_state_attributes` 约束。）
  - 纯静态或轻逻辑片段优先函数化；仅在需要独立 props 语义时升级为组件。
  - 禁止把所有局部片段都升格为 `#[component]` 导致抽象噪音。
  - 拆分后语义标记与测试定位仍需稳定。
- [x] 静态片段常量化：复杂 SVG、页脚、长说明文本等纯静态内容优先常量化/模板化，减少重复 `view!` 渲染指令生成。（N/A：`ActionBar` 当前不包含复杂 SVG、页脚或长静态说明文案；`components/action-bar/src/view.rs` 渲染内容以状态语义标记与 i18n 文案（`aria_label`/`selection_text`/`clear_label`）为主，纯静态结构仅为短小容器标签且无跨处重复动态构造。静态 clear-action 结构已集中在单一路径 `render_clear_action(...)`，不存在同一静态资源散落多个 `view!` 片段导致维护分叉的问题。）
  - 可判定为纯静态的片段应避免重复动态构造。
  - 常量化后仍需维持可访问语义（title/aria-label/role 等）。
  - 静态资源变更路径要清晰，避免散落在多个 `view!` 片段中。
- [x] `inner_html` 使用约束：仅允许注入受信任静态常量，禁止拼接用户输入；使用处必须补充语义与安全回归测试。（N/A：`ActionBar` 实现未使用 `inner_html`/`set_inner_html`/`dangerous_inner_html` 注入路径（`components/action-bar/src/*.rs` 无匹配），渲染全部通过类型化 `view!` 与受控 props/文本节点输出，不存在用户输入拼接到 HTML 字符串的风险面。故本项“白名单常量 + 语义/安全回归补充”当前无适用注入点。）
  - 仅允许编译期常量或明确白名单内容进入 `inner_html`。
  - 严禁直接或间接注入用户输入、远端返回或未清洗模板字符串。
  - 使用 `inner_html` 的节点必须补语义测试与安全回归说明。
- [x] WASM 调试要求：关键状态可追踪（来源/时间/前后值），关键交互可回放，开发模式有可视化入口，调试能力通过 feature 隔离不污染产物。（`ActionBar` 复用全局调试基建而非组件私有调试协议：`ui-headless` 的 `UiTraceEvent{ ts_ms, component, kind }` 提供时间戳与事件来源（`crates/ui-headless/src/trace.rs`），`apps/docs-app/src/debug_overlay.rs` 提供 wasm 可视化入口 `UiDebugOverlay`（Inspect + Events 面板）并输出按事件序列可回放的最小复现链。`ActionBar` 关键状态通过稳定 `data-*` 来源标记暴露（`components/action-bar/src/view.rs`），可被 overlay Inspect/trace 关联定位。调试能力隔离在 docs-app 与全局 trace 层，`components/action-bar/src` 无 `wasm-debug` feature/公开调试 API 污染；该契约由 `components/action-bar/test/semantics.rs::action_bar_performance_governance_budget_is_defined_and_blocking` 中对 `debug_overlay`/`use_ui_trace`/`render_events` 的门禁断言覆盖。）
  - 开发模式下至少能追踪关键状态变更来源与前后值。
  - 关键交互链路应支持最小可复现记录（事件顺序/状态转移）。
  - 调试开关默认不进入生产包体与公共 API。
- [x] DX 要求：样式热重载优先无需重编 wasm；组件热开发尽量保持上下文；提供可选状态保留；有 Workbench 隔离画布。（`ActionBar` docs 页面已提供多组 `Playground`（`apps/docs-app/src/pages/components/pages/actions_extra.rs::action_bar`），其中 “Selection + clear action” 通过 `selected_count` + `set_selected_count` 保持交互上下文；`Playground` 基础设施自带 scoped CSS test 面板（`apps/docs-app/src/playground.rs` 中 `test_css_source` 回退与 `compose_scoped_css`），样式调试走 CSS 路径无需完整 wasm 重编。隔离演练入口由 `ComponentPage + Playground` 提供独立画布。可选状态保留在 `ActionBar` 范围内为 N/A（交互状态轴仅 `selected_count`，无复杂多步骤跨会话上下文；当前以受控/非受控示例保证本地上下文连续）。）
  - 常见样式调整应走快速反馈路径，不依赖完整 wasm 重编译。
  - 组件调试应尽量保持当前交互上下文，降低重复操作成本。
  - 复杂交互组件应有隔离演练入口（workbench/story/demo 之一）。
- [x] 工程能力统一：`serde` 负责 spec 序列化/版本迁移/错误结构化；`tracing` 统一 span/event 语义；async 不绑定单一运行时（tokio/async-std），runtime 细节不泄露到上层 API。（`ActionBar` 已有版本化协议骨架 `components/action-bar/src/protocol.rs`：`ActionBarComponentSchemaVersion` + `ActionBarComponentSpec` 使用 `serde::{Serialize, Deserialize}` 并由 `components/action-bar/test/protocol.rs` 锁定可序列化/反序列化契约。`tracing` 语义统一沿用全库 `ui-headless::UiTrace`（`crates/ui-headless/src/trace.rs`）与 docs debug overlay 观测链，组件层未私造并行埋点语义。async/runtime 侧为 N/A：`ActionBar` 无异步工作流且对外 API 未暴露 `tokio/async-std/Future` 等 runtime 细节，回归由 `components/action-bar/test/semantics.rs::action_bar_has_no_async_loading_protocol_and_keeps_sync_clear_contract` 覆盖。）
  - 若组件涉及 spec/config 输入，序列化与错误输出应走统一结构化路径。
  - 关键流程埋点语义应与全库 tracing 约定一致，避免组件各说各话。
  - 异步边界不得把具体 runtime 类型暴露到组件公共接口。

### 5. 样式与动效（Theme & Motion）
- [x] 样式孤岛防御（Defensive Variables）：`styles.rs` 使用双层回退链 `var(--ui-*, var(--ui-fallback-*))`；禁止组件内硬编码 Hex 或裸尺寸终值，Fallback 终值由 `ui-theme` 统一输出（SSOT）。（`components/action-bar/src/styles.rs` 已将主题变量消费统一为双层回退链（如 `var(--ui-space-xl, var(--ui-fallback-space-xl))`、`var(--ui-border, var(--ui-fallback-border))`、`var(--ui-shadow-md, var(--ui-fallback-shadow-md))`），并移除组件内固定尺寸终值改为 token/clamp 驱动；对应 fallback 终值由 `crates/ui-theme/src/css.rs` 统一输出（含 `--ui-space-xl`/`--ui-fallback-space-xl`、`--ui-fallback-bg`、`--ui-fallback-shadow-md`、`--ui-fallback-focus-ring` 及 action-bar 专项 fallback）。回归断言补充在 `components/action-bar/test/semantics.rs::action_bar_theme_contract_is_token_first_and_ui_theme_owned`。）
- [x] 级联层覆盖（`@layer ui`）：组件 CSS 默认聚合进 `@layer ui`；运行时数值调整仅通过 CSS Custom Properties（如 `style:--x=...`），禁止普通内联样式（如 `style=\"top: 10px\"`）。（`crates/ui/src/css.rs` 的 `push_components_css` 已以 `out.push_str("\n@layer ui {\n"); ... out.push_str("\n}\n");` 包裹组件样式聚合，`component-action_bar` 分支在层内注入 `crate::action_bar::styles::CSS`。`components/action-bar/src/view.rs` 无 `style=`/`style:` 业务内联样式，运行时数值写入仅在 `components/action-bar/src/motion.rs` 通过 `set_property("--ui-action-bar-translate-y", ...)` 与 `set_property("--ui-action-bar-opacity", ...)` 这两个 CSS 自定义属性完成。语义回归由 `components/action-bar/test/semantics.rs::action_bar_token_first_styles_are_static_and_aggregated_via_ui_root` 与 `action_bar_styles_depend_on_explicit_state_markers_not_dom_guessing` 锁定。）
- [x] Motion 合同化：`stiffness`/`damping` 等参数在 `motion.rs` 内置为组件 Contract，并通过 `attach_motion` 挂载；必须尊重 `prefers-reduced-motion` 且在 non-wasm/SSR 安全降级（no-op）。（`components/action-bar/src/motion.rs` 已以内置 `ActionBarMotion { spring: ui_motion::spring::SpringConfig, ... }` 作为组件动效 Contract，并在 `sanitize_motion` 中对 `spring.stiffness/spring.damping/mass/precision` 做合法化归一；`components/action-bar/src/view.rs` 通过 `motion::attach_motion(root_ref, visible, motion)` 完成挂载。wasm 分支在 `!motion.enabled || ui_motion::web::prefers_reduced_motion()` 下走降级路径；non-wasm 分支提供 `#[cfg(not(target_arch = "wasm32"))] pub fn attach_motion(...) {}` no-op，满足 SSR/tooling 安全降级。回归由 `components/action-bar/test/semantics.rs::action_bar_motion_contract_defaults_and_disabled_path_are_locked`、`action_bar_motion_sanitization_and_reduced_motion_paths_are_locked`、`action_bar_motion_uses_spring_driver`、`action_bar_motion_stays_as_mapping_and_attach_layer` 锁定。）
- [x] `ui` 固定入口文件落点正确。（`crates/ui/src/lib.rs` 维持总入口与稳定对外导出：`mod css;`、`pub mod root;`、`pub use root::UiRoot;`，并以 `#[cfg(feature = "component-action_bar")]` 门控 `pub use ui_action_bar as action_bar;` 与 `pub use action_bar::{ActionBar, ActionBarMotion, ActionBarPosition};`；`crates/ui/src/css.rs` 以 `push_components_css` 作为组件 CSS 聚合入口并受 `inject-css + component-*` 条件注入；`crates/ui/src/root.rs` 在 `UiRoot` 中集中注入 `BASE_CSS + theme vars + optional components css`，并提供 `provide_ui_i18n/provide_ui_id_provider`。共享高亮能力位于 `crates/ui-visual-primitive/src/active_highlight.rs`，仅承载通用样式与 motion driver。`crates/ui/src/overlay_open.rs`、`crates/ui/src/presence.rs`、`crates/ui/src/a11y.rs` 均不存在，对应共享原语位于 `crates/ui-headless/src/{controllable_state,presence,a11y}.rs`。以上由 `components/action-bar/test/semantics.rs::action_bar_entrypoint_files_and_headless_boundaries_are_stable` 锁定。）
  - `crates/ui/src/lib.rs`：总模块入口 + 对外 `pub use`（公共 API 面）；组件模块受 `component-*` feature gate 约束；不暴露内部平台细节类型。
  - `crates/ui/src/css.rs`：组件 CSS 聚合入口（`push_components_css`）；按 feature 条件注入；禁止无条件聚合全部组件 CSS。
  - `crates/ui/src/root.rs`：`UiRoot` 统一注入 base css + theme vars +（可选）components css，并提供全局 i18n 上下文；主题与注入策略必须集中在此。
  - `crates/ui-visual-primitive/src/active_highlight.rs`：共享高亮条样式与 motion driver；只承载通用高亮动效能力，不承载具体组件业务语义。
  - `crates/ui/src/overlay_open.rs`：当前仓库中不应存在；open-state 原语固定在 `crates/ui-headless/src/controllable_state.rs`，组件通过 headless API 消费。
  - `crates/ui/src/presence.rs`：当前仓库中不应存在；presence 原语固定在 `crates/ui-headless/src/presence.rs`，组件通过 `ui_headless::use_presence` 消费。
  - `crates/ui/src/a11y.rs`：当前仓库中不应存在；共享 A11y 工具固定在 `crates/ui-headless/src/a11y.rs`（如 `aria_controls_when_open`），组件只负责挂载。
- [x] 组件目录标准文件落点正确。（`components/action-bar/src/` 具备并使用标准实现文件 `mod.rs`、`logic.rs`、`styles.rs`、`view.rs`、`motion.rs`；不存在 `render.rs` 漂移。`mod.rs` 维持最小稳定导出面（仅导出 `ActionBar`/`ActionBarMotion` 等必要 API，未对 `logic/view` 过度公开）；`logic.rs` 负责归一与派生并消费 `ui-state-primitives`，不含 `view!`/DOM；`styles.rs` 保持静态 token-first CSS；`view.rs` 承担 Leptos 结构渲染与 headless 语义挂载并调用 `logic::resolve_view_state`；`motion.rs` 提供 `ActionBarMotion + attach_motion` 且仅做语义到 motion contract 映射与挂载。`spec.rs` 约束已由 `components/action-bar/test/semantics.rs::action_bar_spec_boundary_reuses_button_spec_without_local_spec_file` 锁定（ActionBar 不新增本地 `spec.rs`）。综合回归由 `action_bar_component_files_follow_layered_responsibilities`、`action_bar_does_not_expose_logic_or_view_modules`、`action_bar_stays_in_ui_components_assembly_layer_and_public_api_boundary_is_stable` 覆盖。）
  - `<component>/mod.rs`：最小稳定导出面，存在且无过度导出。
  - `<component>/logic.rs`：props 归一化、派生状态、来源标记；不得承载可下沉原语。
  - `<component>/styles.rs`：静态 CSS 契约，只用 `var(--ui-*)`，不写死主题常量。
  - `<component>/view.rs`：纯 Leptos 结构渲染 + headless 语义挂载；禁止 `render.rs` 漂移；不隐藏关键状态决策。
  - `<component>/motion.rs`：`XxxMotion + attach_motion`；交互组件必须有；只做语义到 motion contract 的映射与挂载。
  - `<component>/spec.rs`：仅极少数组件专用（当前主要 button），无必要不新增。

### 6. AI 原生能力与文件落点（Struct-First & Projection）
- [x] 文件落点纪律：组件目录严格由 `mod.rs`（导出）、`logic.rs`（归一派生）、`styles.rs`（Token 样式）、`view.rs`（渲染）、`motion.rs`（动效）组成；复杂组件可选 `spec.rs`；禁止 `render.rs`。（`components/action-bar/src/` 当前实现文件落点完整：`mod.rs`、`logic.rs`、`styles.rs`、`view.rs`、`motion.rs` 均存在；`render.rs` 不存在；`spec.rs` 对 ActionBar 保持未引入（复杂规范能力仍集中在 button）。目录与边界由 `components/action-bar/test/semantics.rs::action_bar_file_placement_discipline_is_enforced`、`action_bar_component_files_follow_layered_responsibilities`、`action_bar_spec_boundary_reuses_button_spec_without_local_spec_file` 联合锁定。）
- [x] Hyper-Structure Builder（`spec.rs`）：复杂组件必须提供 AI 友好的 `*Spec::new()...render()` 建造者 API。（N/A（ActionBar 非复杂 schema 组件）：`ActionBar` 维持轻量目录，不引入本地 `spec.rs`。复杂组件 builder 能力由 button 承担并保持 `ButtonSpec::new()...render()` 路径（`components/button/src/spec.rs`）。约束由 `components/action-bar/test/semantics.rs::action_bar_spec_boundary_reuses_button_spec_without_local_spec_file` 锁定：一方面禁止 ActionBar 暴露 `ActionBarSpec/ActionBarSchema`，另一方面确保 button 侧 `ButtonSpec` builder+render 合同仍存在。）
- [x] 上下文压缩协议（Manifest + RBI）：新增/大改组件必须同步维护组件目录下 `Component.toml`（能力清单）和 `.rbi`（接口签名投影），避免 AI 检索工具箱过时。（`ActionBar` 已在 `components/action-bar/src/` 维护 `Component.toml`（能力清单）与 `action_bar.rbi`（接口签名投影）；`Component.toml` 覆盖输入/输出/capabilities/dependencies，`action_bar.rbi` 覆盖公开枚举、`ActionBarMotion` 与 `ActionBar(...)` 签名。回归锁定见 `components/action-bar/test/semantics.rs::action_bar_manifest_and_rbi_projection_are_present_and_in_sync`。）
- [x] 语义标记统一升级为 Agent Contract（Schema 化），让 Agent 不依赖 DOM 猜测理解组件状态与意图。（`ActionBar` 已在 `components/action-bar/src/protocol.rs` 提供类型化 Agent Contract：`ActionBarAgentIntent/ActionBarAgentAction/ActionBarAgentDataAttrs` 与 `agent_data_attrs(state)`，并固定 schema id `ui.action-bar.contract.v1`；`components/action-bar/src/view.rs` 挂载 `data-ui-schema`、`data-ui-intent`、`data-ui-action` 及 `data-ui-state-*` / `data-ui-source-*` 字段，字段值统一来自 typed contract 产物而非字符串拼接。渲染链路通过 `ActionBarComponentSpec::render_policy()` 的 capability 白名单门控（`allow_clear_action/allow_children_slot`），未引入脚本注入路径。`components/action-bar/src/Component.toml` 同步登记 `data-ui-*` 输出与 `agent_contract_schema_v1_with_typed_state_source_markers` 能力。回归由 `components/action-bar/test/semantics.rs::action_bar_agent_contract_schema_is_typed_and_whitelisted` 锁定。）
  - 关键交互组件必须输出稳定机器可读语义（至少 `data-*` + 状态来源标记；复杂组件建议补 `data-ui-schema`）。
  - Agent 消费字段应来自类型化 schema 生成，不允许散落字符串拼接。
  - 契约字段需可追溯到组件状态轴与动作语义（intent/action/state/source）。
  - 配置到组件的渲染链路必须走白名单能力边界，禁止任意脚本注入。
- [x] 流式在这里仅指 LLM 输出渲染（只看两种显示模式）。（N/A：`ActionBar` 不是 LLM 正文阅读面，而是批量操作条组件；组件 API 不暴露 `is_streaming/mode/chunk` 等流式输出协议字段，文档示例也不包含 streaming/snapshot 切换入口。当前仅消费完整状态输入（如 `selected_count/default_selected_count/selection_text`）并稳定渲染语义标记。回归由 `components/action-bar/test/semantics.rs::action_bar_is_not_llm_output_surface_and_has_no_streaming_snapshot_modes` 锁定。）
  - `Streaming`：LLM 还在生成，界面边生成边显示。
  - `Snapshot`：LLM 全部生成完成后，一次性显示。
- [x] `Snapshot` 是所有组件的基础能力（默认必须支持）。（`ActionBar` 已支持“完整配置一次性输入 -> 稳定渲染”路径：`view.rs` 公共 props 覆盖 `selected_count/default_selected_count/on_selected_count_change/on_clear_selection/position/is_force_visible/aria_label/clear_label/selection_text/lang/dir/motion/class_name/children`，并稳定输出 `data-state/data-position/data-selection/data-control-mode/data-selected-count-source/data-ui-schema` 等语义标记。docs 示例同时覆盖受控与高配快照场景（含 `is_force_visible`、`position`、`selection_text`、`clear_label`、`motion` 等组合）。回归由 `components/action-bar/test/semantics.rs::action_bar_snapshot_mode_can_consume_complete_input_and_render_stably` 锁定。）
  - 所有组件都应能消费“完整生成结果”并稳定渲染。
  - 即使组件不直接展示正文，也应能在接收上层完整配置后正常渲染。
- [x] `Streaming` 是否强制，按组件职责判断（不能一刀切）。（`ActionBar` 非正文阅读面，采用 `Streaming Optional`：不实现流式分片协议，但在组件语义契约中显式给出 `data-ui-streaming-policy="optional"` 与 `data-ui-streaming-fallback="snapshot"`。同时显式输出状态标记 `data-ui-output-mode="snapshot"`、`data-ui-output-status="validated"`，并保持 `role="toolbar"`、`aria-hidden`、`data-state` 等连续可读语义。数据校验/断线恢复/重试未下沉到组件层，仍由上层负责；组件仅负责稳定渲染。实现位于 `components/action-bar/src/protocol.rs` 与 `components/action-bar/src/view.rs`，清单映射位于 `components/action-bar/src/Component.toml`，回归由 `components/action-bar/test/semantics.rs::action_bar_streaming_is_optional_with_snapshot_fallback_and_status_markers` 锁定。）
  - `Streaming Required`：组件本体就是正文阅读面，用户需要边生成边看。
  - `Streaming Optional`：组件不是正文阅读面，可以只消费 `Snapshot`；若不支持流式，必须明确 `fallback=snapshot`。
  - 无论是否支持 `Streaming`，都要显式标识当前输出状态（草稿/已验证/可提交），并保持 `role`/`aria-*`/`data-*` 连续可读。
  - 数据校验、断线恢复、重试策略由上层负责，组件层只负责稳定渲染。

### 7. 测试、门禁与交付
- [x] 代码卫生（Rust Hygiene）：非测试代码中完全禁止 `unwrap/expect`，禁止无处理的 `let _ = ...`；字符串复制热点收敛为 `Cow<'static, str>`（执行 `./scripts/check-rust-hygiene.sh` 验证）。（`ActionBar` 非测试源码已满足 hygiene 约束：`components/action-bar/src/` 下无 `unwrap/expect/let _ = ...`，且 `components/action-bar/src/logic.rs` 的 class 组装已改为 `Cow<'static, str>`（`Cow::Borrowed` + `Cow::Owned`）收敛字符串复制热点。回归由 `components/action-bar/test/semantics.rs::action_bar_rust_hygiene_avoids_unwrap_expect_let_underscore_and_string_clone_churn` 与 `components/action-bar/test/protocol.rs::agent_contract_defaults_to_streaming_optional_snapshot_output` 联合锁定。补充说明：仓库脚本 `./scripts/check-rust-hygiene.sh` 在当前环境被上游工具链阻断（先是 `./scripts/check-api-contracts.sh` 执行权限，改用 `bash` 后又因 `rg` 缺少 PCRE2 导致 baseline 检查失败），属于环境问题而非 ActionBar 组件代码回归。）
- [x] Tree Shaking & 特性剪裁：组件必须注册到 `ui` 特性树（如 `component-accordion`）；`css.rs` 和 `lib.rs` 聚合必须受 feature 门控，禁止无条件全局依赖。（`ActionBar` 已注册 `ui` 特性树：`crates/ui/Cargo.toml` 含 `component-action_bar = ["component-button", "dep:ui-action-bar"]`；`crates/ui/src/lib.rs` 通过 `#[cfg(feature = "component-action_bar")] pub use ui_action_bar as action_bar;` 做模块导出门控；`crates/ui/src/css.rs` 通过 `#[cfg(feature = "component-action_bar")] out.push_str(crate::action_bar::styles::CSS);` 做样式聚合门控，未出现无条件全量依赖。回归由 `components/action-bar/test/semantics.rs::action_bar_tree_shaking_keeps_component_feature_and_css_boundaries` 与 `action_bar_tree_shaking_check_script_covers_feature_tree_wasm_and_budget` 锁定；`apps/web-demo/Cargo.toml` 使用 `web-demo-components` 且不拉起 `all-components`。）
- [x] 语义测试与性能回归：断言必须覆盖 `aria-*`、`data-*` 与焦点流转，不能只看快照；高频/重型组件必须补齐 `render_count` 断言/测量（如初始化空闲预算为 1）。（`ActionBar` 的语义回归已覆盖 `aria/data` 与交互路径：`components/action-bar/test/semantics.rs::action_bar_emits_toolbar_semantics_and_state_attributes`、`action_bar_semantics_cover_data_aria_and_interaction_matrix`、`action_bar_mounts_headless_contract_in_view_not_logic_layer`。焦点流转通过复用 `ui_button::Button` 的 headless 契约实现，且由 `components/button/test/semantics.rs` 的 `use_focus_ring`/`data-focus-visible` 门禁锁定。性能侧 `components/action-bar/test/semantics.rs::action_bar_performance_governance_budget_is_defined_and_blocking` + `action_bar_semantic_and_performance_regression_gates_cover_aria_data_focus_and_render_count_path` 要求预算门禁脚本保留 ActionBar 性能断言与 `render_count` 跟踪；当前渲染次数基线采用“预算=1 的等价证据 + TODO 自动化跟踪”（`docs/plan/TODO.md` + `scripts/check-ui-performance.sh`），待测试框架能力补齐后切换为精确 `render_count` 自动化断言。）
- [x] 版本弃用迁移（Codemod/Registry）：若提交包含跨大版本 API 破坏升级，必须在 Schema Registry 注册弃用窗口并提供纯函数迁移层（`migrate_v1_to_v2`）。（N/A：当前 ActionBar 变更未引入跨大版本 API 破坏升级，公共签名与 schema 仍保持 v1 稳定契约（`components/action-bar/src/action_bar.rbi` 的 `ActionBar(...)` 参数集未出现破坏性改名/删除；`components/action-bar/src/protocol.rs` 仍为 `ACTION_BAR_AGENT_SCHEMA = \"ui.action-bar.contract.v1\"`；`components/action-bar/src/Component.toml` 保持 `schema_version = \"1\"` 且 `data-ui-schema` 为 v1）。因此本次无需注册弃用窗口或提供 `migrate_v1_to_v2`。回归由 `components/action-bar/test/semantics.rs::action_bar_version_deprecation_migration_is_na_without_major_breaking_upgrade` 锁定，并显式禁止 `migrate_v1_to_v2/contract.v2/schema_version=2` 漂移。）
- [x] 文档即产品（Copy-Paste Ready）：`apps/docs-app` 必须新增 Playground（Hello World、状态矩阵、受控/非受控对照），支持流式/快照展现，并提供 Source-first 一键复制且补全 imports。（`ActionBar` 文档页 `apps/docs-app/src/pages/components/pages/actions_extra.rs::action_bar` 已具备并新增完整验收面：`Hello World`、`Controlled vs Uncontrolled`、`State Matrix (selection + placement + visibility)`、`Snapshot baseline + Streaming optional fallback`。其中受控/非受控分别通过 `selected_count + on_selected_count_change` 与 `default_selected_count` 对照；流式/快照按组件职责采用“Snapshot 基线 + Streaming Optional fallback=snapshot”显示说明，不引入正文流式协议。Source-first 复制链路通过 `Playground` 的 `code_imports=action_bar_code_imports.clone()` 显式补全 imports（`use leptos::prelude::*; use ui::{ActionBar, ActionBarMotion, ActionBarPosition, ActionButton};`），并由 `apps/docs-app/src/playground.rs::compose_copy_ready_code` 统一注入缺失 import。回归由 `components/action-bar/test/semantics.rs::action_bar_docs_copy_paste_ready_contract_covers_playgrounds_matrix_control_and_snapshot_modes` 锁定。）
- [x] 语义测试优先：验证 `data-*` / `aria-*` / role / 状态来源契约，不只视觉快照。（`ActionBar` 已具备专属 `*_semantics.rs` 回归：`components/action-bar/test/semantics.rs`。语义断言覆盖 `role/aria/data-*` 与状态来源契约：`action_bar_emits_toolbar_semantics_and_state_attributes`、`action_bar_machine_readable_contract_uses_typed_inputs_and_semantic_markers`、`action_bar_semantics_cover_data_aria_and_interaction_matrix`；并通过 `action_bar_semantics_suite_prioritizes_contract_assertions_over_snapshots` 明确禁止把视觉快照当主断言。新增/变更语义字段也已同步补测：`data-ui-*` 契约由 `action_bar_agent_contract_schema_is_typed_and_whitelisted` 与 `action_bar_streaming_is_optional_with_snapshot_fallback_and_status_markers` 锁定，文档新增语义展示由 `action_bar_docs_copy_paste_ready_contract_covers_playgrounds_matrix_control_and_snapshot_modes` 锁定。）
  - 每个交互组件至少有对应 `*_semantics.rs` 测试覆盖关键状态轴与动作语义。
  - 断言应聚焦语义契约（状态来源/可访问性/键盘路径），快照仅作补充。
  - 新增/变更语义字段必须同步补测试，否则不得打勾。
- [x] E2E 选择器稳定：使用语义标记，WASM 场景有稳定等待策略。（`ActionBar` E2E 已升级为语义选择器优先：`e2e/tests/docs_app_action_bar.spec.mjs` 使用 `[data-component=\"action-bar\"]`、`[data-slot=\"action-bar\"][data-control-mode=\"controlled\"][data-has-clear=\"true\"]`、`[data-slot=\"action-bar-clear\"] [data-slot=\"button\"]`、`[data-slot=\"button\"][aria-label=\"Increase selected count\"]`，不依赖文本定位或脆弱层级。WASM 等待采用语义就绪条件（`body:not(:has(#boot))` + `toHaveAttribute(data-state/data-selected-count/aria-hidden)`），未使用固定 sleep。组件涉及动效显隐路径时，已在 E2E 中显式覆盖 ready/settled 断点（`visible -> hidden -> visible` 及 `data-selected-count` 变化）。回归由 `components/action-bar/test/semantics.rs::action_bar_check2_documents_e2e_selector_and_stable_wait_rules`、`action_bar_e2e_selector_contract_uses_semantic_markers_and_settled_waits`、`action_bar_e2e_animation_path_covers_ready_and_settled_semantic_breakpoints` 锁定。）
  - E2E 选择器优先 `data-*` 语义标记，禁止依赖脆弱 DOM 层级或文本定位。
  - WASM 场景必须使用稳定等待策略（语义状态就绪而非固定 sleep）。
  - 若组件涉及异步/动画，E2E 需显式覆盖 ready/settled 条件。
- [x] 关键流程纳入可重复回归集合（Playwright/Cypress）。（`ActionBar` 已纳入可重复 Playwright 回归：`e2e/tests/docs_app_action_bar.spec.mjs` 定义 `docs-app action-bar key flow is repeatable with semantic breakpoints`，覆盖“打开页面 -> clear 触发隐藏 -> increment 恢复显示 -> reload -> 同流程复跑”，并在每一步用语义断点断言（`data-state`、`data-selected-count`、`aria-hidden`）定位失败点，不依赖笼统页面快照。高风险路径方面：组件无 overlay/async 场景（N/A），但已优先覆盖 focus + keyboard 路径（`clearButton.focus()` + `page.keyboard.press(\"Space\")`）及动效 settled 断点。回归门禁由 `components/action-bar/test/semantics.rs::action_bar_check2_documents_e2e_repeatable_key_flow_rules`、`action_bar_e2e_key_flow_is_repeatable_and_failure_points_are_semantic`、`action_bar_e2e_high_risk_paths_cover_focus_keyboard_and_settled_semantic_breakpoints` 锁定。）
  - 至少定义一条可重复关键流程（打开/交互/关闭或提交）纳入 E2E 回归。
  - 回归失败需可定位到具体语义契约断点，而不是笼统“页面不一致”。
  - 高风险路径（overlay、focus、keyboard、async）优先进入回归集合。
- [x] docs-app 文档、示例、参数矩阵、状态矩阵同步更新。（`apps/docs-app/src/pages/components/pages/actions_extra.rs::action_bar` 已同步补齐文档面：保留并更新 `Hello World`、`Controlled vs Uncontrolled`、`State Matrix (selection + placement + visibility)` 示例；新增 `docs-card` 语义区块 `data-slot="action-bar-api-matrix"` 与 `data-slot="action-bar-state-matrix"`，参数矩阵覆盖 `selected_count/default_selected_count/on_selected_count_change/on_clear_selection/position/is_force_visible/aria_label/clear_label/selection_text/lang+dir/motion/class_name`，默认值直接对齐 `components/action-bar/src/logic.rs`（`normalize_default_selected_count -> unwrap_or_default`）与常量 `DEFAULT_ARIA_LABEL/DEFAULT_CLEAR_LABEL`。状态矩阵显式列出 `controlled|uncontrolled`、`visible|hidden`、`top|bottom`、`empty|single|multiple` 以及来源轴；`disabled/size/variant` 对 `ActionBar` 根节点标注 N/A 并指向子动作组件。回归由 `components/action-bar/test/semantics.rs::action_bar_docs_api_and_state_matrix_track_logic_defaults_and_contract_axes` 锁定。）
  - 组件行为或参数变更必须同步更新 `apps/docs-app` 示例与说明。
  - 文档示例需覆盖至少一组状态矩阵（受控/非受控、disabled、size/variant 等）。
  - 文档中的 API 名称与默认值必须和 `logic.rs` 当前实现一致。
- [x] 组件文档必须对新手友好（Documentation as Product）：组件 README 或等价文档入口必须存在。（已补齐组件 README：`components/action-bar/src/README.md`，包含零门槛 `Hello World`、常见用法（Controlled/Uncontrolled）与“先默认路径、后高级参数”的学习顺序；并与 docs-app 入口 `apps/docs-app/src/pages/components/pages/actions_extra.rs::action_bar` 对齐（`Hello World` -> `Controlled vs Uncontrolled` -> advanced playground）。回归由 `components/action-bar/test/semantics.rs::action_bar_documentation_as_product_keeps_readme_and_beginner_first_flow` 锁定，防止退化为“只有源码”或“内部实现先行文档”。）
  - 每个基础组件必须提供“零门槛”最小示例（Hello World）与常见用法，避免要求用户先理解底层分层架构。
  - 文档需明确“先用起来，再进阶”：默认 API 路径在前，高级控制参数在后。
  - “只有源码没有文档”或“只写给架构师/机器看的文档”视为不通过。
- [x] `apps/docs-app` 必须提供 Interactive Playground：用户可在线修改 props/状态并实时预览。（`apps/docs-app/src/pages/components/pages/actions_extra.rs::action_bar` 已新增 `Interactive Playground (Props + State + Spec Preview)`：通过 `controls` 面板在线调节 `position/is_force_visible/clear-action/custom-labels/reduced-motion`，并在预览区实时映射到 `ActionBar` props；交互反馈由 `selected_count` 实时显示与 `Select +1/Select -1/Reset` 操作验证。对 AI Spec 相关验收，已提供 `test_config_signal=interactive_spec_preview`，将控制输入联动投影为 `ActionBarInteractiveSpec` 预览输出。可重复关键路径在 Playground 中显式给出 `Select +1 -> Clear selection -> Select +1`，并与既有 E2E 回归（`e2e/tests/docs_app_action_bar.spec.mjs`）保持一致。回归由 `components/action-bar/test/semantics.rs::action_bar_docs_interactive_playground_supports_props_state_and_repeatable_flow` 锁定。）
  - Playground 至少支持基础 props 调整、状态切换、交互反馈观察。
  - 对 AI Spec 相关组件，至少提供一组 Spec 输入与预览输出的联动示例。
  - Playground 作为验收面，需可重复复现关键交互路径。
- [x] Source-first 文档必须 Copy-Paste Ready：提供一键复制组件源码或最小可用片段能力。（`apps/docs-app/src/pages/components/pages/actions_extra.rs::action_bar` 已新增 `data-slot="action-bar-source-first"` 文档区块，明确源码落点（`components/action-bar/src/{mod.rs,view.rs,logic.rs,styles.rs,motion.rs}`）与依赖前提（`ui` 特性：`component-action_bar + inject-css`），并在 Playground `Show code` 面板继续通过 `code_imports` 输出可直接运行片段。复制能力由 docs 基础设施 `apps/docs-app/src/playground.rs::compose_copy_ready_code` + `CodeBlock` 复制按钮链路提供（`components/code-block/src/view.rs`）。回归由 `components/action-bar/test/semantics.rs::action_bar_source_first_docs_are_copy_paste_ready_with_real_paths_and_dependencies` 锁定。）
  - docs-app 页面应提供复制按钮，输出代码默认可直接运行（含必要 imports/依赖提示）。
  - 若为 source-first 组件，文档需指向真实源码落点并说明依赖前提，避免“复制即报错”。
  - 文档代码与当前实现必须同步，防止示例漂移。
- [x] HeroUI 对标文档与组件文档同步：参数模型变更需同步 `docs/spec/heroui-parameter-design-strategy.md`（必要时补充 `docs/research/spectrum-heroui-style-interface-study.md`），并保证组件文档可访问。（已新增 `docs/spec/heroui-parameter-design-strategy.md` 的 `ActionBar 同步记录（2026-02-20）`，明确当前参数模型（`selected_count/on_selected_count_change/default_selected_count`、`on_clear_selection`、`position/is_force_visible`、`selection_text/clear_label`、`motion/lang/dir`）与 docs 同步策略；docs 入口可索引证据为 `apps/docs-app/src/pages/components/pages.rs` 的 `component_doc!(\"ActionBar\", \"action-bar\", \"Actions\", ax::action_bar)` 与 `apps/docs-app/src/pages/components/pages/actions_extra.rs` 的 `slug=\"action-bar\"`。本轮未引入新的 Spectrum/HeroUI 风格结论，因此无需补充 `docs/research/spectrum-heroui-style-interface-study.md`。回归由 `components/action-bar/test/semantics.rs::action_bar_heroui_strategy_and_component_docs_stay_in_sync_for_parameter_changes` 锁定，防止“仅代码更新无文档更新”回归。）
  - 若参数语义发生变化，需同步更新对标策略文档，不允许实现先漂移文档后补。
  - 组件文档入口必须存在（docs-app 页面或等价文档），且可被索引定位。
  - “仅代码更新无文档更新”在接口变更场景下直接判不通过。

### 8. 合并前门禁死命令（最终执行）
在发起 PR 或完成任务前，必须保证本地/CI 以下命令全部通过：

- `cargo fmt --all -- --check`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `./scripts/check-rust-hygiene.sh`
- `cargo check -p ui --target wasm32-unknown-unknown`
- `cargo check -p ui-headless --no-default-features --features ssr`
- `cargo check -p ui --target wasm32-unknown-unknown --no-default-features --features component-<your_component>,inject-css`

依据文档（`rust-ui/docs/spec` 及 `rust-ui/docs`）：

- `rust-ui/docs/spec/ai_context_projection_protocol.md`
- `rust-ui/docs/spec/architectural_fitness_functions.md`
- `rust-ui/docs/spec/async_state_as_data_command.md`
- `rust-ui/docs/spec/collection_registration_protocol.md`
- `rust-ui/docs/spec/compile_time_evolution_migration.md`
- `rust-ui/docs/spec/component_boundaries.md`
- `rust-ui/docs/spec/component_domains.md`
- `rust-ui/docs/spec/controlled_evolution_sandbox.md`
- `rust-ui/docs/spec/core_shell_protocol_infra_baseline.md`
- `rust-ui/docs/spec/environment_subscription_streams.md`
- `rust-ui/docs/spec/event_light_cone_signal_bus.md`
- `rust-ui/docs/spec/focus_global_stack_gc.md`
- `rust-ui/docs/spec/foreign_zone_escape_hatches.md`
- `rust-ui/docs/spec/headless_purification.md`
- `rust-ui/docs/spec/heroui-parameter-design-strategy.md`
- `rust-ui/docs/spec/hyper-structure-ui-development-playbook.md`
- `rust-ui/docs/spec/i18n.md`
- `rust-ui/docs/spec/intent_stack_semantic_layering.md`
- `rust-ui/docs/spec/kernel_shell_architecture.md`
- `rust-ui/docs/spec/macro_micro_dual_state_machine.md`
- `rust-ui/docs/spec/motion.md`
- `rust-ui/docs/spec/mvp.md`
- `rust-ui/docs/spec/platform_abdication_ecosystem.md`
- `rust-ui/docs/spec/README.md`
- `rust-ui/docs/spec/release_versioning.md`
- `rust-ui/docs/spec/side_effect_command_pattern.md`
- `rust-ui/docs/spec/slot_projection_strategy.md`
- `rust-ui/docs/spec/ssr_hydration_discontinuity.md`
- `rust-ui/docs/spec/state_primitives_core_satellite_split.md`
- `rust-ui/docs/spec/style_island_defense.md`
- `rust-ui/docs/spec/styling.md`
- `rust-ui/docs/spec/tree_shaking.md`
- `rust-ui/docs/spec/ui_layout_split.md`
- `rust-ui/docs/spec/ui_physics_two_pass_rendering.md`
- `rust-ui/docs/spec/unified_causality_bus.md`
- `rust-ui/docs/spec/wasm_generic_bloat.md`
- `rust-ui/docs/起点_也即是目的.md`
- `rust-ui/docs/DOCS_GOVERNANCE.md`
- `rust-ui/docs/DOCS_INDEX.md`
- `rust-ui/docs/philosophy.md`
- `rust-ui/docs/README.md`
- `rust-ui/docs/RULES_ZH.md`
