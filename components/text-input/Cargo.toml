[package]
name = "ui-text-input"
version = "0.0.0"
edition = "2024"
publish = false

[lib]
path = "src/lib.rs"

[features]
default = []

[dependencies]
ui-observability = { path = "../../crates/ui-observability" }
ui-components = { path = "../../crates/ui-components", default-features = false, features = ["component-domain-text_input"] }
