# 单组件 Check List（Hyper-Structure 完整版）

> 用途：每次新增、重构或修改一个组件时，必须按本清单逐项检查并在 PR 中附带结果。  
> 执行顺序：先过第 1-2 节（架构与状态），再进入第 3-5 节（具体实现）。  
> 术语统一：`status-primitives` 指状态原语层（当前 crate 名为 `ui-state-primitives`）。

### 0. 适用范围与顺序纪律
本清单仅评估“一个组件”的改动结果，不替代仓库级治理。
先过第 1-2 节（架构与状态）再进入第 3-5 节（具体实现）。
不适用的条目必须明确标注 `N/A` 并说明理由，禁止机械打勾。
组件目标、非目标、风险边界已写清楚；发现跨组件/跨层系统性问题时升级为仓库级任务。

### 1. 架构边界与分层约束（Kernel/Shell 总线）
- [x] `status-primitives` 定义：纯状态原语层（受控/非受控、toggle、selection、list、overlay open state、expansion 等）。不依赖 Leptos/DOM/web-sys；只包含 Rust 数据结构和方法，不含视图与事件绑定。
  - 所有状态原语必须从 `status-primitives`（`ui-state-primitives`）获取，组件层只能消费，不得自造。
  - 下沉判定依据是“稳定状态不变量”；凡属于状态机、归一化、状态派生能力，默认先进入 `ui-state-primitives`。
  - 组件中可保留的仅是装配逻辑：props 归一、样式来源标记、slot 组织、对 `ui-state-primitives` 输出的映射。
  - 组件内若出现状态原语实现（受控/非受控状态机、single/multiple 展开规则、索引归一化、跨事件状态派生），该项直接判不通过。
  - 处理方式固定：先下沉到 `ui-state-primitives/src/<capability>.rs`（如 `expansion.rs`），在 `ui-state-primitives/src/lib.rs` 导出，再回到组件改调用。
  - 下沉后的原语必须有 `ui-state-primitives` 单元测试；组件侧只保留调用与语义挂载测试。
  - 桥接规范：`ui-state-primitives` 结构体必须是 POJO（Plain Old Rust Object），不持有 Leptos `Signal` 或框架绑定状态容器。
  - 消费规范：`ui-headless` 或组件 `logic.rs` 负责解包 `Signal` 当前值传入 primitive 方法，并将结果显式写回 `Signal`。
  - 设计理由：保持 primitives 纯粹可测、可迁移，不与特定响应式库绑定（便于未来替换响应式实现与做纯 Rust 测试）。
- [x] `ui-headless` 定义：交互与 A11y 原语层（press/focus/hover/roving/listbox/menu/tooltip 等），把输入设备事件与状态语义标准化为可复用契约；输出必须是类型化 `attrs + handlers + state`。不做样式、不写组件 CSS、不做组件级动效编排。
  **`ui-headless` 落位硬规则（必须执行）**：
  - 输入边界：消费 `status-primitives` 状态 + 用户输入事件（keyboard/pointer/focus）+ 环境能力（web/ssr）。
  - 输出边界：只输出语义契约（attrs/handlers/state）；组件层只负责挂载与组合，不得把语义判断塞回 `view.rs`。
  - 下沉判定依据是“交互/A11y 语义契约”；凡属于键盘/焦点/指针归一、ARIA 映射、交互状态语义能力，默认先进入 `ui-headless`。
  - 必须下沉：键盘模型、焦点模型、跨设备输入归一、ARIA 状态映射、overlay/presence 等交互语义。
  - A11y 契约与共享工具落点固定在 `crates/ui-headless/src/a11y.rs`；组件只在 `view.rs` 挂载，不在组件层重写。
  - 语义契约必须提供 `lang` / `dir`（LTR/RTL）接入能力；headless 不硬编码用户可见文本，文案由 i18n/l10n 层提供。
  - 语义契约正确性必须有回归：`components/*/test/**` 断言语义标记，`e2e/tests/*` 覆盖关键交互流程。
  - 禁止放在 `ui-headless`：视觉 class 选择、CSS 规则、组件 slot 布局、组件专属动效编排、业务文案。
  - 允许留在组件层：纯视觉一次性交互且不形成可复用语义契约（例如单组件局部微交互）。
- [x] `ui-motion` 定义：动效能力与契约执行层（spring、keyframes、WAAPI/RAF backend），只负责时间函数、插值与运行时驱动，不承载组件业务语义与状态决策。
  - 放在 `crates/ui-motion`：通用动画数学与执行后端（spring solver、keyframe sampling、easing registry、driver adapters），以及 `wasm/non-wasm` 适配与 `reduced-motion` 执行策略。
  - 放在 `crates/ui/src/<component>/motion.rs`：把组件语义状态（open/closed、enter/exit、active/inactive）映射为 `ui-motion` contract，绑定目标节点并调用 attach。
  - 禁止放在 `crates/ui-motion`：组件 slot 结构、组件专属状态机、ARIA/keyboard 语义、业务文案与业务分支。
  - 禁止放在组件 `motion.rs`：自实现 spring/keyframe/driver 执行器；跨组件共享动效算法必须回迁 `ui-motion`。
  - 动效参数优先来自 token/theme；禁止在组件样式与逻辑中散落硬编码时长/曲线/位移常量。
  - 非 wasm 路径必须提供 no-op/stub，保证 SSR/tooling 可编译且行为可预测。
- [x] `ui-theme` 定义：唯一设计 token 与主题上下文层（system/color/scale + Light/Dark/OLED），负责 token 分类、主题映射与 CSS 变量生成。
  - Token 统一基线落点固定：`crates/ui-theme/src/tokens.rs` 定义，`crates/ui-theme/src/theme.rs` 映射，`crates/ui-theme/src/css.rs` 输出变量；组件只在 `crates/ui/src/<component>/styles.rs` 消费。
  - 三轴上下文（`system/color/scale`）在 `theme.rs` 定义；组件在 `logic.rs` 选择并在 `view.rs` 生效，`styles.rs` 只消费变量，不重建主题。
  - Token 分类必须可追溯：分类源在 `tokens.rs`，规范同步 `docs/spec/styling.md`；组件不得引入平行私有 token 命名体系。
  - 量化尺寸基准必须可回归：尺寸基准在 `tokens.rs` 与 `theme.rs` 定义，主题回归在 `crates/ui-theme/tests/token_scale_baseline.rs`，组件语义回归在 `components/*/test/*<component>_semantics.rs`。
  - 主题调色与语义色对比必须满足 `WCAG 2.1 AA` 基线，并覆盖 Light/Dark/OLED 主题变体。
  - 主题层只输出 `theme/tokens/base css` 与变量；不实现组件结构、交互逻辑、组件级动效编排。
  - 新增视觉语义先补 token，再由组件消费；禁止“组件临时值先落地、后补 token”的倒序流程。
- [x] `ui` 定义：最终 Leptos 组件装配层，组合 `status-primitives + ui-headless + ui-motion + ui-theme` 并暴露稳定公共 API。
  - `logic.rs` 负责 props 归一与状态派生；`view.rs` 负责结构渲染与 headless 语义挂载；`styles.rs` 负责 token-first 静态样式；`motion.rs` 负责动效 attach。
  - 组件层不得重写 `status-primitives` 状态机或 `ui-headless` 交互契约；发现即判不通过并回迁到对应层。
  - 对外 API 禁止暴露 `web-sys`/DOM 细节类型；平台差异封装在内部模块。
  - 测试文件位于src同级的test/中，内部测试文件同名（如rust-ui/components/accordion/src/logic.rs与rust-ui/components/accordion/test/logic.rs）。
  - 还需要一个semantics.rs用于测试。可能存在类似rust-ui/components/accordion/test/accordion_semantics.rs的旧版实现，需要迁移到新目录。

### 2. API 设计与状态内核（Logic/Kernel）
- [x] API 命名契约统一：公共 props/回调严格使用 `is_*`、`on_*`、`default_*` 前缀；同语义在全库同名，禁止别名漂移。
  - 布尔状态统一 `is_*`（如 `is_open`/`is_disabled`），事件统一 `on_*`，默认值统一 `default_*`。
  - 同一语义 across 组件必须同名（如都用 `on_open_change`，禁止同义别名并存）。
  - 公共 API 引入新命名时，需说明与现有命名体系的兼容策略与迁移路径。
- [x] 受控/非受控必须成对：每个可控状态轴都提供 `value + on_value_change + default_value`（如 `open/on_open_change/default_open`）；缺一项即不通过。
  - 受控模式：外部值是单一事实来源，内部不得偷偷写回本地状态。
  - 非受控模式：仅由默认值初始化一次，后续状态由内部原语管理。
  - 受控/非受控切换语义需稳定可测，避免“半受控”隐式行为。
- [x] 默认值单一来源：默认值与优先级只在 `logic.rs` 归一化；`view.rs` 禁止二次兜底或隐式改写。
  - 默认值优先级必须可读且可测试（显式规则而非分散 `unwrap_or`）。
  - `view.rs` 不允许再做默认值分支；仅消费 `logic.rs` 的归一化输出。
  - 一旦发现多处默认值来源，直接判不通过并回收至 `logic.rs`。
- [x] 状态归一化集中：状态输入先类型化，再在 `logic.rs` 统一派生；禁止在 `view.rs`、事件回调、样式分支中分散拼状态机。
  - 输入边界统一进入 `logic.rs`，输出统一为可渲染语义状态与来源标记。
  - 事件处理器只触发状态变更，不重建状态机规则。
  - 样式层只消费状态标记，不承担状态判定职责。
- [x] 离散状态必须类型约束：`variant/size/mode/status` 等离散输入使用 `enum`；禁止用多个 `Option<bool>`/字符串自由组合表达互斥状态。
  - 互斥状态优先用 `enum` 建模，利用编译器封住无效组合。
  - 字符串输入若需兼容外部配置，必须先映射到类型化枚举再进入逻辑层。
  - 布尔爆炸（多个 bool 表达一个状态机）应在设计评审阶段直接拦截。
- [x] 状态原语来源正确：组件层只消费 `status-primitives`（当前 `ui-state-primitives`）能力，不直接绑定业务 store；应用级全局状态必须经桥接层适配后再接入组件。
  - 组件中出现可复用状态机实现（受控/非受控、展开规则、选择归一）即判应下沉。
  - 组件与业务全局状态之间必须有适配边界，禁止组件直接依赖业务 store 类型。
  - `logic.rs` 仅做装配与映射，不重新实现状态原语。
- [x] 如果无异步相关，直接打勾。异步交互语义统一：`is_loading`、error/retry、disabled、`aria-busy` 映射一致；优先复用统一 async action 原语（如 `use_async_action`），禁止每组件自定义一套加载/错误协议。
  - 无异步交互时需明确标注 N/A 理由（例如“组件无远程请求与异步状态”），不是机械打勾。
  - 有异步交互时，`is_loading`/disabled/`aria-busy`/retry 语义必须成套一致，且对键盘与读屏路径可用。
  - 异步失败态要有可恢复路径（重试或回退），并有语义测试覆盖。
  - N/A 说明（本组件）：Carousel 无远程请求与异步状态轴，`is_loading`/retry/`aria-busy` 当前不适用。
- [x] API 易用性验收标准（DX Paradox）：把复杂性留在内部，把简单留给用户。
  - 基础用法不得要求用户先理解或手动接线 `ui-state-primitives`/`ui-headless` 状态机。
  - 基础组件 Hello World 示例代码不得超过 5 行（导入与外层模板按仓库约定不计），并可直接运行。
  - 简单需求走简单 API，复杂需求再暴露高级入口：默认 props 覆盖高频场景，高级控制通过受控/扩展参数按需开启。
  - 禁止把内部状态对象作为基础必填参数暴露（例如强制 `state=...` 才能完成点击/展开等基本交互）。
  - docs-app 必须提供最小可用示例，优先展示一眼可懂的默认调用路径。
  - 通过依据：`components/carousel/src/README.md` 的 Hello World 为最小调用（仅 `id_base + items`）；`apps/docs-app/src/pages/components/pages/collections_command.rs` 新增 `Hello World (Minimal)` playground，默认路径无需受控状态接线。
- [x] 组合型组件主 API 必须“显示优于约定”：优先使用显式组合 `<Parent><Item ... /></Parent>`。
  - 每个 item 的标题、语义与内容必须在同一 `Item` 结构维度绑定，避免索引配对式隐式约定。
  - `labels + children`、`titles + panels` 等并行数组/并行槽位写法不得作为默认或推荐 API。
  - 不引入这类语法糖：若为配置式输入，仅允许类型化 `ItemSpec`，并在内部映射为显式 `Item` 语义树。
  - 通过依据：`Carousel` 仅接收 `items: Vec<CarouselItem>`，单个 `CarouselItem` 统一承载 `id/title/description/disabled`，未暴露 `labels + children` 或 `titles + panels` 并行数组 API；docs 默认路径同样基于该单一 item 结构输入。

### 3. 高级交互与物理机制（Shell/Physics）
- [x] 宏观/微观双状态机（Macro/Micro Duality）：拖拽等高频交互在 `Dragging` 期间由 `view/motion` 本地循环执行；禁止每帧穿越回 `logic.rs`，必须在结束时通过 `Action::DragEnd` 回流收敛。
  - N/A 说明（本组件）：Carousel 当前无拖拽交互路径，仅键盘/点击驱动离散选中切换，不存在 `Dragging` 局部循环与 `Action::DragEnd` 回流链路。
- [x] 几何两段式渲染（Two-Pass Rendering）：`Tooltip/Popover/Menu` 等依赖 DOM 测量的组件必须走 `Intent -> Measure(view) -> Rectification(logic)`，并具备幂等收敛保护防死循环。
  - N/A 说明（本组件）：Carousel 不做 overlay 几何定位与 DOM 测量纠偏；组件层仅做离散索引切换与动效挂载，几何测量细节未在 `view.rs/logic.rs` 内实现。
- [x] 集合注册协议（Registration Protocol）：`Accordion/Tabs/Menu` 动态子项必须通过 `RegistrationContext` 上报 `Register/Unregister`，逻辑层维护 `items_order`，禁止依赖 `HashSet` 迭代顺序做导航。
  - N/A 说明（本组件）：Carousel 子项来自 `items: Vec<CarouselItem>` 的确定顺序输入，不支持运行时动态注册/反注册；导航顺序由向量索引与可用项归一规则决定，不依赖 `HashSet` 迭代顺序。
- [x] 插槽投影策略（Slot Projection）：容器组件明确 `Lazy/KeepAlive/Eager`；`KeepAlive` 隐藏时必须通过生命周期通知（如 `NotifyHidden`）暂停轮询/动画等高耗能副作用。
  - 通过依据：Carousel 当前采用固定 `Eager` 投影策略（`viewport` 中直接渲染全部 slides），未暴露 `KeepAlive` 模式；因此不存在隐藏态副作用保活，`NotifyHidden` 约束在本组件当前实现下不触发。
- [x] 环境订阅流（Env Streams）：`Resize/Theme/Intersection` 等环境变化在 `view.rs` 采样、防抖后转化为高层语义 `Action`（如 `BreakpointChanged`）推送到 `logic`；禁止原始事件洪泛。
  - N/A 说明（本组件）：Carousel 当前无 `Resize/Theme/Intersection` 环境订阅，也无 `BreakpointChanged` 类环境语义 `Action`；状态变化仅来自键盘/点击等离散交互路径，不存在原始环境事件洪泛入口。
- [x] 事件光锥（Event Light Cone）：`Table/Grid` 等大型集合批量操作必须走 `Context Bus + Selector` 与状态压缩表达（如 `SelectionState::All`），禁止 O(N) 级向下 prop drilling。
  - N/A 说明（本组件）：Carousel 不属于大型集合批量操作场景，无“全选/批处理”语义轴；当前仅维护单索引焦点/选中切换，不存在 `Context Bus + Selector` 批量广播协议需求。
- [x] 统一因果总线（Causality Bus）：复杂派生总线操作必须支持透传 `TraceId`，确保“用户触发 -> 派生命令 -> 总线广播 -> 订阅者”因果链不断裂。
  - N/A 说明（本组件）：Carousel 当前不存在跨模块复杂派生总线与广播订阅链路；交互在组件内以局部回调与受控状态桥接完成，不涉及 `TraceId` 透传需求。
- [x] 存在 A11y 实现、国际化与本地化实现（至少具备接入点，不硬编码用户可见文本）。
  - 交互元素必须具备可验证语义：`role`/`aria-*`/键盘可达路径完整，且和 headless 契约一致。
  - 用户可见文本来源必须可覆盖：优先 props，其次应用注入（`UiRoot`/i18n bundle），最后组件兜底文案；禁止把业务可见文案硬编码在 `view.rs`。
  - 组件需透传或消费 `lang` / `dir`（LTR/RTL）上下文，不得假设单语言单方向。
  - 共享 A11y 工具优先来自 `crates/ui-headless/src/a11y.rs`，组件层不重复发明同名语义工具。
  - 通过依据：`components/carousel/src/view.rs` 已接入 `ui_headless::i18n::use_ui_i18n + CarouselStrings`，并将 `aria_label/controls_aria_label/indicators_aria_label/previous_label/next_label/indicator_aria_label_template` 统一按“props > UiRoot 注入 i18n > fallback”归一；`view.rs` 移除 `"Previous"/"Next"/"Go to {}"` 硬编码，改由 `logic.rs` 的 `resolve_*_with_fallback` 与 `resolve_indicator_aria_label` 输出。A11y 语义复用 `use_carousel_root`、`labeled_toolbar_attrs`、`labeled_group_attrs`（来源 `crates/ui-headless/src/a11y.rs`），并保持 `lang/dir` 接入。回归覆盖：`components/carousel/test/semantics.rs::carousel_a11y_i18n_l10n_contract_is_headless_driven_and_not_hardcoded_in_view` 与 `components/carousel/test/carousel_semantics.rs::carousel_a11y_i18n_l10n_contract_is_headless_driven_and_not_hardcoded_in_view`。
- [x] 状态可观测、可检索、可验证：使用稳定 `data-*` 与 `aria-*` 标记表达状态和来源。
  - 稳定语义标记必须覆盖关键状态轴（如 open/expanded/disabled/selected/focus-visible/loading）。
  - 状态来源必须可区分（受控/非受控、默认值/外部值、交互来源），通过稳定 marker 暴露而不是隐式推断。
  - 自动化选择器优先基于语义标记，不依赖 DOM 顺序、层级深度或临时 class 名。
  - 标记值应为封闭集合（可枚举），避免自由文本导致契约漂移。
  - 通过依据：`components/carousel/src/view.rs` 在 root/item/indicator 层稳定暴露 `data-state/data-selection/data-focus/data-disabled` 与 `data-*-source`（`id/aria-label/orientation/loop-navigation/selected-index/default-selected-index/selected-index-change/motion`），并挂载 `role/aria-*`（如 root `role+aria-label`、slide `aria-hidden`、controls `aria-orientation`）。状态来源由 `logic.rs`/`ui-state-primitives` 产出闭合集枚举值（如 `selected|focused|idle|empty`、`present|absent`、`custom|default`、`controlled|uncontrolled`），非自由文本拼接。`is_loading` 与 `focus-visible` 作为独立状态轴在 Carousel 当前实现中不适用（组件无异步加载轴，仅有 focus/selected 轴）。回归覆盖：`components/carousel/test/logic.rs::semantic_marker_values_stay_in_closed_sets` 与 `components/carousel/test/semantics.rs::carousel_state_markers_are_observable_and_selector_friendly`。
- [x] 样式依赖显式状态（`data-*`/class），而非脆弱 DOM 结构猜测。
  - `styles.rs` 中状态分支选择器必须基于 `data-*`/`aria-*`/稳定 class，禁止用 `:nth-child`、深层级选择器猜测状态。
  - 运行时样式仅允许传递必要 CSS 变量（custom properties）；禁止把业务样式逻辑塞进 inline style。
  - 视觉状态切换必须可由语义标记直接解释，不能依赖“某节点是否恰好存在”。
  - 通过依据：`components/carousel/src/styles.rs` 的状态分支均绑定显式语义标记（如 `.ui-carousel__slide[data-selected=\"true\"]`、`.ui-carousel__slide[data-state=\"selected\"]`、`.ui-carousel__indicator[data-state=\"selected\"] .ui-carousel__indicator-dot`、`.ui-carousel[data-state=\"empty\"] .ui-carousel__viewport`），未使用 `:nth-child/:nth-of-type/:has` 及结构猜测选择器；`components/carousel/src/view.rs` 不含 `style=`/`style:*` 运行时业务样式。回归覆盖：`components/carousel/test/semantics.rs::carousel_styles_depend_on_explicit_semantic_state_not_dom_guessing` 与 `components/carousel/test/carousel_semantics.rs::carousel_styles_avoid_fragile_dom_structure_guessing_and_view_inline_business_styles`。
- [x] 测试验证“语义契约”而不只验证视觉快照。
  - 至少存在语义测试覆盖关键状态与交互路径（role/aria/data-state/source markers）。
  - 测试矩阵必须覆盖关键分支：受控/非受控、disabled、键盘路径、指针路径、SSR/wasm 差异（按适用范围）。
  - 视觉快照只能作为补充，不得替代语义契约断言。
  - 通过依据：`components/carousel/test/semantics.rs::carousel_semantic_contract_matrix_covers_state_interaction_and_non_snapshot_policy` 与 `components/carousel/test/carousel_semantics.rs::carousel_semantic_contract_matrix_is_asserted_by_semantics_instead_of_snapshots` 显式锁定 `role/aria/data-state/data-*-source`、受控/非受控、disabled、键盘（`on:keydown`）与指针（`on:click/on:focus`）路径，并断言未使用 `assert_snapshot!/insta` 作为主验证。SSR/wasm 在本条按“适用范围”处理：语义契约路径在 `view.rs/logic.rs` 无 `target_arch wasm32` 分支与 `web_sys` 依赖，平台编译矩阵由后续“SSR 与跨平台检查”条目单独验收。
- [x] 组件文件职责正确：`mod.rs`（导出边界）、`logic.rs`（归一/派生/来源标记）、`styles.rs`（静态 token-first CSS）、`view.rs`（Leptos 结构 + headless 挂载）、`motion.rs`（动效契约 + attach）。
  - `mod.rs` 只维护最小稳定导出面与 feature gate，不承载实现细节。
  - `logic.rs` 只做输入归一、状态派生、来源标记；禁止 DOM 操作和样式细节分支。
  - `styles.rs` 只包含 token-first 静态 CSS；禁止硬编码主题常量与业务语义文案。
  - `view.rs` 只做结构渲染与 headless 契约挂载；禁止隐藏关键状态决策。
  - `motion.rs` 只做组件语义到动效契约映射与 attach；禁止在组件内重写通用动效引擎。
  - 通过依据：`components/carousel/src/mod.rs` 仅维护模块边界与稳定导出（`logic/view` 仍为私有模块）；`components/carousel/src/logic.rs` 仅做状态归一/派生并委托 `ui-state-primitives`，未出现 DOM/事件/样式分支；`components/carousel/src/styles.rs` 仅输出 token-first 静态 CSS（`var(--ui-*)`）；`components/carousel/src/view.rs` 仅做 Leptos 结构渲染 + headless 语义挂载，并调用 `logic::resolve_*` 与 `crate::motion::attach_carousel_indicator_motion`；`components/carousel/src/motion.rs` 仅做动效参数 sanitize 与 attach 映射，执行器委托 `ui_visual_primitive/ui-motion`。回归覆盖：`components/carousel/test/semantics.rs::carousel_files_keep_single_responsibility_boundaries` 与 `components/carousel/test/carousel_semantics.rs::carousel_files_keep_single_responsibility_boundaries`。
- [x] `spec.rs` 只用于少数复杂组件（如 button），避免泛滥。
  - 仅当组件存在稳定外部规范/Schema 契约或复杂配置固化需求时才引入 `spec.rs`。
  - 简单组件不得为了“形式统一”新增 `spec.rs`；说明文档应留在 `check2.md`/组件文档。
  - 新增 `spec.rs` 必须同步给出契约测试与版本演进说明。
  - 通过依据：Carousel 当前不存在 `components/carousel/src/spec.rs`，且 `components/carousel/src/mod.rs` 未声明或导出 `spec` 模块（`mod spec; / pub mod spec; / pub use spec::*` 均不存在）；组件说明继续留在 `components/carousel/check2.md` 与 `components/carousel/src/README.md`。回归覆盖：`components/carousel/test/semantics.rs::carousel_simple_component_does_not_introduce_spec_rs` 与 `components/carousel/test/carousel_semantics.rs::carousel_simple_component_does_not_introduce_spec_rs`。
- [x] 组件层遵循 token-first 静态样式契约：样式通过 `styles.rs` 聚合注入；运行时仅传必要 CSS 变量；不把 Utility-First/CSS-in-Rust 当组件库默认范式。
  - 样式规则统一落在 `styles.rs`，由 `crates/ui/src/css.rs` 聚合并通过 `UiRoot` 注入。
  - 颜色/间距/圆角/阴影等视觉值必须来自 `var(--ui-*)`，禁止组件私有 token 体系。
  - Utility-First 仅作为 `apps/*` 应用层布局手段，不得反向污染组件库契约。
  - CSS-in-Rust 仅在有明确类型安全与构建成本净收益时作为例外采用。
  - 通过依据：`components/carousel/src/styles.rs` 仅以 `pub const CSS` 输出静态样式并消费 `var(--ui-*)`（如 `--ui-border/--ui-radius-lg/--ui-shadow-sm`）；`crates/ui/src/css.rs` 在 `component-carousel` feature 下通过 `out.push_str(crate::carousel::styles::CSS);` 聚合；`crates/ui/src/root.rs` 通过 `inject_components_css` 分支调用 `crate::css::push_components_css` 并由 `<style>{...}</style>` 注入。`components/carousel/src/view.rs` 不含 `style=`/`style:*` 与 utility-first class 泄漏，`styles.rs` 不使用 CSS-in-Rust 宏。回归覆盖：`components/carousel/test/semantics.rs::carousel_token_first_static_styles_flow_through_css_aggregation_and_ui_root_injection` 与 `components/carousel/test/carousel_semantics.rs::carousel_token_first_static_styles_flow_through_css_aggregation_and_ui_root_injection`。
- [x] 默认主题美学质量达标（Visual Desire）：以 HeroUI 现代审美为学习对标，默认主题不仅“可用”，还必须“第一眼可信”。
  - 默认主题需通过基础美学清单：信息层级清晰（字重/字号/间距）、对比与层次自然、交互反馈明确（hover/active/focus）。
  - docs-app 必须提供默认主题基线页面与截图基线，关键组件（Button/Input/Overlay）纳入视觉回归对比。
  - 禁止“可访问但粗糙”的最低可用心态：视觉退化（类似旧式 Bootstrap 观感）视为质量回归。
  - HeroUI 对标以“视觉语言与体验质量”对齐为目标，不做无差别 API 表层复制。
  - N/A 说明（本组件）：该条包含 `Button/Input/Overlay` 关键组件截图基线与跨组件视觉回归，属于仓库级视觉治理，不是 Carousel 单组件可独立验收项，按“适用范围与顺序纪律”升级为仓库级任务处理。
  - 组件侧现状（供追溯）：`apps/docs-app/src/pages/components/pages/collections_command.rs` 已提供 Carousel 默认主题展示入口（`title=\"Carousel\"` 与 `Default + Indicator Motion` / `Hello World (Minimal)` 等 playground），但“全库默认主题截图基线”需在仓库级任务统一落地。
- [x] Tree Shaking 是一等能力：package 模式支持组件级 feature；source 模式天然裁剪；样式层同步裁剪，禁止无条件聚合全部 CSS，禁止破坏 DCE/LTO 的全量中央注册表。
  - package 模式必须有组件级 feature（如 `component-accordion`）；未启用组件不得进入编译与链接路径。
  - `lib.rs` 与 `css.rs` 必须按 feature 条件导出/聚合，禁止无条件引用所有组件模块和 CSS 常量。
  - source 模式下仅引入需要的组件源码，不通过中央注册表维持全组件可达。
  - 任意“全量组件映射表/注册表”若导致不可达代码变可达，直接判不通过。
  - 验证命令（特性树）：`cargo tree -e features -p ui --no-default-features --features component-accordion,inject-css`，确认仅启用目标组件特性链。
  - 验证命令（反向依赖）：`cargo tree -e features -i ui -p web-demo`，检查是否被 `all-components` 或隐式特性全量拉起。
  - CI 检查（最小特性编译）：新增任务仅开启目标最小特性（示例：`cargo check -p ui --target wasm32-unknown-unknown --no-default-features --features component-accordion,inject-css`）。
  - CI 检查（体积预算）：对“最小特性构建产物”设定预算并阻断回归（可用固定阈值，如 `< 50KB`，或基于仓库基线的相对阈值）；不得只做编译通过而不做体积约束。
  - 通过依据：`crates/ui/Cargo.toml` 已提供 `component-carousel = ["dep:ui-carousel"]` 且 `ui-carousel` 为 `optional` 依赖；`crates/ui/src/lib.rs` 与 `crates/ui/src/css.rs` 对 Carousel 导出/样式聚合均受 `#[cfg(feature = "component-carousel")]` 门控。实测 `cargo tree -e features -p ui --no-default-features --features component-carousel,inject-css` 输出仅出现 `ui-carousel`（未出现 `all-components`）；`cargo tree -e features -i ui -p web-demo` 显示由 `web-demo-components` 选择性拉起（未出现 `all-components`）。回归覆盖：`components/carousel/test/semantics.rs::carousel_tree_shaking_contract_keeps_feature_gated_entrypoints` 与 `components/carousel/test/carousel_semantics.rs::carousel_tree_shaking_contract_keeps_feature_gated_entrypoints`。
  - N/A 说明（本组件）：CI 体积预算阈值与仓库级最小特性编译门禁属于全仓治理（需在 CI workflow 统一配置）；本地尝试 `cargo check -p ui --target wasm32-unknown-unknown --no-default-features --features component-carousel,inject-css` 受环境 `Invalid cross-device link (os error 18)` 阻塞。
- [x] 类型系统 + 语义标记共同提供机器可读状态；关键输入空间受类型约束。
  - 离散输入与状态轴必须优先使用 `enum`/新类型建模，避免字符串协议与布尔爆炸。
  - 无效状态要么在类型层不可表达，要么在 `logic.rs` 被统一归一化并可测试。
  - 关键状态必须通过稳定语义标记对外可读，供测试与 Agent 自动化消费。
  - 编译器与测试反馈应能直接定位状态契约破坏点，形成可持续闭环。
  - 通过依据：`components/carousel/src/mod.rs` 已以 `CarouselOrientation`/`CarouselItemStatus`/`CarouselSlot` 三个 `enum` 约束离散输入与状态轴；`components/carousel/src/logic.rs` 通过 `resolve_default_selected_index`、`sanitize_selected_index`、`resolve_state` 统一归一化无效状态；`components/carousel/src/view.rs` 挂载 `data-state/data-selection/data-focus/data-selection-mode` 及 `data-*-source` 稳定标记。回归覆盖：`components/carousel/test/logic.rs::semantic_marker_values_stay_in_closed_sets`、`components/carousel/test/logic.rs::default_selected_index_priority_is_centralized_in_logic`、`components/carousel/test/semantics.rs::carousel_state_markers_are_observable_and_selector_friendly`、`components/carousel/test/carousel_semantics.rs::carousel_view_uses_logic_contracts_and_source_markers`。

### 4. DOM/环境边界治理
- [x] 焦点全局栈（Focus Stack & GC）：层叠 `Overlay` 禁止私存 `NodeRef` 作为恢复目标；必须依赖全局 Focus Manager（如 `FallbackTo/Selector`）防止焦点坠落到 `document.body`。
  - N/A 说明（本组件）：Carousel 不是 overlay 组件，不承担层叠弹层的焦点回收职责；组件仅管理轮播项的选中/聚焦索引与指示器高亮，不存在“关闭 overlay 后恢复焦点到触发器”的状态机。
  - 通过依据：`components/carousel/src/view.rs` 与 `components/carousel/src/logic.rs` 未引入 `Overlay`/`FocusManager`/`FallbackTo`/`focus_stack`/`document.body` 相关路径；现有 `NodeRef` 仅用于指示器高亮动效挂载容器，不作为焦点恢复目标。回归覆盖：`components/carousel/test/semantics.rs::carousel_does_not_implement_overlay_focus_stack_restore_paths` 与 `components/carousel/test/carousel_semantics.rs::carousel_does_not_implement_overlay_focus_stack_restore_paths`。
- [x] 受控外交特区（Escape Hatches）：集成 ECharts/Map 等命令式第三方库时必须处于 `Foreign Zone`（`YieldControl/CleanupForeign`）；第三方实例不得暴露为组件公共 API 或反向污染状态机。
  - N/A 说明（本组件）：Carousel 当前不集成 ECharts/Map 等命令式第三方运行时，不存在 `YieldControl/CleanupForeign` 生命周期接管需求；状态机仅由 `ui-state-primitives` 与组件受控/非受控桥接驱动。
  - 通过依据：`components/carousel/src/mod.rs` 未暴露第三方实例类型或挂载入口；`components/carousel/src/view.rs`/`components/carousel/src/logic.rs`/`components/carousel/src/motion.rs` 无 `ECharts`/`Mapbox`/`Leaflet`/`YieldControl`/`CleanupForeign` 接入路径。回归覆盖：`components/carousel/test/semantics.rs::carousel_does_not_integrate_imperative_third_party_foreign_zone_paths` 与 `components/carousel/test/carousel_semantics.rs::carousel_does_not_integrate_imperative_third_party_foreign_zone_paths`。
- [x] SSR 时空断裂治理（Hydration Discontinuity）：逻辑初始化禁止依赖 `now()` 或原生随机 UUID；必须通过 `IdProvider` 注入确定性种子，确保 SSR/Hydration 间 ID 稳定。
  - N/A 说明（本组件）：Carousel 不在组件内部生成随机 ID；`id_base` 为必填输入并经 `logic::normalize_id_base` 归一后参与 DOM id 纯函数派生，因此不存在 `now()/UUID` 导致的 SSR/Hydration 漂移。若应用层需要“统一 seed 注入”，可在上层通过 `UiIdProvider` 生成并下传 `id_base`，组件层不重复造随机源。
  - 通过依据：`components/carousel/src/view.rs` 以 `id_base` 入参启动并调用 `logic::normalize_id_base` 与 `logic::resolve_items(&id_base.get_value(), items)`；`components/carousel/src/logic.rs` 委托 `carousel_primitives::normalize_id_base/resolve_items` 进行确定性 id 生成，未出现 `now()/rand/uuid` 路径。回归覆盖：`components/carousel/test/semantics.rs::carousel_hydration_ids_are_deterministic_without_time_or_random_sources` 与 `components/carousel/test/carousel_semantics.rs::carousel_hydration_ids_are_deterministic_without_time_or_random_sources`。
- [x] SSR 与跨平台检查：覆盖 web/ssr/wasm 分支，不破坏 non-wasm 编译路径。
  - 至少包含 compile-only 证据：web（wasm32）、ssr（native）、默认本地构建三条路径。
  - 平台分支差异必须显式 `cfg` 或 feature 管理，禁止依赖运行时偶然行为。
  - non-wasm 路径禁止引用 `web-sys`/浏览器对象。
  - 通过依据：`components/carousel/Cargo.toml` 已提供显式平台 feature 路径：`default = ["web"]`、`web = ["leptos/csr", "ui-headless/web"]`、`ssr = ["leptos/ssr", "ui-headless/ssr"]`，并将 `ui-headless` 依赖设为 `default-features = false` 以避免隐式平台耦合；`components/carousel/src/mod.rs`/`logic.rs`/`view.rs`/`motion.rs` 未引入 `web-sys/js-sys/wasm-bindgen` 与 `target_arch = "wasm32"` 分支，non-wasm 路径不依赖浏览器对象。回归覆盖：`components/carousel/test/semantics.rs::carousel_platform_build_contract_uses_explicit_web_ssr_features` 与 `components/carousel/test/carousel_semantics.rs::carousel_platform_build_contract_uses_explicit_web_ssr_features`。
  - compile-only 命令记录（本地环境）：已尝试 `cargo check -p ui-carousel`、`cargo check -p ui-carousel --target wasm32-unknown-unknown`、`cargo check -p ui-carousel --no-default-features --features ssr`（均在 `TMPDIR/CARGO_TARGET_DIR` 定位后执行）；当前环境统一阻塞于 `Invalid cross-device link (os error 18)`，需在 CI 或修复主机文件系统后补齐可执行产物证据。
- [x] `ui-headless` web/ssr feature 互斥受 `compile_error!` 保护（`crates/ui-headless/src/lib.rs`）。
  - 组件依赖 `ui-headless` 能力时，不得破坏其 web/ssr 互斥约束。
  - 组件若新增 headless 功能接入，需验证两条 feature 路径都可编译。
  - 发现“同时启用 web+ssr 仍可过编译”视为契约回归。
  - 通过依据：`crates/ui-headless/src/lib.rs` 存在 `#[cfg(all(feature = "web", feature = "ssr"))] compile_error!(...)` 互斥保护；`components/carousel/Cargo.toml` 通过 `web = ["leptos/csr", "ui-headless/web"]` 与 `ssr = ["leptos/ssr", "ui-headless/ssr"]` 显式分流，且 `ui-headless` 依赖为 `default-features = false`，未把 `web+ssr` 同时注入。回归覆盖：`components/carousel/test/semantics.rs::carousel_headless_feature_mutex_guard_is_preserved` 与 `components/carousel/test/carousel_semantics.rs::carousel_headless_feature_mutex_guard_is_preserved`。
  - compile-only 命令记录（本地环境）：已尝试 `cargo check -p ui-headless --no-default-features --features web,ssr` 与 `cargo check -p ui-carousel --no-default-features --features web,ssr`；当前环境统一阻塞于 `Invalid cross-device link (os error 18)`，待 CI/主机文件系统修复后补齐“冲突特性触发 compile_error”执行日志。
- [x] `ui-motion` 非 wasm 提供 no-op/stub（`crates/ui-motion/src/lib.rs`），保证 SSR/tooling 可编译。
  - `motion.rs` 调用必须可在 non-wasm 下安全降级，不触发 panic。
  - 组件不得假设动画句柄一定存在；no-op 分支行为需可预测。
  - toolchain 场景（测试/文档/静态分析）不得因 motion 依赖阻塞编译。
  - 通过依据：`crates/ui-motion/src/lib.rs` 在 `#[cfg(not(target_arch = "wasm32"))]` 下提供 `web::prefers_reduced_motion() -> true` 与 `web::animate(...) {}` 的 no-op/stub，并有 `non_wasm_web_backend_is_predictable_noop` 测试；`crates/ui-visual-primitive/src/active_highlight.rs` 在 non-wasm 下提供 `attach_active_highlight_motion` 空实现；`components/carousel/src/motion.rs` 仅做 `sanitize_motion` 后委托 `attach_active_highlight_motion`，不依赖动画句柄返回值。回归覆盖：`components/carousel/test/semantics.rs::carousel_motion_non_wasm_noop_contract_is_preserved` 与 `components/carousel/test/carousel_semantics.rs::carousel_motion_non_wasm_noop_contract_is_preserved`。
  - compile-only 命令记录（本地环境）：已尝试 `cargo check -p ui-motion`、`cargo check -p ui-motion --target wasm32-unknown-unknown`、`cargo check -p ui-carousel --no-default-features --features ssr`；当前环境统一阻塞于 `Invalid cross-device link (os error 18)`，待 CI/主机文件系统修复后补齐执行日志。
- [x] 组件实现覆盖 `reduced-motion` / SSR / wasm 分支。
  - `reduced-motion` 下动画应跳过或降级为最小必要反馈。
  - SSR 输出必须与客户端 hydration 兼容，避免首帧语义错位。
  - wasm 分支允许增强交互，但语义契约不得与 SSR 分支分裂。
  - 通过依据：`crates/ui-motion/src/spring.rs` 在 `set_target` 中以 `crate::web::prefers_reduced_motion()` 快速路径直接写入目标值并触发 `on_rest`，实现 reduced-motion 最小必要反馈；`crates/ui-motion/src/lib.rs` 与 `crates/ui-visual-primitive/src/active_highlight.rs` 分别在 `#[cfg(not(target_arch = "wasm32"))]` 下提供 no-op/stub（SSR/tooling 安全降级），在 `#[cfg(target_arch = "wasm32")]` 下保留增强动效路径；`components/carousel/src/view.rs`/`logic.rs` 维持平台无关语义挂载（不含 `target_arch`/`web_sys` 分支），避免 SSR 与 wasm 语义分裂。回归覆盖：`components/carousel/test/semantics.rs::carousel_reduced_motion_ssr_wasm_branch_contract_is_preserved` 与 `components/carousel/test/carousel_semantics.rs::carousel_reduced_motion_ssr_wasm_branch_contract_is_preserved`。
  - compile-only 命令记录（本地环境）：已尝试 `cargo check -p ui-motion`、`cargo check -p ui-motion --target wasm32-unknown-unknown`、`cargo check -p ui-carousel --no-default-features --features ssr`；当前环境统一阻塞于 `Invalid cross-device link (os error 18)`，待 CI/主机文件系统修复后补齐执行日志。
- [x] 性能治理：关键路径有预算（首次渲染/更新耗时/内存），回归可检测、可归因、可阻断。
  - 关键交互组件需定义最小预算项（首渲染、关键更新、内存/分配趋势）。
  - 回归检测至少具备可重复基线与失败阈值，不靠主观“感觉变慢”。
  - 性能问题需可归因到状态、渲染、样式或动效路径之一。
  - 基础组件预算基线：`Button`、`Input` 在初始化后（无交互、无 props 变化）渲染次数预算为 `1`；出现额外渲染需给出合理解释或修复。
  - 测试要求：在 `components/*/test/**` 增加 `render_count` 类回归测试（测试框架支持时必须启用）；至少覆盖基础组件与本次改动组件。
  - 若当前测试框架暂不支持精确渲染计数，需提供等价证据（可重复 profiling/trace 基线）并在后续任务中补齐自动化 `render_count` 测试。
  - N/A：`Carousel` 暂未接入精确 `render_count` 自动化计数，当前以 docs 组件页 mount-only 性能预算探针与稳定 `data-perf-*` 标记作为等价证据，并继续追踪 `docs/plan/TODO.md` 中“建立 `render_count` 自动化回归（Button/Input/Accordion/DropZone），替换当前 mount-only 等价证据”任务。
  - 通过依据：`apps/docs-app/src/pages/components/pages/collections_command.rs` 通过 `<ComponentPage title="Carousel" slug="carousel">` 纳入统一性能预算路径；`apps/docs-app/src/pages/components/shell.rs` 通过 `component_page_perf_budget(slug)` + `<UiPerfProbe name=perf_name budget=perf_budget>` 施加 mount/update/heap 预算；`apps/docs-app/src/perf_probe.rs` 输出 `data-perf-mount-ms/data-perf-budget-ms/data-perf-observability/data-perf-violation` 可观测标记，`e2e/tests/docs_app_components_coverage.spec.mjs` 对这些标记执行阻断断言。脚本门禁：`scripts/check-ui-performance.sh` 包含 `cargo test -p ui --test carousel_semantics --no-default-features --features component-carousel,inject-css carousel_performance_governance_contract_is_mount_only_traceable_and_blocking`，并与 `cargo test -p ui --test accordion_semantics docs_perf_probe_budgets_are_wired_for_component_pages`、`cargo test -p ui --test accordion_semantics perf_render_count_follow_up_is_tracked_in_plan` 协同阻断回归。回归覆盖：`components/carousel/test/semantics.rs::carousel_performance_governance_contract_is_mount_only_traceable_and_blocking` 与 `components/carousel/test/carousel_semantics.rs::carousel_performance_governance_contract_is_mount_only_traceable_and_blocking`。
- [x] `view!` 宏复杂度受控：单个 `view!` 块不得承载超长深嵌套结构；复杂布局按语义分块，避免一次性宏展开导致编译与 wasm 体积劣化。
  - 复杂结构按语义子块拆分（header/body/item 等），避免巨型单块 `view!`。
  - `view.rs` 中若出现多层嵌套重复片段，应优先提取局部渲染函数。
  - 编译时间/产物体积异常增长时，优先排查宏展开体量。
  - 通过依据：`components/carousel/src/view.rs` 将根渲染拆分为语义子块 `viewport_view / controls_view / indicators_view`，由根 `<section>` 组合 `{viewport_view}{controls_view}{indicators_view}`，避免单个巨型 `view!` 承载整棵深层结构；`view.rs` 当前 `view!` 总量与文件行数受控（`view!` 计数 ≤ 6，`view.rs` 行数 ≤ 560）的预算契约已由测试锁定。
  - 门禁证据：`scripts/check-ui-view-macro.sh` 已纳入 `cargo test -p ui --test carousel_semantics --no-default-features --features component-carousel,inject-css carousel_view_macro_complexity_is_split_into_semantic_subrenders`，回归失败可阻断。
  - 回归覆盖：`components/carousel/test/semantics.rs::carousel_view_macro_complexity_is_split_into_semantic_subrenders` 与 `components/carousel/test/carousel_semantics.rs::carousel_view_macro_complexity_is_split_into_semantic_subrenders`。
- [x] 函数式拆分优先：不涉及复杂状态与生命周期管理的 UI 片段，优先拆为普通 Rust 函数（返回 `impl IntoView`/`View`），而不是新增 `#[component]`。
  - 纯静态或轻逻辑片段优先函数化；仅在需要独立 props 语义时升级为组件。
  - 禁止把所有局部片段都升格为 `#[component]` 导致抽象噪音。
  - 拆分后语义标记与测试定位仍需稳定。
  - 通过依据：`components/carousel/src/view.rs` 已将局部片段从闭包提升为普通函数 `render_carousel_slide(CarouselSlideRenderInput) -> impl IntoView` 与 `render_carousel_indicator(CarouselIndicatorRenderInput) -> impl IntoView`，根组件仅保留一个 `#[component] pub fn Carousel(...)` 边界；`render_slide/render_indicator` 仅负责装配输入并调用上述函数，不新增局部 `#[component]` 抽象噪音。
  - 语义稳定性：函数式拆分后保留原有关键标记与可访问性挂载（如 slide/indicator 的 `data-state/data-selected/data-focused/data-disabled` 与 slot 标记），测试定位不依赖 DOM 偶然结构。
  - 门禁证据：`scripts/check-ui-view-macro.sh` 已纳入 `cargo test -p ui --test carousel_semantics --no-default-features --features component-carousel,inject-css carousel_view_functional_split_prefers_plain_functions_over_local_components`，回归失败可阻断。
  - 回归覆盖：`components/carousel/test/semantics.rs::carousel_view_functional_split_prefers_plain_functions_over_local_components` 与 `components/carousel/test/carousel_semantics.rs::carousel_view_functional_split_prefers_plain_functions_over_local_components`。
- [x] 静态片段常量化：复杂 SVG、页脚、长说明文本等纯静态内容优先常量化/模板化，减少重复 `view!` 渲染指令生成。
  - 可判定为纯静态的片段应避免重复动态构造。
  - 常量化后仍需维持可访问语义（title/aria-label/role 等）。
  - 静态资源变更路径要清晰，避免散落在多个 `view!` 片段中。
  - 通过依据：`components/carousel/src/view.rs` 已将重复静态 indicator-dot 片段抽成模板函数 `render_carousel_indicator_dot() -> impl IntoView`，并由 indicator 渲染路径统一复用 `{render_carousel_indicator_dot()}`；静态 slot 元数据集中在该模板内（`CarouselSlot::IndicatorDot`），避免在多处 `view!` 重复拼装。
  - 适用范围说明：Carousel 当前无复杂 SVG、页脚或长说明文本静态片段，因此“重静态资源”按简单布局场景 N/A；组件仍显式禁止 `inner_html`/`dangerously_set_inner_html` 注入路径，保持静态资源路径清晰且可审计。
  - 门禁证据：`scripts/check-ui-view-macro.sh` 已纳入 `cargo test -p ui --test carousel_semantics --no-default-features --features component-carousel,inject-css carousel_static_fragments_are_constantized_or_absent_for_simple_layout`，回归失败可阻断。
  - 回归覆盖：`components/carousel/test/semantics.rs::carousel_static_fragments_are_constantized_or_absent_for_simple_layout` 与 `components/carousel/test/carousel_semantics.rs::carousel_static_fragments_are_constantized_or_absent_for_simple_layout`。
- [x] `inner_html` 使用约束：仅允许注入受信任静态常量，禁止拼接用户输入；使用处必须补充语义与安全回归测试。
  - 仅允许编译期常量或明确白名单内容进入 `inner_html`。
  - 严禁直接或间接注入用户输入、远端返回或未清洗模板字符串。
  - 使用 `inner_html` 的节点必须补语义测试与安全回归说明。
  - N/A：`Carousel` 组件实现与 docs 示例页当前无 `inner_html`/`set_inner_html`/`dangerously_set_inner_html` 使用点，本项以“显式缺省 + 回归守卫”验收。
  - 通过依据：`components/carousel/src/mod.rs`、`components/carousel/src/logic.rs`、`components/carousel/src/styles.rs`、`components/carousel/src/motion.rs`、`components/carousel/src/view.rs` 以及 `apps/docs-app/src/pages/components/pages/collections_command.rs` 均未出现 `inner_html` 注入路径；共享 docs shell 仅保留静态白名单挂载 `<div data-slot="component-readme" inner_html=html></div>`，且 `carousel` 不在白名单映射中。
  - 门禁证据：`scripts/check-ui-inner-html.sh` 已纳入 `cargo test -p ui --test carousel_semantics --no-default-features --features component-carousel,inject-css carousel_inner_html_usage_is_explicitly_na_and_guarded`，回归失败可阻断。
  - 回归覆盖：`components/carousel/test/semantics.rs::carousel_inner_html_usage_is_explicitly_na_and_guarded` 与 `components/carousel/test/carousel_semantics.rs::carousel_inner_html_usage_is_explicitly_na_and_guarded`。
- [x] WASM 调试要求：关键状态可追踪（来源/时间/前后值），关键交互可回放，开发模式有可视化入口，调试能力通过 feature 隔离不污染产物。
  - 开发模式下至少能追踪关键状态变更来源与前后值。
  - 关键交互链路应支持最小可复现记录（事件顺序/状态转移）。
  - 调试开关默认不进入生产包体与公共 API。
  - 通过依据（全局调试总线复用）：`apps/docs-app/src/lib.rs` 以 `let debug_overlay_enabled = cfg!(debug_assertions); provide_ui_trace(debug_overlay_enabled);` 仅在开发断言模式启用追踪；`apps/docs-app/src/debug_overlay.rs` 提供可视化调试入口并按时间线展示/回放最近事件；`crates/ui-headless/src/trace.rs` 统一提供 `UiTraceEvent { ts_ms, component, kind }`，包含时间戳与事件有序采样（`MAX_EVENTS` 裁剪）能力。
  - 通过依据（carousel 状态/交互可追踪）：`components/carousel/src/view.rs` 公开稳定 `data-*` 状态与来源标记（如 `data-selected-index`、`data-focused-index`、`data-selected-index-source`、`data-selected-index-change-source`、`data-motion-source`），并通过 `on:keydown`、`on:click`、`on:focus` 驱动关键交互链路，满足“来源/前后值定位 + 最小可复现事件顺序”调试需求。
  - 隔离约束：`components/carousel/src/view.rs` / `logic.rs` / `motion.rs` 不引入 `carousel-wasm-debug`、`trace.emit(`、`request_replay.run(` 或 debug prop，避免组件私有调试通道污染公共 API；调试能力由 `ui` 根层 `wasm_debug_proxy` + docs 全局 overlay 统一承载。
  - 门禁证据：`scripts/check-ui-wasm-debug.sh` 已纳入 `cargo test -p ui --test carousel_semantics --no-default-features --features component-carousel,inject-css carousel_wasm_debug_contract_reuses_global_trace_and_stays_feature_isolated`。
  - 回归覆盖：`components/carousel/test/semantics.rs::carousel_wasm_debug_contract_reuses_global_trace_and_stays_feature_isolated`、`components/carousel/test/semantics.rs::carousel_wasm_debug_check_script_covers_shared_contract`、`components/carousel/test/carousel_semantics.rs::carousel_wasm_debug_contract_reuses_global_trace_and_stays_feature_isolated`、`components/carousel/test/carousel_semantics.rs::carousel_wasm_debug_check_script_covers_shared_contract`。
- [x] DX 要求：样式热重载优先无需重编 wasm；组件热开发尽量保持上下文；提供可选状态保留；有 Workbench 隔离画布。
  - 常见样式调整应走快速反馈路径，不依赖完整 wasm 重编译。
  - 组件调试应尽量保持当前交互上下文，降低重复操作成本。
  - 复杂交互组件应有隔离演练入口（workbench/story/demo 之一）。
  - 通过依据（热重载路径）：`apps/docs-app/src/playground.rs` 的 `Playground` 组件提供 scoped CSS test 面板（`data-slot="playground-test"`），通过 `<style>{move || compose_scoped_css(..., &test_css.get())}</style>` + `on:input` 实现样式热更新，无需重编 wasm。
  - 通过依据（carousel workbench）：`apps/docs-app/src/pages/components/pages/collections_command.rs` 的 `carousel()` 已新增 `Interactive Playground`，包含 `test_css_source=workbench_test_css_source`、`test_config_signal=workbench_actual_config`、`data-slot="carousel-workbench"` 与 `data-slot="carousel-workbench-canvas"`，提供隔离演练画布。
  - 通过依据（可选状态保留）：carousel workbench 提供 `Switch checked=workbench_preserve_context set_checked=set_workbench_preserve_context`，并在 `Effect` 中于 `preserve_context=false` 时重置 `selected_index`，以可选方式保留/清除交互上下文。
  - 门禁证据：`scripts/check-ui-dx.sh` 已纳入 `cargo test -p ui --test carousel_semantics --no-default-features --features component-carousel,inject-css carousel_dx_playground_supports_css_hot_reload_without_wasm_rebuild` 与 `cargo test -p ui --test carousel_semantics --no-default-features --features component-carousel,inject-css carousel_dx_workbench_supports_optional_state_persistence_and_isolated_canvas`。
  - 回归覆盖：`components/carousel/test/semantics.rs::carousel_dx_playground_supports_css_hot_reload_without_wasm_rebuild`、`components/carousel/test/semantics.rs::carousel_dx_workbench_supports_optional_state_persistence_and_isolated_canvas`、`components/carousel/test/semantics.rs::carousel_dx_check_script_covers_hot_reload_and_workbench_contract`、`components/carousel/test/carousel_semantics.rs::carousel_dx_playground_supports_css_hot_reload_without_wasm_rebuild`、`components/carousel/test/carousel_semantics.rs::carousel_dx_workbench_supports_optional_state_persistence_and_isolated_canvas`、`components/carousel/test/carousel_semantics.rs::carousel_dx_check_script_covers_hot_reload_and_workbench_contract`。
- [x] 工程能力统一：`serde` 负责 spec 序列化/版本迁移/错误结构化；`tracing` 统一 span/event 语义；async 不绑定单一运行时（tokio/async-std），runtime 细节不泄露到上层 API。
  - 若组件涉及 spec/config 输入，序列化与错误输出应走统一结构化路径。
  - 关键流程埋点语义应与全库 tracing 约定一致，避免组件各说各话。
  - 异步边界不得把具体 runtime 类型暴露到组件公共接口。
  - N/A（serde/spec 路径）：`Carousel` 当前无 `spec.rs`/`protocol.rs`，不承载外部 schema 反序列化与版本迁移；`components/carousel/Cargo.toml` 已移除 `serde` 依赖，组件层不引入 `Serialize/Deserialize` 与 `serde_json`。
  - tracing 统一：组件代码（`components/carousel/src/{mod,i18n,logic,view,styles,motion}.rs`）不自定义本地 `tracing::span!`/`event!` 目标，不新增 `carousel-wasm-debug` feature，复用仓库既有统一 tracing 语义基线。
  - runtime 边界：组件公共层与实现层不暴露 tokio/async-std/smol 等 runtime 细节类型，且 `mod.rs` 不泄露 `web_sys` 平台类型到公共 API。
  - 门禁证据：`scripts/check-ui-engineering.sh` 已接入 `carousel_engineering_contract_marks_spec_serde_path_as_na_for_simple_component_scope`、`carousel_engineering_contract_keeps_tracing_semantics_unified_without_component_local_events`、`carousel_engineering_contract_avoids_runtime_leaks_in_public_api_surface`。
  - 回归覆盖：`components/carousel/test/semantics.rs::{carousel_engineering_contract_marks_spec_serde_path_as_na_for_simple_component_scope,carousel_engineering_contract_keeps_tracing_semantics_unified_without_component_local_events,carousel_engineering_contract_avoids_runtime_leaks_in_public_api_surface,carousel_engineering_check_script_covers_serde_tracing_and_runtime_boundaries}` 与 `components/carousel/test/carousel_semantics.rs::{carousel_engineering_contract_marks_spec_serde_path_as_na_for_simple_component_scope,carousel_engineering_contract_keeps_tracing_semantics_unified_without_component_local_events,carousel_engineering_contract_avoids_runtime_leaks_in_public_api_surface,carousel_engineering_check_script_covers_serde_tracing_and_runtime_boundaries}`。

### 5. 样式与动效（Theme & Motion）
- [x] 样式孤岛防御（Defensive Variables）：`styles.rs` 使用双层回退链 `var(--ui-*, var(--ui-fallback-*))`；禁止组件内硬编码 Hex 或裸尺寸终值，Fallback 终值由 `ui-theme` 统一输出（SSOT）。
  - 通过依据：`components/carousel/src/styles.rs` 已将关键视觉 token 消费统一升级为双层回退链（如 `var(--ui-space-sm, var(--ui-fallback-space-sm))`、`var(--ui-border, var(--ui-fallback-border))`、`var(--ui-focus-ring, var(--ui-fallback-focus-ring))`、`var(--ui-shadow-sm, var(--ui-fallback-shadow-sm))`），并移除组件内裸终值尺寸常量（`42rem/10rem/5.5rem/2rem/1.75rem/0.5rem/1px solid`）。
  - SSOT 终值来源：`crates/ui-theme/src/css.rs` 继续提供对应 `--ui-fallback-*` 终值（space/component-height/border/radius/bg/fg/focus-ring/accent-fg/shadow），组件不自建并行 fallback 体系。
  - 门禁证据：`scripts/check-ui-contract-hygiene.sh` 已纳入 `cargo test -p ui --test carousel_semantics --no-default-features --features component-carousel,inject-css carousel_styles_use_defensive_variable_fallback_chain`。
  - 回归覆盖：`components/carousel/test/semantics.rs::{carousel_styles_use_defensive_variable_fallback_chain,carousel_defensive_variables_check_script_covers_style_fallback_contract,carousel_check2_marks_defensive_variables_contract_complete}` 与 `components/carousel/test/carousel_semantics.rs::{carousel_styles_use_defensive_variable_fallback_chain,carousel_defensive_variables_check_script_covers_style_fallback_contract,carousel_check2_marks_defensive_variables_contract_complete}`。
- [x] 级联层覆盖（`@layer ui`）：组件 CSS 默认聚合进 `@layer ui`；运行时数值调整仅通过 CSS Custom Properties（如 `style:--x=...`），禁止普通内联样式（如 `style=\"top: 10px\"`）。
  - `@layer ui` 聚合证据：`crates/ui/src/css.rs` 保持统一入口 `out.push_str("\n@layer ui {\n")` 与 `out.push_str("\n}\n")`，并在 `#[cfg(feature = "component-carousel")]` 下聚合 `out.push_str(crate::carousel::styles::CSS);`。
  - 运行时样式证据：`components/carousel/src/view.rs` 不包含 `style="top/left/right/bottom/width/height"` 与 `style:top/style:left` 等普通内联样式写法；若引入 `style:*`，语义回归仅允许 `style:--*` CSS 变量通道。
  - 门禁证据：`scripts/check-ui-contract-hygiene.sh` 已纳入 `cargo test -p ui --test carousel_semantics --no-default-features --features component-carousel,inject-css carousel_cascade_layer_and_runtime_style_contract_is_enforced`。
  - 回归覆盖：`components/carousel/test/semantics.rs::{carousel_cascade_layer_and_runtime_style_contract_is_enforced,carousel_cascade_layer_check_script_covers_contract,carousel_check2_marks_cascade_layer_contract_complete}` 与 `components/carousel/test/carousel_semantics.rs::{carousel_cascade_layer_and_runtime_style_contract_is_enforced,carousel_cascade_layer_check_script_covers_contract,carousel_check2_marks_cascade_layer_contract_complete}`。
- [x] Motion 合同化：`stiffness`/`damping` 等参数在 `motion.rs` 内置为组件 Contract，并通过 `attach_motion` 挂载；必须尊重 `prefers-reduced-motion` 且在 non-wasm/SSR 安全降级（no-op）。
  - 组件合同证据：`components/carousel/src/motion.rs` 以 `CarouselMotion + sanitize_motion + attach_carousel_indicator_motion` 固化动效接口，`sanitize_motion` 统一走 `ui_motion::spring::sanitize_config(motion.spring, default.spring)`，约束 `stiffness/damping/mass/precision` 等参数合法域。
  - 挂载证据：`components/carousel/src/view.rs` 仅通过 `crate::motion::attach_carousel_indicator_motion(...)` 执行挂载；docs 示例 `State + Source Markers` 明确演示 `marker_motion.spring.stiffness = 250.0`、`marker_motion.spring.damping = 22.0` 的合同化配置入口。
  - reduced-motion 与 non-wasm 证据：`crates/ui-motion/src/spring.rs` 保留 `prefers_reduced_motion` 快速收敛分支；`crates/ui-motion/src/lib.rs` 与 `crates/ui-visual-primitive/src/active_highlight.rs` 在 `#[cfg(not(target_arch = "wasm32"))]` 下提供 no-op/stub，保证 SSR/tooling 安全降级。
  - 门禁证据：`scripts/check-ui-platforms.sh` 已纳入 `cargo test -p ui --test carousel_semantics --no-default-features --features component-carousel,inject-css carousel_motion_contract_is_component_scoped_reduced_motion_aware_and_non_wasm_safe`。
  - 回归覆盖：`components/carousel/test/semantics.rs::{carousel_motion_contract_is_component_scoped_reduced_motion_aware_and_non_wasm_safe,carousel_motion_contract_platform_script_covers_guard,carousel_check2_marks_motion_contract_complete}` 与 `components/carousel/test/carousel_semantics.rs::{carousel_motion_contract_is_component_scoped_reduced_motion_aware_and_non_wasm_safe,carousel_motion_contract_platform_script_covers_guard,carousel_check2_marks_motion_contract_complete}`；`components/carousel/test/motion.rs` 继续覆盖 sanitize 保真与非法值回退。
- [x] `ui` 固定入口文件落点正确。
  - `crates/ui/src/lib.rs`：总模块入口 + 对外 `pub use`（公共 API 面）；组件模块受 `component-*` feature gate 约束；不暴露内部平台细节类型。
  - `crates/ui/src/css.rs`：组件 CSS 聚合入口（`push_components_css`）；按 feature 条件注入；禁止无条件聚合全部组件 CSS。
  - `crates/ui/src/root.rs`：`UiRoot` 统一注入 base css + theme vars +（可选）components css，并提供全局 i18n 上下文；主题与注入策略必须集中在此。
  - `crates/ui-visual-primitive/src/active_highlight.rs`：共享高亮条样式与 motion driver；只承载通用高亮动效能力，不承载具体组件业务语义。
  - `crates/ui/src/overlay_open.rs`：当前仓库中不应存在；open-state 原语固定在 `crates/ui-headless/src/controllable_state.rs`，组件通过 headless API 消费。
  - `crates/ui/src/presence.rs`：当前仓库中不应存在；presence 原语固定在 `crates/ui-headless/src/presence.rs`，组件通过 `ui_headless::use_presence` 消费。
  - `crates/ui/src/a11y.rs`：当前仓库中不应存在；共享 A11y 工具固定在 `crates/ui-headless/src/a11y.rs`（如 `aria_controls_when_open`），组件只负责挂载。
  - 入口边界证据：`crates/ui/src/lib.rs` 继续以 `#[cfg(feature = "component-carousel")] pub use ui_carousel as carousel;` 暴露组件能力；`mod css; + pub mod root; + pub use root::UiRoot;` 维持稳定入口；`#[cfg(feature = "inject-css")] pub fn push_components_css(out: &mut String)` 仅透传到 `css::push_components_css(out)`，未暴露 `web_sys/wasm_bindgen` 平台细节类型。
  - CSS/Root/Primitive 证据：`crates/ui/src/css.rs` 维持 `@layer ui` 聚合并对 Carousel 使用 feature 条件注入（`#[cfg(feature = "component-carousel")] out.push_str(crate::carousel::styles::CSS);`）；`crates/ui/src/root.rs` 统一执行 `BASE_CSS + theme vars + optional components css` 注入并提供 `provide_ui_i18n/provide_ui_id_provider`；`crates/ui-visual-primitive/src/active_highlight.rs` 仍只承载共享高亮样式与 motion driver（`ActiveHighlightMotion + attach_active_highlight_motion`），无组件业务语义。
  - 禁止文件证据：`crates/ui/src/overlay_open.rs`、`crates/ui/src/presence.rs`、`crates/ui/src/a11y.rs` 在仓库中不存在；对应 canonical 原语位于 `crates/ui-headless/src/controllable_state.rs`、`crates/ui-headless/src/presence.rs`、`crates/ui-headless/src/a11y.rs`。
  - 回归与门禁：`components/carousel/test/semantics.rs::{carousel_ui_components_fixed_entry_files_follow_layered_boundaries,carousel_entrypoints_check_script_covers_fixed_entrypoint_contract,carousel_check2_marks_ui_components_fixed_entry_files_complete}` 与 `components/carousel/test/carousel_semantics.rs::{carousel_ui_components_fixed_entry_files_follow_layered_boundaries,carousel_entrypoints_check_script_covers_fixed_entrypoint_contract,carousel_check2_marks_ui_components_fixed_entry_files_complete}`；`scripts/check-ui-entrypoints.sh` 新增 `cargo test -p ui --test carousel_semantics --no-default-features --features component-carousel,inject-css carousel_ui_components_fixed_entry_files_follow_layered_boundaries`。当前环境执行 `cargo test` 仍受 `Invalid cross-device link (os error 18)` 阻塞。
- [x] 组件目录标准文件落点正确。
  - `<component>/mod.rs`：最小稳定导出面，存在且无过度导出。
  - `<component>/logic.rs`：props 归一化、派生状态、来源标记；不得承载可下沉原语。
  - `<component>/styles.rs`：静态 CSS 契约，只用 `var(--ui-*)`，不写死主题常量。
  - `<component>/view.rs`：纯 Leptos 结构渲染 + headless 语义挂载；禁止 `render.rs` 漂移；不隐藏关键状态决策。
  - `<component>/motion.rs`：`XxxMotion + attach_motion`；交互组件必须有；只做语义到 motion contract 的映射与挂载。
  - `<component>/spec.rs`：仅极少数组件专用（当前主要 button），无必要不新增。
  - 文件存在证据：`components/carousel/src/` 保持 `mod.rs/logic.rs/styles.rs/view.rs/motion.rs` 五文件结构；`render.rs` 不存在。
  - 导出边界证据：`mod.rs` 维持最小稳定导出（`Carousel/CarouselMotion` 与必要常量、类型）；未出现 `pub mod logic/view/motion` 过度导出。
  - 职责证据：`logic.rs` 仅做 props 归一化、状态派生与来源标记；`styles.rs` 仅承载 token-first 静态 CSS；`view.rs` 仅做 Leptos 结构渲染 + headless 语义挂载；`motion.rs` 仅做 `CarouselMotion + attach_carousel_indicator_motion` 合同映射与挂载。
  - spec N/A 证据：`components/carousel/src/spec.rs` 不存在，`mod.rs` 未声明 `mod spec;`；简单组件不引入 spec。
  - 回归覆盖：`components/carousel/test/semantics.rs::{carousel_component_directory_standard_files_follow_contract_and_na_paths,carousel_component_files_check_script_covers_standard_layout_contract}` 与 `components/carousel/test/carousel_semantics.rs::{carousel_component_directory_standard_files_follow_contract_and_na_paths,carousel_component_files_check_script_covers_standard_layout_contract}`。
  - 门禁证据：`scripts/check-ui-component-files.sh` 新增 `cargo test -p ui --test carousel_semantics --no-default-features --features component-carousel,inject-css carousel_component_directory_standard_files_follow_contract_and_na_paths`。当前环境执行 `cargo test` 仍受 `Invalid cross-device link (os error 18)` 阻塞。

### 6. AI 原生能力与文件落点（Struct-First & Projection）
- [x] 文件落点纪律：组件目录严格由 `mod.rs`（导出）、`logic.rs`（归一派生）、`styles.rs`（Token 样式）、`view.rs`（渲染）、`motion.rs`（动效）组成；复杂组件可选 `spec.rs`；禁止 `render.rs`。
  - 落点证据：`components/carousel/src/mod.rs`、`components/carousel/src/logic.rs`、`components/carousel/src/styles.rs`、`components/carousel/src/view.rs`、`components/carousel/src/motion.rs` 均存在；`components/carousel/src/render.rs` 不存在。
  - 简单组件约束证据：`components/carousel/src/spec.rs` 维持不存在；`components/carousel/src/protocol.rs` 作为 sidecar 保留，不影响五文件主职责拆分。
  - 回归覆盖：`components/carousel/test/semantics.rs::{carousel_file_placement_discipline_is_strict_for_component_scope,carousel_component_files_script_covers_file_placement_discipline,carousel_check2_marks_file_placement_discipline_complete}` 与 `components/carousel/test/carousel_semantics.rs::{carousel_file_placement_discipline_is_strict_for_component_scope,carousel_component_files_script_covers_file_placement_discipline,carousel_check2_marks_file_placement_discipline_complete}`。
  - 门禁证据：`scripts/check-ui-component-files.sh` 新增 `cargo test -p ui --test carousel_semantics --no-default-features --features component-carousel,inject-css carousel_file_placement_discipline_is_strict_for_component_scope`。当前环境执行 `cargo test` 仍受 `Invalid cross-device link (os error 18)` 阻塞。
- [x] Hyper-Structure Builder（`spec.rs`）：复杂组件必须提供 AI 友好的 `*Spec::new()...render()` 建造者 API。
  - N/A：`carousel` 为简单组件，当前无稳定外部 Schema 契约与复杂配置固化需求，不引入 `spec.rs` Builder 面。
  - 约束证据：`components/carousel/src/spec.rs` 不存在；`components/carousel/src/mod.rs`、`components/carousel/src/logic.rs`、`components/carousel/src/view.rs` 以及 docs 页面未出现 `CarouselSpec`、`spec::`、`Spec::new(`、`.render()` 入口。
  - 回归覆盖：`components/carousel/test/semantics.rs::{carousel_hyper_structure_builder_spec_is_not_applicable_for_simple_component,carousel_component_files_script_covers_hyper_structure_builder_na_contract,carousel_check2_marks_hyper_structure_builder_contract_complete}` 与 `components/carousel/test/carousel_semantics.rs::{carousel_hyper_structure_builder_spec_is_not_applicable_for_simple_component,carousel_component_files_script_covers_hyper_structure_builder_na_contract,carousel_check2_marks_hyper_structure_builder_contract_complete}`。
  - 门禁证据：`scripts/check-ui-component-files.sh` 新增 `cargo test -p ui --test carousel_semantics --no-default-features --features component-carousel,inject-css carousel_hyper_structure_builder_spec_is_not_applicable_for_simple_component`。当前环境执行 `cargo test` 仍受 `Invalid cross-device link (os error 18)` 阻塞。
- [x] 上下文压缩协议（Manifest + RBI）：新增/大改组件必须同步维护组件目录下 `Component.toml`（能力清单）和 `.rbi`（接口签名投影），避免 AI 检索工具箱过时。
  - 已落实（工件落点）：`components/carousel/src/Component.toml` 与 `components/carousel/src/carousel.rbi` 已新增并纳入组件目录，避免 AI 检索使用过时接口上下文。
  - 一致性证据：`Component.toml` 已覆盖 `id_base/items/selected_index/default_selected_index/on_selected_index_change/orientation/is_loop_navigation/motion/aria_label/controls_aria_label/indicators_aria_label/previous_label/next_label/indicator_aria_label_template/lang/dir/class_name` 输入与 `context_compression_manifest/rbi_signature_projection` 能力开关；`carousel.rbi` 同步投影 `Carousel(...)` 签名与关键公开符号（`CarouselMotion/CarouselOrientation/CarouselItem` 等）。
  - 回归覆盖：`components/carousel/test/semantics.rs::{carousel_context_compression_manifest_and_rbi_projection_are_present_and_current,carousel_component_files_check_script_covers_context_compression_manifest_contract,carousel_check2_marks_context_compression_manifest_and_rbi_contract_complete}` 与 `components/carousel/test/carousel_semantics.rs::{carousel_context_compression_manifest_and_rbi_projection_are_present_and_current,carousel_component_files_check_script_covers_context_compression_manifest_contract,carousel_check2_marks_context_compression_manifest_and_rbi_contract_complete}`。
  - 门禁证据：`scripts/check-ui-component-files.sh` 新增 `cargo test -p ui --test carousel_semantics --no-default-features --features component-carousel,inject-css carousel_context_compression_manifest_and_rbi_projection_are_present_and_current`。当前环境执行 `cargo test` 仍受 `Invalid cross-device link (os error 18)` 阻塞。
- [x] 语义标记统一升级为 Agent Contract（Schema 化），让 Agent 不依赖 DOM 猜测理解组件状态与意图。
  - 关键交互组件必须输出稳定机器可读语义（至少 `data-*` + 状态来源标记；复杂组件建议补 `data-ui-schema`）。
  - Agent 消费字段应来自类型化 schema 生成，不允许散落字符串拼接。
  - 契约字段需可追溯到组件状态轴与动作语义（intent/action/state/source）。
  - 配置到组件的渲染链路必须走白名单能力边界，禁止任意脚本注入。
  - 落地证据：`components/carousel/src/logic.rs` 新增类型化 `CarouselAgentContract`（`CarouselAgentSchemaVersion/CarouselAgentIntent/CarouselAgentAction/CarouselAgentStateAxis/CarouselAgentSourceAxis/CarouselAgentConfigPolicy`）与 `resolve_agent_contract(state)`，通过枚举 `as_str()` 输出 `schema/intent/action/state/source/config_policy`，避免散落字符串拼接。
  - 挂载证据：`components/carousel/src/view.rs` 根节点新增 `data-ui-schema/data-ui-schema-version/data-ui-intent/data-ui-action/data-ui-state/data-ui-source/data-ui-config-policy`，字段全部来自 `logic::resolve_agent_contract(...)`，并继续复用既有 `data-selected-index-source/data-motion-source` 等来源轴。
  - 白名单边界证据：carousel 渲染链路未引入 `inner_html`/`dangerously_set_inner_html`/`<script>`/`javascript:` 注入路径；Agent Contract 只通过固定 `data-ui-*` 字段输出，不接受任意脚本能力。
  - 回归覆盖：`components/carousel/test/semantics.rs::{carousel_agent_contract_schema_markers_are_typed_traceable_and_whitelist_rendered,carousel_contract_hygiene_check_script_covers_agent_contract_schema_gate,carousel_check2_marks_agent_contract_schema_item_complete}` 与 `components/carousel/test/carousel_semantics.rs::{carousel_agent_contract_schema_markers_are_typed_traceable_and_whitelist_rendered,carousel_contract_hygiene_check_script_covers_agent_contract_schema_gate,carousel_check2_marks_agent_contract_schema_item_complete}`。
  - 门禁证据：`scripts/check-ui-contract-hygiene.sh` 新增 `cargo test -p ui --test carousel_semantics --no-default-features --features component-carousel,inject-css carousel_agent_contract_schema_markers_are_typed_traceable_and_whitelist_rendered` 与 `... carousel_check2_marks_agent_contract_schema_item_complete`。
  - 当前环境说明：本地执行 `cargo test` 仍受 `Invalid cross-device link (os error 18)` 阻塞，需在 CI 或修复主机文件系统后补齐可执行日志。
- [x] 流式在这里仅指 LLM 输出渲染（只看两种显示模式）。
  - `Streaming`：LLM 还在生成，界面边生成边显示。
  - `Snapshot`：LLM 全部生成完成后，一次性显示。
  - 适用范围说明：Carousel 不是 LLM 正文渲染组件，当前实现按 `snapshot-only` 归类；该术语约束仅用于防止误把普通交互组件当作流式文本承载层。
  - 实现证据：`components/carousel/src/view.rs`、`components/carousel/src/logic.rs`、`components/carousel/src/mod.rs`、`components/carousel/src/motion.rs` 未引入 `AiRenderMode/AiOutputStatus/data-ui-stream*` 等流式输出协议字段，组件语义仍聚焦轮播交互状态契约。
  - 回归覆盖：`components/carousel/test/semantics.rs::{carousel_check2_documents_streaming_definition_is_llm_output_only_with_two_modes,carousel_stays_snapshot_only_and_does_not_mount_stream_contract_fields,carousel_streaming_check_script_covers_snapshot_only_contract}` 与 `components/carousel/test/carousel_semantics.rs::{carousel_check2_documents_streaming_definition_is_llm_output_only_with_two_modes,carousel_stays_snapshot_only_and_does_not_mount_stream_contract_fields,carousel_streaming_check_script_covers_snapshot_only_contract}`。
  - 门禁证据：`scripts/check-ui-streaming.sh` 新增 `cargo test -p ui --test carousel_semantics --no-default-features --features component-carousel,inject-css carousel_stays_snapshot_only_and_does_not_mount_stream_contract_fields` 与 `... carousel_check2_documents_streaming_definition_is_llm_output_only_with_two_modes`。
  - 当前环境说明：本地执行 `cargo test` 仍受 `Invalid cross-device link (os error 18)` 阻塞，需在 CI 或修复主机文件系统后补齐可执行日志。
- [x] `Snapshot` 是所有组件的基础能力（默认必须支持）。
  - 所有组件都应能消费“完整生成结果”并稳定渲染。
  - 即使组件不直接展示正文，也应能在接收上层完整配置后正常渲染。
  - 落地证据：`components/carousel/src/view.rs` 维持完整输入消费链路（`items: Vec<CarouselItem>` -> `logic::resolve_items(...)` -> `item_count` -> `indicator_indices=(0..item_count)` -> slides/indicators 双 `For` 渲染），保证完整配置结果可稳定落地到结构渲染；`components/carousel/src/logic.rs` 的 `resolve_items` 继续统一做输入归一与稳定 ID 派生。
  - 回归覆盖：`components/carousel/test/semantics.rs::{carousel_check2_documents_snapshot_as_default_baseline_capability,carousel_snapshot_baseline_consumes_complete_result_and_renders_stably,carousel_streaming_check_script_covers_snapshot_only_contract}` 与 `components/carousel/test/carousel_semantics.rs::{carousel_check2_documents_snapshot_as_default_baseline_capability,carousel_snapshot_baseline_consumes_complete_result_and_renders_stably,carousel_streaming_check_script_covers_snapshot_only_contract}`。
  - 门禁证据：`scripts/check-ui-streaming.sh` 新增 `cargo test -p ui --test carousel_semantics --no-default-features --features component-carousel,inject-css carousel_check2_documents_snapshot_as_default_baseline_capability` 与 `... carousel_snapshot_baseline_consumes_complete_result_and_renders_stably`。
  - 当前环境说明：本地执行 `cargo test` 仍受 `Invalid cross-device link (os error 18)` 阻塞，需在 CI 或修复主机文件系统后补齐可执行日志。
- [x] `Streaming` 是否强制，按组件职责判断（不能一刀切）。
  - `Streaming Required`：组件本体就是正文阅读面，用户需要边生成边看。
  - `Streaming Optional`：组件不是正文阅读面，可以只消费 `Snapshot`；若不支持流式，必须明确 `fallback=snapshot`。
  - 无论是否支持 `Streaming`，都要显式标识当前输出状态（草稿/已验证/可提交），并保持 `role`/`aria-*`/`data-*` 连续可读。
  - 数据校验、断线恢复、重试策略由上层负责，组件层只负责稳定渲染。
  - 职责判定：Carousel 归类为 `Streaming Optional`，当前实现为 `snapshot-only`，在本组件范围显式声明 `fallback=snapshot`（不承载 LLM 增量正文渲染链路）。
  - 状态连续性证据：`components/carousel/src/view.rs` 在根节点持续暴露 `role/aria-*` 与关键 `data-*` 语义，同时新增 `data-ui-output-status=...`（当前为 `verified`），保证可读状态连续可检索。
  - 边界证据：`components/carousel/src/view.rs`、`components/carousel/src/logic.rs`、`components/carousel/src/mod.rs`、`components/carousel/src/motion.rs` 未实现 `retry/reconnect/backoff/stream_error` 等恢复策略；数据校验、断线恢复、重试留在上层。
  - 回归覆盖：`components/carousel/test/semantics.rs::{carousel_check2_documents_streaming_required_optional_classification_rules,carousel_streaming_optional_scope_keeps_role_aria_and_data_markers_continuous,carousel_streaming_validation_retry_resilience_boundaries_stay_outside_component_layer,carousel_streaming_check_script_covers_required_optional_classification_contract}` 与 `components/carousel/test/carousel_semantics.rs::{carousel_check2_documents_streaming_required_optional_classification_rules,carousel_streaming_optional_scope_keeps_role_aria_and_data_markers_continuous,carousel_streaming_validation_retry_resilience_boundaries_stay_outside_component_layer,carousel_streaming_check_script_covers_required_optional_classification_contract}`。
  - 门禁证据：`scripts/check-ui-streaming.sh` 新增 `cargo test -p ui --test carousel_semantics --no-default-features --features component-carousel,inject-css carousel_check2_documents_streaming_required_optional_classification_rules`、`... carousel_streaming_optional_scope_keeps_role_aria_and_data_markers_continuous`、`... carousel_streaming_validation_retry_resilience_boundaries_stay_outside_component_layer`。
  - 当前环境说明：本地执行 `cargo test` 仍受 `Invalid cross-device link (os error 18)` 阻塞，需在 CI 或修复主机文件系统后补齐可执行日志。

### 7. 测试、门禁与交付
- [x] 代码卫生（Rust Hygiene）：非测试代码中完全禁止 `unwrap/expect`，禁止无处理的 `let _ = ...`；字符串复制热点收敛为 `Cow<'static, str>`（执行 `./scripts/check-rust-hygiene.sh` 验证）。
  - 代码证据：`components/carousel/src/logic.rs` 已移除 `fallback.trim().to_string()` 热点（改为无 `to_string` 路径），并将 `compose_class_name` 的 class 组装收敛为 `Vec<Cow<'static, str>>`（Borrowed/Owned 分离）以降低字符串复制 churn。
  - 组件范围验证：执行 `RUST_HYGIENE_SCOPE="components/carousel" ./scripts/check-rust-hygiene.sh`，当前 `carousel` 非测试源码未命中 `unwrap/expect`、`let _ =` 与字符串热点规则。
  - 语义回归：`components/carousel/test/semantics.rs::{carousel_rust_hygiene_contract_forbids_unwrap_expect_and_let_underscore_in_non_test_sources,carousel_rust_hygiene_string_clone_hotspots_converge_to_cow_or_are_absent,carousel_rust_hygiene_script_enforces_repo_level_hygiene_guards}` 与 `components/carousel/test/carousel_semantics.rs::{carousel_rust_hygiene_contract_forbids_unwrap_expect_and_let_underscore_in_non_test_sources,carousel_rust_hygiene_string_clone_hotspots_converge_to_cow_or_are_absent,carousel_rust_hygiene_script_enforces_repo_level_hygiene_guards}`。
  - 门禁证据：`scripts/check-ui-engineering.sh` 新增三条 `carousel_rust_hygiene_*` 测试命令，确保回归失败可阻断。
  - 当前环境说明：本机执行 `cargo test` 仍受 `Invalid cross-device link (os error 18)` 阻塞，需在 CI 或修复宿主文件系统后补齐可执行日志。
- [x] Tree Shaking & 特性剪裁：组件必须注册到 `ui` 特性树（如 `component-accordion`）；`css.rs` 和 `lib.rs` 聚合必须受 feature 门控，禁止无条件全局依赖。
  - 代码证据：`crates/ui/Cargo.toml` 保持 `component-carousel = ["dep:ui-carousel"]` 且 `ui-carousel` 为 `optional`；`crates/ui/src/lib.rs` 保持 `#[cfg(feature = "component-carousel")] pub use ui_carousel as carousel;`；`crates/ui/src/css.rs` 保持 `#[cfg(feature = "component-carousel")] out.push_str(crate::carousel::styles::CSS);`，未出现无条件全量聚合入口。
  - 特性树证据：`cargo tree -e features -i ui -p ui --no-default-features --features component-carousel,inject-css` 输出仅包含 `feature "component-carousel" (command-line)` 与 `feature "inject-css" (command-line)`，未出现 `all-components`。
  - 反向依赖证据：`cargo tree -e features -i ui -p web-demo` 输出包含 `web-demo-components`，未出现 `all-components` 隐式全量拉起。
  - 回归证据：`components/carousel/test/semantics.rs::{carousel_tree_shaking_contract_keeps_feature_gated_entrypoints,carousel_tree_shaking_script_enforces_component_minimal_feature_tree_and_budget,carousel_check2_marks_tree_shaking_feature_pruning_contract_complete}`、`components/carousel/test/carousel_semantics.rs::{carousel_tree_shaking_contract_keeps_feature_gated_entrypoints,carousel_tree_shaking_script_enforces_component_minimal_feature_tree_and_budget,carousel_check2_marks_tree_shaking_feature_pruning_contract_complete}`、`components/carousel/test/semantics.rs::carousel_tree_shaking_contract_keeps_feature_gated_entrypoints`、`components/carousel/test/semantics.rs::carousel_tree_shaking_script_enforces_component_minimal_feature_tree_and_budget`、`components/carousel/test/semantics.rs::carousel_check2_marks_tree_shaking_feature_pruning_contract_complete`、`components/carousel/test/carousel_semantics.rs::carousel_tree_shaking_contract_keeps_feature_gated_entrypoints`、`components/carousel/test/carousel_semantics.rs::carousel_tree_shaking_script_enforces_component_minimal_feature_tree_and_budget`、`components/carousel/test/carousel_semantics.rs::carousel_check2_marks_tree_shaking_feature_pruning_contract_complete`。
  - 门禁证据：`scripts/check-ui-tree-shaking.sh` 新增 carousel 专项 gate（`carousel_tree_shaking_contract_keeps_feature_gated_entrypoints`、`carousel_tree_shaking_script_enforces_component_minimal_feature_tree_and_budget`、`carousel_check2_marks_tree_shaking_feature_pruning_contract_complete`）并执行 `CAROUSEL_TREE_OUTPUT`/wasm 最小特性检查。
  - 当前环境说明：`cargo check -p ui --target wasm32-unknown-unknown --no-default-features --features component-carousel,inject-css` 仍受 `Invalid cross-device link (os error 18)` 阻塞，需在 CI 或修复宿主文件系统后补齐执行日志。
- [x] 语义测试与性能回归：断言必须覆盖 `aria-*`、`data-*` 与焦点流转，不能只看快照；高频/重型组件必须补齐 `render_count` 断言/测量（如初始化空闲预算为 1）。
  - 语义覆盖证据：`components/carousel/src/view.rs` 保持 `role/aria-*` 与稳定语义标记（`data-state/data-selection/data-focus/data-selected-index-source/data-motion-source`）以及焦点/键盘路径（`on:focus=on_focus`、`on:keydown=on_key_down`）；语义断言由 `components/carousel/test/semantics.rs::carousel_semantic_contract_matrix_covers_state_interaction_and_non_snapshot_policy` 与 `components/carousel/test/carousel_semantics.rs::carousel_semantic_contract_matrix_is_asserted_by_semantics_instead_of_snapshots` 锁定。
  - 性能回归证据：`components/carousel/test/semantics.rs::carousel_performance_governance_contract_is_mount_only_traceable_and_blocking` 与 `components/carousel/test/carousel_semantics.rs::carousel_performance_governance_contract_is_mount_only_traceable_and_blocking` 锁定 mount-only 预算、`data-perf-*` 可观测标记与阻断路径；新增 `components/carousel/test/semantics.rs::carousel_semantics_and_performance_regression_cover_aria_data_focus_and_render_count_measurement` 与 `components/carousel/test/carousel_semantics.rs::carousel_semantics_and_performance_regression_cover_aria_data_focus_and_render_count_measurement` 统一校验“语义优先、非快照优先、性能回归闭环”。
  - `render_count` 说明：当前仍采用等价证据路径，`docs/plan/TODO.md` 持续跟踪 `render_count` 自动化补齐任务（`建立 render_count 自动化回归（Button/Input/Accordion），替换当前 mount-only 等价证据`）。
  - 门禁证据：`scripts/check-ui-performance.sh` 新增 `cargo test -p ui --test carousel_semantics --no-default-features --features component-carousel,inject-css carousel_semantics_and_performance_regression_cover_aria_data_focus_and_render_count_measurement`，并保留 `carousel_performance_governance_contract_is_mount_only_traceable_and_blocking`。
  - 当前环境说明：执行 `cargo test -p ui --test carousel_semantics --no-default-features --features component-carousel,inject-css carousel_semantics_and_performance_regression_cover_aria_data_focus_and_render_count_measurement` 与相关命令仍受 `Invalid cross-device link (os error 18)` 阻塞，需在 CI 或修复宿主文件系统后补齐执行日志。
- [x] 版本弃用迁移（Codemod/Registry）：若提交包含跨大版本 API 破坏升级，必须在 Schema Registry 注册弃用窗口并提供纯函数迁移层（`migrate_v1_to_v2`）。（N/A：本次 `Carousel` 未发生跨大版本 API 破坏升级。判定依据：`components/carousel/src/Component.toml` 保持 `schema_version = "1"`；`components/carousel/src/protocol.rs` 保持 `CarouselComponentSchemaVersion::V1`；`components/carousel/src/carousel.rbi` 的 `Carousel(...)` 公共签名未发生破坏性移除/重命名；`components/carousel/src/{mod.rs,logic.rs,view.rs,styles.rs,motion.rs,i18n.rs,protocol.rs}` 未引入 `migrate_v1_to_v2` / `migrate_v2_to_v3` / `SchemaRegistry` / `deprecation_window` / `contract.v2`。回归：`components/carousel/test/semantics.rs::carousel_version_deprecation_migration_registry_is_explicitly_na_without_major_breaking_upgrade`、`components/carousel/test/semantics.rs::carousel_version_deprecation_migration_script_covers_engineering_gate`、`components/carousel/test/carousel_semantics.rs::carousel_version_deprecation_migration_registry_is_explicitly_na_without_major_breaking_upgrade`、`components/carousel/test/carousel_semantics.rs::carousel_version_deprecation_migration_script_covers_engineering_gate`。门禁脚本：`scripts/check-ui-engineering.sh` 新增 `cargo test -p ui --test carousel_semantics --no-default-features --features component-carousel,inject-css carousel_version_deprecation_migration_registry_is_explicitly_na_without_major_breaking_upgrade`。命令已尝试（2026-02-20）：`cargo test -p ui --test carousel_semantics --no-default-features --features component-carousel,inject-css carousel_version_deprecation_migration_registry_is_explicitly_na_without_major_breaking_upgrade`；当前环境仍受 `Invalid cross-device link (os error 18)` 阻断，待环境修复后复跑。）
- [x] 文档即产品（Copy-Paste Ready）：`apps/docs-app` 必须新增 Playground（Hello World、状态矩阵、受控/非受控对照），支持流式/快照展现，并提供 Source-first 一键复制且补全 imports。
  - 本组件落地：`apps/docs-app/src/pages/components/pages/collections_command.rs::carousel` 已补齐 docs-product 验收面：`Hello World / State Matrix / Controlled vs Uncontrolled / Streaming / Snapshot Contract / Source-first Copy-Paste`；并在 `Streaming / Snapshot Contract` 明确 `Streaming Optional; fallback=snapshot.`。
  - Copy-ready 证据：新增 `CAROUSEL_DOC_IMPORTS`，各关键 Playground 显式传入 `code_imports=carousel_imports.clone()`；复制链路复用 `apps/docs-app/src/playground.rs::compose_copy_ready_code` 与 CodeBlock 一键复制能力。
  - Source-first 证据：页面新增 `data-slot="carousel-source-first"`、`data-slot="carousel-copy-ready-hint"`、`data-slot="carousel-source-paths"`，并给出依赖前提 `component-carousel + inject-css` 与源码落点（`components/carousel/src/{mod.rs,logic.rs,view.rs,styles.rs,motion.rs}`）。
  - 回归：`components/carousel/test/semantics.rs::carousel_docs_product_copy_paste_ready_contract_is_documented_and_scripted_locally`；`components/carousel/test/carousel_semantics.rs::{carousel_docs_are_copy_paste_ready_with_hello_world_state_matrix_and_streaming_snapshot,carousel_docs_are_source_first_copy_paste_ready_with_imports_copy_button_and_sync,carousel_check2_documents_docs_product_copy_paste_ready_rules,carousel_check2_marks_docs_product_copy_paste_ready_contract_complete,carousel_dx_check_script_covers_docs_product_copy_paste_ready_contract}`。
  - 门禁脚本：`scripts/check-ui-dx.sh` 新增上述 `carousel_docs_*` / `carousel_check2_*` / `carousel_dx_*` 对应 `cargo test` 命令。
  - 当前环境说明：本地执行 `cargo test` 仍受 `Invalid cross-device link (os error 18)` 阻塞，需在 CI 或修复宿主文件系统后补齐执行日志。
- [x] 语义测试优先：验证 `data-*` / `aria-*` / role / 状态来源契约，不只视觉快照。
  - 每个交互组件至少有对应 `*_semantics.rs` 测试覆盖关键状态轴与动作语义。
  - 断言应聚焦语义契约（状态来源/可访问性/键盘路径），快照仅作补充。
  - 新增/变更语义字段必须同步补测试，否则不得打勾。
  - 语义优先证据：`components/carousel/test/semantics.rs::carousel_semantic_contract_matrix_covers_state_interaction_and_non_snapshot_policy` 与 `components/carousel/test/carousel_semantics.rs::carousel_semantic_contract_matrix_is_asserted_by_semantics_instead_of_snapshots` 已覆盖 `role/aria-*`、`data-*`、键盘/焦点路径和状态来源标记，并在语义契约层阻止快照替代断言。
  - 新增回归：`components/carousel/test/semantics.rs::carousel_semantic_test_priority_prefers_data_aria_role_and_source_contracts_over_snapshot_only_checks`、`components/carousel/test/semantics.rs::carousel_performance_script_covers_semantic_test_priority_contract`、`components/carousel/test/semantics.rs::carousel_check2_marks_semantic_test_priority_contract_complete`；`components/carousel/test/carousel_semantics.rs` 同名镜像测试同步新增。
  - 门禁脚本：`scripts/check-ui-performance.sh` 新增 `echo "[perf] contract: carousel semantic test priority"` 与 `cargo test -p ui --test carousel_semantics --no-default-features --features component-carousel,inject-css carousel_semantic_test_priority_prefers_data_aria_role_and_source_contracts_over_snapshot_only_checks`。
  - 当前环境说明：本地执行上述 `cargo test` 仍受 `Invalid cross-device link (os error 18)` 阻塞，需在 CI 或修复宿主文件系统后补齐可执行日志。
- [x] E2E 选择器稳定：使用语义标记，WASM 场景有稳定等待策略。
  - E2E 选择器优先 `data-*` 语义标记，禁止依赖脆弱 DOM 层级或文本定位。
  - WASM 场景必须使用稳定等待策略（语义状态就绪而非固定 sleep）。
  - 若组件涉及异步/动画，E2E 需显式覆盖 ready/settled 条件。
  - E2E 合同落地：新增 `e2e/tests/docs_app_carousel_contract.spec.mjs`，覆盖 `docs-app carousel contract uses semantic selectors with wasm-stable ready waits` 与 `docs-app carousel motion interaction uses semantic ready and settled breakpoints`，使用 `waitForWasmReady` 与语义断言（`data-*` / `role` / `aria-*`）替代固定 sleep。
  - 稳定锚点证据：`apps/docs-app/src/pages/components/pages/collections_command.rs` 新增 `data-slot="carousel-e2e-markers"`、`data-slot="carousel-e2e-marker-actions"`、`data-slot="carousel-e2e-select-overview"`、`data-slot="carousel-e2e-select-analytics"`、`data-slot="carousel-e2e-clear"`，并复用现有 `data-slot="carousel-state-matrix"`、`data-slot="carousel-controlled-uncontrolled"`。
  - 语义回归：`components/carousel/test/semantics.rs::{carousel_check2_documents_e2e_selector_and_stable_wait_rules,carousel_e2e_selector_contract_uses_semantic_markers_and_settled_waits,carousel_e2e_contract_covers_ready_and_settled_conditions_for_motion_paths,carousel_e2e_check_script_covers_selector_and_settled_wait_contract,carousel_check2_marks_e2e_selector_stability_item_complete}`；`components/carousel/test/carousel_semantics.rs` 同名镜像测试已补齐。
  - 门禁脚本：新增 `components/carousel/scripts/check-ui-e2e-carousel.sh`，覆盖 `carousel_check2_documents_e2e_selector_and_stable_wait_rules`、`carousel_e2e_selector_contract_uses_semantic_markers_and_settled_waits`、`carousel_e2e_contract_covers_ready_and_settled_conditions_for_motion_paths`。
  - 当前环境说明：本地执行 `cargo test` 仍受 `Invalid cross-device link (os error 18)` 阻塞，需在 CI 或修复宿主文件系统后补齐执行日志。
- [x] 关键流程纳入可重复回归集合（Playwright/Cypress）。
  - 至少定义一条可重复关键流程（打开/交互/关闭或提交）纳入 E2E 回归。
  - 回归失败需可定位到具体语义契约断点，而不是笼统“页面不一致”。
  - 高风险路径（overlay、focus、keyboard、async）优先进入回归集合。
  - 关键流程回归：`e2e/tests/docs_app_carousel_contract.spec.mjs` 新增 `docs-app carousel key flow is repeatable with semantic breakpoints`，对 `carousel-workbench` 路径执行两轮循环（`for (const cycle of [1, 2])`）并在每步断言 `data-selection/data-selected-index/data-selected-index-source/data-selected-index-change-source`，同时在末尾 `page.reload()` 后复验可重复性。
  - 高风险路径回归：同文件新增 `docs-app carousel high-risk paths keep focus keyboard and settled semantic breakpoints`，覆盖焦点/键盘链路（indicator focus、`Enter`、`ArrowLeft/ArrowRight`）与动效/受控语义断点（`data-motion-source=custom`、`data-selection-mode=controlled`、`data-selected-index-source=external`、`data-selected-index-change-source=custom`）。
  - 语义定位证据：新增 docs 锚点 `data-slot="carousel-workbench-select-0"`、`data-slot="carousel-workbench-select-1"`、`data-slot="carousel-workbench-clear"` 与既有 `data-slot="carousel-e2e-markers"`，失败点可直接映射到具体语义契约节点。
  - N/A 说明（高风险子集）：Carousel 非 overlay 且无 async 远程交互，`overlay/async` 风险轴不适用；本项已优先覆盖本组件适用的 `focus/keyboard` 高风险路径。
  - 语义回归：`components/carousel/test/semantics.rs::{carousel_check2_documents_e2e_repeatable_key_flow_rules,carousel_e2e_key_flow_is_repeatable_and_failure_points_are_semantic,carousel_e2e_high_risk_paths_cover_focus_keyboard_and_settled_semantic_breakpoints,carousel_e2e_check_script_covers_repeatable_key_flow_contract,carousel_check2_marks_e2e_repeatable_key_flow_item_complete}`；`components/carousel/test/carousel_semantics.rs` 同名镜像测试同步新增。
  - 门禁脚本：`components/carousel/scripts/check-ui-e2e-carousel.sh` 新增 `carousel_check2_documents_e2e_repeatable_key_flow_rules`、`carousel_e2e_key_flow_is_repeatable_and_failure_points_are_semantic`、`carousel_e2e_high_risk_paths_cover_focus_keyboard_and_settled_semantic_breakpoints` 三条命令。
  - 当前环境说明：本地执行 `cargo test` 仍受 `Invalid cross-device link (os error 18)` 阻塞，需在 CI 或修复宿主文件系统后补齐执行日志。
- [x] docs-app 文档、示例、参数矩阵、状态矩阵同步更新。
  - 组件行为或参数变更必须同步更新 `apps/docs-app` 示例与说明。
  - 文档示例需覆盖至少一组状态矩阵（受控/非受控、disabled、size/variant 等）。
  - 文档中的 API 名称与默认值必须和 `logic.rs` 当前实现一致。
  - docs 同步证据：`apps/docs-app/src/pages/components/pages/collections_command.rs::carousel` 已同步 `title="State Matrix"`、`title="Controlled vs Uncontrolled"`、`data-slot="carousel-state-matrix"`、`data-slot="carousel-controlled-uncontrolled"`，并覆盖 `selected_index + on_selected_index_change` 与 `default_selected_index` 双路径。
  - API/默认值对齐证据：文档示例与 `components/carousel/src/{logic.rs,view.rs}` 当前实现对齐（`DEFAULT_ORIENTATION`、`DEFAULT_LOOP_NAVIGATION`、`selected_index`、`default_selected_index`、`on_selected_index_change`、`orientation`、`is_loop_navigation`）。
  - 回归：`components/carousel/test/semantics.rs::{carousel_check2_documents_docs_sync_and_state_matrix_rules,carousel_docs_examples_and_state_matrix_sync_with_logic_api_names_and_defaults,carousel_dx_check_script_covers_docs_sync_and_state_matrix_contract,carousel_check2_marks_docs_sync_and_state_matrix_item_complete}`；`components/carousel/test/carousel_semantics.rs` 同名镜像测试同步新增。
  - 门禁脚本：`scripts/check-ui-dx.sh` 新增 `echo "[dx] contract: carousel docs examples + api/state matrix sync with logic API/defaults"`，并执行 `carousel_check2_documents_docs_sync_and_state_matrix_rules` 与 `carousel_docs_examples_and_state_matrix_sync_with_logic_api_names_and_defaults`。
  - 当前环境说明：本地执行 `cargo test` 仍受 `Invalid cross-device link (os error 18)` 阻塞，需在 CI 或修复宿主文件系统后补齐执行日志。
- [x] 组件文档必须对新手友好（Documentation as Product）：组件 README 或等价文档入口必须存在。
  - 每个基础组件必须提供“零门槛”最小示例（Hello World）与常见用法，避免要求用户先理解底层分层架构。
  - 文档需明确“先用起来，再进阶”：默认 API 路径在前，高级控制参数在后。
  - “只有源码没有文档”或“只写给架构师/机器看的文档”视为不通过。
  - 文档入口证据：`components/carousel/src/README.md` 与 `apps/docs-app/src/pages/components/pages.rs`/`apps/docs-app/src/pages/components/pages/collections_command.rs::carousel` 同步存在，`Carousel` 在 docs 目录可检索。
  - 新手优先证据：`README` 已按 `Hello World（最小可用） -> 先用起来，再进阶 -> 常见用法 -> Controlled Example（高级入口）` 顺序组织，默认路径先于高级受控路径。
  - docs-app 入口证据：`collections_command.rs::carousel` 保持 `title="Carousel"`、`slug="carousel"` 以及 `Hello World (Minimal)`、`State Matrix`、`Controlled vs Uncontrolled` 展示面。
  - 回归：`components/carousel/test/semantics.rs::{carousel_check2_documents_documentation_as_product_rules,carousel_documentation_entry_exists_with_beginner_first_progression,carousel_dx_check_script_covers_documentation_as_product_contract,carousel_check2_marks_documentation_as_product_item_complete}`；`components/carousel/test/carousel_semantics.rs` 同名镜像测试同步新增。
  - 门禁脚本：`scripts/check-ui-dx.sh` 新增 `echo "[dx] contract: carousel documentation-as-product keeps beginner-first docs entry"`，并执行 `carousel_check2_documents_documentation_as_product_rules` 与 `carousel_documentation_entry_exists_with_beginner_first_progression`。
  - 当前环境说明：本地执行 `cargo test` 仍受 `Invalid cross-device link (os error 18)` 阻塞，需在 CI 或修复宿主文件系统后补齐执行日志。
- [x] `apps/docs-app` 必须提供 Interactive Playground：用户可在线修改 props/状态并实时预览。
  - Playground 至少支持基础 props 调整、状态切换、交互反馈观察。
  - 对 AI Spec 相关组件，至少提供一组 Spec 输入与预览输出的联动示例。
  - Playground 作为验收面，需可重复复现关键交互路径。
  - docs-app 证据：`apps/docs-app/src/pages/components/pages/collections_command.rs::carousel` 已提供 `title="Interactive Playground"`，并包含 `controls + preview + code + CSS test` 路径（`code_signal=workbench_code`、`test_css_source=workbench_test_css_source`、`test_config_signal=workbench_actual_config`）。
  - 交互可调证据：Playground 控制面支持场景切换与状态切换（`selected_index=workbench_index`、`set_selected_index=set_workbench_index`、`Switch checked=workbench_preserve_context`），并通过 `data-slot="carousel-workbench-*"` 暴露稳定锚点。
  - 可重复回归证据：`e2e/tests/docs_app_carousel_contract.spec.mjs` 复用 `docs-app carousel key flow is repeatable with semantic breakpoints`（`for (const cycle of [1, 2])` + `page.reload()`）验证交互流程可重复。
  - N/A：`Carousel` 非 AI Spec 组件，本项“Spec 输入与预览联动”不适用；已以常规 props/state playground 完成验收。
  - 回归：`components/carousel/test/semantics.rs::{carousel_check2_documents_interactive_playground_rules,carousel_docs_app_provides_interactive_playground_for_props_state_and_preview,carousel_interactive_playground_reuses_repeatable_semantic_e2e_flow,carousel_dx_check_script_covers_interactive_playground_contract,carousel_check2_marks_interactive_playground_item_complete}`；`components/carousel/test/carousel_semantics.rs` 同名镜像测试同步新增。
  - 门禁脚本：`scripts/check-ui-dx.sh` 新增 `echo "[dx] contract: carousel interactive playground docs acceptance surface"`，并执行 `carousel_check2_documents_interactive_playground_rules`、`carousel_docs_app_provides_interactive_playground_for_props_state_and_preview`、`carousel_interactive_playground_reuses_repeatable_semantic_e2e_flow`。
  - 当前环境说明：本地执行 `cargo test` 仍受 `Invalid cross-device link (os error 18)` 阻塞，需在 CI 或修复宿主文件系统后补齐执行日志。
- [x] Source-first 文档必须 Copy-Paste Ready：提供一键复制组件源码或最小可用片段能力。
  - docs-app 页面应提供复制按钮，输出代码默认可直接运行（含必要 imports/依赖提示）。
  - 若为 source-first 组件，文档需指向真实源码落点并说明依赖前提，避免“复制即报错”。
  - 文档代码与当前实现必须同步，防止示例漂移。
  - docs-app 证据：`apps/docs-app/src/pages/components/pages/collections_command.rs::carousel` 已提供 `data-slot="carousel-source-first"` 与 `data-slot="carousel-source-paths"`，并声明 `component-carousel + inject-css` 依赖前提及真实源码落点（`components/carousel/src/{mod.rs,logic.rs,view.rs,styles.rs,motion.rs}`）。
  - 复制链路证据：页面引导 `Show code` + CodeBlock `Copy`，并通过 `apps/docs-app/src/playground.rs::compose_copy_ready_code` + `CAROUSEL_DOC_IMPORTS` 生成可运行代码（含 imports）。
  - 同步回归：`components/carousel/test/semantics.rs::{carousel_check2_documents_source_first_copy_paste_ready_rules,carousel_docs_source_first_copy_paste_ready_with_real_paths_and_dependencies,carousel_dx_check_script_covers_source_first_copy_paste_ready_contract,carousel_check2_marks_source_first_copy_paste_ready_contract_complete}`；`components/carousel/test/carousel_semantics.rs` 同名镜像测试同步新增。
  - 门禁脚本：`scripts/check-ui-dx.sh` 新增 `echo "[dx] contract: carousel source-first docs are copy-paste-ready with real paths and deps"`，并执行 `carousel_check2_documents_source_first_copy_paste_ready_rules` 与 `carousel_docs_source_first_copy_paste_ready_with_real_paths_and_dependencies`。
  - 当前环境说明：本地执行 `cargo test` 仍受 `Invalid cross-device link (os error 18)` 阻塞，需在 CI 或修复宿主文件系统后补齐执行日志。
- [x] HeroUI 对标文档与组件文档同步：参数模型变更需同步 `docs/spec/heroui-parameter-design-strategy.md`（必要时补充 `docs/research/spectrum-heroui-style-interface-study.md`），并保证组件文档可访问。
  - 若参数语义发生变化，需同步更新对标策略文档，不允许实现先漂移文档后补。
  - 组件文档入口必须存在（docs-app 页面或等价文档），且可被索引定位。
  - “仅代码更新无文档更新”在接口变更场景下直接判不通过。
  - 策略文档同步：`docs/spec/heroui-parameter-design-strategy.md` 新增 `### Carousel 同步记录（2026-02-20）`，明确参数主轴、docs 入口、README 等价文档入口与 `Source-first` 复制链路；并注明本轮不需要追加 `docs/research/spectrum-heroui-style-interface-study.md`。
  - docs 入口可索引：`apps/docs-app/src/pages/components/pages.rs` 继续通过 `component_doc!("Carousel", "carousel", "Collections", collections_command::carousel)` 暴露入口；`apps/docs-app/src/pages/components/pages/collections_command.rs::carousel()` 保持 `title="Carousel"` 与 `slug="carousel"`。
  - 组件文档可访问：`components/carousel/src/README.md` 保持 `# Carousel` 与 `## Hello World（最小可用）` 新手入口。
  - 回归：`components/carousel/test/semantics.rs::{carousel_check2_documents_heroui_benchmark_docs_sync_rules,carousel_heroui_strategy_and_component_docs_are_synchronized_and_indexable,carousel_dx_check_script_covers_heroui_benchmark_docs_sync_contract,carousel_check2_marks_heroui_benchmark_docs_sync_contract_complete}`；`components/carousel/test/carousel_semantics.rs::{carousel_check2_documents_heroui_benchmark_docs_sync_rules,carousel_heroui_strategy_and_component_docs_are_synchronized_and_indexable,carousel_dx_check_script_covers_heroui_benchmark_docs_sync_contract,carousel_check2_marks_heroui_benchmark_docs_sync_contract_complete}`。
  - 双侧测试落点标记：`components/carousel/test/semantics.rs::carousel_check2_documents_heroui_benchmark_docs_sync_rules`、`components/carousel/test/semantics.rs::carousel_heroui_strategy_and_component_docs_are_synchronized_and_indexable`、`components/carousel/test/semantics.rs::carousel_dx_check_script_covers_heroui_benchmark_docs_sync_contract`、`components/carousel/test/semantics.rs::carousel_check2_marks_heroui_benchmark_docs_sync_contract_complete`、`components/carousel/test/carousel_semantics.rs::carousel_check2_documents_heroui_benchmark_docs_sync_rules`、`components/carousel/test/carousel_semantics.rs::carousel_heroui_strategy_and_component_docs_are_synchronized_and_indexable`、`components/carousel/test/carousel_semantics.rs::carousel_dx_check_script_covers_heroui_benchmark_docs_sync_contract`、`components/carousel/test/carousel_semantics.rs::carousel_check2_marks_heroui_benchmark_docs_sync_contract_complete`。
  - 门禁脚本：`scripts/check-ui-dx.sh` 新增 `echo "[dx] contract: carousel heroui benchmark strategy + docs entry synchronization"`，并执行 `carousel_check2_documents_heroui_benchmark_docs_sync_rules`、`carousel_heroui_strategy_and_component_docs_are_synchronized_and_indexable`、`carousel_dx_check_script_covers_heroui_benchmark_docs_sync_contract`。
  - 当前环境说明：本地执行 `cargo test` 仍受 `Invalid cross-device link (os error 18)` 阻塞，需在 CI 或修复宿主文件系统后补齐执行日志。

### 8. 合并前门禁死命令（最终执行）
在发起 PR 或完成任务前，必须保证本地/CI 以下命令全部通过：

- `cargo fmt --all -- --check`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `./scripts/check-rust-hygiene.sh`
- `cargo check -p ui --target wasm32-unknown-unknown`
- `cargo check -p ui-headless --no-default-features --features ssr`
- `cargo check -p ui --target wasm32-unknown-unknown --no-default-features --features component-<your_component>,inject-css`

依据文档（`rust-ui/docs/spec` 及 `rust-ui/docs`）：

- `rust-ui/docs/spec/ai_context_projection_protocol.md`
- `rust-ui/docs/spec/architectural_fitness_functions.md`
- `rust-ui/docs/spec/async_state_as_data_command.md`
- `rust-ui/docs/spec/collection_registration_protocol.md`
- `rust-ui/docs/spec/compile_time_evolution_migration.md`
- `rust-ui/docs/spec/component_boundaries.md`
- `rust-ui/docs/spec/component_domains.md`
- `rust-ui/docs/spec/controlled_evolution_sandbox.md`
- `rust-ui/docs/spec/core_shell_protocol_infra_baseline.md`
- `rust-ui/docs/spec/environment_subscription_streams.md`
- `rust-ui/docs/spec/event_light_cone_signal_bus.md`
- `rust-ui/docs/spec/focus_global_stack_gc.md`
- `rust-ui/docs/spec/foreign_zone_escape_hatches.md`
- `rust-ui/docs/spec/headless_purification.md`
- `rust-ui/docs/spec/heroui-parameter-design-strategy.md`
- `rust-ui/docs/spec/hyper-structure-ui-development-playbook.md`
- `rust-ui/docs/spec/i18n.md`
- `rust-ui/docs/spec/intent_stack_semantic_layering.md`
- `rust-ui/docs/spec/kernel_shell_architecture.md`
- `rust-ui/docs/spec/macro_micro_dual_state_machine.md`
- `rust-ui/docs/spec/motion.md`
- `rust-ui/docs/spec/mvp.md`
- `rust-ui/docs/spec/platform_abdication_ecosystem.md`
- `rust-ui/docs/spec/README.md`
- `rust-ui/docs/spec/release_versioning.md`
- `rust-ui/docs/spec/side_effect_command_pattern.md`
- `rust-ui/docs/spec/slot_projection_strategy.md`
- `rust-ui/docs/spec/ssr_hydration_discontinuity.md`
- `rust-ui/docs/spec/state_primitives_core_satellite_split.md`
- `rust-ui/docs/spec/style_island_defense.md`
- `rust-ui/docs/spec/styling.md`
- `rust-ui/docs/spec/tree_shaking.md`
- `rust-ui/docs/spec/ui_layout_split.md`
- `rust-ui/docs/spec/ui_physics_two_pass_rendering.md`
- `rust-ui/docs/spec/unified_causality_bus.md`
- `rust-ui/docs/spec/wasm_generic_bloat.md`
- `rust-ui/docs/起点_也即是目的.md`
- `rust-ui/docs/DOCS_GOVERNANCE.md`
- `rust-ui/docs/DOCS_INDEX.md`
- `rust-ui/docs/philosophy.md`
- `rust-ui/docs/README.md`
- `rust-ui/docs/RULES_ZH.md`
