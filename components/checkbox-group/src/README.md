# CheckboxGroup

`CheckboxGroup` 为多选项提供字段级语义封装（`fieldset/legend`、描述与错误信息、required/invalid 状态）。

## Playground 展示区（Display / Config / Code / CSS Test）

- `Display`：实时渲染分组、选项、描述与错误提示。
- `Config`：切换 required/invalid/disabled 与文案显示。
- `Code`：输出当前配置对应的最小可复制示例。
- `CSS Test`：注入分组样式源码做契约校验。
