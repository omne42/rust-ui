# 单组件 Check List（Hyper-Structure 完整版）

> 用途：每次新增、重构或修改一个组件时，必须按本清单逐项检查并在 PR 中附带结果。  
> 执行顺序：先过第 1-2 节（架构与状态），再进入第 3-5 节（具体实现）。  
> 术语统一：`status-primitives` 指状态原语层（当前 crate 名为 `ui-state-primitives`）。

### 0. 适用范围与顺序纪律
本清单仅评估“一个组件”的改动结果，不替代仓库级治理。
先过第 1-2 节（架构与状态）再进入第 3-5 节（具体实现）。
不适用的条目必须明确标注 `N/A` 并说明理由，禁止机械打勾。
组件目标、非目标、风险边界已写清楚；发现跨组件/跨层系统性问题时升级为仓库级任务。

### 1. 架构边界与分层约束（Kernel/Shell 总线）
- [x] `status-primitives` 定义：纯状态原语层（受控/非受控、toggle、selection、list、overlay open state、expansion 等）。不依赖 Leptos/DOM/web-sys；只包含 Rust 数据结构和方法，不含视图与事件绑定。（`CheckboxGroupState` / `resolve_checkbox_group_state` / 文本归一化 helpers 均位于 `crates/ui-state-primitives/src/checkbox_group.rs`；组件层 `components/checkbox-group/src/logic.rs` 仅 `pub use` 并在 `view.rs` 消费，不重写状态机；原语单测在 `crates/ui-state-primitives/src/test/checkbox_group.rs`，组件侧契约回归在 `components/checkbox-group/test/logic.rs` 与 `components/checkbox-group/test/checkbox_group_semantics.rs`）
  - 所有状态原语必须从 `status-primitives`（`ui-state-primitives`）获取，组件层只能消费，不得自造。
  - 下沉判定依据是“稳定状态不变量”；凡属于状态机、归一化、状态派生能力，默认先进入 `ui-state-primitives`。
  - 组件中可保留的仅是装配逻辑：props 归一、样式来源标记、slot 组织、对 `ui-state-primitives` 输出的映射。
  - 组件内若出现状态原语实现（受控/非受控状态机、single/multiple 展开规则、索引归一化、跨事件状态派生），该项直接判不通过。
  - 处理方式固定：先下沉到 `ui-state-primitives/src/<capability>.rs`（如 `expansion.rs`），在 `ui-state-primitives/src/lib.rs` 导出，再回到组件改调用。
  - 下沉后的原语必须有 `ui-state-primitives` 单元测试；组件侧只保留调用与语义挂载测试。
  - 桥接规范：`ui-state-primitives` 结构体必须是 POJO（Plain Old Rust Object），不持有 Leptos `Signal` 或框架绑定状态容器。
  - 消费规范：`ui-headless` 或组件 `logic.rs` 负责解包 `Signal` 当前值传入 primitive 方法，并将结果显式写回 `Signal`。
  - 设计理由：保持 primitives 纯粹可测、可迁移，不与特定响应式库绑定（便于未来替换响应式实现与做纯 Rust 测试）。
- [x] `ui-headless` 定义：交互与 A11y 原语层（press/focus/hover/roving/listbox/menu/tooltip 等），把输入设备事件与状态语义标准化为可复用契约；输出必须是类型化 `attrs + handlers + state`。不做样式、不写组件 CSS、不做组件级动效编排。（`crates/ui-headless/src/checkbox_group.rs` 新增 `use_checkbox_group` 契约并固定输出 `CheckboxGroupA11y { attrs, handlers, state }`，`lang/dir` 通过 `a11y::locale_attrs` 归一；组件层 `components/checkbox-group/src/logic.rs` 仅桥接 `Signal` 与方向映射，`components/checkbox-group/src/view.rs` 只挂载 headless attrs/state。回归：`crates/ui-headless/src/test/checkbox_group.rs`、`components/checkbox-group/test/logic.rs`、`components/checkbox-group/test/checkbox_group_semantics.rs`。`e2e/tests/*` 当前 N/A：checkbox-group 根节点无独立键盘/指针状态机，关键交互由子项 checkbox 组件覆盖）
  **`ui-headless` 落位硬规则（必须执行）**：
  - 输入边界：消费 `status-primitives` 状态 + 用户输入事件（keyboard/pointer/focus）+ 环境能力（web/ssr）。
  - 输出边界：只输出语义契约（attrs/handlers/state）；组件层只负责挂载与组合，不得把语义判断塞回 `view.rs`。
  - 下沉判定依据是“交互/A11y 语义契约”；凡属于键盘/焦点/指针归一、ARIA 映射、交互状态语义能力，默认先进入 `ui-headless`。
  - 必须下沉：键盘模型、焦点模型、跨设备输入归一、ARIA 状态映射、overlay/presence 等交互语义。
  - A11y 契约与共享工具落点固定在 `crates/ui-headless/src/a11y.rs`；组件只在 `view.rs` 挂载，不在组件层重写。
  - 语义契约必须提供 `lang` / `dir`（LTR/RTL）接入能力；headless 不硬编码用户可见文本，文案由 i18n/l10n 层提供。
  - 语义契约正确性必须有回归：`components/*/test/**` 断言语义标记，`e2e/tests/*` 覆盖关键交互流程。
  - 禁止放在 `ui-headless`：视觉 class 选择、CSS 规则、组件 slot 布局、组件专属动效编排、业务文案。
  - 允许留在组件层：纯视觉一次性交互且不形成可复用语义契约（例如单组件局部微交互）。
- [x] `ui-motion` 定义：动效能力与契约执行层（spring、keyframes、WAAPI/RAF backend），只负责时间函数、插值与运行时驱动，不承载组件业务语义与状态决策。（`components/checkbox-group/src/motion.rs` 仅保留 `sanitize_motion + motion_source_attr + attach_motion` 合同，默认参数来自 `ui-theme`（`default_checkbox_group_motion_tokens`），`reduced-motion` 与 non-wasm 降级走 `ui_motion::web::prefers_reduced_motion()`；`view.rs` 仅将 `resolved_state` 映射为 `data-motion-phase` 并挂载 `style/data-motion-source`，不重写 driver。回归：`components/checkbox-group/test/motion.rs`、`components/checkbox-group/test/checkbox_group_semantics.rs`）
  - 放在 `crates/ui-motion`：通用动画数学与执行后端（spring solver、keyframe sampling、easing registry、driver adapters），以及 `wasm/non-wasm` 适配与 `reduced-motion` 执行策略。
  - 放在 `crates/ui-components/src/<component>/motion.rs`：把组件语义状态（open/closed、enter/exit、active/inactive）映射为 `ui-motion` contract，绑定目标节点并调用 attach。
  - 禁止放在 `crates/ui-motion`：组件 slot 结构、组件专属状态机、ARIA/keyboard 语义、业务文案与业务分支。
  - 禁止放在组件 `motion.rs`：自实现 spring/keyframe/driver 执行器；跨组件共享动效算法必须回迁 `ui-motion`。
  - 动效参数优先来自 token/theme；禁止在组件样式与逻辑中散落硬编码时长/曲线/位移常量。
  - 非 wasm 路径必须提供 no-op/stub，保证 SSR/tooling 可编译且行为可预测。
- [x] `ui-theme` 定义：唯一设计 token 与主题上下文层（system/color/scale + Light/Dark/OLED），负责 token 分类、主题映射与 CSS 变量生成。（新增 `CheckboxGroupMotionTokens/CheckboxGroupLayoutTokens`：`crates/ui-theme/src/tokens.rs` 定义，`crates/ui-theme/src/theme.rs` 提供 `checkbox_group_*_tokens` 映射，`crates/ui-theme/src/css.rs` 输出 `--ui-checkbox-group-*` 变量；组件 `components/checkbox-group/src/styles.rs` 与 `components/checkbox-group/src/motion.rs` 仅消费变量与主题默认值，不再落地硬编码视觉常量；回归覆盖：`crates/ui-theme/tests/token_scale_baseline.rs`、`crates/ui-theme/src/test/theme.rs`、`components/checkbox-group/test/checkbox_group_semantics.rs`。三轴选择入口在 `UiRoot`，对叶子组件 `checkbox-group` 的 `logic.rs` 为 N/A）
  - Token 统一基线落点固定：`crates/ui-theme/src/tokens.rs` 定义，`crates/ui-theme/src/theme.rs` 映射，`crates/ui-theme/src/css.rs` 输出变量；组件只在 `crates/ui-components/src/<component>/styles.rs` 消费。
  - 三轴上下文（`system/color/scale`）在 `theme.rs` 定义；组件在 `logic.rs` 选择并在 `view.rs` 生效，`styles.rs` 只消费变量，不重建主题。
  - Token 分类必须可追溯：分类源在 `tokens.rs`，规范同步 `docs/spec/styling.md`；组件不得引入平行私有 token 命名体系。
  - 量化尺寸基准必须可回归：尺寸基准在 `tokens.rs` 与 `theme.rs` 定义，主题回归在 `crates/ui-theme/tests/token_scale_baseline.rs`，组件语义回归在 `components/*/test/*<component>_semantics.rs`。
  - 主题调色与语义色对比必须满足 `WCAG 2.1 AA` 基线，并覆盖 Light/Dark/OLED 主题变体。
  - 主题层只输出 `theme/tokens/base css` 与变量；不实现组件结构、交互逻辑、组件级动效编排。
  - 新增视觉语义先补 token，再由组件消费；禁止“组件临时值先落地、后补 token”的倒序流程。
- [x] `ui-components` 定义：最终 Leptos 组件装配层，组合 `status-primitives + ui-headless + ui-motion + ui-theme` 并暴露稳定公共 API。（`components/checkbox-group/src/logic.rs` 只做 props 归一与 headless/primitives 桥接，`view.rs` 只做结构渲染与语义挂载，`styles.rs` 仅消费 token 变量，`motion.rs` 仅做 motion attach 合同；`mod.rs` 保持 `logic/view` 私有且公开 API 未暴露 `web-sys`/DOM 类型。语义测试已迁移到 `components/checkbox-group/test/semantics.rs`，`components/checkbox-group/test/checkbox_group_semantics.rs` 仅保留路径桥接入口。）
  - `logic.rs` 负责 props 归一与状态派生；`view.rs` 负责结构渲染与 headless 语义挂载；`styles.rs` 负责 token-first 静态样式；`motion.rs` 负责动效 attach。
  - 组件层不得重写 `status-primitives` 状态机或 `ui-headless` 交互契约；发现即判不通过并回迁到对应层。
  - 对外 API 禁止暴露 `web-sys`/DOM 细节类型；平台差异封装在内部模块。
  - 测试文件位于src同级的test/中，内部测试文件同名（如rust-ui/components/accordion/src/logic.rs与rust-ui/components/accordion/test/logic.rs）。
  - 还需要一个semantics.rs用于测试。可能存在类似rust-ui/components/accordion/test/accordion_semantics.rs的旧版实现，需要迁移到新目录。

### 2. API 设计与状态内核（Logic/Kernel）
- [x] API 命名契约统一：公共 props/回调严格使用 `is_*`、`on_*`、`default_*` 前缀；同语义在全库同名，禁止别名漂移。（`CheckboxGroup` 公共布尔 props 已统一为 `is_invalid/is_required/is_disabled`：`components/checkbox-group/src/view.rs`、`components/checkbox-group/src/Component.toml`、`components/checkbox-group/src/checkbox_group.rbi` 同步；docs 示例与代码字符串同步到 `apps/docs-app/src/pages/components/pages/forms.rs`。兼容策略：本次按组件 API 规范执行破坏性收敛，不保留旧别名 `invalid/required/disabled`；迁移路径为机械替换 `invalid -> is_invalid`、`required -> is_required`、`disabled -> is_disabled`，并由 `components/checkbox-group/test/semantics.rs` 的命名契约断言锁定。）
  - 布尔状态统一 `is_*`（如 `is_open`/`is_disabled`），事件统一 `on_*`，默认值统一 `default_*`。
  - 同一语义 across 组件必须同名（如都用 `on_open_change`，禁止同义别名并存）。
  - 公共 API 引入新命名时，需说明与现有命名体系的兼容策略与迁移路径。
- [x] 受控/非受控必须成对：每个可控状态轴都提供 `value + on_value_change + default_value`（如 `open/on_open_change/default_open`）；缺一项即不通过。（`N/A`：`CheckboxGroup` 当前不持有“组值/选中集合”状态轴，仅暴露分组语义（`is_invalid/is_required/is_disabled`）并由子级 `Checkbox` 各自管理选中态，因此不存在需要成对暴露的 `value` 受控轴；契约由 `components/checkbox-group/test/semantics.rs::checkbox_group_does_not_expose_group_value_control_axis` 锁定。若未来引入组选中状态，必须一次性补齐 `value + on_value_change + default_value`。）
  - 受控模式：外部值是单一事实来源，内部不得偷偷写回本地状态。
  - 非受控模式：仅由默认值初始化一次，后续状态由内部原语管理。
  - 受控/非受控切换语义需稳定可测，避免“半受控”隐式行为。
- [x] 默认值单一来源：默认值与优先级只在 `logic.rs` 归一化；`view.rs` 禁止二次兜底或隐式改写。（新增 `resolve_checkbox_group_content` 与 `resolve_checkbox_group_class_name`，将 label/description/error/class 的默认值与归一逻辑集中到 `components/checkbox-group/src/logic.rs`；`components/checkbox-group/src/view.rs` 仅消费归一结果，不再出现 `unwrap_or`/文本归一兜底。回归覆盖：`components/checkbox-group/test/logic.rs`、`components/checkbox-group/test/semantics.rs::checkbox_group_resolves_ids_and_normalizes_text_inputs`。）
  - 默认值优先级必须可读且可测试（显式规则而非分散 `unwrap_or`）。
  - `view.rs` 不允许再做默认值分支；仅消费 `logic.rs` 的归一化输出。
  - 一旦发现多处默认值来源，直接判不通过并回收至 `logic.rs`。
- [x] 状态归一化集中：状态输入先类型化，再在 `logic.rs` 统一派生；禁止在 `view.rs`、事件回调、样式分支中分散拼状态机。（新增 `resolve_checkbox_group_view_state` / `resolve_checkbox_group_motion_phase` 于 `components/checkbox-group/src/logic.rs`，将可渲染状态与 `data-motion-phase` 派生集中；`components/checkbox-group/src/view.rs` 仅消费 `view_state` 标记挂载 class/aria/data，不再包含 `if resolved_state...` 分支。回归覆盖：`components/checkbox-group/test/logic.rs` 与 `components/checkbox-group/test/semantics.rs`。）
  - 输入边界统一进入 `logic.rs`，输出统一为可渲染语义状态与来源标记。
  - 事件处理器只触发状态变更，不重建状态机规则。
  - 样式层只消费状态标记，不承担状态判定职责。
- [x] 离散状态必须类型约束：`variant/size/mode/status` 等离散输入使用 `enum`；禁止用多个 `Option<bool>`/字符串自由组合表达互斥状态。（本组件无 `variant/size/mode/status` 外部离散输入，当前离散状态轴 `motion_phase` 已类型化为 `CheckboxGroupMotionPhase`（`Active/Inactive`）并通过 `as_data_attr()` 映射到 `data-motion-phase`；不再以裸字符串承载状态协议。回归覆盖：`components/checkbox-group/test/logic.rs`、`components/checkbox-group/test/semantics.rs`。）
  - 互斥状态优先用 `enum` 建模，利用编译器封住无效组合。
  - 字符串输入若需兼容外部配置，必须先映射到类型化枚举再进入逻辑层。
  - 布尔爆炸（多个 bool 表达一个状态机）应在设计评审阶段直接拦截。
- [x] 状态原语来源正确：组件层只消费 `status-primitives`（当前 `ui-state-primitives`）能力，不直接绑定业务 store；应用级全局状态必须经桥接层适配后再接入组件。（`components/checkbox-group/src/logic.rs` 通过 `pub use ui_state_primitives::checkbox_group::{...}` 消费状态原语并仅做装配映射；`components/checkbox-group/src/Component.toml` 依赖声明包含 `ui-state-primitives`（`kind = "state-normalization"`）；回归由 `components/checkbox-group/test/semantics.rs::checkbox_group_state_primitive_boundary_stays_component_local` 与现有状态原语来源断言共同锁定，防止组件直接绑定业务 store。）
  - 组件中出现可复用状态机实现（受控/非受控、展开规则、选择归一）即判应下沉。
  - 组件与业务全局状态之间必须有适配边界，禁止组件直接依赖业务 store 类型。
  - `logic.rs` 仅做装配与映射，不重新实现状态原语。
- [x] 如果无异步相关，直接打勾。异步交互语义统一：`is_loading`、error/retry、disabled、`aria-busy` 映射一致；优先复用统一 async action 原语（如 `use_async_action`），禁止每组件自定义一套加载/错误协议。（`N/A`：`CheckboxGroup` 仅提供分组语义容器，无远程请求与异步状态轴，不暴露 `is_loading`/retry/`aria-busy`/`use_async_action` 协议入口；由 `components/checkbox-group/test/semantics.rs::checkbox_group_has_no_async_loading_contract_surface` 锁定。）
  - 无异步交互时需明确标注 N/A 理由（例如“组件无远程请求与异步状态”），不是机械打勾。
  - 有异步交互时，`is_loading`/disabled/`aria-busy`/retry 语义必须成套一致，且对键盘与读屏路径可用。
  - 异步失败态要有可恢复路径（重试或回退），并有语义测试覆盖。
- [x] API 易用性验收标准（DX Paradox）：把复杂性留在内部，把简单留给用户。（`CheckboxGroup` 基础 API 仅要求 `id + label + children`，不要求用户理解或手动接线 `ui-state-primitives`/`ui-headless`；docs 已新增 `Hello World（默认路径）` Playground（`apps/docs-app/src/pages/components/pages/forms.rs`）并展示默认调用；组件公共接口未暴露内部 `state` 对象必填项。回归由 `components/checkbox-group/test/semantics.rs::checkbox_group_api_supports_hello_world_without_internal_state_wiring` 及 docs 场景断言锁定。）
  - 基础用法不得要求用户先理解或手动接线 `ui-state-primitives`/`ui-headless` 状态机。
  - 基础组件 Hello World 示例代码不得超过 5 行（导入与外层模板按仓库约定不计），并可直接运行。
  - 简单需求走简单 API，复杂需求再暴露高级入口：默认 props 覆盖高频场景，高级控制通过受控/扩展参数按需开启。
  - 禁止把内部状态对象作为基础必填参数暴露（例如强制 `state=...` 才能完成点击/展开等基本交互）。
  - docs-app 必须提供最小可用示例，优先展示一眼可懂的默认调用路径。
- [x] 组合型组件主 API 必须“显示优于约定”：优先使用显式组合 `<Parent><Item ... /></Parent>`。（`CheckboxGroup` API 通过 `children: Children` 暴露显式组合入口（`components/checkbox-group/src/view.rs`、`components/checkbox-group/src/checkbox_group.rbi`），docs 默认路径与主要示例均采用 `<CheckboxGroup><Checkbox ... /></CheckboxGroup>`；组件接口未引入 `labels/titles/panels/items` 并行数组或 `ItemSpec` 配置糖。回归由 `components/checkbox-group/test/semantics.rs::checkbox_group_prefers_explicit_parent_child_composition_api` 锁定。）
  - 每个 item 的标题、语义与内容必须在同一 `Item` 结构维度绑定，避免索引配对式隐式约定。
  - `labels + children`、`titles + panels` 等并行数组/并行槽位写法不得作为默认或推荐 API。
  - 不引入这类语法糖：若为配置式输入，仅允许类型化 `ItemSpec`，并在内部映射为显式 `Item` 语义树。

### 3. 高级交互与物理机制（Shell/Physics）
- [x] 宏观/微观双状态机（Macro/Micro Duality）：拖拽等高频交互在 `Dragging` 期间由 `view/motion` 本地循环执行；禁止每帧穿越回 `logic.rs`，必须在结束时通过 `Action::DragEnd` 回流收敛。（`N/A`：`CheckboxGroup` 为字段语义容器，不承载拖拽交互路径；组件 `logic/view/motion` 未定义 `Dragging`/`Action::DragEnd`/pointer move 协议。回归由 `components/checkbox-group/test/semantics.rs::checkbox_group_has_no_dragging_macro_micro_state_machine_contract` 锁定，防止后续误引入每帧拖拽状态机。）
- [x] 几何两段式渲染（Two-Pass Rendering）：`Tooltip/Popover/Menu` 等依赖 DOM 测量的组件必须走 `Intent -> Measure(view) -> Rectification(logic)`，并具备幂等收敛保护防死循环。（`N/A`：`CheckboxGroup` 不参与浮层定位与几何校正，不依赖 DOM 尺寸测量；组件 `logic/view/motion` 未定义 `Intent -> Measure -> Rectification` 两段流程。回归由 `components/checkbox-group/test/semantics.rs::checkbox_group_has_no_geometry_two_pass_rendering_contract` 锁定，防止后续引入测量-回写循环。）
- [x] 集合注册协议（Registration Protocol）：`Accordion/Tabs/Menu` 动态子项必须通过 `RegistrationContext` 上报 `Register/Unregister`，逻辑层维护 `items_order`，禁止依赖 `HashSet` 迭代顺序做导航。（`N/A`：`CheckboxGroup` 仅负责字段分组语义与 slot 挂载，不承担动态子项导航与索引编排；组件 `logic/view` 未定义 `RegistrationContext/Register/Unregister/items_order` 协议。回归由 `components/checkbox-group/test/semantics.rs::checkbox_group_has_no_registration_protocol_contract` 锁定，防止后续误引入集合注册总线。）
- [x] 插槽投影策略（Slot Projection）：容器组件明确 `Lazy/KeepAlive/Eager`；`KeepAlive` 隐藏时必须通过生命周期通知（如 `NotifyHidden`）暂停轮询/动画等高耗能副作用。（`N/A`：`CheckboxGroup` 仅透传 `children` 并常驻渲染，不管理内容投影策略，也不存在隐藏态副作用暂停需求；组件未定义 `Lazy/KeepAlive/Eager` 或 `NotifyHidden` 生命周期协议。回归由 `components/checkbox-group/test/semantics.rs::checkbox_group_has_no_slot_projection_lifecycle_contract` 锁定，防止后续误引入投影策略状态机。）
- [x] 环境订阅流（Env Streams）：`Resize/Theme/Intersection` 等环境变化在 `view.rs` 采样、防抖后转化为高层语义 `Action`（如 `BreakpointChanged`）推送到 `logic`；禁止原始事件洪泛。（`N/A`：`CheckboxGroup` 为静态语义容器，不依赖窗口尺寸/主题监听/可见性观测驱动状态机；组件 `view.rs` 未采样 `Resize/Intersection` 原始事件，`logic.rs` 也无 `BreakpointChanged` 等环境 Action 回流。回归由 `components/checkbox-group/test/semantics.rs::checkbox_group_has_no_env_stream_sampling_contract` 锁定，防止后续引入环境事件洪泛。）
- [x] 事件光锥（Event Light Cone）：`Table/Grid` 等大型集合批量操作必须走 `Context Bus + Selector` 与状态压缩表达（如 `SelectionState::All`），禁止 O(N) 级向下 prop drilling。（`N/A`：`CheckboxGroup` 只承担字段分组语义，不承载大集合批量选择或跨节点批处理事件路由；组件 `logic/view` 未定义 `Context Bus/Selector/SelectionState::All` 协议，也不存在 O(N) 向下传递选择控制面。回归由 `components/checkbox-group/test/semantics.rs::checkbox_group_has_no_event_light_cone_contract` 锁定，防止后续误引入集合批处理总线。）
- [x] 统一因果总线（Causality Bus）：复杂派生总线操作必须支持透传 `TraceId`，确保“用户触发 -> 派生命令 -> 总线广播 -> 订阅者”因果链不断裂。（`N/A`：`CheckboxGroup` 为轻量字段容器，无跨模块派生命令总线与订阅广播链路；组件 `logic/view` 未定义 `TraceId` 透传或 bus 广播协议。回归由 `components/checkbox-group/test/semantics.rs::checkbox_group_has_no_unified_causality_bus_contract` 锁定，防止后续误引入总线因果链复杂度。）
- [x] 存在 A11y 实现、国际化与本地化实现（至少具备接入点，不硬编码用户可见文本）。（`CheckboxGroup` 通过 `ui_headless::use_checkbox_group` 挂载 `aria-* + lang/dir` 语义契约，`crates/ui-headless/src/checkbox_group.rs` 复用 `crates/ui-headless/src/a11y.rs::locale_attrs`，组件层不重写同名 A11y 工具；用户可见文本由 `label/description/error` props 提供并经 `logic.rs` 归一，默认兜底文案位于 `ui-state-primitives`（`DEFAULT_LABEL`），`view.rs` 不硬编码业务文案。键盘交互路径在本组件层为 `N/A`（`CheckboxGroup` 根节点是语义容器，实际键盘可达交互由子项 `Checkbox` 组件覆盖）。回归由 `components/checkbox-group/test/semantics.rs::checkbox_group_a11y_i18n_l10n_contract_stays_headless_and_overridable` 联合既有 headless/locale 语义测试锁定。）
  - 交互元素必须具备可验证语义：`role`/`aria-*`/键盘可达路径完整，且和 headless 契约一致。
  - 用户可见文本来源必须可覆盖：优先 props，其次应用注入（`UiRoot`/i18n bundle），最后组件兜底文案；禁止把业务可见文案硬编码在 `view.rs`。
  - 组件需透传或消费 `lang` / `dir`（LTR/RTL）上下文，不得假设单语言单方向。
  - 共享 A11y 工具优先来自 `crates/ui-headless/src/a11y.rs`，组件层不重复发明同名语义工具。
- [x] 状态可观测、可检索、可验证：使用稳定 `data-*` 与 `aria-*` 标记表达状态和来源。（`CheckboxGroup` 根节点已稳定暴露 `aria-*` 与关键 `data-*` 语义轴（`disabled/enabled/invalid/valid/required/optional/has-*`），并新增封闭来源标记 `data-state-source="semantic-props"` 与动效来源/阶段标记（`data-motion-source`、`data-motion-phase`）；状态值来源由 `logic.rs` 的枚举（`CheckboxGroupStateSource` / `CheckboxGroupMotionPhase`）统一映射，避免自由文本漂移。受控/非受控来源区分在本组件层 `N/A`（无 group value 状态轴），通过 `semantic-props` 明确“由语义 props 驱动”。回归由 `components/checkbox-group/test/semantics.rs::checkbox_group_emits_baseline_state_data_attributes` 与 `components/checkbox-group/test/semantics.rs::checkbox_group_state_markers_use_closed_enums_for_source_and_phase` 锁定。）
  - 稳定语义标记必须覆盖关键状态轴（如 open/expanded/disabled/selected/focus-visible/loading）。
  - 状态来源必须可区分（受控/非受控、默认值/外部值、交互来源），通过稳定 marker 暴露而不是隐式推断。
  - 自动化选择器优先基于语义标记，不依赖 DOM 顺序、层级深度或临时 class 名。
  - 标记值应为封闭集合（可枚举），避免自由文本导致契约漂移。
- [x] 样式依赖显式状态（`data-*`/class），而非脆弱 DOM 结构猜测。（`CheckboxGroup` 状态分支选择器基于稳定 class/语义属性（如 `.ui-checkbox-group--invalid`、`.ui-checkbox-group--required`、`:disabled`），`view.rs` 同步暴露 `data-invalid/data-shows-error` 等语义标记，不依赖 DOM 位置猜测；运行时 `style` 仅通过 `motion_contract::attach_motion(None, motion)` 注入 `--ui-checkbox-group-motion-*` 变量，不塞业务样式逻辑。回归由 `components/checkbox-group/test/semantics.rs::checkbox_group_styles_depend_on_semantic_state_markers_not_structural_guesses` 与既有 motion/style 语义测试锁定。）
  - `styles.rs` 中状态分支选择器必须基于 `data-*`/`aria-*`/稳定 class，禁止用 `:nth-child`、深层级选择器猜测状态。
  - 运行时样式仅允许传递必要 CSS 变量（custom properties）；禁止把业务样式逻辑塞进 inline style。
  - 视觉状态切换必须可由语义标记直接解释，不能依赖“某节点是否恰好存在”。
- [x] 测试验证“语义契约”而不只验证视觉快照。（`CheckboxGroup` 采用语义契约断言为主：`components/checkbox-group/test/semantics.rs` 覆盖 `aria-*` 与 `data-*`/`data-state-source`（`checkbox_group_view_mounts_headless_locale_contract`、`checkbox_group_emits_baseline_state_data_attributes`、`checkbox_group_state_markers_use_closed_enums_for_source_and_phase`）；关键分支矩阵中 `disabled` 由语义标记与 docs 场景断言覆盖，受控/非受控为 `N/A`（无 group value 轴，见 `checkbox_group_does_not_expose_group_value_control_axis`），键盘/指针在容器层为 `N/A`（见 `checkbox_group_has_no_dragging_macro_micro_state_machine_contract`），SSR/wasm 差异通过 motion 合同测试锁定契约入口（`checkbox_group_motion_contract_is_token_first_and_uses_ui_motion_backend`）。新增 `components/checkbox-group/test/semantics.rs::checkbox_group_semantics_suite_prioritizes_contract_assertions_over_snapshots` 明确禁止 snapshot 宏替代语义断言。）
  - 至少存在语义测试覆盖关键状态与交互路径（role/aria/data-state/source markers）。
  - 测试矩阵必须覆盖关键分支：受控/非受控、disabled、键盘路径、指针路径、SSR/wasm 差异（按适用范围）。
  - 视觉快照只能作为补充，不得替代语义契约断言。
- [x] 组件文件职责正确：`mod.rs`（导出边界）、`logic.rs`（归一/派生/来源标记）、`styles.rs`（静态 token-first CSS）、`view.rs`（Leptos 结构 + headless 挂载）、`motion.rs`（动效契约 + attach）。（`components/checkbox-group/src/mod.rs` 仅维护最小导出面（`CheckboxGroup` + `CheckboxGroupMotion`）；`logic.rs` 只做归一/派生与 headless 桥接；`styles.rs` 仅保留 token-first 静态 CSS；`view.rs` 仅做结构渲染与语义挂载；`motion.rs` 仅保留动效参数归一与 attach。回归由 `components/checkbox-group/test/semantics.rs::checkbox_group_module_files_keep_responsibility_boundaries` 锁定，防止文件职责回归漂移。）
  - `mod.rs` 只维护最小稳定导出面与 feature gate，不承载实现细节。
  - `logic.rs` 只做输入归一、状态派生、来源标记；禁止 DOM 操作和样式细节分支。
  - `styles.rs` 只包含 token-first 静态 CSS；禁止硬编码主题常量与业务语义文案。
  - `view.rs` 只做结构渲染与 headless 契约挂载；禁止隐藏关键状态决策。
  - `motion.rs` 只做组件语义到动效契约映射与 attach；禁止在组件内重写通用动效引擎。
- [x] `spec.rs` 只用于少数复杂组件（如 button），避免泛滥。（`N/A`：`CheckboxGroup` 为语义容器型简单组件，不存在稳定外部 Schema 契约或复杂配置固化需求，当前目录保持 `mod.rs + logic.rs + styles.rs + view.rs + motion.rs` 即可；组件说明留在 `check2.md` 与 `README.md`。回归由 `components/checkbox-group/test/semantics.rs::checkbox_group_does_not_introduce_spec_rs_without_schema_contract` 锁定，防止无依据引入 `spec.rs`。）
  - 仅当组件存在稳定外部规范/Schema 契约或复杂配置固化需求时才引入 `spec.rs`。
  - 简单组件不得为了“形式统一”新增 `spec.rs`；说明文档应留在 `check2.md`/组件文档。
  - 新增 `spec.rs` 必须同步给出契约测试与版本演进说明。
- [x] 组件层遵循 token-first 静态样式契约：样式通过 `styles.rs` 聚合注入；运行时仅传必要 CSS 变量；不把 Utility-First/CSS-in-Rust 当组件库默认范式。（`components/checkbox-group/src/styles.rs` 保持静态 `CSS` 常量并只消费 `var(--ui-*)/var(--ui-fallback-*)`；`crates/ui-components/src/css.rs` 通过 `#[cfg(feature = "component-checkbox_group")] out.push_str(crate::checkbox_group::styles::CSS);` 聚合注入；运行时仅在 `components/checkbox-group/src/motion.rs` 写入 `--ui-checkbox-group-motion-*` 变量并由 `view.rs` 挂载 `style=style`。回归由 `components/checkbox-group/test/semantics.rs::checkbox_group_css_aggregation_uses_new_top_level_contract`、`components/checkbox-group/test/semantics.rs::checkbox_group_token_first_style_contract_stays_in_styles_and_css_vars`、`components/checkbox-group/test/semantics.rs::checkbox_group_styles_depend_on_semantic_state_markers_not_structural_guesses` 锁定，禁止 Utility-First/CSS-in-Rust 污染组件契约。）
  - 样式规则统一落在 `styles.rs`，由 `crates/ui-components/src/css.rs` 聚合并通过 `UiRoot` 注入。
  - 颜色/间距/圆角/阴影等视觉值必须来自 `var(--ui-*)`，禁止组件私有 token 体系。
  - Utility-First 仅作为 `apps/*` 应用层布局手段，不得反向污染组件库契约。
  - CSS-in-Rust 仅在有明确类型安全与构建成本净收益时作为例外采用。
- [x] 默认主题美学质量达标（Visual Desire）：以 HeroUI 现代审美为学习对标，默认主题不仅“可用”，还必须“第一眼可信”。（该项为仓库级主题基线能力，`checkbox-group` 作为消费方复用统一证据链：`apps/docs-app/src/pages/components/pages/theme_visual_baseline.rs` 已提供默认主题基线页（层级/对比/hover-active-focus 反馈）并固定 `Button/Input/Overlay` 三个关键锚点；`e2e/tests/docs_app_theme_visual_baseline.spec.mjs` 已提供页面与分区截图基线（`docs-app-theme-visual-baseline-{page,button,input,overlay}.png`）。组件侧回归通过 `components/checkbox-group/test/semantics.rs::checkbox_group_visual_desire_contract_uses_theme_baseline_page_and_snapshots` 锁定“基线页 + 截图回归”契约，防止退化到“仅可访问但视觉粗糙”的最低可用。）
  - 默认主题需通过基础美学清单：信息层级清晰（字重/字号/间距）、对比与层次自然、交互反馈明确（hover/active/focus）。
  - docs-app 必须提供默认主题基线页面与截图基线，关键组件（Button/Input/Overlay）纳入视觉回归对比。
  - 禁止“可访问但粗糙”的最低可用心态：视觉退化（类似旧式 Bootstrap 观感）视为质量回归。
  - HeroUI 对标以“视觉语言与体验质量”对齐为目标，不做无差别 API 表层复制。
- [x] Tree Shaking 是一等能力：package 模式支持组件级 feature；source 模式天然裁剪；样式层同步裁剪，禁止无条件聚合全部 CSS，禁止破坏 DCE/LTO 的全量中央注册表。（`crates/ui-components/Cargo.toml` 已将 `component-checkbox_group` 定义为独立 feature（`component-checkbox_group = ["component-checkbox", "dep:ui-checkbox-group"]`）且依赖为 optional；`crates/ui-components/src/lib.rs` 与 `crates/ui-components/src/css.rs` 均通过 `#[cfg(feature = "component-checkbox_group")]` 条件导出/聚合，未启用特性不会进入可达路径。命令证据：`cargo tree -e features -i ui-components -p ui-components --no-default-features --features component-accordion,inject-css` 仅出现 command-line `component-accordion/inject-css`（无 `all-components`）；`cargo tree -e features -i ui-components -p web-demo` 仅出现 `web-demo-components`（未出现 `all-components`）。CI 侧已由 `.github/workflows/ci.yml` 的 `Tree Shaking Budget` 调用 `scripts/check-ui-components-tree-shaking.sh`，脚本覆盖最小特性 wasm 编译 + release 产物预算（`scripts/tree_shaking_budget.env`）。组件回归由 `components/checkbox-group/test/semantics.rs::checkbox_group_tree_shaking_contract_is_feature_gated_and_budgeted` 锁定。）
  - package 模式必须有组件级 feature（如 `component-accordion`）；未启用组件不得进入编译与链接路径。
  - `lib.rs` 与 `css.rs` 必须按 feature 条件导出/聚合，禁止无条件引用所有组件模块和 CSS 常量。
  - source 模式下仅引入需要的组件源码，不通过中央注册表维持全组件可达。
  - 任意“全量组件映射表/注册表”若导致不可达代码变可达，直接判不通过。
  - 验证命令（特性树）：`cargo tree -e features -p ui-components --no-default-features --features component-accordion,inject-css`，确认仅启用目标组件特性链。
  - 验证命令（反向依赖）：`cargo tree -e features -i ui-components -p web-demo`，检查是否被 `all-components` 或隐式特性全量拉起。
  - CI 检查（最小特性编译）：新增任务仅开启目标最小特性（示例：`cargo check -p ui-components --target wasm32-unknown-unknown --no-default-features --features component-accordion,inject-css`）。
  - CI 检查（体积预算）：对“最小特性构建产物”设定预算并阻断回归（可用固定阈值，如 `< 50KB`，或基于仓库基线的相对阈值）；不得只做编译通过而不做体积约束。
- [x] 类型系统 + 语义标记共同提供机器可读状态；关键输入空间受类型约束。（`checkbox-group` 无 `variant/size/mode/status` 这类离散输入轴（该子场景 `N/A`），但关键状态轴已类型化并可枚举：`components/checkbox-group/src/logic.rs` 使用 `CheckboxGroupStateSource` 与 `CheckboxGroupMotionPhase` 枚举（`as_data_attr -> &'static str`）以及 `ui-state-primitives::CheckboxGroupState`；无效组合统一由 `resolve_checkbox_group_state` + `resolve_checkbox_group_view_state` 归一并在 `components/checkbox-group/test/logic.rs` 覆盖。`components/checkbox-group/src/view.rs` 稳定暴露 `data-*`（`data-invalid/data-required/data-shows-error/data-state-source/data-motion-phase`）供自动化检索。回归由 `components/checkbox-group/test/semantics.rs::checkbox_group_type_system_and_semantic_markers_form_machine_readable_contract`、`components/checkbox-group/test/semantics.rs::checkbox_group_state_markers_use_closed_enums_for_source_and_phase`、`components/checkbox-group/test/logic.rs` 对应用例共同锁定，契约破坏可被编译器与测试直接定位。）
  - 离散输入与状态轴必须优先使用 `enum`/新类型建模，避免字符串协议与布尔爆炸。
  - 无效状态要么在类型层不可表达，要么在 `logic.rs` 被统一归一化并可测试。
  - 关键状态必须通过稳定语义标记对外可读，供测试与 Agent 自动化消费。
  - 编译器与测试反馈应能直接定位状态契约破坏点，形成可持续闭环。

### 4. DOM/环境边界治理
- [x] 焦点全局栈（Focus Stack & GC）：层叠 `Overlay` 禁止私存 `NodeRef` 作为恢复目标；必须依赖全局 Focus Manager（如 `FallbackTo/Selector`）防止焦点坠落到 `document.body`。（`N/A`：`checkbox-group` 为表单语义容器，不创建 overlay/portal，也不承担焦点恢复职责；该能力应由 `ui-headless` 的 `focus_trap/overlay_stack` 统一提供。组件侧已通过 `components/checkbox-group/test/semantics.rs::checkbox_group_has_no_overlay_focus_stack_contract` 锁定：禁止在 `logic/view/motion/Component.toml/rbi` 引入 `NodeRef` 焦点恢复、`use_focus_trap`、`FallbackTo/Selector`、`document.body` 等契约；并反向断言这些能力仍落在 `crates/ui-headless/src/focus_trap.rs` 与 `crates/ui-headless/src/lib.rs`。）
- [x] 受控外交特区（Escape Hatches）：集成 ECharts/Map 等命令式第三方库时必须处于 `Foreign Zone`（`YieldControl/CleanupForeign`）；第三方实例不得暴露为组件公共 API 或反向污染状态机。（`N/A`：`checkbox-group` 不集成 ECharts/Map 等命令式第三方实例，不存在 `Foreign Zone` 接入需求。组件侧以负向契约锁定该边界：`components/checkbox-group/test/semantics.rs::checkbox_group_has_no_foreign_zone_escape_hatch_contract` 断言 `logic/view/motion/Component.toml/rbi/Cargo.toml` 不出现 `ForeignZone/YieldControl/CleanupForeign`、第三方实例句柄及相关公共 API（如 `foreign_instance/on_foreign_ready`），防止后续把第三方实例暴露到组件公共面。）
- [x] SSR 时空断裂治理（Hydration Discontinuity）：逻辑初始化禁止依赖 `now()` 或原生随机 UUID；必须通过 `IdProvider` 注入确定性种子，确保 SSR/Hydration 间 ID 稳定。（`checkbox-group` 采用确定性 ID 边界：公共 API 强制 `id: String`（`Component.toml` 中 `id` 为 `default = "required"`），组件仅做纯字符串派生（`resolve_checkbox_group_ids -> "{id}-label"`），不在逻辑初始化阶段生成随机 ID。该项在组件层对“`IdProvider` 注入”属于 `N/A`（无内部自动 ID 生成需求），`IdProvider` 能力保留在 `crates/ui-headless/src/id_provider.rs` 供需要自动种子的组件使用。回归由 `components/checkbox-group/test/semantics.rs::checkbox_group_hydration_discontinuity_contract_uses_deterministic_ids_only` 锁定：禁止 `now()/random/uuid` 路径并断言 `id` 必填与确定性派生。）
- [x] SSR 与跨平台检查：覆盖 web/ssr/wasm 分支，不破坏 non-wasm 编译路径。（仓库级 compile-only 证据已存在：`scripts/check.sh` 与 `scripts/check-ui-components-platforms.sh` 覆盖 native 默认构建、`ui-headless` 的 `ssr` native 编译、`ui-headless` 的 `web` wasm 编译、`ui-components` wasm 编译与 `ui-motion` native/wasm 编译。`checkbox-group` 组件侧保持平台中立：`logic/view/motion/styles/mod` 不引用 `web_sys/js_sys/window/document`，平台差异由依赖层（`ui-headless`/`ui-motion`）承接。回归由 `components/checkbox-group/test/semantics.rs::checkbox_group_platform_contract_covers_ssr_wasm_and_non_wasm_boundaries` 锁定。）
  - 至少包含 compile-only 证据：web（wasm32）、ssr（native）、默认本地构建三条路径。
  - 平台分支差异必须显式 `cfg` 或 feature 管理，禁止依赖运行时偶然行为。
  - non-wasm 路径禁止引用 `web-sys`/浏览器对象。
- [x] `ui-headless` web/ssr feature 互斥受 `compile_error!` 保护（`crates/ui-headless/src/lib.rs`）。（`crates/ui-headless/src/lib.rs` 已定义 `#[cfg(all(feature = "web", feature = "ssr"))] compile_error!(...)` 强制互斥；仓库脚本 `scripts/check.sh` 分别验证 `ssr` 与 `web` 编译路径，`scripts/check-ui-components-platforms.sh` 明确执行 `cargo check -p ui-headless --no-default-features --features web,ssr` 并要求出现 `mutually exclusive` 失败信息。`checkbox-group` 侧通过常规依赖消费 `ui-headless`（`components/checkbox-group/Cargo.toml` 与 `Component.toml`），未显式开启冲突特性；回归由 `components/checkbox-group/test/semantics.rs::checkbox_group_respects_ui_headless_web_ssr_mutex_contract` 锁定。）
  - 组件依赖 `ui-headless` 能力时，不得破坏其 web/ssr 互斥约束。
  - 组件若新增 headless 功能接入，需验证两条 feature 路径都可编译。
  - 发现“同时启用 web+ssr 仍可过编译”视为契约回归。
- [x] `ui-motion` 非 wasm 提供 no-op/stub（`crates/ui-motion/src/lib.rs`），保证 SSR/tooling 可编译。（`crates/ui-motion/src/lib.rs` 已在 `#[cfg(not(target_arch = "wasm32"))]` 下提供 `web` no-op/stub：`prefers_reduced_motion() -> true` 与 `animate(&(), ..)` 空实现；`crates/ui-motion/tests/non_wasm_stub.rs` 锁定“非 wasm 安全 no-op”回归。`checkbox-group` 侧 `components/checkbox-group/src/motion.rs` 只通过 `ui_motion::web::prefers_reduced_motion()` 计算 `reduced` 并将时长降级到 `1ms`，不假设动画句柄存在、不触发 panic；回归由 `components/checkbox-group/test/motion.rs::attach_motion_uses_predictable_non_wasm_reduced_motion_fallback` 与 `components/checkbox-group/test/semantics.rs::checkbox_group_motion_non_wasm_stub_contract_is_predictable_and_safe` 锁定。toolchain 侧 `scripts/check-ui-components-platforms.sh` 继续覆盖 `cargo check -p ui-motion`、`cargo check -p ui-motion --target wasm32-unknown-unknown` 与 `cargo test -p ui-motion --test non_wasm_stub`。）
  - `motion.rs` 调用必须可在 non-wasm 下安全降级，不触发 panic。
  - 组件不得假设动画句柄一定存在；no-op 分支行为需可预测。
  - toolchain 场景（测试/文档/静态分析）不得因 motion 依赖阻塞编译。
- [x] 组件实现覆盖 `reduced-motion` / SSR / wasm 分支。（`reduced-motion`：`components/checkbox-group/src/motion.rs` 通过 `let reduced = !motion.enabled || ui_motion::web::prefers_reduced_motion()` 统一降级，`effective_duration` 在降级路径固定为 `1ms`；回归由 `components/checkbox-group/test/motion.rs::attach_motion_reduced_motion_branch_uses_minimal_feedback_when_disabled` 与 `components/checkbox-group/test/motion.rs::attach_motion_uses_predictable_non_wasm_reduced_motion_fallback` 锁定。`SSR/hydration`：`components/checkbox-group/src/view.rs` 始终挂载同一语义标记（`data-state-source`、`data-motion-phase`）与 `attach_motion` 输出，未引入 wasm/ssr 组件分叉；`wasm`：编译路径证据由 `scripts/check-ui-components-platforms.sh` 的 `ui-headless(ssr/web)` 与 `ui-motion(native/wasm)` 检查覆盖。总回归由 `components/checkbox-group/test/semantics.rs::checkbox_group_reduced_motion_ssr_wasm_branches_keep_semantics_consistent` 锁定。）
  - `reduced-motion` 下动画应跳过或降级为最小必要反馈。
  - SSR 输出必须与客户端 hydration 兼容，避免首帧语义错位。
  - wasm 分支允许增强交互，但语义契约不得与 SSR 分支分裂。
- [x] 性能治理：关键路径有预算（首次渲染/更新耗时/内存），回归可检测、可归因、可阻断。（`checkbox-group` 已接入可阻断性能契约：`apps/docs-app/src/pages/components/shell.rs` 为 `"checkbox-group"` 定义预算 `UiPerfBudget { max_mount_ms: 26.0, max_update_ms: Some(9.0), max_heap_kb: Some(448.0) }`，并通过 `<UiPerfProbe name=perf_name budget=perf_budget>` 挂载；`apps/docs-app/src/perf_probe.rs` 输出稳定 `data-perf-*` 指标与 `data-perf-violation` 阈值断言，`e2e/tests/docs_app_components_coverage.spec.mjs` 对这些标记执行回归检查。组件侧 `components/checkbox-group/src/view.rs` 暴露状态来源与动效来源标记（`data-state-source/data-motion-source/data-motion-phase`）用于性能归因。CI 阻断入口已接入 `scripts/check-ui-components-performance.sh`：`cargo test -p ui-components --test checkbox_group_semantics --no-default-features --features component-checkbox_group,inject-css checkbox_group_performance_governance_contract_is_budgeted_traceable_and_blocking`。`render_count` 精确计数当前 `N/A`：`CheckboxGroup` 暂未接入精确 `render_count` 自动化计数，采用 mount+budget+trace 等价证据，后续自动化跟踪沿用 `docs/plan/TODO.md` 的 `render_count` 任务。）
  - 关键交互组件需定义最小预算项（首渲染、关键更新、内存/分配趋势）。
  - 回归检测至少具备可重复基线与失败阈值，不靠主观“感觉变慢”。
  - 性能问题需可归因到状态、渲染、样式或动效路径之一。
  - 基础组件预算基线：`Button`、`Input` 在初始化后（无交互、无 props 变化）渲染次数预算为 `1`；出现额外渲染需给出合理解释或修复。
  - 测试要求：在 `components/*/test/**` 增加 `render_count` 类回归测试（测试框架支持时必须启用）；至少覆盖基础组件与本次改动组件。
  - 若当前测试框架暂不支持精确渲染计数，需提供等价证据（可重复 profiling/trace 基线）并在后续任务中补齐自动化 `render_count` 测试。
- [x] `view!` 宏复杂度受控：单个 `view!` 块不得承载超长深嵌套结构；复杂布局按语义分块，避免一次性宏展开导致编译与 wasm 体积劣化。（`components/checkbox-group/src/view.rs` 当前保持紧凑规模（129 行）且 `view!` 使用受控为 3 处（根节点 + description/error 可选片段），未出现循环密集/集合拼装型宏展开（如 `for item in`、`collect::<Vec<_>>()`）；组件结构维持语义分层（label/list/description/error）并避免巨型单块分支堆叠。回归由 `components/checkbox-group/test/semantics.rs::checkbox_group_view_macro_complexity_is_small_and_does_not_require_semantic_subrenders` 锁定，CI 门禁由 `scripts/check-ui-components-view-macro.sh` 新增命令 `cargo test -p ui-components --test checkbox_group_semantics --no-default-features --features component-checkbox_group,inject-css checkbox_group_view_macro_complexity_is_small_and_does_not_require_semantic_subrenders` 覆盖。）
  - 复杂结构按语义子块拆分（header/body/item 等），避免巨型单块 `view!`。
  - `view.rs` 中若出现多层嵌套重复片段，应优先提取局部渲染函数。
  - 编译时间/产物体积异常增长时，优先排查宏展开体量。
- [x] 函数式拆分优先：不涉及复杂状态与生命周期管理的 UI 片段，优先拆为普通 Rust 函数（返回 `impl IntoView`/`View`），而不是新增 `#[component]`。（`components/checkbox-group/src/view.rs` 已将轻量片段拆为普通函数 `render_description_block` 与 `render_error_block`（返回 `impl IntoView`），主组件只负责装配 `description_view/error_view`；未引入局部 `#[component]` 抽象噪音。回归由 `components/checkbox-group/test/semantics.rs::checkbox_group_view_functional_split_prefers_plain_functions_over_local_components` 锁定，并纳入 `scripts/check-ui-components-view-macro.sh` 阻断命令：`cargo test -p ui-components --test checkbox_group_semantics --no-default-features --features component-checkbox_group,inject-css checkbox_group_view_functional_split_prefers_plain_functions_over_local_components`。）
  - 纯静态或轻逻辑片段优先函数化；仅在需要独立 props 语义时升级为组件。
  - 禁止把所有局部片段都升格为 `#[component]` 导致抽象噪音。
  - 拆分后语义标记与测试定位仍需稳定。
- [x] 静态片段常量化：复杂 SVG、页脚、长说明文本等纯静态内容优先常量化/模板化，减少重复 `view!` 渲染指令生成。（`CheckboxGroup` 当前属于简单表单分组布局：`components/checkbox-group/src/view.rs` 不包含复杂 SVG/页脚/长文本模板与 `inner_html` 路径，因此该子项按“常量化或缺省无重片段”通过；同时保留稳定语义挂载（`data-slot` 与 `aria-*`）防止优化破坏可访问契约。回归由 `components/checkbox-group/test/semantics.rs::checkbox_group_static_fragments_are_constantized_or_absent_for_simple_layout` 锁定，门禁接入 `scripts/check-ui-components-view-macro.sh`：`cargo test -p ui-components --test checkbox_group_semantics --no-default-features --features component-checkbox_group,inject-css checkbox_group_static_fragments_are_constantized_or_absent_for_simple_layout`。）
  - 可判定为纯静态的片段应避免重复动态构造。
  - 常量化后仍需维持可访问语义（title/aria-label/role 等）。
  - 静态资源变更路径要清晰，避免散落在多个 `view!` 片段中。
- [x] `inner_html` 使用约束：仅允许注入受信任静态常量，禁止拼接用户输入；使用处必须补充语义与安全回归测试。（`CheckboxGroup` 不存在 `inner_html` 注入面：`components/checkbox-group/src/view.rs` 仅通过类型化节点渲染，未使用 `inner_html/set_inner_html/dangerously_set_inner_html`；组件与 docs 相关路径（`logic/styles/motion/README/docs forms page`）均未引入脚本注入标记（`<script`、`javascript:`、`onerror=` 等）。语义与安全负向回归由 `components/checkbox-group/test/semantics.rs::checkbox_group_inner_html_usage_is_absent_and_untrusted_html_paths_are_blocked` 锁定，并接入 `scripts/check-ui-components-contract-hygiene.sh` 阻断命令：`cargo test -p ui-components --test checkbox_group_semantics --no-default-features --features component-checkbox_group,inject-css checkbox_group_inner_html_usage_is_absent_and_untrusted_html_paths_are_blocked`。）
  - 仅允许编译期常量或明确白名单内容进入 `inner_html`。
  - 严禁直接或间接注入用户输入、远端返回或未清洗模板字符串。
  - 使用 `inner_html` 的节点必须补语义测试与安全回归说明。
- [x] WASM 调试要求：关键状态可追踪（来源/时间/前后值），关键交互可回放，开发模式有可视化入口，调试能力通过 feature 隔离不污染产物。（`CheckboxGroup` 作为容器装配层不新增本地 replay runtime，走 N/A-by-design + 全局链路复用：组件侧通过 `data-state-source/data-motion-source/data-motion-phase/data-invalid/data-required` 暴露稳定来源与状态标记；时间线与可视化入口复用 `apps/docs-app/src/lib.rs` 的 `provide_ui_trace(debug_overlay_enabled)` + `UiDebugOverlay`（`UiTraceEvent { ts_ms, component, kind }`）；关键交互最小复现链路由 docs 交互页 `apps/docs-app/src/pages/components/pages/forms.rs` 的 `checkbox-group` playground（`interactive_alpha/beta` 勾选路径 + 选中计数）提供。feature 隔离保持默认生产纯净：`components/checkbox-group/Cargo.toml` 与 `crates/ui-components/Cargo.toml` 均无 `checkbox-group-wasm-debug` 私有特性。回归：`components/checkbox-group/test/semantics.rs::checkbox_group_wasm_debug_contract_is_explicitly_na_and_feature_isolated`；脚本门禁：`scripts/check-ui-components-wasm-debug.sh` 新增 `cargo test -p ui-components --test checkbox_group_semantics --no-default-features --features component-checkbox_group,inject-css checkbox_group_wasm_debug_contract_is_explicitly_na_and_feature_isolated`。）
  - 开发模式下至少能追踪关键状态变更来源与前后值。
  - 关键交互链路应支持最小可复现记录（事件顺序/状态转移）。
  - 调试开关默认不进入生产包体与公共 API。
- [x] DX 要求：样式热重载优先无需重编 wasm；组件热开发尽量保持上下文；提供可选状态保留；有 Workbench 隔离画布。（`CheckboxGroup` docs 已提供隔离演练入口：`apps/docs-app/src/pages/components/pages/forms.rs` 中 `title=\"Interactive Playground\"` 同时挂载 `test_css_source=interactive_test_css`、`test_config_signal=interactive_config` 与 `controls=move || view!`，可在不重编 wasm 的前提下通过 CSS Test 面板调整样式并保留当前交互上下文（`interactive_alpha/beta` 与 selected count 可持续观测）。隔离画布能力复用 `apps/docs-app/src/playground.rs`（`data-playground-scope` + `compose_scoped_css` + preview stage）；“可选状态保留”在本组件范围按 N/A 管理：当前未引入 `checkbox-group` 专属本地存储键，避免额外协议污染。回归：`components/checkbox-group/test/semantics.rs::checkbox_group_dx_playground_supports_css_hot_reload_and_isolated_canvas_with_optional_persist_na`；脚本门禁：`scripts/check-ui-components-dx.sh` 新增 `cargo test -p ui-components --test checkbox_group_semantics --no-default-features --features component-checkbox_group,inject-css checkbox_group_dx_playground_supports_css_hot_reload_and_isolated_canvas_with_optional_persist_na`。）
  - 常见样式调整应走快速反馈路径，不依赖完整 wasm 重编译。
  - 组件调试应尽量保持当前交互上下文，降低重复操作成本。
  - 复杂交互组件应有隔离演练入口（workbench/story/demo 之一）。
- [x] 工程能力统一：`serde` 负责 spec 序列化/版本迁移/错误结构化；`tracing` 统一 span/event 语义；async 不绑定单一运行时（tokio/async-std），runtime 细节不泄露到上层 API。（`CheckboxGroup` 作为简单表单分组组件在工程能力上按 N/A+统一基线收敛：目录不存在 `components/checkbox-group/src/spec.rs`，实现与接口投影（`mod/logic/view/styles/motion/Component.toml/checkbox_group.rbi`）未引入 `serde/serde_json/SchemaError` 路径；tracing 语义复用全库统一约定（`crates/ui-components/Cargo.toml` 仅保留共享 `button-wasm-debug/accordion-wasm-debug` tracing 开关，组件未新增 `checkbox-group-wasm-debug` 或本地 tracing target）；runtime 边界保持纯净（无 `tokio/async-std/smol/runtime::Handle` 泄露）。回归：`components/checkbox-group/test/semantics.rs::{checkbox_group_engineering_contract_marks_spec_serde_path_as_na_for_simple_component_scope,checkbox_group_engineering_contract_keeps_tracing_semantics_unified_without_component_local_events,checkbox_group_engineering_contract_avoids_runtime_leaks_in_public_api_surface,checkbox_group_engineering_check_script_covers_serde_tracing_and_runtime_boundaries}`；脚本门禁：`scripts/check-ui-components-engineering.sh` 新增对应三条 `cargo test` 命令。）
  - 若组件涉及 spec/config 输入，序列化与错误输出应走统一结构化路径。
  - 关键流程埋点语义应与全库 tracing 约定一致，避免组件各说各话。
  - 异步边界不得把具体 runtime 类型暴露到组件公共接口。

### 5. 样式与动效（Theme & Motion）
- [x] 样式孤岛防御（Defensive Variables）：`styles.rs` 使用双层回退链 `var(--ui-*, var(--ui-fallback-*))`；禁止组件内硬编码 Hex 或裸尺寸终值，Fallback 终值由 `ui-theme` 统一输出（SSOT）。（`components/checkbox-group/src/styles.rs` 已对分组关键视觉轴统一使用双层回退链：`gap/label typography/required marker/error color/motion duration&easing/disabled opacity` 均为 `var(--ui-*, var(--ui-fallback-*))`，且无 `rgb()/hsl()/px/rem/em` 终值与 Hex 硬编码；Fallback 终值由 `crates/ui-theme/src/css.rs` 提供（`--ui-fallback-checkbox-group-*` 与通用 `--ui-fallback-font-size-*`、`--ui-fallback-line-height-*`、`--ui-fallback-danger`、`--ui-fallback-fg-muted`）。回归：`components/checkbox-group/test/semantics.rs::checkbox_group_styles_use_defensive_variable_fallback_chain`；脚本门禁：`scripts/check-ui-components-contract-hygiene.sh` 新增 `cargo test -p ui-components --test checkbox_group_semantics --no-default-features --features component-checkbox_group,inject-css checkbox_group_styles_use_defensive_variable_fallback_chain`。）
- [x] 级联层覆盖（`@layer ui`）：组件 CSS 默认聚合进 `@layer ui`；运行时数值调整仅通过 CSS Custom Properties（如 `style:--x=...`），禁止普通内联样式（如 `style=\"top: 10px\"`）。（组件样式聚合由 `crates/ui-components/src/css.rs` 统一包裹在 `@layer ui`，且 `component-checkbox_group` 通过条件分支注入 `crate::checkbox_group::styles::CSS`；`UiRoot` 仍由 `crates/ui-components/src/root.rs` 统一触发 `push_components_css`。`CheckboxGroup` 运行时仅走 `style=style` 变量通道，`components/checkbox-group/src/motion.rs` 仅写入 `--ui-checkbox-group-motion-*` 自定义属性集合（`duration/easing/stiffness/damping/mass/precision/reduced`），未引入 `style=\"top/left/...\"` 或 `style:top=` 一类普通内联样式。回归：`components/checkbox-group/test/semantics.rs::checkbox_group_cascade_layer_and_runtime_style_contract_is_enforced`；脚本门禁：`scripts/check-ui-components-contract-hygiene.sh` 新增 `cargo test -p ui-components --test checkbox_group_semantics --no-default-features --features component-checkbox_group,inject-css checkbox_group_cascade_layer_and_runtime_style_contract_is_enforced`。）
- [x] Motion 合同化：`stiffness`/`damping` 等参数在 `motion.rs` 内置为组件 Contract，并通过 `attach_motion` 挂载；必须尊重 `prefers-reduced-motion` 且在 non-wasm/SSR 安全降级（no-op）。（`components/checkbox-group/src/motion.rs` 已把 `CheckboxGroupMotion` 扩展为内置 spring 合同（`spring: ui_motion::spring::SpringConfig`，含 `stiffness/damping/mass/precision`），默认值来自 `ui_theme::default_checkbox_group_motion_tokens()` 并经 `ui_motion::spring::sanitize_config` 归一；`attach_motion` 统一挂载 `--ui-checkbox-group-motion-{duration,easing,stiffness,damping,mass,precision,reduced}`。`resolve_effective_motion` 使用 `!motion.enabled || ui_motion::web::prefers_reduced_motion()` 降级为 `1ms` 最小反馈；non-wasm/SSR 路径由 `crates/ui-motion/src/lib.rs` 的 no-op `web::prefers_reduced_motion` 保证可预测与安全。回归：`components/checkbox-group/test/motion.rs` + `components/checkbox-group/test/semantics.rs::checkbox_group_motion_contract_is_builtin_and_attached_with_reduced_motion_and_non_wasm_noop`；脚本门禁：`scripts/check-ui-components-contract-hygiene.sh` 新增 `cargo test -p ui-components --test checkbox_group_semantics --no-default-features --features component-checkbox_group,inject-css checkbox_group_motion_contract_is_builtin_and_attached_with_reduced_motion_and_non_wasm_noop`。）
- [x] `ui-components` 固定入口文件落点正确。（入口边界已按固定落点锁定：`crates/ui-components/src/lib.rs` 维持 `component-checkbox_group` feature gate + `pub use ui_checkbox_group as checkbox_group` + `pub use root::UiRoot`；`crates/ui-components/src/css.rs` 维持 `push_components_css` 的 feature 条件注入并在 `@layer ui` 下按 `component-checkbox_group` 注入 `crate::checkbox_group::styles::CSS`；`crates/ui-components/src/root.rs` 继续集中注入 base css + theme vars + components css，并统一提供 `provide_ui_i18n/provide_ui_id_provider`。共享视觉原语落点保持在 `crates/ui-visual-primitive/src/active_highlight.rs`，且 `crates/ui-components/src/overlay_open.rs`、`crates/ui-components/src/presence.rs`、`crates/ui-components/src/a11y.rs` 均不存在；对应 canonical 能力仍位于 `crates/ui-headless/src/controllable_state.rs`、`crates/ui-headless/src/presence.rs`、`crates/ui-headless/src/a11y.rs`。回归：`components/checkbox-group/test/semantics.rs::checkbox_group_ui_components_fixed_entry_files_follow_layered_boundaries`（通过 `components/checkbox-group/test/checkbox_group_semantics.rs` 复用执行）；脚本门禁：`scripts/check-ui-components-entrypoints.sh` 新增 `cargo test -p ui-components --test checkbox_group_semantics --no-default-features --features component-checkbox_group,inject-css checkbox_group_ui_components_fixed_entry_files_follow_layered_boundaries`。）
  - `crates/ui-components/src/lib.rs`：总模块入口 + 对外 `pub use`（公共 API 面）；组件模块受 `component-*` feature gate 约束；不暴露内部平台细节类型。
  - `crates/ui-components/src/css.rs`：组件 CSS 聚合入口（`push_components_css`）；按 feature 条件注入；禁止无条件聚合全部组件 CSS。
  - `crates/ui-components/src/root.rs`：`UiRoot` 统一注入 base css + theme vars +（可选）components css，并提供全局 i18n 上下文；主题与注入策略必须集中在此。
  - `crates/ui-visual-primitive/src/active_highlight.rs`：共享高亮条样式与 motion driver；只承载通用高亮动效能力，不承载具体组件业务语义。
  - `crates/ui-components/src/overlay_open.rs`：当前仓库中不应存在；open-state 原语固定在 `crates/ui-headless/src/controllable_state.rs`，组件通过 headless API 消费。
  - `crates/ui-components/src/presence.rs`：当前仓库中不应存在；presence 原语固定在 `crates/ui-headless/src/presence.rs`，组件通过 `ui_headless::use_presence` 消费。
  - `crates/ui-components/src/a11y.rs`：当前仓库中不应存在；共享 A11y 工具固定在 `crates/ui-headless/src/a11y.rs`（如 `aria_controls_when_open`），组件只负责挂载。
- [x] 组件目录标准文件落点正确。（`components/checkbox-group/src/` 维持标准落点：`mod.rs/logic.rs/styles.rs/view.rs/motion.rs` 均存在，`render.rs/spec.rs` 维持缺省 N/A；`mod.rs` 仅导出稳定边界（`pub use view::CheckboxGroup` + `pub use motion::CheckboxGroupMotion`，未暴露 `logic/view` 实现细节）；`logic.rs` 保持 props 归一与状态派生（无 `view!/web_sys`）；`styles.rs` 保持 token-first 静态 CSS（仅 `var(--ui-*)` 通道）；`view.rs` 只做 Leptos 结构渲染与 headless 语义挂载；`motion.rs` 保持 `XxxMotion + attach_motion` 合同映射且不承载视图语义。`spec.rs` 判定 N/A（简单组件，无稳定外部 schema 需求）。回归：`components/checkbox-group/test/semantics.rs::checkbox_group_component_directory_standard_files_follow_contract_and_na_paths`（由 `components/checkbox-group/test/checkbox_group_semantics.rs` 复用执行）；脚本门禁：`scripts/check-ui-components-component-files.sh` 新增 `cargo test -p ui-components --test checkbox_group_semantics --no-default-features --features component-checkbox_group,inject-css checkbox_group_component_directory_standard_files_follow_contract_and_na_paths`。）
  - `<component>/mod.rs`：最小稳定导出面，存在且无过度导出。
  - `<component>/logic.rs`：props 归一化、派生状态、来源标记；不得承载可下沉原语。
  - `<component>/styles.rs`：静态 CSS 契约，只用 `var(--ui-*)`，不写死主题常量。
  - `<component>/view.rs`：纯 Leptos 结构渲染 + headless 语义挂载；禁止 `render.rs` 漂移；不隐藏关键状态决策。
  - `<component>/motion.rs`：`XxxMotion + attach_motion`；交互组件必须有；只做语义到 motion contract 的映射与挂载。
  - `<component>/spec.rs`：仅极少数组件专用（当前主要 button），无必要不新增。

### 6. AI 原生能力与文件落点（Struct-First & Projection）
- [x] 文件落点纪律：组件目录严格由 `mod.rs`（导出）、`logic.rs`（归一派生）、`styles.rs`（Token 样式）、`view.rs`（渲染）、`motion.rs`（动效）组成；复杂组件可选 `spec.rs`；禁止 `render.rs`。（`components/checkbox-group/src/` 当前仅包含 `mod.rs/logic.rs/styles.rs/view.rs/motion.rs`（以及 `Component.toml/README.md/checkbox_group.rbi` 文档投影），不存在 `render.rs`；`spec.rs` 在本组件判定为 `N/A`（简单语义容器，无稳定外部 schema 契约需求）。职责边界保持稳定：`mod.rs` 只导出 `CheckboxGroup/CheckboxGroupMotion`，`logic.rs` 做归一派生，`styles.rs` 仅静态 token CSS，`view.rs` 负责结构 + headless 语义挂载，`motion.rs` 负责动效合同映射。回归：`components/checkbox-group/test/semantics.rs::checkbox_group_file_placement_discipline_is_strict_for_component_scope`；脚本门禁：`scripts/check-ui-components-component-files.sh` 新增 `cargo test -p ui-components --test checkbox_group_semantics --no-default-features --features component-checkbox_group,inject-css checkbox_group_file_placement_discipline_is_strict_for_component_scope`。）
- [x] Hyper-Structure Builder（`spec.rs`）：复杂组件必须提供 AI 友好的 `*Spec::new()...render()` 建造者 API。（`N/A`：`CheckboxGroup` 为简单语义分组组件，不存在稳定外部 Schema 契约与复杂配置固化需求；当前保持无 `spec.rs` 设计，避免为假问题引入额外抽象与版本负担。若未来演进为复杂配置组件，再引入 `CheckboxGroupSpec::new()...render()` 并补齐契约版本迁移测试。回归：`components/checkbox-group/test/semantics.rs::checkbox_group_hyper_structure_builder_spec_is_not_applicable_for_simple_component`；脚本门禁：`scripts/check-ui-components-component-files.sh` 新增 `cargo test -p ui-components --test checkbox_group_semantics --no-default-features --features component-checkbox_group,inject-css checkbox_group_hyper_structure_builder_spec_is_not_applicable_for_simple_component`。）
- [x] 上下文压缩协议（Manifest + RBI）：新增/大改组件必须同步维护组件目录下 `Component.toml`（能力清单）和 `.rbi`（接口签名投影），避免 AI 检索工具箱过时。（`components/checkbox-group/src/Component.toml` 与 `components/checkbox-group/src/checkbox_group.rbi` 已同步存在并保持关键输入签名对齐（`id/label/description/error/is_invalid/is_required/is_disabled/motion/lang/dir/aria_describedby/class_name`）；`Component.toml` 继续声明能力依赖（`ui-headless/ui-state-primitives/ui-motion/ui-theme`）用于 Agent 上下文压缩检索，`checkbox_group.rbi` 提供 `pub fn CheckboxGroup(...)` 的稳定接口投影，避免工具箱语义漂移。回归：`components/checkbox-group/test/semantics.rs::checkbox_group_context_compression_manifest_and_rbi_projection_are_present_and_current`；脚本门禁：`scripts/check-ui-components-component-files.sh` 新增 `cargo test -p ui-components --test checkbox_group_semantics --no-default-features --features component-checkbox_group,inject-css checkbox_group_context_compression_manifest_and_rbi_projection_are_present_and_current`。）
- [x] 语义标记统一升级为 Agent Contract（Schema 化），让 Agent 不依赖 DOM 猜测理解组件状态与意图。（`components/checkbox-group/src/logic.rs` 新增类型化 Agent Contract（`CHECKBOX_GROUP_AGENT_SCHEMA` + `CheckboxGroupAgent{SchemaVersion,Intent,Action,State,Source,ConfigPolicy}` + `resolve_checkbox_group_agent_contract`），`components/checkbox-group/src/view.rs` 根节点挂载 `data-ui-schema/data-ui-schema-version/data-ui-intent/data-ui-action/data-ui-state/data-ui-source/data-ui-state-source/data-ui-config-policy`，字段全部来自枚举 `as_data_attr()`，不使用自由 `format!` 拼接；现有 `data-state-source/data-motion-phase` 继续提供状态来源可追溯。`components/checkbox-group/src/Component.toml` 同步声明 `[[agent_contract_markers]]` 与 `[[agent_contract_whitelist]]`（`blocked = ["inner_html", "<script", "javascript:"]`）以锁定白名单渲染边界。回归：`components/checkbox-group/test/semantics.rs::checkbox_group_agent_contract_is_schema_typed_traceable_and_whitelist_rendered`；脚本门禁：`scripts/check-ui-components-contract-hygiene.sh` 新增 `cargo test -p ui-components --test checkbox_group_semantics --no-default-features --features component-checkbox_group,inject-css checkbox_group_agent_contract_is_schema_typed_traceable_and_whitelist_rendered`。）
  - 关键交互组件必须输出稳定机器可读语义（至少 `data-*` + 状态来源标记；复杂组件建议补 `data-ui-schema`）。
  - Agent 消费字段应来自类型化 schema 生成，不允许散落字符串拼接。
  - 契约字段需可追溯到组件状态轴与动作语义（intent/action/state/source）。
  - 配置到组件的渲染链路必须走白名单能力边界，禁止任意脚本注入。
- [x] 流式在这里仅指 LLM 输出渲染（只看两种显示模式）。（`checkbox-group` 不是 LLM 正文阅读面，当前保持 snapshot-only 渲染路径；本组件不承载 token 增量传输协议。`Streaming`/`Snapshot` 在本条仅作为术语边界：`Streaming` 表示 LLM 生成中增量显示，`Snapshot` 表示生成完成后一次性显示。回归：`components/checkbox-group/test/semantics.rs::checkbox_group_check2_documents_streaming_definition_is_llm_output_only_with_two_modes`（同时断言组件与 docs 不出现 `stream_chunk/token_delta/data-ui-stream-mode` 等协议字段）；脚本门禁：`scripts/check-ui-components-streaming.sh` 新增 `cargo test -p ui-components --test checkbox_group_semantics --no-default-features --features component-checkbox_group,inject-css checkbox_group_check2_documents_streaming_definition_is_llm_output_only_with_two_modes`。）
  - `Streaming`：LLM 还在生成，界面边生成边显示。
  - `Snapshot`：LLM 全部生成完成后，一次性显示。
- [x] `Snapshot` 是所有组件的基础能力（默认必须支持）。（`CheckboxGroup` 默认消费完整配置快照输入（`id/label/description/error/is_invalid/is_required/is_disabled/motion/lang/dir/aria_describedby/class_name + children`）并通过 `logic::resolve_checkbox_group_content` + `logic::resolve_checkbox_group_view_state` 稳定渲染语义标记；组件不依赖 token 增量协议。`components/checkbox-group/src/Component.toml` 新增 `[[capabilities]] name = "snapshot_rendering" enabled = true` 固化基础能力，`components/checkbox-group/src/checkbox_group.rbi` 维持完整签名投影。回归：`components/checkbox-group/test/semantics.rs::checkbox_group_check2_documents_snapshot_as_default_baseline_capability`、`components/checkbox-group/test/semantics.rs::checkbox_group_snapshot_baseline_consumes_complete_result_and_renders_stably`；脚本门禁：`scripts/check-ui-components-streaming.sh` 新增 `cargo test -p ui-components --test checkbox_group_semantics --no-default-features --features component-checkbox_group,inject-css checkbox_group_check2_documents_snapshot_as_default_baseline_capability` 与 `cargo test -p ui-components --test checkbox_group_semantics --no-default-features --features component-checkbox_group,inject-css checkbox_group_snapshot_baseline_consumes_complete_result_and_renders_stably`。）
  - 所有组件都应能消费“完整生成结果”并稳定渲染。
  - 即使组件不直接展示正文，也应能在接收上层完整配置后正常渲染。
- [x] `Streaming` 是否强制，按组件职责判断（不能一刀切）。（`checkbox-group` 不是正文阅读面，归类为 `Streaming Optional`，以 `Snapshot` 为默认渲染基线并固定 `fallback=snapshot`。`components/checkbox-group/src/logic.rs` 新增类型化流式治理枚举（`CheckboxGroupAgentStreamSupport/CheckboxGroupAgentStreamFallback/CheckboxGroupAgentOutputStatus`），并在 `resolve_checkbox_group_agent_contract` 固定输出 `Optional + Snapshot + Verified`；`components/checkbox-group/src/view.rs` 根节点挂载 `data-ui-stream-support/data-ui-stream-fallback/data-ui-output-status`，保持 `aria-*` 与 `data-*` 连续可读；`components/checkbox-group/src/Component.toml` 同步声明 `stream_support/stream_fallback/output_status` 标记封闭值集合。数据校验、断线恢复、重试策略保持上层负责，组件层不实现重试协议。回归：`components/checkbox-group/test/semantics.rs::checkbox_group_check2_marks_streaming_scope_as_optional_with_snapshot_fallback`；脚本门禁：`scripts/check-ui-components-streaming.sh` 新增 `cargo test -p ui-components --test checkbox_group_semantics --no-default-features --features component-checkbox_group,inject-css checkbox_group_check2_marks_streaming_scope_as_optional_with_snapshot_fallback`。）
  - `Streaming Required`：组件本体就是正文阅读面，用户需要边生成边看。
  - `Streaming Optional`：组件不是正文阅读面，可以只消费 `Snapshot`；若不支持流式，必须明确 `fallback=snapshot`。
  - 无论是否支持 `Streaming`，都要显式标识当前输出状态（草稿/已验证/可提交），并保持 `role`/`aria-*`/`data-*` 连续可读。
  - 数据校验、断线恢复、重试策略由上层负责，组件层只负责稳定渲染。

### 7. 测试、门禁与交付
- [x] 代码卫生（Rust Hygiene）：非测试代码中完全禁止 `unwrap/expect`，禁止无处理的 `let _ = ...`；字符串复制热点收敛为 `Cow<'static, str>`（执行 `./scripts/check-rust-hygiene.sh` 验证）。（`components/checkbox-group/src/logic.rs::resolve_checkbox_group_class_name` 已引入 `Vec<Cow<'static, str>>` 收敛 class 拼接字符串复制热点；组件非测试源码（`mod.rs/logic.rs/styles.rs/view.rs/motion.rs`）维持无 `unwrap/expect` 与无吞错 `let _ = ...`。回归：`components/checkbox-group/test/semantics.rs::checkbox_group_rust_hygiene_contract_forbids_unwrap_expect_and_let_underscore_in_non_test_sources`、`components/checkbox-group/test/semantics.rs::checkbox_group_rust_hygiene_string_clone_hotspots_converge_to_cow_or_are_absent`、`components/checkbox-group/test/semantics.rs::checkbox_group_rust_hygiene_script_enforces_repo_level_hygiene_guards`；脚本门禁：`scripts/check-ui-components-engineering.sh` 新增对应 `cargo test` 目标。验证：`RUST_HYGIENE_FILES=\"components/checkbox-group/src/mod.rs components/checkbox-group/src/logic.rs components/checkbox-group/src/styles.rs components/checkbox-group/src/view.rs components/checkbox-group/src/motion.rs\" ./scripts/check-rust-hygiene.sh`，输出 `[rust-hygiene] OK`（并提示当前环境 `ripgrep` 无 PCRE2，跳过 `check-api-contracts.sh`）。）
- [x] Tree Shaking & 特性剪裁：组件必须注册到 `ui-components` 特性树（如 `component-accordion`）；`css.rs` 和 `lib.rs` 聚合必须受 feature 门控，禁止无条件全局依赖。（`checkbox-group` 已注册到 `ui-components` 特性树：`crates/ui-components/Cargo.toml` 定义 `component-checkbox_group = ["component-checkbox", "dep:ui-checkbox-group"]` 且依赖 `ui-checkbox-group` 为 optional；`crates/ui-components/src/lib.rs` 与 `crates/ui-components/src/css.rs` 均通过 `#[cfg(feature = "component-checkbox_group")]` 做条件导出/样式聚合，不存在无条件全局注册。门禁脚本 `scripts/check-ui-components-tree-shaking.sh` 已新增 `checkbox-group` 专项：`cargo test -p ui-components --test checkbox_group_semantics --no-default-features --features component-checkbox_group,inject-css checkbox_group_tree_shaking_contract_is_feature_gated_and_budgeted`、`cargo test -p ui-components --test checkbox_group_semantics --no-default-features --features component-checkbox_group,inject-css checkbox_group_check2_marks_tree_shaking_feature_pruning_contract_complete`，并执行 `cargo tree -e features -i ui-components -p ui-components --no-default-features --features \"$CHECKBOX_GROUP_MIN_FEATURES\"` + wasm 最小特性编译检查，阻断 `all-components` 被误拉起。回归：`components/checkbox-group/test/semantics.rs::checkbox_group_tree_shaking_contract_is_feature_gated_and_budgeted`、`components/checkbox-group/test/semantics.rs::checkbox_group_check2_marks_tree_shaking_feature_pruning_contract_complete`。）
- [x] 语义测试与性能回归：断言必须覆盖 `aria-*`、`data-*` 与焦点流转，不能只看快照；高频/重型组件必须补齐 `render_count` 断言/测量（如初始化空闲预算为 1）。（`CheckboxGroup` 已补齐语义+性能回归闭环：`components/checkbox-group/src/view.rs` 稳定挂载 `aria-labelledby/aria-describedby/aria-invalid/aria-required` 与 `data-state-source/data-motion-source/data-motion-phase`，并由 `components/checkbox-group/test/semantics.rs::checkbox_group_view_mounts_headless_locale_contract`、`components/checkbox-group/test/semantics.rs::checkbox_group_emits_baseline_state_data_attributes` 覆盖。焦点流转在容器层按 `N/A` 处理（组件不持有局部焦点状态机，焦点由子 Checkbox 路径负责），通过 `components/checkbox-group/test/semantics.rs::checkbox_group_has_no_overlay_focus_stack_contract` 约束。性能链路继续由 `components/checkbox-group/test/semantics.rs::checkbox_group_performance_governance_contract_is_budgeted_traceable_and_blocking` 锁定预算/可观测/阻断，并新增综合矩阵 `components/checkbox-group/test/semantics.rs::checkbox_group_semantics_and_performance_regression_cover_aria_data_focus_and_render_count_measurement`。门禁脚本 `scripts/check-ui-components-performance.sh` 已加入 `cargo test -p ui-components --test checkbox_group_semantics --no-default-features --features component-checkbox_group,inject-css checkbox_group_semantics_and_performance_regression_cover_aria_data_focus_and_render_count_measurement`。`render_count` 精确计数当前 `N/A`，沿用 `docs/plan/TODO.md` 的统一 follow-up；当前环境运行 `cargo test` 仍可能报 `Invalid cross-device link (os error 18)`，故以脚本命令与契约测试作为可重复阻断证据。）
- [x] 版本弃用迁移（Codemod/Registry）：若提交包含跨大版本 API 破坏升级，必须在 Schema Registry 注册弃用窗口并提供纯函数迁移层（`migrate_v1_to_v2`）。（N/A：本次 `CheckboxGroup` 提交未引入跨大版本 API 破坏升级；组件契约保持 `v1`（`components/checkbox-group/src/logic.rs` 的 `CheckboxGroupAgentSchemaVersion::V1`，`components/checkbox-group/src/Component.toml` 的 `schema_version = "1"` 与 `ui.checkbox-group.agent-contract.v1`），因此不触发 Schema Registry 弃用窗口与 `migrate_v1_to_v2` 迁移层要求。兼容清理保持生效：`components/checkbox-group/test/semantics.rs::checkbox_group_breaking_migration_removes_legacy_namespace_and_path_shim` 持续禁止旧命名空间回流。回归：`components/checkbox-group/test/semantics.rs::checkbox_group_version_deprecation_migration_registry_is_explicitly_na_without_major_breaking_upgrade`、`components/checkbox-group/test/semantics.rs::checkbox_group_check2_marks_version_deprecation_migration_item_complete`；门禁脚本：`scripts/check-ui-components-engineering.sh` 新增 `cargo test -p ui-components --test checkbox_group_semantics --no-default-features --features component-checkbox_group,inject-css checkbox_group_version_deprecation_migration_registry_is_explicitly_na_without_major_breaking_upgrade`。当前环境执行 `cargo test` 可能仍受 `Invalid cross-device link (os error 18)` 阻断。）
- [x] 文档即产品（Copy-Paste Ready）：`apps/docs-app` 必须新增 Playground（Hello World、状态矩阵、受控/非受控对照），支持流式/快照展现，并提供 Source-first 一键复制且补全 imports。（`apps/docs-app/src/pages/components/pages/forms.rs::checkbox_group` 已提供 `Hello World（默认路径）`、`Validation + Required`、`Disabled + Optional` 三个 Playground，覆盖基础路径与状态矩阵；受控/非受控在组级按 `N/A` 明示（`data-slot=\"checkbox-group-controlled-uncontrolled-na\"`：`CheckboxGroup` 不持有 group value 轴，选中态由子 `Checkbox` 管理）。文档页新增 `Streaming / Snapshot`（`data-slot=\"checkbox-group-streaming-policy\"`）与 `Source-first / Copy-ready`（`data-slot=\"checkbox-group-copy-ready\"`）分区，明确 `Streaming Optional; fallback=snapshot` 与源码路径/依赖前提。复制链路由 `apps/docs-app/src/playground.rs::compose_copy_ready_code` 统一补齐 imports（`use leptos::prelude::*; use ui_components::*;`），且 `checkbox_group` 各 Playground 显式设置 `code_imports`。回归：`components/checkbox-group/test/semantics.rs::checkbox_group_docs_are_copy_paste_ready_with_hello_world_state_matrix_and_streaming_snapshot`、`components/checkbox-group/test/semantics.rs::checkbox_group_dx_check_script_covers_documentation_as_product_contract`、`components/checkbox-group/test/semantics.rs::checkbox_group_check2_marks_documentation_as_product_item_complete`；门禁脚本：`scripts/check-ui-components-dx.sh` 新增 `cargo test -p ui-components --test checkbox_group_semantics --no-default-features --features component-checkbox_group,inject-css checkbox_group_docs_are_copy_paste_ready_with_hello_world_state_matrix_and_streaming_snapshot`。当前环境执行 `cargo test` 可能仍受 `Invalid cross-device link (os error 18)` 阻断。）
- [x] 语义测试优先：验证 `data-*` / `aria-*` / role / 状态来源契约，不只视觉快照。（`CheckboxGroup` 已以语义契约为主进行回归：`components/checkbox-group/src/view.rs` 稳定挂载 `aria-labelledby/aria-describedby/aria-invalid/aria-required` 与 `data-state-source/data-motion-source/data-motion-phase/data-ui-source`，并由 `components/checkbox-group/test/semantics.rs::checkbox_group_view_mounts_headless_locale_contract`、`components/checkbox-group/test/semantics.rs::checkbox_group_emits_baseline_state_data_attributes`、`components/checkbox-group/test/semantics.rs::checkbox_group_state_markers_use_closed_enums_for_source_and_phase` 覆盖。`role` 在该组件按 `N/A` 处理：根节点采用原生 `<fieldset>` 语义而非手工 `role` 覆盖，避免重复声明与语义漂移。新增优先级回归：`components/checkbox-group/test/semantics.rs::checkbox_group_semantic_test_priority_prefers_data_aria_role_and_source_contracts_over_snapshot_only_checks`，显式禁止 snapshot-only 断言并锁定语义字段新增必须同步补测。门禁脚本：`scripts/check-ui-components-performance.sh` 新增 `cargo test -p ui-components --test checkbox_group_semantics --no-default-features --features component-checkbox_group,inject-css checkbox_group_semantic_test_priority_prefers_data_aria_role_and_source_contracts_over_snapshot_only_checks`。当前环境执行 `cargo test` 可能仍受 `Invalid cross-device link (os error 18)` 阻断。）
  - 每个交互组件至少有对应 `*_semantics.rs` 测试覆盖关键状态轴与动作语义。
  - 断言应聚焦语义契约（状态来源/可访问性/键盘路径），快照仅作补充。
  - 新增/变更语义字段必须同步补测试，否则不得打勾。
- [x] E2E 选择器稳定：使用语义标记，WASM 场景有稳定等待策略。（已补 `e2e/tests/docs_app_checkbox_group_contract.spec.mjs`：选择器仅使用稳定语义锚点 `#docs-checkbox-group[data-slot="checkbox-group"]` + `[data-slot="checkbox-group-list"] [data-slot="checkbox"][role="checkbox"]`，不依赖文本或脆弱层级；WASM 等待统一 `body:not(:has(#boot))`，无固定 sleep。交互路径显式覆盖 ready/settled 断点：初始 `data-valid/data-motion-phase=inactive`，切换后断言 `data-invalid/data-shows-error/data-ui-action=render-semantic-with-error/data-ui-state=enabled-invalid/data-motion-phase=active`，再收敛回 `render-semantic + enabled-valid + inactive`。回归：`components/checkbox-group/test/semantics.rs::checkbox_group_check2_documents_e2e_selector_and_stable_wait_rules`、`components/checkbox-group/test/semantics.rs::checkbox_group_e2e_selector_contract_uses_semantic_markers_and_settled_waits`、`components/checkbox-group/test/semantics.rs::checkbox_group_e2e_contract_covers_ready_and_settled_conditions_for_checkbox_group_paths`、`components/checkbox-group/test/semantics.rs::checkbox_group_e2e_check_script_covers_selector_and_settled_wait_contract`；门禁脚本：`scripts/check-ui-components-e2e-checkbox-group.sh`。当前环境执行 `cargo test` 仍可能受 `Invalid cross-device link (os error 18)` 阻断。）
  - E2E 选择器优先 `data-*` 语义标记，禁止依赖脆弱 DOM 层级或文本定位。
  - WASM 场景必须使用稳定等待策略（语义状态就绪而非固定 sleep）。
  - 若组件涉及异步/动画，E2E 需显式覆盖 ready/settled 条件。
- [x] 关键流程纳入可重复回归集合（Playwright/Cypress）。（已补可重复关键流程：`e2e/tests/docs_app_checkbox_group_contract.spec.mjs::docs-app checkbox-group key flow is repeatable and failures map to semantic breakpoints`，在同一路径中执行两轮 `focus -> keyboard(Space) -> invalid/valid 语义断点 -> reload 后重放`；失败可定位到 `data-valid/data-invalid/data-shows-error/data-ui-action/data-ui-state/data-motion-phase` 等稳定语义契约，而非笼统“页面不一致”。高风险路径已覆盖 `focus + keyboard + disabled`：`e2e/tests/docs_app_checkbox_group_contract.spec.mjs::docs-app checkbox-group high-risk paths cover focus keyboard and disabled semantic breakpoints`，并校验 `#docs-checkbox-group-disabled[data-slot="checkbox-group"]` 下 `aria-disabled` 与 disabled no-op 收敛。`overlay/async` 在 `CheckboxGroup` 组件范围为 `N/A`（非 overlay 组件、无异步协议）。回归：`components/checkbox-group/test/semantics.rs::checkbox_group_check2_documents_e2e_repeatable_key_flow_rules`、`components/checkbox-group/test/semantics.rs::checkbox_group_e2e_key_flow_is_repeatable_and_failure_points_are_semantic`、`components/checkbox-group/test/semantics.rs::checkbox_group_e2e_high_risk_paths_cover_focus_keyboard_and_settled_semantic_breakpoints`、`components/checkbox-group/test/semantics.rs::checkbox_group_e2e_check_script_covers_repeatable_key_flow_contracts`；门禁脚本：`scripts/check-ui-components-e2e-checkbox-group.sh`。当前环境执行 `cargo test` 仍可能受 `Invalid cross-device link (os error 18)` 阻断。）
  - 至少定义一条可重复关键流程（打开/交互/关闭或提交）纳入 E2E 回归。
  - 回归失败需可定位到具体语义契约断点，而不是笼统“页面不一致”。
  - 高风险路径（overlay、focus、keyboard、async）优先进入回归集合。
- [x] docs-app 文档、示例、参数矩阵、状态矩阵同步更新。（`apps/docs-app/src/pages/components/pages/forms.rs::checkbox_group` 已同步维护组件文档与示例：`Hello World（默认路径）`、`Interactive Playground`、`Validation + Required`、`Disabled + Optional`，覆盖状态矩阵（invalid/required/disabled + 组级 controlled/uncontrolled `N/A` 说明 `data-slot="checkbox-group-controlled-uncontrolled-na"`）。文档 API 与默认值路径对齐当前实现：`components/checkbox-group/src/view.rs` 使用 `is_invalid/is_required/is_disabled/aria_describedby` 与 optional props，`components/checkbox-group/src/logic.rs` 统一归一 `resolve_checkbox_group_content`（`normalize_checkbox_group_label/normalize_checkbox_group_optional_text`）与 `resolve_checkbox_group_class_name`，并通过 `CheckboxGroupOptions` 固定语义输入轴。回归：`components/checkbox-group/test/semantics.rs::checkbox_group_check2_documents_docs_sync_and_state_matrix_rules`、`components/checkbox-group/test/semantics.rs::checkbox_group_docs_examples_and_state_matrix_sync_with_logic_api_names_and_defaults`、`components/checkbox-group/test/semantics.rs::checkbox_group_dx_check_script_covers_docs_sync_and_state_matrix_contract`、`components/checkbox-group/test/semantics.rs::checkbox_group_check2_marks_docs_sync_and_state_matrix_item_complete`；门禁脚本：`scripts/check-ui-components-dx.sh` 新增 `cargo test -p ui-components --test checkbox_group_semantics --no-default-features --features component-checkbox_group,inject-css checkbox_group_check2_documents_docs_sync_and_state_matrix_rules` 与 `cargo test -p ui-components --test checkbox_group_semantics --no-default-features --features component-checkbox_group,inject-css checkbox_group_docs_examples_and_state_matrix_sync_with_logic_api_names_and_defaults`。当前环境执行 `cargo test` 仍可能受 `Invalid cross-device link (os error 18)` 阻断。）
  - 组件行为或参数变更必须同步更新 `apps/docs-app` 示例与说明。
  - 文档示例需覆盖至少一组状态矩阵（受控/非受控、disabled、size/variant 等）。
  - 文档中的 API 名称与默认值必须和 `logic.rs` 当前实现一致。
- [x] 组件文档必须对新手友好（Documentation as Product）：组件 README 或等价文档入口必须存在。（`components/checkbox-group/src/README.md` 已补齐新手优先文档结构：`Hello World（最小可用）`、`常见用法`、`先用起来，再进阶`，并在内容中明确默认路径与进阶路径（`<CheckboxGroup id=... label=...>` -> `is_required/is_invalid/is_disabled/aria_describedby/description/error/motion/class_name`）。等价文档入口保持可索引：`apps/docs-app/src/pages/components/pages/forms.rs::checkbox_group`（`title=\"CheckboxGroup\"`、`slug=\"checkbox-group\"`、`Hello World（默认路径）`、`Interactive Playground`、`Validation + Required`、`Disabled + Optional`）。回归：`components/checkbox-group/test/semantics.rs::checkbox_group_check2_documents_documentation_as_product_rules`、`components/checkbox-group/test/semantics.rs::checkbox_group_documentation_entry_exists_with_beginner_first_progression`、`components/checkbox-group/test/semantics.rs::checkbox_group_dx_check_script_covers_documentation_as_product_beginner_contract`、`components/checkbox-group/test/semantics.rs::checkbox_group_check2_marks_documentation_as_product_beginner_item_complete`；镜像执行入口：`components/checkbox-group/test/checkbox_group_semantics.rs::checkbox_group_check2_documents_documentation_as_product_rules`、`components/checkbox-group/test/checkbox_group_semantics.rs::checkbox_group_documentation_entry_exists_with_beginner_first_progression`、`components/checkbox-group/test/checkbox_group_semantics.rs::checkbox_group_dx_check_script_covers_documentation_as_product_beginner_contract`。门禁脚本：`scripts/check-ui-components-dx.sh` 已新增对应 cargo test。当前环境执行 `cargo test` 仍可能受 `Invalid cross-device link (os error 18)` 阻断。）
  - 每个基础组件必须提供“零门槛”最小示例（Hello World）与常见用法，避免要求用户先理解底层分层架构。
  - 文档需明确“先用起来，再进阶”：默认 API 路径在前，高级控制参数在后。
  - “只有源码没有文档”或“只写给架构师/机器看的文档”视为不通过。
- [x] `apps/docs-app` 必须提供 Interactive Playground：用户可在线修改 props/状态并实时预览。（已满足：`apps/docs-app/src/pages/components/pages/forms.rs::checkbox_group` 提供 `title="Interactive Playground"`，并在同一验收面覆盖基础 props/状态调整与实时反馈：`is_required/is_invalid/is_disabled` 开关、`description/error` 显隐、`selected count` 回显；同时挂载 `test_css_source=interactive_test_css`、`test_config_signal=interactive_config`、`controls=move || view!`，可直接观察配置变更到渲染输出。交互画布能力复用 `apps/docs-app/src/playground.rs` 的 `data-playground-scope` 与 `playground__preview-stage` 契约。可重复关键路径由 `e2e/tests/docs_app_checkbox_group_contract.spec.mjs::docs-app checkbox-group key flow is repeatable and failures map to semantic breakpoints` 覆盖（focus -> keyboard Space -> data-invalid/data-ui-state 断点 -> reload 重放）。AI Spec 相关联动示例：N/A（`checkbox-group` 非 Spec 构建器组件）。回归：`components/checkbox-group/test/semantics.rs::checkbox_group_check2_documents_interactive_playground_rules`、`components/checkbox-group/test/semantics.rs::checkbox_group_docs_app_provides_interactive_playground_for_props_state_and_preview`、`components/checkbox-group/test/semantics.rs::checkbox_group_interactive_playground_reuses_repeatable_semantic_e2e_flow`、`components/checkbox-group/test/semantics.rs::checkbox_group_dx_check_script_covers_interactive_playground_contract`、`components/checkbox-group/test/semantics.rs::checkbox_group_check2_marks_interactive_playground_contract_complete`；镜像执行入口：`components/checkbox-group/test/checkbox_group_semantics.rs::checkbox_group_check2_documents_interactive_playground_rules`、`components/checkbox-group/test/checkbox_group_semantics.rs::checkbox_group_docs_app_provides_interactive_playground_for_props_state_and_preview`、`components/checkbox-group/test/checkbox_group_semantics.rs::checkbox_group_interactive_playground_reuses_repeatable_semantic_e2e_flow`、`components/checkbox-group/test/checkbox_group_semantics.rs::checkbox_group_dx_check_script_covers_interactive_playground_contract`、`components/checkbox-group/test/checkbox_group_semantics.rs::checkbox_group_check2_marks_interactive_playground_contract_complete`；门禁脚本：`scripts/check-ui-components-dx.sh`。当前环境执行 cargo 仍可能受 `Invalid cross-device link (os error 18)` 影响。）
  - Playground 至少支持基础 props 调整、状态切换、交互反馈观察。
  - 对 AI Spec 相关组件，至少提供一组 Spec 输入与预览输出的联动示例。
  - Playground 作为验收面，需可重复复现关键交互路径。
- [x] Source-first 文档必须 Copy-Paste Ready：提供一键复制组件源码或最小可用片段能力。（已满足：`apps/docs-app/src/pages/components/pages/forms.rs::checkbox_group` 的 `Source-first / Copy-ready` 区块已给出复制能力与真实路径（`data-slot="checkbox-group-copy-ready|checkbox-group-source-paths|checkbox-group-source-prerequisites"`），并声明依赖前提 `component-checkbox_group` + `inject-css`。复制链路由 `apps/docs-app/src/playground.rs::compose_copy_ready_code` + `DEFAULT_PLAYGROUND_IMPORTS` + `code_imports` 提供，Playground 代码块保持 `data-copyable`，输出默认包含 `use leptos::prelude::*; use ui_components::*;` 可直接运行。文档与实现同步由 `code_imports` 显式绑定当前 `forms.rs` 示例。回归：`components/checkbox-group/test/semantics.rs::checkbox_group_check2_documents_source_first_copy_paste_ready_rules`、`components/checkbox-group/test/semantics.rs::checkbox_group_docs_source_first_copy_paste_ready_with_real_paths_and_dependencies`、`components/checkbox-group/test/semantics.rs::checkbox_group_dx_check_script_covers_source_first_copy_paste_ready_contract`、`components/checkbox-group/test/semantics.rs::checkbox_group_check2_marks_source_first_copy_paste_ready_contract_complete`；镜像执行入口：`components/checkbox-group/test/checkbox_group_semantics.rs::checkbox_group_check2_documents_source_first_copy_paste_ready_rules`、`components/checkbox-group/test/checkbox_group_semantics.rs::checkbox_group_docs_source_first_copy_paste_ready_with_real_paths_and_dependencies`、`components/checkbox-group/test/checkbox_group_semantics.rs::checkbox_group_dx_check_script_covers_source_first_copy_paste_ready_contract`。门禁脚本：`scripts/check-ui-components-dx.sh`。当前环境执行 cargo 仍可能受 `Invalid cross-device link (os error 18)` 影响。）
  - docs-app 页面应提供复制按钮，输出代码默认可直接运行（含必要 imports/依赖提示）。
  - 若为 source-first 组件，文档需指向真实源码落点并说明依赖前提，避免“复制即报错”。
  - 文档代码与当前实现必须同步，防止示例漂移。
- [x] HeroUI 对标文档与组件文档同步：参数模型变更需同步 `docs/spec/heroui-parameter-design-strategy.md`（必要时补充 `docs/research/spectrum-heroui-style-interface-study.md`），并保证组件文档可访问。（已满足：`docs/spec/heroui-parameter-design-strategy.md` 已新增 `### CheckboxGroup 同步记录（2026-02-20）`，明确参数主轴 `is_required/is_invalid/is_disabled`、docs 索引入口 `component_doc!("CheckboxGroup", "checkbox-group", "Forms", forms::checkbox_group)` 与文档可访问性约束。`apps/docs-app/src/pages/components/pages/forms.rs::checkbox_group()` 保持 `title="CheckboxGroup"` / `slug="checkbox-group"`，`apps/docs-app/src/pages/components/pages.rs` 可索引到该入口；`components/checkbox-group/src/README.md` 已补 `## docs-app 入口`（`forms.rs::checkbox_group()`、`#/components/checkbox-group`）。本轮判定不需要追加 `docs/research/spectrum-heroui-style-interface-study.md`（未引入新的对标结论）。回归：`components/checkbox-group/test/semantics.rs::checkbox_group_check2_documents_heroui_benchmark_docs_sync_rules`、`components/checkbox-group/test/semantics.rs::checkbox_group_heroui_strategy_and_component_docs_are_synchronized_and_indexable`、`components/checkbox-group/test/semantics.rs::checkbox_group_dx_check_script_covers_heroui_benchmark_docs_sync_contract`、`components/checkbox-group/test/semantics.rs::checkbox_group_check2_marks_heroui_benchmark_docs_sync_contract_complete`、`components/checkbox-group/test/checkbox_group_semantics.rs::checkbox_group_check2_documents_heroui_benchmark_docs_sync_rules`、`components/checkbox-group/test/checkbox_group_semantics.rs::checkbox_group_heroui_strategy_and_component_docs_are_synchronized_and_indexable`、`components/checkbox-group/test/checkbox_group_semantics.rs::checkbox_group_dx_check_script_covers_heroui_benchmark_docs_sync_contract`、`components/checkbox-group/test/checkbox_group_semantics.rs::checkbox_group_check2_marks_heroui_benchmark_docs_sync_contract_complete`；门禁脚本：`scripts/check-ui-components-dx.sh`。当前环境执行 cargo 仍可能受 `Invalid cross-device link (os error 18)` 影响。）
  - 若参数语义发生变化，需同步更新对标策略文档，不允许实现先漂移文档后补。
  - 组件文档入口必须存在（docs-app 页面或等价文档），且可被索引定位。
  - “仅代码更新无文档更新”在接口变更场景下直接判不通过。

### 8. 合并前门禁死命令（最终执行）
在发起 PR 或完成任务前，必须保证本地/CI 以下命令全部通过：

- `cargo fmt --all -- --check`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `./scripts/check-rust-hygiene.sh`
- `cargo check -p ui-components --target wasm32-unknown-unknown`
- `cargo check -p ui-headless --no-default-features --features ssr`
- `cargo check -p ui-components --target wasm32-unknown-unknown --no-default-features --features component-<your_component>,inject-css`

依据文档（`rust-ui/docs/spec` 及 `rust-ui/docs`）：

- `rust-ui/docs/spec/ai_context_projection_protocol.md`
- `rust-ui/docs/spec/architectural_fitness_functions.md`
- `rust-ui/docs/spec/async_state_as_data_command.md`
- `rust-ui/docs/spec/collection_registration_protocol.md`
- `rust-ui/docs/spec/compile_time_evolution_migration.md`
- `rust-ui/docs/spec/component_boundaries.md`
- `rust-ui/docs/spec/component_domains.md`
- `rust-ui/docs/spec/controlled_evolution_sandbox.md`
- `rust-ui/docs/spec/core_shell_protocol_infra_baseline.md`
- `rust-ui/docs/spec/environment_subscription_streams.md`
- `rust-ui/docs/spec/event_light_cone_signal_bus.md`
- `rust-ui/docs/spec/focus_global_stack_gc.md`
- `rust-ui/docs/spec/foreign_zone_escape_hatches.md`
- `rust-ui/docs/spec/headless_purification.md`
- `rust-ui/docs/spec/heroui-parameter-design-strategy.md`
- `rust-ui/docs/spec/hyper-structure-ui-development-playbook.md`
- `rust-ui/docs/spec/i18n.md`
- `rust-ui/docs/spec/intent_stack_semantic_layering.md`
- `rust-ui/docs/spec/kernel_shell_architecture.md`
- `rust-ui/docs/spec/macro_micro_dual_state_machine.md`
- `rust-ui/docs/spec/motion.md`
- `rust-ui/docs/spec/mvp.md`
- `rust-ui/docs/spec/platform_abdication_ecosystem.md`
- `rust-ui/docs/spec/README.md`
- `rust-ui/docs/spec/release_versioning.md`
- `rust-ui/docs/spec/side_effect_command_pattern.md`
- `rust-ui/docs/spec/slot_projection_strategy.md`
- `rust-ui/docs/spec/ssr_hydration_discontinuity.md`
- `rust-ui/docs/spec/state_primitives_core_satellite_split.md`
- `rust-ui/docs/spec/style_island_defense.md`
- `rust-ui/docs/spec/styling.md`
- `rust-ui/docs/spec/tree_shaking.md`
- `rust-ui/docs/spec/ui_layout_split.md`
- `rust-ui/docs/spec/ui_physics_two_pass_rendering.md`
- `rust-ui/docs/spec/unified_causality_bus.md`
- `rust-ui/docs/spec/wasm_generic_bloat.md`
- `rust-ui/docs/起点_也即是目的.md`
- `rust-ui/docs/DOCS_GOVERNANCE.md`
- `rust-ui/docs/DOCS_INDEX.md`
- `rust-ui/docs/philosophy.md`
- `rust-ui/docs/README.md`
- `rust-ui/docs/RULES_ZH.md`
