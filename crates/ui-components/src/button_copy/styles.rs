pub const CSS: &str = r#"
.ui-button-copy {
  display: inline-flex;
  align-items: center;
  gap: var(--ui-space-xs);
}

.ui-button-copy__a11y-status {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border: 0;
}
"#;
